---
phase: 03-interc-mbio-de-modalidade-de-cardio
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - src/components/session/SkipReasonSheet.tsx
  - src/screens/ActiveSessionScreen.tsx
  - __tests__/skipReasonSheetTroca.test.tsx
  - __tests__/activeSessionScreen.test.tsx
autonomous: true
requirements: [REQ-06]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SkipReasonSheet, quando o motivo selecionado é sem_equipamento E o exercício é de cardio, oferece 'Trocar modalidade' como alternativa a 'Recusar mesmo assim' — evolui o fluxo de recusa declarada para substituição, per ROADMAP Fase 3."
    - "Escolher 'Trocar modalidade' NUNCA chama skip_session_exercise/skipExercise — é um caminho inteiramente novo que reusa o mesmo SwapModalitySheet do Plano 03-03, nunca o fluxo de recusa."
    - "Todo o comportamento pré-existente de SkipReasonSheet para os outros 5 motivos, e para sem_equipamento em exercício NÃO-cardio, permanece bit-a-bit idêntico — __tests__/recusaDeclarada.test.ts e __tests__/recusaDeclaradaFluxo.test.ts passam sem modificação."
  artifacts: []
  key_links:
    - "SkipReasonSheet (reason === 'sem_equipamento' && ehCardio) -> onSolicitarTroca -> ActiveSessionScreen (fecha recusa, abre troca) -> SwapModalitySheet (Plano 03-03) -> activeSessionStore.swapExercise (Plano 03-02)."
  prohibitions:
    - "onSolicitarTroca nunca chama onConfirm/skipExercise — o caminho de troca a partir do SkipReasonSheet é aditivo e não reduz nem reinterpreta o caminho de recusa existente."
---

<objective>
Entry point 2 do REQ-06 (ROADMAP: "evolui o fluxo de recusa declarada... para substituição"):
ao selecionar o motivo `sem_equipamento` num exercício de cardio, `SkipReasonSheet` oferece
"Trocar modalidade" como alternativa a "Recusar mesmo assim", reusando o MESMO
`SwapModalitySheet`/`swapExercise` provados nos Planos 03-02/03-03 — nunca uma implementação
paralela.

Purpose: RESEARCH.md (Pattern 2) mapeia este SEGUNDO sinal exigido pelo ROADMAP de forma
explícita: os dois entry points (fila e recusa por falta de equipamento) precisam levar ao
MESMO seletor de modalidade (Open Question 2, resolvida a favor de compartilhar — Pitfall 5
já mostra o custo de duplicar lógica de cardio neste repo). Este plano é aditivo: o caminho
"Recusar mesmo assim" continua idêntico ao "Não vou fazer" de hoje, testado pelas suítes já
existentes de recusa declarada.
Output: `SkipReasonSheet.tsx` com o ramo condicional novo, `ActiveSessionScreen.tsx` roteando
para o `SwapModalitySheet` já existente.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-CONTEXT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-RESEARCH.md
@src/components/session/SkipReasonSheet.tsx
@src/screens/ActiveSessionScreen.tsx
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-03-PLAN.md
@__tests__/recusaDeclaradaFluxo.test.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `SkipReasonSheet.tsx`: props `ehCardio`/`onSolicitarTroca` (novas), botão condicional "Trocar modalidade" (novo)
- `ActiveSessionScreen.tsx`: roteamento de `SkipReasonSheet.onSolicitarTroca` para o estado `troca`/`SwapModalitySheet` já existente
- `__tests__/skipReasonSheetTroca.test.tsx` (novo)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SkipReasonSheet — ramo condicional sem_equipamento + cardio</name>
  <files>src/components/session/SkipReasonSheet.tsx, __tests__/skipReasonSheetTroca.test.tsx</files>
  <read_first>
    src/components/session/SkipReasonSheet.tsx (INTEIRO, já em contexto — `Props` linhas 26-38,
    render do `ScrollView` de motivos linhas 90-124, o `Button` principal linhas 126-136 com
    `label={ehSessao ? 'Recusar o treino' : 'Não vou fazer'}` e `onPress` chamando
    `onConfirm(reason, note...)`); __tests__/recusaDeclaradaFluxo.test.ts linhas 249-277 (o
    modo de falha 4 já cobre `reason: 'sem_equipamento'` chegando a `skipSessionExercise` sem
    mudança de contrato — ESTE teste NÃO deve precisar de nenhuma edição depois desta task,
    porque exercita o STORE diretamente, não o componente); 03-RESEARCH.md seção "Pattern 2"
    (o exemplo exato de UI recomendado — leia antes de implementar).
  </read_first>
  <behavior>
    - Teste 1 (não-cardio, sem_equipamento): `escopo="exercicio"`, `ehCardio={false}`,
      `reason` selecionado `'sem_equipamento'` -> NENHUM botão "Trocar modalidade" aparece;
      o botão principal continua com o rótulo "Não vou fazer" (comportamento IDÊNTICO ao de
      hoje).
    - Teste 2 (cardio, outro motivo): `ehCardio={true}`, `reason` selecionado `'cansaco'` ->
      NENHUM botão "Trocar modalidade" aparece (o ramo é específico de `sem_equipamento`).
    - Teste 3 (cardio + sem_equipamento, o caso novo): `ehCardio={true}`, `reason` selecionado
      `'sem_equipamento'` -> botão `testID="skip-reason-oferecer-troca"` aparece, E o botão
      principal (`testID="skip-reason-confirm"`) muda o rótulo para "Recusar mesmo assim".
    - Teste 4 (troca não confirma recusa): no cenário do Teste 3, pressionar
      `skip-reason-oferecer-troca` chama `onSolicitarTroca` — E `onConfirm` NÃO é chamado
      (a troca nunca invoca o caminho de recusa).
    - Teste 5 (recusar mesmo assim continua funcionando): no cenário do Teste 3, pressionar
      `skip-reason-confirm` chama `onConfirm('sem_equipamento', <nota>)` normalmente — o
      caminho antigo sobrevive intocado.
    - Teste 6 (escopo sessão nunca oferece troca): `escopo="sessao"`, `ehCardio={true}`,
      `reason` `'sem_equipamento'` -> NENHUM botão "Trocar modalidade" (não existe um único
      exercício de cardio para trocar quando a recusa é da sessão inteira).
  </behavior>
  <action>
    Em `src/components/session/SkipReasonSheet.tsx`: adicione a `Props` os campos opcionais
    `ehCardio?: boolean;` (o exercício-alvo é medido por tempo — só relevante quando `escopo
    === 'exercicio'`) e `onSolicitarTroca?: () => void;`. Calcule `const ofereceTroca = escopo
    === 'exercicio' && reason === 'sem_equipamento' && ehCardio === true && onSolicitarTroca !=
    null;` logo antes do `return`. Insira, IMEDIATAMENTE ANTES do `Button` principal (linha
    126), condicionado a `ofereceTroca`: `<Button label="Trocar modalidade" variant="outline"
    onPress={() => onSolicitarTroca!()} disabled={busy} testID="skip-reason-oferecer-troca" />`.
    Troque o `label` do `Button` principal de `ehSessao ? 'Recusar o treino' : 'Não vou fazer'`
    para `ofereceTroca ? 'Recusar mesmo assim' : ehSessao ? 'Recusar o treino' : 'Não vou
    fazer'` — o `onPress`/`onConfirm(reason, note...)` do botão principal NÃO muda, só o
    rótulo. Comentário citando que este é o SEGUNDO entry point do REQ-06 (evolução do fluxo
    de recusa para substituição) e que `onSolicitarTroca` NUNCA invoca `onConfirm`/
    `skip_session_exercise` — caminho aditivo, não substitui a recusa.
  </action>
  <verify>
    <automated>npx jest __tests__/skipReasonSheetTroca.test.tsx -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/skipReasonSheetTroca.test.tsx -q` verde (6 testes de `<behavior>`);
    `npx jest __tests__/recusaDeclarada.test.ts __tests__/recusaDeclaradaFluxo.test.ts -q`
    continua verde sem NENHUMA edição nesses dois arquivos (regressão); `npx tsc --noEmit` sem
    erros novos.
  </acceptance_criteria>
  <done>SkipReasonSheet oferece "Trocar modalidade" só quando sem_equipamento + cardio +
  escopo exercício; o caminho de recusa antigo é bit-a-bit preservado.</done>
  <reversibility rating="reversible">Mudança de componente puro de apresentação, revertível via
  git; nenhum novo call site de RPC.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Wire entry point 2 — ActiveSessionScreen roteia SkipReasonSheet para SwapModalitySheet</name>
  <files>src/screens/ActiveSessionScreen.tsx, __tests__/activeSessionScreen.test.tsx</files>
  <read_first>
    src/screens/ActiveSessionScreen.tsx (já modificado pelo Plano 03-03 — releia o resultado:
    estado `troca`/`trocaBusy`/`modalidadesAceitas`/`modalidadesAceitasErro`,
    `carregarModalidadesAceitas`, `onConfirmarTroca`, as DUAS renderizações de
    `SwapModalitySheet`, e o `SessionQueue` com `onSolicitarTroca` já fiado); linhas 95-146
    (`recusa`/`recusaBusy`, `onConfirmarRecusa`) e 480-520 (as DUAS renderizações de
    `SkipReasonSheet`, onde as novas props `ehCardio`/`onSolicitarTroca` do Plano 03-04 Task 1
    precisam ser passadas); .planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-03-PLAN.md
    (shape exato do estado `troca`/`SwapModalitySheet` já fiado — REUSE, não recrie).
  </read_first>
  <action>
    Em `src/screens/ActiveSessionScreen.tsx`: calcule, logo antes das renderizações de
    `SkipReasonSheet`, `const recusaExercicio = recusa?.escopo === 'exercicio' ?
    draft?.exercises.find((e) => e.exerciseId === recusa.exerciseId) : null;` e `const
    recusaEhCardio = recusaExercicio != null && isTimeBased(metricOf(recusaExercicio));`
    (`isTimeBased`/`metricOf` já importados de `../engine/sessionModel` desde antes desta
    fase). Crie `const onSolicitarTrocaAPartirDaRecusa = () => { if (recusa?.escopo !==
    'exercicio') return; setTroca({ exerciseId: recusa.exerciseId, nome: recusa.nome });
    setModalContent('swap_modality'); setRecusa(null); if (modalidadesAceitas == null &&
    !modalidadesAceitasErro) void carregarModalidadesAceitas(); };` (mesma busca lazy do
    Plano 03-03). Passe `ehCardio={recusaEhCardio}`
    `onSolicitarTroca={onSolicitarTrocaAPartirDaRecusa}` às DUAS renderizações de
    `SkipReasonSheet` (fora do Modal de andamento e inline dentro dele).
  </action>
  <verify>
    <automated>npx jest __tests__/activeSessionScreen.test.tsx -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/activeSessionScreen.test.tsx -q` verde, incluindo o novo teste desta
    task (abaixo); `npx tsc --noEmit` sem erros novos; `grep -n "recusaEhCardio"
    src/screens/ActiveSessionScreen.tsx` confirma o cálculo novo.

    Adicione a `__tests__/activeSessionScreen.test.tsx` (mesmo padrão de
    `mock(getSessionDetail).mockResolvedValueOnce(...)` já usado no Plano 03-03 para um
    exercício de cardio, SEM tocar o `detail` module-level de 2 exercícios de força) um novo
    `it('entry point 2: sem_equipamento em cardio oferece troca em vez de recusar')` que: abre
    a sessão, abre "Ver andamento", pressiona "Não vou fazer" no exercício de cardio, seleciona
    o motivo `skip-reason-sem_equipamento`, pressiona `skip-reason-oferecer-troca`, e afirma
    que o `SwapModalitySheet` abre (texto do título "Trocar <nome do exercício>" ou testID
    equivalente aparece) E que `skipSessionExercise` NUNCA foi chamado.
  </acceptance_criteria>
  <done>Selecionar sem_equipamento num exercício de cardio e pedir a troca abre o
  SwapModalitySheet existente, nunca chama skip_session_exercise.</done>
  <reversibility rating="reversible">Fiação de estado local reutilizando componentes já
  existentes; revertível via git.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `SkipReasonSheet` → `ActiveSessionScreen` (callback `onSolicitarTroca`) | Nenhuma entrada de usuário nova cruza esta fronteira — `onSolicitarTroca` não recebe parâmetros; o exercício-alvo já vem do estado `recusa` que a tela já validou ao abrir o sheet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-09 | Tampering | Segundo entry point contornando a validação de posse do exercício | low | accept | `onSolicitarTroca` só reaproveita `recusa.exerciseId`, que já veio de um exercício real do draft renderizado (não é input de texto); a RPC (migration 0034, T-03-01) valida posse de qualquer forma, independente do caminho de UI que chegou até ela. |
</threat_model>

<verification>
- `npx jest __tests__/skipReasonSheetTroca.test.tsx __tests__/activeSessionScreen.test.tsx -q` verde.
- `npx jest __tests__/recusaDeclarada.test.ts __tests__/recusaDeclaradaFluxo.test.ts -q` verde, sem edição (regressão).
- `npx tsc --noEmit` sem erros novos.
</verification>

<success_criteria>
Satisfaz o segundo sinal explícito do ROADMAP da Fase 3 (evolução do fluxo de recusa declarada
para substituição) sem duplicar lógica de troca — o mesmo SwapModalitySheet/swapExercise dos
Planos 03-02/03-03 atende os dois entry points.
</success_criteria>

<output>
Criar `.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-04-SUMMARY.md` ao final.
</output>
