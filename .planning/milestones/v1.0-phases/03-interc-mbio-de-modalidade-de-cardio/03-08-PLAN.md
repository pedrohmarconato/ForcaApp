---
phase: 03-interc-mbio-de-modalidade-de-cardio
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0036_guarda_set_log_troca_cardio.sql
  - __tests__/cardioSwapGuardaSerieConcluida.test.ts
autonomous: false
requirements: [REQ-06]

estimate:
  tokens: 33000
  raw_tokens: 33000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A migration 0036 recria swap_session_exercise (create or replace, mesma assinatura uuid,uuid,text,text) acrescentando uma guarda que recusa a troca quando já existe ao menos um set_log gravado para o planned_exercise_id alvo — provado por teste que lê o arquivo .sql, sem tocar banco vivo."
    - "Nenhuma aplicação da migration 0036 a um banco vivo (staging ou produção) acontece sem decisão explícita do dono (checkpoint bloqueante) — mesmo padrão da migration 0034 (Plano 03-01) e da migration 0033 (Fase 2)."
    - statement: "Aplicada a um banco vivo, uma chamada direta a swap_session_exercise para um planned_exercise_id que já tem set_logs gravados é recusada com errcode P0005 em vez de inserir em cardio_exercise_swaps — fechando exatamente o comportamento que o 03-UAT.md teste 5 (server_gap) comprovou aceito hoje."
      verification: backstop
  artifacts:
    - supabase/migrations/0036_guarda_set_log_troca_cardio.sql
    - __tests__/cardioSwapGuardaSerieConcluida.test.ts
  key_links:
    - "swap_session_exercise (migration 0036) -> set_logs (via planned_set_id) -> planned_sets.exercise_id -> guarda P0005 -> insert em cardio_exercise_swaps só ocorre se a guarda passar. O MESMO caminho que o 03-UAT.md teste 5 provou ACEITO contra a RPC real passa a ser RECUSADO."
    - "Guard client-side (activeSessionStore.swapExercise, CR-01) continua a primeira linha de defesa; migration 0036 é a SEGUNDA linha, no servidor — nenhuma delas substitui a outra."
  prohibitions:
    - "O guard client-side de CR-01 (src/store/activeSessionStore.ts:1518-1521, `alvo.sets.some(s => s.status === 'done')`) NÃO é tocado por esta plan — a mensagem 'Não é possível trocar a modalidade depois de uma série concluída.' permanece bit a bit a mesma; migration 0036 é defesa em profundidade ADICIONAL, nunca uma substituição do guard existente."
    - "A guarda `pe.metric in ('tempo', 'tempo_distancia')` instalada pela migration 0035 (com asserção runtime anti-regressão) não é enfraquecida, reordenada nem removida por esta migration — 0036 só ACRESCENTA a guarda de set_logs à MESMA função, preservando a guarda de métrica e sua asserção final linha por linha."
    - "Nenhum comando supabase (db push, migration up, link) roda de forma autônoma nesta ou em qualquer task desta plan — a aplicação a um banco vivo é sempre decisão humana explícita, registrada no checkpoint desta plan, mesmo padrão de 03-01."
---

<objective>
Fecha G-03-5-servidor (MAJOR, 03-UAT.md teste 5, `server_gap`): comprovado por comportamento
contra a RPC real que, com um `set_log` `on_target` (1500 s, 4200 m) já gravado para o
exercício de cardio, uma chamada direta a `swap_session_exercise` foi ACEITA e persistiu em
`cardio_exercise_swaps` — `pg_get_functiondef` da função instalada não menciona `set_logs`.
A guarda CR-01 existe SÓ no cliente (`activeSessionStore.ts:1513-1521`); um dispositivo sem
essa guarda, uma chamada direta à API, ou uma corrida entre dois dispositivos grava a troca
mesmo assim, e a série já executada como a modalidade original passa a ser exibida sob a
nova.

Purpose: por decisão explícita do dono (OD-01), este risco deixa de ser "aceito" e passa a
ser fechado: a migration 0036 (`create or replace`, mesma assinatura de `swap_session_exercise`
já instalada por 0034/0035) recusa a troca no PRÓPRIO SERVIDOR quando já existe um `set_log`
para o `planned_exercise_id` alvo, seguindo byte a byte o padrão de guarda + revoke/grant +
asserção runtime anti-regressão que 0035 já estabeleceu para o guard de métrica. Segue o
mesmo precedente de checkpoint de decisão da migration 0034 (Plano 03-01) antes de qualquer
aplicação a staging/produção — 0034 e 0035 já estão vivas nos dois ambientes (AGENTS.md,
commit f69f45f), então esta é uma migration de follow-up sobre uma função já em produção, não
uma criação do zero.
Output: `supabase/migrations/0036_guarda_set_log_troca_cardio.sql` (arquivo, não aplicado),
harness de teste que confere o conteúdo sem tocar banco, e a decisão registrada do dono sobre
quando aplicar.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-UAT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-01-PLAN.md
@supabase/migrations/0034_troca_modalidade_cardio.sql
@supabase/migrations/0035_guarda_metric_troca_cardio.sql
@supabase/migrations/0001_modelo_treino.sql
@AGENTS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `supabase/migrations/0036_guarda_set_log_troca_cardio.sql` (novo) — `create or replace
  function public.swap_session_exercise`, mesma assinatura, com a guarda nova de `set_logs`
  já registrado; revoke/grant repetidos; asserção runtime final estendida
- `__tests__/cardioSwapGuardaSerieConcluida.test.ts` (novo) — harness de leitura do .sql, sem
  banco, molde de `__tests__/cardioSwapMigration.test.ts`
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: Migração 0036 (guarda server-side de série já concluída) + harness de teste local</name>
  <files>supabase/migrations/0036_guarda_set_log_troca_cardio.sql, __tests__/cardioSwapGuardaSerieConcluida.test.ts</files>
  <read_first>
    supabase/migrations/0035_guarda_metric_troca_cardio.sql INTEIRA, já em contexto — molde
    byte a byte: comentário de cabeçalho citando o achado que motiva a migration, `create or
    replace function public.swap_session_exercise(p_session_log_id uuid, p_planned_exercise_id
    uuid, p_to_modality text, p_note text default null) returns public.cardio_exercise_swaps
    language plpgsql security invoker set search_path = public, pg_temp`, o corpo COMPLETO
    (guardas de auth/nulidade/vocabulário/nota, `select ... for update`/`if not found`, guarda
    `finished_at`, guarda de posse do exercício, guarda de métrica cardio — linhas 22-93 —
    ESTA ORDEM E TEXTO PERMANECEM IDÊNTICOS, a guarda nova entra DEPOIS da guarda de métrica
    (linha 93) e ANTES do `insert into public.cardio_exercise_swaps` (linha 95)), o bloco de
    revoke/grant (linhas 111-115, repetir exatamente) e o bloco de asserção runtime final
    (linhas 117-136, `do $$ ... select pg_get_functiondef(...) into v_def; if v_def not like
    '%...%' then raise exception ...; end $$` — a asserção de 0035 sobre o guard de métrica
    DEVE continuar presente e passando; esta migration ACRESCENTA novas checagens ao mesmo
    bloco, não o substitui); supabase/migrations/0001_modelo_treino.sql linhas 105-129 (DDL
    real de `session_logs`/`set_logs`: `set_logs.session_log_id references session_logs(id)`,
    `set_logs.planned_set_id references planned_sets(id) on delete set null` — SEM FK direta
    para `planned_exercises`, o join precisa passar por `planned_sets.exercise_id`) e linhas
    89-100 (`planned_sets.exercise_id references planned_exercises(id)` — mesma coluna real
    confirmada e corrigida no Plano 03-07; NUNCA `planned_exercise_id`, que não existe nessa
    tabela); supabase/migrations/0020_recusa_declarada.sql linhas 327-402 (para relembrar o
    estilo de guarda `if not exists (...) then raise exception ... using errcode = '...'`,
    já replicado por 0034/0035); __tests__/cardioSwapMigration.test.ts INTEIRO, já em contexto
    — molde EXATO de estilo para o harness novo (`readFileSync` do `.sql`, `describe`/`it`
    com `toMatch`/`toContain`, sem abrir conexão de banco); AGENTS.md linhas 21-30 e 35 e 59
    (preflight obrigatório antes de qualquer `db push`; staging primeiro; nunca aplicar por
    SQL direto sem registrar) e a nota de que 0034/0035 JÁ estão vivas em staging
    (`mjdjtiujhwklchalquhc`) e produção (`zanqygwsgxkyjiuhrzju`) desde 2026-08-10.
  </read_first>
  <action>
    Confira `ls supabase/migrations/ | tail -1` — se ainda for `0035_guarda_metric_troca_cardio.sql`,
    esta migration nasce como `0036_guarda_set_log_troca_cardio.sql`; se outro número já
    existir, use o próximo livre.

    Crie `supabase/migrations/0036_guarda_set_log_troca_cardio.sql`:

    (1) Comentário de cabeçalho citando G-03-5-servidor (03-UAT.md teste 5, `server_gap`): a
    guarda CR-01 existia só no cliente (`activeSessionStore.ts:1513-1521`); comprovado por
    comportamento contra a RPC real que uma chamada direta com um `set_log` já gravado para o
    `planned_exercise_id` era ACEITA; decisão do dono (OD-01) foi promover a guarda para o
    servidor como defesa em profundidade, SEM remover o guard client-side. Cite que 0034/0035
    já estão aplicadas em staging/produção (AGENTS.md) — esta é uma migration de follow-up,
    `create or replace` com a MESMA assinatura, nunca um `drop function`.

    (2) `create or replace function public.swap_session_exercise(...)`: reproduza o corpo
    INTEIRO de 0035 (guardas de auth 42501, nulidade 22004, vocabulário 22023, nota 22023,
    posse do log P0002, sessão finalizada P0001, posse do exercício 42501, métrica cardio
    22023 — nesta ordem, texto idêntico) e insira a guarda NOVA logo após a guarda de métrica
    (0035, linhas 84-93) e ANTES do `insert into public.cardio_exercise_swaps`: `if exists
    (select 1 from public.set_logs sl join public.planned_sets ps on ps.id =
    sl.planned_set_id where sl.session_log_id = p_session_log_id and ps.exercise_id =
    p_planned_exercise_id) then raise exception 'exercício % já tem série registrada nesta
    sessão; troca de modalidade recusada', p_planned_exercise_id using errcode = 'P0005'; end
    if;` — comentário acima explicando que P0005 é um código NOVO e distinto (não reutiliza
    P0001/P0002, que já têm outro significado no app, nem P0002-P0004, que são nomes
    reservados de condição do PL/pgSQL) para que o cliente possa, no futuro, distinguir esta
    recusa das demais por `error.code === 'P0005'` caso decida tratá-la de forma específica
    (hoje `errMsg`/`SessionExecutionRequestError.code`, em `sessionExecutionRepository.ts`, já
    propaga o texto e o código sem necessidade de nenhuma mudança de cliente para esta plan).
    Mantenha o `insert ... on conflict (session_log_id, planned_exercise_id) do update ...
    returning * into v_row; return v_row;` idêntico a 0035.

    (3) Revoke/grant: repita EXATAMENTE as duas linhas de 0035 (`revoke all on function
    public.swap_session_exercise(uuid, uuid, text, text) from public, anon; grant execute on
    function public.swap_session_exercise(uuid, uuid, text, text) to authenticated;`) — mesma
    assinatura, `create or replace` não muda o nome dos parâmetros posicionais.

    (4) Bloco de asserção runtime `do $$ ... end $$`: reproduza a asserção de 0035 (guard de
    métrica presente, `muscle_group = 'Cardio'` ausente) e ACRESCENTE duas checagens novas
    sobre a MESMA `v_def` (`pg_get_functiondef('public.swap_session_exercise(uuid, uuid, text,
    text)'::regprocedure)`): `v_def like '%from public.set_logs sl%'` (a guarda nova existe) e
    `v_def like '%errcode = ''P0005''%'` (o errcode novo está presente) — `raise exception` em
    cada falha, texto claro do que quebrou.

    NÃO rode nenhum comando `supabase` (`db push`, `migration up`, `link`, etc.) — é decisão
    do dono, gated pela Task 2 deste plano. Nenhuma outra migration existente é editada
    (0034/0035 permanecem byte a byte como estão).

    Crie `__tests__/cardioSwapGuardaSerieConcluida.test.ts`, molde de
    `__tests__/cardioSwapMigration.test.ts` (`readFileSync` do `.sql`, sem banco). Asserte via
    `toMatch`: (a) `create or replace function public.swap_session_exercise` aparece; (b) a
    guarda nova existe — `from public.set_logs sl` e `join public.planned_sets ps on ps.id =
    sl.planned_set_id` e `ps.exercise_id = p_planned_exercise_id` aparecem, e `errcode =
    'P0005'` aparece; (c) a guarda de métrica de 0035 CONTINUA presente e intocada —
    `pe.metric in ('tempo', 'tempo_distancia')` aparece e `muscle_group = 'Cardio'` NÃO
    aparece em lugar nenhum do arquivo; (d) `revoke all on function` e `grant execute on
    function public.swap_session_exercise(uuid, uuid, text, text) to authenticated` aparecem,
    e nenhum `grant ... to anon` aparece no arquivo inteiro; (e) o bloco de asserções finais
    contém as duas checagens novas (`from public.set_logs sl` e `errcode = ''P0005''` dentro
    do `do $$ ... $$`) além da checagem herdada de 0035 (`pe.metric in`).
  </action>
  <verify>
    <automated>npx jest __tests__/cardioSwapGuardaSerieConcluida.test.ts -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/cardioSwapGuardaSerieConcluida.test.ts -q` verde (ler "Tests: X
    passed", per AGENTS.md linha 70); `ls supabase/migrations/ | tail -1` mostra
    `0036_guarda_set_log_troca_cardio.sql`; `git status supabase/migrations/` mostra APENAS o
    arquivo novo (0034 e 0035 continuam byte a byte como estavam —
    `git diff supabase/migrations/0034_troca_modalidade_cardio.sql
    supabase/migrations/0035_guarda_metric_troca_cardio.sql` vazio); nenhum comando `supabase`
    aparece no histórico desta task; `grep -c "grant execute.*to anon"
    supabase/migrations/0036_guarda_set_log_troca_cardio.sql` retorna 0; `grep -c
    "muscle_group = 'Cardio'" supabase/migrations/0036_guarda_set_log_troca_cardio.sql`
    retorna 0.
  </acceptance_criteria>
  <done>Migration 0036 criada como create or replace sobre swap_session_exercise: guarda nova
  de set_logs já registrado (errcode P0005), guarda de métrica de 0035 preservada, revoke/grant
  e asserção runtime estendida. Harness de teste comprova o conteúdo sem tocar banco vivo.</done>
  <reversibility rating="costly">O arquivo em si é revertível via git; uma vez aplicada a um
  banco vivo já em produção (0034/0035 já estão lá), reverter exige outro create or replace
  manual e decisão sobre chamadas que passariam a ser recusadas — por isso a Task 2
  (checkpoint) precede qualquer aplicação real. A guarda em si é estritamente mais restritiva
  (nunca menos), então o risco é rejeitar uma troca legítima por falso positivo, não corromper
  dado — mitigado pelo harness textual da Task 1.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Quando aplicar a migration 0036 (supabase db push) em cada ambiente?</decision>
  <context>
    O arquivo `supabase/migrations/0036_guarda_set_log_troca_cardio.sql` está pronto e testado
    localmente (harness da Task 1), mas NÃO foi aplicado a nenhum banco. Diferente da migration
    0034 (Plano 03-01), esta migration recria uma função (`swap_session_exercise`) que JÁ ESTÁ
    viva em staging (`forcaapp-staging`, ref `mjdjtiujhwklchalquhc`) e produção
    (`forcaapp-prod`, ref `zanqygwsgxkyjiuhrzju`) desde 2026-08-10 (AGENTS.md, commit
    `f69f45f`) — aplicar é `scripts/supabase-preflight.sh hml && supabase db push`,
    staging primeiro (AGENTS.md linha 35), depois produção com a confirmação `PRODUCAO`
    digitada.

    RISCO OPERACIONAL enquanto a migration 0036 NÃO estiver aplicada: o comportamento
    reproduzido no 03-UAT.md teste 5 continua valendo — uma chamada DIRETA à RPC (fora do app,
    ou um build sem o guard client-side, ou uma corrida entre dois dispositivos) ainda
    consegue gravar uma troca para um exercício com série já concluída. O guard client-side
    (`activeSessionStore.ts:1513-1521`) continua protegendo o fluxo NORMAL do app hoje — a
    03-VERIFICATION.md já registrava isso como "risco residual aceito" antes desta plan; a
    0036 fecha esse risco, mas só depois de aplicada.
  </context>
  <options>
    <option id="option-a">
      <name>Aplicar agora, staging primeiro</name>
      <pros>Fecha G-03-5-servidor de fato, não só no código — a RPC real passa a recusar o
      cenário comprovado no 03-UAT.md teste 5; `create or replace` sobre uma função já em
      produção é uma operação de baixo risco (nenhuma tabela nova, nenhum dado migrado,
      estritamente mais restritiva que a versão atual).</pros>
      <cons>Exige rodar `scripts/supabase-preflight.sh hml && supabase db push`
      manualmente em staging e depois em produção (confirmação `PRODUCAO`) antes de qualquer
      merge que dependa deste guard.</cons>
    </option>
    <option id="option-b">
      <name>Aguardar e aplicar mais perto do próximo deploy da fase</name>
      <pros>Não força uma janela de manutenção agora; o dano de esperar é o MESMO risco
      residual que já era aceito antes desta plan (nada piora).</pros>
      <cons>O gap de G-03-5-servidor continua explorável (chamada direta à API, segundo
      dispositivo) até a migration ser aplicada — quem decidir "option-b" deve reconferir
      antes de cada merge/deploy que dependa deste guard estar vivo.</cons>
    </option>
  </options>
  <resume-signal>Digite "option-a" ou "option-b"; se "option-b", confirme que vai reconferir a
  aplicação da migration 0036 antes de qualquer merge/deploy que dependa deste guard estar
  vivo em staging/produção.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Cliente (autenticado, ou chamada direta à API) → RPC `swap_session_exercise` | Mesma fronteira de 03-01/03-05: entrada não confiável, RLS sozinha não basta — a guarda de negócio precisa viver DENTRO da função, não só no client. |
| Arquivo de migração (git) → banco vivo | Aplicação manual e deliberada (`supabase db push` via preflight), fora do escopo automatizado desta plan — o checkpoint existe justamente para essa fronteira, mesmo padrão de 03-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-11 | Tampering | `swap_session_exercise` aceitando uma troca quando `set_logs` já existe para o `planned_exercise_id` alvo (G-03-5-servidor, comprovado exploitável no 03-UAT.md teste 5) | high | mitigate | Guarda nova em 0036: `exists (select 1 from set_logs sl join planned_sets ps on ps.id = sl.planned_set_id where sl.session_log_id = p_session_log_id and ps.exercise_id = p_planned_exercise_id)` recusa com `errcode = 'P0005'` antes do upsert; asserção runtime `do $$...$$` falha a migration alto se um `create or replace` futuro remover a guarda. |
| T-03-12 | Elevation of Privilege | `anon` executando a `swap_session_exercise` recriada | high | mitigate | `revoke all ... from public, anon; grant execute ... to authenticated;` repetido para a assinatura recriada, verificado pelo harness (`cardioSwapGuardaSerieConcluida.test.ts`) — mesma lição de 0019/0020/0034/0035; `create or replace` sozinho não removeria grants existentes, mas o padrão do repo repete a declaração por auditabilidade explícita. |
</threat_model>

<verification>
- `npx jest __tests__/cardioSwapGuardaSerieConcluida.test.ts -q` verde.
- `git diff supabase/migrations/0034_troca_modalidade_cardio.sql supabase/migrations/0035_guarda_metric_troca_cardio.sql` vazio (nenhuma das duas migrations existentes foi editada).
- Checkpoint (Task 2) respondido antes de considerar este plano encerrado.
</verification>

<success_criteria>
Fecha G-03-5-servidor: a RPC `swap_session_exercise`, uma vez aplicada, recusa no PRÓPRIO
SERVIDOR o cenário que o 03-UAT.md teste 5 comprovou aceito hoje — sem remover o guard
client-side de CR-01, que continua a primeira linha de defesa.
</success_criteria>

<output>
Criar `.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-08-SUMMARY.md` ao final,
incluindo a resposta do checkpoint.
</output>
