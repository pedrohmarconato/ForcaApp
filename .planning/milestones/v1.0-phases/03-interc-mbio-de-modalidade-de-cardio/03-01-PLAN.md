---
phase: 03-interc-mbio-de-modalidade-de-cardio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0034_troca_modalidade_cardio.sql
  - __tests__/cardioSwapMigration.test.ts
autonomous: false
requirements: [REQ-06]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A migration 0034 cria a tabela satélite cardio_exercise_swaps (nunca UPDATE em planned_exercises/planned_sets), a função de vocabulário fechado _forca_modalidade_cardio_valida com as 9 modalidades exatas de CARDIO_MODALIDADES, e a RPC swap_session_exercise com guarda de posse (planned_exercise pertence à sessão do log) e guarda de metrica cardio — provado por teste de harness que lê o arquivo .sql, sem tocar banco vivo."
    - "Nenhuma aplicação da migration a um banco vivo (staging ou produção) acontece sem decisão explícita do dono (checkpoint bloqueante)."
  artifacts:
    - supabase/migrations/0034_troca_modalidade_cardio.sql
    - __tests__/cardioSwapMigration.test.ts
  key_links:
    - "cardio_exercise_swaps.session_log_id -> session_logs(id) (RLS 'own' herdada) e cardio_exercise_swaps.planned_exercise_id -> planned_exercises(id) (guarda de posse dentro da RPC, não só RLS) — mesmo padrão de exercise_skips/skip_session_exercise (migration 0020)."
  prohibitions:
    - "Esta migration nunca faz UPDATE em planned_exercises ou planned_sets — a troca é anotada numa tabela satélite ao lado, nunca reescreve o que a IA gerou (mutar planned_exercises.name vazaria para weeklyReplanRepository.ts e seria porta de mão única não autorizada pelo dono)."
    - "Nenhum comando supabase (db push, migration up, link) roda de forma autônoma nesta ou em nenhuma task futura da fase — a aplicação a um banco vivo é sempre decisão humana explícita, registrada no checkpoint desta plan."
    - "Esta migration não toca a tabela órfã cardio_goals nem cria nenhum DROP/ALTER sobre ela — a limpeza dessa tabela é OUT-OF-SCOPE explícito de 03-CONTEXT.md, decisão do dono para fase futura."
    - "Nenhuma coluna, view ou RPC desta migration alimenta backend/services/dose_cardio.py ou qualquer motor de calibração/nível declarado — o loop de adaptação de dose de cardio pelo realizado é OUT-OF-SCOPE explícito desta fase."
---

<objective>
Preparar (sem aplicar) a migração que sustenta REQ-06: a tabela satélite `cardio_exercise_swaps`
(padrão `exercise_skips`, migration 0020), o vocabulário fechado de modalidade válida
(`_forca_modalidade_cardio_valida`, espelhando `CARDIO_MODALIDADES`) e a RPC
`swap_session_exercise` — e um checkpoint de decisão do dono antes de qualquer aplicação em
banco vivo (REVERSIBILITY_GATES: persistência da troca é "no mínimo costly" per
03-CONTEXT.md).

Purpose: RESEARCH.md (Pattern 4, Pitfall 3) confirma que a troca DEVE ser persistida no
servidor — um draft só-local não sobrevive a `getSessionLogDetail` e falha D-08 por
construção — e que a única forma segura de fazer isso sem abrir a porta de mão única do
schema do plano gerado é uma tabela satélite que nunca toca `planned_exercises`/`planned_sets`.
Esta plan entrega esse artefato pronto e testado localmente, para que 03-02 (motor +
repositório + store) e 03-05 (histórico) possam referenciar as colunas com confiança, sem que
a aplicação real em staging/produção seja decidida em silêncio.
Output: `supabase/migrations/0034_troca_modalidade_cardio.sql` (arquivo, não aplicado),
harness de teste que confere o conteúdo sem tocar banco, e a decisão registrada do dono sobre
quando aplicar.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-CONTEXT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-RESEARCH.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-PATTERNS.md
@supabase/migrations/0020_recusa_declarada.sql
@supabase/migrations/0021_dose_cardio_declarada.sql
@src/constants/cardioModalidades.ts
@AGENTS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `supabase/migrations/0034_troca_modalidade_cardio.sql` (novo) — função `_forca_modalidade_cardio_valida`, tabela `cardio_exercise_swaps`, RPC `swap_session_exercise`, revoke/grant, asserções finais
- `__tests__/cardioSwapMigration.test.ts` (novo) — harness de leitura do .sql, sem banco
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migração 0034 (troca de modalidade de cardio) + harness de teste local</name>
  <files>supabase/migrations/0034_troca_modalidade_cardio.sql, __tests__/cardioSwapMigration.test.ts</files>
  <read_first>
    supabase/migrations/0020_recusa_declarada.sql (INTEIRO — molde byte a byte: comentário de
    cabeçalho "O que não existia"/"O que muda", função `_forca_motivo_recusa_valido` em SQL
    `language sql immutable set search_path = public, pg_temp`, tabela `exercise_skips` com
    `unique (session_log_id, planned_exercise_id)`, RLS `enable row level security` + policy
    "own exercise skips" com `for all using (...) with check (...)` que exige
    `pe.session_id = l.planned_session_id` e `l.finished_at is null`, RPC
    `skip_session_exercise` completa (linhas 327-402: guarda `auth.uid() is null` -> 42501,
    guarda de vocabulário -> 22023, `select ... for update` + `if not found` -> P0002, guarda
    `finished_at is not null` -> P0001, guarda de posse do exercício -> 42501, `insert ...
    on conflict (session_log_id, planned_exercise_id) do update ... returning * into v_row`),
    bloco de revoke/grant (linhas 588-604: `revoke all on function ... from public, anon;
    grant execute on function ... to authenticated;` — repetir para CADA função nova ou
    recriada), bloco de asserções finais (linhas 610-658: `information_schema.columns`/
    `information_schema.tables`, `pg_tables ... rowsecurity`,
    `has_function_privilege('anon', ..., 'EXECUTE')` deve ser falso,
    `has_function_privilege('authenticated', ..., 'EXECUTE')` deve ser verdadeiro);
    supabase/migrations/0021_dose_cardio_declarada.sql:52,107-108 (comentário "Vazio/null =
    qualquer modalidade" — NÃO se aplica a este arquivo: aquele comentário é sobre a leitura
    do GERADOR de plano, não sobre a validade de `to_modality` na troca — a validação aqui é
    só "este texto está no catálogo de 9 modalidades", não "o aluno aceitou esta modalidade");
    src/constants/cardioModalidades.ts (INTEIRO — os 9 nomes EXATOS de `CARDIO_MODALIDADES`
    que a função `_forca_modalidade_cardio_valida` deve reproduzir literalmente, incluindo os
    parênteses de "Cardio Contínuo (LISS)" e "Cardio Intervalado (HIIT)"); __tests__/recusaDeclarada.test.ts
    linhas 101-112 (padrão de teste que lê o `.sql` bruto via `readFileSync` +
    `join(process.cwd(), 'supabase/migrations/...')` e usa `expect(sql).toMatch(...)` — SEM
    abrir conexão de banco; replicar esse padrão, não o de `backend/tests/test_*.py` desta
    vez, porque esta migration não tem nenhum consumidor Python); AGENTS.md linhas 21-30, 35 e
    59 (preflight obrigatório antes de qualquer `db push`; staging primeiro; nunca aplicar por
    SQL direto sem registrar).
  </read_first>
  <action>
    Confira `ls supabase/migrations/ | tail -3` — o maior arquivo hoje é
    `0033_anamnese_cardio_declarada.sql` (já aplicado, Fase 2), então esta migration nasce como
    `0034_troca_modalidade_cardio.sql`; se outro número já existir, use o próximo livre.

    Crie `supabase/migrations/0034_troca_modalidade_cardio.sql` seguindo BYTE A BYTE a
    estrutura de `0020_recusa_declarada.sql`, com estas seções, nesta ordem:

    (1) Comentário de cabeçalho no formato "O que não existia" / "O que muda" citando REQ-06,
    explicando que a troca evolui o fluxo de recusa declarada (0020) para substituição
    preservando a dose por tempo, que a persistência é satélite (nunca `UPDATE` em
    `planned_exercises`/`planned_sets` — cite `weeklyReplanRepository.ts` como o consumidor que
    seria contaminado se o nome fosse mutado in-place) e que os errcodes que o app trata são os
    mesmos já documentados em `sessionExecutionRepository.ts` (42501, 22023, P0001, P0002).

    (2) Função `public._forca_modalidade_cardio_valida(p_modalidade text) returns boolean
    language sql immutable set search_path = public, pg_temp` cujo corpo é
    `select p_modalidade in (...)` com os 9 literais EXATOS de `CARDIO_MODALIDADES`
    ('Caminhada', 'Corrida', 'Bicicleta Ergométrica', 'Elíptico', 'Remo Ergômetro',
    'Escada Ergométrica', 'Pular Corda', 'Cardio Contínuo (LISS)', 'Cardio Intervalado
    (HIIT)'), seguida de `comment on function` explicando que é a fonte única do CHECK e da
    RPC (mesmo padrão de `_forca_motivo_recusa_valido`).

    (3) Tabela `public.cardio_exercise_swaps`: `id uuid primary key default gen_random_uuid()`,
    `session_log_id uuid not null references public.session_logs (id) on delete cascade`,
    `planned_exercise_id uuid not null references public.planned_exercises (id) on delete
    cascade`, `to_modality text not null`, `note text`, `created_at timestamptz not null
    default now()`, `updated_at timestamptz not null default now()`,
    `unique (session_log_id, planned_exercise_id)` (repetir a troca corrige a escolha, não
    empilha — mesma semântica de `exercise_skips`). Em bloco `do $$ ... end $$` com
    `if not exists (select 1 from pg_constraint where conname = '...')`, adicione as
    constraints `cardio_exercise_swaps_modalidade_check` (`check
    (public._forca_modalidade_cardio_valida(to_modality))`) e
    `cardio_exercise_swaps_note_len` (`check (note is null or char_length(note) <= 280)`).
    Índice `create index if not exists cardio_exercise_swaps_exercise_idx on
    public.cardio_exercise_swaps (planned_exercise_id, created_at desc)` (mesmo padrão do
    índice de `exercise_skips`). `comment on table` explicando o escopo (satélite do
    session_log, nunca reescreve o plano).

    (4) RLS: `alter table public.cardio_exercise_swaps enable row level security;` +
    `drop policy if exists "own cardio exercise swaps" on public.cardio_exercise_swaps;
    create policy "own cardio exercise swaps" on public.cardio_exercise_swaps for all using
    (exists (select 1 from public.session_logs l where l.id = session_log_id and l.user_id =
    auth.uid())) with check (exists (select 1 from public.session_logs l join
    public.planned_exercises pe on pe.id = planned_exercise_id and pe.session_id =
    l.planned_session_id where l.id = session_log_id and l.user_id = auth.uid() and
    l.finished_at is null))` — byte a byte igual à policy de `exercise_skips` (0020, linhas
    163-183), só trocando o nome da tabela.

    (5) RPC `public.swap_session_exercise(p_session_log_id uuid, p_planned_exercise_id uuid,
    p_to_modality text, p_note text default null) returns public.cardio_exercise_swaps
    language plpgsql security invoker set search_path = public, pg_temp`. Corpo, na ordem de
    `skip_session_exercise` (0020:327-402): guarda `auth.uid() is null` -> raise exception
    `errcode = '42501'`; guarda `p_planned_exercise_id is null` -> `errcode = '22004'`; guarda
    `p_to_modality is null or not public._forca_modalidade_cardio_valida(p_to_modality)` ->
    `errcode = '22023'`; guarda de nota `char_length(v_note) > 280` -> `errcode = '22023'`
    (com `v_note text := nullif(btrim(coalesce(p_note, '')), '')`, mesmo padrão de
    `skip_session_exercise`); `select * into v_log from public.session_logs where id =
    p_session_log_id and user_id = auth.uid() for update; if not found` -> `errcode = 'P0002'`;
    `if v_log.finished_at is not null` -> `errcode = 'P0001'`; guarda de posse do exercício
    (`exists (select 1 from public.planned_exercises pe where pe.id = p_planned_exercise_id
    and pe.session_id = v_log.planned_session_id)`) -> `errcode = '42501'`; guarda NOVA
    específica desta migration — o exercício alvo tem de ser cardio: `exists (select 1 from
    public.planned_exercises pe where pe.id = p_planned_exercise_id and (pe.metric in
    ('tempo', 'tempo_distancia') or pe.muscle_group = 'Cardio'))` -> `errcode = '22023'`,
    mensagem citando que só exercício de cardio/tempo pode ser trocado de modalidade (defesa
    em profundidade: a UI já só oferece o botão em exercício `isTimeBased`, mas a RPC não
    confia só nisso). Depois: `insert into public.cardio_exercise_swaps (session_log_id,
    planned_exercise_id, to_modality, note) values (p_session_log_id, p_planned_exercise_id,
    p_to_modality, v_note) on conflict (session_log_id, planned_exercise_id) do update set
    to_modality = excluded.to_modality, note = excluded.note, updated_at = now() returning *
    into v_row; return v_row;`.

    (6) Revoke/grant: `revoke all on function public._forca_modalidade_cardio_valida(text)
    from public, anon; grant execute on function public._forca_modalidade_cardio_valida(text)
    to authenticated;` e o mesmo par para
    `public.swap_session_exercise(uuid, uuid, text, text)`.

    (7) Bloco de asserções finais `do $$ ... end $$` (molde 0020:610-658): confirma que a
    tabela `cardio_exercise_swaps` existe (`information_schema.tables`), que RLS está ligada
    (`pg_tables ... rowsecurity`), e que para as DUAS funções (`_forca_modalidade_cardio_valida`,
    `swap_session_exercise`) `has_function_privilege('anon', ..., 'EXECUTE')` é falso e
    `has_function_privilege('authenticated', ..., 'EXECUTE')` é verdadeiro — `raise exception`
    em cada falha, texto claro do que falhou.

    NÃO rode nenhum comando `supabase` (`db push`, `migration up`, `link`, etc.) — é decisão
    do dono, gated pela Task 2 deste plano. Nenhuma outra migration existente é editada.

    Crie `__tests__/cardioSwapMigration.test.ts` seguindo o padrão de
    `__tests__/recusaDeclarada.test.ts:101-112` (lê o `.sql` via `fs.readFileSync(join(process.cwd(),
    'supabase/migrations/0034_troca_modalidade_cardio.sql'), 'utf8')`, sem banco). Asserte via
    `toMatch`/`toContain`: (a) os 9 nomes de `CARDIO_MODALIDADES` aparecem dentro do corpo de
    `_forca_modalidade_cardio_valida` (incluindo os dois nomes com parênteses); (b) `create
    table if not exists public.cardio_exercise_swaps` aparece e `unique (session_log_id,
    planned_exercise_id)` aparece; (c) `enable row level security` e `"own cardio exercise
    swaps"` aparecem; (d) a RPC `swap_session_exercise` contém as 4 checagens de erro
    (`errcode = '42501'` aparece ao menos 2×, `'22023'` ao menos 2×, `'P0002'`, `'P0001'`) e a
    guarda de metrica cardio (`pe.metric in`); (e) `on conflict (session_log_id,
    planned_exercise_id) do update` aparece; (f) para as duas funções, `revoke all on function`
    E `grant execute on function ... to authenticated` aparecem, e nenhum `grant ... to anon`
    aparece no arquivo inteiro; (g) o bloco de asserções finais contém
    `has_function_privilege('anon'`.
  </action>
  <verify>
    <automated>npx jest __tests__/cardioSwapMigration.test.ts -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/cardioSwapMigration.test.ts -q` verde (ler o resumo "Tests: X passed",
    não o exit code isolado, per AGENTS.md linha 70); `ls supabase/migrations/ | tail -1`
    mostra `0034_troca_modalidade_cardio.sql`; `git status supabase/migrations/` mostra APENAS
    o arquivo novo (nenhuma migration existente foi editada); nenhum comando `supabase` aparece
    no histórico desta task; `grep -c "grant execute.*to anon" supabase/migrations/0034_troca_modalidade_cardio.sql`
    retorna 0.
  </acceptance_criteria>
  <done>Migration 0034 criada seguindo o molde 0020 byte a byte: tabela satélite, vocabulário
  fechado sincronizado com CARDIO_MODALIDADES, RPC com guardas de posse e de métrica cardio,
  revoke/grant e asserções finais. Harness de teste comprova o conteúdo sem tocar banco vivo.</done>
  <reversibility rating="costly">O arquivo em si é revertível via git; uma vez aplicada a um
  banco vivo, reverter exige DDL manual (drop table/function) e decisão sobre trocas já
  gravadas — por isso a Task 2 (checkpoint) precede qualquer aplicação real. A tabela em si é
  aditiva (nunca reescreve planned_exercises/planned_sets), então o risco é bem menor que uma
  migration que muta dado existente.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Quando aplicar a migration 0034 (supabase db push) em cada ambiente?</decision>
  <context>
    O arquivo `supabase/migrations/0034_troca_modalidade_cardio.sql` está pronto e testado
    localmente (harness da Task 1), mas NÃO foi aplicado a nenhum banco — nem staging
    (`forcaapp-staging`, ref `mjdjtiujhwklchalquhc`), nem produção (`forcaapp-prod`, ref
    `zanqygwsgxkyjiuhrzju`). Aplicar é ação manual (`scripts/supabase-preflight.sh hml &&
    supabase db push`, staging primeiro per AGENTS.md linha 35).

    RISCO OPERACIONAL se o código do app (planos 03-02..03-05 desta fase, que chamam a RPC
    `swap_session_exercise` e leem a tabela `cardio_exercise_swaps`) chegar a um ambiente
    compartilhado ANTES desta migration ser aplicada lá: a chamada à RPC falha com "function
    does not exist" (PostgREST 42883), a troca de modalidade NÃO funciona nesse ambiente — mas
    o app já tem o mesmo padrão de erro tratado (`SessionExecutionRequestError` -> `saveError`
    -> `Alert.alert`, mesmo caminho de `skipExercise`) e NENHUMA funcionalidade PRÉ-EXISTENTE
    quebra (ao contrário da migration 0033 na Fase 2, que bloqueava TODO o questionário se a
    coluna faltasse — aqui o raio de dano é: só a feature de troca fica indisponível até a
    migration ser aplicada).
  </context>
  <options>
    <option id="option-a">
      <name>Aplicar agora, staging primeiro</name>
      <pros>Destrava os Planos 03-02..03-05 para chegar a staging sem acumular risco de
      deploy; segue o fluxo já documentado em AGENTS.md; evita ter que reconferir manualmente
      antes de cada merge.</pros>
      <cons>Exige rodar `scripts/supabase-preflight.sh hml && supabase db push` manualmente em
      staging e depois `scripts/supabase-preflight.sh prod && supabase db push` (com a
      confirmação `PRODUCAO` digitada) em produção antes de qualquer merge para essas
      branches.</cons>
    </option>
    <option id="option-b">
      <name>Aguardar e aplicar mais perto do deploy da fase completa</name>
      <pros>Não força uma janela de manutenção agora, enquanto o resto da fase ainda está em
      execução; o dano de esquecer é contido (só a troca fica indisponível, nada pré-existente
      quebra).</pros>
      <cons>Quem mesclar o código dos Planos 03-02..03-05 para staging/produção SEM checar que
      a migration já rodou LÁ vê a feature de troca falhar silenciosamente para o usuário até
      alguém notar e aplicar a migration.</cons>
    </option>
  </options>
  <resume-signal>Digite "option-a" ou "option-b"; se "option-b", confirme que vai reconferir a
  aplicação da migration antes de cada merge/deploy que inclua os Planos 03-02..03-05 para
  staging/produção.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Cliente autenticado (app) → RPC `swap_session_exercise` | Entrada não confiável: `p_session_log_id`, `p_planned_exercise_id`, `p_to_modality`, `p_note` chegam do app e podem ser manipulados por um cliente adulterado — RLS sozinha não basta (mesma lição de `skip_session_exercise`). |
| Arquivo de migração (git) → banco vivo | A aplicação é ação manual e deliberada (`supabase db push` via preflight), fora do escopo automatizado deste plano — o checkpoint existe justamente para essa fronteira. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-01 | Tampering / Elevation of Privilege | `swap_session_exercise` — `p_planned_exercise_id` de OUTRA sessão/usuário | high | mitigate | Guarda explícita dentro da RPC: `exists (select 1 from planned_exercises pe where pe.id = p_planned_exercise_id and pe.session_id = v_log.planned_session_id)` — sem essa checagem, a RLS "own" do `session_log` não impede referenciar um `planned_exercise_id` alheio (mesmo padrão de `skip_session_exercise`, 0020:373-382). |
| T-03-02 | Elevation of Privilege | `anon` executando `swap_session_exercise` ou `_forca_modalidade_cardio_valida` sem autenticação | high | mitigate | `revoke all ... from public, anon; grant execute ... to authenticated;` nas duas funções, com asserção `has_function_privilege('anon', ..., 'EXECUTE')` falsa no bloco final — mesmo padrão obrigatório de toda RPC nova neste repo (lição da migration 0019, reforçada em 0020/0021/0033). |
| T-03-03 | Tampering | Modalidade fora do catálogo (`to_modality`) persistida por drift silencioso | high | mitigate | CHECK constraint via `_forca_modalidade_cardio_valida` (vocabulário fechado, imutável) **e** teste JS (`cardioSwapMigration.test.ts`) que compara literalmente com `CARDIO_MODALIDADES` — sem isso, um nome divergente do catálogo do backend seria aceito pelo banco mas descartado no prompt/histórico posterior. |
| T-03-04 | Tampering | Troca aplicada a um exercício que não é de cardio (bypass da UI que só mostra o botão em `isTimeBased`) | medium | mitigate | Guarda `pe.metric in ('tempo', 'tempo_distancia') or pe.muscle_group = 'Cardio'` dentro da RPC — defesa em profundidade contra chamada direta à RPC fora da UI. |
</threat_model>

<verification>
- `npx jest __tests__/cardioSwapMigration.test.ts -q` verde.
- `git diff supabase/migrations/` mostra só o arquivo novo `0034_troca_modalidade_cardio.sql`.
- Checkpoint (Task 2) respondido antes de considerar este plano encerrado.
</verification>

<success_criteria>
Contribui para o Success Criteria 2 do ROADMAP Fase 3 (a troca preserva a duração-alvo e não
inventa distância — a tabela satélite garante que `planned_exercises`/`planned_sets` do plano
gerado permanecem intocados) e garante que nenhuma aplicação de schema aconteça sem decisão
explícita do dono.
</success_criteria>

<output>
Criar `.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-01-SUMMARY.md` ao final,
incluindo a resposta do checkpoint.
</output>
