---
phase: 03-interc-mbio-de-modalidade-de-cardio
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/cardioGoals.ts
  - src/engine/cardioPrescrito.ts
  - src/components/progress/CardioPrescritoSection.tsx
  - __tests__/cardioGoals.test.ts
  - __tests__/cardioPrescrito.test.ts
  - __tests__/cardioPrescritoSecao.test.tsx
autonomous: true
requirements: [REQ-06]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "distanciaRealizadaSemanaM soma a distância realizada da semana de QUALQUER modalidade, trocada ou não, num total único — sem linha separada por modalidade (D-05, Success Criterion 3 do ROADMAP)."
    - "O lado PRESCRITO (prescritoKm, prescritoSessoes, duracaoSegundos) permanece CHEIO — nenhuma regra de desconto para sessão trocada foi adicionada a cardioPrescrito.ts/cardioPrescritoRepository.ts nesta plan (D-06)."
    - "A aba Progresso mostra 'X de Y km' com barra de progresso quando a semana tem km prescrito; quando não há km prescrito, nenhuma linha de km aparece (nem '0 de 0 km')."
  artifacts: []
  key_links:
    - "cardioGoalRepository.getCardioLogs (já modality-agnostic, D-03 automático) -> cardioGoals.distanciaRealizadaSemanaM (novo) -> cardioPrescrito.progressoPrescrito.realizadoKm (novo) -> CardioPrescritoSection (linha de km nova)."
  prohibitions:
    - "Nenhuma menção a swap/troca entra em cardioPrescrito.ts, cardioPrescritoRepository.ts ou na lógica de progressoConsistencia — essa camada permanece inteiramente alheia à existência de trocas (D-06, Pitfall 7 do RESEARCH.md)."
    - "Nenhuma mudança em progressoConsistencia (contagem de dias distintos) ou em getPrescricaoSemanaCorrente — D-05 é aditivo, D-07 e a contagem de sessões já estão corretos por não dependerem de nome de exercício."
---

<objective>
D-05/D-06 e o Success Criterion 3 do ROADMAP: a aba Progresso soma o km REALIZADO da semana de
qualquer modalidade (trocada ou não) num total único, comparado ao km PRESCRITO — que
permanece cheio, sem desconto por sessão trocada.

Purpose: RESEARCH.md confirma que este trabalho não depende do motor de troca em nada — a
distância realizada já chega modality-agnostic de `getCardioLogs`
(`cardioGoalRepository.ts:23-64`, já traz `distanceM` por log sem olhar identidade do
exercício) e o lado prescrito já está correto por construção (lê `planned_sets`, que a troca
nunca toca). Este plano é 100% independente das demais plans da fase — roda em paralelo (Wave
1) sem tocar nenhum arquivo que elas tocam.
Output: `distanciaRealizadaSemanaM` (motor puro), `progressoPrescrito` estendido com
`realizadoKm`/`fracaoKm`, e a linha de km na tela.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-CONTEXT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-RESEARCH.md
@src/engine/cardioGoals.ts
@src/engine/cardioPrescrito.ts
@src/components/progress/CardioPrescritoSection.tsx
@__tests__/cardioPrescritoSecao.test.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `cardioGoals.ts`: `distanciaRealizadaSemanaM` (função pura nova)
- `cardioPrescrito.ts`: `ProgressoPrescrito.realizadoKm`/`.fracaoKm` (campos novos), `progressoPrescrito` estendido
- `CardioPrescritoSection.tsx`: linha "X de Y km" + barra de progresso (novo)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: distanciaRealizadaSemanaM + extensão de progressoPrescrito</name>
  <files>src/engine/cardioGoals.ts, src/engine/cardioPrescrito.ts, __tests__/cardioGoals.test.ts, __tests__/cardioPrescrito.test.ts</files>
  <read_first>
    src/engine/cardioGoals.ts (INTEIRO, já em contexto — `progressoConsistencia` linhas
    185-233 é o MOLDE exato: mesmo corte de semana via `inicioDaSemana`, mesmo
    `numeroPositivo`, mesmo "zero é fato" quando há amostra na janela mas sem o campo);
    src/engine/cardioPrescrito.ts (INTEIRO, já em contexto — `ProgressoPrescrito` linhas 70-79,
    `progressoPrescrito` linhas 88-107, o comentário de cabeçalho linhas 1-19 sobre "sem
    amostra é null vs. zero é fato" — releia antes de decidir o comportamento de
    `realizadoKm`); __tests__/cardioGoals.test.ts linhas 174-300 (`describe('meta de
    consistência', ...)` — molde de teste para `distanciaRealizadaSemanaM`, mesmo padrão de
    `REFERENCIA`/fixture de `CardioLog`); __tests__/cardioPrescrito.test.ts linhas 75-116
    (`describe('progressoPrescrito', ...)` INTEIRO — os 3 testes existentes NÃO devem precisar
    de edição, só ganhar testes novos).
  </read_first>
  <behavior>
    `distanciaRealizadaSemanaM(logs, referencia)` (`cardioGoals.ts`):
    - Teste 1: logs de DUAS modalidades diferentes ("Corrida" 3000m, "Remo Ergômetro" 2000m)
      na mesma semana -> soma `5000` (D-05, "km é km", nome não importa).
    - Teste 2: log sem `distanceM` (modalidade só-tempo) na semana -> não entra na soma, mas
      NÃO derruba os outros logs somáveis da mesma semana.
    - Teste 3: nenhum log com distância na janela -> devolve `null` (sem amostra, mesma
      convenção do resto do arquivo — a COERÇÃO para "0 é fato" acontece em
      `cardioPrescrito.ts`, não aqui, porque este arquivo não sabe se existe prescrição).
    - Teste 4: log fora da janela da semana (antes/depois) não conta.

    `progressoPrescrito` (`cardioPrescrito.ts`), estendido:
    - Teste 5: `prescricao.distanciaM: 5000`, logs somando `2000` de distância realizada na
      semana -> `p.realizadoKm === 2`, `p.prescritoKm === 5`, `p.fracaoKm === 0.4`.
    - Teste 6 (zero é fato, mesmo espírito do cabeçalho do arquivo): `prescricao.distanciaM:
      5000`, NENHUM log com distância na semana -> `p.realizadoKm === 0` (fato, não `null`),
      `p.fracaoKm === 0`.
    - Teste 7: `prescricao.distanciaM: null` (sem km prescrito) -> `p.realizadoKm` é `null`
      (a tela não deve ter o que comparar) e `p.fracaoKm` é `null`.
    - Teste 8 (regressão D-06/Pitfall 7): os 3 testes JÁ EXISTENTES do describe
      `'progressoPrescrito'` continuam passando sem edição — confirma que nada no lado
      PRESCRITO mudou.
  </behavior>
  <action>
    Em `src/engine/cardioGoals.ts`, adicione, ao lado de `progressoConsistencia` (após a linha
    233), a função `distanciaRealizadaSemanaM(logs: readonly CardioLog[], referencia: Date =
    new Date()): number | null` — mesmo corte de semana (`inicioDaSemana(referencia)` +
    janela de 7 dias, igual a `progressoConsistencia`), soma `numeroPositivo(l.distanceM)` de
    cada log cuja `completedAt` cai na janela, começando de `metros: number | null = null` e só
    virando número no primeiro valor somável (mesmo padrão de `melhor`/acumuladores que
    começam `null` neste arquivo) — devolve `null` se nenhum log da janela tinha distância.
    Comentário citando D-05 ("km é km": soma de QUALQUER modalidade, sem olhar `l.name`) e a
    divisão de responsabilidade com `cardioPrescrito.ts` (aqui é "sem amostra = null"; a
    decisão de mostrar "0 km" quando existe prescrição mas nenhuma amostra é de QUEM CONSOME
    este valor, não deste motor genérico).

    Em `src/engine/cardioPrescrito.ts`: importe `distanciaRealizadaSemanaM` de
    `./cardioGoals` (junto ao `progressoConsistencia` já importado). Adicione a
    `ProgressoPrescrito` os campos `realizadoKm: number | null;` (comentário: "km realizado da
    semana, QUALQUER modalidade — D-05. null só quando NÃO há prescrição de km (nada a
    comparar); com prescrição e nenhuma amostra, o valor é 0 (fato, mesma disciplina do resto
    deste arquivo)") e `fracaoKm: number | null;` (0..1, mesmo padrão de `fracaoMinutos`/
    `fracaoSessoes`). Em `progressoPrescrito`, calcule `const metros =
    distanciaRealizadaSemanaM(logs, referencia);` uma vez, e adicione ao objeto de retorno:
    `realizadoKm: prescricao.distanciaM != null ? (metros ?? 0) / 1000 : null, fracaoKm:
    prescricao.distanciaM != null && prescricao.distanciaM > 0 ? Math.min(1, (metros ?? 0) /
    prescricao.distanciaM) : null`. NÃO toque `somarPrescricaoSemana` nem
    `getPrescricaoSemanaCorrente`/`cardioPrescritoRepository.ts` — o lado prescrito não muda
    (D-06).
  </action>
  <verify>
    <automated>npx jest __tests__/cardioGoals.test.ts __tests__/cardioPrescrito.test.ts -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/cardioGoals.test.ts __tests__/cardioPrescrito.test.ts -q` verde
    INTEIRO (não só os describes novos — confirma que os 3 testes pré-existentes de
    `progressoPrescrito` e toda a suíte de `cardioGoals.test.ts` não regrediram); `npx tsc
    --noEmit` sem erros novos; `git diff src/services/cardioPrescritoRepository.ts` vazio
    (nenhuma mudança no repositório de leitura do prescrito).
  </acceptance_criteria>
  <done>distanciaRealizadaSemanaM existe e soma qualquer modalidade; progressoPrescrito expõe
  realizadoKm/fracaoKm sem alterar o lado prescrito.</done>
  <reversibility rating="reversible">Extensão aditiva de motor puro, sem I/O, sem migration.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Linha de km prescrito × realizado na aba Progresso</name>
  <files>src/components/progress/CardioPrescritoSection.tsx, __tests__/cardioPrescritoSecao.test.tsx</files>
  <read_first>
    src/components/progress/CardioPrescritoSection.tsx (INTEIRO, já em contexto — o bloco de
    `progresso.metaMinutos != null ? (<>...</>) : null` linhas 91-102 é o MOLDE EXATO a
    replicar para km: mesma estrutura `<Text style={styles.detalhe}>` + `<ProgressTrack
    ratio={...} accessibilityLabel="..." style={styles.barra} />`; `formatarMinutos` linhas
    35-42 é uma função de formatação DECIMAL genérica apesar do nome — reutilizável para km sem
    alteração); __tests__/cardioPrescritoSecao.test.tsx (INTEIRO, já em contexto — `renderSecao`
    linhas 44-53, `prescricaoRotina`/`prescricaoVazia` linhas 31-42, o describe `'prescrito ×
    realizado'` linhas 82-133 é onde os testes novos entram).
  </read_first>
  <action>
    Em `src/components/progress/CardioPrescritoSection.tsx`, dentro do bloco `progresso ? (
    <Card testID="card-cardio-prescrito"> ... )` (linhas 82-118), adicione uma nova seção
    condicional ENTRE o bloco de `metaMinutos` (linha 91-102) e o bloco de `metaSessoes` (linha
    104-115): `{progresso.prescritoKm != null ? (<><Text style={styles.detalhe}>{`${formatarMinutos(progresso.realizadoKm
    ?? 0)} de ${formatarMinutos(progresso.prescritoKm)} km`}</Text><ProgressTrack
    ratio={progresso.fracaoKm ?? 0} accessibilityLabel="Progresso da distância de cardio
    prescrita" style={styles.barra} /></>) : null}`. Nenhuma mudança de estilo nova é
    necessária (reaproveita `styles.detalhe`/`styles.barra` já existentes).
  </action>
  <verify>
    <automated>npx jest __tests__/cardioPrescritoSecao.test.tsx -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/cardioPrescritoSecao.test.tsx -q` verde INTEIRO (os testes
    pré-existentes de `'estados iniciais'`/`'nenhuma ação de escrita de meta sobrevive'`
    continuam passando sem edição); `npx tsc --noEmit` sem erros novos. Adicione ao describe
    `'prescrito × realizado'`: (a) `it('mostra km prescrito × realizado com barra')` —
    `prescricao: prescricaoRotina({ distanciaM: 5000 })`, `logs: [log('Corrida', 1800, 3000,
    ...), log('Remo Ergômetro', 900, 2000, ...)]` (duas modalidades DIFERENTES, D-05) ->
    `getByText('5 de 5 km')` E `getByLabelText('Progresso da distância de cardio prescrita')`;
    (b) `it('sem km prescrito, nenhuma linha de km aparece')` — `prescricao:
    prescricaoRotina({ distanciaM: null })` -> `queryByText(/km/)` é `null`.
  </acceptance_criteria>
  <done>A aba Progresso mostra km prescrito × realizado somando qualquer modalidade, sem linha
  quando não há km prescrito.</done>
  <reversibility rating="reversible">UI de leitura pura, revertível via git.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `getCardioLogs` (RLS "own") → `distanciaRealizadaSemanaM` → tela | Puro consumo de dado já autorizado; nenhuma fronteira de confiança nova nesta plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-10 | Tampering | Nenhum — motor puro sem I/O, sem input externo além de dado já lido via RLS | low | accept | Não há superfície nova: `distanciaRealizadaSemanaM` só agrega dado que `getCardioLogs` já trouxe com RLS "own" aplicada. |
</threat_model>

<verification>
- `npx jest __tests__/cardioGoals.test.ts __tests__/cardioPrescrito.test.ts __tests__/cardioPrescritoSecao.test.tsx -q` verde.
- `npx tsc --noEmit` sem erros novos.
- `git diff src/services/cardioPrescritoRepository.ts src/engine/cardioPrescrito.ts` mostra que `somarPrescricaoSemana`/`getPrescricaoSemanaCorrente` não foram tocados (só `progressoPrescrito`/`ProgressoPrescrito` ganharam campos).
</verification>

<success_criteria>
Satisfaz o Success Criterion 3 do ROADMAP da Fase 3 (realizado na modalidade trocada conta
normalmente no Progresso) e D-05/D-06, sem qualquer acoplamento ao motor de troca.
</success_criteria>

<output>
Criar `.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-06-SUMMARY.md` ao final.
</output>
