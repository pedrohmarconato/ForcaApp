---
phase: 02-anamnese-e-calibra-o-do-cardio
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0033_anamnese_cardio_declarada.sql
  - backend/tests/test_migration_anamnese_cardio.py
autonomous: false
requirements: [REQ-04]

estimate:
  tokens: 42000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A migration 0033 adiciona cardio_pratica_atualmente, cardio_distancia_confortavel_km e cardio_objetivo em questionario_usuario com CHECK de faixa/coerência, espelha as 3 colunas em questionario_historico, e snapshot_questionario() grava as 3 — provado por teste de harness que lê o arquivo .sql, sem tocar banco vivo."
    - "Nenhuma aplicação da migration a um banco vivo acontece sem decisão explícita do dono (checkpoint bloqueante)."
  artifacts:
    - supabase/migrations/0033_anamnese_cardio_declarada.sql
  key_links:
    - "questionario_usuario (colunas novas) -> questionario_historico (espelho) -> snapshot_questionario() (trigger AFTER INSERT/UPDATE) — mesmo padrão de 0021"
---

<objective>
Preparar (sem aplicar) a migração que sustenta REQ-04: as 3 colunas de anamnese de cardio
(`cardio_pratica_atualmente`, `cardio_distancia_confortavel_km`, `cardio_objetivo`) em
`questionario_usuario`, espelhadas em `questionario_historico`, com CHECK de faixa/coerência —
seguindo byte a byte o precedente de `0021_dose_cardio_declarada.sql` — e um checkpoint de
decisão do dono antes de qualquer aplicação em banco vivo (D: REVERSIBILITY_GATES, migração é
"no mínimo costly").

Purpose: RESEARCH.md confirma que `questionario_usuario` é tabela TIPADA, não JSONB — campo novo
sempre exige migração. E o `handleSubmit` da tela relança qualquer erro de `saveQuestionnaireDataAPI`
que não seja token expirado/409, bloqueando TODO o salvamento do questionário se uma coluna
esperada não existir no banco — por isso a migração precisa estar pronta e testada ANTES do
Plano 02-01/02-03 chegarem a qualquer ambiente compartilhado, e sua aplicação real precisa ser
uma decisão explícita do dono, não um efeito colateral de merge.
Output: `supabase/migrations/0033_anamnese_cardio_declarada.sql` (arquivo, não aplicado),
harness de teste que confere o conteúdo sem tocar banco, e a decisão registrada do dono sobre
quando aplicar.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `supabase/migrations/0033_anamnese_cardio_declarada.sql` (novo) — 3 colunas + CHECK + espelho + snapshot_questionario() + asserção
- `backend/tests/test_migration_anamnese_cardio.py` (novo) — harness de leitura do .sql, sem banco
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-anamnese-e-calibra-o-do-cardio/02-CONTEXT.md
@.planning/phases/02-anamnese-e-calibra-o-do-cardio/02-RESEARCH.md
@supabase/migrations/0021_dose_cardio_declarada.sql
@supabase/migrations/0008_questionario_usuario.sql
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migração 0033 (anamnese de cardio) + harness de teste local</name>
  <files>supabase/migrations/0033_anamnese_cardio_declarada.sql, backend/tests/test_migration_anamnese_cardio.py</files>
  <read_first>
    supabase/migrations/0021_dose_cardio_declarada.sql (INTEIRO — molde byte a byte: comentário de
    cabeçalho, `alter table ... add column if not exists`, bloco `do $$ ... end $$` com
    constraints condicionais via `if not exists (select 1 from pg_constraint where conname = '...')`,
    `comment on column`, espelho em `questionario_historico`, `create or replace function
    snapshot_questionario()` + `revoke all ... from public, anon` logo depois, bloco de asserção
    final com `pg_get_functiondef`); supabase/migrations/0008_questionario_usuario.sql:9-24 (tipos
    de coluna existentes — `peso_kg numeric`, RLS `auth.uid() = usuario_id`, grant só a
    `authenticated`, sem grant a `anon` — confirma que RLS por linha já cobre qualquer coluna nova
    sem precisar de policy adicional); backend/tests/test_plan_repository.py:132-167
    (`test_migration_declara_serializacao_rls_e_todas_as_insercoes`/
    `test_migration_0015_recoloca_progression_rules_sem_regredir_cardio` — padrão de harness já
    usado neste repo: `Path(REPO_ROOT) / "supabase" / "migrations" / "NNNN_....sql"`,
    `.read_text(...).lower()`, asserções de substring — SEM abrir conexão de banco); AGENTS.md
    linha 35 e 59 (migrations vão para staging primeiro; nunca aplicar por SQL direto sem
    registrar, sob pena de quebrar o próximo `supabase db push`).
  </read_first>
  <action>
    Confira primeiro `ls supabase/migrations/ | tail -3` — se já existir uma migração com número
    ≥ 0033, ajuste o número desta migração para o próximo livre (RESEARCH.md Assumption A2; no
    momento da pesquisa o maior era `0032_harden_m0031_backup_tables.sql`).
    Crie `supabase/migrations/0033_anamnese_cardio_declarada.sql` seguindo BYTE A BYTE a estrutura
    de `0021_dose_cardio_declarada.sql`: (1) comentário de cabeçalho explicando o que muda —
    anamnese de cardio para calibrar nível de experiência (cite REQ-04) e cite que
    `nivel_cardio_declarado()` (backend/services/dose_cardio.py) vai ler estas colunas; (2)
    `alter table public.questionario_usuario add column if not exists cardio_pratica_atualmente boolean, add column if not exists cardio_distancia_confortavel_km numeric, add column if not exists cardio_objetivo text;`;
    (3) bloco `do $$ ... end $$` com 4 constraints condicionais: `questionario_cardio_distancia_km_check`
    (`cardio_distancia_confortavel_km is null or cardio_distancia_confortavel_km between 0 and 50`
    — teto realista, mesmo espírito de `questionario_cardio_minutos_check` da 0021 que usa 5-180);
    `questionario_cardio_objetivo_check` (`cardio_objetivo is null or cardio_objetivo in ('condicionamento', 'completar_5k', 'emagrecimento')`
    — estes 3 literais EXATOS, os mesmos que o frontend vai usar como `value` das opções no Plano
    02-03); `questionario_cardio_distancia_coerente` (`cardio_distancia_confortavel_km is null or cardio_pratica_atualmente is true`
    — só quem pratica hoje declara distância confortável); `questionario_anamnese_cardio_coerente`
    (mesmo padrão de `questionario_cardio_dose_coerente` da 0021: `inclui_cardio or (cardio_pratica_atualmente is null and cardio_distancia_confortavel_km is null and cardio_objetivo is null)`
    — quem não quer cardio não carrega anamnese de cardio); (4) 3 `comment on column` descrevendo
    cada coluna e citando `nivel_cardio_declarado`; (5) espelho das 3 colunas em
    `questionario_historico` via `add column if not exists`; (6)
    `create or replace function public.snapshot_questionario()` reescrita para listar as 3 colunas
    novas no INSERT (nomes de coluna E `new.<coluna>` na MESMA ordem, por último — comparar com a
    versão atual da função, que hoje termina em
    `cardio_dias_semana, cardio_minutos_sessao, cardio_modalidades`) seguida de
    `revoke all on function public.snapshot_questionario() from public, anon;` logo depois do
    `create or replace` (reforça o padrão da 0021 — `CREATE OR REPLACE FUNCTION` preserva o ACL
    existente na prática, mas o revoke redundante já é a convenção estabelecida neste repo); (7)
    bloco de asserção final `do $$ declare v_col text; begin foreach v_col in array array['cardio_pratica_atualmente', 'cardio_distancia_confortavel_km', 'cardio_objetivo'] loop ... end loop; end $$;`
    idêntico em estrutura ao de 0021 (existência da coluna nas DUAS tabelas via
    `information_schema.columns` + `position(v_col in pg_get_functiondef('public.snapshot_questionario()'::regprocedure)) = 0`
    lançando `raise exception`). NÃO rode nenhum comando `supabase` (`db push`, `migration up`,
    etc.) — é decisão do dono, gated pela Task 2 deste plano.
    Crie `backend/tests/test_migration_anamnese_cardio.py`: lê
    `supabase/migrations/0033_anamnese_cardio_declarada.sql` como texto em minúsculas (mesmo padrão
    de `test_plan_repository.py:132-147`) e afirma: (a) as 3 colunas aparecem em
    `add column if not exists` DUAS vezes cada (uma por tabela); (b) os 4 nomes de constraint
    (`questionario_cardio_distancia_km_check`, `questionario_cardio_objetivo_check`,
    `questionario_cardio_distancia_coerente`, `questionario_anamnese_cardio_coerente`) aparecem no
    texto; (c) `create or replace function public.snapshot_questionario` aparece E as 3 colunas
    aparecem dentro do bloco de INSERT da função (mesma técnica simplificada de substring de
    `test_migration_0015_recoloca_progression_rules_sem_regredir_cardio`, sem parsear SQL de
    verdade); (d) `raise exception` aparece e as 3 colunas aparecem dentro do bloco de asserção
    final; (e) os 3 literais `'condicionamento'`, `'completar_5k'`, `'emagrecimento'` aparecem no
    texto (prova que o CHECK do objetivo usa o vocabulário certo).
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/test_migration_anamnese_cardio.py -q</automated>
  </verify>
  <acceptance_criteria>
    `python3 -m pytest backend/tests/test_migration_anamnese_cardio.py -q` verde;
    `ls supabase/migrations/ | tail -1` mostra `0033_anamnese_cardio_declarada.sql` (ou o número
    seguinte, se outra migração tiver sido mesclada entretanto — confirmar antes de nomear);
    `git status supabase/migrations/` mostra APENAS o arquivo novo (nenhuma migração existente
    foi editada); nenhum comando `supabase` aparece no histórico desta task.
  </acceptance_criteria>
  <done>Migration 0033 criada seguindo o molde 0021 byte a byte, com CHECK de faixa/coerência para as 3 colunas novas, espelho em questionario_historico e snapshot_questionario() reescrito; harness de teste comprova o conteúdo sem tocar banco vivo.</done>
  <reversibility rating="costly">O arquivo em si é revertível via git; uma vez aplicada a um banco vivo, reverter exige DDL manual e uma decisão sobre dado já gravado nas colunas novas — por isso a Task 2 (checkpoint) precede qualquer aplicação real, mesmo a migração em si sendo "só" um ADD COLUMN aditivo.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Quando aplicar a migration 0033 (supabase db push) em cada ambiente?</decision>
  <context>
    O arquivo `supabase/migrations/0033_anamnese_cardio_declarada.sql` está pronto e testado
    localmente (harness da Task 1), mas NÃO foi aplicado a nenhum banco — nem staging
    (`forcaapp-staging`), nem produção. Aplicar é ação manual (`supabase db push`, staging primeiro
    per AGENTS.md linha 35).

    RISCO OPERACIONAL se a ordem for invertida: o Plano 02-01 (mesma wave) já adiciona
    `cardio_pratica_atualmente` a `formDataForApi`/`QuestionnairePayload` no frontend, e o Plano
    02-03 vai somar os outros 2 campos. Se ESSE código chegar a um ambiente ANTES desta migration
    ser aplicada LÁ, `saveQuestionnaireDataAPI` (upsert em `questionario_usuario`,
    `src/services/api/questionnaireService.ts:43-45`) falha com coluna inexistente (PostgREST
    42703) — e `handleSubmit` (QuestionnaireScreen.tsx:420-423) relança esse erro para o catch
    externo, que mostra "Erro ao Salvar" e INTERROMPE a submissão. Isso quebra TODO o questionário
    para TODO MUNDO nesse ambiente, não só o campo novo — mesma classe de risco que a migration
    0021 já teve, historicamente mitigada aplicando a migration antes do deploy do frontend
    correspondente.
  </context>
  <options>
    <option id="option-a">
      <name>Aplicar agora, staging primeiro</name>
      <pros>Destrava os Planos 02-01/02-03 para chegar a staging sem acumular risco de deploy; segue o fluxo já documentado em AGENTS.md.</pros>
      <cons>Exige rodar `supabase db push` manualmente em staging e depois produção antes de qualquer merge para essas branches.</cons>
    </option>
    <option id="option-b">
      <name>Aguardar e aplicar mais perto do deploy</name>
      <pros>Não força uma janela de manutenção agora, enquanto o resto da fase ainda está em execução.</pros>
      <cons>Quem mesclar o código dos Planos 02-01/02-03 para staging/produção SEM checar que a migration já rodou LÁ quebra o onboarding inteiro nesse ambiente — exige reconferência manual antes de cada merge.</cons>
    </option>
  </options>
  <resume-signal>Digite "option-a" ou "option-b"; se "option-b", confirme que vai reconferir a aplicação da migration antes de cada merge para staging/produção.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Arquivo de migração (git) → banco vivo | A aplicação é ação manual e deliberada (`supabase db push`), fora do escopo automatizado deste plano — o checkpoint existe justamente para essa fronteira. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-04 | Tampering | `cardio_distancia_confortavel_km` com valor absurdo (ex.: 9999 km) virando alvo impossível para o molde | medium | mitigate | CHECK `between 0 and 50` na migração — mesmo padrão de `questionario_cardio_minutos_check` (0021, faixa 5-180). |
| T-02-05 | Elevation of Privilege | `snapshot_questionario()` (SECURITY DEFINER) reescrita por esta migração | medium | mitigate | `revoke all on function ... from public, anon` reaplicado logo após o `create or replace` (mesmo padrão de 0021); nenhum objeto/tabela nova é criado — RLS de `questionario_usuario` (0008) já cobre as colunas novas automaticamente (RLS é por linha, não por coluna), sem precisar de policy adicional. |
</threat_model>

<verification>
- `python3 -m pytest backend/tests/test_migration_anamnese_cardio.py -q` verde.
- `git diff backend/schemas/molde_schema.py` vazio (esta fase não muda o schema do molde).
- Checkpoint (Task 2) respondido antes de considerar este plano encerrado.
</verification>

<success_criteria>
Contribui para o Success Criteria 1 do ROADMAP Fase 2 (persistência das respostas de anamnese,
com CHECK de faixa/coerência) e garante que nenhuma aplicação de schema aconteça sem decisão
explícita do dono.
</success_criteria>

<output>
Criar `.planning/phases/02-anamnese-e-calibra-o-do-cardio/02-02-SUMMARY.md` ao final, incluindo a
resposta do checkpoint.
</output>
