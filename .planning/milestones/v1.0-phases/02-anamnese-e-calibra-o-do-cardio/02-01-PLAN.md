---
phase: 02-anamnese-e-calibra-o-do-cardio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/QuestionnaireScreen.tsx
  - src/services/api/questionnaireService.ts
  - __tests__/questionnaireScreen.test.tsx
  - backend/services/dose_cardio.py
  - backend/app.py
  - backend/tests/test_dose_cardio.py
autonomous: true
requirements: [REQ-04, REQ-05]

estimate:
  tokens: 78000
  raw_tokens: 56000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A pergunta 'Já pratica corrida, caminhada rápida, pedal ou outro cardio hoje?' aparece no passo de cardio (includeCardio===true) e a resposta chega ao payload como cardio_pratica_atualmente (boolean exato) — provado em teste de payload no frontend."
    - "O backend deriva um nível de cardio (iniciante/intermediario/avancado) deterministicamente de cardio_pratica_atualmente/cardio_distancia_confortavel_km, sem nunca lançar exceção para dado ausente ou malformado."
    - "A instrução de calibração (nível + teto de progressão) entra na parte VOLÁTIL do prompt do molde (mensagem por aluno), nunca na parte estável/cacheada — provado em teste."
  artifacts:
    - src/screens/QuestionnaireScreen.tsx
    - backend/services/dose_cardio.py
    - backend/app.py
  key_links:
    - "formDataForApi.cardio_pratica_atualmente (QuestionnaireScreen.tsx) -> questionnaire_data.get('cardio_pratica_atualmente') (dose_cardio.py::nivel_cardio_declarado) -> _instrucao_calibracao_cardio (app.py) -> dose_cardio_str -> _montar_chamada_do_molde (parte volátil)"
---

<objective>
Fechar a fatia tracer da Fase 2 (D: REQ-04/REQ-05, CONTEXT.md "perguntas objetivas NO QUESTIONÁRIO" +
"dose inicial conservadora e teto de progressão por nível"): UMA pergunta nova de anamnese
("já pratica cardio hoje?") atravessando tela → payload → backend → prompt, com teste de
payload nos dois lados (lição do commit `011509f`/PR #64 citada no RESEARCH.md), e o motor
completo de derivação de nível + instrução de calibração no backend (`nivel_cardio_declarado`,
`TETO_PROGRESSAO_POR_NIVEL`, `_instrucao_calibracao_cardio`) — já preparado para os 3 campos de
anamnese que o Plano 02-03 vai terminar de wire na UI.

Purpose: a pesquisa confirmou que uma chave de payload divergente entre `QuestionnaireScreen.tsx`
e o `.get()` do backend faz o bloco de calibração desaparecer do prompt em SILÊNCIO (sem
exceção, sem log) — exatamente a classe de bug do commit `011509f`. Provar essa trilha com o
campo MAIS simples (booleano) antes de investir nos outros 2 campos (Plano 02-03) reduz o risco
arquitetural ao menor commit possível. A calibração vive em `_instrucao_calibracao_cardio`
(função irmã de `_instrucao_dose_cardio`, concatenada ao mesmo `dose_cardio_str`), NUNCA em
`_INSTRUCOES_MOLDE` (bloco estável que o plano 01-04 da Fase 1 já editou — ver Pitfall 2 do
RESEARCH.md).
Output: `cardio_pratica_atualmente` capturado e persistido em memória local do questionário,
`nivel_cardio_declarado`/`TETO_PROGRESSAO_POR_NIVEL` (backend/services/dose_cardio.py) e
`_instrucao_calibracao_cardio` (backend/app.py) completos e testados, sem nenhuma mudança em
`backend/schemas/molde_schema.py`.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `src/screens/QuestionnaireScreen.tsx` (editado) — estado + pergunta + payload de `cardio_pratica_atualmente`
- `src/services/api/questionnaireService.ts` (editado) — `QuestionnairePayload.cardio_pratica_atualmente`
- `__tests__/questionnaireScreen.test.tsx` (editado) — payload novo em `preencherTudo`/asserção de submissão
- `backend/services/dose_cardio.py` (editado) — `nivel_cardio_declarado`, `TETO_PROGRESSAO_POR_NIVEL`
- `backend/app.py` (editado) — `_instrucao_calibracao_cardio`, concatenação no `_executar_geracao_molde`
- `backend/tests/test_dose_cardio.py` (editado) — `TestNivelCardioDeclarado`, `TestCalibracaoNoPrompt`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-anamnese-e-calibra-o-do-cardio/02-CONTEXT.md
@.planning/phases/02-anamnese-e-calibra-o-do-cardio/02-RESEARCH.md
@src/screens/QuestionnaireScreen.tsx
@src/services/api/questionnaireService.ts
@backend/services/dose_cardio.py
@backend/app.py
@backend/schemas/molde_schema.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Uma pergunta de anamnese ponta a ponta — "já pratica cardio hoje?" até o payload</name>
  <files>src/screens/QuestionnaireScreen.tsx, src/services/api/questionnaireService.ts, __tests__/questionnaireScreen.test.tsx</files>
  <read_first>
    QuestionnaireScreen.tsx:116-123 (estados de cardio: `includeCardio`/`cardioDias`/`cardioMinutos`/
    `cardioModalidades` — molde para o estado novo); :210-250 (`loadSavedData` — hidratação campo a
    campo do rascunho: `setCardioDias(typeof data.cardio_dias_semana === 'number' ? ... : null)` é o
    molde exato para o campo novo); :271-278 (`definirIncluiCardio` — limpa a dose ao desligar
    cardio; a limpeza precisa incluir o campo novo, senão sobra dado órfão que a migração 0033 do
    Plano 02-02 recusaria); :289-316 (`blocosRespondidos()` — índice 8 é o bloco de cardio, condição
    atual em 310-311); :388-395 (`formDataForApi` — onde a chave nova entra, mesmo padrão de
    `cardio_dias_semana`); :656-719 (bloco JSX `{includeCardio === true && (...)}` — inserir o campo
    novo entre "Prefere alguma modalidade?" (699-715) e `{botaoContinuar(blocos[8])}` (717));
    src/components/ui/Controls.tsx:26-67 (`OptionButtonProps`/`OptionButton` — `accessibilityLabel={label}`
    direto, sem outro identificador: dois pares "Sim"/"Não" visíveis ao mesmo tempo colidiriam em
    teste — usar rótulos distintos do par de `includeCardio`); src/services/api/questionnaireService.ts:11-28
    (`QuestionnairePayload` — precisa ganhar o campo novo, senão `formDataForApi` passado como
    literal a `saveQuestionnaireDataAPI` reprova em `tsc --noEmit` por excess-property-check);
    __tests__/questionnaireScreen.test.tsx:62-107 (`preencherTudo` — helper compartilhado por TODOS
    os testes do arquivo; o press do campo novo entra aqui, entre '30 min' e o `Continuar` do passo
    de cardio) e :249-273 (asserção do payload em `objectContaining`).
  </read_first>
  <action>
    Em `QuestionnaireScreen.tsx`, adicione `const [cardioPraticaAtualmente, setCardioPraticaAtualmente] = useState<boolean | null>(null);`
    logo após a declaração de `cardioModalidades` (linha 123). Em `definirIncluiCardio`, no ramo
    `if (!valor)`, chame também `setCardioPraticaAtualmente(null)` (mesma limpeza de `cardioDias`/
    `cardioMinutos`/`cardioModalidades`). Em `loadSavedData`, adicione a hidratação
    `setCardioPraticaAtualmente(typeof data.cardio_pratica_atualmente === 'boolean' ? data.cardio_pratica_atualmente : null)`,
    no mesmo padrão de `setCardioDias`. Em `blocosRespondidos()`, índice 8 (linha 310-311), estenda
    a condição para exigir `cardioPraticaAtualmente !== null` quando `includeCardio === true`
    (mantendo `includeCardio === false` como saída antecipada, igual hoje).
    No JSX do bloco `{includeCardio === true && (...)}`, insira um novo `<View style={styles.field}>`
    IMEDIATAMENTE após o bloco "Prefere alguma modalidade?" (linha ~715) e ANTES de
    `{botaoContinuar(blocos[8])}` (linha 717): título "Já pratica corrida, caminhada rápida, pedal
    ou outro cardio hoje?", um par de `OptionButton` no estilo `styles.pair`/`styles.pairItem`
    (mesmo padrão visual de `includeCardio`, linhas 637-654) com rótulos EXATOS "Sim, já pratico" e
    "Não, ainda não" (rótulos diferentes de "Sim"/"Não" DE PROPÓSITO — os dois pares ficam visíveis
    ao mesmo tempo na tela e `OptionButton` usa `label` como `accessibilityLabel` sem outro
    identificador; "Sim"/"Não" colidiria com o par de `includeCardio` já visível no mesmo passo).
    `onPress` de cada botão chama `setCardioPraticaAtualmente(true)`/`setCardioPraticaAtualmente(false)`
    (sem `selecionarEAvancar` — mesmo padrão dos outros campos de dose, que não avançam sozinhos).
    Em `formDataForApi` (linha 388-395), adicione a chave
    `cardio_pratica_atualmente: includeCardio === true ? cardioPraticaAtualmente : null,` no MESMO
    padrão de `cardio_dias_semana`/`cardio_minutos_sessao` (linha 392-393) — literal
    `cardio_pratica_atualmente`, EXATAMENTE esta string, é a chave que a Task 2 deste plano vai ler
    no backend.
    Em `src/services/api/questionnaireService.ts`, adicione `cardio_pratica_atualmente: boolean | null;`
    ao tipo `QuestionnairePayload`, logo após `cardio_modalidades`.
    Em `__tests__/questionnaireScreen.test.tsx`, dentro de `preencherTudo` (depois do press em
    '30 min' e ANTES do `fireEvent.press(getByLabelText('Continuar'))` do passo de cardio, linha
    ~99-100), adicione `fireEvent.press(getByLabelText('Sim, já pratico'));`. Na asserção
    `objectContaining` do teste de submissão (linha ~249-273), adicione `cardio_pratica_atualmente: true,`
    no mesmo bloco de comentário "Dose declarada (0021)".
  </action>
  <verify>
    <automated>npx jest __tests__/questionnaireScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/questionnaireScreen.test.tsx` verde (inclui a asserção nova
    `cardio_pratica_atualmente: true` no payload); `npx tsc --noEmit` verde (confirma que
    `QuestionnairePayload` foi atualizado, senão `formDataForApi` reprova por excess-property-check);
    `grep -c "cardio_pratica_atualmente" src/screens/QuestionnaireScreen.tsx` retorna pelo menos 3
    (estado, hidratação, payload).
  </acceptance_criteria>
  <done>A pergunta "Já pratica corrida..." aparece no passo de cardio quando includeCardio===true, a resposta é obrigatória para habilitar Continuar, e cardio_pratica_atualmente (boolean exato) chega ao formDataForApi/QuestionnairePayload — provado em teste.</done>
  <reversibility rating="reversible">Campo de UI + tipo TS; revertível via git revert. Backend trata a ausência da chave como neutro (retorna "" na instrução), então nenhum contrato de API quebra mesmo se este código rodar antes da migração do Plano 02-02 estar aplicada — o risco de deploy fica só do lado da GRAVAÇÃO no Supabase, documentado no checkpoint do Plano 02-02.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Nível de cardio declarado → instrução de calibração no prompt (motor completo no backend)</name>
  <files>backend/services/dose_cardio.py, backend/app.py, backend/tests/test_dose_cardio.py</files>
  <precondition>backend/app.py já tem 9 itens numerados em _INSTRUCOES_MOLDE (Fase 1, plano 01-04, mesclado nesta branch — confirmado via `git log --oneline -1 -- backend/app.py` mostrando o commit "feat(01-04): instruir o prompt do molde..."). Se o grep de itens numerados mostrar 8 em vez de 9, a Fase 1 wave 1 ainda não chegou nesta branch e este plano deve aguardar.</precondition>
  <read_first>
    backend/services/dose_cardio.py:1-20 (docstring do módulo — as DUAS linhas que ele não cruza:
    nunca reprova por impossibilidade, nunca levanta exceção — `nivel_cardio_declarado` segue a
    MESMA regra); :35-49 (`_inteiro_na_faixa` — padrão "fora da faixa vira None, não vira coação",
    e o cuidado de excluir `bool` explicitamente porque é subclasse de `int` em Python); :95-113
    (`dose_declarada` — o MESMO shape de função que `nivel_cardio_declarado` deve seguir: lê o
    dict, devolve None quando não há dado suficiente, nunca lança); backend/app.py:1527-1574
    (`_instrucao_dose_cardio` — padrão de função de instrução: retorna "" quando não há dado) e
    :1777-1782 (`_executar_geracao_molde` — ponto exato onde `_instrucao_dose_cardio(questionnaire_data)`
    já é passado a `_montar_chamada_do_molde`; a instrução de calibração se concatena AQUI, não
    dentro de `_instrucao_dose_cardio`); backend/schemas/molde_schema.py:233-238
    (`delta_cardio_percentual` — `minimum: 1.0, maximum: 10.0` — QUALQUER teto por nível tem de
    caber dentro desta faixa, nunca pedir mais); backend/tests/test_dose_cardio.py:66-72
    (`QUEST_BASE` fixture) e :310-376 (`TestDoseNoPrompt` — molde exato a replicar:
    `test_dose_entra_na_chamada_do_molde_como_instrucao` prova que o bloco entra na parte volátil
    via `app._montar_chamada_do_molde(..., app._instrucao_dose_cardio(QUEST_BASE))`).
  </read_first>
  <action>
    Em `backend/services/dose_cardio.py`, adicione a constante
    `TETO_PROGRESSAO_POR_NIVEL: Dict[str, float] = {"iniciante": 3.0, "intermediario": 6.0, "avancado": 10.0}`
    (os 3 valores DENTRO de [1.0, 10.0], nunca ultrapassando o teto do schema) e a função
    `nivel_cardio_declarado(questionario: Any) -> Optional[str]`, que: devolve `None` se
    `questionario` não for dict; lê `cardio_pratica_atualmente` — se não for `bool` (isinstance
    estrito), devolve `None` (sem dado); se `False`, devolve `"iniciante"` direto (quem não pratica
    hoje começa conservador, independente de qualquer outra resposta); se `True`, lê
    `cardio_distancia_confortavel_km` — valor que não seja `int`/`float` (excluindo `bool`) ou fora
    de `(0, 50]` vira `None`; com distância válida, `< 3.0` devolve `"iniciante"`, `< 8.0` devolve
    `"intermediario"`, senão `"avancado"`; SEM distância válida mas com prática confirmada, devolve
    `"intermediario"` (meio-termo — nem o mais conservador nem o mais agressivo, dado incompleto).
    Nunca levanta exceção — todo `isinstance` malformado cai em `None`, nunca em `raise`.
    Em `backend/app.py`, adicione (próximo a `_instrucao_dose_cardio`, linha ~1527-1574) o
    dicionário `_TEXTO_NIVEL_CARDIO` (chaves `"iniciante"`/`"intermediario"`/`"avancado"`, cada uma
    com um texto que MENCIONA A PALAVRA em maiúsculas — ex.: "...nível INICIANTE. Comece pelo piso
    da faixa aceita e ritmo leve (caminhada ou trote leve) na semana 1..." — os testes vão procurar
    essa palavra em maiúsculas) e a função `_instrucao_calibracao_cardio(questionnaire_data) -> str`,
    que importa `TETO_PROGRESSAO_POR_NIVEL`/`nivel_cardio_declarado` de `backend.services.dose_cardio`,
    devolve `""` quando `nivel_cardio_declarado(...)` é `None`, e caso contrário devolve um bloco
    começando com a string literal `"CALIBRAÇÃO DE CARDIO"` seguido do texto do nível e uma linha
    citando a string literal `"TETO DE PROGRESSÃO"` com o valor de `TETO_PROGRESSAO_POR_NIVEL[nivel]`
    formatado sem casas decimais desnecessárias (`f"{teto:g}"`).
    No corpo de `_executar_geracao_molde` (linha ~1777-1782), troque a chamada atual
    `_montar_chamada_do_molde(questionnaire_str, diretrizes_str, catalogo_str, _instrucao_dose_cardio(questionnaire_data))`
    por uma que primeiro monta
    `dose_cardio_str = "\n\n".join(filter(None, [_instrucao_dose_cardio(questionnaire_data), _instrucao_calibracao_cardio(questionnaire_data)]))`
    e então passa essa `dose_cardio_str` como quarto argumento — preserva o comportamento atual
    quando só a dose existe (o `filter(None, ...)` descarta strings vazias) e soma os dois blocos
    quando ambos existem.
    Em `backend/tests/test_dose_cardio.py`, adicione a classe `TestNivelCardioDeclarado` com 6
    testes (pratica+distância curta → iniciante; pratica+distância média → intermediário;
    pratica+distância longa → avançado; não pratica + qualquer distância → iniciante SEMPRE;
    pratica sem distância → intermediário; dict vazio/`cardio_pratica_atualmente: None` → `None`) e
    a classe `TestCalibracaoNoPrompt` (bloco cita "INICIANTE" e o teto "3" para quem não pratica;
    dict vazio devolve `""`; a chamada `app._montar_chamada_do_molde("{}", "{}", "Cardio: Corrida", app._instrucao_calibracao_cardio({...}))`
    entra na parte VOLÁTIL e NUNCA na parte estável, mesmo padrão de
    `test_dose_entra_na_chamada_do_molde_como_instrucao`; um teste parametrizado que confere
    `1.0 <= TETO_PROGRESSAO_POR_NIVEL[nivel] <= 10.0` para os 3 níveis).
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/test_dose_cardio.py -q</automated>
  </verify>
  <acceptance_criteria>
    `python3 -m pytest backend/tests/test_dose_cardio.py -q` verde, incluindo as 2 classes novas;
    `python3 -c "import backend.app as app; print('CALIBRAÇÃO DE CARDIO' in app._instrucao_calibracao_cardio({'cardio_pratica_atualmente': False}))"`
    imprime `True`; `grep -n "cardio_pratica_atualmente" backend/app.py backend/services/dose_cardio.py`
    mostra a MESMA string literal que a Task 1 usou no `.tsx` (cross-check manual contra o diff do
    frontend — lição PR #64); `git diff backend/schemas/molde_schema.py` vazio.
  </acceptance_criteria>
  <done>nivel_cardio_declarado deriva o nível dos 2 sinais (pratica/distância) com as regras acima, nunca lança exceção; _instrucao_calibracao_cardio compõe o texto e entra na parte volátil do prompt via _executar_geracao_molde; testes verdes; nenhuma mudança no schema do molde.</done>
  <reversibility rating="reversible">Funções puras + texto de prompt; revertível via git, sem contrato de API/schema alterado.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Aluno (questionário) → payload local (`formDataForStorage`) → prompt pago da IA | Canal já existente desde a dose declarada (migration 0021); este plano soma UM sinal booleano fechado ao mesmo canal, não abre canal novo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-01 | Tampering (prompt injection) | `cardio_pratica_atualmente` na instrução de calibração | low | accept | Campo é `boolean` estrito (`isinstance(valor, bool)`) — não há string livre do aluno neste campo para injetar; valor fora do tipo vira `None` (neutro), nunca vira texto no prompt. |
| T-02-02 | Repudiation / desaparecimento silencioso (classe PR #64) | Nome de chave divergente entre `formDataForApi` e `.get()` do backend | high | mitigate | Teste de payload obrigatório nos dois lados (Task 1 frontend, Task 2 backend) usando a MESMA string literal `cardio_pratica_atualmente`, cross-checada por `grep` na acceptance_criteria de cada task. |
| T-02-03 | Information Disclosure | Nenhum dado sensível novo | low | accept | Mesma RLS existente de `questionario_usuario` (`auth.uid() = usuario_id`, migration 0008) — RLS é por linha, cobre a coluna nova automaticamente; nenhuma tabela/rota nova nesta plano. |
</threat_model>

<verification>
- `npx jest __tests__/questionnaireScreen.test.tsx` verde.
- `python3 -m pytest backend/tests/test_dose_cardio.py -q` verde.
- `npx tsc --noEmit` verde.
- `git diff backend/schemas/molde_schema.py` vazio (nenhuma mudança de schema nesta fase).
</verification>

<success_criteria>
Contribui para o Success Criteria 1 (parcial — 1 de 3 campos de anamnese ponta a ponta, com teste
de payload) e para o Success Criteria 2 (motor completo de nível/teto de progressão pronto e
testado, ainda sem os outros 2 sinais de anamnese que o Plano 02-03 adiciona) do ROADMAP Fase 2.
</success_criteria>

<output>
Criar `.planning/phases/02-anamnese-e-calibra-o-do-cardio/02-01-SUMMARY.md` ao final.
</output>
