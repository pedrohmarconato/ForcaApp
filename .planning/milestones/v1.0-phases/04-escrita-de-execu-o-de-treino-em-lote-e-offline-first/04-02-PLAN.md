---
phase: 04-escrita-de-execu-o-de-treino-em-lote-e-offline-first
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/store/activeSessionStore.ts
  - src/services/sessionOutboxDrain.ts
  - __tests__/activeSessionStore.test.ts
autonomous: true
requirements: [REQ-07]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "resolveAdaptation enfileira update_set_log_adaptation com plannedSetId (nunca setLogId, que não existe mais synchronamente) e a decisão automática de 'manter' (auto-keep) dentro de completeSet também passa a enfileirar em vez de chamar updateSetLogAdaptation direto — fecha o gap deixado explicitamente pelo 04-01-PLAN.md."
    - "skipExercise, unskipExercise e swapExercise aplicam a mudança no draft local IMEDIATAMENTE após enfileirar (sem esperar a RPC), e uma falha de transporte ou servidor-não-classificado em qualquer uma delas NÃO seta store().saveError — extensão do D-05 às 4 operações que a lista do D-01 ainda cobre além de save_set_log/update_set_log_adaptation."
    - "A guarda local CR-01 (recusar troca de modalidade quando já há série concluída, hoje em swapExercise) continua rodando ANTES do enfileiramento — nenhuma chamada de rede/fila acontece para uma troca já inválida localmente (invariante pré-existente preservada, não regride)."
    - "finishSession marca a sessão como 'finished' e chama retireLocalDraft SEM aguardar a confirmação de finish_session no servidor; o item finish_session fica pendente na fila (storage separado de sessionDraftStorage, D-09) e drena em segundo plano mesmo depois do draft já ter sido limpo — D-08."
    - "skipWholeSession (RPC skip_planned_session) NÃO é tocada por este plano — D-01 lista exatamente 6 operações e skip_planned_session não está entre elas; permanece write-through síncrona como start_session."
  artifacts: []
  key_links:
    - "activeSessionStore.finishSession -> sessionOutboxDrain.enqueueAndDrain(kind:'finish_session') -> sessionExecutionRepository.finishSessionLog -> RPC finish_session (idempotente, migration 0004) — o item sobrevive à limpeza do draft porque mora em storage PRÓPRIO (@session_outbox_<userId>, D-09), não em @active_session_draft_*."
    - "activeSessionStore.resolveAdaptation -> sessionOutboxDrain.enqueueAndDrain(kind:'update_set_log_adaptation', payload com plannedSetId+plannedSessionId) -> resolução tardia via getOpenSessionLog (construída no 04-01-PLAN.md) -> sessionExecutionRepository.updateSetLogAdaptation."
  prohibitions:
    - statement: "skipWholeSession (skip_planned_session) NUNCA passa a enfileirar nesta fase — não está na lista fechada do D-01; ampliar o escopo silenciosamente contradiria a decisão travada."
      status: kept
      verification: unverified
---

<objective>
Fecha a lista do D-01: as 4 operações que faltam (`skip_session_exercise`,
`unskip_session_exercise`, `swap_session_exercise`, `finish_session`) passam a
enfileirar via `sessionOutboxDrain.enqueueAndDrain` em vez de aguardar a RPC direto
— mesmo padrão otimista que `completeSet` já usa desde o `04-01-PLAN.md`. Fecha
também o gap explícito deixado pelo 04-01: `update_set_log_adaptation` (decisão
automática dentro de `completeSet` E decisão manual em `resolveAdaptation`) volta
a persistir no servidor, agora via fila com resolução tardia de `setLogId`.

Purpose: sem este plano, o REQ-07 ficaria parcialmente implementado — só
`save_set_log` seria offline-first; recusar um exercício, trocar modalidade ou
finalizar o treino continuariam travando a tela em soluço de rede.
Output: `activeSessionStore.ts` com as 6 operações do D-01 todas passando pela
fila; `sessionOutboxDrain.ts` com dispatcher completo (6 kinds).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-CONTEXT.md
@.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-RESEARCH.md
@.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-01-SUMMARY.md
@src/store/activeSessionStore.ts
@src/services/sessionOutboxDrain.ts
@src/services/sessionExecutionRepository.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `src/store/activeSessionStore.ts` (EDITADO, sem arquivo novo): campo `plannedSetId: string` adicionado
  ao tipo `PendingAdaptation`; nenhuma função nova exportada — `skipExercise`/`unskipExercise`/
  `swapExercise`/`finishSession`/`resolveAdaptation` reescritos internamente, mesma assinatura pública.
- `src/services/sessionOutboxDrain.ts` (EDITADO, sem arquivo novo): dispatcher interno de `kind` estendido
  com 5 cases (`update_set_log_adaptation`, `skip_session_exercise`, `unskip_session_exercise`,
  `swap_session_exercise`, `finish_session`) — `enqueueItem`/`drainAll`/`enqueueAndDrain` (já exportados
  pelo 04-01-PLAN.md) mantêm a mesma assinatura pública.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: update_set_log_adaptation via fila (auto-keep + resolveAdaptation)</name>
  <files>src/store/activeSessionStore.ts, src/services/sessionOutboxDrain.ts</files>
  <read_first>
    src/store/activeSessionStore.ts — releia `completeSet` COMO FICOU depois do 04-01-PLAN.md (o
    comentário deixado na linha onde `updateSetLogAdaptation` era chamada direto, em torno da antiga
    linha 1349-1353, referenciando este plano) e o bloco de `pending`/adaptação automática (antiga faixa
    1300-1365) — ambos os pontos onde `PendingAdaptation`/`autoKeep` são construídos usam hoje `plannedSetId`
    disponível localmente (`serie.plannedSetId`) e `draft.plannedSessionId`; `resolveAdaptation` (antiga
    faixa 1397-1434) — o campo que ela lê hoje é `pending.setLogId`, que o 04-01 deixou sempre `null`.
    src/services/sessionOutboxDrain.ts — releia o `switch`/dispatcher de `kind` dentro de `drainAll`
    (construído no 04-01) e a resolução tardia de `setLogId` já implementada para
    `update_set_log_adaptation` (via `getOpenSessionLog`) — ela já espera um payload com `plannedSetId`;
    confirme se falta `plannedSessionId` no payload (necessário porque `getOpenSessionLog(userId,
    plannedSessionId)` exige os dois parâmetros) e adicione se faltar.
  </read_first>
  <behavior>
    - `PendingAdaptation` (tipo já existente): mantém `setLogId: string | null` (compatibilidade, sempre
      `null` a partir de agora) e passa a incluir `plannedSetId: string` — usado por `resolveAdaptation`.
    - Dentro de `completeSet`, ao construir `pending` (série fora do alvo, ajuste pendente) e `autoKeep`
      (decisão automática de "manter"): inclua `plannedSetId: serie.plannedSetId`.
    - Para o auto-keep: em vez da chamada direta removida no 04-01, chame
      `enqueueAndDrain(draft.userId, { sessionLogId: sid, kind: 'update_set_log_adaptation', payload: {
      plannedSetId: serie.plannedSetId, plannedSessionId: draft.plannedSessionId, adaptation: autoKeep,
      decision } }, { onSessionClosed: ..., onSummaryChanged: ... })` — best-effort, sem bloquear o retorno
      de `completeSet` (já não bloqueia, por já estar depois do commit otimista).
    - `resolveAdaptation`: substitui a chamada direta a `updateSetLogAdaptation(pending.setLogId, ...)` por
      `enqueueAndDrain(get().draft?.userId ?? ..., { sessionLogId: pending.sessionLogId!, kind:
      'update_set_log_adaptation', payload: { plannedSetId: pending.plannedSetId, plannedSessionId: ...,
      adaptation: adjustment, decision } }, callbacks)` — o `userId`/`plannedSessionId` vêm do `draft`
      corrente se ainda for a mesma sessão (`atual.sessionLogId === pending.sessionLogId`), senão do
      último `draft` conhecido antes de trocar (mesma cautela de CAS que o resto do arquivo já pratica —
      NUNCA envie para a sessão errada).
  </behavior>
  <action>
    Implemente o `<behavior>` acima. Rode `npx tsc --noEmit` para confirmar que `PendingAdaptation` e os
    dois pontos de construção (`pending`, `autoKeep`) continuam consistentes. Adicione/ajuste testes em
    `__tests__/sessionOutboxDrain.test.ts` (do 04-01) SE a resolução tardia precisar do campo
    `plannedSessionId` que ainda não estava coberto — rode o teste ANTES de alterar o payload (RED) e
    confirme GREEN depois.
  </action>
  <verify>
    <automated>npx jest __tests__/sessionOutboxDrain.test.ts -q && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "updateSetLogAdaptation(" src/store/activeSessionStore.ts` não retorna NENHUMA ocorrência (a
    store não chama mais o repositório direto para esta RPC — só a fila chama, dentro de
    `sessionOutboxDrain.ts`). `npx tsc --noEmit` sem erro novo. `npx jest __tests__/sessionOutboxDrain.test.ts
    -q` verde, incluindo o caso de resolução tardia com `plannedSessionId`.
  </acceptance_criteria>
  <done>Toda decisão de adaptação (automática ou escolhida pelo aluno) persiste via fila, com resolução
  tardia de setLogId — nenhuma chamada direta a updateSetLogAdaptation resta na store.</done>
</task>

<task type="auto">
  <name>Task 2: skip/unskip/swap/finish via fila</name>
  <files>src/store/activeSessionStore.ts, src/services/sessionOutboxDrain.ts</files>
  <read_first>
    src/store/activeSessionStore.ts — releia `skipExercise` (faixa 1436-1486 na versão pré-04-01, confirme
    a faixa atual pós-04-01), `unskipExercise` (faixa 1488-1516), `swapExercise` (faixa 1518-1577 — note a
    guarda CR-01 local, `alvo.sets.some((s) => s.status === 'done')`, ANTES de qualquer chamada de rede —
    essa guarda NÃO muda de lugar), `finishSession` (faixa 1624-1660 — note `retireLocalDraft(atual)` no
    final, que chama `clearDraft` de `sessionDraftStorage.ts`; storage da fila é OUTRO arquivo/chave, D-09,
    não é afetado por essa limpeza).
    src/services/sessionExecutionRepository.ts linhas 276-364 (`skipSessionExercise`, `swapSessionExercise`,
    `unskipSessionExercise` — params exatos a carregar no payload do item de fila) e linha 535+
    (`finishSessionLog(sessionLogId)` — sem params além do id).
    src/services/sessionOutboxDrain.ts — releia o dispatcher de `kind` construído no 04-01 (hoje só trata
    `save_set_log`/`update_set_log_adaptation`) — adicione os 4 cases que faltam, reaproveitando A MESMA
    lógica de classificação de erro/backoff/quarentena/P0001 já existente (nenhuma duplicação de pipeline,
    só o `switch` de "qual RPC chamar" cresce).
  </read_first>
  <action>
    Em `sessionOutboxDrain.ts`, estenda o `switch`/dispatcher de `kind` dentro de `drainAll` com os 4 cases
    que faltam: `skip_session_exercise` → chama `skipSessionExercise({ sessionLogId, plannedExerciseId,
    reason, note })`; `unskip_session_exercise` → `unskipSessionExercise({ sessionLogId,
    plannedExerciseId })`; `swap_session_exercise` → `swapSessionExercise({ sessionLogId,
    plannedExerciseId, toModality, note })`; `finish_session` → `finishSessionLog(sessionLogId)`. Todos sob
    o MESMO `withTimeout(..., RPC_TIMEOUT_MS)` e a MESMA classificação de erro já usada para
    `save_set_log` — não duplique a lógica de retry/quarentena/P0001, só o mapeamento kind→chamada.

    Em `activeSessionStore.ts`, reescreva `skipExercise`/`unskipExercise`/`swapExercise` no MESMO molde de
    `completeSet` pós-04-01: mantenha toda validação local (exercício já recusado é no-op, guarda CR-01 do
    swap, exercício não trocado é no-op) INTACTA e ANTES de qualquer chamada de fila; troque o
    `await skipSessionExercise(...)`/`swapSessionExercise(...)`/`unskipSessionExercise(...)` direto por
    `enqueueAndDrain(draft.userId, { sessionLogId: sid, kind: '<kind correspondente>', payload }, {
    onSessionClosed, onSummaryChanged })`; aplique a mudança local (`applyExerciseSkipToDraft`/
    `removeExerciseSkipFromDraft`/`applyCardioSwapToDraft`) IMEDIATAMENTE após o `enqueueAndDrain` resolver
    (que é rápido — só persistência local, não a rede), sem esperar confirmação; NUNCA setar `saveError`
    para falha de rede nestas 3 ações a partir de agora (só para falha de validação local, que continua
    igual).

    Reescreva `finishSession` no mesmo molde: `enqueueAndDrain(draft.userId, { sessionLogId: sid, kind:
    'finish_session', payload: {} }, callbacks)`; em seguida `set({ draft: {...atual, status:'finished'},
    status:'finished', saveError:null })` e `await retireLocalDraft(atual)` — SEM aguardar a confirmação do
    `finish_session` no servidor (D-08: a tela de fim aparece como hoje, a fila drena depois, inclusive
    após reabrir o app).

    NÃO toque `skipWholeSession` (chama `skipPlannedSession`) — fica fora da lista D-01, permanece
    write-through síncrona como está hoje.
  </action>
  <verify>
    <automated>npx jest __tests__/activeSessionStore.test.ts -q && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "await skipSessionExercise(\|await unskipSessionExercise(\|await swapSessionExercise(\|await finishSessionLog(" src/store/activeSessionStore.ts`
    não retorna NENHUMA ocorrência dentro de `skipExercise`/`unskipExercise`/`swapExercise`/`finishSession`
    (só dentro de `sessionOutboxDrain.ts` a partir de agora). `grep -n "skipPlannedSession(" src/store/activeSessionStore.ts`
    continua retornando a chamada direta original (skipWholeSession INTOCADA). `npx tsc --noEmit` sem erro novo.
  </acceptance_criteria>
  <done>As 6 operações do D-01 passam todas pela fila; skip_planned_session permanece fora, por leitura
  literal do D-01 (04-RESEARCH.md, Open Question 3).</done>
</task>

<task type="auto">
  <name>Task 3: corrige testes de skip/unskip/swap/finish afetados + cobertura nova</name>
  <files>__tests__/activeSessionStore.test.ts</files>
  <read_first>
    __tests__/activeSessionStore.test.ts — releia o describe `'concluir a sessão'` (faixa 694-745 na versão
    pré-04-02): o teste "erro ao fechar não engole: mantém erro e não conclui" (linha ~710,
    `mock(finishSessionLog).mockRejectedValue(new Error('timeout')); ... expect(ok).toBe(false); ...
    expect(store().saveError).toMatch(/timeout/)`) — REESCREVER: sob D-08, `finishSession` retorna `true`
    de imediato, `store().status` vira `'finished'`, `saveError` permanece `null`, e o item `finish_session`
    fica pendente na fila (confirmar chamando `drainAll` explicitamente e checando `finishSessionLog`
    chamado). O teste "F4: finish idempotente" (linha ~725) — conferir se ainda faz sentido sem mudança (a
    idempotência da RPC em si não muda) ou se precisa do mesmo ajuste de await explícito da drenagem.
    Procure também por qualquer teste que faça `mock(skipSessionExercise).mockRejectedValue`/
    `mock(swapSessionExercise).mockRejectedValue`/`mock(unskipSessionExercise).mockRejectedValue` — se
    existir algum (grep não encontrou nenhum na leitura do 04-01, mas confirme de novo pois o arquivo pode
    ter mudado no 04-01/Task 1 deste plano), reescreva no mesmo espírito (falha de rede não bloqueia a
    aplicação local, não seta saveError).
  </read_first>
  <action>
    Reescreva os testes identificados no `<read_first>` para refletir o contrato pós-fila. Adicione um
    teste novo no describe `'concluir a sessão'`: `finishSessionLog` rejeita com erro de transporte;
    `finishSession()` retorna `true`; `store().status` é `'finished'`; `store().draft` é `null`
    (retireLocalDraft rodou); chamar `drainAll` depois confirma `finishSessionLog` foi CHAMADO (o item
    ficou pendente e foi retentado), mesmo com o draft já limpo — prova de nível-mock do critério de
    sucesso #3 do 04-CONTEXT.md ("drena o que faltou, inclusive quando a sessão já foi finalizada"). Se
    não houver teste de rede para skip/unskip/swap, adicione um caso mínimo por ação confirmando que a
    mudança local se aplica mesmo com a RPC rejeitando (sem saveError).
  </action>
  <verify>
    <automated>npx jest __tests__/activeSessionStore.test.ts -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/activeSessionStore.test.ts -q` sai 0. Nenhum teste do describe `'concluir a
    sessão'` afirma `saveError` não-nulo para uma rejeição de `finishSessionLog`. `npx jest -q` (suíte
    padrão completa) sem falha nova.
  </acceptance_criteria>
  <done>A suíte reflete D-08 (finalizar não bloqueia) e cobre, em nível de mock, a drenagem pós-limpeza do
  draft.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Mesmos boundaries do `04-01-PLAN.md` — este plano não introduz trust boundary novo, só estende o número de
`kind`s que passam pelo MESMO pipeline de classificação/timeout já construído.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-05 | Tampering | Reenvio de `swap_session_exercise`/`skip_session_exercise` após retry | medium | mitigate | Guardas `0005`/`0036` (server-side) continuam sendo a fonte de verdade; a chave natural por `(sessionLogId, plannedExerciseId)` (D-13) garante que reenfileirar o MESMO alvo substitui o item, nunca duplica na fila do cliente. |
| T-04-06 | Repudiation | `finishSession` retorna sucesso ao usuário antes de o servidor confirmar `finish_session` | low | accept | A RPC é idempotente (migration 0004) e fica na fila até drenar; se falhar definitivamente (código fora da allowlist de transporte), cai no mesmo backstop de expiração/quarentena do D-11 — risco aceito porque D-08 exige explicitamente esse comportamento não-bloqueante. |
</threat_model>

<verification>
- `npx jest __tests__/activeSessionStore.test.ts -q` verde.
- `npx jest -q` (suíte padrão completa) sem falha nova.
- `npx tsc --noEmit` sem erro novo.
- `grep -n "await skipSessionExercise(\|await unskipSessionExercise(\|await swapSessionExercise(\|await finishSessionLog(\|updateSetLogAdaptation(" src/store/activeSessionStore.ts` só aparece dentro de comentários ou não aparece.
</verification>

<success_criteria>
As 6 operações do D-01 passam todas pela fila offline-first; nenhuma delas bloqueia a tela nem mostra erro
por soluço de rede; skip_planned_session permanece fora por decisão explícita (Open Question 3 do
04-RESEARCH.md).
</success_criteria>

<output>
Criar `.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-02-SUMMARY.md` ao final.
</output>
