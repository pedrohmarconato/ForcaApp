---
phase: 12-p-gina-de-instala-o-guiada
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified: []
autonomous: false
requirements: [INST-02]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "UAT do dono (ou de um aluno real) no iPhone: acessa /instalar pelo Safari, segue os passos exibidos sem ajuda adicional, e consegue instalar o app pela Tela de Início (critério de sucesso 3 do ROADMAP Fase 12)."
  artifacts: []
  key_links:
    - "vercel deploy --prod (manual, dono) -> InstallScreen + registro nos 3 navigators do Plano 12-01 servidos em produção -> UAT real no Safari do iPhone"
  prohibitions: []
---

<objective>
Fechar a Fase 12 com o único item que não pode ser verificado neste ambiente
de desenvolvimento: um aluno leigo de verdade (ou o dono simulando um)
instalando o ForçaApp a partir de `/instalar` num iPhone real, sem instrução
verbal — máquina de dev sem toolchain nativa (sem Xcode), sem simulador de
iPhone.

<precondition>Requer `vercel deploy --prod` (manual, dono) publicando os
commits do Plano 12-01 em produção antes do passo 2 do checkpoint abaixo — as
tasks automatizadas do Plano 12-01 só tocam source/testes locais, nunca
deploy.</precondition>

Purpose: confirmar o critério de sucesso 3 do ROADMAP (Fase 12) — o objetivo
final desta fase inteira ("qualquer aluno leigo consegue instalar o ForçaApp
sozinho a partir do site, sem instrução verbal do dono") só é prova real num
dispositivo físico, nunca em jsdom/CI local.
Output: confirmação humana registrada de que a instalação guiada funciona
sem ajuda, no iPhone real.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-p-gina-de-instala-o-guiada/12-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT do dono/aluno no iPhone real — instalação guiada sem ajuda (INST-02, critério 3)</name>
  <what-built>Rota pública `/instalar` com detecção de iOS/Safari/standalone e os 4 estados de copy literal (Plano 12-01), alcançável em qualquer estado de sessão (deslogado, onboarding, logado).</what-built>
  <how-to-verify>
OBSERVAR DURANTE TODO O TESTE (Pitfall 1 da research): ao abrir /instalar deslogado, note se aparece um flash de "Carregando..." ou tela escura antes da página — reporte se durar mais que um instante ou se a rota se perder (cair no login).
    1. Rodar `vercel deploy --prod` manualmente (deploy automático desligado
       por decisão do projeto, mesma configuração das Fases 9-11).
    2. No iPhone real, no Safari (não em outro navegador), acessar
       `https://<domínio-de-produção>/instalar` SEM estar logado no app.
    3. Seguir os 3 passos exibidos na tela (Compartilhar -> Adicionar à Tela
       de Início -> Adicionar) sem nenhuma instrução verbal adicional.
    4. Confirmar que o ícone do ForçaApp aparece na Tela de Início e o app
       abre em modo standalone (sem barra de endereço do Safari).
    5. Reabrir `/instalar` pelo Safari com o app já instalado — confirmar
       que a tela mostra "Você já instalou o ForçaApp" com o botão "Abrir o
       ForçaApp", sem repetir o passo a passo.
    6. (Reforça cobertura, opcional) Repetir o acesso a `/instalar` em
       Chrome iOS (CriOS) e num desktop/Android, confirmando as mensagens
       de redirecionamento corretas (Estados 2 e 3).
  </how-to-verify>
  <resume-signal>Digite "instalar-ok" se o aluno/dono instalou sem ajuda, ou descreva onde travou/confundiu.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dono/aluno (humano) -> `vercel deploy --prod` | Único caminho de publicação em produção; sem CI de deploy automático. |
| Aluno leigo (não técnico) -> instruções em tela | Falha de compreensão do passo a passo é o modo de falha real deste checkpoint, não uma falha de segurança. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Tampering | `vercel deploy --prod` manual fora de CI | low | accept | Mesma decisão já aceita nas Fases 9-11 (`git.deploymentEnabled: false`) — este plano não adiciona nenhuma automação de deploy nova. |
| T-12-07 | Repudiation | Resultado do UAT sem evidência automática além do texto do dono | low | accept | Mesmo padrão de checkpoint humano das Fases 9-11 — o SUMMARY.md registra a resposta literal do dono como evidência. |

</threat_model>

<verification>
O checkpoint humano confirma, num iPhone real: instalação sem ajuda a partir
de `/instalar` no Safari, e o estado "já instalado" adaptado (sem repetir o
passo a passo) numa segunda visita.
</verification>

<success_criteria>
Critério de sucesso 3 do ROADMAP (Fase 12) confirmado pelo dono/aluno em
dispositivo real — não depende mais de suposição. Fase 12 completa (INST-02).
</success_criteria>

## Artifacts this phase produces

Nenhum artefato de código novo — este plano só produz evidência de
verificação humana (registrada no SUMMARY.md), fechando o único critério de
sucesso do ROADMAP que depende de um iPhone físico fora deste clone.

<output>
Create `.planning/phases/12-p-gina-de-instala-o-guiada/12-02-SUMMARY.md` when done
</output>
