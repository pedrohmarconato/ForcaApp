---
phase: 10-identidade-do-app-instal-vel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/splash/**
  - public/index.html
  - vercel.json
  - package.json
  - __tests__/splashAssets.test.ts
autonomous: false
requirements: [INST-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Todo link apple-touch-startup-image em public/index.html resolve para um PNG existente em public/splash/ (guard jest automatizado — __tests__/splashAssets.test.ts, molde D-08 da fase 9)."
    - "vercel.json exclui 'splash' da regex negativa em AMBAS as ocorrências (bloco rewrites e bloco headers de Cache-Control no-cache) — /splash/*.png não é reescrito para index.html nem herda no-cache indevido, provado por teste automatizado que instancia o regex real do arquivo."
    - "public/splash/ contém somente PNGs de iPhone (device-width < 700px no media do link correspondente) — nenhum PNG de iPad (device-width >= 744px) nem das 5 combinações iPhone pré-2020 identificadas em 10-RESEARCH.md (XS Max/11 Pro Max, X/XS/11 Pro, XR/11, 6+/7+/8+, 5/5s/SE1)."
    - "public/icons/ (apple-touch-icon 180, icon-192, icon-512) permanece byte-idêntico — nenhuma regeneração de ícone nesta fase (per 10-CONTEXT.md: só se o UAT do dono acusar problema)."
    - "apple-mobile-web-app-title e <title> permanecem 'Força', apple-mobile-web-app-status-bar-style permanece 'black-translucent', theme-color e background-color do #expo-reset permanecem #0A0A0A — nenhuma meta tag existente é editada ou duplicada por esta fase."
    - "A splash gerada é logo-only sobre #0A0A0A, símbolo F centralizado com --padding 20%, sem texto adicional e sem degradê/sombra (regra de identidade em branding/forca-identidade-final.md)."
    - "O bloco <link rel=apple-touch-startup-image> foi colado manualmente em public/index.html (sem a flag --index do pwa-asset-generator) — nenhuma reescrita automática do arquivo, nenhuma meta tag apple-mobile-web-app-capable duplicada."
    - "A splash É o único estado de loading do app instalado: nenhum spinner/skeleton adicional é introduzido, o iOS descarta a splash sozinho quando o app shell renderiza."
    - "Não existe estado 'vazio' separado do estado normal da splash — a única forma de vazio é a ausência do PNG referenciado, prevenida pelo guard automatizado acima."
    - "Se o apple-touch-icon não for servido no momento da instalação, o iOS gera miniatura genérica — condição já coberta pela allowlist 'icons' existente do vercel.json e pelo critério 3 do UAT (não requer mudança de código nesta fase)."
    - { statement: "Ao instalar o ForcaApp pela Tela de Início a partir do Safari e abrir o app, não há flash de tela branca — a splash screen aparece corretamente na resolução exata do iPhone real do dono (critério de sucesso 2 do ROADMAP)", verification: backstop }
    - { statement: "PNG de splash ausente ou engolido pelo rewrite SPA do vercel.json resulta em flash branco silencioso — mitigado pelo guard automatizado + exclusão do vercel.json desta plan; confirmação final só é possível no UAT do dono no iPhone real", verification: backstop }
    - { statement: "O match de media query do apple-touch-startup-image é exato (CSS px, não pixel físico) — se nenhum link bater com o device real, o iOS não usa nenhuma splash e não há fallback por proximidade; mitigado copiando os valores de media literalmente da saída fresca do pwa-asset-generator, sem edição manual de número", verification: backstop }
    - { statement: "Um PWA já instalado antes desta fase não relê o index.html para atualizar a splash — o dono precisa remover o ícone da Tela de Início e reinstalar antes de validar (limitação documentada do iOS, não bug desta fase)", verification: backstop }
    - { statement: "O ícone na Tela de Início e o nome abaixo dele são os do ForcaApp (não a URL genérica da Vercel), e o app abre sem a barra de endereço do Safari (modo standalone) — critério de sucesso 3 do ROADMAP", verification: backstop }
  artifacts:
    - public/splash/*.png
    - __tests__/splashAssets.test.ts
  key_links:
    - "public/index.html <link href=/splash/...> -> public/splash/*.png (resolução de path, guardada por teste)"
    - "vercel.json rewrites[].source (regex) -> NÃO deve casar com /splash/apple-splash-*.png"
    - "vercel.json headers[].source (regex, bloco Cache-Control no-cache) -> NÃO deve casar com /splash/apple-splash-*.png"
    - "package.json scripts.generate:pwa-assets -> comando pwa-asset-generator@8.1.5 pinado, documentado para regeneração futura"
  prohibitions:
    - "o pwa-asset-generator NÃO roda no buildCommand da Vercel (vercel.json) — só é invocado ad hoc na máquina do dev via npx, nunca no pipeline de deploy."
    - "nenhuma dependência nova entra em package.json dependencies/devDependencies — pwa-asset-generator é chamado só via npx com versão pinada, nunca vira devDependency residente."
    - "nenhuma alteração na identidade visual fora das regras da marca — a splash gerada não recebe sombra, degradê, contorno ou texto adicional ao símbolo F."
    - "public/icons/ (ícones 180/192/512 já existentes) não é regenerado, redimensionado nem substituído nesta fase."
---

<objective>
Fechar o gap real da Fase 10 (INST-01): gerar a splash screen iOS
(`apple-touch-startup-image`) com `pwa-asset-generator`, curá-la para o catálogo
real de iPhone dos últimos ~5 anos, versioná-la em `public/splash/`, ligá-la a
`public/index.html`, e corrigir a regra de rewrite/headers do `vercel.json` que
hoje engoliria `/splash/*` no fallback de SPA (achado crítico da pesquisa,
`10-RESEARCH.md` Pitfall #1). Ícone, nome e status bar já estão corretos
(`10-CONTEXT.md`) e não são tocados por esta fase.

Nota sobre a sonda de edge coverage: INST-01 veio "unclassified" (unresolved) da
sonda automática determinística — não há SPEC.md nesta fase. Os edges reais
(media query sem match exato, `/splash/*` engolido pelo rewrite, PWA já
instalado não atualiza splash sem reinstalar, regressão futura de arquivo
removido/renomeado) foram autorados manualmente como `must_haves` acima
(truths automatizáveis + backstop verificável só em device real), a partir de
`10-RESEARCH.md` (Pitfalls #1, #4, #5) e do guard permanente. As proibições do
domínio (generator fora do build, sem dependência pesada nova, sem tocar
identidade fora da marca, sem regenerar ícones) vêm de `10-CONTEXT.md`
(decisões travadas) e foram autoradas como `must_haves.prohibitions`
descriptor-less, flagged-unverified.

Purpose: o ForcaApp instalado pela Tela de Início abre em modo standalone com
splash própria, sem flash de tela branca — última peça visível da identidade
antes do React montar.
Output: `public/splash/*.png` (curado), `public/index.html` com o bloco
`<link rel="apple-touch-startup-image">`, `vercel.json` corrigido,
`package.json` com script de regeneração, `__tests__/splashAssets.test.ts`
(guard duplo: link→arquivo e regex do vercel.json), UAT do dono aprovado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-identidade-do-app-instal-vel/10-CONTEXT.md
@.planning/phases/10-identidade-do-app-instal-vel/10-RESEARCH.md
@.planning/phases/10-identidade-do-app-instal-vel/10-UI-SPEC.md
@public/index.html
@vercel.json
@package.json
@__tests__/alertNoAlertRemanescente.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Gerar splash iOS curada, ligar a index.html e fechar o rewrite do vercel.json — fatia ponta a ponta</name>
  <files>public/splash/ (novo diretório), public/index.html, vercel.json, __tests__/splashAssets.test.ts</files>
  <precondition>Google Chrome instalado em "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" (verificado em 10-RESEARCH.md, 2026-08-14). Confirmar com `ls "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"` antes de rodar o generator; se o caminho for diferente nesta execução, localizar o binário real primeiro (Pitfall #3 de 10-RESEARCH.md).</precondition>
  <read_first>
    10-RESEARCH.md (comando testado com CHROME_PATH, seção "Code Examples" com os 22 links reais gerados em 2026-08-14 e a regra objetiva de curadoria por device-width, Pitfall #1 com o diff exato do vercel.json, Pitfall #2 sobre ausência de flag de filtro por device, Pitfall #3 sobre o workaround CHROME_PATH, Pitfall #5 sobre match exato de media query);
    public/index.html (head atual — onde colar o bloco de links, logo após a linha `<link rel="apple-touch-icon" ...>`);
    vercel.json (linhas do bloco `rewrites` e do bloco `headers` que usam o padrão `"/((?!_expo|icons|assets|manifest\\.json|favicon\\.ico).*)"` — hoje duas ocorrências idênticas, sem "splash" na lista de exclusão);
    assets/icon.png (fonte de arte 1024x1024 — NÃO usar assets/splash.png, 525x300, resolução baixa demais per 10-CONTEXT.md);
    __tests__/alertNoAlertRemanescente.test.ts (molde de guard jest recursivo D-08 da Fase 9 — estilo de varredura + assert "não parou de achar coisa").
  </read_first>
  <behavior>
    - Teste 1 (RED antes da geração): `__tests__/splashAssets.test.ts` lê `public/index.html`, extrai via regex todo `href` de `<link rel="apple-touch-startup-image">`, e falha porque `hrefs.length` é 0 — nenhum link colado ainda (`expect(hrefs.length).toBeGreaterThan(0)` quebra).
    - Teste 2 (GREEN após geração + curadoria + wiring): cada `href` extraído resolve para um arquivo existente em `public/splash/` (`existsSync` true para todos) — `expect(ausentes).toEqual([])` passa.
  </behavior>
  <action>
    Rodar (sem a flag `--index`, para não reescrever `public/index.html` automaticamente):
    `CHROME_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" npx pwa-asset-generator@8.1.5 assets/icon.png public/splash --splash-only --portrait-only --scrape true --type png --padding "20%" --background "#0A0A0A"`.
    Se falhar de novo com `TypeError: find.default is not a function` mesmo com `CHROME_PATH` setado, rodar `lsof -i :<porta-do-erro> | grep Chrome`, fechar essa instância do Chrome e repetir o comando (Pitfall #3). A saída do console emite ~20-22 blocos `<link rel="apple-touch-startup-image" href="apple-splash-WIDTH-HEIGHT.png" media="...">` e salva os PNGs correspondentes em `public/splash/`.
    Curar manualmente por regra objetiva de `device-width` (CSS px) no atributo `media` de cada link: DESCARTAR toda linha com `device-width >= 700px` (iPad) e as 5 combinações iPhone pré-2020 confirmadas em `10-RESEARCH.md` (414×896 dpr3, 375×812 dpr3, 414×896 dpr2, 414×736 dpr3, 320×568 dpr2) — apagar os PNGs correspondentes de `public/splash/` e não colar essas linhas no HTML. MANTER as linhas restantes de iPhone dos últimos ~5 anos (a lista real do dia da execução pode diferir levemente da amostra fixada na pesquisa — usar a saída fresca deste comando, não copiar os hrefs da pesquisa sem reconferir; se aparecer uma entrada de identidade incerta como `1080-2340`/`360×780 dpr3`, manter por padrão — custo zero se nunca casar com device real, per Open Question #1 da pesquisa).
    Reescrever cada `href` mantido de `apple-splash-WIDTH-HEIGHT.png` (path relativo, saída bruta do generator) para `/splash/apple-splash-WIDTH-HEIGHT.png` (path absoluto a partir da raiz web, consistente com `/icons/apple-touch-icon.png` já existente). Colar o bloco curado de `<link rel="apple-touch-startup-image">` dentro de `<head>` de `public/index.html`, logo após `<link rel="apple-touch-icon" .../>`, sem alterar nenhuma meta tag existente e sem editar nenhum número dentro de `media="..."` (copiar literal — Pitfall #5).
    Em `vercel.json`, adicionar `splash` ao grupo de exclusão regex nas DUAS ocorrências do padrão `"/((?!_expo|icons|assets|manifest\\.json|favicon\\.ico).*)"` — a do bloco `rewrites` (fallback de SPA) e a do bloco `headers` que aplica `Cache-Control: no-cache` — resultando em `"/((?!_expo|icons|assets|splash|manifest\\.json|favicon\\.ico).*)"` nas duas, sem alterar mais nada na estrutura do regex.
    Criar `__tests__/splashAssets.test.ts` com a função `listarLinksSplash` (regex sobre `public/index.html`, molde `alertNoAlertRemanescente.test.ts`): extrai todos os `href` de `<link rel="apple-touch-startup-image">`, guarda contra a própria regex parar de casar (`expect(hrefs.length).toBeGreaterThan(0)`), e confirma que cada `href` resolve para um arquivo existente em `public/splash/` via `existsSync` (`expect(ausentes).toEqual([])`).
  </action>
  <verify>
    <automated>npx jest __tests__/splashAssets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `public/splash/` contém somente PNGs cujo `device-width` correspondente no `media` do link é menor que 700px;
    `public/index.html` contém entre 6 e 12 linhas `<link rel="apple-touch-startup-image">` dentro de `<head>`, cada `href` começando com `/splash/`;
    a string `"splash"` aparece dentro do grupo negativo `(?!...)` tanto na entrada de `rewrites[].source` quanto na entrada de `headers[].source` (bloco Cache-Control no-cache) de `vercel.json`;
    `npx jest __tests__/splashAssets.test.ts` reporta "Tests: 1 passed" (ou mais) na saída textual.
  </acceptance_criteria>
  <reversibility rating="reversible">Decisões de identidade visual implementadas aqui (fonte de arte assets/icon.png, fundo único #0A0A0A sem variantes prefers-color-scheme, portrait-only, versionamento estático em vez de geração no build — todas per 10-CONTEXT.md) são revertíveis via git revert ou regeração do zero; nenhuma migração de dado ou infraestrutura irreversível está envolvida.</reversibility>
  <done>Splash curada gerada e versionada, `public/index.html` referencia os PNGs corretos com paths absolutos, `vercel.json` não engole mais `/splash/*` em nenhuma das duas regras, guard jest de link→arquivo passa.</done>
</task>

<task type="auto">
  <name>Task 2: Script npm de regeneração, teste de exclusão do vercel.json e verificação completa</name>
  <files>package.json, __tests__/splashAssets.test.ts</files>
  <read_first>
    package.json (bloco "scripts" atual — para preservar formatação e não quebrar entradas existentes);
    vercel.json (já editado pela Task 1 — confirmar as duas ocorrências corrigidas antes de escrever o teste de regex);
    10-RESEARCH.md (seção "Pattern 1" para o nome/forma do script `generate:pwa-assets`, seção "Common Pitfalls #1" para o diff exato esperado do vercel.json);
    AGENTS.md linha 91 (nota: `npx jest` com `--runInBand` pode sair com código 1 mesmo com todos os testes verdes — ler a saída textual "Tests: N passed", nunca o exit code, como portão).
  </read_first>
  <action>
    Adicionar ao bloco `"scripts"` de `package.json` a chave `"generate:pwa-assets"` com o valor
    `CHROME_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" npx pwa-asset-generator@8.1.5 assets/icon.png public/splash --splash-only --portrait-only --scrape true --type png --padding "20%" --background "#0A0A0A"`
    — mesma versão pinada (8.1.5) e mesmas flags da Task 1, documentando o comando testado para regeneração futura sem depender de lembrar o workaround `CHROME_PATH` de cor.
    Em `__tests__/splashAssets.test.ts`, adicionar um segundo `describe`/`it`: ler `vercel.json` com `readFileSync` + `JSON.parse` (sem reimplementar parser), extrair o `source` de `rewrites[0]` e o `source` da entrada de `headers` que aplica `Cache-Control: no-cache` ao catch-all (identificar pela presença do valor `"no-cache"` combinado com um `source` que NÃO seja `/manifest.json` nem `/_expo/static/(.*)`), montar `new RegExp(source)` para cada um, e testar contra o path fixo `"/splash/apple-splash-1170-2532.png"` que NENHum dos dois regex faz match (`expect(regex.test(path)).toBe(false)` para ambos).
    Rodar `npx tsc --noEmit` (checagem de tipos completa do projeto) e `npx jest` (suíte completa) — ler a saída textual `"Tests: N passed, 0 failed"`, NÃO usar o exit code do processo como portão (nota AGENTS.md:91).
  </action>
  <verify>
    <automated>npx jest __tests__/splashAssets.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `package.json` → `scripts["generate:pwa-assets"]` existe e contém a substring `"pwa-asset-generator@8.1.5"`;
    `npx jest __tests__/splashAssets.test.ts` reporta pelo menos 2 testes (`it`) passando — link→arquivo (Task 1) e regex do vercel.json (esta task);
    `npx tsc --noEmit` sai sem erro (saída vazia ou "Found 0 errors");
    `npx jest` (suíte completa) reporta `"Tests: X passed, 0 failed"` na saída textual — nenhuma regressão introduzida nos testes pré-existentes.
  </acceptance_criteria>
  <reversibility rating="reversible">Nome do script npm e organização do teste dentro do mesmo arquivo são discricionários (10-CONTEXT.md, "Claude's Discretion") — trivialmente revertíveis/renomeáveis via git revert.</reversibility>
  <done>Script de regeneração documentado e pinado; guard duplo (link→arquivo + regex do vercel.json) verde; suíte completa e `tsc --noEmit` sem regressão.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT do dono no iPhone real — splash, ícone/nome e modo standalone (INST-01)</name>
  <what-built>
    Splash screen iOS (`apple-touch-startup-image`) gerada com `pwa-asset-generator@8.1.5`,
    curada para iPhone dos últimos ~5 anos, versionada em `public/splash/` e ligada em
    `public/index.html`. `vercel.json` corrigido para não engolir `/splash/*` no fallback
    de SPA nem aplicar `Cache-Control: no-cache` indevido ao asset imutável. Guard
    automatizado permanente (`__tests__/splashAssets.test.ts`) protege as próximas fases
    (11-13) de uma regressão silenciosa (link apontando para PNG inexistente, ou o fix do
    vercel.json sendo desfeito). Ícone, nome "Força" e status bar `black-translucent` NÃO
    foram tocados nesta fase — já estavam corretos.
  </what-built>
  <how-to-verify>
    1. Confirme que o commit desta fase foi enviado (`git push`) e que o deploy da Vercel
       terminou (painel da Vercel ou aguardar alguns minutos) antes de instalar — esta fase
       não inclui o deploy de produção em si.
    2. Opcional, mas recomendado antes de abrir o iPhone: rode
       `curl -I https://<domínio-de-produção>/splash/<um-arquivo-real-de-public/splash/>.png`
       e confirme `content-type: image/png` (não `text/html`) — se vier `text/html`, o fix do
       `vercel.json` não chegou ao ar; não prossiga para o iPhone sem corrigir isso primeiro.
    3. **Se o ForçaApp já estava instalado na Tela de Início antes desta fase**: remova o
       ícone da Tela de Início e reinstale do zero. O iOS cacheia a splash no momento da
       instalação e não relê `index.html` depois — reabrir um app já instalado NÃO mostra a
       splash nova, mesmo com o deploy correto (limitação documentada do iOS, não bug desta
       fase).
    4. Pelo Safari, acesse o site em produção e use "Adicionar à Tela de Início".
    5. Toque no ícone recém-instalado. Confirme que, entre o tap e o app abrir, aparece a
       splash com o símbolo F neon centrado sobre fundo preto (#0A0A0A) — SEM nenhum
       instante de tela branca.
    6. Confirme que o ícone na Tela de Início e o nome abaixo dele ("Força") são os do
       ForçaApp, não a URL genérica da Vercel nem um ícone de screenshot genérico.
    7. Confirme que o app abre em modo standalone — sem a barra de endereço do Safari por
       cima.
  </how-to-verify>
  <resume-signal>Digite "aprovado" se os passos 5-7 passaram, ou descreva o que falhou (inclua se era uma instalação nova ou uma reinstalação, e o resultado do curl do passo 2 se você rodou).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build/Test tooling → CDN/Static | O guard jest roda no clone do repo (dev/CI), não em runtime do app — protege a camada estática (`public/splash/`, `public/index.html`) de regressão silenciosa antes de chegar à borda da Vercel. |
| Dev machine → npm registry | `npx pwa-asset-generator@8.1.5` baixa e executa código de terceiro (via Puppeteer/Chromium local) uma única vez, fora do pipeline de deploy — nunca roda com credenciais de produção. |
| CDN/Static (Vercel edge) → Browser/Client | `vercel.json` decide se `/splash/*.png` é servido como arquivo estático ou reescrito para `index.html`; o iOS/WebKit do dispositivo do usuário é quem lê e decide qual `apple-touch-startup-image` usar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | `vercel.json` regex de exclusão (rewrites + headers) | medium | mitigate | Editar só a lista dentro do grupo negativo já existente (`(?!_expo\|icons\|assets\|splash\|manifest\\.json\|favicon\\.ico)`), sem tocar na estrutura do regex; Task 2 adiciona teste automatizado que instancia o `RegExp` real do arquivo e prova que `/splash/*.png` não casa em nenhuma das duas regras, antes de qualquer deploy. |
| T-10-02 | Tampering | Supply chain — `pwa-asset-generator` (npm) | high | mitigate | Pacote já auditado em `10-RESEARCH.md` (Package Legitimacy Audit: verdict OK, ~7 anos, sem `postinstall`); nunca vira `devDependency` residente — só invocado via `npx <pacote>@8.1.5` (versão pinada) ad hoc, tanto na Task 1 quanto no script `generate:pwa-assets` da Task 2, reduzindo a superfície de ataque residente no `node_modules` do projeto. |
| T-10-03 | Tampering | `public/index.html` — colagem manual do bloco `<link>` | low | mitigate | Rodar o generator SEM a flag `--index` (evita reescrita automática/"prettify" do arquivo, que arriscaria duplicar `apple-mobile-web-app-capable` ou remover comentários existentes); `npx tsc --noEmit` + suíte completa (Task 2) confirmam ausência de regressão estrutural detectável. |
| T-10-04 | Information Disclosure | `public/splash/*.png`, `public/index.html` | low | accept | Assets estáticos públicos por natureza (mesmo conteúdo de qualquer PWA instalável) — nenhum dado sensível ou credencial é incluído; `CSP img-src 'self' data: blob:` já cobre `/splash/*` por ser mesma origem, sem mudança necessária. |
| T-10-SC | Tampering | npm installs (`pwa-asset-generator`) | high | mitigate | Legitimidade confirmada na pesquisa (npm view: sem `postinstall`, ~22.485 downloads/semana, repo oficial `elegantapp/pwa-asset-generator`); sem checkpoint humano adicional necessário aqui porque o pacote não é `[ASSUMED]`/`[SUS]` (já `Approved` na auditoria) e não entra em `package.json` como dependência residente. |

</threat_model>

<verification>
`npx jest __tests__/splashAssets.test.ts` verde (guard duplo: link→arquivo + regex do vercel.json);
`npx jest` (suíte completa) sem falhas na saída textual;
`npx tsc --noEmit` sem erros;
`public/splash/` só com PNGs de iPhone dos últimos ~5 anos (device-width < 700px);
`vercel.json` com "splash" excluído nas duas regras (rewrites e headers no-cache);
UAT do dono aprovado no iPhone real (splash sem flash branco, ícone/nome corretos, modo standalone), com nota explícita se foi reinstalação.
</verification>

<success_criteria>
Critério de sucesso 1 do ROADMAP (splash gerada via `pwa-asset-generator` e referenciada no `index.html`) fechado automaticamente pela Task 1/2. Critérios 2 e 3 (sem flash branco; ícone/nome corretos e modo standalone) fechados pelo UAT do dono na Task 3. INST-01 completo.
</success_criteria>

## Artifacts this phase produces

- `public/splash/*.png` — novo (Task 1), PNGs curados de splash iOS.
- Bloco `<link rel="apple-touch-startup-image">` em `public/index.html` — novo (Task 1).
- `vercel.json` — modificado (Task 1): `splash` adicionado à exclusão regex em `rewrites` e em `headers` (bloco Cache-Control no-cache).
- `package.json` → `scripts["generate:pwa-assets"]` — novo (Task 2).
- `__tests__/splashAssets.test.ts` — novo (Task 1 + Task 2): guard duplo, link→arquivo e regex do vercel.json.

<output>
Create `.planning/phases/10-identidade-do-app-instal-vel/10-01-SUMMARY.md` when done
</output>
