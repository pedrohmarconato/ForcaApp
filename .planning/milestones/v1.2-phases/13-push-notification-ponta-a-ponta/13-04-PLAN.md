---
phase: 13-push-notification-ponta-a-ponta
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03", "13-05"]
files_modified:
  - docker-compose.yml
autonomous: false
requirements: [PUSH-01, PUSH-02, PUSH-03, PUSH-04, PUSH-05]
user_setup:
  - service: web-push-vapid
    why: "Par de chaves VAPID de PRODUÇÃO (assina os pushes reais enviados aos alunos) — nunca reusar o par descartável do spike."
    env_vars:
      - name: VAPID_PRIVATE_KEY
        source: "Gerado uma única vez via `vapid --gen` (py-vapid, já instalado com pywebpush) fora do repo; guardado só como env var do VPS."
      - name: VAPID_SUBJECT
        source: "mailto: do dono (contato real, exigido pelo protocolo VAPID como identificação do remetente)."
      - name: EXPO_PUBLIC_VAPID_PUBLIC_KEY
        source: "Chave pública derivada do mesmo par, formato applicationServerKey (base64url) — vai no ambiente de build do frontend (Vercel), não no VPS."
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard (projeto de PRODUÇÃO, zanqygwsgxkyjiuhrzju) -> Settings -> API -> service_role key."
    dashboard_config:
      - task: "Definir PUSH_REMINDER_SCHEDULER_ENABLED=true no ambiente do VPS (só em produção — nunca em dev/CI)."
        location: "docker-compose.yml environment / painel de projetos da VPS"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - { statement: "Migrations 0038 e 0039 aplicadas em PRODUÇÃO (zanqygwsgxkyjiuhrzju) com o mesmo md5 de schema que staging, confirmado pelo mesmo protocolo da Fase 7 do v1.1 — checkpoint do dono, não automatizável.", verification: backstop }
    - { statement: "UAT do dono no iPhone real (PWA instalado): concede a permissão, recebe o lembrete de treino no horário configurado e a notificação de replanejamento quando confirma uma proposta — critério de sucesso 3 do ROADMAP Fase 13.", verification: backstop }
    - { statement: "UAT do dono no iPhone real: toca na notificação de lembrete e o app abre direto na tela da sessão ativa, pronto para registrar reps/peso — critério de sucesso 4 do ROADMAP Fase 13.", verification: backstop }
    - { statement: "UAT do dono no iPhone real: com permissão concedida, o ícone exibe badge de treino pendente; uma subscription inválida (410/404 simulado ou orgânico) some da tabela sem intervenção manual — critério de sucesso 5 do ROADMAP Fase 13.", verification: backstop }
  artifacts: []
  key_links:
    - "docker-compose.yml (VAPID_PRIVATE_KEY, VAPID_SUBJECT, SUPABASE_SERVICE_ROLE_KEY, PUSH_REMINDER_SCHEDULER_ENABLED=true) -> backend/services/push_sender.py + push_reminder_scheduler.py -> primeiro push real disparado em produção."
  prohibitions:
    - statement: "O par de chaves VAPID de teste do spike (13-SPIKE.md, scratchpad) nunca é usado como VAPID_PRIVATE_KEY de produção — um par novo é gerado especificamente para este checkpoint."
      status: resolved
      verification: judgment
    - statement: "Nenhuma migration desta fase é aplicada em produção sem a verificação de md5 staging×produção (mesmo padrão da Fase 7 do v1.1) — o dono confirma antes do push real."
      status: resolved
      verification: judgment
---

<objective>
Fechar a fase: gerar o par VAPID real de produção, aplicar as migrations 0038
e 0039 em produção com o checkpoint humano padrão do projeto (staging→prod,
verificação md5, mesmo protocolo da Fase 7 do v1.1), configurar os secrets
novos no VPS (`VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `SUPABASE_SERVICE_ROLE_KEY`,
`PUSH_REMINDER_SCHEDULER_ENABLED=true`), fazer o deploy do backend
(docker-compose no VPS Hostinger — fora das tasks automáticas, checkpoint do
dono) e fechar os 5 critérios de sucesso do ROADMAP com UAT real no iPhone do
dono. Este plano é inteiramente `autonomous: false` — cada task é um
checkpoint humano, nenhuma delas é reversível de forma barata (migration em
produção, secret novo em produção, deploy real).

Purpose: sair de "código pronto e testado localmente/staging" para "push
funcionando de ponta a ponta no iPhone real do dono", fechando os 5
requisitos PUSH-01..05 do ROADMAP Fase 13.
Output: par VAPID de produção, migrations 0038/0039 em produção, backend
atualizado no VPS, UAT aprovado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-push-notification-ponta-a-ponta/13-CONTEXT.md
@.planning/phases/13-push-notification-ponta-a-ponta/13-SPIKE.md
@docker-compose.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Gerar credenciais de produção (VAPID real + service role key)</name>
  <what-built>Todo o código de push (Planos 13-01/02/03) está pronto, testado e com a migration aplicada em STAGING. Falta gerar as credenciais reais de produção que nenhuma automação deve gerar sozinha.</what-built>
  <how-to-verify>
    1. Gerar o par VAPID de produção: rodar `vapid --gen` num diretório
       TEMPORÁRIO fora do repo (mesmo comando do spike, `13-SPIKE.md §2`,
       nunca reusar o par descartável já gerado ali).
    2. Guardar `private_key.pem` como o valor de `VAPID_PRIVATE_KEY` — SÓ no
       painel de variáveis de ambiente do VPS (nunca em arquivo do repo, nunca
       em `.env` comitado).
    3. Definir `VAPID_SUBJECT` como `mailto:<email real do dono>`.
    4. Derivar a chave pública no formato `applicationServerKey` (mesma
       conversão do spike, `13-SPIKE.md §2`, `Vapid02.from_file` +
       `public_bytes(Encoding.X962, PublicFormat.UncompressedPoint)`, base64url)
       e publicar como `EXPO_PUBLIC_VAPID_PUBLIC_KEY` no ambiente de build do
       frontend (painel da Vercel).
    5. Obter `SUPABASE_SERVICE_ROLE_KEY` do projeto de PRODUÇÃO
       (`zanqygwsgxkyjiuhrzju`, Dashboard -> Settings -> API) e guardá-la
       também só no VPS.
    6. Confirmar que nenhum dos 4 valores acima foi colado em nenhum arquivo
       do repositório (`git status`/`git diff` limpos após esta etapa).
  </how-to-verify>
  <resume-signal>Digite "credenciais prontas" quando os 4 valores estiverem gerados e guardados só no VPS/Vercel (nunca no repo).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Migrations 0038/0039 em produção (md5 staging×prod)</name>
  <what-built>Migrations 0038 (push_subscriptions) e 0039 (reminder_sent_at) já aplicadas e verificadas em STAGING nos Planos 13-01/13-02. Falta o mesmo checkpoint em PRODUÇÃO.</what-built>
  <how-to-verify>
    1. Conferir `cat supabase/.temp/project-ref` — relinkar explicitamente
       para `zanqygwsgxkyjiuhrzju` (produção) SÓ nesta etapa.
    2. Rodar `supabase db push` (ou o comando equivalente já usado na Fase 7
       do v1.1) para aplicar 0038 e 0039 em produção.
    3. Comparar o md5 do schema resultante (`push_subscriptions` +
       `planned_sessions.reminder_sent_at`) entre staging e produção — mesmo
       protocolo de verificação já usado na migration 0037 (Fase 7, v1.1).
    4. Confirmar que a policy RLS e os GRANTs (select/insert/update/delete
       para `authenticated`) existem em produção (`select * from pg_policies
       where tablename = 'push_subscriptions'`).
  </how-to-verify>
  <resume-signal>Digite "migrations em produção confirmadas" (ou descreva divergência de md5, se houver) quando a verificação estiver completa.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy do backend no VPS + UAT do dono no iPhone real</name>
  <what-built>Backend com todo o código de push (subscribe/unsubscribe, scheduler de lembrete, notify-replan-applied) pronto para deploy; docker-compose.yml já documenta as 4 variáveis novas.</what-built>
  <how-to-verify>
    1. No painel de projetos do VPS Hostinger, definir
       `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `SUPABASE_SERVICE_ROLE_KEY` (os 3
       gerados na Task 1) e `PUSH_REMINDER_SCHEDULER_ENABLED=true`.
    2. Fazer o deploy (`docker compose up --build -d` ou o fluxo padrão do
       provisionamento da VPS — fora do escopo automatizável deste plano).
    3. Confirmar `GET /api/health` e `GET /api/ready` respondendo 200 pós-deploy.
    4. UAT no iPhone real (PWA instalado, Safari): (a) tocar "Ativar
       notificações" no Perfil e conceder a permissão; (b) aguardar/forçar o
       horário do lembrete (ou reduzir `PUSH_REMINDER_HOUR`/`PUSH_REMINDER_POLL_SECONDS`
       temporariamente para testar) e confirmar o recebimento da notificação;
       (c) confirmar uma proposta de replanejamento numa sessão ativa e
       confirmar o recebimento da notificação de replanejamento; (d) tocar em
       cada notificação e confirmar que o app abre direto na sessão/Home
       correspondente; (e) confirmar que o ícone mostra o badge com o treino
       pendente do dia e que ele some após a sessão ser concluída; (f) se
       possível, revogar a permissão de notificação no iOS e confirmar que uma
       tentativa de envio subsequente resulta na subscription sendo removida
       da tabela (410/404 real do endpoint `web.push.apple.com`).
  </how-to-verify>
  <resume-signal>Digite "UAT aprovado" ou descreva o que falhou (item a-f específico) para abrir gap de correção.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dono (VPS/Vercel dashboards) → secrets de produção | Único ponto onde os 4 valores sensíveis desta fase (VAPID privada, service role) existem fora do processo em memória. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-10 | Information Disclosure | Secrets de produção coladas em algum arquivo do repo por engano durante o setup manual | critical | mitigate | Passo explícito de checkpoint (`git status`/`git diff` limpos) antes de prosseguir; nenhuma automação deste plano lê/escreve os valores — só o dono, direto nos dashboards. |
| T-13-11 | Tampering | Migration de produção aplicada sem verificação de md5 | high | mitigate | Checkpoint humano dedicado replicando o protocolo já usado e comprovado na Fase 7 do v1.1. |

</threat_model>

<verification>
Migrations 0038/0039 confirmadas em produção (md5 igual a staging); backend
respondendo em produção (`/api/health`/`/api/ready`); UAT do dono aprovado nos
6 itens (a-f) do checkpoint 3.
</verification>

<success_criteria>
Os 5 critérios de sucesso do ROADMAP Fase 13 fechados: spike documentado
(já feito em 13-SPIKE.md), botão com gesto síncrono gravando subscription,
lembrete + replanejamento recebidos no iPhone real, toque abrindo a sessão em
1 gesto, badge funcionando e subscriptions expiradas removidas
automaticamente.
</success_criteria>

## Artifacts this phase produces

- `docker-compose.yml` — modificado (checkpoint, fora de automação): 4
  variáveis de ambiente novas de produção documentadas/preenchidas no painel
  do VPS.
- Migrations 0038/0039 aplicadas em produção (`zanqygwsgxkyjiuhrzju`).
- Par VAPID de produção (privada só no VPS, pública em `EXPO_PUBLIC_VAPID_PUBLIC_KEY` do build da Vercel).

<output>
Create `.planning/phases/13-push-notification-ponta-a-ponta/13-04-SUMMARY.md` when done
</output>
