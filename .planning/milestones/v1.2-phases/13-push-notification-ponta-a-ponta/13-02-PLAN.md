---
phase: 13-push-notification-ponta-a-ponta
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - supabase/migrations/0039_push_reminder_idempotencia.sql
  - docker-compose.yml
  - backend/services/push_reminder_scheduler.py
  - backend/app.py
  - backend/tests/test_push_reminder_scheduler.py
  - backend/tests/test_push_replan_notify.py
  - src/services/api/apiClient.ts
  - src/store/activeSessionStore.ts
autonomous: true
requirements: [PUSH-02, PUSH-03]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Às 8h America/Sao_Paulo, o scheduler seleciona exatamente as planned_sessions com scheduled_date = hoje, status = 'pending' e reminder_sent_at IS NULL, envia um push por aluno e marca reminder_sent_at = now() imediatamente após o envio (ou tentativa) — verificado por pytest com 'agora' injetado e candidatos mockados."
    - "Um reinício do processo Flask no meio do dia não duplica o lembrete: uma segunda chamada do tick, no mesmo dia, para a MESMA sessão já marcada com reminder_sent_at não dispara um novo push (idempotência por chave dia+sessão/usuário, edge de concorrência PUSH-02) — verificado por pytest chamando a função de tick duas vezes sobre o mesmo conjunto simulado de dados."
    - "Aluno sem nenhuma linha em push_subscriptions é pulado silenciosamente (sem erro, sem exceção) mas a sessão dele ainda recebe reminder_sent_at = now() (evita reprocessamento infinito do mesmo aluno no mesmo dia) — verificado por pytest."
    - "Após reschedule_week_sessions/applyConfirmedReplan confirmar com sucesso (confirmReplan() em activeSessionStore.ts), o cliente chama POST /api/push/notify-replan-applied de forma best-effort — falha de rede nessa chamada NUNCA reverte nem bloqueia a confirmação do replanejamento já aplicado, só gera um logger.warn (decisão explícita, assumida: best-effort sem retry, per specless-probe-fallback PUSH-03 concurrency)."
    - "POST /api/push/notify-replan-applied só envia push para as subscriptions do PRÓPRIO usuário autenticado (via JWT + RLS, sem SERVICE_ROLE_KEY) — nunca aceita um user_id no corpo da requisição."
  artifacts:
    - supabase/migrations/0039_push_reminder_idempotencia.sql
    - backend/services/push_reminder_scheduler.py
    - backend/tests/test_push_reminder_scheduler.py
    - backend/tests/test_push_replan_notify.py
  key_links:
    - "backend/app.py (import da app) -> push_reminder_scheduler.iniciar_scheduler() chamado uma vez, gated por PUSH_REMINDER_SCHEDULER_ENABLED (default false — testes nunca sobem thread real; docker-compose.yml do VPS liga explicitamente)."
    - "activeSessionStore.ts confirmReplan() (linha ~896, após applyConfirmedReplan) -> apiClient.post(ENDPOINTS.PUSH.NOTIFY_REPLAN) -> backend/app.py handle_push_notify_replan -> push_sender.listar_subscriptions(g.user['id']) -> push_sender.enviar_push por subscription."
    - "push_reminder_scheduler.py (SUPABASE_SERVICE_ROLE_KEY) -> GET /rest/v1/planned_sessions?scheduled_date=eq.hoje&status=eq.pending&reminder_sent_at=is.null -> resolve o MESMO sessionId usado por linkingConfig.ts ActiveSession:'active-session/:sessionId' (Open Question Q3 de 13-RESEARCH.md, resolvida por leitura direta em vez de reimplementar agendaDias.ts)."
  prohibitions:
    - statement: "push_reminder_scheduler.py nunca reimplementa a lógica de weeklyReplanner.ts/agendaDias.ts em Python — resolve a sessão de hoje por uma query direta a planned_sessions (scheduled_date/status), nunca recalculando reancoragem/aderência no backend (Anti-Pattern de 13-RESEARCH.md)."
      status: resolved
      verification: judgment
    - statement: "SUPABASE_SERVICE_ROLE_KEY nunca é usado em nenhuma rota HTTP alcançável pelo cliente (só dentro de push_reminder_scheduler.py, processo interno) — as rotas /api/push/* continuam usando o JWT do usuário + anon key, nunca a chave de serviço."
      status: resolved
      verification: test
---

<objective>
Fechar PUSH-02 (lembrete diário de treino) e PUSH-03 (aviso de replanejamento
aplicado), corrigindo a premissa que 13-RESEARCH.md documentou como falsa: não
existe "job de replanejamento" nem cron algum no Flask hoje. PUSH-02 ganha um
scheduler novo, em thread única dentro do processo Flask (mesmo padrão MVP de
`job_manager.py`), que lê `planned_sessions` diretamente (não `dias_treino`) —
resolvendo de graça o `sessionId` do deep link (Open Question Q3) e o
"sem sessão iniciada" (status `pending`). PUSH-03 dispara no evento real e já
existente: a confirmação bem-sucedida de `applyConfirmedReplan()` dentro de
`confirmReplan()` (`activeSessionStore.ts`) — NÃO a RPC `reschedule_week_sessions`
citada em 13-RESEARCH.md, que na verdade pertence a uma feature diferente
(reagendamento manual de sessões via `planEditRepository.ts`); ver a correção
de leitura de código abaixo.

Correção de leitura de código em relação a 13-RESEARCH.md: a pesquisa afirma
que `reschedule_week_sessions` é "chamada pelo cliente via
weeklyReplanRepository.ts quando o aluno CONFIRMA o banner (ReplanBanner.tsx)".
A leitura direta do código nesta sessão de planejamento mostra o contrário:
`reschedule_week_sessions` é chamada por `planEditRepository.ts`
(`reagendarSessoesDaSemana`), uma feature de edição manual de plano, SEM
relação com `ReplanBanner`. O fluxo real do banner de replanejamento por
aderência é `ActiveSessionScreen.tsx` -> `activeSessionStore.confirmReplan()`
-> `weeklyReplanRepository.applyConfirmedReplan()`, que grava
`session_logs.adherence_snapshot` via `UPDATE` direto ao PostgREST (SEM
nenhuma RPC). Este é o evento real, persistido, server-side que PUSH-03 deve
observar — a interpretação (a) do Open Question Q1 de 13-RESEARCH.md
("dispara quando o aluno confirma a proposta") continua correta, só o nome da
função/arquivo mudou.

Nota sobre a sonda de edge coverage: PUSH-02 e PUSH-03 vieram "unresolved"
(concurrency/unclassified) da sonda determinística. Os edges autorados acima
em `must_haves.truths` cobrem: idempotência do scheduler a um restart do
processo (chave `reminder_sent_at` persistida — NÃO em memória, decisão
explícita desta task porque um dedup em memória não sobreviveria a um
restart do container); aluno sem subscription pulado sem erro; e a falha de
rede do endpoint de replanejamento tratada como best-effort sem retry
(assunção sinalizada — decisão do planner, não do dono, porque nenhuma fonte
travou o comportamento exato de retry).

Purpose: o aluno recebe o lembrete de treino no horário fixo do MVP e o aviso
de que a semana foi replanejada, sem duplicar nenhuma lógica de domínio no
backend.
Output: `supabase/migrations/0039_push_reminder_idempotencia.sql`,
`backend/services/push_reminder_scheduler.py`, rota
`POST /api/push/notify-replan-applied`, hook em `confirmReplan()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-push-notification-ponta-a-ponta/13-CONTEXT.md
@.planning/phases/13-push-notification-ponta-a-ponta/13-RESEARCH.md
@backend/services/job_manager.py
@backend/services/push_sender.py
@backend/app.py
@supabase/migrations/0001_modelo_treino.sql
@supabase/migrations/0008_questionario_usuario.sql
@src/store/activeSessionStore.ts
@src/services/weeklyReplanRepository.ts
@src/services/planEditRepository.ts
@src/services/api/apiClient.ts
@docker-compose.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0039 (idempotência) + scheduler de lembrete (PUSH-02)</name>
  <reversibility rating="costly">Introduz SUPABASE_SERVICE_ROLE_KEY como secret novo no VPS — primeira vez que o backend lê/escreve fora do padrão JWT-do-usuário+RLS estabelecido no projeto. Reversível (revogar/rotacionar a chave no dashboard do Supabase a qualquer momento, remover a leitura do módulo num follow-up), mas é uma mudança de postura de segurança que o dono deve estar ciente — não é puramente mecânico.</reversibility>
  <files>supabase/migrations/0039_push_reminder_idempotencia.sql, docker-compose.yml, backend/services/push_reminder_scheduler.py, backend/tests/test_push_reminder_scheduler.py</files>
  <read_first>
    13-RESEARCH.md ("CONTRADIÇÃO ENCONTRADA" completo — por que não existe cron
    hoje; "Architectural Responsibility Map" linha "Disparo de push por
    lembrete de treino"; Open Questions Q2/Q3; Common Pitfalls #6);
    backend/services/job_manager.py (padrão de thread daemon MVP, comentário
    de cabeçalho "produção multi-worker: trocar por fila externa" — mesmo
    aviso operacional vale aqui);
    backend/services/push_sender.py (Plano 13-01 — `enviar_push`,
    `EXPIRED_STATUS_CODES`, para reusar sem duplicar a lógica 410/404);
    supabase/migrations/0001_modelo_treino.sql linhas 37-58 (colunas reais de
    `planned_sessions`: `id`, `user_id`, `scheduled_date`, `status`, índice
    `planned_sessions_user_date_idx`);
    docker-compose.yml (bloco `environment` — padrão `${VAR:?defina...}` para
    obrigatórias vs `${VAR:-default}` para opcionais, a seguir exatamente);
    backend/app.py linhas 93-95 (`_flag`, padrão de flag booleana por env var).
  </read_first>
  <behavior>
    - Teste 1: dado um "agora" injetado às 08:00 America/Sao_Paulo e uma lista
      simulada de `planned_sessions` candidatas (scheduled_date=hoje,
      status='pending', reminder_sent_at=None) com subscriptions associadas,
      o tick chama `enviar_push` uma vez por (sessão, subscription) e marca
      `reminder_sent_at` para cada sessão processada.
    - Teste 2: rodar o tick DUAS VEZES seguidas sobre o mesmo conjunto
      simulado (a segunda chamada representa um restart do processo) —
      `enviar_push` é chamado apenas na PRIMEIRA rodada; a segunda rodada não
      reenvia para nenhuma sessão já marcada.
    - Teste 3: uma sessão candidata sem nenhuma subscription associada não
      chama `enviar_push`, não lança exceção, e ainda assim recebe
      `reminder_sent_at` marcado (evita reprocessamento eterno do mesmo
      aluno).
    - Teste 4: com "agora" fora da hora configurada (ex.: 14:00), o tick não
      consulta candidatos nem envia nada (early return).
    - Teste 5: `iniciar_scheduler()` NÃO inicia nenhuma thread quando
      `PUSH_REMINDER_SCHEDULER_ENABLED` não está definido como `"true"`
      (default seguro para testes/dev).
  </behavior>
  <action>
    Criar `supabase/migrations/0039_push_reminder_idempotencia.sql`:
    `alter table public.planned_sessions add column if not exists
    reminder_sent_at timestamptz;` com `comment on column` explicando que é a
    chave de idempotência de PUSH-02 (NULL = ainda não enviado; o scheduler
    nunca reenvia após um restart porque a query exclui linhas preenchidas);
    `create index if not exists planned_sessions_reminder_pending_idx on
    public.planned_sessions (scheduled_date) where status = 'pending' and
    reminder_sent_at is null;` (índice parcial — acelera a query diária do
    scheduler). Sem mudança de RLS/GRANT nesta migration: a leitura/escrita
    deste campo pelo scheduler usa `SUPABASE_SERVICE_ROLE_KEY` (bypassa RLS
    por design — é o único processo do sistema sem um usuário logado por
    trás, cenário para o qual o service role existe). Aplicar em STAGING
    seguindo o MESMO protocolo do Plano 13-01 Task 1 (conferir
    `supabase/.temp/project-ref` antes do push).

    Em `docker-compose.yml`, adicionar `SUPABASE_SERVICE_ROLE_KEY:
    ${SUPABASE_SERVICE_ROLE_KEY:?defina SUPABASE_SERVICE_ROLE_KEY no ambiente
    do projeto (Supabase Dashboard -> Settings -> API -> service_role key) —
    usado SÓ pelo scheduler de lembrete para ler across-user}` ao bloco de
    variáveis obrigatórias (mesmo padrão `:?` de `SUPABASE_URL`), e
    `PUSH_REMINDER_SCHEDULER_ENABLED: ${PUSH_REMINDER_SCHEDULER_ENABLED:-false}`
    junto das opcionais com default — o VPS de produção precisa setar
    explicitamente `true` (feito no Plano 13-04, checkpoint do dono).

    Criar `backend/services/push_reminder_scheduler.py`: constantes
    `TZ_SAO_PAULO = zoneinfo.ZoneInfo("America/Sao_Paulo")`,
    `REMINDER_HOUR = int(os.environ.get("PUSH_REMINDER_HOUR", "8"))`,
    `POLL_INTERVAL_SECONDS = int(os.environ.get("PUSH_REMINDER_POLL_SECONDS",
    "300"))`. Função `_service_role_headers() -> dict`: lê
    `SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_URL` de `os.environ` (lazy, dentro da
    função — nunca em nível de módulo, para não vazar em import de teste);
    monta `{"apikey": key, "Authorization": f"Bearer {key}", "Content-Type":
    "application/json"}`. Função `_candidatos_do_dia(hoje_iso: str) ->
    list[dict]`: GET em `{base_url}/rest/v1/planned_sessions` com query
    `scheduled_date=eq.<hoje_iso>&status=eq.pending&reminder_sent_at=is.null&select=id,user_id,title`
    usando os headers de service role — devolve a lista crua. Função
    `_subscriptions_por_usuarios(user_ids: list[str]) -> dict[str, list[dict]]`:
    GET em `push_subscriptions` com `user_id=in.(<lista>)&select=user_id,endpoint,p256dh,auth`,
    agrupando por `user_id`. Função `_marcar_lembrete_enviado(session_id: str)
    -> None`: PATCH em `planned_sessions?id=eq.<session_id>` com corpo
    `{"reminder_sent_at": <ISO agora>}`. Função pública `processar_tick(agora:
    datetime.datetime) -> int` (devolve quantos pushes foram enviados,
    testável com `agora` injetado — NUNCA chama `datetime.now()` internamente):
    se `agora.astimezone(TZ_SAO_PAULO).hour != REMINDER_HOUR`, retorna 0 sem
    consultar nada; senão busca candidatos do dia
    (`agora.astimezone(TZ_SAO_PAULO).date().isoformat()`), busca subscriptions
    agrupadas, para cada sessão: monta payload `{"title": "Hora do treino!",
    "body": f"{sessao['title']} está te esperando.", "url":
    f"/home/active-session/{sessao['id']}"}` (URL usa o MESMO `id` de
    `planned_sessions`, o mesmo path de `linkingConfig.ts`); se não há
    subscriptions para o `user_id`, chama `_marcar_lembrete_enviado` e
    continua (skip silencioso); senão, para cada subscription chama
    `push_sender.enviar_push` (reusar, não duplicar a lógica 410/404) dentro de
    um try/except amplo por subscription (uma falha numa subscription não
    impede as demais nem a marcação da sessão), apagando via
    `push_sender.delete_subscription` quando `enviar_push` retorna False, e
    SEMPRE chama `_marcar_lembrete_enviado(sessao['id'])` ao final do
    processamento da sessão (sucesso, falha parcial ou zero subscriptions).
    Função `iniciar_scheduler() -> None`: se `_flag`-equivalente
    (`os.environ.get("PUSH_REMINDER_SCHEDULER_ENABLED", "false").strip().lower()
    != "true"`), retorna sem criar thread nenhuma; senão inicia uma
    `threading.Thread(target=_loop, daemon=True)` onde `_loop` chama
    `processar_tick(datetime.datetime.now(datetime.timezone.utc))` dentro de
    um `while True` com `time.sleep(POLL_INTERVAL_SECONDS)` e um try/except
    amplo por iteração (uma exceção não tratada não pode matar a thread
    silenciosamente — logar e continuar, mesmo espírito do `_wrapper` de
    `job_manager.executar_job`).
  </behavior>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_push_reminder_scheduler.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_push_reminder_scheduler.py -x` cobre os 5 comportamentos do bloco `<behavior>`.
    - `grep -n "reminder_sent_at" supabase/migrations/0039_push_reminder_idempotencia.sql` mostra a coluna e o índice parcial.
    - Migration 0039 aplicada e verificada em STAGING (mesma checagem de project-ref do Plano 13-01).
    - `grep -n "datetime.now()" backend/services/push_reminder_scheduler.py` não retorna nenhuma ocorrência dentro de `processar_tick` (o "agora" é sempre parâmetro injetado — só `_loop`/`iniciar_scheduler` podem chamar `datetime.now()`).
    - `grep -n "SUPABASE_SERVICE_ROLE_KEY" backend/app.py` não retorna nenhuma ocorrência (a chave de serviço só existe dentro de `push_reminder_scheduler.py`).
  </acceptance_criteria>
  <done>Scheduler pronto, testado e idempotente a restart; migration 0039 aplicada em staging; SUPABASE_SERVICE_ROLE_KEY documentado como obrigatório no docker-compose.yml (produção fica para o Plano 13-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Endpoint de notificação de replanejamento aplicado (PUSH-03, backend)</name>
  <reversibility rating="reversible">Rota nova, aditiva — reverter é remover a rota e o import; nenhum dado existente muda de forma.</reversibility>
  <files>backend/app.py, backend/tests/test_push_replan_notify.py</files>
  <read_first>
    backend/services/push_sender.py (`listar_subscriptions`, `enviar_push`,
    `delete_subscription` — reusar sem duplicar);
    backend/app.py linhas 509-520 (padrão de rota autenticada com rate limit);
    backend/tests/test_app_security.py (padrão de mock de `token_required` via
    `mock.patch("backend.utils.auth.requests.get", ...)`).
  </read_first>
  <behavior>
    - Teste 1: POST sem token retorna 401.
    - Teste 2: POST com token válido e uma subscription cadastrada para o
      usuário chama `push_sender.enviar_push` uma vez e retorna 200 com
      `{"sent": 1}`.
    - Teste 3: usuário sem nenhuma subscription retorna 200 com `{"sent": 0}`
      (nunca 404/erro — a ausência de subscription não é uma falha do
      endpoint).
    - Teste 4: `enviar_push` retornando False (expirada) chama
      `delete_subscription` e o `sent` final não conta essa subscription.
    - Teste 5: o corpo da requisição NUNCA é usado para determinar de quem são
      as subscriptions — mesmo enviando um `user_id` arbitrário no corpo, o
      endpoint usa `g.user['id']`.
  </behavior>
  <action>
    Em `backend/app.py`, adicionar ao import de `push_sender` (já presente do
    Plano 13-01) as funções `listar_subscriptions`, `delete_subscription`.
    Criar `@app.route('/api/push/notify-replan-applied', methods=['POST'])
    @token_required def handle_push_notify_replan():` — lê `user_id =
    g.user['id']`, `access_token = g.access_token`; aplica
    `_rate_limit_hit("push_notify_replan", user_id, PUSH_RATE_LIMIT,
    PUSH_RATE_WINDOW_SECONDS)` (429 se excedido); chama
    `listar_subscriptions(user_id, access_token)`; se vazia, retorna
    `{"sent": 0}, 200` imediatamente; monta payload `{"title": "Treino
    replanejado", "body": "Sua semana foi ajustada. Toque para ver.", "url":
    "/home"}` (sem depender de nenhum `sessionId` específico — o replanejamento
    é da SEMANA, não de uma sessão isolada, então o deep link leva para a Home,
    onde o treino de hoje/próximo já aparece em destaque); para cada
    subscription, chama `push_sender.enviar_push` com `VAPID_PRIVATE_KEY`/
    `VAPID_SUBJECT` lidos de `os.environ` dentro do handler (mesmo padrão lazy
    de `push_sender.py`); em falha (`enviar_push` retorna False) chama
    `delete_subscription`; conta quantos enviaram com sucesso e retorna
    `{"sent": <n>}, 200` — NUNCA retorna erro para o cliente por causa de uma
    subscription expirada individual (best-effort, mesmo espírito do envio de
    lembrete).
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_push_replan_notify.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_push_replan_notify.py -x` cobre os 5 comportamentos do bloco `<behavior>`.
    - `grep -n "g.user\['id'\]" backend/app.py` dentro de `handle_push_notify_replan` confirma que o `user_id` nunca vem do corpo da requisição.
  </acceptance_criteria>
  <done>Endpoint autenticado envia push só das próprias subscriptions do usuário, best-effort, nunca falha por subscription expirada individual.</done>
</task>

<task type="auto">
  <name>Task 3: Hook do frontend — confirmReplan() dispara a notificação (PUSH-03, frontend)</name>
  <reversibility rating="reversible">Uma chamada best-effort adicional após um fluxo já existente — remover a chamada não muda nenhum comportamento de replanejamento em si.</reversibility>
  <files>src/services/api/apiClient.ts, src/store/activeSessionStore.ts</files>
  <read_first>
    src/store/activeSessionStore.ts linhas 874-937 (`confirmReplan()` completo
    — o ponto exato de inserção é logo após `applyConfirmedReplan` resolver
    com sucesso, ANTES ou junto do bloco que já trata `saveDraft` como
    best-effort com `console.warn`, linhas 913-919 — mesmo padrão de
    tolerância a falha a replicar);
    src/services/api/apiClient.ts (ENDPOINTS.PUSH já criado no Plano 13-01 —
    só adicionar a chave nova);
    src/utils/logger.ts (API `logger.warn`, já usada em outros pontos do
    arquivo — nunca `console.error` para uma falha esperada/não-fatal).
  </read_first>
  <action>
    Em `src/services/api/apiClient.ts`, estender `ENDPOINTS.PUSH` (criado no
    Plano 13-01) com `NOTIFY_REPLAN: '/push/notify-replan-applied'`.

    Em `src/store/activeSessionStore.ts`, dentro de `confirmReplan()`, logo
    após a chamada a `applyConfirmedReplan(...)` resolver com sucesso (mesmo
    ponto onde hoje começa o bloco `const atual = get().draft; ...`), disparar
    `apiClient.post(ENDPOINTS.PUSH.NOTIFY_REPLAN, {}).catch((e) => {
    logger.warn('[activeSession] notificação de replanejamento não enviada
    (não-fatal):', e); })` — SEM `await` bloqueando o fluxo de
    `confirmReplan()` (fire-and-forget best-effort, decisão explícita
    assumida: a UX de "replanejamento confirmado" nunca deve esperar nem
    falhar por causa do envio da notificação). Import `apiClient, { ENDPOINTS
    }` de `'../services/api/apiClient'` e `logger` de `'../utils/logger'` no
    topo do arquivo, se ainda não importados.
  </action>
  <verify>
    <automated>npx jest __tests__/activeSessionStore --silent -t "confirmReplan" 2>/dev/null || npx jest -t "confirmReplan" --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "NOTIFY_REPLAN" src/store/activeSessionStore.ts` confirma a chamada.
    - `grep -n "await apiClient.post(ENDPOINTS.PUSH.NOTIFY_REPLAN" src/store/activeSessionStore.ts` NÃO retorna nada (a chamada nunca é `await`ada dentro do fluxo síncrono de sucesso — best-effort real).
    - Suíte existente de `activeSessionStore`/`confirmReplan` (se houver) continua verde após a mudança — nenhuma asserção de retorno de `confirmReplan()` quebrou.
  </acceptance_criteria>
  <done>Toda confirmação de replanejamento bem-sucedida dispara a notificação best-effort, sem nunca bloquear ou falhar o fluxo principal do aluno.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `push_reminder_scheduler.py` (processo interno) → Supabase (service role) | Único ponto do backend que bypassa RLS — leitura/escrita cross-user, sem JWT de usuário nenhum por trás. |
| Browser (aluno, dentro de uma sessão já autenticada) → `/api/push/notify-replan-applied` | Corpo da requisição é ignorado para fins de identidade — só `g.user` decide de quem são as subscriptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Information Disclosure / Elevation of Privilege | `SUPABASE_SERVICE_ROLE_KEY` vazada (log, exceção, commit) | critical | mitigate | Lida via `os.environ` só dentro de `push_reminder_scheduler.py`, dentro de função (nunca nível de módulo/import), nunca logada; env var só no VPS via docker-compose (`:?` obrigatório, nunca default); asserção automatizada confirma que a chave NUNCA aparece em `app.py` (nenhuma rota HTTP a usa). |
| T-13-07 | Elevation of Privilege | `/api/push/notify-replan-applied` aceitando `user_id` do corpo | medium | mitigate | Handler usa exclusivamente `g.user['id']` (JWT validado) — corpo da requisição não tem nenhum campo de identidade, testado explicitamente (Task 2, Teste 5). |
| T-13-08 | Denial of Service | Scheduler reenviando lembrete em loop por bug de fuso/hora | low | mitigate | `processar_tick` recebe "agora" como parâmetro puro (nunca `datetime.now()` interno), testável determinística; a chave `reminder_sent_at` persistida (não em memória) garante que mesmo um bug de reentrância não duplica envios já marcados. |

</threat_model>

<verification>
`pytest backend/tests/test_push_reminder_scheduler.py backend/tests/test_push_replan_notify.py -x`
verde; migration 0039 aplicada e verificada em staging; hook em
`confirmReplan()` confirmado por grep (best-effort, nunca `await`ado
bloqueante).
</verification>

<success_criteria>
O lembrete de treino dispara uma vez por dia por aluno, sobrevive a restart do
processo sem duplicar, e pula silenciosamente quem não tem subscription. A
confirmação de um replanejamento aplicado sempre tenta notificar, sem nunca
travar ou falhar a ação principal do aluno.
</success_criteria>

## Artifacts this phase produces

- `supabase/migrations/0039_push_reminder_idempotencia.sql` — novo (Task 1):
  `planned_sessions.reminder_sent_at` + índice parcial, aplicada em staging.
- `docker-compose.yml` — modificado (Task 1): `SUPABASE_SERVICE_ROLE_KEY`
  (obrigatória) e `PUSH_REMINDER_SCHEDULER_ENABLED` (opcional, default false).
- `backend/services/push_reminder_scheduler.py` — novo (Task 1): thread
  daemon MVP, idempotente por `reminder_sent_at`.
- `backend/tests/test_push_reminder_scheduler.py` — novo (Task 1).
- `backend/app.py` — modificado (Task 2): rota `POST /api/push/notify-replan-applied`.
- `backend/tests/test_push_replan_notify.py` — novo (Task 2).
- `src/services/api/apiClient.ts` — modificado (Task 3): `ENDPOINTS.PUSH.NOTIFY_REPLAN`.
- `src/store/activeSessionStore.ts` — modificado (Task 3): hook best-effort em `confirmReplan()`.

<output>
Create `.planning/phases/13-push-notification-ponta-a-ponta/13-02-SUMMARY.md` when done
</output>
