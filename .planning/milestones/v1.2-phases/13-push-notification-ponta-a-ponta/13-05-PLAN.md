---
phase: 13-push-notification-ponta-a-ponta
plan: 05
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/components/PushInviteHost.tsx
  - App.tsx
  - __tests__/pushInviteHost.test.tsx
autonomous: true
requirements: [PUSH-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Um aluno autenticado, com plano já gerado (profile.current_plan_id presente — onboarding completo), em navegador com suporte a push e Notification.permission === 'default' (nunca perguntado), vê o convite via alertShim (Modal web, mesmo padrão de AlertHost) exatamente UMA VEZ por instalação — nunca a cada login/reabertura do app (flag persistida em AsyncStorage, decisão travada de 13-CONTEXT.md: 'convite ÚNICO')."
    - "Ao tocar 'Ativar' no convite, subscribeToPush() é a primeira expressão síncrona do onPress do botão do Modal (mesmo critério 2 do ROADMAP que vale para o botão do Perfil — o toque no botão do Alert.web também é um gesto de usuário genuíno) — sem await antes."
    - "Tocar 'Agora não' (ou fechar o convite de qualquer forma) marca a flag como 'já mostrado' do mesmo jeito que tocar 'Ativar' — o convite nunca reaparece, mesmo se o aluno recusar (é convite único, não recorrente até aceitar)."
    - "Onboarding incompleto (profile.current_plan_id ausente) ou usuário deslogado NUNCA dispara o convite — o momento oportuno escolhido é depois que o aluno já tem um plano de treino ativo, nunca durante o questionário inicial."
  artifacts:
    - src/components/PushInviteHost.tsx
    - __tests__/pushInviteHost.test.tsx
  key_links:
    - "App.tsx (mesma árvore de UpdateBanner/AlertHost, montado uma vez para o app inteiro) -> PushInviteHost -> src/utils/alertShim.ts showAlert() -> src/services/pushSubscription.ts subscribeToPush() (Plano 13-01) -> apiClient.post(ENDPOINTS.PUSH.SUBSCRIBE)."
  prohibitions:
    - statement: "PushInviteHost nunca duplica a lógica de subscribe/persistência — reusa subscribeToPush() e o mesmo POST /api/push/subscribe do Plano 13-01, sem reimplementar upsert nem chamar PushManager diretamente."
      status: resolved
      verification: judgment
---

<objective>
Fechar a peça que faltava de PUSH-01/opt-in no ROADMAP: "convite ÚNICO via
alertShim em momento oportuno" (13-CONTEXT.md, seção Frontend) — a razão
literal pela qual a Fase 13 declara dependência da Fase 9 no ROADMAP ("o
convite de opt-in usa o alertShim"). Sem este plano, o ÚNICO caminho de
opt-in seria o aluno descobrir sozinho o botão "Ativar notificações" no
Perfil (Plano 13-01) — o convite proativo, mostrado uma única vez via o
Modal web já usado em todo o app (`alertShim`/`AlertHost`, Fase 9), é quem
efetivamente convida o aluno a ativar.

Nota de descoberta: este plano foi adicionado após o levantamento inicial das
4 outras plans da fase — a auditoria de cobertura contra 13-CONTEXT.md
(seção "Frontend (opt-in + service worker)") encontrou este item travado sem
nenhuma task cobrindo-o. Corrigido aqui, antes de finalizar o conjunto de
plans, para não fechar a fase com um requisito do dono silenciosamente
descartado.

Purpose: aumentar a chance real de opt-in sem exigir que o aluno navegue até
o Perfil por conta própria, sem nunca incomodar durante o onboarding nem
repetir o convite para quem já decidiu (em qualquer direção).
Output: `src/components/PushInviteHost.tsx` montado em `App.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-push-notification-ponta-a-ponta/13-CONTEXT.md
@App.tsx
@src/utils/alertShim.ts
@src/components/AlertHost.tsx
@src/components/UpdateBanner.tsx
@src/contexts/AuthContext.tsx
@src/services/auth/secureStorage.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PushInviteHost — decide UMA VEZ, convida via alertShim (PUSH-01)</name>
  <reversibility rating="reversible">Componente novo, mesmo padrão de UpdateBanner/AlertHost — remover é apagar o import/linha em App.tsx, sem efeito em nenhum dado.</reversibility>
  <files>src/components/PushInviteHost.tsx, __tests__/pushInviteHost.test.tsx</files>
  <read_first>
    13-CONTEXT.md ("Frontend (opt-in + service worker)": "Opt-in: botão
    'Ativar notificações' no Perfil + convite ÚNICO via alertShim em momento
    oportuno"; "Frontend opt-in — gesto síncrono e critério 2 literal");
    src/utils/alertShim.ts (assinatura de `showAlert(title, message?,
    buttons?)`, `AlertButton` — no web renderiza Modal via `alertStore`,
    cada `onPress` de botão É um gesto real de toque);
    src/components/UpdateBanner.tsx (molde de componente global montado uma
    vez em App.tsx, decide sozinho se deve aparecer, sem props externas);
    src/services/auth/secureStorage.ts (padrão de leitura/escrita
    `AsyncStorage` — mesmo import `@react-native-async-storage/async-storage`,
    já dependência do projeto);
    src/contexts/AuthContext.tsx (shape de `user`/`profile` retornado por
    `useAuth()` — `profile.current_plan_id` como sinal de onboarding
    completo, mesmo campo citado em `supabase/migrations/0001_modelo_treino.sql:30-32`);
    src/screens/ProfileScreen.tsx (Plano 13-01 — `isPushSupported`,
    `subscribeToPush` de `src/services/pushSubscription.ts`, reusados aqui
    sem duplicação).
  </read_first>
  <behavior>
    - Teste 1: usuário autenticado, `profile.current_plan_id` presente, push
      suportado, `Notification.permission === 'default'`, flag AsyncStorage
      `push_invite_shown` ausente — `showAlert` é chamado exatamente uma vez
      após o mount (dado um delay controlado/fake timer).
    - Teste 2: mesmas condições, mas flag `push_invite_shown` já `'true'` no
      AsyncStorage — `showAlert` NUNCA é chamado.
    - Teste 3: `profile.current_plan_id` ausente (onboarding incompleto) —
      `showAlert` nunca é chamado, independentemente dos outros sinais.
    - Teste 4: `Notification.permission === 'granted'` OU `'denied'` (já
      decidido antes, por qualquer caminho) — `showAlert` nunca é chamado.
    - Teste 5: simular o toque no botão "Ativar" do convite — `subscribeToPush`
      é chamado como a primeira expressão do handler (nenhum `await`/checagem
      antes) E a flag `push_invite_shown` é gravada como `'true'` no AsyncStorage.
    - Teste 6: simular o toque no botão "Agora não" — `subscribeToPush` NUNCA
      é chamado, mas a flag `push_invite_shown` ainda assim é gravada como
      `'true'` (convite não reaparece mesmo recusado).
  </behavior>
  <action>
    Criar `src/components/PushInviteHost.tsx`: componente funcional sem
    props, montado uma vez (mesmo padrão de `UpdateBanner`/`AlertHost`).
    Usa `useAuth()` para `user`/`profile`. `useEffect` com dependências
    `[user?.id, profile?.current_plan_id]`: se `!user || !profile?.current_plan_id
    || !isPushSupported() || typeof Notification === 'undefined' ||
    Notification.permission !== 'default'`, retorna cedo (sem side-effect
    nenhum). Senão, checa a flag `AsyncStorage.getItem('push_invite_shown')`
    — se já `'true'`, retorna. Senão, agenda a exibição do convite (um
    pequeno atraso, ex.: `setTimeout(..., 2000)`, para não competir com o
    primeiro paint da tela — limpar o timeout no cleanup do efeito) chamando
    `showAlert('Ativar notificações?', 'Receba lembretes de treino e avisos
    de replanejamento direto no seu iPhone.', [{ text: 'Agora não', style:
    'cancel', onPress: () => marcarConviteMostrado() }, { text: 'Ativar',
    onPress: () => { subscribeToPush().then((subJson) =>
    apiClient.post(ENDPOINTS.PUSH.SUBSCRIBE, subJson)).catch((e) => {
    logger.warn('[pushInvite] falha ao ativar pelo convite:', e); });
    marcarConviteMostrado(); } }])` — a chamada a `subscribeToPush()` é
    LITERALMENTE a primeira linha do `onPress` do botão "Ativar" (mesmo
    critério 2 do botão do Perfil: o toque no botão do Modal web é o gesto
    de usuário que o iOS exige, e não pode ter nenhum `await`/checagem
    antes do `subscribe()`). Função local `marcarConviteMostrado = () =>
    AsyncStorage.setItem('push_invite_shown', 'true')` chamada nos DOIS
    botões (aceitar ou recusar) — o convite é único independente do
    resultado. Componente retorna `null` sempre (não renderiza nada
    diretamente — só orquestra o `showAlert`, que já é o Modal do
    `AlertHost`, já montado em `App.tsx`).

    Em `App.tsx`: importar `PushInviteHost` de
    `'./src/components/PushInviteHost'` e montá-lo DEPOIS de `AlertHost`
    (precisa que `alertStore`/`AlertHost` já estejam prontos para receber o
    `showAlert`), dentro do mesmo `<AuthProvider>`.

    Criar `__tests__/pushInviteHost.test.tsx` com docblock `@jest-environment
    jsdom` como primeiro token (usa `AsyncStorage`/timers reais); mock de
    `useAuth`, `useAlertStore`/`showAlert` (ou spy real sobre
    `alertStore.show`), `subscribeToPush`/`isPushSupported` de
    `pushSubscription.ts`, e `@react-native-async-storage/async-storage`
    (mock in-memory simples ou o mock oficial `@react-native-async-storage/async-storage/jest/async-storage-mock`,
    se já configurado no projeto — checar `jest` config/`__mocks__` antes de
    inventar um novo). Usar `jest.useFakeTimers()` para controlar o
    `setTimeout` do atraso de exibição.
  </action>
  <verify>
    <automated>npx jest __tests__/pushInviteHost.test.tsx --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/pushInviteHost.test.tsx` sai com código 0 e cobre os 6 comportamentos do bloco `<behavior>`.
    - `grep -n "subscribeToPush()" src/components/PushInviteHost.tsx` mostra a chamada como a primeira expressão do `onPress` do botão "Ativar" (nenhum `await`/`if` antes no mesmo bloco).
    - `grep -n "push_invite_shown" src/components/PushInviteHost.tsx` aparece nos dois caminhos (aceitar e recusar).
  </acceptance_criteria>
  <done>Convite único de opt-in entregue e testado — nunca aparece mais de uma vez, nunca durante onboarding incompleto, sempre reusando o pipeline de subscribe do Plano 13-01.</done>
</task>

<task type="auto">
  <name>Task 2: Regressão de App.tsx (montagem não quebra o boot do app)</name>
  <reversibility rating="reversible">Só a linha de montagem em App.tsx — reverter é remover a linha.</reversibility>
  <files>App.tsx</files>
  <read_first>
    App.tsx (estado após o Task 1 — `PushInviteHost` já importado/montado).
  </read_first>
  <action>
    Confirmar que `App.tsx` monta `PushInviteHost` dentro de `<AuthProvider>`,
    após `<AlertHost />`, sem alterar a ordem de `<RootNavigator />`/
    `<UpdateBanner />` já existentes — nenhuma outra mudança nesta task além
    da posição de montagem, já feita no Task 1 (esta task é a verificação de
    regressão, não uma nova implementação).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p .</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p .` sai com código 0.
    - `grep -n "PushInviteHost" App.tsx` mostra o import e a montagem, na árvore de `AuthProvider`.
  </acceptance_criteria>
  <done>App.tsx continua compilando e montando a árvore existente sem regressão, com PushInviteHost incluído.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PushInviteHost (global, montado uma vez) → alertShim/AlertHost (Modal web já existente) | Nenhuma superfície nova — reusa o mesmo canal de UI já auditado na Fase 9. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-12 | Denial of Service (UX) | Convite reaparecendo em loop, incomodando o aluno | low | mitigate | Flag `push_invite_shown` gravada nos DOIS caminhos (aceitar/recusar) antes de qualquer chance de re-disparo — testado explicitamente (Teste 6). |

</threat_model>

<verification>
`npx jest __tests__/pushInviteHost.test.tsx` verde; `npx tsc --noEmit -p .`
sai com código 0.
</verification>

<success_criteria>
O convite proativo de opt-in aparece exatamente uma vez, no momento certo
(onboarding completo, permissão ainda não decidida), e nunca mais depois
disso — reusando o mesmo pipeline de subscribe do botão do Perfil.
</success_criteria>

## Artifacts this phase produces

- `src/components/PushInviteHost.tsx` — novo (Task 1): convite único via alertShim.
- `__tests__/pushInviteHost.test.tsx` — novo (Task 1): guarda dos 6 comportamentos.
- `App.tsx` — modificado (Task 1, confirmado Task 2): monta `PushInviteHost`.

<output>
Create `.planning/phases/13-push-notification-ponta-a-ponta/13-05-SUMMARY.md` when done
</output>
