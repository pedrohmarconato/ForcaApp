---
phase: 11-service-worker-e-atualiza-o-segura
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified: []
autonomous: false
requirements: [OFF-01, OFF-02]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Vercel Project Settings -> General -> Node.js Version está em 20 ou mais antes do primeiro deploy que depende do workbox-cli (que exige Node >=20)."
    - "curl -I em produção confirma Cache-Control: no-cache, must-revalidate para /sw.js, /register-sw.js e /manifest.json."
    - "Com o PWA instalado e o iPhone em modo avião, abrir o app pelo ícone da Tela de Início mostra a casca do app (não uma tela branca/erro de rede)."
  artifacts: []
  key_links:
    - "vercel deploy --prod (manual, dono) -> dist/sw.js + vercel.json dos Planos 11-01/11-02 servidos em produção -> curl -I confirma os headers"
  prohibitions: []
---

<objective>
Fechar a Fase 11 com os três itens que só podem ser verificados fora deste
ambiente de desenvolvimento: a versão de Node configurada no painel da Vercel
(pré-requisito do workbox-cli), os headers de produção reais (`curl -I`) e o
comportamento offline no iPhone físico (modo avião). Nenhuma automação
substitui esses três passos — máquina de dev sem toolchain nativa, sem deploy
automático (`git.deploymentEnabled: false`) e sem simulador de iPhone real.

Purpose: fechar os critérios de sucesso 2 e 4 do ROADMAP (Fase 11), que
dependem de infraestrutura fora do alcance de comandos automatizados deste
clone.
Output: confirmação humana registrada de que o pipeline dos Planos
11-01/11-02 funciona em produção e offline no dispositivo real.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-service-worker-e-atualiza-o-segura/11-01-SUMMARY.md
@.planning/phases/11-service-worker-e-atualiza-o-segura/11-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirmar Node.js Version >=20 no painel da Vercel</name>
  <what-built>Pipeline de build que depende de workbox-cli@7.4.1 (exige Node >=20) encadeado no buildCommand da Vercel (Plano 11-01).</what-built>
  <how-to-verify>
    1. Abrir o painel da Vercel -> projeto forca-app -> Settings -> General -> Node.js Version.
    2. Confirmar que está em 20.x ou mais recente. Se estiver abaixo de 20, atualizar para 20.x (ou mais recente) ANTES de rodar `vercel deploy --prod` — senão o build de produção falha com erro de engine dentro do workbox-build.
  </how-to-verify>
  <resume-signal>Digite "node-ok" quando confirmar (ou já ter corrigido) a versão, ou descreva o que encontrou.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy manual + curl -I nos headers de produção (OFF-02, critério 2)</name>
  <what-built>vercel.json com headers Cache-Control: no-cache, must-revalidate para sw.js, register-sw.js e manifest.json (Plano 11-01), e o pipeline de build que gera esses arquivos.</what-built>
  <how-to-verify>
    1. Rodar `vercel deploy --prod` manualmente (deploy automático está
       desligado por decisão do projeto — `git.deploymentEnabled: false`).
    2. Depois do deploy concluir, rodar: `curl -I https://<domínio-de-produção>/sw.js`,
       `curl -I https://<domínio-de-produção>/register-sw.js`,
       `curl -I https://<domínio-de-produção>/manifest.json`.
    3. Confirmar que os três respondem `Cache-Control: no-cache, must-revalidate` no header.
  </how-to-verify>
  <resume-signal>Cole a saída dos três `curl -I` (ou confirme "headers ok") para eu registrar a evidência.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT do dono no iPhone real — modo avião (OFF-01, critério 4)</name>
  <what-built>Service worker (Workbox generateSW) que precacheia o app shell completo, registrado via register-sw.js em toda visita HTTPS (Planos 11-01/11-02).</what-built>
  <how-to-verify>
    1. No iPhone real, com o ForçaApp já instalado pela Tela de Início (Fase
       10), abrir o app normalmente pelo menos uma vez enquanto conectado à
       internet (para o SW instalar e precachear).
    2. Ativar o Modo Avião.
    3. Fechar o app completamente e abrir de novo pelo ícone da Tela de
       Início.
    4. Confirmar que a casca do app aparece (splash, layout, navegação) mesmo
       sem rede — não precisa funcionar por completo (login/dados exigem
       rede), só a casca estática.
  </how-to-verify>
  <resume-signal>Digite "offline-ok" se a casca apareceu sem rede, ou descreva o que travou/apareceu em branco.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dono (humano) -> `vercel deploy --prod` | Único caminho de publicação em produção; sem CI de deploy automático. |
| Produção pública (`curl -I`) | Superfície externa que qualquer cliente HTTP pode inspecionar sem autenticação. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08 | Tampering | `vercel deploy --prod` manual fora de CI | low | accept | Decisão já aceita do projeto (`git.deploymentEnabled: false`) — este plano não adiciona automação de deploy nova, só confirma que o pipeline dos Planos 11-01/11-02 funciona depois do deploy manual existente. |
| T-11-09 | Information Disclosure | `curl -I` expõe headers de cache publicamente | low | accept | Headers de Cache-Control são informação pública e inofensiva por natureza — nenhum dado sensível trafega neste checkpoint. |

</threat_model>

<verification>
Os três checkpoints humanos confirmam, em ordem: versão de Node compatível com
workbox-cli no painel da Vercel, headers de cache corretos em produção via
`curl -I`, e a casca do app funcionando offline no iPhone real em modo avião.
</verification>

<success_criteria>
Critérios de sucesso 2 e 4 do ROADMAP (Fase 11) confirmados pelo dono em
produção/dispositivo real; nenhum dos dois depende mais de suposição — ambos
têm evidência registrada. Fase 11 completa (OFF-01, OFF-02).
</success_criteria>

## Artifacts this phase produces

Nenhum artefato de código novo — este plano só produz evidência de verificação
humana (registrada no SUMMARY.md), fechando os dois critérios de sucesso do
ROADMAP que dependem de infraestrutura fora deste clone.

<output>
Create `.planning/phases/11-service-worker-e-atualiza-o-segura/11-03-SUMMARY.md` when done
</output>
