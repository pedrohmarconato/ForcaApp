---
phase: 05-integra-o-e-review-do-gr-fico-de-cardio
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-PAINEL-REPORT.md
  - src/engine/cardioEvolucao.ts
  - src/components/progress/CardioEvolucaoChart.tsx
  - __tests__/cardioEvolucao.test.ts
  - src/screens/ProgressScreen.tsx
autonomous: false
requirements: [INT-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O painel avalia o diff `origin/main..HEAD` COMPLETO (não só o(s) commit(s) do 05-01) — 4 agentes em paralelo, `revisor-seguranca`, `revisor-integridade`, `revisor-regressao`, `revisor-contrato` — cada um lendo, nunca editando — D-03, ROADMAP Fase 5 SC4."
    - "`05-PAINEL-REPORT.md` existe com uma linha de bottom-line, uma tabela de achados (# | Severidade | Achado | Prova file:linha | Cenário de falha | Revisor | Resolução) e uma seção explícita do que NÃO foi encontrado — mesmo que a lista de achados fique vazia, o relatório afirma isso explicitamente, nunca por omissão."
    - "Todo achado CONFIRMADO tem status terminal de Resolução — `corrigido-com-teste` (teste que reproduz a falha commitado ANTES do fix) ou `aceito-pelo-dono:<motivo>` — nenhuma linha fica `pendente-decisão-do-dono` ao fechar a fase — D-03, regra do projeto (teste antes do fix)."
    - "O checkpoint apresenta o relatório ao dono e aguarda a resposta real dele por achado antes de a Task 3 agir — nenhuma decisão é presumida silenciosamente."
    - "Nenhum `git push` foi executado nesta fase — `git rev-list --count origin/main..HEAD` só cresce (nunca zera) do início da Task 1 ao fim da Task 3 — D-06."
    - "Depois de qualquer fix aplicado, `npx tsc --noEmit` e `npm test` voltam a rodar e terminam verdes ANTES do commit da correção."
  artifacts:
    - .planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-PAINEL-REPORT.md
  key_links:
    - "4 agentes revisor-* (só leitura, mandato estreito por categoria) -> achados consolidados só CONFIRMADOS com file:linha em `05-PAINEL-REPORT.md` -> checkpoint:human-verify decide fix vs aceite por achado -> Task 3 aplica (teste-antes-do-fix) ou registra aceite -> commit único de docs fecha a fase sem push."
---

<objective>
Fase 5, plan 2 de 2 — painel adversarial de 4 revisores sobre o diff COMPLETO
`origin/main..HEAD` (que já inclui os 3 commits do `05-01-PLAN.md` mais os ~46
commits acumulados de v1.0/v1.1 que ainda não foram publicados), consolidação
dos achados CONFIRMADOS num relatório, decisão do dono por achado (corrigir ou
aceitar) via checkpoint, e fechamento com teste-antes-do-fix para o que for
corrigido — sem nenhum push (D-06, que é a Fase 6).

**Nota sobre tracer-first:** não se aplica — este plano é revisão e
remediação de código já existente, não construção de feature nova; a ordem é
a natural do processo de review: rodar painel → checkpoint de decisão do dono
→ aplicar resolução.

**Escopo condicional dos arquivos de fix:** `files_modified` lista os 4
arquivos do gráfico de cardio (D-07: único código em escopo desta fase) como
alvos CONDICIONAIS de correção — só são tocados se um achado CONFIRMADO do
painel apontar um problema real neles. Se o painel voltar limpo (Fase 4 do
skill `/painel`: "passou limpo" também é um resultado válido e deve ser
declarado explicitamente), nenhum desses 4 arquivos é modificado nesta fase —
só o relatório é commitado.

Purpose: fechar INT-02 (REQUIREMENTS.md) — todo o diff que vai a produção
passa por painel adversarial antes do push da Fase 6.
Output: `05-PAINEL-REPORT.md` com resolução final por achado; 0-N commits de
fix/test (D-08); nenhum push.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-CONTEXT.md
@.planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-01-PLAN.md
@$HOME/.claude/skills/painel/SKILL.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `.planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-PAINEL-REPORT.md` (NOVO): relatório
  consolidado do painel de 4 revisores, com resolução terminal por achado.
- 0 a N commits `fix(05): ...` / `test(05): ...` (só se o painel confirmar achado e o dono pedir correção)
  — nenhum símbolo de código novo é criado a priori; o que existe é fix pontual sobre os 4 arquivos já
  presentes (D-07).
- 1 commit `docs(05): ...` com o relatório finalizado.
- Nenhum push (D-06) — `origin/main` não muda nesta fase.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: rodar o painel adversarial de 4 revisores sobre origin/main..HEAD e consolidar achados</name>
  <files>.planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-PAINEL-REPORT.md</files>
  <read_first>
    `$HOME/.claude/skills/painel/SKILL.md` INTEIRO (já em contexto) — Fase 1 (montar o alvo), Fase 2
    (tabela de mandato dos 4 agentes, disparo em paralelo NA MESMA mensagem), Fase 3 (descartar
    especulativo, deduplicar, reconferir achado de severidade alta abrindo o arquivo, ordenar por
    severidade), Fase 4 (formato do relatório: bottom-line + tabela + "o que não foi encontrado"), regra
    "nenhum revisor edita arquivo".
    `05-CONTEXT.md` `<decisions>` D-03 (locked — os 4 agentes exatos e a regra de achado CONFIRMADO
    file:linha) e `<specifics>` ("o diff que o painel avalia é origin/main..HEAD — tudo que vai a produção
    inclui os ~46 commits já feitos + o(s) commit(s) novo(s) desta fase").
    `05-01-PLAN.md` `<objective>` e `must_haves` — a lista exata dos 3 commits que este painel também
    precisa cobrir (chore/docs/feat da Task 1 do plano anterior).
  </read_first>
  <action>
    Rode `git fetch origin main --quiet` (só leitura, atualiza a referência remota sem tocar no working
    tree local — outras sessões podem ter avançado `origin/main` por caminhos que não são desta fase).
    Capture `git rev-parse origin/main` e `git rev-list --count origin/main..HEAD` — guarde os dois valores
    no SUMMARY.md como baseline (vão ser reconferidos no fim da Task 3 para provar que nada foi empurrado).
    Rode `git diff --stat origin/main..HEAD` para montar a lista de arquivos tocados. Monte o "pedido
    original" que cada revisor recebe: a meta da Fase 5 (ROADMAP.md), as decisões D-01 a D-08 do
    05-CONTEXT.md resumidas, e como rodar os testes do projeto (`npm test`, `npx tsc --noEmit`, `python3 -m
    pytest backend/tests -q`). Dispare, na MESMA mensagem (para rodarem em paralelo), 4 chamadas ao Task
    tool — uma para cada `subagent_type`: `revisor-seguranca`, `revisor-integridade`, `revisor-regressao`,
    `revisor-contrato` — cada uma recebendo: o alvo (`origin/main..HEAD` no branch `main` do repo
    `/Users/phmarconato/ForcaApp`), a lista de arquivos tocados, o pedido original montado acima, o caminho
    do repo, e a instrução explícita de que o mandato é o da linha correspondente na tabela da Fase 2 do
    SKILL.md — nenhum revisor deve "revisar tudo", e nenhum revisor pode usar Edit/Write, só ler e reportar
    achados com `file:linha`. Depois que os 4 retornarem, consolide seguindo a Fase 3 do SKILL.md:
    descarte todo achado ESPECULATIVO (não entra no relatório); deduplique achados coincidentes de
    revisores diferentes numa única linha citando as duas leituras; para cada achado de severidade alta,
    abra o arquivo você mesmo e confirme antes de incluir; ordene por severidade. Escreva
    `05-PAINEL-REPORT.md` no formato da Fase 4: uma linha de bottom-line; a tabela de achados com uma
    coluna extra `Resolução` (valor inicial `pendente-decisão-do-dono` para cada linha); e uma seção
    explícita do que NÃO foi encontrado — se os 4 revisores voltarem limpos, o relatório afirma isso por
    extenso ("os 4 revisores (segurança, integridade, regressão, contrato) avaliaram o diff completo
    origin/main..HEAD e não confirmaram nenhum achado"), nunca por ausência de seção. NÃO faça `git add`
    nem commit deste arquivo ainda — a Task 3 commita a versão final, já com resolução.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-PAINEL-REPORT.md && git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    `05-PAINEL-REPORT.md` existe e contém, no mínimo: uma linha de bottom-line, a string `Resolução`
    (cabeçalho da coluna), e a seção do que não foi encontrado. Se a tabela tiver alguma linha de achado,
    cada uma tem uma referência no formato `arquivo:número` na coluna de prova. `git status --porcelain`
    mostra o relatório como o ÚNICO arquivo novo/alterado nesta task (nenhum revisor editou código —
    prova de que a regra "nenhum revisor edita arquivo" do SKILL.md foi respeitada).
  </acceptance_criteria>
  <done>Painel de 4 revisores rodou em paralelo sobre o diff completo que vai a produção; relatório
  consolidado existe com achados CONFIRMADOS (ou declaração explícita de que não houve nenhum) e cada
  achado tem status pendente de decisão, pronto para o checkpoint da Task 2.</done>
  <reversibility rating="reversible">Só escreve um arquivo de relatório novo, não commitado ainda — sem
  efeito no código ou no histórico de git.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: decisão do dono por achado do painel — corrigir ou aceitar</name>
  <what-built>Painel adversarial completo sobre o diff `origin/main..HEAD` (todo o histórico local que vai
  para produção na Fase 6, incluindo os 3 commits do `05-01-PLAN.md`) — relatório em
  `.planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-PAINEL-REPORT.md` com o bottom-line, a
  tabela de achados CONFIRMADOS (se houver) e a seção do que passou limpo.</what-built>
  <how-to-verify>
    Leia `.planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-PAINEL-REPORT.md`. Se a tabela
    estiver vazia (os 4 revisores voltaram limpos), responda confirmando que pode fechar a fase sem
    correção. Se houver 1 ou mais achados CONFIRMADOS, decida, PARA CADA achado (pelo número da linha da
    tabela): "corrigir" (a Task 3 escreve um teste que reproduz a falha, corrige, roda a suíte de novo e
    commita) ou "aceitar" (a Task 3 só registra a aceitação e a justificativa no relatório, sem tocar em
    código). Se algum achado exigir tocar em arquivo FORA dos 4 do D-01 (`cardioEvolucao.ts`,
    `CardioEvolucaoChart.tsx`, `cardioEvolucao.test.ts`, `ProgressScreen.tsx`), essa expansão de escopo
    exige aprovação explícita sua nesta resposta — a Task 3 não expande escopo por conta própria.
  </how-to-verify>
  <resume-signal>Responda com a decisão por achado (ex.: "achado 1: corrigir; achado 2: aceitar — motivo:
  X") ou "aprovado, sem achados" se o relatório voltou limpo.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: aplicar resolução do painel (fix com teste-antes ou registro de aceite) e fechar com um commit de docs</name>
  <files>.planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-PAINEL-REPORT.md, src/engine/cardioEvolucao.ts, src/components/progress/CardioEvolucaoChart.tsx, __tests__/cardioEvolucao.test.ts, src/screens/ProgressScreen.tsx</files>
  <read_first>
    A resposta do dono capturada na Task 2 (checkpoint) — a fonte de verdade de qual achado vira fix e
    qual vira aceite; nenhuma decisão é presumida a partir do relatório sozinho.
    `05-PAINEL-REPORT.md` (escrito pela Task 1) — a tabela de achados com a coluna `Resolução` a
    preencher.
    Regra do projeto (CLAUDE.md do usuário, seção "Loop de PR: review → fix → merge" e
    "common/testing.md"): todo achado corrigido exige um teste que reproduza o modo de falha ANTES do
    fix, rodado e visto falhando, só então a correção.
  </read_first>
  <action>
    Se a resposta do checkpoint foi "aprovado, sem achados": pule direto para o commit final abaixo, sem
    nenhuma alteração de código. Caso contrário, para CADA achado marcado "corrigir": escreva, no arquivo
    de teste apropriado (`__tests__/cardioEvolucao.test.ts` se o achado for no motor/componente do
    gráfico, ou o arquivo de teste correspondente se a decisão do dono autorizou tocar fora do escopo do
    D-01), um caso que reproduz exatamente o cenário de falha descrito na coluna "Cenário de falha" do
    relatório; rode esse teste sozinho e confirme que falha (RED); implemente a correção mínima no arquivo
    apontado pela coluna "Prova (file:linha)"; rode o teste de novo e confirme que passa (GREEN); rode
    `npx tsc --noEmit` e `npm test` completos e confirme 0 erro/0 falha; stage nomeado só dos arquivos
    deste achado (nunca em lote); commit `fix(05): <descrição curta do achado>` (ou `test(05): ...` se a
    mudança for só de teste), sem gerúndio, corpo curto citando o número do achado do relatório (D-08).
    Para CADA achado marcado "aceitar": edite APENAS a coluna `Resolução` da linha correspondente em
    `05-PAINEL-REPORT.md` para `aceito-pelo-dono: <justificativa da resposta do checkpoint> (<data de
    hoje>)` — nenhuma mudança de código. Depois de processar todos os achados (ou confirmar que não havia
    nenhum), atualize a coluna `Resolução` de toda linha corrigida para `corrigido-com-teste` e faça o
    commit final: stage nomeado `git add
    .planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-PAINEL-REPORT.md`, commit `docs(05):
    registra painel adversarial pré-push com resolução por achado`. Reconfira, ao final, `git rev-list
    --count origin/main..HEAD` contra o valor capturado no início da Task 1 — o número só pode ter
    crescido (nunca voltado a um valor menor, o que indicaria um push acidental).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    Toda linha de achado em `05-PAINEL-REPORT.md` tem `Resolução` preenchida com `corrigido-com-teste` ou
    `aceito-pelo-dono:...` — nenhuma linha permanece `pendente-decisão-do-dono`. Se houve fix, `git log
    --name-only` de cada commit `fix`/`test` lista só os arquivos daquele achado (nunca um add em lote).
    `npx tsc --noEmit` sai 0. `npm test` termina sem `failed`. `git status --porcelain` sai vazio ao final.
    `git rev-list --count origin/main..HEAD` é maior ou igual ao valor capturado no início da Task 1 —
    nunca menor (prova de que nenhum push aconteceu, D-06).
  </acceptance_criteria>
  <done>Todo achado CONFIRMADO do painel tem resolução terminal e rastreável; correções (se houve) seguem
  teste-antes-do-fix e ficam em commits atômicos nomeados; relatório final commitado; fase encerra sem
  push, pronta para a Fase 6.</done>
  <reversibility rating="reversible">Commits locais, não empurrados (D-06) — reversíveis até a Fase
  6.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `origin/main..HEAD` (108 arquivos / ~8.7k inserções de v1.0+v1.1 ainda não publicados) → produção (Fase 6 em diante) | Este é o ÚLTIMO portão de revisão antes do push; qualquer coisa que passar aqui sem achado vira parte do histórico público do repositório. |
| 4 subagentes revisor-* → relatório consolidado | Os revisores leem código (inclusive potencialmente sensível) mas nunca escrevem; a fronteira de confiança é "leem e reportam", não "leem e corrigem". |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-05 | Repudiation | Achado CONFIRMADO do painel descartado silenciosamente, sem fix nem aceite registrado, tornando a alegação "foi revisado" não-verificável depois | high | mitigate | Toda linha de `05-PAINEL-REPORT.md` precisa de status terminal (`corrigido-com-teste` ou `aceito-pelo-dono:<motivo>`) antes do commit final da Task 3; acceptance_criteria da Task 3 bloqueia em qualquer linha `pendente-decisão-do-dono` remanescente. |
| T-05-06 | Tampering | Um subagente revisor edita código diretamente em vez de só reportar (viola o contrato read-only do skill /painel) | medium | mitigate | A action da Task 1 instrui explicitamente cada revisor-* a nunca usar Edit/Write; a acceptance_criteria confirma via `git status --porcelain` que só o relatório mudou depois dos 4 retornarem. |
| T-05-07 | Information Disclosure / Tampering | `revisor-seguranca` é o único ponto que varre segredo/PII/furo de RLS/injeção nos ~46 commits que nunca passaram por push nem CI — é a primeira vez que este código enfrenta um review adversarial | high | mitigate | `revisor-seguranca` é um dos 4 agentes obrigatórios do D-03; qualquer achado CONFIRMADO de segurança bloqueia o fechamento da fase até corrigido ou explicitamente aceito pelo dono (ASVS L1, `security_block_on: high`) — nenhum achado alto fica silencioso. |
| T-05-08 | Elevation of Privilege | N/A nesta fase — nenhuma alteração de auth/permissão é feita pelas Tasks 1-3; a cobertura de RLS/permissão fica a cargo do mandato do `revisor-seguranca` sobre o diff, não de uma mudança de código nova aqui | low | accept | Coberto pelo T-05-07 (mandato diff-wide do revisor-seguranca) em vez de um controle separado. |
</threat_model>

<verification>
- `05-PAINEL-REPORT.md` existe com bottom-line, tabela de achados (ou declaração explícita de "limpo") e
  seção do que não foi encontrado.
- Toda linha de achado tem `Resolução` terminal antes do commit final.
- `npx tsc --noEmit` e `npm test` verdes após qualquer fix.
- `git status --porcelain` vazio ao final.
- `git rev-list --count origin/main..HEAD` nunca diminui ao longo da fase (nenhum push).
</verification>

<success_criteria>
Todo o diff que vai a produção (origin/main..HEAD, incluindo os commits do 05-01-PLAN.md) passou por
painel adversarial de 4 revisores (D-03); achados CONFIRMADOS foram corrigidos com teste-antes-do-fix ou
explicitamente aceitos pelo dono; nenhum push ocorreu (D-06) — a Fase 5 encerra pronta para a Fase 6
publicar.
</success_criteria>

<output>
Criar `.planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-02-SUMMARY.md` ao final.
</output>
