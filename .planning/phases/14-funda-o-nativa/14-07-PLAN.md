---
phase: 14-funda-o-nativa
plan: 07
type: execute
wave: 5
depends_on: ["14-06"]
files_modified:
  - app.json
  - targets/session-widget/expo-target.config.js
  - .planning/phases/14-funda-o-nativa/14-SPIKE-APP-GROUPS.md
autonomous: true
requirements: [NAT-02]

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - statement: "14-SPIKE-APP-GROUPS.md documenta explicitamente COM ou SEM App Group, com a data e o resultado PASS/FAIL/AMBIGUO reportado pelo dono, citavel pelas fases 15-17"
      verification: backstop
    - statement: "Se o resultado do round-trip do spike for AMBIGUO (funciona em um sentido mas nao no outro, ou de forma inconsistente entre tentativas), a decisao registrada trata o caso como SEM App Group (default conservador) ate nova evidencia"
      verification: backstop
    - statement: "Se a leitura do UserDefaults(suiteName:) compartilhado retornar nil/vazio em qualquer direcao do round-trip, o resultado e tratado como FALHA (nao como 'ainda nao testado') e a decisao registrada e SEM App Group"
      verification: backstop
  artifacts:
    - .planning/phases/14-funda-o-nativa/14-SPIKE-APP-GROUPS.md
  key_links:
    - "14-SPIKE-APP-GROUPS.md e a fonte de verdade que as fases 15 (LOCK) e 16 (CMD) devem citar antes de decidir a arquitetura de estado da Live Activity"
  prohibitions:
    - statement: "Nunca manter a entitlement com.apple.security.application-groups configurada em app.json/expo-target.config.js se o spike nao confirmou PASS nas duas direcoes — reverter e obrigatorio no branch FAIL/AMBIGUO"
      status: flagged-unverified
---

<objective>
Registrar por escrito, a partir do resultado físico reportado na Plano
14-06, a decisão de arquitetura que orienta as Fases 15-17 (com ou sem App
Group) — e deixar o repositório num estado consistente com essa decisão: sem
entitlement órfã se o spike falhou, com entitlement congelada (D-06) se
aprovou.

Purpose: NAT-02 exige literalmente que essa decisão "esteja registrada por
escrito"; deixar a entitlement configurada sem uma capability funcionando por
trás dela é o Pitfall 5/6 (assinatura quebrada silenciosamente, quota
queimada) — reverter é tão importante quanto documentar.
Output: `.planning/phases/14-funda-o-nativa/14-SPIKE-APP-GROUPS.md` com a
decisão explícita; `app.json`/`targets/session-widget/expo-target.config.js`
em estado consistente com ela.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-funda-o-nativa/14-CONTEXT.md
@.planning/phases/14-funda-o-nativa/14-06-SUMMARY.md
@.planning/phases/14-funda-o-nativa/14-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar a decisão do spike e reverter/manter a entitlement</name>
  <files>app.json, targets/session-widget/expo-target.config.js, .planning/phases/14-funda-o-nativa/14-SPIKE-APP-GROUPS.md</files>
  <read_first>.planning/phases/14-funda-o-nativa/14-06-SUMMARY.md (resultado PASS/FAIL/AMBIGUO reportado pelo dono); app.json (estado pós-Plano 14-05, com entitlement de spike); targets/session-widget/expo-target.config.js (estado pós-Plano 14-05); modules/app-group-spike/ (código temporário a remover em qualquer branch); scripts/verify-native-skeleton.sh (Plano 14-02, gate final desta task)</read_first>
  <action>
    Ler o resultado reportado em 14-06-SUMMARY.md e ramificar:
    - Se `round-trip=PASS` (confirmado nas duas direções): MANTER a
      entitlement `com.apple.security.application-groups` e o App Group ID em
      `app.json` e `targets/session-widget/expo-target.config.js` (D-06:
      congelado, é a arquitetura decidida) — mas DELETAR o bloco
      `// SPIKE-ONLY (14-05)` do arquivo Swift do target de widget e DELETAR
      inteiramente o diretório `modules/app-group-spike/` (seu único propósito
      era este spike; as Fases 15/16 constroem o consumo real de
      ActivityKit/App Group do zero, não estendem este módulo descartável).
    - Se `round-trip=FAIL` ou `round-trip=AMBIGUO` (política resolvida para o
      caso de borda "resultado ambíguo/vazio": tratar como SEM App Group até
      nova evidência — nunca presumir sucesso por omissão): REMOVER
      inteiramente a chave `com.apple.security.application-groups` de AMBOS
      `app.json` e `targets/session-widget/expo-target.config.js` (Pitfall
      5/6 — nenhuma entitlement sem capability funcionando por trás), e
      também deletar o bloco `// SPIKE-ONLY (14-05)` e
      `modules/app-group-spike/`.
    Em qualquer um dos dois branches, rodar `bash
    scripts/verify-native-skeleton.sh` (Plano 14-02) para confirmar que o
    esqueleto nativo ainda sobrevive a `--clean` depois da mudança de
    entitlement. Escrever
    `.planning/phases/14-funda-o-nativa/14-SPIKE-APP-GROUPS.md` contendo: a
    data, o resultado literal reportado em 14-06 (`app=...,
    round-trip=...`), a linha `Decisão: COM App Group (id = &lt;valor&gt;)` ou
    `Decisão: SEM App Group`, e um parágrafo explicando o que essa decisão
    implica para a arquitetura de estado das Fases 15 (LOCK) e 16 (CMD) —
    citável diretamente por elas sem precisar re-perguntar ao dono.
  </action>
  <verify>
    <automated>grep -q 'Decisão:' .planning/phases/14-funda-o-nativa/14-SPIKE-APP-GROUPS.md && bash scripts/verify-native-skeleton.sh</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/14-funda-o-nativa/14-SPIKE-APP-GROUPS.md` existe e
    contém a string literal `Decisão:` seguida de `COM App Group` ou `SEM App
    Group`; se `SEM App Group`, nem `app.json` nem
    `targets/session-widget/expo-target.config.js` contêm a string
    `application-groups`; `modules/app-group-spike/` não existe mais em
    nenhum dos dois branches; `bash scripts/verify-native-skeleton.sh` sai com
    código 0 depois da mudança.
  </acceptance_criteria>
  <done>Decisão do spike registrada por escrito; repositório consistente com ela (sem entitlement órfã, sem código de spike remanescente).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Estado congelado de entitlements (app.json / expo-target.config.js) | Última escrita nestes arquivos antes das Fases 15-17 assumirem a decisão como fato consolidado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07-01 | Tampering | Entitlement órfã deixada após decisão negativa | high | mitigate | Revert obrigatório no branch FAIL/AMBIGUO + re-run de `scripts/verify-native-skeleton.sh` nesta mesma task (Pitfall 5) |
| T-14-07-02 | Repudiation | Decisão de arquitetura não citável pelas fases seguintes | medium | mitigate | `14-SPIKE-APP-GROUPS.md` com a linha `Decisão:` explícita e parágrafo de implicação para as Fases 15/16 |
</threat_model>

## Artifacts this phase produces

- `.planning/phases/14-funda-o-nativa/14-SPIKE-APP-GROUPS.md` (novo)

<verification>
`grep -q 'Decisão:' .planning/phases/14-funda-o-nativa/14-SPIKE-APP-GROUPS.md
&& bash scripts/verify-native-skeleton.sh` deve sair com código 0.
</verification>

<success_criteria>
A decisão de arquitetura (COM ou SEM App Group) está registrada por escrito e
o repositório está em estado consistente com ela — sem entitlement órfã, sem
código de spike remanescente.
</success_criteria>

<output>
Create `.planning/phases/14-funda-o-nativa/14-07-SUMMARY.md` when done.
</output>
