---
phase: 14-funda-o-nativa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [NAT-01, NAT-02]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A decisão de bundle identifier (app principal + target de widget + App Group candidato) está registrada pelo dono antes de qualquer scaffold nativo ser criado (D-06)"
    - "Os dois pacotes sinalizados SUS pelo gate de legitimidade (@bacons/apple-targets, expo-build-properties) têm aprovação explícita do dono antes de qualquer npm install"
  artifacts: []
  key_links:
    - "Decisão desta Plano 14-01 → app.json + targets/session-widget/expo-target.config.js escritos em 14-02"
  prohibitions:
    - statement: "Nunca instalar pacote npm/pip/cargo sinalizado ASSUMED/SUS sem a confirmação explícita do dono contra o registro público (npmjs.com), mesmo quando a pesquisa já argumenta legitimidade"
      status: flagged-unverified
---

<objective>
Travar, antes de qualquer código nativo existir, as duas decisões que a Fase 14
não pode reverter sem custo: o esquema de bundle identifiers/App Group (D-06,
reversibilidade custosa — quota de ~10 App IDs/semana em time pessoal gratuito)
e a aprovação humana dos dois pacotes npm sinalizados SUS pelo gate de
legitimidade (`@bacons/apple-targets`, `expo-build-properties`).

Purpose: nenhuma linha de `app.json`, `targets/` ou `modules/` deve existir
antes dessas duas confirmações — reverter depois delas (renomear identificador,
ou descobrir que um pacote instalado era ilegítimo) é o cenário mais caro que
D-06 e o Package Legitimacy Gate existem para evitar.
Output: decisão de identificadores registrada na resposta do dono; aprovação
explícita para instalar os dois pacotes SUS.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-funda-o-nativa/14-CONTEXT.md
@.planning/phases/14-funda-o-nativa/14-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Decisão: esquema de bundle identifiers e App Group ID (D-06)</name>
  <read_first>app.json (estado atual — android.package = com.pmarconato.forcaapp, ios apenas supportsTablet); .planning/phases/14-funda-o-nativa/14-RESEARCH.md seção "Pattern 1: Congelar identificadores antes de qualquer scaffold (D-06)"; .planning/phases/14-funda-o-nativa/14-CONTEXT.md decisão D-06</read_first>
  <decision>Qual bundle identifier congelar para o app principal, para o target de widget e (se o spike da Plano 14-06 aprovar) para o App Group ID — antes de qualquer `npx create-target`/`expo prebuild`.</decision>
  <context>
    D-06 (CONTEXT.md): reversibilidade CUSTOSA — o time pessoal gratuito tem
    quota de ~10 App IDs/semana (RESEARCH.md Pitfall 6); renomear depois de
    criado queima quota, exige re-confiar o certificado no aparelho e invalida
    qualquer resultado de spike já obtido. A convenção do plugin
    `@bacons/apple-targets`: um bundle id do target prefixado com "." é
    concatenado automaticamente ao bundle id do app principal (ex.:
    ".session-widget" vira "&lt;app-id&gt;.session-widget").
  </context>
  <options>
    <option id="option-a">
      <name>Espelhar o Android (recomendado pela pesquisa)</name>
      <pros>
        ios.bundleIdentifier = com.pmarconato.forcaapp (idêntico ao
        android.package já existente em app.json); widget =
        com.pmarconato.forcaapp.session-widget (via sufixo ".session-widget");
        App Group candidato, se aprovado = group.com.pmarconato.forcaapp.shared.
        Zero ambiguidade, consistente com o identificador já em uso.
      </pros>
      <cons>Nenhuma.</cons>
    </option>
    <option id="option-b">
      <name>Identificador customizado</name>
      <pros>Permite separar o namespace nativo do Android se o dono preferir.</pros>
      <cons>Exige que o dono informe o valor exato agora, antes de prosseguir — nenhum outro passo da fase avança sem ele.</cons>
    </option>
  </options>
  <acceptance_criteria>A resposta do dono contém "option-a" OU um bundle identifier customizado explícito, registrada nesta conversa antes que a Task 1 da Plano 14-02 escreva em app.json.</acceptance_criteria>
  <resume-signal>Responda "option-a" para espelhar o Android, ou informe o bundle identifier customizado que a Plano 14-02 deve usar.</resume-signal>
  <reversibility rating="costly">Quota de ~10 App IDs/semana em time pessoal gratuito (RESEARCH.md Pitfall 6) — renomear depois queima quota, exige re-confiar certificado e invalida o spike de App Groups (D-06).</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Aprovação: legitimidade dos 2 pacotes npm SUS antes do install</name>
  <read_first>.planning/phases/14-funda-o-nativa/14-RESEARCH.md seção "Package Legitimacy Audit"</read_first>
  <what-built>
    Nenhum código ainda — este checkpoint antecede a instalação de 2 pacotes
    npm que o gate de legitimidade sinalizou como SUS pela heurística
    "too-new" (data do último publish, não idade real do pacote). A pesquisa já
    levantou os sinais de legitimidade (autor, downloads, repositório) — falta
    a confirmação humana contra o registro público antes do install real.
  </what-built>
  <how-to-verify>
    Confira os dois pacotes diretamente no npm antes de aprovar:
    1. https://www.npmjs.com/package/@bacons/apple-targets — confirmar autor
       (Evan Bacon, mesmo autor do Expo Router), ~371.350 downloads/semana,
       repositório github.com/evanbacon/expo-apple-targets, último publish
       2026-07-17, versão 5.0.0.
    2. https://www.npmjs.com/package/expo-build-properties — confirmar
       repositório github.com/expo/expo (pacote first-party do monorepo
       Expo), ~2.956.904 downloads/semana, último publish 2026-08-14.
  </how-to-verify>
  <acceptance_criteria>O dono respondeu "aprovado" (ou bloqueou com motivo explícito) antes de qualquer `npm install`/`npx expo install` desses dois pacotes ocorrer na Task 1 da Plano 14-02.</acceptance_criteria>
  <resume-signal>Responda "aprovado" para liberar a instalação dos dois pacotes na próxima plano, ou "bloqueado: &lt;motivo&gt;" para cancelar.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repositório local | Pacote de terceiro instalado localmente com capacidade de rodar scripts de build (postinstall) |
| Dono → decisão irreversível de identificador | Escolha registrada nesta conversa é a única fonte de verdade para D-06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-SC | Tampering | npm install de `@bacons/apple-targets` e `expo-build-properties` | high | mitigate | checkpoint:human-verify bloqueante (Task 2) antes de qualquer install real na Plano 14-02 — nunca auto-aprovável |
| T-14-01-02 | Repudiation | Decisão de bundle identifier (D-06) | low | mitigate | Resposta do dono citada literalmente no SUMMARY.md desta plano, reutilizada como fonte de verdade pela Plano 14-02 |
</threat_model>

## Artifacts this phase produces

Nenhum artefato de código nesta plano — apenas decisões registradas na
conversa, consumidas pela Plano 14-02.

<verification>
Nenhuma verificação automatizada aplicável — esta plano só produz decisões
registradas na conversa (não código). A Plano 14-02 valida que a decisão foi
aplicada corretamente ao app.json.
</verification>

<success_criteria>
O dono escolheu explicitamente entre option-a/option-b (D-06) e aprovou
explicitamente a instalação dos dois pacotes SUS, ambos registrados nesta
conversa antes de qualquer arquivo ser escrito.
</success_criteria>

<output>
Create `.planning/phases/14-funda-o-nativa/14-01-SUMMARY.md` when done — deve
citar literalmente a opção escolhida (D-06) e a resposta de aprovação dos
pacotes, para a Plano 14-02 reutilizar sem precisar reperguntar ao dono.
</output>
