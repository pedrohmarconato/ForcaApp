---
phase: 14-funda-o-nativa
plan: 05
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - app.json
  - targets/session-widget/expo-target.config.js
  - modules/app-group-spike/expo-module.config.json
  - modules/app-group-spike/index.ts
  - modules/app-group-spike/ios/AppGroupSpikeModule.swift
autonomous: true
requirements: [NAT-02]

user_setup:
  - service: xcode-signing
    why: "A build assinada para device (xcodebuild -allowProvisioningUpdates) exige uma conta Apple ID pessoal ja configurada no Xcode antes desta plano rodar"
    dashboard_config:
      - task: "Adicionar o Apple ID pessoal em Xcode -> Settings -> Accounts, e confirmar que o time pessoal (gratuito) aparece disponivel para assinatura automatica"
        location: "Xcode -> Settings (Cmd+,) -> Accounts"
      - task: "Parear o iPhone do dono ao Mac pelo menos uma vez (necessario para o devicectl da Plano 14-06 encontrar o device)"
        location: "Xcode -> Window -> Devices and Simulators"

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Antes da Sessao 1 fisica (Plano 14-06), o build assinado com a entitlement de App Group ja compila para device (build exit 0), restando so a confirmacao fisica do round-trip"
    - "O App Group ID declarado em app.json e em targets/session-widget/expo-target.config.js e identico nos dois arquivos e comeca com o prefixo group. exigido pela Apple"
  artifacts:
    - modules/app-group-spike/index.ts
    - modules/app-group-spike/ios/AppGroupSpikeModule.swift
  key_links:
    - "widget target escreve em UserDefaults(suiteName: group.*) -> modules/app-group-spike le de volta no app principal"
  prohibitions:
    - statement: "Nunca instalar o build assinado gerado aqui em qualquer dispositivo alem do iPhone pessoal do dono"
      status: flagged-unverified
---

<objective>
Preparar, sem precisar do aparelho físico ainda, tudo que o spike de App
Groups (D-09 Sessão 1) vai testar: adicionar a entitlement
`com.apple.security.application-groups` temporariamente aos dois targets, um
código mínimo de escrita no target de widget e um módulo descartável
(`modules/app-group-spike/`) para ler de volta no app principal, e um build
assinado pronto para instalação.

Purpose: a Sessão 1 física com o dono (Plano 14-06) é cara de agendar — tudo
que puder ser preparado e validado ANTES (compila? os dois arquivos de config
concordam no mesmo App Group ID?) reduz o que só pode ser descoberto com o
aparelho na mão a exatamente uma pergunta: o round-trip funciona de verdade?
Output: build assinado (app + widget) com a entitlement de spike, pronto em
DerivedData; `modules/app-group-spike/` isolado do módulo permanente
`native-info` para não contaminar a Plano 14-03 com código descartável.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-funda-o-nativa/14-CONTEXT.md
@.planning/phases/14-funda-o-nativa/14-RESEARCH.md
@.planning/phases/14-funda-o-nativa/14-01-SUMMARY.md
@.planning/phases/14-funda-o-nativa/14-02-SUMMARY.md
@app.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entitlement temporária + módulo descartável de round-trip</name>
  <files>app.json, targets/session-widget/expo-target.config.js, modules/app-group-spike/expo-module.config.json, modules/app-group-spike/index.ts, modules/app-group-spike/ios/AppGroupSpikeModule.swift</files>
  <read_first>app.json (estado pós-Plano 14-02, sem entitlements ainda); targets/session-widget/expo-target.config.js (estado pós-Plano 14-02); modules/native-info/index.ts (padrão de wrapper JS a espelhar, sem tocar neste arquivo); .planning/research/PITFALLS.md item 3 (protocolo do spike, citado por 14-RESEARCH.md Pitfall 4); .planning/phases/14-funda-o-nativa/14-CONTEXT.md decisão D-06/D-09</read_first>
  <precondition>Apple ID pessoal já adicionado em Xcode -> Settings -> Accounts com o time gratuito disponível para assinatura automática, e o iPhone do dono já pareado ao Mac (Xcode -> Window -> Devices and Simulators) — nenhum dos dois é automatizável por este executor.</precondition>
  <action>
    Adicionar `com.apple.security.application-groups` como entitlement de
    SPIKE (temporária) aos dois targets, usando o App Group ID derivado da
    decisão de 14-01-SUMMARY.md (ex.: se a decisão foi "option-a",
    "group.com.pmarconato.forcaapp.shared"): em `app.json`, adicionar
    `expo.ios.entitlements` com a chave
    `"com.apple.security.application-groups": ["&lt;app-group-id&gt;"]`; em
    `targets/session-widget/expo-target.config.js`, adicionar a mesma chave
    `entitlements: { "com.apple.security.application-groups":
    ["&lt;app-group-id&gt;"] }` — os dois valores DEVEM ser idênticos. No
    arquivo Swift default que `npx create-target` gerou para o target de
    widget na Plano 14-02 (localizar em `targets/session-widget/` — o nome
    exato depende do scaffold), adicionar um bloco marcado com o comentário
    `// SPIKE-ONLY (14-05) — remover em 14-07` que escreve
    `"app-group-spike-\(Date())"` em `UserDefaults(suiteName:
    "&lt;app-group-id&gt;")` sob a chave `"appGroupSpikeValue"`, com `NSLog`
    confirmando sucesso/falha da escrita. Scaffoldar um segundo módulo local
    descartável via `npx create-expo-module --local` com nome/slug
    "app-group-spike" — seu lado Swift lê o MESMO `UserDefaults(suiteName:)` /
    chave de volta e expõe `readAppGroupSpikeValue(): Promise&lt;string |
    null&gt;` para JS, espelhando o padrão de leitura já usado em
    `modules/native-info` (sem tocar nesse módulo — mantê-lo isolado do
    código descartável do spike). Regenerar e compilar assinado para device
    reusando exatamente a sequência prebuild + descoberta de scheme + build da
    Plano 14-02 Task 1 / 14-04 Task 1: `npx expo prebuild -p ios --clean
    --non-interactive` seguido de `xcodebuild -workspace
    ios/*.xcworkspace -scheme "&lt;scheme&gt;" -configuration Debug
    -destination "generic/platform=iOS" -allowProvisioningUpdates build`.
    Localizar o `.app` resultante em DerivedData (mesmo padrão `find` de
    `scripts/resign.sh`) — este caminho é o que a Plano 14-06 vai instalar.
  </action>
  <verify>
    <automated>npx expo prebuild -p ios --clean --non-interactive && SCHEME=$(xcodebuild -list -workspace ios/*.xcworkspace -json | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>console.log(JSON.parse(d).workspace.schemes[0]))") && xcodebuild -workspace ios/*.xcworkspace -scheme "$SCHEME" -configuration Debug -destination "generic/platform=iOS" -allowProvisioningUpdates build | tail -10</automated>
  </verify>
  <acceptance_criteria>
    `app.json` e `targets/session-widget/expo-target.config.js` contêm a MESMA
    chave `com.apple.security.application-groups` com o MESMO valor de App
    Group ID; `modules/app-group-spike/index.ts` exporta
    `readAppGroupSpikeValue`; o build assinado para device (comando acima)
    sai com código 0; o `.app` resultante é localizado em DerivedData.
  </acceptance_criteria>
  <done>Build assinado para device com a entitlement de spike existe, pronto para a instalação física da Plano 14-06.</done>
</task>

<task type="auto">
  <name>Task 2: Checagem estrutural do spike sem depender do aparelho</name>
  <files>app.json, targets/session-widget/expo-target.config.js</files>
  <read_first>app.json (após Task 1); targets/session-widget/expo-target.config.js (após Task 1)</read_first>
  <action>
    Extrair o valor de `com.apple.security.application-groups` de `app.json` e
    de `targets/session-widget/expo-target.config.js` (ex.: via `grep -oE
    "group\.[A-Za-z0-9.]+"` em cada arquivo) e confirmar que os dois valores
    são IDÊNTICOS byte a byte — um App Group ID divergente entre os dois
    arquivos faz o round-trip falhar mesmo que a capability funcione na
    plataforma, e esse tipo de erro não aparece no build (só no round-trip
    físico, onde seria confundido com "App Groups não funciona em time
    gratuito"). Confirmar também que o valor começa literalmente com o
    prefixo `group.` exigido pela Apple para App Group identifiers.
  </action>
  <verify>
    <automated>test "$(grep -oE 'group\.[A-Za-z0-9.]+' app.json | head -1)" = "$(grep -oE 'group\.[A-Za-z0-9.]+' targets/session-widget/expo-target.config.js | head -1)"</automated>
  </verify>
  <acceptance_criteria>
    O comando de verify acima sai com código 0 (os dois valores extraídos são
    idênticos e ambos começam com `group.`).
  </acceptance_criteria>
  <done>Os dois arquivos de config concordam no mesmo App Group ID, válido no formato exigido pela Apple.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App Group compartilhado (UserDefaults(suiteName:)) | Armazenamento local compartilhado entre dois processos (app principal + extensão de widget) no mesmo device |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05-01 | Information Disclosure | Payload do App Group compartilhado | medium | mitigate | Valor único, trivial (string com timestamp), sem dado de sessão real — RESEARCH.md Security Domain já recomenda manter o payload mínimo |
| T-14-05-02 | Tampering | Entitlement deixada configurada permanentemente mesmo se o spike falhar | high | mitigate | A Plano 14-07 reverte obrigatoriamente app.json/expo-target.config.js se o resultado físico não for PASS nas duas direções (Pitfall 5/6) |
</threat_model>

## Artifacts this phase produces

- `modules/app-group-spike/expo-module.config.json`, `modules/app-group-spike/index.ts`, `modules/app-group-spike/ios/AppGroupSpikeModule.swift` — módulo temporário, existência condicionada ao resultado do spike (removido ou mantido pela Plano 14-07)
- `AppGroupSpikeModule.swift::readAppGroupSpikeValue()`
- Bloco Swift temporário marcado `// SPIKE-ONLY (14-05)` no target de widget (arquivo default gerado pela Plano 14-02, nome exato descoberto em runtime)

<verification>
O build assinado para device (comando da Task 1) e a checagem de consistência
de App Group ID (Task 2) devem sair com código 0 nesta máquina, sem depender
do aparelho físico ainda — só a confirmação do round-trip em si depende do
device (Plano 14-06).
</verification>

<success_criteria>
Um `.app` assinado com a entitlement de App Group existe em DerivedData,
pronto para a Sessão 1 física; os dois arquivos de config concordam no mesmo
App Group ID.
</success_criteria>

<output>
Create `.planning/phases/14-funda-o-nativa/14-05-SUMMARY.md` when done — deve
registrar o caminho exato do `.app` localizado, para a Plano 14-06 usar
diretamente no comando de instalação.
</output>
