---
phase: 14-funda-o-nativa
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - app.json
  - package.json
  - package-lock.json
  - targets/session-widget/expo-target.config.js
  - modules/native-info/expo-module.config.json
  - modules/native-info/index.ts
  - modules/native-info/ios/NativeInfoModule.swift
  - scripts/verify-native-skeleton.sh
autonomous: true
requirements: [NAT-01, NAT-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "npx expo prebuild -p ios --clean regenera ios/ com o target session-widget presente no .xcodeproj gerado"
    - "Nenhum .entitlements gerado contém a chave aps-environment antes do spike de App Groups (Plano 14-06) aprovar qualquer capability"
    - "bash scripts/verify-native-skeleton.sh produz o mesmo resultado PASS/FAIL em duas execuções consecutivas sem alteração de estado entre elas"
    - "xcodebuild compila o esqueleto (app + target de widget) para o simulador sem exigir assinatura de device, provando que config + target + módulo funcionam ponta a ponta antes de qualquer sessão física"
  artifacts:
    - app.json
    - targets/session-widget/expo-target.config.js
    - modules/native-info/index.ts
    - modules/native-info/ios/NativeInfoModule.swift
    - scripts/verify-native-skeleton.sh
  key_links:
    - "app.json expo.plugins → targets/session-widget/expo-target.config.js consumido por @bacons/apple-targets no prebuild"
    - "modules/native-info autolinked via nativeModulesDir default './modules' → target principal no ios/ gerado"
    - "scripts/verify-native-skeleton.sh reutilizado por scripts/resign.sh (Plano 14-04) como gate final"
  prohibitions:
    - statement: "Nunca declarar a entitlement com.apple.security.application-groups (ou qualquer capability de App Group) em app.json/expo-target.config.js nesta plano — só a Plano 14-05, depois da checkpoint de decisão, pode fazer isso, e só como spike temporário"
      status: flagged-unverified
---

<objective>
Provar, de ponta a ponta e uma única vez, que o pipeline nativo completo
funciona: resolver o gap de CocoaPods desta máquina, aplicar a decisão de
identificadores da Plano 14-01, instalar os dois pacotes aprovados, scaffoldar
o target de widget e o módulo Swift local (vazios, sem lógica de produto
ainda), regenerar `ios/` do zero e compilar — sem precisar de device físico
nem de assinatura de distribuição. Depois, formalizar essa prova numa
ferramenta de regressão reutilizável (`scripts/verify-native-skeleton.sh`) que
qualquer plano/sessão futura desta fase (e a rotina semanal de reassinatura)
possa rodar em 1 comando.

Purpose: é o único jeito de saber, antes de gastar a Sessão 1 física com o
dono (Plano 14-06), que a arquitetura escolhida (target via
`@bacons/apple-targets`, módulo via `create-expo-module --local`) realmente
sobrevive a `expo prebuild --clean` (NAT-02) e realmente compila.
Output: `targets/session-widget/`, `modules/native-info/` e
`scripts/verify-native-skeleton.sh` existindo e comprovadamente funcionais;
`app.json` com os identificadores congelados e os plugins registrados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-funda-o-nativa/14-CONTEXT.md
@.planning/phases/14-funda-o-nativa/14-RESEARCH.md
@.planning/phases/14-funda-o-nativa/14-01-SUMMARY.md
@app.json
@package.json
@scripts/supabase-preflight.sh
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Pipeline nativo completo, do zero ao binário compilado</name>
  <files>app.json, package.json, package-lock.json, targets/session-widget/expo-target.config.js, modules/native-info/expo-module.config.json, modules/native-info/index.ts, modules/native-info/ios/NativeInfoModule.swift</files>
  <read_first>app.json (estado atual — android.package, plugins existentes expo-asset/expo-font); package.json (scripts existentes, engines node>=20); .planning/phases/14-funda-o-nativa/14-RESEARCH.md seções "Standard Stack", "Architecture Patterns" (Pattern 1, Pattern 7/Anti-Patterns), "Common Pitfalls" (2, 3, 5, 7), "Code Examples"; .planning/phases/14-funda-o-nativa/14-01-SUMMARY.md (decisão de bundle identifier confirmada pelo dono)</read_first>
  <action>
    1. Resolver o gap de ambiente (RESEARCH.md Pitfall 2 — sem fallback): rodar
    `brew install cocoapods`; confirmar com `pod --version` (deve imprimir uma
    versão, não "command not found").
    2. Aplicar a decisão da Plano 14-01 (lida em 14-01-SUMMARY.md) a `app.json`:
    definir `expo.ios.bundleIdentifier` com o valor decidido (default sugerido:
    "com.pmarconato.forcaapp", espelhando `expo.android.package` já existente).
    NÃO definir `expo.ios.appleTeamId` ainda — a assinatura automática via
    Xcode resolve isso na Plano 14-05, sem precisar do valor hardcoded aqui.
    3. Adicionar dois plugins ao array `expo.plugins` (depois de "expo-asset",
    "expo-font", que já existem): a string `"@bacons/apple-targets"` e a
    tupla `["expo-build-properties", { "ios": { "deploymentTarget": "17.0" } }]`
    (17.0 dá margem acima do mínimo 16.1 exigido por Dynamic Island).
    4. Instalar os dois pacotes aprovados na Plano 14-01 Task 2 — nunca
    `npm install &lt;pkg&gt;@latest` (RESEARCH.md Pitfall 3: o dist-tag latest do
    ecossistema Expo já aponta para SDK 57, 3 majors à frente do SDK 54 deste
    projeto): `npx expo install expo-build-properties` (resolve a versão
    pinada `~1.0.10` do SDK 54 automaticamente) seguido de
    `npm install @bacons/apple-targets@^5.0.0`.
    5. Scaffoldar o target de widget: rodar `npx create-target`; quando
    perguntado o tipo, escolher "widget"; quando perguntado o nome/slug, usar
    "session-widget" (RESEARCH.md Pitfall 7 — o comando é interativo, não
    aceita `npx create-target widget` como argumento). Se o CLI não aceitar
    respostas via stdin não-interativo neste ambiente de execução, criar
    manualmente `targets/session-widget/expo-target.config.js` exportando um
    objeto default com os campos `type: "widget"`, `name: "session-widget"`,
    `bundleIdentifier: ".session-widget"`, `deploymentTarget: "17.0"` (a
    convenção do plugin concatena o prefixo "." ao bundle id do app
    principal — RESEARCH.md Pattern 1), e copiar os arquivos Swift/Info.plist
    default documentados no exemplo "widget" do README do
    `@bacons/apple-targets`.
    6. Scaffoldar o módulo nativo: rodar `npx create-expo-module --local`;
    quando perguntado o nome/slug do módulo, usar "native-info". Isso cria
    `modules/native-info/expo-module.config.json`, `modules/native-info/index.ts`
    e `modules/native-info/ios/NativeInfoModule.swift` com a função de exemplo
    default do scaffold — NÃO adicionar a lógica de leitura de provisioning
    profile (D-03) ainda, isso é escopo da Plano 14-03; o objetivo aqui é só
    provar que o autolinking funciona.
    7. Regenerar o projeto nativo: `npx expo prebuild -p ios --clean
    --non-interactive`.
    8. Descobrir o nome do scheme gerado (RESEARCH.md Open Question 2 — NÃO
    hardcodar "ForcaApp"): rodar `xcodebuild -list -workspace
    ios/*.xcworkspace -json` e extrair `.workspace.schemes[0]` (ex.: via `node
    -e` lendo stdin e fazendo `JSON.parse`). Registrar o valor descoberto no
    SUMMARY.md desta plano — as Planos 14-04 e 14-05 reusam exatamente esse
    valor, nunca redescobrem do zero.
    9. Provar que o pipeline inteiro compila sem precisar de assinatura de
    device: `xcodebuild -workspace ios/*.xcworkspace -scheme
    "&lt;scheme-descoberto&gt;" -sdk iphonesimulator -configuration Debug build
    CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` deve sair com código 0.
    10. Confirmar NAT-02 literalmente: `grep -q session-widget
    ios/*.xcodeproj/project.pbxproj` (o target está presente) e `grep -L
    aps-environment ios/*/*.entitlements` (RESEARCH.md Pitfall 5 — nenhum
    entitlement de push vazou para nenhum dos dois targets antes da hora).
    11. D-07 (não é ação, é confirmação): o bundle embarcado já aponta para
    produção automaticamente via `EXPO_PUBLIC_API_BASE_URL`/babel-preset-expo
    inline, sem nenhuma mudança de código nesta plano — não criar nenhum
    arquivo `.env` novo nem sobrescrever o comportamento existente.
  </action>
  <verify>
    <automated>npx expo prebuild -p ios --clean --non-interactive && SCHEME=$(xcodebuild -list -workspace ios/*.xcworkspace -json | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>console.log(JSON.parse(d).workspace.schemes[0]))") && xcodebuild -workspace ios/*.xcworkspace -scheme "$SCHEME" -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO | tail -5 && grep -q session-widget ios/*.xcodeproj/project.pbxproj && grep -L aps-environment ios/*/*.entitlements</automated>
  </verify>
  <acceptance_criteria>
    `pod --version` sai com código 0; `app.json` contém o `ios.bundleIdentifier`
    congelado e os dois novos plugins; `targets/session-widget/expo-target.config.js`
    existe com `bundleIdentifier: ".session-widget"`; `modules/native-info/index.ts`
    e `modules/native-info/ios/NativeInfoModule.swift` existem; o build para
    simulador (comando acima) sai com código 0; `grep -q session-widget
    ios/*.xcodeproj/project.pbxproj` retorna verdadeiro; nenhum `.entitlements`
    gerado contém "aps-environment".
  </acceptance_criteria>
  <done>Esqueleto nativo (target + módulo) existe, compila para simulador sem device signing, e nenhum entitlement de push vazou.</done>
  <reversibility rating="costly">Esta task materializa a decisão de bundle identifier da Plano 14-01 (D-06) — reverter depois de criado o App ID consome quota semanal e invalida o spike físico posterior.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Formalizar o esqueleto nativo em scripts/verify-native-skeleton.sh</name>
  <files>scripts/verify-native-skeleton.sh, package.json</files>
  <read_first>scripts/supabase-preflight.sh (arquivo inteiro — estilo canônico de trava shell do repo: cores vermelho/amarelo/verde, ABORTADO: + comando de correção); scripts/verify-web-bundle.mjs (padrão de trava que falha o processo com mensagem acionável); .planning/phases/14-funda-o-nativa/14-PATTERNS.md seção "scripts/verify-native-skeleton.sh (utility, batch — smoke/trava)"</read_first>
  <action>
    Criar `scripts/verify-native-skeleton.sh` copiando literalmente o estilo de
    `scripts/supabase-preflight.sh`: `#!/usr/bin/env bash`, `set -euo
    pipefail`, `readonly REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.."
    && pwd)"`, as funções `vermelho()`/`amarelo()`/`verde()`, e um comentário
    de cabeçalho explicando o porquê (guarda contra o Pitfall 1 —
    `expo prebuild --clean` apagando o target/módulo em silêncio). Codificar
    estas checagens, cada uma abortando com `vermelho "ABORTADO: &lt;causa&gt;"` +
    linha com o comando de correção antes de `exit 1`:
    (a) `npx expo prebuild -p ios --clean --non-interactive`;
    (b) `grep -q session-widget ios/*.xcodeproj/project.pbxproj` — se ausente,
    "ABORTADO: target session-widget não sobreviveu ao --clean";
    (c) `grep -L aps-environment ios/*/*.entitlements` deve listar TODOS os
    arquivos de entitlements (ou seja, nenhum contém a chave) — se algum
    contiver, "ABORTADO: aps-environment vazou para &lt;arquivo&gt;";
    (d) confirmar que o módulo `native-info` aparece no autolinking:
    `npx expo-modules-autolinking search --platform ios | grep -q
    native-info` — se ausente, "ABORTADO: módulo native-info não foi
    autolinked".
    Depois de (a)-(d) passarem uma vez, repetir as MESMAS quatro checagens
    (sem nenhuma mudança de arquivo entre as duas rodadas) e confirmar que o
    resultado é idêntico nas duas execuções — resolve o caso de borda "o
    resultado é estável entre execuções repetidas sem alteração de estado".
    Terminar com `verde "OK: esqueleto nativo sobrevive ao --clean (2x
    consecutivas)."` e `exit 0`. Adicionar `"verify:native": "bash
    scripts/verify-native-skeleton.sh"` ao objeto `scripts` de `package.json`,
    ao lado das entradas existentes `"pods"`/`"build:web"`.
  </action>
  <verify>
    <automated>bash scripts/verify-native-skeleton.sh</automated>
  </verify>
  <acceptance_criteria>
    `bash scripts/verify-native-skeleton.sh` sai com código 0 (rodando as
    checagens duas vezes internamente, sem diferença de resultado entre elas);
    `grep -c 'ABORTADO:' scripts/verify-native-skeleton.sh` retorna pelo menos
    3; `package.json` contém `"verify:native": "bash
    scripts/verify-native-skeleton.sh"`.
  </acceptance_criteria>
  <done>scripts/verify-native-skeleton.sh existe, é idempotente entre execuções e está encadeável por scripts/resign.sh (Plano 14-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolchain local (CocoaPods/Xcode) executando com permissão do repo | `pod install` dentro do prebuild roda scripts de terceiros (CocoaPods) com acesso ao filesystem local |
| Bundle nativo compilado ↔ ambiente (prod vs local) | O binário embarcado herda EXPO_PUBLIC_* do momento do build (D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02-01 | Tampering | Entitlements gerados (`ios/*/*.entitlements`) | high | mitigate | Task 1 passo 10 + `scripts/verify-native-skeleton.sh` checagem (c) — falha explícita se `aps-environment` vazar (RESEARCH.md Pitfall 5) |
| T-14-02-02 | Information Disclosure | Binário nativo embarcado apontando para ambiente errado | medium | accept | D-07 já resolve isso via inlining automático do `babel-preset-expo` (mesmo mecanismo já provado no build web, sem override de `.env` ativo nesta plano); criar um verify-native-bundle equivalente ao `verify-web-bundle.mjs` está fora do escopo NAT-01/02 — não solicitado, seria scope creep |
| T-14-02-03 | Tampering | Pacotes npm instalados (`@bacons/apple-targets`, `expo-build-properties`) | high | mitigate | Mitigado a montante — checkpoint bloqueante já aprovado na Plano 14-01 Task 2 antes desta plano rodar |
</threat_model>

## Artifacts this phase produces

- `targets/session-widget/expo-target.config.js` — config do target de widget scaffolded
- `modules/native-info/expo-module.config.json`, `modules/native-info/index.ts`, `modules/native-info/ios/NativeInfoModule.swift` — módulo Expo local scaffolded (função de exemplo default, sem lógica de produto)
- `scripts/verify-native-skeleton.sh` — script novo, checagens (a)-(d), reutilizado por `scripts/resign.sh` (Plano 14-04)
- `package.json` script `"verify:native"`

<verification>
`bash scripts/verify-native-skeleton.sh` e `xcodebuild ... -sdk iphonesimulator ... build` (comando completo na Task 1) devem sair com código 0 nesta máquina, sem depender de device físico nem de assinatura de distribuição.
</verification>

<success_criteria>
`app.json` tem os identificadores congelados e os plugins registrados;
`targets/session-widget/` e `modules/native-info/` existem e sobrevivem a um
`--clean`; o esqueleto compila para simulador; `scripts/verify-native-skeleton.sh`
existe, é idempotente entre execuções e está encadeável por outros scripts.
</success_criteria>

<output>
Create `.planning/phases/14-funda-o-nativa/14-02-SUMMARY.md` when done — deve
registrar literalmente o nome do scheme descoberto no passo 8 da Task 1, para
as Planos 14-04 e 14-05 reutilizarem sem redescobrir.
</output>
