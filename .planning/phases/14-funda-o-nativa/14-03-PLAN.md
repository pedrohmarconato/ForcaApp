---
phase: 14-funda-o-nativa
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - modules/native-info/ios/NativeInfoModule.swift
  - modules/native-info/index.ts
  - src/components/ProvisioningBanner.tsx
  - __tests__/ProvisioningBanner.test.tsx
  - App.tsx
autonomous: true
requirements: [NAT-01]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ProvisioningBanner não renderiza nada fora de iOS nem enquanto a data de expiração ainda não chegou do módulo nativo"
    - "ProvisioningBanner mostra o aviso quando faltam <=2 dias para a expiração do provisioning profile, incluindo o caso-limite de exatamente 2 dias (adjacência)"
    - "getProvisioningProfileExpiry() do módulo nativo expõe somente a data de expiração para a camada JS, nunca o conteúdo bruto do provisioning profile"
  artifacts:
    - src/components/ProvisioningBanner.tsx
    - __tests__/ProvisioningBanner.test.tsx
    - modules/native-info/ios/NativeInfoModule.swift
    - modules/native-info/index.ts
  key_links:
    - "ProvisioningBanner.tsx -> modules/native-info/index.ts::getProvisioningProfileExpiry() -> NativeInfoModule.swift::readProvisioningProfileExpiry()"
    - "App.tsx monta <ProvisioningBanner /> imediatamente depois de <UpdateBanner />"
---

<objective>
Implementar a única lógica Swift genuinamente necessária nesta fase (D-03):
ler a data de expiração do `embedded.mobileprovision` embarcado no bundle e
mostrar um banner discreto e não-bloqueante quando faltarem 2 dias ou menos —
sem sistema de notificação, sem botões de ação, só um aviso "Reassine até
&lt;dia&gt;".

Purpose: é o único sinal visível dentro do próprio app de que a rotina de
reassinatura semanal (D-01, Plano 14-04) precisa rodar — sem isso, o dono só
descobre que o certificado expirou quando o app já parou de abrir.
Output: `ProvisioningBanner.tsx` montado em `App.tsx`, lendo do módulo nativo
scaffolded na Plano 14-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-funda-o-nativa/14-CONTEXT.md
@.planning/phases/14-funda-o-nativa/14-RESEARCH.md
@.planning/phases/14-funda-o-nativa/14-PATTERNS.md
@src/components/UpdateBanner.tsx
@__tests__/UpdateBanner.test.tsx
@src/theme/theme.ts
@App.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: NativeInfoModule.swift lê a expiração do provisioning profile</name>
  <files>modules/native-info/ios/NativeInfoModule.swift, modules/native-info/index.ts</files>
  <read_first>modules/native-info/ios/NativeInfoModule.swift (scaffold default criado na Plano 14-02 — função de exemplo a substituir); modules/native-info/index.ts (wrapper default do scaffold); .planning/phases/14-funda-o-nativa/14-RESEARCH.md seção "Pattern 3: Leitura do provisioning profile embarcado (D-03)" (snippet Swift completo, fonte process-one.net)</read_first>
  <action>
    Substituir a função de exemplo default do scaffold em `NativeInfoModule.swift`
    por `readProvisioningProfileExpiry() -> Date?`, implementando exatamente o
    padrão de RESEARCH.md Pattern 3: `Bundle.main.path(forResource: "embedded",
    ofType: "mobileprovision")` -&gt; ler como string ISO Latin-1 via
    `NSString(contentsOfFile:encoding:)` -&gt; `Scanner` isola o bloco
    `&lt;plist&gt;...&lt;/plist&gt;` -&gt; `PropertyListDecoder` decodifica uma struct
    `MobileProvision: Decodable` com `enum CodingKeys: String, CodingKey { case
    expirationDate = "ExpirationDate" }`. Expor a função pela API de registro
    do módulo Expo (o formato exato já está no scaffold — usar o mesmo padrão
    `AsyncFunction`/`Function` que a função de exemplo default usava) com o
    nome `getProvisioningProfileExpiry`, retornando a data como string ISO-8601
    (`ISO8601DateFormatter().string(from:)`) ou `nil` quando o arquivo não
    existir (esperado em builds de Simulator — RESEARCH.md confirma que não há
    fallback nem é necessário, D-02 já fixa instalação por cabo em device
    físico). Em `index.ts`, exportar `export async function
    getProvisioningProfileExpiry(): Promise&lt;string | null&gt;` chamando o
    binding nativo (`requireNativeModule` ou equivalente já presente no
    scaffold) — NUNCA retornar o plist inteiro, só a data.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `modules/native-info/ios/NativeInfoModule.swift` contém `func
    readProvisioningProfileExpiry` e `case expirationDate = "ExpirationDate"`;
    `modules/native-info/index.ts` exporta `getProvisioningProfileExpiry`;
    `npx tsc --noEmit` sai com código 0. Comportamento real em device é
    manual-only (RESEARCH.md Validation Architecture — não testável em
    Simulator), validado fisicamente na Plano 14-06/14-09.
  </acceptance_criteria>
  <done>getProvisioningProfileExpiry() existe e type-checa; expõe só a data, nunca o profile bruto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProvisioningBanner.tsx + teste + montagem em App.tsx</name>
  <files>src/components/ProvisioningBanner.tsx, __tests__/ProvisioningBanner.test.tsx, App.tsx</files>
  <read_first>src/components/UpdateBanner.tsx (arquivo inteiro — analog exato de estrutura/estilo); __tests__/UpdateBanner.test.tsx (arquivo inteiro — analog exato de teste RTL + mock de Platform.OS); src/theme/theme.ts (tokens: zIndex.toast, elevation.floating, colors.surface.card, spacing, fonts, typography); App.tsx (ponto de montagem — UpdateBanner já montado logo antes de AlertHost)</read_first>
  <behavior>
    - Test 1: `Platform.OS !== 'ios'` (ex.: 'web', 'android') -&gt; o componente
      renderiza `null` e nunca chama `getProvisioningProfileExpiry()`.
    - Test 2: `Platform.OS === 'ios'` mas a leitura nativa ainda não resolveu
      (estado inicial `null`) -&gt; renderiza `null`.
    - Test 3: `shouldShowProvisioningBanner(expiryDate, now)` com `expiryDate`
      mais de 2 dias no futuro -&gt; `false`, componente renderiza `null`.
    - Test 4: `shouldShowProvisioningBanner(expiryDate, now)` com `expiryDate`
      exatamente 2 dias no futuro (caso-limite de adjacência) e com
      `expiryDate` já no passado (expirado) -&gt; ambos `true`, componente
      renderiza o texto de aviso.
    - Test 5: quando visível, o texto renderizado cita o dia da semana
      derivado de `expiryDate` (ex.: "Reassine até &lt;dia&gt;") e o
      `testID="provisioning-banner"` está presente.
  </behavior>
  <action>
    Extrair `export function shouldShowProvisioningBanner(expiryDate: Date, now:
    Date): boolean` no topo de `ProvisioningBanner.tsx` — pura, sem I/O,
    retorna `expiryDate.getTime() - now.getTime() &lt;= 2 * 24 * 60 * 60 * 1000`
    (D-03: "quando faltarem &lt;=2 dias", limite inclusivo). No componente,
    copiar a estrutura de `UpdateBanner.tsx`: `useEffect` cujo PRIMEIRO passo é
    `if (Platform.OS !== 'ios') return undefined;`, chamando
    `getProvisioningProfileExpiry()` de `../../modules/native-info` uma vez no
    mount e gravando o resultado parseado (`new Date(iso)`) em
    `useState&lt;Date | null&gt;(null)`. Dois early-returns antes do JSX: `if
    (Platform.OS !== 'ios') return null;` depois `if (!expiryDate ||
    !shouldShowProvisioningBanner(expiryDate, new Date())) return null;`. JSX:
    reusar a `View` fixa de `UpdateBanner.tsx` (`position: 'absolute', left:
    0, right: 0, bottom: 0, zIndex: theme.zIndex.toast,
    ...theme.elevation.floating, backgroundColor: theme.colors.surface.card`)
    com `testID="provisioning-banner"`, mas com um único `Text` informativo
    (D-03: "um aviso, não um sistema de notificação" — sem botões de ação),
    lendo "Reassine até &lt;dia da semana em pt-BR calculado de expiryDate&gt;".
    Nenhum valor de cor/espaçamento hardcoded — tudo via `theme`. Montar
    `&lt;ProvisioningBanner /&gt;` em `App.tsx` imediatamente depois de
    `&lt;UpdateBanner /&gt;` (antes de `&lt;AlertHost /&gt;`), dentro de
    `&lt;AuthProvider&gt;`.
  </action>
  <verify>
    <automated>npx jest __tests__/ProvisioningBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/ProvisioningBanner.test.tsx` sai com código 0 cobrindo
    os 5 casos do bloco `behavior`; `App.tsx` contém `&lt;ProvisioningBanner
    /&gt;` imediatamente depois de `&lt;UpdateBanner /&gt;`;
    `shouldShowProvisioningBanner` está exportado de
    `src/components/ProvisioningBanner.tsx`.
  </acceptance_criteria>
  <done>ProvisioningBanner está montado em App.tsx, testado com os 5 casos do bloco behavior, todos verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Módulo nativo (Swift) -> camada JS | `NativeInfoModule.swift` lê um arquivo local do bundle (`embedded.mobileprovision`) que contém Team ID, UUIDs de device e entitlements — potencialmente sensível |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03-01 | Information Disclosure | `getProvisioningProfileExpiry()` (index.ts / NativeInfoModule.swift) | medium | mitigate | A função retorna SOMENTE a data de expiração (string ISO), nunca o plist decodificado inteiro — Task 1 acceptance_criteria confirma que só `ExpirationDate` é mapeado |
| T-14-03-02 | Information Disclosure | Texto do banner exibido na tela | low | accept | O banner mostra só o dia da semana, nenhum dado sensível do profile (Team ID, UUID) chega à UI |
</threat_model>

## Artifacts this phase produces

- `NativeInfoModule.swift::readProvisioningProfileExpiry()` (função Swift)
- `index.ts::getProvisioningProfileExpiry()` (export TS)
- `src/components/ProvisioningBanner.tsx::shouldShowProvisioningBanner()` (função pura exportada) e componente `ProvisioningBanner` (default export)
- `__tests__/ProvisioningBanner.test.tsx` (novo)
- `App.tsx` modificado (linha de montagem `<ProvisioningBanner />`)

<verification>
`npx tsc --noEmit` e `npx jest __tests__/ProvisioningBanner.test.tsx` devem
sair com código 0. Comportamento real do módulo Swift em device físico é
validado nas Planos 14-06/14-09 (manual-only, RESEARCH.md).
</verification>

<success_criteria>
`ProvisioningBanner` está montado em `App.tsx`, com lógica de decisão pura
testada isoladamente e cobrindo o caso-limite de adjacência (exatamente 2
dias); o módulo nativo expõe só a data, nunca o profile bruto.
</success_criteria>

<output>
Create `.planning/phases/14-funda-o-nativa/14-03-SUMMARY.md` when done.
</output>
