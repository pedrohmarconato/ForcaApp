---
phase: 14-funda-o-nativa
plan: 09
type: execute
wave: 7
depends_on: ["14-08"]
files_modified: []
autonomous: false
requirements: [NAT-01]

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A fase 14 so fecha depois que os 4 itens da Sessao 2 (reassinatura, identidade visual, fluxo de treino, banner) recebem PASS/FAIL/N-A explicito do dono"
  artifacts: []
  key_links: []
  prohibitions:
    - statement: "Nunca reportar a fase 14 como concluida sem a resposta explicita do dono nos 4 itens desta UAT — 'buildou'/'compilou' nunca e criterio de conclusao (D-10)"
      status: flagged-unverified
---

<objective>
Sessão 2 física com o dono (D-09, fim da fase): a rotina de reassinatura
semanal roda de ponta a ponta e o dono faz uma sessão de treino real no app
nativo, confirmando que não há diferença percebida em relação ao PWA.

Purpose: é a UAT que fecha literalmente os critérios de sucesso 1 e 2 do
ROADMAP Phase 14 — nenhum dos dois é verificável por comando automatizado,
ambos exigem o dono no aparelho, com a própria conta real (D-08).
Output: resposta do dono, PASS/FAIL/N-A para os 4 itens do runbook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-funda-o-nativa/14-CONTEXT.md
@.planning/milestones/v1.2-phases/13-push-notification-ponta-a-ponta/13-INFRA-RUNBOOK.md
@scripts/resign.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Sessão 2 (física): reassinatura + UAT de paridade com o PWA</name>
  <read_first>scripts/resign.sh (Plano 14-04 — comando exato que o dono vai rodar); .planning/phases/14-funda-o-nativa/14-CONTEXT.md decisões D-01, D-02, D-04, D-08, D-09, D-10; .planning/phases/14-funda-o-nativa/14-08-SUMMARY.md (confirmação de que o pre-flight automatizado já passou)</read_first>
  <what-built>
    `scripts/resign.sh` (rotina de reassinatura em 1 comando, D-01),
    `ProvisioningBanner.tsx` (aviso de expiração, D-03) e
    `scripts/verify-native-skeleton.sh` (regressão do esqueleto nativo) — todo
    o pipeline de build/assinatura está pronto e já passou no pre-check
    automatizado da Plano 14-08.
  </what-built>
  <how-to-verify>
    Roteiro auto-contido (D-10) — rode na ordem, com o iPhone conectado ao
    Mac por cabo:

    1. Rodar `npm run resign` neste terminal — repete o fluxo completo
       (prebuild, build assinado, instalação via `xcrun devicectl`).
    2. Confirmar no iPhone: o app reabre SEM nenhum erro de
       confiança/certificado — item "reassinatura" (critério 2 do ROADMAP).
    3. Confirmar visualmente: o ícone e o nome sob o ícone mostram a
       identidade "Força" (assets/icon.png, assets/splash.png já reusados,
       D-04), não um nome genérico de bundle — item "identidade".
    4. Fazer login com a conta real do dono (D-08 — UAT usa dado real, não
       teste artificial) e executar uma sessão de treino de ponta a ponta:
       escolher um exercício, registrar pelo menos 1 série, avançar o
       descanso — comparar mentalmente com o fluxo do PWA: deve ser
       indistinguível — item "fluxo de treino" (critério 1 do ROADMAP).
    5. Se o provisioning profile estiver a ≤2 dias de expirar neste momento,
       confirmar que o banner "Reassine até &lt;dia&gt;" aparece; caso
       contrário, confirmar apenas que NENHUM aviso falso aparece — item
       "banner" (D-03, pode ser N/A se não houver profile próximo da
       expiração).
    6. Reportar EXATAMENTE neste formato:
       `reassinatura=PASS|FAIL`, `identidade=PASS|FAIL`,
       `fluxo_de_treino=PASS|FAIL`, `banner=PASS|FAIL|N-A`, com qualquer
       diferença percebida do PWA ou erro exato descrito por item em FAIL.
  </how-to-verify>
  <acceptance_criteria>A resposta do dono cobre os 4 itens no formato explícito acima antes da fase ser considerada concluída — nenhum item é presumido PASS por ausência de resposta (D-10).</acceptance_criteria>
  <resume-signal>Reporte: reassinatura=PASS|FAIL, identidade=PASS|FAIL, fluxo_de_treino=PASS|FAIL, banner=PASS|FAIL|N-A.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Conta real do dono contra produção | D-08: UAT usa dado real de produção deliberadamente, não uma conta de teste |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-09-01 | Information Disclosure / Tampering | UAT contra dados reais de produção (D-08) | medium | accept | Decisão deliberada do dono (D-08) — é o próprio propósito da UAT, validar é treinar de verdade |
| T-14-09-02 | Repudiation | Fase reportada como concluída sem confirmação de todos os itens | high | mitigate | Formato de resposta obrigatório (resume-signal) com os 4 itens explícitos, nunca inferido |
</threat_model>

## Artifacts this phase produces

Nenhum artefato de código nesta plano — só a resposta final de UAT do dono,
que fecha a Fase 14.

<verification>
Nenhuma verificação automatizada aplicável — este checkpoint depende
inteiramente da confirmação física do dono. Todas as checagens automatizadas
já rodaram na Plano 14-08.
</verification>

<success_criteria>
O dono reportou PASS/FAIL/N-A explícito para os 4 itens, fechando os
critérios de sucesso 1 e 2 do ROADMAP Phase 14.
</success_criteria>

<output>
Create `.planning/phases/14-funda-o-nativa/14-09-SUMMARY.md` when done.
</output>
