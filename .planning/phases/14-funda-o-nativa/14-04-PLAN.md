---
phase: 14-funda-o-nativa
plan: 04
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - scripts/resign.sh
  - package.json
autonomous: true
requirements: [NAT-01]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "npm run resign executa prebuild + build assinado + instalacao via cabo em um unico comando, abortando com mensagem acionavel em cada ponto de falha conhecido (CocoaPods ausente, zero/multiplos devices conectados, build sem sucesso, .app nao encontrado)"
  artifacts:
    - scripts/resign.sh
  key_links:
    - "scripts/resign.sh chama scripts/verify-native-skeleton.sh (Plano 14-02) como gate final antes de considerar a reassinatura concluida"
  prohibitions:
    - statement: "Nunca instalar ou distribuir o build assinado para qualquer dispositivo alem do iPhone pessoal do dono — app pessoal, sem distribuicao a terceiros (REQUIREMENTS.md Out of Scope)"
      status: flagged-unverified
---

<objective>
Criar o comando único que cumpre D-01: `npm run resign` regenera `ios/`,
compila um build assinado e instala via cabo (D-02) — sem exigir que o dono
lembre a sequência de comandos toda semana.

Purpose: é o artefato que faz NAT-01 ("rotina de reassinatura semanal
documentada e repetível em 1 comando") ser literalmente verdade, não só uma
lista de instruções manuais.
Output: `scripts/resign.sh` + entrada `npm run resign` em `package.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-funda-o-nativa/14-CONTEXT.md
@.planning/phases/14-funda-o-nativa/14-RESEARCH.md
@.planning/phases/14-funda-o-nativa/14-02-SUMMARY.md
@scripts/supabase-preflight.sh
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/resign.sh — comando único de reassinatura (D-01, D-02)</name>
  <files>scripts/resign.sh, package.json</files>
  <read_first>scripts/supabase-preflight.sh (arquivo inteiro — estilo canônico: set -euo pipefail, REPO_ROOT via BASH_SOURCE, cores vermelho/amarelo/verde, ABORTADO: + comando de correção); .planning/phases/14-funda-o-nativa/14-RESEARCH.md seção "Code Examples" (esqueleto de resign.sh e sintaxe de `xcrun devicectl device install app`); .planning/phases/14-funda-o-nativa/14-02-SUMMARY.md (nome do scheme já descoberto — reutilizar, não redescobrir hardcoded)</read_first>
  <action>
    Criar `scripts/resign.sh` seguindo exatamente o estilo de
    `scripts/supabase-preflight.sh` (`#!/usr/bin/env bash`, `set -euo
    pipefail`, `readonly REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.."
    && pwd)"`, funções `vermelho()`/`amarelo()`/`verde()`, passos numerados em
    português). Passos: (1) checar `pod --version`; se falhar,
    `vermelho "ABORTADO: CocoaPods ausente"` + `echo "  Rode: brew install
    cocoapods"` + `exit 1`. (2) `npx expo prebuild -p ios --clean
    --non-interactive`. (3) descobrir o scheme dinamicamente com o MESMO
    padrão `xcodebuild -list -workspace ios/*.xcworkspace -json` + parse via
    `node -e` usado na Plano 14-02 Task 1 (não hardcodar o valor registrado em
    14-02-SUMMARY.md — ele pode mudar se o `name` do app.json mudar). (4)
    build assinado: `xcodebuild -workspace "ios/${SCHEME}.xcworkspace" -scheme
    "$SCHEME" -configuration Debug -destination "generic/platform=iOS"
    -allowProvisioningUpdates build` (Debug/dev-client é a configuração do
    dia a dia durante o v1.3 — D-05; a troca para Release fica para o
    fechamento do milestone, fora do escopo desta fase); se falhar, `vermelho "ABORTADO: build
    falhou"` + `echo "  Confira Xcode > Settings > Accounts: Apple ID pessoal
    com o time selecionavel para assinatura automatica"` + `exit 1`. (5)
    localizar o `.app`: `find ~/Library/Developer/Xcode/DerivedData -name
    "${SCHEME}.app" -path "*Debug-iphoneos*" -print -quit`; se vazio,
    `vermelho "ABORTADO: .app nao encontrado em DerivedData"` + `echo "
    Confira se a etapa 4 (build) terminou sem erro"` + `exit 1`. (6) aceitar
    `$1` como UDID opcional (D-02: cabo); se vazio, `xcrun devicectl list
    devices` e exigir exatamente 1 device conectado — se zero ou mais de um,
    `vermelho "ABORTADO: nenhum ou mais de um device conectado"` + `echo "
    Passe o UDID como argumento: npm run resign -- &lt;UDID&gt;"` + `exit 1`. (7)
    `xcrun devicectl device install app --device "$UDID" "$APP_PATH"`. (8)
    gate final: `bash "${REPO_ROOT}/scripts/verify-native-skeleton.sh"` — se
    falhar, a reassinatura inteira é considerada FALHA (não "compilou = ok").
    Adicionar `"resign": "bash scripts/resign.sh"` ao objeto `scripts` de
    `package.json` (D-01: "npm run resign").
  </action>
  <verify>
    <automated>bash -n scripts/resign.sh</automated>
  </verify>
  <acceptance_criteria>
    `scripts/resign.sh` existe com as 8 etapas numeradas nesta ordem (grep por
    cada marcador de etapa); `package.json` tem `scripts.resign` igual a
    `"bash scripts/resign.sh"`; `bash -n scripts/resign.sh` sai com código 0
    (sintaxe válida).
  </acceptance_criteria>
  <done>npm run resign existe como comando único cobrindo prebuild + build + install (D-01).</done>
</task>

<task type="auto">
  <name>Task 2: Hardening — guard ABORTADO: em todos os pontos de falha conhecidos</name>
  <files>scripts/resign.sh</files>
  <read_first>scripts/resign.sh (recém-criado na Task 1); scripts/supabase-preflight.sh (padrão ABORTADO: + comando de correção, linhas 68-85)</read_first>
  <action>
    Confirmar (e completar se faltar) que os 4 pontos de falha conhecidos têm
    guard explícito no formato `vermelho "ABORTADO: &lt;causa&gt;"` seguido de um
    `echo` com o comando exato de correção antes de `exit 1`: CocoaPods
    ausente (etapa 1), build sem sucesso (etapa 4), `.app` não encontrado
    (etapa 5), zero/múltiplos devices conectados (etapa 6). Nenhum destes
    pontos deve deixar o script morrer só com o stderr cru de
    `xcodebuild`/`devicectl`/`find` sem contexto.
  </action>
  <verify>
    <automated>test $(grep -c 'ABORTADO:' scripts/resign.sh) -ge 4</automated>
  </verify>
  <acceptance_criteria>
    `grep -c 'ABORTADO:' scripts/resign.sh` retorna pelo menos 4; cada
    ocorrência de `ABORTADO:` é seguida (nas linhas imediatamente próximas) de
    um comando de correção explícito, não apenas a descrição do erro.
  </acceptance_criteria>
  <done>Todos os pontos de falha conhecidos de resign.sh abortam com ABORTADO: + comando de correção, nunca com stderr cru.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/resign.sh -> device físico | Script com poder de instalar um binário assinado em QUALQUER device conectado por cabo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04-01 | Tampering | Etapa 6 (`xcrun devicectl device install app`) | high | mitigate | Guard explícito de "zero ou múltiplos devices" aborta em vez de escolher um device arbitrário (D-02) |
| T-14-04-02 | Repudiation | Nenhuma trilha de quando a última reassinatura ocorreu | low | accept | Fora de escopo — o banner de expiração (Plano 14-03, D-03) já cobre o sinal relevante para o dono |
</threat_model>

## Artifacts this phase produces

- `scripts/resign.sh` (novo)
- `package.json` script `"resign"`

<verification>
`bash -n scripts/resign.sh` (sintaxe) e `grep -c 'ABORTADO:' scripts/resign.sh
-ge 4` devem passar. Validação física de ponta a ponta acontece na Plano
14-09 (Sessão 2 UAT).
</verification>

<success_criteria>
`npm run resign` existe como comando único, com guard explícito em cada ponto
de falha conhecido e gate final via `scripts/verify-native-skeleton.sh`.
</success_criteria>

<output>
Create `.planning/phases/14-funda-o-nativa/14-04-SUMMARY.md` when done.
</output>
