---
phase: 14-funda-o-nativa
plan: 08
type: execute
wave: 6
depends_on: ["14-03", "14-04", "14-07"]
files_modified: []
autonomous: true
requirements: [NAT-01]

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Antes da UAT fisica da Sessao 2, todas as checagens automatizadas (skeleton nativo, testes do banner, tsc) ja passam e um build assinado fresco existe, restando so a instalacao/observacao fisica ao dono"
  artifacts: []
  key_links:
    - "scripts/resign.sh (14-04) + scripts/verify-native-skeleton.sh (14-02) + __tests__/ProvisioningBanner.test.tsx (14-03) rodados juntos como pre-flight desta plano"
---

<objective>
Rodar a regressão completa (esqueleto nativo, testes do banner, type-check,
build fresco) UMA vez antes de pedir a Sessão 2 física ao dono — para que a
UAT (Plano 14-09) só precise confirmar comportamento observável no aparelho,
nunca descobrir um erro de build que o pre-check já teria pego.

Purpose: cada sessão física com o dono é cara de agendar; gastar uma nela
descobrindo que `npx tsc --noEmit` falha seria desperdiçar o tempo do dono com
algo 100% automatizável.
Output: confirmação de que `verify-native-skeleton.sh`, os testes do banner e
`tsc` passam juntos, e um `.app` assinado fresco pronto para a instalação
física da Plano 14-09.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-funda-o-nativa/14-CONTEXT.md
@scripts/resign.sh
@scripts/verify-native-skeleton.sh
@__tests__/ProvisioningBanner.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight completo antes da Sessão 2</name>
  <files>ios/ (gerado, não versionado)</files>
  <read_first>scripts/resign.sh (Plano 14-04 — passos 1-5, sem a etapa de instalação); scripts/verify-native-skeleton.sh (Plano 14-02); __tests__/ProvisioningBanner.test.tsx (Plano 14-03)</read_first>
  <action>
    Rodar, na ordem, e confirmar que TODOS saem com código 0 na mesma
    execução: (1) `npx tsc --noEmit` — o projeto inteiro type-checa com as
    adições de `modules/native-info` e `src/components/ProvisioningBanner.tsx`;
    (2) `npx jest __tests__/ProvisioningBanner.test.tsx` — a lógica do banner
    continua correta depois de todas as mudanças das Planos 14-03/14-05/14-07;
    (3) `bash scripts/verify-native-skeleton.sh` — o esqueleto nativo
    (target + módulo) continua sobrevivendo a `--clean` depois da decisão
    final do spike (Plano 14-07, entitlement mantida ou revertida); (4)
    reproduzir os passos 1-5 de `scripts/resign.sh` (CocoaPods, prebuild,
    descoberta de scheme, build assinado, localização do `.app`) SEM a etapa
    6/7 de instalação — produz um `.app` fresco em DerivedData, com timestamp
    mais recente que o build da Plano 14-05, pronto para o dono instalar
    fisicamente na Plano 14-09.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest __tests__/ProvisioningBanner.test.tsx && bash scripts/verify-native-skeleton.sh</automated>
  </verify>
  <acceptance_criteria>
    Os 3 comandos do bloco `verify` saem com código 0 na mesma execução; um
    `.app` fresco existe em `~/Library/Developer/Xcode/DerivedData` com
    timestamp de modificação posterior ao build da Plano 14-05.
  </acceptance_criteria>
  <done>Pre-flight automatizado 100% verde; build fresco pronto para a Sessão 2 física (Plano 14-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhum trust boundary novo | Esta plano só reexecuta checagens já mitigadas nas Planos 14-02/14-03/14-04/14-05/14-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-08-01 | Tampering | Regressão silenciosa entre as Planos 14-02 e esta (ex.: entitlement revertida quebrando o build) | medium | mitigate | Rodar as 3 checagens juntas nesta plano, antes de gastar a Sessão 2 física — herda a mitigação de cada plano anterior |
</threat_model>

## Artifacts this phase produces

Nenhum artefato novo — esta plano só reexecuta ferramentas já criadas nas
Planos 14-02/14-03/14-04.

<verification>
`npx tsc --noEmit && npx jest __tests__/ProvisioningBanner.test.tsx && bash
scripts/verify-native-skeleton.sh` deve sair com código 0.
</verification>

<success_criteria>
Todas as checagens automatizadas passam juntas e um build assinado fresco
existe, pronto para a Sessão 2 física.
</success_criteria>

<output>
Create `.planning/phases/14-funda-o-nativa/14-08-SUMMARY.md` when done.
</output>
