---
phase: 01-fluxo-cardio-e-alongamento
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/cardioPrescrito.ts
  - __tests__/cardioPrescrito.test.ts
  - src/services/cardioPrescritoRepository.ts
  - __tests__/cardioPrescritoRepository.test.ts
autonomous: true
requirements: [REQ-02]

estimate:
  tokens: 55000
  raw_tokens: 39000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "cardioPrescrito calcula prescrito×realizado sem inventar número: sem prescrição na semana, os campos prescritos voltam null (nunca 0 fingindo medição)"
    - "getPrescricaoSemanaCorrente lê SOMENTE planned_sets do plano ATIVO do usuário autenticado, escopados à semana corrente por scheduled_date e filtrados por muscle_group='Cardio' — nunca dado de outro usuário nem de outro plano"
  artifacts:
    - src/engine/cardioPrescrito.ts
    - src/services/cardioPrescritoRepository.ts
  key_links:
    - "cardioPrescritoRepository.getPrescricaoSemanaCorrente → trainingRepository.getActivePlanId (reaproveitado, não recriado)"
    - "cardioPrescritoRepository.getPrescricaoSemanaCorrente → weekSummary.inicioDaSemana (mesmo corte de semana do resto da aba Progresso)"
    - "cardioPrescrito.progressoPrescrito → cardioGoals.progressoConsistencia (reaproveita o cálculo de 'realizado', não duplica)"
---

<objective>
Construir REQ-02 (D-02/CONTEXT.md — meta de cardio deriva da prescrição do plano ativo,
prescrito × realizado, sem nenhuma migration): o motor puro (`cardioPrescrito.ts`) que soma a
prescrição da semana corrente e compara com o realizado já lido por `getCardioLogs`, e o
repositório (`cardioPrescritoRepository.ts`) que busca essa prescrição em `planned_sets` do
plano ativo — nenhuma tabela nova, nenhuma coluna nova, só leitura sobre o schema que a
migration 0014 já persiste.

Purpose: hoje a meta de cardio (`cardio_goals`) é uma definição paralela ao treino — o dono
decidiu (CONTEXT.md, travado) que ela deve virar prescrito×realizado lido do próprio plano.
Este plano entrega o motor e a leitura; o plano seguinte (01-03) troca a UI.
Output: `src/engine/cardioPrescrito.ts`, `src/services/cardioPrescritoRepository.ts`, com
testes cobrindo "sem amostra = null" e o escopo por usuário/plano/semana.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `src/engine/cardioPrescrito.ts` (novo) — `somarPrescricaoSemana`, `progressoPrescrito`, tipos `PrescricaoCardio`/`PlannedCardioSet`/`ProgressoPrescrito`
- `src/services/cardioPrescritoRepository.ts` (novo) — `getPrescricaoSemanaCorrente`
- `__tests__/cardioPrescrito.test.ts` (novo)
- `__tests__/cardioPrescritoRepository.test.ts` (novo)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-fluxo-cardio-e-alongamento/01-CONTEXT.md
@.planning/phases/01-fluxo-cardio-e-alongamento/01-RESEARCH.md
@.planning/phases/01-fluxo-cardio-e-alongamento/01-PATTERNS.md
@src/engine/cardioGoals.ts
@src/services/cardioGoalRepository.ts
@src/services/trainingRepository.ts
@src/utils/weekSummary.ts
@__tests__/cardioGoalRepository.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Motor puro cardioPrescrito.ts — prescrito × realizado</name>
  <files>src/engine/cardioPrescrito.ts, __tests__/cardioPrescrito.test.ts</files>
  <read_first>
    src/engine/cardioGoals.ts inteiro (comentário de topo linhas 1-13 — disciplina "sem
    amostra=null, zero é fato"; `numeroPositivo` linhas 64-67; `progressoConsistencia`
    linhas 185-233, especialmente o corte de semana com `inicioDaSemana`/`fim` e a contagem de
    `dias` distintos via `diaLocal`); src/utils/weekSummary.ts:39-44 (`inicioDaSemana`, NÃO
    reimplementar); __tests__/cardioGoals.test.ts (padrão de fixtures/casos a espelhar).
  </read_first>
  <behavior>
    - `somarPrescricaoSemana(sets: readonly PlannedCardioSet[]): PrescricaoCardio` — função
      pura, SEM I/O, SEM data (o corte de semana é feito pelo repositório na query, não aqui).
      `PlannedCardioSet = { targetDistanceM: number | null; targetDurationSeconds: number |
      null; sessionKey: string }` (o repositório passa `scheduled_date` ou `session_id` como
      `sessionKey` para permitir contar sessões distintas sem reimplementar corte de data).
      `PrescricaoCardio = { distanciaM: number | null; duracaoSegundos: number | null; sessoes:
      number }`. Caso 1: array vazio → `{ distanciaM: null, duracaoSegundos: null, sessoes: 0 }`
      (zero sessões é FATO, mas soma de distância/duração sem nenhuma prescrição é `null`, não
      0 — mesma disciplina de `numeroPositivo`). Caso 2: dois sets na mesma `sessionKey` com
      `targetDurationSeconds=600` cada → `duracaoSegundos=1200`, `sessoes=1` (sessão é dia/chave
      distinta, não contagem de séries — mesmo raciocínio de `progressoConsistencia`). Caso 3:
      sets com `targetDistanceM=null` misturados com sets com valor → soma só os não-nulos;
      se TODOS forem null, `distanciaM=null` (não 0).
    - `progressoPrescrito(logs: readonly CardioLog[], prescricao: PrescricaoCardio, referencia:
      Date = new Date()): ProgressoPrescrito` — reaproveita `progressoConsistencia(logs, {
      weeklyMinutes: prescricao.duracaoSegundos != null ? prescricao.duracaoSegundos / 60 :
      null, weeklySessions: prescricao.sessoes > 0 ? prescricao.sessoes : null }, referencia)`
      de `cardioGoals.ts` para o lado "realizado" (NÃO duplicar a lógica de corte de semana ou
      soma de minutos — importar e chamar). `ProgressoPrescrito` estende o retorno de
      `progressoConsistencia` com `prescritoKm: number | null` (derivado de
      `prescricao.distanciaM`) e `prescritoSessoes: number | null` (o `metaSessoes` já
      devolvido, renomeado na saída para o vocabulário "prescrito"). Caso: `prescricao.sessoes
      === 0` (nenhuma sessão de cardio prescrita esta semana) → `prescritoSessoes = null` (sem
      prescrição, não "prescrito zero") — é isso que a UI usa para decidir entre "mostrar
      números" e "sem cardio prescrito esta semana".
  </behavior>
  <action>
    Crie `src/engine/cardioPrescrito.ts` com cabeçalho de arquivo (per CONVENTIONS.md: path +
    propósito + invariantes) citando explicitamente REQ-02/D-02 e a disciplina "sem amostra =
    null" herdada de `cardioGoals.ts`. Importe `progressoConsistencia` e `type CardioLog` de
    `./cardioGoals` (mesmo diretório `src/engine/`). NÃO importe `inicioDaSemana` aqui — o
    corte de semana é responsabilidade do repositório (Task 2), este módulo só agrega o que
    recebe. Exporte os tipos e funções do bloco `<behavior>` acima com esses nomes exatos.
    Escreva os testes em `__tests__/cardioPrescrito.test.ts` (arquivo novo — Wave 0 gap) ANTES
    da implementação (RED→GREEN), espelhando a estrutura de `describe`/`it` em pt-BR de
    `__tests__/cardioGoals.test.ts`.
  </action>
  <verify>
    <automated>npx jest __tests__/cardioPrescrito.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    `somarPrescricaoSemana([])` retorna `{ distanciaM: null, duracaoSegundos: null, sessoes: 0
    }` (asserção literal no teste); nenhum caso do teste usa `?? 0` para transformar ausência
    em zero fora do que o `<behavior>` especifica; `npx tsc --noEmit` sem erro novo.
  </acceptance_criteria>
  <done>cardioPrescrito.ts exporta somarPrescricaoSemana/progressoPrescrito com o contrato do behavior; testes verdes; disciplina "sem amostra = null" preservada.</done>
  <reversibility rating="reversible">Módulo novo, sem consumidores ainda; deletável sem impacto.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repositório cardioPrescritoRepository.ts — leitura escopada por usuário/plano/semana</name>
  <files>src/services/cardioPrescritoRepository.ts, __tests__/cardioPrescritoRepository.test.ts</files>
  <read_first>
    src/services/trainingRepository.ts:90-100 (`getActivePlanId` — REAPROVEITAR via import, não
    recriar) e :9-47 (tipos `PlannedSet`/`PlannedExercise`/`PlannedSession` — `target_distance_m`/
    `target_duration_seconds` em `PlannedSet`, `muscle_group`/`metric` em `PlannedExercise`,
    `scheduled_date` em `PlannedSession`); src/services/cardioGoalRepository.ts:66-107
    (`getCardioLogs` — padrão de join `set_logs!inner`/`planned_sets!inner`/
    `planned_exercises!inner`, `.eq('planned_sets.planned_exercises.muscle_group', 'Cardio')`,
    `if (error) throw error;` sempre, `toNum` para coerção de `numeric`); src/utils/weekSummary.ts:39-44
    (`inicioDaSemana`); __tests__/cardioGoalRepository.test.ts:1-65 (padrão de mock: cada método
    encadeável do supabase é `jest.fn(() => query)`, o terminal — aqui `.then`/`.select` final —
    resolve `{ error, data }`); __tests__/trainingRepository.test.ts:14-30 (mesmo padrão de mock
    do `supabaseClient`).
  </read_first>
  <behavior>
    - `getPrescricaoSemanaCorrente(userId: string, referencia: Date = new Date()):
      Promise<PrescricaoCardio>` (tipo `PrescricaoCardio` importado de `../engine/cardioPrescrito`).
    - Sem plano ativo (`getActivePlanId` devolve `null`) → devolve `{ distanciaM: null,
      duracaoSegundos: null, sessoes: 0 }` SEM consultar `planned_sessions` — mesmo padrão de
      `getUpcomingSessions`/`getTodaySession` em `trainingRepository.ts`.
    - Com plano ativo: consulta `planned_sessions` filtrada por `user_id`, `plan_id`, e
      `scheduled_date` dentro de `[inicioDaSemana(referencia), inicioDaSemana(referencia)+7dias)`
      — usar `.gte('scheduled_date', ...)`/`.lt('scheduled_date', ...)` com as datas em ISO
      (YYYY-MM-DD), NÃO por `week_number` (RESEARCH.md: evita a aba Progresso discordar de si
      mesma). Join aninhado `planned_exercises!inner(planned_sets!inner(...))` filtrado por
      `.eq('planned_exercises.muscle_group', 'Cardio')`, selecionando
      `target_distance_m`/`target_duration_seconds` de `planned_sets` e `scheduled_date`/`id`
      da sessão para servir de `sessionKey`.
    - Cada linha do resultado vira um `PlannedCardioSet` (`targetDistanceM`/`targetDurationSeconds`
      via `toNum`, `sessionKey` = `scheduled_date` da sessão pai) e a lista alimenta
      `somarPrescricaoSemana` de `../engine/cardioPrescrito`.
    - `if (error) throw error;` em toda chamada Supabase — nunca engolir erro (mesma regra de
      `cardioGoalRepository.ts:4-7`).
  </behavior>
  <action>
    Crie `src/services/cardioPrescritoRepository.ts` importando `supabase` de
    `'../config/supabaseClient'`, `toNum` de `'../engine/sessionModel'`, `inicioDaSemana` de
    `'../utils/weekSummary'`, `getActivePlanId` de `'./trainingRepository'`, e
    `somarPrescricaoSemana`/`type PrescricaoCardio`/`type PlannedCardioSet` de
    `'../engine/cardioPrescrito'`. Implemente exatamente `getPrescricaoSemanaCorrente` conforme
    `<behavior>`. Escreva `__tests__/cardioPrescritoRepository.test.ts` (arquivo novo — Wave 0
    gap) com `jest.mock('../src/config/supabaseClient', ...)` no MESMO padrão de
    `__tests__/cardioGoalRepository.test.ts:5-7`, cobrindo: (a) sem plano ativo → não chama
    `.from('planned_sessions')`; (b) a query usa `.eq('planned_exercises.muscle_group',
    'Cardio')` — asserção `toHaveBeenCalledWith`; (c) duas sessões com prescrição em datas
    distintas dentro da semana somam corretamente e a contagem de `sessoes` é 2, não a
    contagem de séries.
  </action>
  <verify>
    <automated>npx jest __tests__/cardioPrescritoRepository.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    Teste (b) confirma explicitamente o filtro `muscle_group='Cardio'` na query (defesa contra
    T-02-01 abaixo); `getPrescricaoSemanaCorrente` nunca lê `planned_sessions`/`planned_exercises`
    sem passar `userId` como filtro `.eq('user_id', ...)` — asserção explícita no teste;
    `npx tsc --noEmit` sem erro novo.
  </acceptance_criteria>
  <done>getPrescricaoSemanaCorrente lê só o plano ativo do usuário, escopado à semana corrente e a muscle_group='Cardio'; testes verdes provam o escopo por usuário/plano.</done>
  <reversibility rating="reversible">Novo módulo de leitura, sem consumidor ainda (wired na 01-03); deletável sem impacto em produção.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| App (cliente autenticado) → Supabase Postgres | `cardioPrescritoRepository` é uma leitura NOVA sobre `planned_sessions`/`planned_exercises`/`planned_sets` — tabelas já protegidas por RLS (migration `0001_modelo_treino.sql:137-163`, endurecida em `0002_rls_hardening.sql`), mas uma query client-side sem filtro explícito de `user_id` seria uma segunda camada de confiança quebrada caso a RLS falhe silenciosamente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-01 | Information Disclosure | `cardioPrescritoRepository.getPrescricaoSemanaCorrente` | high | mitigate | Query filtra `user_id`/`plan_id` explicitamente no client (defesa em profundidade, mesmo padrão de `trainingRepository.getPlanSessions`) ALÉM da RLS `"own sessions"`/`"own exercises"`/`"own sets"` já ativa desde `0001_modelo_treino.sql`; Task 2 exige asserção de teste explícita sobre o filtro. |
| T-02-02 | Tampering | `somarPrescricaoSemana` | low | accept | Entrada vem só de leitura própria do banco (numeric via `toNum`), nunca de input do usuário nesta camada — sem superfície de tampering nova. |
</threat_model>

<verification>
- `npx jest __tests__/cardioPrescrito.test.ts __tests__/cardioPrescritoRepository.test.ts --silent` verde.
- `npx tsc --noEmit` sem novo erro.
- Nenhuma migration criada (checagem: `git status supabase/migrations/` vazio ao final do plano).
</verification>

<success_criteria>
Base de dados para o Success Criterion 2 do ROADMAP (Fase 1) pronta: prescrito×realizado
calculável a partir do plano ativo, sem tabela paralela — falta só a UI (01-03).
</success_criteria>

<output>
Criar `.planning/phases/01-fluxo-cardio-e-alongamento/01-02-SUMMARY.md` ao final.
</output>
