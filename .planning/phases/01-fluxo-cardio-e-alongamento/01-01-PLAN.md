---
phase: 01-fluxo-cardio-e-alongamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/session/ManualExerciseRow.tsx
  - __tests__/manualExerciseRow.test.tsx
  - __tests__/sessionPlayerTransitions.test.tsx
autonomous: true
requirements: [REQ-01]

estimate:
  tokens: 42000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Usuário digita '2,4' no editor manual de exercícios (ManualExerciseRow) e a lista mostra '2,4 km' (vírgula pt-BR), nunca '2.4 km'"
    - "SessionPlayer (player de sessão) já persiste '2,4' digitado como actualDistanceM=2400 — comportamento coberto por teste de regressão automatizado, não só leitura estática"
  artifacts:
    - src/components/session/ManualExerciseRow.tsx
    - __tests__/manualExerciseRow.test.tsx
  key_links:
    - "ManualExerciseRow.metricLabel → formatDistance (src/engine/sessionModel.ts:311-314) — reaproveitado via import, NUNCA reimplementado"
---

<objective>
Fechar REQ-01 (D-01/CONTEXT.md — decimal com vírgula em TODOS os pontos de entrada/exibição
de distância de cardio): provar em runtime — via teste automatizado, não leitura estática —
que o caminho principal (SessionPlayer.tsx) já está correto desde o commit `925ba42`, e
corrigir o gap real e único confirmado pela pesquisa: `ManualExerciseRow.tsx:13` interpola
`exercise.distancia_km` cru (ponto) em vez de formatar em pt-BR (vírgula).

Purpose: o dono relatou "só aceita inteiro" — a pesquisa (RESEARCH.md, Assumption A1) suspeita
que isso é deploy defasado ou tela diferente (editor manual), não o player de sessão. Esta
tarefa decide entre as duas hipóteses com evidência de teste, não com confiança na leitura de
código, antes de tocar em qualquer arquivo.
Output: `ManualExerciseRow.tsx` exibindo distância em pt-BR; dois arquivos de teste provando
os dois pontos de entrada (player = já correto; editor manual = corrigido nesta tarefa).
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `src/components/session/ManualExerciseRow.tsx` (editado) — passa a importar e usar `formatDistance`
- `__tests__/manualExerciseRow.test.tsx` (novo) — cobre REQ-01 no editor manual
- `__tests__/sessionPlayerTransitions.test.tsx` (editado) — novo caso de regressão do decimal no player
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-fluxo-cardio-e-alongamento/01-CONTEXT.md
@.planning/phases/01-fluxo-cardio-e-alongamento/01-RESEARCH.md
@.planning/phases/01-fluxo-cardio-e-alongamento/01-PATTERNS.md
@src/components/session/ManualExerciseRow.tsx
@src/engine/sessionModel.ts
@src/components/session/SessionPlayer.tsx
@src/types/manualPlan.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Verificar em runtime qual dos dois pontos de entrada do decimal está quebrado</name>
  <files>__tests__/manualExerciseRow.test.tsx, __tests__/sessionPlayerTransitions.test.tsx</files>
  <read_first>
    src/components/session/ManualExerciseRow.tsx (linha 13 — `exercise.distancia_km` interpolado
    cru); src/engine/sessionModel.ts:311-314 (`formatDistance`, espera METROS, não km);
    src/components/session/SessionPlayer.tsx:65-70 (`parseFloatOrNull`, já converte vírgula→ponto)
    e :525-554 (campo "Distância (km)", `accessibilityLabel` = `Distância da série ${set.setOrder}
    em quilômetros`, chama `setDistance(exerciseId, setOrder, Math.round(km*1000))`);
    __tests__/sessionPlayerTransitions.test.tsx:57-188 (fixtures `serie`/`exercicio`/`draftCom`/
    `renderComDraft`, real `useActiveSessionStore` sem mock — só `sessionExecutionRepository` é
    mockado); src/types/manualPlan.ts:5-20 (`ManualExerciseDraft`, todos os campos obrigatórios).
  </read_first>
  <behavior>
    - Teste NOVO em `__tests__/manualExerciseRow.test.tsx` (arquivo não existe — Wave 0 gap):
      renderiza `ManualExerciseRow` com `exercise.metrica='tempo_distancia'`, `series=3`,
      `duracao_minutos=20`, `distancia_km=2.4` (demais campos de `ManualExerciseDraft`
      preenchidos com valores válidos) → HOJE deve FALHAR: o texto renderizado contém
      "2.4 km" (ponto), não "2,4 km".
    - Caso adicional no mesmo arquivo: `distancia_km=null` → texto NÃO contém " · " nem "km"
      (sem sufixo quando não há distância).
    - Caso adicional: `distancia_km=5` → texto contém "5 km" (sem decimal, sem zero à toa).
    - Teste NOVO em `__tests__/sessionPlayerTransitions.test.tsx`: monta `exercicio(...)` com
      `metric: 'tempo_distancia'`, um `serie(...)` com `status: 'active'` (sem precisar
      completar a série); `renderComDraft`; `fireEvent.changeText` no campo com
      `accessibilityLabel` `Distância da série 1 em quilômetros` digitando `'2,4'`; assert
      `useActiveSessionStore.getState().draft?.exercises[0].sets[0].actualDistanceM === 2400`
      → HOJE deve PASSAR (prova que o player já está correto, commit `925ba42`).
  </behavior>
  <action>
    Escreva os dois testes ANTES de tocar em `ManualExerciseRow.tsx`. Rode
    `npx jest __tests__/sessionPlayerTransitions.test.tsx __tests__/manualExerciseRow.test.tsx --silent`
    e registre o resultado: o teste do SessionPlayer DEVE passar (RED nenhum) e o(s) teste(s)
    novo(s) de `manualExerciseRow.test.tsx` que checam "2,4 km" DEVEM falhar (RED esperado — é
    o gap que a Task 2 fecha). Se o teste do SessionPlayer FALHAR (ou seja, o player também
    está quebrado, contrariando a Assumption A1 do RESEARCH.md), PARE: não amplie o escopo
    desta tarefa sozinho — documente o achado real no SUMMARY.md desta fase junto com a
    evidência do teste e sinalize a divergência com o CONTEXT.md, sem alterar `SessionPlayer.tsx`
    sem uma decisão explícita.
  </action>
  <verify>
    <automated>npx jest __tests__/sessionPlayerTransitions.test.tsx __tests__/manualExerciseRow.test.tsx --silent</automated>
  </verify>
  <acceptance_criteria>
    `__tests__/manualExerciseRow.test.tsx` existe e roda (mesmo que com casos ainda RED); o
    caso de `sessionPlayerTransitions.test.tsx` referente a `actualDistanceM === 2400` passa
    GREEN, confirmando por execução real (não leitura estática) que `925ba42` cobre o player de
    sessão; o(s) caso(s) de "2,4 km" em `ManualExerciseRow` falham GREEN-esperado-RED antes da
    Task 2.
  </acceptance_criteria>
  <done>Ambos os arquivos de teste existem e rodam; o caso do SessionPlayer está GREEN; o gap em ManualExerciseRow está provado por um teste RED (não por leitura de código).</done>
  <reversibility rating="reversible">Só adiciona testes; nenhum código de produção muda.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corrigir a formatação de distância em ManualExerciseRow.tsx</name>
  <files>src/components/session/ManualExerciseRow.tsx</files>
  <read_first>
    src/components/session/ManualExerciseRow.tsx:1-15 (imports atuais, `metricLabel`);
    src/engine/sessionModel.ts:311-314 (`formatDistance(meters)` — espera METROS, retorna
    "—" para null/≤0, remove zero à toa, troca ponto por vírgula).
  </read_first>
  <action>
    Em `ManualExerciseRow.tsx`, adicione `formatDistance` ao import existente de
    `'../../engine/sessionModel'` (novo import — hoje o arquivo só importa de
    `'../../types/manualPlan'` e `theme`). Na função `metricLabel` (linha 8-15), troque a
    linha `const distance = exercise.distancia_km != null ? \` · ${exercise.distancia_km} km\` : '';`
    por uma que preserva o guard `!= null` (não mostrar sufixo quando não há distância) mas
    formata via `formatDistance`, convertendo KM→METROS antes de chamar
    (`exercise.distancia_km * 1000`) — `formatDistance` já devolve a string COM o sufixo " km",
    então remova o " km" literal duplicado que o template atual adiciona. Não toque em
    `duracao_minutos`/`formatWorkDuration` nem em nenhum outro arquivo — o gap é só esta linha
    (per RESEARCH.md "Anti-Patterns to Avoid": não reescrever SessionPlayer.tsx).
  </action>
  <verify>
    <automated>npx jest __tests__/manualExerciseRow.test.tsx __tests__/sessionPlayerTransitions.test.tsx --silent</automated>
  </verify>
  <acceptance_criteria>
    Todos os casos de `__tests__/manualExerciseRow.test.tsx` passam GREEN (2,4→"2,4 km",
    null→sem sufixo, 5→"5 km" sem decimal); `ManualExerciseRow.tsx` NÃO contém mais
    `distancia_km} km` (interpolação crua) — grep
    `grep -n 'distancia_km}\s*km' src/components/session/ManualExerciseRow.tsx` retorna vazio;
    `npx tsc --noEmit` sem erro novo.
  </acceptance_criteria>
  <done>Distância no editor manual exibe vírgula pt-BR via formatDistance reaproveitado; todos os testes da Task 1 passam GREEN; nenhuma interpolação crua de distancia_km resta no arquivo.</done>
  <reversibility rating="reversible">Mudança de formatação isolada a um componente; git revert trivial.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Aluno (client) → editor manual de treino | Texto digitado em `distancia_km` chega já como `number \| null` validado por `isValidExerciseDistance` (0.01–100 km) antes de renderizar em `ManualExerciseRow` — este plano só formata exibição, não input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | Tampering | `ManualExerciseRow.metricLabel` | low | mitigate | `formatDistance` já trata `null`/valores ≤0 devolvendo "—"; o guard `!= null` evita que um valor inválido vaze como "NaN km" — coberto pelo caso `distancia_km=null` da Task 1. |
</threat_model>

<verification>
- `npx jest __tests__/manualExerciseRow.test.tsx __tests__/sessionPlayerTransitions.test.tsx --silent` verde.
- `npx tsc --noEmit` sem novo erro.
- Nenhuma outra tela de exibição de distância foi tocada (grep `distancia_km` fora de
  `ManualExerciseRow.tsx`/`manualPlan.ts` não deve mostrar novo ponto de formatação bruta).
</verification>

<success_criteria>
Success Criterion 1 do ROADMAP (Fase 1): usuário digita "2,4" e o valor volta a aparecer como
"2,4 km" — comprovado por teste automatizado nos dois pontos de entrada (player de sessão,
já correto; editor manual, corrigido aqui).
</success_criteria>

<output>
Criar `.planning/phases/01-fluxo-cardio-e-alongamento/01-01-SUMMARY.md` ao final, incluindo o
resultado exato dos testes da Task 1 (o que estava GREEN vs RED antes do fix) como evidência.
</output>
