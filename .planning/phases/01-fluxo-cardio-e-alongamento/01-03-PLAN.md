---
phase: 01-fluxo-cardio-e-alongamento
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - src/components/progress/CardioPrescritoSection.tsx
  - src/components/progress/CardioGoalsSection.tsx
  - src/components/progress/CardioGoalSheet.tsx
  - __tests__/cardioPrescritoSecao.test.tsx
  - __tests__/cardioGoalsSecao.test.tsx
  - src/screens/ProgressScreen.tsx
  - __tests__/progressScreenOrigemJoint.test.tsx
autonomous: true
requirements: [REQ-02]

estimate:
  tokens: 58000
  raw_tokens: 41000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A tela Progresso não oferece mais 'Definir meta de tempo'/'Definir meta de rotina' — mostra prescrito × realizado direto da prescrição do plano ativo, sem UI de escrita"
    - "Sem plano ativo ou sem cardio prescrito na semana, a seção mostra estado vazio explícito ('sem cardio prescrito esta semana'), nunca 0 inventado nem card de meta em branco"
    - "A tabela cardio_goals e suas RPCs continuam intactas no banco (decisão do dono) — nenhuma migration de drop/arquivamento nesta tarefa"
  artifacts:
    - src/components/progress/CardioPrescritoSection.tsx
  key_links:
    - "ProgressScreen → CardioPrescritoSection (substitui CardioGoalsSection na mesma posição da árvore)"
    - "CardioPrescritoSection → cardioPrescritoRepository.getPrescricaoSemanaCorrente + cardioPrescrito.progressoPrescrito (01-02)"
---

<objective>
Fechar REQ-02 (D-02/CONTEXT.md) na UI: trocar a seção "Cardio" da aba Progresso — hoje
`CardioGoalsSection`, que lê `cardio_goals` e oferece "Definir meta" — por `CardioPrescritoSection`,
que lê a prescrição do plano ativo (01-02) e mostra prescrito × realizado, sem nenhuma ação de
escrita de meta.

Purpose: a UI de definição manual de meta "sai" (decisão travada do dono) porque ela é
exatamente a definição paralela que REQ-02 elimina; os dados legados em `cardio_goals`
continuam no banco, só a tela para de os oferecer.
Output: `CardioPrescritoSection.tsx` novo; `CardioGoalsSection.tsx`/`CardioGoalSheet.tsx`
removidos; `ProgressScreen.tsx` religado.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `src/components/progress/CardioPrescritoSection.tsx` (novo, substitui CardioGoalsSection)
- `src/components/progress/CardioGoalsSection.tsx` (removido)
- `src/components/progress/CardioGoalSheet.tsx` (removido)
- `__tests__/cardioPrescritoSecao.test.tsx` (novo, substitui cardioGoalsSecao.test.tsx)
- `__tests__/cardioGoalsSecao.test.tsx` (removido)
- `src/screens/ProgressScreen.tsx` (editado — religado a CardioPrescritoSection)
- `__tests__/progressScreenOrigemJoint.test.tsx` (editado — novo mock de cardioPrescritoRepository)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-fluxo-cardio-e-alongamento/01-CONTEXT.md
@.planning/phases/01-fluxo-cardio-e-alongamento/01-RESEARCH.md
@.planning/phases/01-fluxo-cardio-e-alongamento/01-PATTERNS.md
@src/components/progress/CardioGoalsSection.tsx
@src/screens/ProgressScreen.tsx
@src/engine/cardioPrescrito.ts
@src/services/cardioPrescritoRepository.ts
@__tests__/progressScreenOrigemJoint.test.tsx
@__tests__/cardioGoalsSecao.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar CardioPrescritoSection.tsx e remover CardioGoalsSection/CardioGoalSheet</name>
  <files>src/components/progress/CardioPrescritoSection.tsx, src/components/progress/CardioGoalsSection.tsx, src/components/progress/CardioGoalSheet.tsx, __tests__/cardioPrescritoSecao.test.tsx, __tests__/cardioGoalsSecao.test.tsx</files>
  <read_first>
    src/components/progress/CardioGoalsSection.tsx inteiro (estrutura de `Card`/`SectionHeader`/
    `EmptyState`/`Skeleton`/`Notice`/`ProgressTrack`, import de `formatDistance`/`formatDuration`
    de `../../engine/sessionModel`, `formatarMinutos` local linhas 44-51); src/engine/cardioPrescrito.ts
    (01-02 — `progressoPrescrito`, `type PrescricaoCardio`, `type ProgressoPrescrito`); __tests__/cardioGoalsSecao.test.tsx
    inteiro (para saber o que este arquivo de teste cobre antes de apagá-lo e reescrever
    equivalente).
  </read_first>
  <action>
    Crie `src/components/progress/CardioPrescritoSection.tsx` com `Props = { logs: CardioLog[] |
    null; prescricao: PrescricaoCardio | null; erro: boolean; onRecarregar: () => void }` (tipos
    de `../../engine/cardioGoals` e `../../engine/cardioPrescrito`). Renderização: `erro` →
    `Notice tone="danger"` com botão "Tentar novamente" (mesmo padrão da seção antiga); `logs ===
    null || prescricao === null` → `Skeleton`; `prescricao.duracaoSegundos == null &&
    prescricao.sessoes === 0 && prescricao.distanciaM == null` (nenhuma prescrição de cardio na
    semana) → `EmptyState` "Sem cardio prescrito nesta semana" SEM nenhuma ação de definir meta
    (a UI de escrita não existe mais); caso contrário, chame `progressoPrescrito(logs, prescricao)`
    e renderize um único `Card` "Cardio desta semana" mostrando (quando o respectivo campo
    prescrito não for `null`) "{realizado} de {prescrito} min" com `ProgressTrack` e "{sessoes
    realizadas} de {prescritoSessoes} {dia|dias}" com `ProgressTrack` — SEM botões "Trocar"/
    "Remover"/"Bati esta meta" (não há mais meta editável). Reaproveite `formatDuration` de
    `../../engine/sessionModel` para exibir a duração prescrita quando fizer sentido no texto.
    Depois, DELETE `src/components/progress/CardioGoalsSection.tsx`,
    `src/components/progress/CardioGoalSheet.tsx` e `__tests__/cardioGoalsSecao.test.tsx` (o
    dono já marcou ambos os componentes como "saem" em CONTEXT.md). Escreva
    `__tests__/cardioPrescritoSecao.test.tsx` (arquivo novo) cobrindo: estado vazio quando
    `prescricao` não tem nenhum eixo definido; "sem amostra" no realizado (não mostra 0%
    fingindo medição quando `logs=[]` mas há prescrição); nenhum `getByTestId`/`getByLabelText`
    do tipo "Trocar"/"Remover"/"Definir meta" existe na árvore renderizada (prova negativa de
    que a UI de escrita foi removida).
  </action>
  <verify>
    <automated>npx jest __tests__/cardioPrescritoSecao.test.tsx --silent</automated>
  </verify>
  <acceptance_criteria>
    `git status` confirma `CardioGoalsSection.tsx`/`CardioGoalSheet.tsx`/`cardioGoalsSecao.test.tsx`
    deletados; `grep -rn "definirMeta\|CardioGoalSheet" src/components/progress/CardioPrescritoSection.tsx`
    retorna vazio (nenhuma ação de escrita de meta no componente novo); testes verdes.
  </acceptance_criteria>
  <done>CardioPrescritoSection.tsx substitui CardioGoalsSection sem nenhuma ação de escrita de meta; CardioGoalsSection.tsx/CardioGoalSheet.tsx removidos; testes verdes.</done>
  <reversibility rating="reversible">Componentes removidos permanecem no histórico git; `cardio_goals` (tabela) intacta — reversão é só código.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Religar ProgressScreen.tsx e atualizar o teste de regressão de origem conjunta</name>
  <files>src/screens/ProgressScreen.tsx, __tests__/progressScreenOrigemJoint.test.tsx</files>
  <read_first>
    src/screens/ProgressScreen.tsx:34-40 (imports de `CardioGoalsSection`/`cardioGoalRepository`),
    :63-114 (estado `metasCardio`/`setMetasCardio`, `carregar()` com
    `Promise.all([getCardioLogs(user.id), getMetasAtivas(user.id)])`), :265-271 (`<CardioGoalsSection
    ... />`); __tests__/progressScreenOrigemJoint.test.tsx:55-61 (`jest.mock('../src/services/cardioGoalRepository', ...)`
    — precisa continuar mockando `getCardioLogs` e ganhar um mock análogo para o repositório
    novo, senão o render de `ProgressScreen` tenta tocar o Supabase real).
  </read_first>
  <action>
    Em `ProgressScreen.tsx`: troque o import de `CardioGoalsSection` (linha 40) por
    `CardioPrescritoSection` de `'../components/progress/CardioPrescritoSection'`; troque o
    import de `{ getCardioLogs, getMetasAtivas, type CardioGoal }` de `cardioGoalRepository`
    (linhas 34-38) por só `{ getCardioLogs }` — e adicione
    `{ getPrescricaoSemanaCorrente, type PrescricaoCardio }` de
    `'../services/cardioPrescritoRepository'`. Troque o estado `metasCardio`/`setMetasCardio`
    (linha 66) por `prescricaoCardio`/`setPrescricaoCardio: useState<PrescricaoCardio |
    null>(null)`. Em `carregar()` (linha 100), troque
    `Promise.all([getCardioLogs(user.id), getMetasAtivas(user.id)])` por
    `Promise.all([getCardioLogs(user.id), getPrescricaoSemanaCorrente(user.id)])`, ajustando o
    `.then(([logs, prescricao]) => ...)` para `setPrescricaoCardio(prescricao)`. Troque o JSX
    (linhas 266-271) de `<CardioGoalsSection logs={cardioLogs} metas={metasCardio} .../>` para
    `<CardioPrescritoSection logs={cardioLogs} prescricao={prescricaoCardio} erro={erroCardio}
    onRecarregar={carregar} />`. Em `__tests__/progressScreenOrigemJoint.test.tsx`, adicione
    `jest.mock('../src/services/cardioPrescritoRepository', () => ({ getPrescricaoSemanaCorrente:
    jest.fn(async () => ({ distanciaM: null, duracaoSegundos: null, sessoes: 0 })) }))` junto ao
    mock existente de `cardioGoalRepository` (pode reduzir esse mock a só `getCardioLogs: jest.fn(async
    () => [])`, já que `arquivarMeta`/`definirMeta`/`registrarMetaBatida`/`getMetasAtivas`
    deixam de ser importados por `ProgressScreen.tsx`).
  </action>
  <verify>
    <automated>npx jest __tests__/progressScreenOrigemJoint.test.tsx --silent && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "CardioGoalsSection\|getMetasAtivas" src/screens/ProgressScreen.tsx` retorna vazio;
    `__tests__/progressScreenOrigemJoint.test.tsx` continua verde (prova que o render de
    `ProgressScreen` não bate no Supabase real); `npx tsc --noEmit` sem erro novo.
  </acceptance_criteria>
  <done>ProgressScreen renderiza CardioPrescritoSection a partir de getPrescricaoSemanaCorrente, sem nenhum resquício de CardioGoalsSection/getMetasAtivas; teste de regressão de origem conjunta continua verde.</done>
  <reversibility rating="reversible">Troca de import/estado num único componente de tela; git revert direto.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| UI removida ↔ dado legado no banco | `cardio_goals` e as RPCs `upsert_cardio_goal`/`archive_cardio_goal`/`achieve_cardio_goal` continuam ativas no banco (decisão do dono) mesmo sem UI que as chame. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-01 | Elevation of Privilege | RPCs `upsert_cardio_goal`/`archive_cardio_goal`/`achieve_cardio_goal` (órfãs de UI) | low | accept | Já endurecidas em `0022_metas_de_cardio.sql:283-291` (`revoke all ... grant execute ... to authenticated` + assert `has_function_privilege('anon', ...) = false`) — remover a UI não amplia a superfície: um usuário autenticado já podia chamar a RPC direto antes desta fase; a RLS por `user_id` continua valendo. Nenhuma mudança de privilégio necessária nesta fase (dono decidiu manter a tabela intacta). |
</threat_model>

<verification>
- `npx jest __tests__/cardioPrescritoSecao.test.tsx __tests__/progressScreenOrigemJoint.test.tsx --silent` verde.
- `npx tsc --noEmit` sem novo erro.
- `git status supabase/migrations/` vazio (nenhuma migration criada — `cardio_goals` fica intacta).
</verification>

<success_criteria>
Success Criterion 2 do ROADMAP (Fase 1): a tela Progresso não exibe mais meta de cardio
desconectada do treino — mostra prescrito × realizado derivado do plano ativo.
</success_criteria>

<output>
Criar `.planning/phases/01-fluxo-cardio-e-alongamento/01-03-SUMMARY.md` ao final.
</output>
