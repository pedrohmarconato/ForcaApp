---
phase: 04-escrita-de-execu-o-de-treino-em-lote-e-offline-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/sessionOutboxPolicy.ts
  - src/engine/config.ts
  - src/services/sessionOutboxStorage.ts
  - src/services/sessionOutboxDrain.ts
  - src/hooks/useSessionOutboxDrain.ts
  - src/navigation/RootNavigator.js
  - src/store/activeSessionStore.ts
  - src/screens/ActiveSessionScreen.tsx
  - __tests__/sessionOutboxPolicy.test.ts
  - __tests__/sessionOutboxStorage.test.ts
  - __tests__/sessionOutboxDrain.test.ts
  - __tests__/activeSessionStore.test.ts
  - __tests__/completeSetAdaptacaoNaoDerruba.test.ts
autonomous: true
requirements: [REQ-07]

estimate:
  tokens: 135000
  raw_tokens: 135000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Com saveSetLog rejeitando por erro de transporte (isTransportSessionExecutionError), completeSet resolve com true, a série do draft passa a status 'done' no MESMO tick da chamada (sem aguardar a rede) e store().saveError permanece null — D-05."
    - "O item de save_set_log fica persistido em AsyncStorage sob a chave `@session_outbox_<userId>` com nextAttemptAt no futuro após uma falha de transporte; reenfileirar o MESMO (sessionLogId, plannedSetId) enquanto o item original ainda está pendente substitui o payload existente em vez de criar um segundo item — D-13."
    - "Um erro cujo .code está na allowlist definitiva (P0005, 42501, 23023 formatado como 22023, 22004, P0002) move o item para a quarentena do documento (com reason, code, quarantinedAt, expiresAt) e a drenagem de OUTRAS sessões do mesmo usuário não é bloqueada por isso — D-04, D-07, D-14."
    - "Um erro cujo .code é P0001 (sessão já finalizada) durante a drenagem de QUALQUER item de uma sessionLogId descarta TODOS os itens pendentes daquela sessionLogId (não só o item que falhou), dispara o callback onSessionClosed e NÃO cria entrada de quarentena para eles — Pitfall 3 do RESEARCH.md, D-06 não se aplica a este caso (não é recusa de item, é reconciliação de estado)."
    - "nextDrainable nunca deixa um item em cooldown de backoff de UMA sessionLogId impedir a cabeça de OUTRA sessionLogId de ser drenada — provado por teste unitário puro com duas sessões, uma em cooldown e outra livre — D-04/D-10, Pitfall 4."
    - "Falha do AsyncStorage.setItem durante o enfileiramento não lança para o chamador de completeSet: o item segue contabilizado em memória, uma tentativa de drenagem ainda ocorre, e store().storageWarning é setado com o STORAGE_WARNING_MSG já existente — D-12."
    - "Um erro de servidor sem .code reconhecido (nem allowlist definitiva, nem P0001) permanece retentável (nextAttemptAt recalculado por backoff) e NUNCA é movido para quarentena só por não ter sido classificado — Pitfall 2 do RESEARCH.md."
    - "Com saveSetLog resolvendo normalmente (rede boa), o comportamento observável de completeSet é equivalente ao de hoje: draft.exercises[].sets[].status vira 'done', a próxima série do mesmo exercício sugere a carga usada, e nenhum erro aparece — critério de sucesso #5 do 04-CONTEXT.md."
  artifacts:
    - src/engine/sessionOutboxPolicy.ts
    - src/services/sessionOutboxStorage.ts
    - src/services/sessionOutboxDrain.ts
    - src/hooks/useSessionOutboxDrain.ts
    - __tests__/sessionOutboxPolicy.test.ts
    - __tests__/sessionOutboxStorage.test.ts
    - __tests__/sessionOutboxDrain.test.ts
  key_links:
    - "activeSessionStore.completeSet -> sessionOutboxDrain.enqueueAndDrain -> sessionOutboxStorage.saveOutbox (durável, D-12) -> sessionOutboxDrain.drainAll (fire-and-forget) -> sessionExecutionRepository.saveSetLog (RPC intocada, D-02) -> guarda 0005 no Postgres — o caminho que substitui o await síncrono que hoje trava a tela em soluço de rede."
    - "sessionOutboxDrain P0001 -> callback onSessionClosed injetado pela store -> activeSessionStore.reconcileRemoteSessionClosed -> retireLocalDraft (mesma função já existente) — preserva o comportamento visível de hoje (fecha a tela como finalizada) sem duplicar a lógica de tombstone."
    - "useSessionOutboxDrain (montado em RootNavigator.js, acima da tela de sessão) -> sessionOutboxDrain.drainAll no mount e em todo AppState 'active' — a fila é do usuário, não da tela (D-10); drena mesmo com o aluno fora de ActiveSessionScreen."

  prohibitions:
    - statement: "A fila NUNCA descarta um item pendente ou de quarentena sem deixar rastro local (nem em log nem em disco) — perda silenciosa de uma série registrada é exatamente o dano que esta fase existe para eliminar (D-07)."
      status: kept
      verification: unverified
    - statement: "A fila NUNCA produz uma segunda linha em set_logs para o mesmo (session_log_id, planned_set_id) ao reenviar um item — a garantia de exatamente-uma-vez da guarda 0005 nunca é reimplementada nem contornada no cliente (D-02, D-13)."
      status: kept
      verification: unverified
    - statement: "completeSet NUNCA aguarda a confirmação de rede antes de aplicar o estado 'done' local — bloquear a tela de novo reintroduziria o sintoma exato que originou a fase (D-05)."
      status: kept
      verification: unverified
---

<objective>
Fase 04, REQ-07 — cria a arquitetura da fila offline-first como camada nova ENTRE
store e repository (D-15: engine puro em `src/engine/`, I/O em `src/services/`,
`activeSessionStore.ts` não engorda), com gatilho de drenagem por tentativa
imediata + backoff + retorno ao primeiro plano via `AppState` — sem
`@react-native-community/netinfo`/`expo-network`, nenhuma dependência nativa nova
(D-03) — e liga a PRIMEIRA operação
da lista do D-01 — `save_set_log`, junto com `update_set_log_adaptation`, que
depende do mesmo `completeSet` e do mesmo `plannedSetId` — ponta a ponta: do toque
em "concluir série" até a linha em `set_logs`, passando pelo buffer local. As
outras quatro operações (`skip_session_exercise`, `unskip_session_exercise`,
`swap_session_exercise`, `finish_session`) entram no `04-02-PLAN.md`, que
reaproveita a mesma máquina de classificação/quarentena/backoff construída aqui.

**Nota sobre edge não classificado (specless-probe fallback):** o probe
determinístico de edges não conseguiu classificar o texto de REQ-07
(`category: unclassified`, `verification: null`). Os critérios de aceite abaixo
(`must_haves.truths`) foram derivados manualmente do 04-CONTEXT.md (D-01..D-16) e
do 04-RESEARCH.md (Pitfalls 1-5) precisamente para cobrir os casos de borda que o
probe não classificou: enfileiramento offline, drenagem na reconexão, retry ×
quarentena, tratamento de P0001 (sessão finalizada), first-write-wins contra envio
duplicado, sobrevivência da fila a fechar o app, falha parcial de lote. Esta é a
resolução explícita do edge não classificado — não um drop silencioso.

Purpose: fechar a lacuna estrutural confirmada em
`.planning/debug/typeerror-envio-series-treino.md` (causa-raiz 2) — hoje não existe
NENHUMA fila/outbox/retry para escritas de execução de sessão.
Output: `src/engine/sessionOutboxPolicy.ts`, `src/services/sessionOutboxStorage.ts`,
`src/services/sessionOutboxDrain.ts`, `src/hooks/useSessionOutboxDrain.ts` novos;
`activeSessionStore.completeSet`/`resolveAdaptation` e `ActiveSessionScreen.tsx`
reescritos para o novo contrato otimista; suíte de testes existente afetada
(Pitfall 5) corrigida.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-CONTEXT.md
@.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-RESEARCH.md
@.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@src/store/activeSessionStore.ts
@src/services/sessionExecutionRepository.ts
@src/services/sessionDraftStorage.ts
@src/hooks/useDiaLocal.ts
@src/engine/config.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `src/engine/sessionOutboxPolicy.ts` (NOVO): `OutboxItemKind`, `OutboxItem`, `QuarantineItem`,
  `OutboxDocument`, `buildItemId`, `upsertItem`, `nextDrainable`, `isDefinitiveRejection`,
  `isSessionClosedCode`, `computeBackoff`, `isExpired`, `buildQuarantineItem`,
  `pruneExpiredQuarantine`.
- `src/engine/config.ts` (EDITADO): `OutboxConfig` type + `OUTBOX_CONFIG` const.
- `src/services/sessionOutboxStorage.ts` (NOVO): `loadOutbox`, `saveOutbox`.
- `src/services/sessionOutboxDrain.ts` (NOVO): `RPC_TIMEOUT_MS` (realocado),
  `DrainCallbacks` type, `enqueueItem`, `drainAll`, `enqueueAndDrain`.
- `src/hooks/useSessionOutboxDrain.ts` (NOVO): `useSessionOutboxDrain`.
- `src/store/activeSessionStore.ts` (EDITADO): campos `pendingCount`, `quarantineCount` em
  `ActiveSessionState`; ações `reconcileRemoteSessionClosed`, `setOutboxSummary`;
  `completeSet` e `resolveAdaptation` reescritos.
- `__tests__/sessionOutboxPolicy.test.ts`, `__tests__/sessionOutboxStorage.test.ts`,
  `__tests__/sessionOutboxDrain.test.ts` (NOVOS).
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: fila ponta a ponta para save_set_log + update_set_log_adaptation (engine, storage, drain, hook, store, selo)</name>
  <files>src/engine/sessionOutboxPolicy.ts, src/engine/config.ts, src/services/sessionOutboxStorage.ts, src/services/sessionOutboxDrain.ts, src/hooks/useSessionOutboxDrain.ts, src/navigation/RootNavigator.js, src/store/activeSessionStore.ts, src/screens/ActiveSessionScreen.tsx, __tests__/sessionOutboxPolicy.test.ts, __tests__/sessionOutboxStorage.test.ts, __tests__/sessionOutboxDrain.test.ts</files>
  <read_first>
    src/store/activeSessionStore.ts linhas 95-220 (interface `ActiveSessionState`, `PendingAdaptation`
    tipo em torno da linha 83-88 — note `setLogId: string | null`, JÁ aceita null, não precisa mudar o
    tipo), linhas 206-268 (`inFlight`, `operationEpoch`, `RPC_TIMEOUT_MS` linha 215, `withTimeout` linhas
    222-265 — SERÃO MOVIDOS para `sessionOutboxDrain.ts`; `isClosedSessionError` linha 267-268 — molde do
    P0001), linhas 339-350 (`retireLocalDraft`, best-effort tombstone-então-clear, REUTILIZAR sem
    reescrever), linhas 519-556 (`STORAGE_WARNING_MSG` linha 535, início do `create<ActiveSessionState>`),
    linhas 1178-1395 (`completeSet` INTEIRA — é esta função que este task reescreve; note especialmente:
    validação/CAS/reentrância linhas 1178-1231 ficam INTACTAS; a RPC direta linhas 1232-1394 é o que muda;
    `pending`/adaptação automática linhas 1300-1365 usa hoje `saved.actualLoadKg`/`saved.actualRir`/
    `saved.setLogId` — precisam virar valores LOCAIS já calculados em `serie`/`actualReps`/`actualLoadKg`
    etc; a chamada `updateSetLogAdaptation(saved.setLogId, autoKeep, decision)` linha 1351 deve ser
    REMOVIDA nesta task — plano 04-02 a reintroduz via fila, comentário explicando por quê), linhas
    1397-1434 (`resolveAdaptation` — NÃO reescrita nesta task, mas o campo que ela lê,
    `pending.setLogId`, vai ficar sempre `null` até o 04-02; deixar como está, plano 04-02 assume).
    src/services/sessionExecutionRepository.ts linhas 70-133 (`SessionExecutionRequestError`,
    `isTransportSessionExecutionError`, `.kind`/`.code`/`.status` — REUSAR, nunca reimplementar), linhas
    174-230 (`getOpenSessionLog(userId, plannedSessionId)` — filtra `finished_at IS NULL`; retorna
    `setLogs: ServerSetLog[]` com `id`/`planned_set_id` — é a única fonte para resolver `setLogId` a
    partir de `plannedSetId` sem RPC nova; SÓ funciona com a sessão ainda aberta — por FIFO estrito
    (D-04), o item `finish_session` da mesma sessão nunca drena antes de um item de adaptação enfileirado
    antes dele, então a sessão estará sempre aberta quando a resolução tardia rodar, EXCETO se o
    save_set_log correspondente foi para quarentena — nesse caso não existe setLogId a resolver: trate
    como quarentena também, motivo "set_log alvo nunca foi confirmado"), linhas 420-500 (`saveSetLog`
    — params e retorno exatos: `setLogId, actualReps, actualLoadKg, actualRir, outcome,
    actualDurationSeconds, actualDistanceM, paceSecondsPerKm, perceivedEffort, completedAt`), linhas
    502-528 (`updateSetLogAdaptation(setLogId, adaptation, decision)` — UPDATE direto por id).
    src/services/sessionDraftStorage.ts INTEIRO (114 linhas, já em contexto) — molde a copiar literalmente
    para `sessionOutboxStorage.ts`: `withKeyQueue` (linhas 16-36, copiar verbatim), padrão
    parse-com-guarda-de-version (linhas 38-56), `saveDraft`/`loadDraft` (linhas 58-87) como forma de
    `saveOutbox`/`loadOutbox`.
    src/hooks/useDiaLocal.ts INTEIRO (51 linhas, já em contexto) — molde do listener de `AppState` (linhas
    40-47) para `useSessionOutboxDrain.ts`.
    src/engine/config.ts INTEIRO (114 linhas, já em contexto) — estilo de `ADAPT_CONFIG`/`AdaptConfig`
    (linhas 10-45) a replicar para `OUTBOX_CONFIG`/`OutboxConfig`.
    src/screens/ActiveSessionScreen.tsx linhas 68-99 (seletores `useActiveSessionStore((s) => s.X)` — o
    padrão a seguir para `pendingCount`), linhas 382-404 (header e o banner `saveError` via `Notice
    tone="danger"` — o selo de pendência (D-05) NÃO usa `danger`).
    src/components/ui/Feedback.tsx — procurar o componente `Chip` (tones `'neutral' | 'accent' | 'info' |
    'danger'`) — usar `tone="neutral"` para o selo de pendência.
    src/navigation/RootNavigator.js linhas 1-40 (`const { session, profile, loadingSession, ... } =
    useAuth();` — `session.user.id` é o `userId`; montar `useSessionOutboxDrain` logo após essa
    desestruturação, ANTES de qualquer `return` condicional — Rules of Hooks, mesmo cuidado que o
    comentário de `ehMain` já documenta no arquivo).
    __tests__/completeSetAdaptacaoNaoDerruba.test.ts INTEIRO (já em contexto) — mostra o padrão de mock de
    `sessionExecutionRepository` a replicar em `sessionOutboxDrain.test.ts`.
  </read_first>
  <behavior>
    `sessionOutboxPolicy.ts` (puro, sem I/O):
    - `OutboxItemKind`: união com os 6 valores do D-01 (`'save_set_log' | 'update_set_log_adaptation' |
      'skip_session_exercise' | 'unskip_session_exercise' | 'swap_session_exercise' | 'finish_session'`)
      — os 4 últimos não têm dispatcher de RPC ainda (chegam no 04-02), mas o tipo já cobre os 6 (tabela
      completa do D-13/Pattern 2 do RESEARCH.md).
    - `buildItemId(kind, ids)`: chave natural exata por kind — `save_set_log` →
      `${sessionLogId}:set:${plannedSetId}`; `update_set_log_adaptation` →
      `${sessionLogId}:set:${plannedSetId}:adapt`; `skip_session_exercise`/`unskip_session_exercise`/
      `swap_session_exercise` → `${sessionLogId}:exercise:${plannedExerciseId}`; `finish_session` →
      `${sessionLogId}:finish`.
    - `upsertItem(items, newItem)`: identidade é o par (`kind`, `id`) — NÃO só `id`. `skip_session_exercise`
      e `unskip_session_exercise` do MESMO exercício produzem a MESMA string de `id` mas são `kind`s
      diferentes e devem coexistir como itens distintos (nota do Pattern 2 do RESEARCH.md). Mesmo par
      (kind, id) já presente → substitui o payload/nextAttemptAt do item existente (no-op de duplicação,
      D-13); ausente → adiciona ao fim (preserva ordem de inserção = FIFO).
    - `nextDrainable(items, nowISO)`: agrupa por `sessionLogId`, devolve só a cabeça (primeira ocorrência
      na ordem de inserção) de CADA grupo cujo `nextAttemptAt <= nowISO` — teste dedicado com duas sessões
      (uma em cooldown, outra livre) prova que a livre aparece e a outra não (Pitfall 4).
    - `isDefinitiveRejection(code)`: allowlist exata `['P0005', '42501', '22023', '22004', 'P0002']` —
      `P0001` NUNCA está aqui (tratamento à parte).
    - `isSessionClosedCode(code)`: `code === 'P0001'`.
    - `computeBackoff(attempts, nowISO)`: `nextAttemptAt` = agora + min(`OUTBOX_CONFIG.backoffMaxMs`,
      `OUTBOX_CONFIG.backoffBaseMs * 2^attempts`) + jitter aleatório (0 a 1000ms) — nunca usado como
      critério de desistência, só de espaçamento (D-11 é claro: idade, não tentativas).
    - `isExpired(enqueuedAtISO, nowISO)`: idade em dias > `OUTBOX_CONFIG.maxAgeDays`.
    - `buildQuarantineItem(item, reason, code, nowISO)`: `expiresAt` = agora +
      `OUTBOX_CONFIG.quarantineRetentionDays` dias.
    - `pruneExpiredQuarantine(quarantine, nowISO)`: remove entradas com `expiresAt < nowISO` (D-07: expira
      sozinho, sem crescer sem teto).

    `sessionOutboxStorage.ts` (I/O, molde de `sessionDraftStorage.ts`):
    - `keyFor(userId)` → `` `@session_outbox_${userId}` `` — UMA chave por usuário (não por sessão — a
      fila precisa ser descoberta depois de `finish_session`, quando o rascunho já foi limpo; Recomendação
      A1 do RESEARCH.md).
    - `loadOutbox(userId)`: NUNCA lança; formato desconhecido/corrompido/version ≠ 1 → `{ version: 1,
      items: [], quarantine: [] }`.
    - `saveOutbox(userId, doc)`: `withKeyQueue` copiado verbatim de `sessionDraftStorage.ts`.

    `sessionOutboxDrain.ts` (orquestrador, único ponto que chama RPC sob timeout):
    - `RPC_TIMEOUT_MS` (valor 15000, movido de `activeSessionStore.ts`) e `withTimeout` (movido verbatim,
      MESMA implementação — abort + reject no timeout, resolve/reject settle uma vez só).
    - `DrainCallbacks` type: `{ onSessionClosed?: (sessionLogId: string) => void; onSummaryChanged?:
      (pendingCount: number, quarantineCount: number) => void }`.
    - `enqueueItem(userId, { sessionLogId, kind, payload })`: constrói o item via `buildItemId`, faz
      `loadOutbox` → `upsertItem` → tenta `saveOutbox`; SE a persistência falhar (D-12), NÃO relança — o
      item ainda é considerado "pendente" para fins de contagem/retorno, e a tentativa de drenagem
      ACONTECE do mesmo jeito (drenar direto da versão em memória que acabou de ser montada). Retorna o
      documento resultante (para contagem síncrona imediata).
    - `drainAll(userId, callbacks?)`: `pruneExpiredQuarantine` primeiro; laço limitado (ex.: máximo 50
      iterações de segurança) processando `nextDrainable` a cada rodada: para cada item da rodada, monta a
      Promise de RPC pelo `kind` (nesta task, SÓ `save_set_log` e `update_set_log_adaptation` têm case —
      os outros 4 kinds ainda não são gerados por lugar nenhum do store, então não precisam de dispatcher
      ainda; 04-02 adiciona), chama sob `withTimeout`; classifica o resultado:
        - sucesso → remove o item da lista;
        - `isTransportSessionExecutionError(e)` → mantém, `nextAttemptAt = computeBackoff(...)`;
        - `isSessionClosedCode(e.code)` (P0001) → descarta TODOS os itens (de qualquer kind) daquela
          `sessionLogId`, chama `callbacks?.onSessionClosed?.(sessionLogId)`, NÃO gera quarentena;
        - `isDefinitiveRejection(e.code)` → move para quarentena via `buildQuarantineItem`;
        - qualquer outro erro (server sem code reconhecido, ou erro sem `.code` nenhum) → mantém,
          `nextAttemptAt = computeBackoff(...)` (Pitfall 2 — nunca quarentena por classificação incerta);
        - independentemente da classificação acima, SE `isExpired(item.enqueuedAt, now)` → quarentena em
          vez de manter pendente (D-11 é o backstop de idade).
      Para `update_set_log_adaptation`: ANTES de chamar `updateSetLogAdaptation`, resolve `setLogId`
      chamando `getOpenSessionLog(userId, plannedSessionId)` (payload precisa carregar `userId` E
      `plannedSessionId`, não só `plannedSetId`) e procurando `setLogs.find(sl => sl.planned_set_id ===
      payload.plannedSetId)?.id`; se não encontrar (sessão fechada ou set_log nunca confirmado) → trata
      como falha NÃO transporte: se `isExpired` → quarentena com reason "sem setLogId resolvido"; senão →
      mantém pendente com backoff (ainda pode resolver numa próxima passada, ex.: o save_set_log
      correspondente ainda está retentando).
      Persiste o documento resultante (`saveOutbox`) ao fim de cada rodada processada; chama
      `callbacks?.onSummaryChanged?.(items.length, quarantine.length)` ao final. Retorna
      `{ pendingCount, quarantineCount }`.
    - `enqueueAndDrain(userId, params, callbacks?)`: `await enqueueItem(...)` (rápido, só I/O local);
      calcula `{ pendingCount, quarantineCount }` SÍNCRONOS a partir do documento retornado; dispara `void
      drainAll(userId, callbacks)` SEM aguardar (fire-and-forget — é isto que garante D-05: a UI nunca
      espera a rede); retorna os contadores síncronos imediatamente.

    `useSessionOutboxDrain.ts` (hook, molde de `useDiaLocal.ts`, gatilho de drenagem do D-03 — tentativa
    imediata a cada escrita já acontece dentro de `enqueueAndDrain`; este hook cobre o retorno ao primeiro
    plano, sem `@react-native-community/netinfo`/`expo-network`): monta uma vez, chama `drainAll(userId,
    { onSessionClosed: (id) => useActiveSessionStore.getState().reconcileRemoteSessionClosed(id),
    onSummaryChanged: (p, q) => useActiveSessionStore.getState().setOutboxSummary(p, q) })` no mount e em
    todo `AppState.addEventListener('change', ...)` que vira `'active'`; `userId: string | null` — no-op se
    `null`.

    `activeSessionStore.ts`:
    - Remove `RPC_TIMEOUT_MS`/`withTimeout` (linhas 210-265) — agora vivem em `sessionOutboxDrain.ts`.
    - `ActiveSessionState` ganha `pendingCount: number`, `quarantineCount: number` (init `0`),
      `reconcileRemoteSessionClosed: (sessionLogId: string) => void`, `setOutboxSummary: (pendingCount:
      number, quarantineCount: number) => void`.
    - `reconcileRemoteSessionClosed(sessionLogId)`: SÓ age se `get().draft?.sessionLogId === sessionLogId`
      (guarda de CAS, mesma cautela do resto do arquivo) — replica exatamente o bloco `isClosedSessionError`
      de hoje (linhas 1380-1386): `set({ draft: {...atual, status:'finished'}, status:'finished',
      saveError:null })` + `void retireLocalDraft(atual)`.
    - `setOutboxSummary(pendingCount, quarantineCount)`: `set({ pendingCount, quarantineCount })`.
    - `completeSet`: mantém idêntica a validação/CAS/reentrância (linhas 1178-1231); troca o bloco de RPC
      direta por: monta o payload igual ao que `saveSetLog` recebe hoje; chama `enqueueAndDrain(draft.userId,
      { sessionLogId: sid, kind: 'save_set_log', payload }, { onSessionClosed: (id) =>
      get().reconcileRemoteSessionClosed(id), onSummaryChanged: (p,q) => set({pendingCount:p,
      quarantineCount:q}) })`; usa os contadores retornados para `set({pendingCount, quarantineCount, ...})`
      JUNTO do commit otimista do draft (status `'done'`, valores LOCAIS já calculados — `actualReps`,
      `actualLoadKg`, `actualRir`, `outcome`, `actualDurationSeconds`, `actualDistanceM`, `perceivedEffort`
      — `setLogId: null`, `completedAt: null`, reconciliados depois via `applyServerSetLogs` na próxima
      retomada); a lógica de recomendação de adaptação (linhas 1300-1365) passa a usar esses valores locais
      em vez de `saved.*`; a chamada direta a `updateSetLogAdaptation` (linha 1351) é REMOVIDA (comentário:
      "04-02-PLAN.md reintroduz via fila com resolução tardia de setLogId"); `saveDraft(finalDraft)`
      continua igual (best-effort, `storageWarning` em falha); `completeSet` sempre retorna `true` depois da
      validação local passar — o `catch` que hoje trata falha de rede como "não concluiu" deixa de existir
      para esta chamada (a falha vira item pendente/quarentena, nunca `saveError`).

    `ActiveSessionScreen.tsx`: novo seletor `const pendingCount = useActiveSessionStore((s) =>
    s.pendingCount);`; renderiza um `Chip` `tone="neutral"` com um rótulo curto (ex.: "N registro(s) a
    caminho") quando `pendingCount > 0`; NADA quando `pendingCount === 0` (nunca "0 pendentes" — convenção
    "sem dado inventado").
  </behavior>
  <action>
    Implemente exatamente o `<behavior>` acima. Ordem sugerida (TDD): escreva primeiro
    `__tests__/sessionOutboxPolicy.test.ts` cobrindo `nextDrainable` (FIFO por sessão, Pitfall 4),
    `upsertItem` (dedupe por par kind+id, D-13), `computeBackoff`/`isExpired`, `isDefinitiveRejection`
    (allowlist exata) e `buildQuarantineItem`/`pruneExpiredQuarantine` — rode FALHANDO antes de escrever
    `sessionOutboxPolicy.ts`. Implemente `sessionOutboxPolicy.ts` até passar. Adicione `OutboxConfig`/
    `OUTBOX_CONFIG` em `src/engine/config.ts` (mesmo estilo de `AdaptConfig`/`ADAPT_CONFIG`): `maxAgeDays:
    7` (candidato do D-11), `backoffBaseMs: 2000`, `backoffMaxMs: 300000`, `quarantineRetentionDays: 30`
    (Claude's Discretion, comentário inline em cada campo). Escreva
    `__tests__/sessionOutboxStorage.test.ts` (mock manual de `@react-native-async-storage/async-storage`
    com um `Map` em memória, molde de `__tests__/loadDraftCoercion.test.ts`) cobrindo version-guard e
    isolamento por usuário; implemente `sessionOutboxStorage.ts`. Escreva
    `__tests__/sessionOutboxDrain.test.ts` (mock de `sessionExecutionRepository` no molde de
    `completeSetAdaptacaoNaoDerruba.test.ts`) cobrindo: sucesso remove item; transporte retenta com
    backoff; código definitivo quarentena; P0001 descarta a sub-fila inteira e chama `onSessionClosed`;
    código desconhecido NUNCA quarentena antes de expirar; `enqueueAndDrain` retorna contagem síncrona sem
    aguardar `drainAll`; resolução tardia de `update_set_log_adaptation` via `getOpenSessionLog` (mock
    retornando `setLogs` com o `planned_set_id` procurado). Implemente `sessionOutboxDrain.ts` até passar.
    Implemente `useSessionOutboxDrain.ts`. Monte o hook em `src/navigation/RootNavigator.js` (não em
    `App.tsx` — `RootNavigator` já tem `session` via `useAuth()`). Reescreva `completeSet` e o estado
    novo em `activeSessionStore.ts` conforme o `<behavior>`. Adicione o `Chip` de pendência em
    `ActiveSessionScreen.tsx`. Rode `npx tsc --noEmit` e corrija qualquer quebra de tipo cascateada (ex.:
    chamadores de `withTimeout`/`RPC_TIMEOUT_MS` que sumiram de `activeSessionStore.ts`).
  </action>
  <verify>
    <automated>npx jest __tests__/sessionOutboxPolicy.test.ts __tests__/sessionOutboxStorage.test.ts __tests__/sessionOutboxDrain.test.ts -q && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `src/engine/sessionOutboxPolicy.ts` exporta `nextDrainable`, `upsertItem`, `isDefinitiveRejection`,
    `isSessionClosedCode`, `computeBackoff`, `isExpired`, `buildQuarantineItem`, `pruneExpiredQuarantine`,
    `buildItemId`; `grep -c "export const\|export type" src/engine/sessionOutboxPolicy.ts` retorna >= 8.
    `src/store/activeSessionStore.ts` não contém mais `RPC_TIMEOUT_MS` nem a função `withTimeout` (`grep -c
    "^const withTimeout\|^export const RPC_TIMEOUT_MS" src/store/activeSessionStore.ts` retorna `0`).
    `src/services/sessionOutboxDrain.ts` exporta `RPC_TIMEOUT_MS`, `enqueueItem`, `drainAll`,
    `enqueueAndDrain`. `npx jest __tests__/sessionOutboxPolicy.test.ts __tests__/sessionOutboxStorage.test.ts
    __tests__/sessionOutboxDrain.test.ts -q` sai 0 com pelo menos um teste cobrindo cada truth do
    `must_haves.truths` acima. `npx tsc --noEmit` sem erros novos.
  </acceptance_criteria>
  <done>completeSet enfileira em vez de aguardar a RPC; a série conclui localmente na hora (D-05); a
  fila persiste, drena, reclassifica erro e quarentena conforme D-04/D-07/D-11/D-13/D-14; P0001 reconcilia
  o estado local sem silêncio (Pitfall 3); update_set_log_adaptation resolve setLogId tardiamente
  (Pitfall 1, opção 1); o hook drena na abertura do app e no retorno ao primeiro plano, montado acima da
  tela de sessão (D-10).</done>
  <reversibility rating="costly">Trocar a granularidade de storage (documento único por usuário → sharded
  por sessão) depois exigiria migrar dado local já gravado no aparelho dos alunos — por isso a Recomendação
  A1 do RESEARCH.md (documento único, sub-filas em memória) é adotada aqui em vez de deixar para decidir
  depois.</reversibility>
</task>

<task type="auto">
  <name>Task 2: corrige a suíte pré-existente afetada pelo contrato otimista (Pitfall 5)</name>
  <files>__tests__/activeSessionStore.test.ts, __tests__/completeSetAdaptacaoNaoDerruba.test.ts</files>
  <read_first>
    __tests__/activeSessionStore.test.ts linhas 1-90 (mocks de módulo no topo — `sessionExecutionRepository`
    NÃO tem `sessionOutboxDrain`/`sessionOutboxStorage` mockados; esta task NÃO deve adicionar esses mocks —
    deixar rodar contra o AsyncStorage automocado do preset `jest-expo`, para exercitar o comportamento REAL
    da fila junto do store), linhas 250-396 (describe `'concluir série'` — os 4 testes a reescrever: linha
    260 e 285 continuam válidos sem mudança de asserção — conferir; linha 294 "PRIMEIRA CARGA" continua
    válido, é falha de VALIDAÇÃO LOCAL, não de rede — conferir sem mudar; linha 307 "BODYWEIGHT" continua
    válido; linha 325 "ERRO do banco ao salvar" — REESCREVER: sob D-05 o `ok` esperado passa a `true`, o
    `status` do set 1 passa a `'done'`, `store().saveError` passa a `null`; a asserção nova de valor deve
    verificar que a série ficou pendente/tentando na fila, não que "falhou"; linha 340 "log finalizado
    remotamente" — REESCREVER: `completeSet` retorna `true` de imediato (otimista); o fechamento da sessão
    (`status: 'finished'`, `clearDraft` chamado) só acontece DEPOIS que a drenagem em segundo plano descobre
    o P0001 — o teste precisa `await`ar essa drenagem explicitamente, ex. importando `drainAll` de
    `../src/services/sessionOutboxDrain` e chamando `await drainAll('user-1', { onSessionClosed: (id) =>
    useActiveSessionStore.getState().reconcileRemoteSessionClosed(id) })` depois do `completeSet`, então
    conferir `store().status === 'finished'` e `clearDraft` chamado), linhas 958-1020 (describe `'trava de
    reentrância (F9)'` — REESCREVER: sob D-05, `r1` (resultado da 1ª chamada, cuja RPC trava até o timeout)
    passa a `true`, `store().saveError` continua `null`, `status` do set vira `'done'` IMEDIATAMENTE — a
    trava `inFlight` ainda existe mas agora protege só a janela curta do enfileiramento local, não mais 15s
    de rede; a asserção "TRAVA foi liberada: nova tentativa consegue disparar a RPC de novo" deixa de fazer
    sentido do jeito atual, porque a 2ª chamada a `completeSet('ex-1', 1)` agora bate no short-circuit
    `serie.status === 'done'` (linha 1189) e retorna `true` sem nova RPC — reescreva o teste para provar,
    em vez disso, que o item de `save_set_log` ORIGINAL (o que ficou "travado" até o timeout) continua na
    fila com backoff agendado após o timeout, sem duplicar).
    __tests__/completeSetAdaptacaoNaoDerruba.test.ts linhas 237-248 (describe `'completeSet: falha REAL de
    rede continua sendo reportada'` — este é o teste que afirma o comportamento PRÉ-fase que o REQ-07
    substitui; REESCREVER o título e a asserção para o comportamento NOVO: `saveSetLog` rejeitando com
    `new Error('rede caiu')` → `ok` é `true`, `set1.status` é `'done'`, `store().saveError` é `null`, e o
    item continua pendente na fila — atualize também o comentário do cabeçalho do arquivo, linhas 1-10, que
    hoje descreve "ERRO do banco ao salvar a série NÃO marca como feita" como comportamento vigente).
  </read_first>
  <action>
    Reescreva os testes listados no `<read_first>` para refletirem o contrato pós-fila: falha de rede
    (transporte OU servidor não classificado) em `save_set_log` NUNCA impede `completeSet` de retornar
    `true` e marcar a série `'done'` de imediato, e NUNCA seta `saveError` (D-05). Preserve os testes que já
    validam comportamento inalterado (rede boa, validação local, BODYWEIGHT, F2 concorrência) SEM tocar
    suas asserções, só confirmando que ainda passam. Adicione, no describe `'concluir série'`, um teste
    NOVO ("reconecta e drena sem duplicar") que: mocka `saveSetLog` para rejeitar UMA vez com erro de
    transporte e resolver na segunda chamada; completa a série; chama `drainAll` diretamente; confirma
    `saveSetLog` foi chamado exatamente 2 vezes (1 falha + 1 retry bem-sucedido) e nenhuma vez a mais —
    aproxima o critério de sucesso #2 do 04-CONTEXT.md em nível de mock (a prova contra Postgres real fica
    no 04-03-PLAN.md).
  </action>
  <verify>
    <automated>npx jest __tests__/activeSessionStore.test.ts __tests__/completeSetAdaptacaoNaoDerruba.test.ts -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/activeSessionStore.test.ts -q` sai 0 com TODOS os describes existentes (incluindo os
    não tocados) verdes. `npx jest __tests__/completeSetAdaptacaoNaoDerruba.test.ts -q` sai 0. Nenhum teste
    do arquivo afirma `expect(ok).toBe(false)` para uma falha de REDE em `saveSetLog` (grep de regressão:
    `grep -n "toBe(false)" __tests__/activeSessionStore.test.ts` — cada ocorrência remanescente corresponde
    a falha de VALIDAÇÃO LOCAL, não de rede — conferir manualmente cada linha listada). `npx jest -q`
    (suíte padrão completa) não introduz nenhuma falha NOVA em outros arquivos.
  </acceptance_criteria>
  <done>A suíte pré-existente reflete o contrato "falha de rede não interrompe o registro local" (D-05);
  nenhum teste afirma o comportamento pré-fase que o REQ-07 substitui.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `sessionOutboxDrain.ts` → `sessionExecutionRepository` (RPCs Supabase existentes) | Mesmo boundary de hoje (JWT do usuário, RLS por `auth.uid()`); a fila não introduz novo trust boundary, só reordena QUANDO a chamada acontece. |
| App → AsyncStorage local (`@session_outbox_<userId>`) | Dado do próprio usuário no próprio aparelho; mesma postura de `sessionDraftStorage.ts` (sem criptografia — reps/carga/motivo de recusa não são segredo). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-01 | Tampering | `sessionOutboxDrain.drainAll` reenviando `save_set_log` após timeout/retry | medium | mitigate | A guarda `0005_set_log_first_write_wins.sql` (server-side, first-write-wins) continua sendo a fonte de verdade; o cliente nunca deriva idempotência própria, só chama a MESMA RPC com a MESMA chave natural (D-02, D-13) — provado no nível 2 (04-03-PLAN.md) contra Postgres real. |
| T-04-02 | Tampering | Documento `@session_outbox_<userId>` editável em aparelho comprometido (rooteado/jailbroken) | low | accept | Mesmo risco residual já aceito para `sessionDraftStorage.ts`; os vocabulários fechados e CHECKs do servidor (RIR, outcome, `SkipReason`, modalidade) permanecem autoritativos independentemente do que o cliente envia. |
| T-04-03 | Denial of Service (do próprio usuário, não multi-tenant) | Item "envenenado" (payload que sempre falha) travando a sub-fila de uma sessão | medium | mitigate | Allowlist de código definitivo (`isDefinitiveRejection`) move o item para quarentena; `isExpired`/D-11 é o backstop de idade — nenhum item bloqueia sua sub-fila para sempre. |
| T-04-04 | Tampering / Elevation of Privilege | P0001 (sessão já finalizada) classificado por engano como quarentena comum, deixando o draft local "ativo" enquanto o servidor já fechou a sessão | high | mitigate | Branch dedicado para `isSessionClosedCode` em `drainAll` (Pitfall 3): descarta a sub-fila inteira da sessão e chama `onSessionClosed`, que reconcilia o estado local (mesmo caminho de `retireLocalDraft` que `completeSet` já usa hoje) — nunca silencioso, nunca tratado como o D-06 (quarentena comum). |
</threat_model>

<verification>
- `npx jest __tests__/sessionOutboxPolicy.test.ts __tests__/sessionOutboxStorage.test.ts __tests__/sessionOutboxDrain.test.ts -q` verde.
- `npx jest __tests__/activeSessionStore.test.ts __tests__/completeSetAdaptacaoNaoDerruba.test.ts -q` verde.
- `npx jest -q` (suíte padrão completa) sem falha nova.
- `npx tsc --noEmit` sem erro novo.
</verification>

<success_criteria>
completeSet nunca bloqueia nem mostra erro por soluço de rede (critério de sucesso #1 do 04-CONTEXT.md,
parcial — cobre save_set_log; os demais 4 tipos entram no 04-02); a fila persiste, classifica e drena
localmente com prova unitária dos 5 modos de falha (transporte, definitivo, P0001, desconhecido, idade);
update_set_log_adaptation resolve setLogId sem RPC nova.
</success_criteria>

<output>
Criar `.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-01-SUMMARY.md` ao final.
</output>
