---
phase: 04-escrita-de-execu-o-de-treino-em-lote-e-offline-first
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - __tests__/integration/sessionOutboxDrain.postgrest.test.ts
autonomous: false
requirements: [REQ-07]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - statement: "Rodado localmente contra Postgres real (npm run test:integration:pg), reenviar o mesmo item de save_set_log (mesma sessionLogId+plannedSetId) duas vezes via sessionOutboxDrain produz UMA única linha em set_logs — a guarda 0005 (first-write-wins) viva, não uma reimplementação no cliente (D-02, D-13, critério de sucesso #2 do 04-CONTEXT.md)."
      verification: backstop
    - statement: "Rodado localmente contra Postgres real, uma troca de modalidade que dispara P0005 da migration 0036 (exercício com série já concluída) move o item para a quarentena do documento local em vez de retentar indefinidamente, e a drenagem de OUTROS itens pendentes da mesma sessão continua normalmente (D-04, D-07, D-14)."
      verification: backstop
    - statement: "Com o aparelho em modo avião no meio de um treino real, concluir uma série marca a série como feita na tela, sem mensagem de erro, com o selo de pendência visível; desativado o modo avião, a fila drena sozinha (sem ação do usuário) e a série aparece no histórico; fechar e reabrir o app com fila pendente — inclusive depois de finalizar a sessão — drena o restante (critérios de sucesso #1 e #3 do 04-CONTEXT.md, D-16 nível 3, único nível que reproduz o sintoma real que originou a fase)."
      verification: manual-only
  artifacts:
    - __tests__/integration/sessionOutboxDrain.postgrest.test.ts
  key_links:
    - "sessionOutboxDrain.drainAll -> sessionExecutionRepository.saveSetLog/swapSessionExercise (cliente REAL, autenticado como o usuário semeado, não service_role) -> RPC Postgres local -> guardas 0005/0036 -> resultado observado no harness — a única forma, neste repo, de provar retry-sem-duplicar e P0005-vira-quarentena contra o banco de verdade em vez de um mock que nunca fala com o Postgres."
  prohibitions:
    - statement: "O harness de integração NUNCA aponta para staging (mjdjtiujhwklchalquhc) ou produção (zanqygwsgxkyjiuhrzju) — trava de loopback (regex sobre SUPABASE_INTEGRATION_URL, só 127.0.0.1/localhost) roda ANTES de qualquer chamada de rede, mesmo padrão de T-03-09 do 03-07-PLAN.md."
      status: kept
      verification: unverified
---

<objective>
Fecha o D-16 (Barra de prova em três níveis): o nível 1 (política pura, unitário) já
foi entregue no `04-01-PLAN.md`. Este plano entrega o nível 2 (drenagem real contra
Postgres local, provando retry-sem-duplicar e P0005-vira-quarentena) e o nível 3
(UAT com o aparelho em modo avião no meio de um treino — o único nível que
reproduz o sintoma real de `.planning/debug/typeerror-envio-series-treino.md`).

Purpose: os níveis 1 e 2 fecham `04-02-PLAN.md` só até "mock nunca mentiu"; sem
prova contra Postgres real e sem o teste manual, a Fase 4 repetiria o mesmo erro
de processo que a Fase 3 cometeu (G-03-3: suíte 100% mockada nunca capturou um
erro que só o PostgREST real devolvia).
Output: `__tests__/integration/sessionOutboxDrain.postgrest.test.ts` novo, fora da
suíte padrão (mesmo `testPathIgnorePatterns` já existente); UAT registrado via
checkpoint humano.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-CONTEXT.md
@.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-RESEARCH.md
@.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-01-SUMMARY.md
@.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-02-SUMMARY.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-07-PLAN.md
@AGENTS.md
@src/services/sessionExecutionRepository.ts
@supabase/migrations/0005_set_log_first_write_wins.sql
@supabase/migrations/0036_guarda_set_log_troca_cardio.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: harness de integração — retry não duplica (0005) e P0005 (0036) vira quarentena</name>
  <precondition>Stack Supabase local (OrbStack/Docker) está de pé e alcançável — checar com `curl -sf http://127.0.0.1:54321/rest/v1/ -o /dev/null` (ou o host de SUPABASE_INTEGRATION_URL, se sobrescrito) retornando exit 0 — e SUPABASE_INTEGRATION_SERVICE_ROLE_KEY e SUPABASE_INTEGRATION_ANON_KEY exportadas no shell (copiadas de `supabase status`) antes de semear qualquer fixture.</precondition>
  <files>__tests__/integration/sessionOutboxDrain.postgrest.test.ts</files>
  <read_first>
    .planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-07-PLAN.md INTEIRO (já em contexto) — molde
    COMPLETO a seguir: trava de loopback (regex só `127.0.0.1`/`localhost` sobre
    `SUPABASE_INTEGRATION_URL`, hard-fail no import, ANTES de qualquer chamada de rede — T-03-09), leitura
    de env vars sem default para credenciais, `jest.mock('../../src/config/supabaseClient', () => ({
    supabase: <client> }))` ANTES do import dos módulos sob teste, seed via client `service_role`
    (`admin.auth.admin.createUser`, insert em `training_plans`/`planned_sessions`/`session_logs`),
    `afterAll` com `admin.auth.admin.deleteUser` (cascade cuida do resto). ESTE harness precisa de UMA
    diferença central em relação ao 03-07: lá a função sob teste (`getSessionLogDetail`) era um SELECT sem
    checagem de `auth.uid()`; AQUI as RPCs (`save_set_log`, `swap_session_exercise`) verificam
    `session_logs.user_id = auth.uid()` internamente (ver `supabase/migrations/0005_set_log_first_write_wins.sql`
    linhas 98-101, "session_log_id inexistente ou alheio") — chamar via client `service_role` faria
    `auth.uid()` retornar null e a RPC rejeitar como "alheio". Por isso este harness precisa de DOIS
    clientes: um ADMIN (`service_role`, só para seed/teardown) e um USUÁRIO (criado com
    `createClient(url, anonKey)` + `.auth.signInWithPassword({ email, password })`, usando a MESMA senha
    definida no `admin.auth.admin.createUser`) — é o client USUÁRIO que deve ser retornado pelo
    `jest.mock('../../src/config/supabaseClient', ...)`, não o admin.
    supabase/migrations/0005_set_log_first_write_wins.sql INTEIRO (já em contexto) — `on conflict (...)
    where planned_set_id is not null` (first-write-wins) e os `raise exception` com `errcode` (`P0001`,
    `P0002`, `22004`, `42501`).
    supabase/migrations/0036_guarda_set_log_troca_cardio.sql INTEIRO (já em contexto) — condição EXATA que
    dispara `P0005` (linhas 130-137: exercício de cardio com pelo menos um `set_logs` já gravado para
    aquele `planned_exercise_id` nesta `session_log_id`) — o seed precisa: 1 sessão, 1 exercício de cardio
    com >= 1 `planned_sets`, uma série JÁ gravada nele (via `saveSetLog` real, chamado no próprio `beforeAll`
    do teste) ANTES de tentar `swapSessionExercise` no mesmo exercício.
    src/services/sessionOutboxDrain.ts — releia `enqueueItem`/`drainAll`/`enqueueAndDrain` (já
    implementados no 04-01/04-02) — o teste chama estas funções DIRETAMENTE (não passa pela store React),
    com `sessionExecutionRepository` REAL (não mockado) e `AsyncStorage` REAL do ambiente Node do harness
    (mesmo `jest.integration.config.js`, `testEnvironment: 'node'` — `@react-native-async-storage/
    async-storage` precisa ser mockado com um `Map` em memória neste harness, IGUAL ao padrão de
    `__tests__/loadDraftCoercion.test.ts`, porque não há ambiente RN real aqui).
    package.json — confirme que `test:integration:pg`/`testPathIgnorePatterns` já cobrem
    `__tests__/integration/**` (criados pelo 03-07, não precisam mudar).
  </read_first>
  <behavior>
    DOIS testes no mesmo arquivo, cada um com seed próprio (usuário/plano/sessão/exercício isolados, para
    não interferir):

    Teste A — "retry não duplica contra a guarda 0005 real": seed 1 usuário + training_plan +
    planned_session + planned_exercise (musculação) + 1 planned_set + session_log (via insert direto,
    client admin). Chama `enqueueItem` + `drainAll` para um item `save_set_log` cujo payload aponta para o
    `planned_set_id` semeado — confirma 1 linha em `set_logs` (client admin, select direto). Chama
    `enqueueItem` de NOVO com o MESMO `sessionLogId`+`plannedSetId` (payload pode variar levemente, ex.
    `actualReps` diferente — não importa, o servidor preserva a PRIMEIRA gravação) + `drainAll` — confirma
    AINDA 1 linha em `set_logs` (não 2), com os valores da PRIMEIRA gravação (prova first-write-wins do
    lado do cliente, não só do banco).

    Teste B — "P0005 da 0036 vira quarentena sem travar a drenagem": seed 1 usuário + training_plan +
    planned_session + 2 planned_exercises de cardio (A e B, cada um com 1 planned_set) + session_log. Grava
    uma série REAL no exercício A (via `enqueueItem`+`drainAll` de um `save_set_log`, confirmando sucesso).
    Enfileira DOIS itens: um `swap_session_exercise` no exercício A (que vai bater no P0005 porque A já tem
    série gravada) e um `save_set_log` no exercício B (item independente, sem relação com o P0005). Chama
    `drainAll` UMA vez — confirma: (1) o item de swap foi para a quarentena do documento (ler o documento
    via a função de storage exportada do 04-01, ou via um getter de teste); (2) o item de save_set_log do
    exercício B foi processado normalmente (linha em `set_logs` existe); ou seja, a quarentena de UM item
    não travou a drenagem do OUTRO.
  </behavior>
  <action>
    Crie `__tests__/integration/sessionOutboxDrain.postgrest.test.ts` seguindo o molde do 03-07-PLAN.md
    (trava de loopback, leitura de env sem default, dois clientes admin+usuário conforme o `<read_first>`,
    seed/teardown via admin). Adicione ao topo do arquivo o comentário de propósito (D-16 nível 2, como
    rodar via `supabase start` + `npm run test:integration:pg`). Implemente os dois testes descritos no
    `<behavior>`. Rode `npm run test:integration:pg` e confirme os dois testes verdes contra o Postgres
    local de verdade — se falhar, DIAGNOSTIQUE contra o código real (não ajuste a asserção para "passar")
    antes de prosseguir. Rode a suíte padrão completa (`npx jest -q`) e `npx tsc --noEmit` para confirmar
    que nada regrediu.
  </action>
  <verify>
    <automated>npm run test:integration:pg && npx jest -q && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `npm run test:integration:pg` sai 0 com os dois testes (`A` e `B`) verdes. O arquivo não contém
    NENHUMA URL de staging/produção literal (`grep -c "mjdjtiujhwklchalquhc\|zanqygwsgxkyjiuhrzju"
    __tests__/integration/sessionOutboxDrain.postgrest.test.ts` retorna `0`). A trava de loopback aparece
    ANTES de qualquer `createClient`/chamada de rede no arquivo (leitura manual). `npx jest -q` (suíte
    padrão) e `npx tsc --noEmit` sem regressão.
  </acceptance_criteria>
  <done>Provado contra Postgres real: reenviar o mesmo save_set_log não duplica (guarda 0005 viva do lado
  do servidor, respeitada pelo cliente); P0005 da 0036 move o item para quarentena sem travar a drenagem de
  outros itens da mesma sessão.</done>
  <reversibility rating="reversible">Teste novo + infraestrutura de teste, revertível via git; nenhuma
  migration, nenhuma mutação em banco vivo além dos dados efêmeros criados/apagados pelo próprio harness
  dentro do stack LOCAL.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT modo avião — fila offline-first no meio de um treino real</name>
  <what-built>
    A fila offline-first completa: 6 operações de execução de sessão (save_set_log,
    update_set_log_adaptation, skip_session_exercise, unskip_session_exercise,
    swap_session_exercise, finish_session) passam por um buffer local durável
    (AsyncStorage, `@session_outbox_<userId>`) com retry/backoff, FIFO por sessão,
    quarentena para recusa definitiva do servidor e reconciliação de P0001. A tela
    nunca mostra erro de rede para estas 6 operações; um selo discreto indica
    registro pendente. Provado até aqui por 3 níveis: unitário (04-01), mock de
    store (04-01/04-02), Postgres real (Task 1 deste plano).
  </what-built>
  <how-to-verify>
    Este é o D-16 nível 3 — o ÚNICO nível que reproduz o sintoma real que originou a
    fase (`.planning/debug/typeerror-envio-series-treino.md`). Rode no aparelho
    (físico ou simulador com controle de rádio) ou emulador Android com rede
    desligável:

    1. Abra um treino e ative o modo avião (ou desligue Wi-Fi/dados) no MEIO do
       treino, antes de concluir uma série.
    2. Toque em "concluir série". Confirme: a série aparece marcada como feita
       IMEDIATAMENTE, SEM mensagem de erro na tela, e aparece um selo discreto
       indicando registro pendente (sem cor de erro).
    3. Continue o treino normalmente (mais séries, um recusar exercício, uma troca
       de modalidade se aplicável) — confirme que nada trava nem mostra erro.
    4. Desative o modo avião. SEM tocar em nada, confirme que a fila drena sozinha
       em segundo plano (o selo de pendência some depois de alguns segundos).
    5. Finalize o treino (ainda em modo avião OU já com rede de volta) — confirme
       que a tela de fim aparece normalmente, sem esperar confirmação de rede.
    6. Feche o app COMPLETAMENTE com a fila ainda tendo item pendente (repita o
       passo 1-2 e feche o app antes de reativar a rede). Reabra o app com rede
       normal — confirme que a fila drena na abertura, mesmo que a sessão já tenha
       sido finalizada no passo 5 de uma rodada anterior.
    7. Confira o histórico do treino (`getSessionLogDetail`/tela de histórico) —
       confirme que as séries registradas em modo avião aparecem corretamente, sem
       duplicação.
  </how-to-verify>
  <resume-signal>Digite "aprovado" se os 7 passos se comportaram como descrito, ou descreva o que
  divergiu (qual passo, o que apareceu na tela em vez do esperado).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Harness de integração (client USUÁRIO autenticado + client `service_role` para seed/teardown) → stack Supabase LOCAL | Dois clientes com poderes diferentes na mesma sessão de teste; o `service_role` bypassa RLS só para seed/teardown, nunca para exercitar a RPC sob teste (que precisa rodar como o usuário real, para respeitar `auth.uid()` do jeito que produção respeita). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-04-07 | Information Disclosure / Elevation of Privilege | Harness de integração usando `service_role` (seed/teardown) e credencial de usuário real (sign-in) sobre a rede | high | mitigate | Mesma trava de loopback hard-fail do T-03-09 (regex só `127.0.0.1`/`localhost` sobre `SUPABASE_INTEGRATION_URL`, roda ANTES de qualquer chamada de rede); `service_role`/senha do usuário de teste só vêm de env var, nunca hardcoded; o harness fica fora de `npm test`/CI via `testPathIgnorePatterns` já existente, só roda por invocação explícita. |
| T-04-08 | Repudiation | UAT manual (Task 2) sem log automatizado do resultado | low | accept | É a natureza de um teste manual — o resultado fica registrado no SUMMARY.md da execução e no resume-signal do checkpoint; risco aceito porque é o único nível capaz de reproduzir o sintoma real (rádio do aparelho), sem alternativa automatizável. |
</threat_model>

<verification>
- `npm run test:integration:pg` verde (Postgres local real, dois testes).
- `npx jest -q` (suíte padrão completa) sem regressão.
- `npx tsc --noEmit` sem erro novo.
- UAT modo avião aprovado pelo humano (checkpoint bloqueante).
</verification>

<success_criteria>
D-16 fechado nos três níveis: unitário (04-01), mock (04-01/04-02), Postgres real (Task 1) e UAT modo avião
(Task 2) — os 5 critérios de sucesso derivados no 04-CONTEXT.md estão todos providos com prova, não só
implementados.
</success_criteria>

<output>
Criar `.planning/phases/04-escrita-de-execu-o-de-treino-em-lote-e-offline-first/04-03-SUMMARY.md` ao final.
</output>
