---
phase: 05-integra-o-e-review-do-gr-fico-de-cardio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .planning/reviews/REVIEW.md
  - src/engine/cardioEvolucao.ts
  - src/components/progress/CardioEvolucaoChart.tsx
  - __tests__/cardioEvolucao.test.ts
  - src/screens/ProgressScreen.tsx
autonomous: true
requirements: [INT-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`git branch --show-current` imprime `main` e `git status --porcelain` bate com o baseline documentado no 05-CONTEXT.md imediatamente antes de cada um dos 3 commits desta fase — D-05."
    - "Os 3 commits desta fase usam SOMENTE `git add` nomeado por arquivo — `git log --name-only` de cada commit lista exatamente os arquivos pretendidos, nunca um add em lote do diretório inteiro — D-01, D-07."
    - "`.claude/` está listado no `.gitignore` E `git check-ignore .claude` sai 0 ANTES do commit de feature — D-02."
    - "`.planning/reviews/REVIEW.md` está rastreado pelo git (fora do `git status --porcelain` untracked) após o commit de docs, com o conteúdo idêntico ao untracked original — D-02 e specifics do 05-CONTEXT.md (commitar como está, sem editar)."
    - "`npx tsc --noEmit` sai 0, `npm test` (suíte jest completa) termina com 0 `failed` (um retry é aceitável em caso de timeout de carga, e deve ser reportado no SUMMARY), e `python3 -m pytest backend/tests -q` sai 0 — TODOS antes do commit de feature — D-04."
    - "O commit de feature lista exatamente `src/engine/cardioEvolucao.ts`, `src/components/progress/CardioEvolucaoChart.tsx`, `__tests__/cardioEvolucao.test.ts`, `src/screens/ProgressScreen.tsx` — nem mais, nem menos — D-01, D-07."
    - "Nenhum `git push` foi executado durante esta fase — `git status` continua mostrando o branch local à frente de `origin/main` (pelos commits pré-existentes mais os novos desta fase), sem nenhuma sincronização remota — D-06."
  artifacts:
    - .gitignore
    - .planning/reviews/REVIEW.md
    - src/engine/cardioEvolucao.ts
    - src/components/progress/CardioEvolucaoChart.tsx
    - __tests__/cardioEvolucao.test.ts
    - src/screens/ProgressScreen.tsx
  key_links:
    - "ProgressScreen.tsx já importa e renderiza `<CardioEvolucaoChart />` (diff de 4 linhas presente no working tree) — o commit de feature preserva essa integração exatamente como está, sem tocar no componente."
    - "`__tests__/cardioEvolucao.test.ts` (20/20 verde, medido em 2026-08-14) é o único portão automatizado do motor puro `cardioEvolucao.ts` — precisa estar incluído no total da suíte `npm test` antes do commit de feature."
---

<objective>
Fase 5, plan 1 de 2 — integra ao repositório o trabalho do gráfico de evolução de
cardio que já existe, testado e funcionando no working tree (feito fora do GSD
após o arquivamento do v1.0), com higiene de git: `.claude/` entra no
`.gitignore`, `.planning/reviews/` vira documentação commitada, e o gráfico
(motor + componente + teste + wiring na tela) é commitado com adds nomeados —
depois de uma verificação local completa (tsc + jest + pytest).

**Nota sobre tracer-first (TRACER_MODE=true):** decomposição em tracer não se
aplica a este plano — não há feature nova nem camada de aplicação a provar
ponta a ponta; o gráfico já está implementado e testado no working tree, o
trabalho aqui é 100% integração de git e verificação. As 3 tasks seguem a
ordem natural de execução: verificar → commitar higiene → commitar feature.
Nenhuma task usa `type="tracer"`.

**Ordem commit-antes-do-painel:** por decisão registrada em "Claude's
Discretion" do 05-CONTEXT.md, os commits acontecem NESTE plano (wave 1) para
que o diff fique endereçável; o painel adversarial roda no `05-02-PLAN.md`
(wave 2) sobre o `origin/main..HEAD` já incluindo estes commits.

Purpose: fechar INT-01 (REQUIREMENTS.md) — dono vê o gráfico commitado com
higiene de git e suíte completa verde.
Output: 3 commits locais (`chore`, `docs`, `feat`); nenhum push (D-06).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-CONTEXT.md
@.gitignore
@src/engine/cardioEvolucao.ts
@src/components/progress/CardioEvolucaoChart.tsx
@__tests__/cardioEvolucao.test.ts
@src/screens/ProgressScreen.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `.gitignore` (EDITADO): nova seção com a entrada `.claude/`.
- 3 commits locais em `main`: `chore` (gitignore), `docs` (.planning/reviews/),
  `feat` (gráfico de cardio) — nenhum símbolo de código novo é criado; os 4
  arquivos do gráfico já existem no working tree e só passam a ser rastreados.
- Nenhum push (D-06) — `origin/main` não muda nesta fase.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: portão de verificação local pré-commit (tsc + jest + pytest) e reconferência de branch/tree</name>
  <files>Nenhum arquivo criado ou modificado — task de verificação (lê o estado do git e roda os comandos de teste sobre a árvore atual)</files>
  <read_first>
    `.planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-CONTEXT.md` — seção `<decisions>` D-04 e
    D-05 (comandos exatos exigidos e a exigência de reconferir branch/tree antes de qualquer commit, porque
    sessões paralelas usam clones diferentes) e `<specifics>` (máquina sem toolchain nativa — verificação é
    jest/tsc/CLI apenas; `pytest` roda se o Python local tiver o pacote, senão registra como adiado para o
    CI da Fase 6).
    `package.json` — script `test` mapeia para `jest` (a config raiz já ignora `__tests__/integration/` via
    `testPathIgnorePatterns`, então `npm test` não tenta o harness de integração Postgres, fora de escopo
    per REQUIREMENTS.md).
  </read_first>
  <action>
    Rode, nesta ordem, e capture a saída literal de cada comando (vai para o SUMMARY.md, nunca resumida
    como "passou"): `git branch --show-current` (esperado: `main`); `git status --porcelain` (confirme que
    bate com o baseline do STATE.md — modificado: `src/screens/ProgressScreen.tsx`; não rastreados:
    `.claude/`, `.planning/reviews/`, `__tests__/cardioEvolucao.test.ts`,
    `src/components/progress/CardioEvolucaoChart.tsx`, `src/engine/cardioEvolucao.ts` — se houver QUALQUER
    arquivo a mais ou a menos, pare e reporte antes de prosseguir, pois indicaria outra sessão tocando este
    clone); `npx tsc --noEmit`; `npm test` (suíte jest completa — se a primeira rodada tiver falhas que
    sejam só timeout de 5000ms sob carga, rode `npm test` uma segunda vez e reporte no SUMMARY.md que houve
    retry e por quê; qualquer falha que NÃO seja timeout de carga bloqueia a fase até investigada);
    `python3 -m pytest backend/tests -q` (o Python do repo tem `pytest` instalado localmente neste clone —
    rode; só registre como "adiado para o CI da Fase 6" se o comando falhar por ausência do interpretador
    ou do pacote, não por falha de teste real).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && python3 -m pytest backend/tests -q</automated>
  </verify>
  <acceptance_criteria>
    `git branch --show-current` imprime exatamente `main`. `git status --porcelain` não lista nenhum
    arquivo além dos 6 esperados (1 modificado + 5 não rastreados, contando `.claude/` como uma entrada).
    `npx tsc --noEmit` sai com código 0 e sem linhas de erro. A saída de `npm test` contém uma linha
    `Tests:` sem a palavra `failed` associada a um número maior que zero (formato `Tests: N passed, N
    total`); se um retry foi necessário, o SUMMARY.md documenta quantos testes falharam na 1ª rodada e o
    motivo (timeout de carga). `python3 -m pytest backend/tests -q` sai com código 0, OU a saída documenta
    explicitamente por que não pôde rodar (interpretador/pacote ausente) para registro como pendência da
    Fase 6.
  </acceptance_criteria>
  <done>Branch e working tree reconferidos batendo com o baseline esperado (D-05); tsc, jest completo e
  pytest (quando disponível) verdes ANTES de qualquer commit desta fase (D-04).</done>
  <reversibility rating="reversible">Task somente de leitura/verificação — nenhum estado é alterado.</reversibility>
</task>

<task type="auto">
  <name>Task 2: commits de higiene — `.claude/` no `.gitignore` e `.planning/reviews/` como documentação</name>
  <files>.gitignore, .planning/reviews/REVIEW.md</files>
  <read_first>
    `.gitignore` INTEIRO (já em contexto) — siga o mesmo estilo de seção com cabeçalho de comentário e
    emoji usado nas seções existentes (ex.: "🔍 Testes & Coverage", "📝 Editores & IDEs") para a nova seção
    do Claude Code; note o precedente do comentário `DEP-01` explicando uma decisão de gitignore inline —
    siga o mesmo padrão de comentário breve justificando a entrada.
    `.planning/reviews/REVIEW.md` INTEIRO (já em contexto) — é um review de PR arquivado de um ciclo
    anterior; NÃO editar o conteúdo, apenas passar a rastrear o arquivo como está (specifics do
    05-CONTEXT.md).
    `05-CONTEXT.md` D-02 (locked) e D-08 (locked, formato de commit) e "Claude's Discretion" (granularidade
    sugerida: `chore` do gitignore separado do `docs` do `.planning/reviews`, separado do `feat` do
    gráfico — Task 3).
  </read_first>
  <action>
    Reconfira `git branch --show-current` (== `main`) e `git status --porcelain` imediatamente antes de
    cada `git add` desta task (D-05 — nunca presumir o estado do repo). Edite `.gitignore` adicionando uma
    nova seção ao final do arquivo, no mesmo estilo das existentes (cabeçalho com emoji + comentário breve
    dizendo que é config local de sessão do Claude Code e não deve viajar no repositório) contendo a linha
    `.claude/`. Rode `git check-ignore .claude` e confirme saída 0 antes de prosseguir. Stage nomeado:
    `git add .gitignore` (nunca um add em lote). Commit conventional, sem gerúndio, corpo curto: `chore:
    adiciona .claude/ ao gitignore`, corpo explicando que é config local de sessão (D-02). Em seguida,
    reconfira branch/tree de novo, stage nomeado `git add .planning/reviews/REVIEW.md` (sem editar o
    conteúdo do arquivo), commit: `docs: versiona review de PR arquivado em .planning/reviews`, corpo
    curto explicando que é o padrão do projeto de commitar `.planning/` como documentação.
  </action>
  <verify>
    <automated>git check-ignore .claude && git log -2 --name-only --oneline</automated>
  </verify>
  <acceptance_criteria>
    `git check-ignore .claude` sai com código 0. `git log -1 --name-only` do commit `chore` lista
    EXATAMENTE `.gitignore`. `git log -1 --name-only` do commit `docs` (o commit anterior ao `chore` na
    ordem cronológica, ou seguinte, conforme a ordem executada) lista EXATAMENTE
    `.planning/reviews/REVIEW.md`. `git diff HEAD~2 -- .planning/reviews/REVIEW.md` (contra o estado
    untracked original, comparando com uma cópia salva antes do commit se necessário) não mostra nenhuma
    alteração de conteúdo, só a entrada no índice do git. `git status --porcelain` não lista mais
    `.claude/` nem `.planning/reviews/` como untracked.
  </acceptance_criteria>
  <done>`.claude/` gitignorado e verificado antes de qualquer outro commit da fase; `.planning/reviews/`
  rastreado sem alteração de conteúdo; 2 commits atômicos, cada um com exatamente 1 arquivo nomeado.</done>
  <reversibility rating="reversible">Commits locais, não empurrados (D-06) — reversível via `git reset`/`git
  commit --amend` até a Fase 6 fazer push.</reversibility>
</task>

<task type="auto">
  <name>Task 3: commit de feature — gráfico de evolução de cardio com adds nomeados</name>
  <files>src/engine/cardioEvolucao.ts, src/components/progress/CardioEvolucaoChart.tsx, __tests__/cardioEvolucao.test.ts, src/screens/ProgressScreen.tsx</files>
  <read_first>
    `05-CONTEXT.md` `<decisions>` D-01 (locked — lista EXATA dos 4 arquivos) e D-07 (locked — escopo
    fechado, nada de refactor/dependência nova/melhoria extra além destes 4 arquivos + gitignore +
    .planning/reviews, já cobertos na Task 2).
    `src/screens/ProgressScreen.tsx` — confirme que o diff atual (import de `CardioEvolucaoChart` +
    `<CardioEvolucaoChart />` renderizado entre a seção de prescrição de cardio e "Recordes") é o único
    diff pendente no arquivo antes de commitar — nenhuma edição adicional deve ser feita aqui.
  </read_first>
  <action>
    Reconfira `git branch --show-current` (== `main`) e `git status --porcelain` imediatamente antes do
    `git add` desta task (D-05, mesmo cuidado da Task 2 — outra sessão pode ter tocado o clone entre as
    tasks). Stage nomeado, um comando por arquivo ou uma lista explícita de 4 caminhos no mesmo `git add`
    (nunca um add em lote do diretório): `src/engine/cardioEvolucao.ts`,
    `src/components/progress/CardioEvolucaoChart.tsx`, `__tests__/cardioEvolucao.test.ts`,
    `src/screens/ProgressScreen.tsx`. Não edite nenhum destes arquivos — eles já estão prontos e
    verificados (Task 1). Commit conventional, sem gerúndio, corpo curto: `feat: adiciona gráfico de
    evolução de cardio na aba Progresso`, corpo mencionando motor puro (`cardioEvolucao.ts`), componente
    (`CardioEvolucaoChart.tsx`), 20 testes novos e a integração de 4 linhas em `ProgressScreen.tsx` (D-01).
  </action>
  <verify>
    <automated>git log -1 --name-only --pretty=format:"%s%n"</automated>
  </verify>
  <acceptance_criteria>
    `git log -1 --name-only` do commit `feat` lista EXATAMENTE 4 arquivos:
    `src/engine/cardioEvolucao.ts`, `src/components/progress/CardioEvolucaoChart.tsx`,
    `__tests__/cardioEvolucao.test.ts`, `src/screens/ProgressScreen.tsx` — nenhum arquivo a mais (em
    particular, nenhum arquivo de `.claude/` ou fora desta lista). `git status --porcelain` sai vazio
    (working tree limpo) depois dos 3 commits da fase. `git log --oneline -5` mostra os 3 commits desta
    fase (chore, docs, feat) no topo, sobre o commit `docs(05): captura o contexto da fase a partir de
    decisões da sessão` já existente.
  </acceptance_criteria>
  <done>O gráfico de evolução de cardio está commitado com adds nomeados, escopo fechado aos 4 arquivos do
  D-01, working tree limpo, pronto para o painel adversarial do 05-02-PLAN.md.</done>
  <reversibility rating="reversible">Commit local, não empurrado (D-06) — reversível até a Fase 6.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Working tree local (arquivos soltos/não rastreados) → histórico de commits em `main` | Este plano decide o que atravessa essa fronteira; tudo que entra aqui ainda vai passar pelo painel adversarial (05-02) antes de qualquer push (Fase 6). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-01 | Tampering | `git add` nesta fase (risco de incluir `.claude/` ou arquivo estranho no commit) | high | mitigate | Adds SEMPRE nomeados por caminho explícito (nunca em lote); `.claude/` entra no `.gitignore` na Task 2, ANTES de qualquer commit de feature; cada commit é verificado por `git log --name-only` contra a lista exata esperada (Tasks 2 e 3). |
| T-05-02 | Information Disclosure | `.claude/settings.local.json`, `.claude/worktrees/`, `.claude/scheduled_tasks.lock` carregam metadado de sessão local (session id, PID, caminhos absolutos da máquina) que vazaria para o histórico compartilhado se commitado | medium | mitigate | `.gitignore` ganha a entrada `.claude/` como primeiro commit da fase (Task 2), verificado via `git check-ignore .claude` saindo 0 antes de qualquer outro commit. |
| T-05-03 | Repudiation | Verificação local (tsc/jest/pytest) alegada como "passou" sem evidência literal, questionável depois | low | mitigate | Task 1 exige captura literal da saída de cada comando no SUMMARY.md (nunca "passou" sem prova); acceptance_criteria ancorada em padrão de saída, não em afirmação otimista. |
| T-05-04 | Spoofing / Elevation of Privilege | N/A — nenhum código de autenticação/permissão é tocado neste plano | low | accept | Escopo fechado (D-07) é motor de gráfico + componente + teste + wiring de UI + hygiene de git; nenhuma superfície de auth/RLS entra nestes 3 commits. |
</threat_model>

<verification>
- `npx tsc --noEmit` sai 0 (Task 1, reconfirmado antes do commit de feature).
- `npm test` (suíte completa) sem `failed` (Task 1).
- `python3 -m pytest backend/tests -q` sai 0 (Task 1, disponível localmente neste clone).
- `git status --porcelain` vazio ao final da Task 3.
- `git log --oneline -5` mostra os 3 commits novos (chore, docs, feat) sem nenhum push.
</verification>

<success_criteria>
Gráfico de evolução de cardio commitado no repositório local com higiene de git completa (D-01, D-02,
D-07, D-08), verificação local (tsc/jest/pytest) provada ANTES do commit de feature (D-04), branch/tree
reconferidos antes de cada commit (D-05), nenhum push executado (D-06) — pronto para o painel adversarial
do 05-02-PLAN.md avaliar o `origin/main..HEAD` completo.
</success_criteria>

<output>
Criar `.planning/phases/05-integra-o-e-review-do-gr-fico-de-cardio/05-01-SUMMARY.md` ao final.
</output>
