---
phase: 15-tela-bloqueada-ver-e-cronometrar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - modules/live-activity/expo-module.config.json
  - modules/live-activity/package.json
  - modules/live-activity/LiveActivityModule.podspec
  - modules/live-activity/index.ts
  - modules/live-activity/ios/LiveActivityModule.swift
  - modules/live-activity/ios/SessionActivityAttributes.swift
  - targets/session-widget/SessionActivityAttributes.swift
  - targets/session-widget/WidgetLiveActivity.swift
  - scripts/verify-native-skeleton.sh
  - src/engine/sessionModel.ts
  - src/engine/sessionSummary.ts
  - src/engine/liveActivityContentState.ts
  - src/store/activeSessionStore.ts
  - src/components/session/SessionPlayer.tsx
  - src/native/liveActivitySync.ts
  - App.tsx
  - __tests__/activeSessionStore.test.ts
  - __tests__/sessionSummary.test.ts
  - __tests__/liveActivityContentState.test.ts
  - __tests__/liveActivitySync.test.ts
autonomous: true
requirements: [LOCK-01, LOCK-02]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Ao iniciar uma sessao de treino (status vira 'active' no store), a Live Activity sobe sozinha mostrando o exercicio atual e a serie X/Y, sem acao extra do dono — D-05"
    - "O timer de descanso e renderizado nativamente via Text(timerInterval:) a partir de restEndsAt — nenhum Activity.update() e disparado a cada segundo, so em mudanca de estado real"
    - "Ao restEndsAt chegar a zero, o descanso NUNCA avanca sozinho para a proxima serie — nem no app (SessionPlayer.tsx) nem por qualquer efeito automatico — D-09/D-10, reversibilidade costly"
    - "Quando restEndsAt e null (nenhum descanso em curso), a funcao que deriva o ContentState nunca tenta montar um Text(timerInterval:) — cai para a fase 'measuring'"
    - "restEndsAt e sempre serializado como ISO-8601 UTC (.toISOString(), sufixo Z) tanto no store quanto na ponte para o modulo nativo; o Swift decodifica com ISO8601DateFormatter sem depender do fuso local do aparelho — app e widget leem o mesmo relogio do sistema, sem risco de deriva entre processos"
    - statement: "O primeiro frame do ContentState (no instante de Activity.request) ja carrega exercicio/serie reais — nao existe estado 'carregando' visivel no card, porque o valor inicial vem do draft ja populado, nao de um round-trip de rede"
      verification: backstop
  artifacts:
    - path: "src/native/liveActivitySync.ts"
      provides: "Unico escritor JS->ActivityKit: assina activeSessionStore.subscribe, chama startActivity/updateActivity do modulo nativo"
    - path: "src/engine/liveActivityContentState.ts"
      provides: "Funcao pura buildLiveActivityContentState(draft) — deriva phase/exerciseName/setIndex/setTotal/restEndsAt do SessionDraft"
    - path: "modules/live-activity/index.ts"
      provides: "Bridge Expo Modules: startActivity/updateActivity/endActivity/isActivityRunning/reconcileOrphans"
    - path: "targets/session-widget/WidgetLiveActivity.swift"
      provides: "ActivityConfiguration real (substitui o scaffold Hello-emoji) com as 4 apresentacoes do Dynamic Island"
  key_links:
    - from: "src/store/activeSessionStore.ts (completeSet)"
      to: "SessionDraft.restEndsAt"
      via: "completeSet calcula restEndsAt = now + exercise.restSeconds quando ha proxima serie pendente"
      pattern: "restEndsAt"
    - from: "src/native/liveActivitySync.ts"
      to: "modules/live-activity/index.ts"
      via: "useActiveSessionStore.subscribe((state, prev) => ...) chama startActivity/updateActivity"
      pattern: "subscribe\\("
    - from: "App.tsx"
      to: "src/native/liveActivitySync.ts"
      via: "host montado no root, side-effect no mount, independente de navegacao"
      pattern: "liveActivitySync"
  prohibitions:
    - statement: "Nenhum caminho de codigo reintroduz avanco automatico de serie ao restEndsAt zerar — a regra D-09 vale igualmente com o app em foreground e com a tela bloqueada (D-10)"
      status: flagged-unverified
    - statement: "ContentState/ActivityAttributes nunca carrega mais dado do que o necessario para renderizar o card atual — nunca token de autenticacao, nunca o plano completo da semana, nunca dado de outro usuario"
      status: flagged-unverified
---

<objective>
Fundacao nativa da Live Activity: o modulo Expo local `modules/live-activity/`
(molde `modules/native-info/`), o card real em `targets/session-widget/`
(substitui o scaffold "Hello emoji" do `@bacons/apple-targets`), e o refactor
que tira o timer de descanso de `SessionPlayer.tsx` para um timestamp absoluto
`restEndsAt` no `activeSessionStore` — a mudanca de comportamento mais
invasiva da fase (D-09/D-10: o descanso deixa de auto-avancar, no app inteiro,
nao so na tela bloqueada).

Esta e a fatia tracer da fase (TRACER_MODE): ao final desta plano, travar o
iPhone durante uma sessao mostra o exercicio atual, a serie X/Y e o descanso
contando regressivamente de verdade, sem depender do app acordado. As 4
apresentacoes completas do Dynamic Island por estado (D-01/D-02), a reducao
para cardio/alongamento (D-03) e o estado "Pronto" com tempo excedido (D-04)
sao a Plano 15-02 (expansao sobre este mesmo ContentState/widget). O ciclo de
vida de encerramento (D-06/D-08/D-11/D-12) e a Plano 15-03.

Nenhum pacote npm novo entra nesta fase — `@bacons/apple-targets` ja esta
instalado e auditado (RESEARCH.md Package Legitimacy Audit, verdict OK,
Fase 14). `modules/live-activity/` e codigo local (Swift + expo-module.config.json),
nao um pacote de registro.

Nota de estimativa: esta task e um tracer monolitico de 20 arquivos (~115k
tokens, 1.15x o smart-zone, confidence low) por decisao deliberada — o
contrato `SessionActivityAttributes`/`ContentState` e o refactor de
`restEndsAt` precisam pousar juntos para nao deixar um estado
meio-migrado (SessionPlayer.tsx lendo `restRemaining` local enquanto o
store ja tem `restEndsAt`, ou vice-versa). Nao dividir a task; revisar o
progresso de execucao no meio se surgir pressao de contexto.

Purpose: LOCK-02 exige que o timer de descanso saia de `SessionPlayer.tsx` e
vire `restEndsAt` no `activeSessionStore` — fundacao compartilhada que a
Fase 16 (CMD, App Intents) e a Fase 17 (REG/PRED) tambem vao consumir. LOCK-01
exige exercicio/serie/prescricao visiveis sem desbloquear o iPhone; esta plano
entrega a via minima real (measuring/resting), a Plano 15-02 completa
blockOnly/readyOvertime.
Output: `modules/live-activity/` funcional, `targets/session-widget/WidgetLiveActivity.swift`
renderizando dado real para measuring/resting, `restEndsAt` como fonte de
verdade do descanso no store e no app, `liveActivitySync.ts` como unico
escritor nativo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-CONTEXT.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-RESEARCH.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-PATTERNS.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-UI-SPEC.md
@modules/native-info/index.ts
@modules/native-info/ios/NativeInfoModule.swift
@modules/native-info/expo-module.config.json
@modules/native-info/NativeInfoModule.podspec
@modules/native-info/package.json
@targets/session-widget/WidgetLiveActivity.swift
@src/store/activeSessionStore.ts
@src/components/session/SessionPlayer.tsx
@src/engine/sessionModel.ts
@src/engine/sessionSummary.ts
@scripts/verify-native-skeleton.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Live Activity real de ponta a ponta — sobe com a sessao, timer nativo via restEndsAt, nunca auto-avanca</name>
  <precondition>scripts/verify-native-skeleton.sh existe e sai com exit 0 no HEAD atual (artefato da Fase 14) antes desta task modifica-lo</precondition>
  <reversibility rating="costly">SessionActivityAttributes/ContentState vira o contrato Swift&lt;-&gt;JS que as Fases 16/17 (App Intents, REG/PRED) tambem vao consumir; mudar nomes/tipos de campo depois exige reabrir os dois arquivos duplicados (modulo + target) + o tipo TS + liveActivitySync.ts em coordenacao — nenhum sistema externo ainda le este contrato (nada publicado, sem migracao de dado), entao e uma mudanca chata e multi-arquivo, nao um caminho sem volta. A decisao de formato (campos numericos, nao string pre-formatada) ja e travada por CONTEXT.md ("Claude's Discretion" com restricao registrada para REG-02).</reversibility>
  <files>app.json, modules/live-activity/expo-module.config.json, modules/live-activity/package.json, modules/live-activity/LiveActivityModule.podspec, modules/live-activity/index.ts, modules/live-activity/ios/LiveActivityModule.swift, modules/live-activity/ios/SessionActivityAttributes.swift, targets/session-widget/SessionActivityAttributes.swift, targets/session-widget/WidgetLiveActivity.swift, scripts/verify-native-skeleton.sh, src/engine/sessionModel.ts, src/engine/sessionSummary.ts, src/engine/liveActivityContentState.ts, src/store/activeSessionStore.ts, src/components/session/SessionPlayer.tsx, src/native/liveActivitySync.ts, App.tsx, __tests__/activeSessionStore.test.ts, __tests__/sessionSummary.test.ts, __tests__/liveActivityContentState.test.ts, __tests__/liveActivitySync.test.ts</files>
  <read_first>
    src/components/session/SessionPlayer.tsx (linhas 1-306: findActiveSet/findNextPendingSet, RestState, o useEffect de auto-avanco em restRemaining===0 a REMOVER, ajustarRest, endRest);
    src/store/activeSessionStore.ts (linhas 122-216: shape do ActiveSessionState/Status; linhas 1080-1193: activateSet; linhas 1193-1430: completeSet completo, incluindo o calculo de `proxima`/`fechouExercicio`; linhas 1743-1786: finishSession, para nao colidir com o campo restEndsAt);
    src/engine/sessionModel.ts (linhas 60-149: DraftSet/DraftExercise/SessionDraft; linha 277-283: isTimeBased/metricOf);
    src/engine/sessionSummary.ts (linhas 60-72: ajustarDescanso, a funcao-irma a criar em timestamp);
    modules/native-info/index.ts, modules/native-info/ios/NativeInfoModule.swift, modules/native-info/expo-module.config.json, modules/native-info/NativeInfoModule.podspec, modules/native-info/package.json (molde EXATO do modulo Expo local a copiar);
    targets/session-widget/WidgetLiveActivity.swift (scaffold atual: WidgetAttributes/ContentState com `emoji`, ActivityConfiguration com Color.cyan/Color.black, DynamicIsland com as 4 closures ja estruturadas — compactLeading/compactTrailing/minimal + 3 DynamicIslandExpandedRegion, .widgetURL/.keylineTint(Color.red) a substituir);
    scripts/verify-native-skeleton.sh (linha 90: grep de autolinking; linha 111: for modulo_local in NativeInfoModule — loop a estender);
    App.tsx (linhas 34-46: ordem de montagem dos hosts no root — ProvisioningBanner/PushInviteHost como molde de "componente sem UI, side-effect no mount");
    .planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-UI-SPEC.md (Typography/Color: Display=.title2 bold monospacedDigit, Label=.subheadline regular, cores #0A0A0A/#EBFF00/#FFFFFF/#8B9098; secao "Live Activity chrome" com os 4 placeholders do scaffold e seus substitutos exatos)
  </read_first>
  <action>
    PARTE A — modulo nativo `modules/live-activity/` (copia estrutural de
    `modules/native-info/`, so os identificadores mudam):
    `expo-module.config.json` com `podspecPath: "LiveActivityModule.podspec"` e
    `modules: ["LiveActivityModule"]`. `package.json` com `name: "live-activity"`,
    descricao citando Fase 15/LOCK-01/02/03, `peerDependencies.expo: "*"`.
    `LiveActivityModule.podspec` identico ao molde (mesmo `platforms ios 15.1`,
    `swift_version 5.4`, `static_framework true`, `dependency 'ExpoModulesCore'`,
    mesmo `pod_target_xcconfig`), so `s.name = 'LiveActivityModule'`.

    `modules/live-activity/ios/SessionActivityAttributes.swift` (novo,
    `import ActivityKit`): `public struct SessionActivityAttributes: ActivityAttributes`
    com propriedade fixa `var sessionLogId: String`; `public struct ContentState:
    Codable, Hashable` com `var phase: Phase` (um `enum Phase: String, Codable,
    Hashable { case measuring, resting, readyOvertime, blockOnly }` declarado no
    mesmo arquivo), `var exerciseName: String`, `var setIndex: Int`,
    `var setTotal: Int`, `var targetRepsMin: Int?`, `var targetRepsMax: Int?`,
    `var targetLoadKg: Double?`, `var isBodyweight: Bool`, `var restEndsAt: Date?`,
    `var blockLabel: String?`, `var blockIndex: Int?`, `var blockTotal: Int?`.
    Copiar este arquivo BYTE A BYTE para `targets/session-widget/SessionActivityAttributes.swift`
    (Pitfall 4 do RESEARCH.md: sao dois targets de compilacao separados, sem
    pacote Swift compartilhado — a duplicacao e estrutural, nao um descuido; sem
    script de sync dedicado nesta fase, Open Question 3 do RESEARCH.md resolvida
    a favor de manter os dois arquivos escritos/revisados juntos nesta mesma
    task).

    `modules/live-activity/ios/LiveActivityModule.swift` (`import ExpoModulesCore`,
    `import ActivityKit`): declarar `public struct LiveActivityContentStateRecord:
    Record` (protocolo `Record` do ExpoModulesCore, mesmo padrao usado em outros
    modulos Expo para receber objetos JS tipados — consultar Context7/docs do
    Expo Modules API se a sintaxe exata de `@Field` divergir) com um `@Field`
    por campo do ContentState acima (nomes IDENTICOS aos da TS
    LiveActivityContentState da PARTE C — a ponte Expo casa por nome de
    propriedade, sem tradutor). Manter uma referencia de modulo
    `private var currentActivity: Activity<SessionActivityAttributes>?`. Cinco
    `AsyncFunction`: `startActivity(record: LiveActivityContentStateRecord,
    sessionLogId: String) -> Bool` (monta `SessionActivityAttributes.ContentState`
    a partir do record — parseia `restEndsAt: String?` ISO-8601 via
    `ISO8601DateFormatter` para `Date?` — chama `Activity<SessionActivityAttributes>.request(attributes:content:)`,
    guarda em `currentActivity`, `do/catch` retornando `false` em erro, nunca
    lancando atraves da ponte — mesmo padrao de `modules/native-info` que nunca
    lanca); `updateActivity(record: LiveActivityContentStateRecord) -> Bool`
    (chama `currentActivity?.update(content:)`, `false` se nao havia activity);
    `endActivity(dismissalPolicy: String, afterSeconds: Int?) -> Bool` ("immediate"
    -> `.immediate`, "afterDate" -> `.after(Date().addingTimeInterval(TimeInterval(afterSeconds ?? 0)))`,
    chama `currentActivity?.end(dismissalPolicy:)`, zera `currentActivity`);
    `isActivityRunning() -> Bool` (`currentActivity != nil`);
    `reconcileOrphans(stillActiveSessionLogId: String?) -> Bool` (itera
    `Activity<SessionActivityAttributes>.activities`, chama `.end(dismissalPolicy: .immediate)`
    em cada uma — "um caminho so", D-11 — zera `currentActivity`, retorna
    `stillActiveSessionLogId != nil` como sinal para o JS decidir se chama
    `startActivity` de novo). Nenhuma destas 3 ultimas funcoes e chamada pelo
    lado JS nesta task (ficam definidas e testadas isoladamente no Swift, sem
    chamador ainda — nao e stub, e codigo real nao invocado, preenchido pela
    Plano 15-03 sem reabrir este arquivo para mudar assinatura).

    `modules/live-activity/index.ts`: `requireNativeModule<LiveActivityModuleType>('LiveActivityModule')`
    igual ao molde de `native-info/index.ts`; exporta `startLiveActivity`,
    `updateLiveActivity`, `endLiveActivity`, `isLiveActivityRunning`,
    `reconcileLiveActivityOrphans` como wrappers async finos.

    Adicionar `ios.infoPlist.NSSupportsLiveActivities: true` em `app.json`
    (Pitfall 1 do RESEARCH.md: a chave e exigida no Info.plist do APP
    PRINCIPAL, nao so da extensao — `app.json` hoje so declara
    `ios.entitlements`; adicionar a chave `infoPlist` como irma de
    `entitlements` dentro do bloco `ios`).

    PARTE B — `scripts/verify-native-skeleton.sh`: linha 90, estender o grep de
    autolinking para tambem confirmar `live-activity` (alem de `native-info`) na
    saida de `npx expo-modules-autolinking search --platform ios`. Linha 111,
    mudar `for modulo_local in NativeInfoModule; do` para
    `for modulo_local in NativeInfoModule LiveActivityModule; do` (Pitfall 5 do
    RESEARCH.md — prova compilacao real em `ios/Podfile.lock`, nao so
    descoberta).

    PARTE C — `src/engine/sessionModel.ts`: mover `findActiveSet`/`findNextPendingSet`
    de `SessionPlayer.tsx` (linhas 104-121) para ca como funcoes puras exportadas
    (mesma assinatura, mesmo `type SetRef = { exercise: DraftExercise; set:
    DraftSet }` — tambem movido/exportado); acrescentar `restEndsAt: string | null`
    ao tipo `SessionDraft` (linha ~138, ao lado do campo `status` existente,
    mesma convencao de campo plano). Atualizar o import em `SessionPlayer.tsx`
    de `findActiveSet`/`findNextPendingSet` para vir de `../../engine/sessionModel`
    em vez de serem definidas localmente.

    `src/engine/sessionSummary.ts`: nova funcao irma de `ajustarDescanso`
    (linha 65-72) — `ajustarRestEndsAt(restEndsAt: string, deltaSeconds: number,
    now: Date = new Date()): string` — calcula `restanteMs =
    new Date(restEndsAt).getTime() - now.getTime()`, aplica o mesmo piso de
    `ajustarDescanso` traduzido para ms (`Math.max(1000, restanteMs +
    deltaSeconds * 1000)`), devolve `new Date(now.getTime() + novoRestanteMs).toISOString()`
    — nunca reescrever `ajustarDescanso` em si (RESEARCH.md Pattern 2: "traducao
    direta, nao reescrita").

    `src/engine/liveActivityContentState.ts` (novo): exporta `type
    LiveActivityContentState` com os mesmos 12 campos do Swift ContentState
    (fase como uniao de string `'measuring' | 'resting' | 'readyOvertime' |
    'blockOnly'`) e a funcao pura `buildLiveActivityContentState(draft:
    SessionDraft, now: Date = new Date()): LiveActivityContentState | null`.
    Logica desta task (measuring/resting apenas — blockOnly/readyOvertime
    completos na Plano 15-02, mas a UNIAO de tipo ja inclui os 4 valores para o
    Swift enum bater desde ja): usa `findActiveSet(draft)` e
    `findNextPendingSet(draft)`; se `draft.restEndsAt` esta setado e ainda no
    futuro (`new Date(draft.restEndsAt).getTime() > now.getTime()`) -> fase
    `resting`, `exerciseName`/`setIndex`/`setTotal` do exercicio associado ao
    proximo set (`findNextPendingSet`), `restEndsAt` propagado; senao se `active`
    existe -> fase `measuring` com os campos do `active.exercise`/`active.set`
    (`targetRepsMin`/`targetRepsMax`/`targetLoadKg`/`isBodyweight`),
    `restEndsAt: null`; senao (nenhum active nem rest) -> retorna `null` (sem
    activity a montar). `blockLabel`/`blockIndex`/`blockTotal` sempre `null`
    nesta task.

    PARTE D — `src/store/activeSessionStore.ts`: em `completeSet` (apos marcar a
    serie `done` e calcular `proxima = findNextPendingSet`/exercicio concluido —
    reaproveitar a MESMA logica que `SessionPlayer.onConcluir` fazia), calcular
    `restEndsAt: proxima && exercise.restSeconds ? new Date(Date.now() +
    exercise.restSeconds * 1000).toISOString() : null` e incluir no novo draft
    (`set({ draft: { ...novo, restEndsAt } })`, dentro do MESMO guard de CAS que
    `completeSet` ja usa — capturar `sid` antes do await, reconferir depois,
    igual ao padrao de `finishSession`). Em `activateSet` (linha ~1080-1093):
    ao transicionar o set para `active`, tambem zerar `restEndsAt: null` no
    draft resultante — e o unico ponto que precisa saber "descanso acabou de
    virar serie ativa", cobrindo tanto o avanco explicito (Pular descanso) quanto
    qualquer futura chamada. Nova acao `adjustRest: (deltaSeconds: number) =>
    void` na interface `ActiveSessionState` e na implementacao: le
    `get().draft`, se `draft.restEndsAt` for `null` nao faz nada, senao chama
    `ajustarRestEndsAt(draft.restEndsAt, deltaSeconds)` e `set({ draft: {
    ...draft, restEndsAt: novo } })`.

    PARTE E — `src/components/session/SessionPlayer.tsx`: remover os states
    locais `restRemaining`/`restTotal` e o `restTick` `setInterval` que os
    DECREMENTAVA de forma autoritativa (linhas 161-165, 268-279). Manter um
    `setInterval` cosmetico de 1s SOMENTE para forcar re-render enquanto
    `draft.restEndsAt` nao for `null` (nao decrementa nada, so dispara um tick
    de estado local tipo `useState<number>` incrementado, ou re-le
    `Date.now()`). Derivar `restRemainingDisplay = draft.restEndsAt ?
    Math.round((new Date(draft.restEndsAt).getTime() - Date.now()) / 1000) :
    0` a cada render/tick — permitir valor NEGATIVO (overtime) em vez de
    clampar em 0. Manter um `restTotalSecondsRef`/state local so para o
    denominador do anel (cosmetico): iniciar a partir de `exercise.restSeconds`
    quando `rest` comeca, e ajustar com o MESMO `Math.max` de
    `ajustarRestEndsAt` sempre que os botoes +30s/-30s forem tocados, para o
    anel nunca ultrapassar 100%. `ajustarRest(delta)` (linha 282-286) passa a
    chamar a nova acao do store `adjustRest(delta)` (via
    `useActiveSessionStore((s) => s.adjustRest)`) em vez dos setters de estado
    local antigos. REMOVER POR COMPLETO o `useEffect` das linhas 297-300, cujo
    unico corpo hoje chama `endRest(true)` automaticamente na transicao para
    zero segundos restantes, sem nenhuma acao do dono — D-09/D-10: chegar a
    zero NUNCA aciona `endRest(true)` sozinho. O botao "Pular descanso" (linha
    444-451) continua chamando `endRest(true)` — isso e ACAO EXPLICITA do dono,
    permitida por D-09, e `endRest`/`activateSet` ja zeram `restEndsAt` (Parte
    D). Quando `restRemainingDisplay < 0` (overtime), o card de descanso (bloco
    `if (rest && !active)`, linha 338+) continua renderizando o MESMO layout,
    trocando so o texto do relogio para mostrar o tempo excedido com prefixo
    "+" (reaproveitar `formatarTempo` sobre o valor absoluto) — D-04/D-10:
    mesma semantica visual no app e na tela bloqueada, sem inventar uma tela
    nova.

    PARTE F — `src/native/liveActivitySync.ts` (novo, unico escritor JS->ActivityKit,
    RESEARCH.md Architecture): `useActiveSessionStore.subscribe((state, prev) =>
    {...})` (Zustand v4 vanilla, sem middleware — a assinatura `(state,
    prevState)` ja e o listener padrao de `create()`). Dentro do listener: se
    `state.status === 'active' && prev.status !== 'active'` -> montar
    `buildLiveActivityContentState(state.draft, new Date())`, se nao-nulo,
    chamar `startLiveActivity(contentState, state.draft.sessionLogId)`; senao,
    se `state.status === 'active'` e o `draft` mudou de forma relevante
    (`state.draft !== prev.draft` — comparacao de referencia basta, o store
    sempre cria objeto novo por imutabilidade) -> montar o novo ContentState e
    chamar `updateLiveActivity(contentState)`. Exportar uma funcao
    `initLiveActivitySync(): () => void` que registra o `subscribe` e devolve o
    unsubscribe, para `App.tsx` chamar uma vez no mount. O listener reage SO a
    transicoes de `status`/`draft` do store — nunca a eventos de ciclo de vida
    do app (`AppState`, `visibilitychange`) — entao alternar foreground/background
    nunca dispara um `startLiveActivity`/`endLiveActivity` extra: com o app em
    foreground a Activity continua viva, um unico ciclo de vida por sessao
    (start -> update -> end), satisfazendo D-07 por construcao (nao ha
    listener de `AppState` neste arquivo para violar).

    `App.tsx`: importar `initLiveActivitySync` de `./src/native/liveActivitySync`
    e chamar dentro de um `useEffect(() => initLiveActivitySync(), [])` no
    componente `App` (mesmo espirito de montagem de `ProvisioningBanner`/`PushInviteHost`
    — side-effect no root, independente de navegacao).

    Testes: estender `__tests__/activeSessionStore.test.ts` com casos para
    `completeSet` setando `restEndsAt` corretamente (com e sem proxima serie
    pendente, com e sem `exercise.restSeconds`), `activateSet` zerando
    `restEndsAt`, e a nova acao `adjustRest`. Criar `__tests__/sessionSummary.test.ts`
    (nao existe hoje) cobrindo `ajustarRestEndsAt` (mesmo padrao AAA de
    `__tests__/sessionFlow.test.ts`: piso de 1s, +30s aumenta o timestamp,
    -30s nunca deixa `restEndsAt` no passado). Criar
    `__tests__/liveActivityContentState.test.ts` cobrindo
    `buildLiveActivityContentState`: retorna `null` sem active/rest, fase
    `resting` com `restEndsAt` propagado quando ha descanso em curso, fase
    `measuring` quando ha active e `restEndsAt` e `null`, nunca monta
    `Text(timerInterval:)`-shaped output (isto e, nunca retorna
    `restEndsAt` nao-null na fase `measuring`). Criar
    `__tests__/liveActivitySync.test.ts` mockando `modules/live-activity`
    (`jest.mock('../modules/live-activity', () => ({ startLiveActivity:
    jest.fn(), updateLiveActivity: jest.fn(), ... }))`, mesmo padrao de
    `__tests__/ProvisioningBanner.test.tsx`) — assert que `startLiveActivity` e
    chamado exatamente uma vez ao status transicionar para `active`, e que
    `updateLiveActivity` e chamado quando o draft muda com status ja `active`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx jest __tests__/activeSessionStore.test.ts __tests__/sessionSummary.test.ts __tests__/liveActivityContentState.test.ts __tests__/liveActivitySync.test.ts __tests__/sessionFlow.test.ts &amp;&amp; npm run verify:native</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc --noEmit` sai 0. As 5 suites de teste listadas passam 100%, incluindo
    pelo menos um caso que prova que o descanso zerar sozinho NUNCA aciona a
    proxima serie sem uma chamada explicita de `adjustRest`/"Pular descanso"
    partindo do teste (nenhum teste observa `activateSet` sendo chamado so pela
    passagem simulada do tempo). `npm run verify:native` sai 0, confirmando
    (a)-(f) incluindo o `LiveActivityModule` linkado em `ios/Podfile.lock`.
    `grep -c "adjustRest" src/components/session/SessionPlayer.tsx` e
    `grep -c "restEndsAt" src/components/session/SessionPlayer.tsx` retornam
    ambos >= 1 (a nova acao do store e o novo campo estao de fato lidos pelo
    componente). Leitura manual de `src/components/session/SessionPlayer.tsx`
    confirma que nenhum `useEffect` chama a funcao de avanco de serie a partir
    so de um valor de tempo derivado chegar a zero — toda chamada de avanco
    parte de um `onPress`/acao do dono.
  </acceptance_criteria>
  <done>Sessao ativa sobe a Live Activity com exercicio+serie reais; restEndsAt dirige o timer nativo e o do app; descanso zerar nunca avanca a proxima serie sem toque do dono; suite de testes e verify:native verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App principal (processo RN) -> Widget Extension (processo separado) | `Activity<SessionActivityAttributes>.update(content:)` cruza para um processo com sandbox proprio, fora do controle de runtime do app principal |
| Tela bloqueada / Dynamic Island | Superficie de UI do sistema, visivel a qualquer pessoa que veja o aparelho fisicamente bloqueado — nao so o dono |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01-01 | Information Disclosure | ContentState/ActivityAttributes cruzando para o processo da extensao | medium | mitigate | `LiveActivityContentState`/`SessionActivityAttributes.ContentState` carregam so campos de exibicao minimos (nome, serie, prescricao, timestamp) — nunca token de auth, plano completo ou dado de outro usuario (RESEARCH.md Security Domain, ja o desenho desta task) |
| T-15-01-02 | Tampering | Entitlement `aps-environment` vazando para `app.json`/`expo-target.config.js`/modulo novo | high | mitigate | `scripts/verify-native-skeleton.sh` checagem (c), ja existente da Fase 14, reconfirmada nesta task (roda 2x sem alteracao entre rodadas) — nenhuma mudanca de codigo necessaria, so reverificacao |
| T-15-01-03 | Information Disclosure | Exercicio/serie visiveis no Lock Screen a qualquer pessoa que olhe o aparelho | low | accept | Intencao explicita do produto (LOCK-01) — app pessoal single-user, sem dado financeiro/PII alem do proprio treino, mesmo padrao de apps de musica mostrando faixa tocando na tela bloqueada |
</threat_model>

## Artifacts this phase produces

- `modules/live-activity/expo-module.config.json`, `package.json`,
  `LiveActivityModule.podspec`, `index.ts` — bridge Expo Modules local
- `modules/live-activity/ios/LiveActivityModule.swift` — `Module` Expo com
  `startActivity`/`updateActivity`/`endActivity`/`isActivityRunning`/`reconcileOrphans`
- `modules/live-activity/ios/SessionActivityAttributes.swift` e sua copia
  `targets/session-widget/SessionActivityAttributes.swift` — `ActivityAttributes`/`ContentState`
  compartilhado por contrato (nao por pacote)
- `targets/session-widget/WidgetLiveActivity.swift` — `ActivityConfiguration`
  real com as 4 apresentacoes do Dynamic Island (measuring/resting nesta
  plano; blockOnly/readyOvertime completos na 15-02)
- `src/engine/liveActivityContentState.ts` — `LiveActivityContentState`,
  `buildLiveActivityContentState`
- `src/native/liveActivitySync.ts` — `initLiveActivitySync`
- `src/engine/sessionSummary.ts` — `ajustarRestEndsAt` (nova, ao lado de
  `ajustarDescanso`)
- `src/engine/sessionModel.ts` — `findActiveSet`, `findNextPendingSet`,
  `SetRef` (movidos de `SessionPlayer.tsx`), `SessionDraft.restEndsAt`
- `src/store/activeSessionStore.ts` — `adjustRest`, `restEndsAt` computado em
  `completeSet`/limpo em `activateSet`
- Funcoes de teste: `__tests__/sessionSummary.test.ts`,
  `__tests__/liveActivityContentState.test.ts`, `__tests__/liveActivitySync.test.ts`
  (todos novos)

<verification>
`npx tsc --noEmit` limpo; as 5 suites Jest citadas passam; `npm run verify:native`
sai 0 (esqueleto nativo, incluindo `LiveActivityModule`, sobrevive a 2 rodadas
de `expo prebuild --clean`). Comportamento real em Lock Screen/Dynamic Island
NAO e verificavel aqui — depende da Plano 15-05 (Sessao 1 fisica, D-13).
</verification>

<success_criteria>
Sessao de treino iniciada -> Live Activity sobe automaticamente com
exercicio/serie reais. Descanso conta via `restEndsAt`/`Text(timerInterval:)`
nativo. Zerar o descanso jamais avanca sozinho para a proxima serie, em
nenhum dos dois lugares (app ou tela bloqueada). Toda a bateria de testes e o
esqueleto nativo passam localmente, sem depender de aparelho fisico.
</success_criteria>

<output>
Create `.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-01-SUMMARY.md` when done
</output>
