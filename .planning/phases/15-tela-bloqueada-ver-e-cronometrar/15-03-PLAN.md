---
phase: 15-tela-bloqueada-ver-e-cronometrar
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/native/liveActivitySync.ts
  - src/components/LiveActivityUnavailableBanner.tsx
  - App.tsx
  - __tests__/liveActivitySync.test.ts
  - __tests__/LiveActivityUnavailableBanner.test.tsx
autonomous: true
requirements: [LOCK-03]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Ao concluir o treino (finishSession, draft permanece nao-nulo com status 'finished'), o card vira resumo curto e desaparece sozinho em 2-5 min (dismissalPolicy .after, nao .default) — D-06 caminho 'terminar'"
    - "Ao cancelar a sessao (skipWholeSession, draft vira null no MESMO set() que status vira 'finished' — sem frame intermediario com draft nao-nulo), a Live Activity encerra imediatamente (dismissalPolicy .immediate) — D-06 caminho 'cancelar'. PREMISSA DO PLANNER, verificada ao vivo em 2026-08-16 contra src/store/activeSessionStore.ts:1698-1728 (detalhe na secao 'Premissas do planner' no corpo deste plano): skipWholeSession ja cobre uma sessao em andamento, so nao tinha um consumidor de Live Activity — nenhuma UI nova e criada aqui. Se o executor encontrar o store divergindo dessa leitura, PARE e reporte em vez de improvisar um cancelSession()"
    - "Sem nenhuma serie registrada por 3h (D-08, timeout de inatividade), a Live Activity se encerra sozinha; a sessao no store permanece intacta para retomada — so o card sai da tela bloqueada"
    - "Ao reabrir o app apos force-quit durante uma sessao ativa: encerra TODA Live Activity existente e, se a sessao ainda estiver viva no store, sobe um card novo ja com o estado corrente — um caminho so (D-11), sem tentar decidir se o card orfao ainda servia"
    - "Se a Live Activity nao conseguir subir (Live Activities desativada em Ajustes, recusa do iOS, limite do sistema), o app mostra um aviso discreto UMA UNICA VEZ, sem bloquear o treino — nunca silencio total, nunca aviso repetido a cada tentativa (D-12)"
    - statement: "Se endActivity()/reconcileOrphans() falhar do lado nativo (erro, activity ja nao existe), o app nao trava nem propaga excecao — o card orfao remanescente e limpo na proxima reconciliacao de boot (D-11) como rede de seguranca"
      verification: backstop
  artifacts:
    - path: "src/components/LiveActivityUnavailableBanner.tsx"
      provides: "Banner discreto D-12, molde ProvisioningBanner.tsx, montado uma vez no root"
  key_links:
    - from: "src/native/liveActivitySync.ts"
      to: "modules/live-activity (endActivity, reconcileOrphans, isActivityRunning)"
      via: "chamadas que a Plano 15-01 ja definiu no Swift mas nunca invocou do JS"
      pattern: "endLiveActivity|reconcileLiveActivityOrphans"
    - from: "App.tsx"
      to: "src/native/liveActivitySync.ts (reconciliation on boot)"
      via: "chamado no MESMO useEffect de mount que ja registra o subscribe (15-01), antes de qualquer navegacao"
      pattern: "reconcile"
  prohibitions:
    - statement: "A Live Activity nunca fica presa mostrando treino velho — todo caminho de termino (concluir, cancelar, force-quit) resulta em encerramento, nunca em card indefinidamente vivo"
      status: flagged-unverified
---

<objective>
Fecha o ciclo de vida da Live Activity (LOCK-03): dismissal correto para
terminar vs cancelar (D-06), reconciliacao de card orfao no boot apos
force-quit (D-11, criterio de sucesso 4 do ROADMAP), timeout de inatividade
(D-08) e o aviso discreto quando a Activity nao consegue subir (D-12).

So toca `src/native/liveActivitySync.ts` (estendendo o que a 15-01 criou),
`App.tsx` (montando a reconciliacao + o banner) e um componente novo — nao
reabre nenhum arquivo Swift: os 5 metodos nativos (`startActivity`/`updateActivity`/`endActivity`/`isActivityRunning`/`reconcileOrphans`)
ja existem e ja funcionam desde a Plano 15-01, so faltava o lado JS chamar os
3 ultimos.

Purpose: fecha LOCK-03 e o criterio de sucesso 3 e 4 do ROADMAP da Fase 15 —
os dois unicos que exigem comportamento apos o treino acabar ou o app morrer.
Output: `liveActivitySync.ts` completo (start/update/end/reconcile/timeout),
`LiveActivityUnavailableBanner.tsx` montado no root.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-CONTEXT.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-RESEARCH.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-01-SUMMARY.md
@src/native/liveActivitySync.ts
@src/store/activeSessionStore.ts
@src/components/ProvisioningBanner.tsx
@App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encerramento correto — terminar (resumo + dismiss apos N min) vs cancelar (imediato)</name>
  <files>src/native/liveActivitySync.ts, __tests__/liveActivitySync.test.ts</files>
  <read_first>
    src/native/liveActivitySync.ts (versao da Plano 15-01: subscribe cobre so start/update em status->'active');
    src/store/activeSessionStore.ts (linhas 1698-1741: skipWholeSession completo — `set({ draft: null, status: 'finished', ... })` em UMA chamada atomica; linhas 1743-1786: finishSession — `set({ draft: { ...atual, status: 'finished' }, status: 'finished', ... })`, draft permanece NAO-nulo);
    __tests__/liveActivitySync.test.ts (mocks de modules/live-activity ja estabelecidos na 15-01)
  </read_first>
  <action>
    `src/native/liveActivitySync.ts`: estender o listener de
    `useActiveSessionStore.subscribe((state, prev) => {...})` para reagir a
    `state.status === 'finished' &amp;&amp; prev.status !== 'finished'`. Nesse
    instante, ler `state.draft`: se `state.draft !== null` (caminho
    `finishSession` — o draft.status virou `'finished'` mas o objeto
    sobrevive), montar um ContentState de resumo (reaproveitar
    `buildLiveActivityContentState`-like, ou montar diretamente a partir de
    `state.draft` os campos minimos: `exerciseName`/`setIndex`/`setTotal` do
    ULTIMO exercicio, `restEndsAt: null`), chamar `updateLiveActivity(resumo)`
    e em seguida `endLiveActivity('afterDate', N)` — `N` em segundos dentro da
    janela de 2-5 min do D-06 (usar 180 = 3 min, ponto medio). Se
    `state.draft === null` (caminho `skipWholeSession` — draft e status viram
    juntos na MESMA chamada `set()`, entao o subscribe NUNCA observa um frame
    intermediario com status='finished' e draft ainda presente para este
    caminho), chamar direto `endLiveActivity('immediate')`, sem update previo.
    Nao criar nenhuma acao nova no store — a distincao inteira vem de ler
    `draft` no mesmo instante em que `status` vira `'finished'`, que e o
    unico sinal real que ja existe hoje (RESEARCH.md Pitfall 3 resolvido).
    Envolver as duas chamadas nativas em `try/catch`/`.catch()` — falha nunca
    propaga excecao para o app (rede de seguranca da reconciliacao, Task 2).
  </action>
  <verify>
    <automated>npx jest __tests__/liveActivitySync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Teste simulando uma transicao `status: 'active' -> 'finished'` com `draft`
    permanecendo objeto (nao-null) chama `endLiveActivity` com o primeiro
    argumento `'afterDate'` e um `afterSeconds` numerico entre 120 e 300. Teste
    simulando a MESMA transicao com `draft: null` no estado final chama
    `endLiveActivity('immediate')` sem segundo argumento relevante. Nenhum dos
    dois testes deixa uma excecao nao capturada quando o mock de
    `endLiveActivity` rejeita a Promise.
  </acceptance_criteria>
  <done>liveActivitySync distingue terminar (afterDate) de cancelar (immediate) usando so o sinal ja existente draft/status; testes cobrindo os dois caminhos passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconciliacao de Activity orfa no boot (D-11, criterio de sucesso 4 do ROADMAP)</name>
  <files>src/native/liveActivitySync.ts, App.tsx, __tests__/liveActivitySync.test.ts</files>
  <read_first>
    src/native/liveActivitySync.ts (apos Task 1);
    App.tsx (useEffect de mount criado na Plano 15-01 — `initLiveActivitySync()`);
    modules/live-activity/ios/LiveActivityModule.swift (assinatura de `reconcileOrphans(stillActiveSessionLogId:)` — ja implementada na 15-01, so sem chamador ainda)
  </read_first>
  <action>
    `src/native/liveActivitySync.ts`: nova funcao exportada
    `reconcileOrphanActivities(): Promise&lt;void&gt;` — le
    `useActiveSessionStore.getState().draft`/`.status`, chama
    `reconcileLiveActivityOrphans(draft &amp;&amp; status === 'active' ? draft.sessionLogId
    : null)`. Se o retorno nativo for `true` (havia sessao ainda ativa no
    momento da chamada) E o draft/status no store CONTINUAREM os mesmos apos o
    `await` (mesmo guard de CAS que o resto do arquivo usa — reconferir
    `useActiveSessionStore.getState()` de novo depois do await, comparando
    `sessionLogId`), montar `buildLiveActivityContentState(draft)` e chamar
    `startLiveActivity`. Um caminho so — nao tenta decidir se o card orfao
    "ainda servia" (D-11 explicito). `App.tsx`: no MESMO `useEffect` que chama
    `initLiveActivitySync()` (Plano 15-01), chamar `void
    reconcileOrphanActivities()` ANTES de registrar o subscribe (a
    reconciliacao precisa rodar uma vez no boot, antes de qualquer
    `startActivity` reativo disparar por uma transicao de estado durante o
    proprio boot).
  </action>
  <verify>
    <automated>npx jest __tests__/liveActivitySync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Teste com `reconcileLiveActivityOrphans` mockado retornando `false`
    (nenhuma sessao ativa) confirma que `startLiveActivity` NAO e chamado
    depois. Teste com o mock retornando `true` e o store tendo um draft
    `status: 'active'` confirma que `startLiveActivity` E chamado com o
    ContentState derivado do draft corrente. Teste de CAS: se o
    `sessionLogId` do store mudar entre a chamada e a resolucao do `await`
    (simulando outra sessao assumindo durante a reconciliacao), `startLiveActivity`
    NAO e chamado para a sessao antiga.
  </acceptance_criteria>
  <done>App.tsx roda reconcileOrphanActivities no boot, antes de qualquer navegacao; card orfao sempre encerrado, card novo sobe se a sessao seguir ativa; testes cobrindo os 3 cenarios passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Timeout de inatividade (D-08, 3h) + aviso de indisponibilidade (D-12)</name>
  <files>src/native/liveActivitySync.ts, src/components/LiveActivityUnavailableBanner.tsx, App.tsx, __tests__/liveActivitySync.test.ts, __tests__/LiveActivityUnavailableBanner.test.tsx</files>
  <read_first>
    src/native/liveActivitySync.ts (apos Task 2);
    src/components/ProvisioningBanner.tsx (molde estrutural completo: useEffect no mount com cancelled-guard, Platform.OS !== 'ios' early-return, testID/estilos via theme);
    __tests__/ProvisioningBanner.test.tsx (molde de teste: Platform.OS mockado via Object.defineProperty, mock do modulo nativo);
    .planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-UI-SPEC.md (Copywriting Contract, linha D-12: "Ative as Live Activities em Ajustes para ver o treino na tela bloqueada"; Color/Typography para o banner — identicos a ProvisioningBanner)
  </read_first>
  <action>
    `src/native/liveActivitySync.ts`: dentro do listener do `subscribe`, ao
    detectar `startLiveActivity`/`updateLiveActivity` chamado com sucesso por
    causa de um `completeSet` (isto e, uma mudanca de draft que corresponde a
    uma serie concluida), resetar um timer local `setTimeout` de 3h (constante
    `INACTIVITY_TIMEOUT_MS = 3 * 60 * 60 * 1000`, modulo top-level, fora do
    Zustand) que, se disparar, chama `endLiveActivity('immediate')` SEM tocar
    no store (a sessao continua `active` no store — D-08: "a sessao no store
    permanece intacta para retomada, so o card sai da tela bloqueada"). Cada
    chamada de `updateLiveActivity` reinicia o timer (clearTimeout +
    setTimeout novo). Limpar o timer quando `status` sair de `'active'`
    (`clearTimeout` no mesmo bloco que trata `status === 'finished'` da
    Task 1).

    `src/components/LiveActivityUnavailableBanner.tsx` (novo, molde EXATO de
    `ProvisioningBanner.tsx`): `useEffect` no mount (guard `Platform.OS !==
    'ios'` -> `return null`) chamando `isLiveActivityRunning()` OU um novo
    retorno de `startLiveActivity` (Task 1/15-01 ja retornam `Bool` de
    sucesso/falha) — reaproveitar o `Bool` de retorno de `startLiveActivity`
    exposto por `liveActivitySync.ts` via um pequeno estado observavel (ex.:
    um EventEmitter simples ou um campo no proprio `liveActivitySync.ts`
    exportado como `getLastStartFailed(): boolean`, atualizado sempre que
    `startLiveActivity` resolve `false`) — mostrar o banner UMA UNICA VEZ por
    sessao de app (guard local `hasShown` em modulo, nunca resetado por
    re-mount, so por reload do processo — D-12: "nunca aviso repetido a cada
    tentativa"). Texto exato: "Ative as Live Activities em Ajustes para ver o
    treino na tela bloqueada" — `Text` SEM `numberOfLines` (quebra livre em 2
    linhas, UI-SPEC E11 overflow). Estilos identicos a `ProvisioningBanner.styles`
    (`theme.zIndex.toast`, `theme.colors.surface.card`, `theme.spacing.lg/md`,
    `theme.elevation.floating`, `theme.colors.text.primary`, `theme.fonts.ui`,
    `theme.typography.fontSizes.base`/`fontWeights.medium`). `testID="live-activity-unavailable-banner"`.

    `App.tsx`: montar `&lt;LiveActivityUnavailableBanner /&gt;` ao lado de
    `&lt;ProvisioningBanner /&gt;` (mesma familia de overlay informativo no
    root).
  </action>
  <verify>
    <automated>npx jest __tests__/liveActivitySync.test.ts __tests__/LiveActivityUnavailableBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    Teste com fake timers confirma que `endLiveActivity('immediate')` e
    chamado apos 3h SEM nenhum `completeSet`/update no meio, e que uma
    atualizacao a menos de 3h reinicia a contagem (o timeout nao dispara na
    marca original). `LiveActivityUnavailableBanner`: `Platform.OS !== 'ios'`
    renderiza `null` sem chamar nada (mesmo padrao de
    `ProvisioningBanner.test.tsx`); quando o ultimo `startLiveActivity` falhou,
    o banner aparece com `testID="live-activity-unavailable-banner"` e o texto
    exato "Ative as Live Activities em Ajustes"; uma segunda falha na MESMA
    sessao de app nao dispara um segundo aviso (teste explicito de "mostra uma
    vez so").
  </acceptance_criteria>
  <done>3h sem serie registrada encerra a Live Activity sem tocar a sessao no store; falha ao subir mostra o banner uma vez, nunca mais na mesma sessao de app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chamadas nativas end/reconcile podendo falhar silenciosamente | O modulo `LiveActivityModule` (Plano 15-01) ja nunca lanca atraves da ponte — esta plano depende disso para nao travar o app em nenhum caminho de encerramento |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-03-01 | Denial of Service | Live Activity presa (nunca encerra) por falha silenciosa em end/reconcile | medium | mitigate | D-11 e a propria rede de seguranca: mesmo se um `endActivity`/`updateActivity` isolado falhar, a proxima reabertura do app roda `reconcileOrphanActivities()` e encerra qualquer card remanescente — nunca depende de um unico caminho ter sucesso |
| T-15-03-02 | Repudiation | Reconciliacao roda mas o dono nunca sabe se funcionou (sem log/evidencia) | medium | mitigate | Coberto por teste automatizado (Task 2) + confirmacao fisica explicita na Plano 15-05/15-06 (D-13/D-14: "compilou" nunca e criterio) |
</threat_model>

## Premissas do planner (verificadas ao vivo)

Registradas aqui — e no texto do `must_haves.truths` correspondente — porque
comentário YAML é descartado no parse e some para `/gsd-execute-phase` e
`/gsd-verify-work`.

**P-15-03-01 — "cancelar a sessão" reaproveita `skipWholeSession()`, sem UI nova.**

- **Evidência ao vivo (2026-08-16):** `src/store/activeSessionStore.ts:1698-1728`
  — `skipWholeSession` seta `draft: null` e `status: 'finished'` na MESMA chamada
  `set()` (linhas 1721-1728), sem frame intermediário com `draft` não-nulo. O
  comentário inline do próprio store (linhas 1701-1702, "Vale antes de começar …
  e durante a execução") confirma que o caminho já cobre uma sessão em andamento,
  não só o check-in pré-treino.
- **Caminho de UI acessível:** link "Não vou treinar hoje"
  (`testID="recusar-treino-hoje"`, `src/screens/ActiveSessionScreen.tsx:615-625`),
  visível quando `progresso.done === 0 && !sessionSemNadaAFazer(draft)`. Não
  confundir com `recusar-treino-sem-nada`, que é outro botão, para sessão sem
  nenhuma série planejada.
- **Documento corrigido:** `15-RESEARCH.md` Open Question 1 dizia o contrário e
  está marcada RESOLVED nesta revisão.
- **Se a premissa não se sustentar na execução:** PARE e reporte. Não improvisar
  um `cancelSession()` novo nem nova tela — a decisão de criar superfície de UI
  é do dono, não do executor.

## Artifacts this phase produces

- `src/native/liveActivitySync.ts` — `reconcileOrphanActivities`,
  `getLastStartFailed`, timeout de inatividade (extensoes sobre a 15-01)
- `src/components/LiveActivityUnavailableBanner.tsx` — banner D-12 (novo)

<verification>
`npx jest __tests__/liveActivitySync.test.ts __tests__/LiveActivityUnavailableBanner.test.tsx`
passam 100%. Comportamento fisico real (force-quit + reabrir, timeout de 3h em
tempo real, Live Activities desativada em Ajustes) e verificado na Plano
15-06 (Sessao 2, D-13) — nao automatizavel aqui.
</verification>

<success_criteria>
Terminar e cancelar produzem `dismissalPolicy` diferentes e corretos. Boot
apos force-quit encerra orfaos e retoma o card se a sessao segue ativa. 3h
sem registro encerra o card sem tocar a sessao. Falha ao subir mostra o
aviso uma vez, nunca trava o treino. LOCK-03 fechado por completo.
</success_criteria>

<output>
Create `.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-03-SUMMARY.md` when done
</output>
