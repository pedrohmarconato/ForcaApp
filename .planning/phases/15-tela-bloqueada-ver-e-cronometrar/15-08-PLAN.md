---
phase: 15-tela-bloqueada-ver-e-cronometrar
plan: 08
type: execute
wave: 6
depends_on: ["15-07"]
files_modified:
  - src/native/liveActivitySync.ts
  - src/engine/sessionModel.ts
  - __tests__/liveActivitySync.test.ts
  - __tests__/sessionModel.test.ts
autonomous: true
gap_closure: true
requirements: [LOCK-01, LOCK-03]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "O prazo de inatividade de três horas é armado ao iniciar a sessão e somente é renovado por uma nova série done da mesma sessionLogId; editar reps, carga, RIR ou descanso não o adia — D-08."
    - "Depois do timeout, updateLiveActivity=false recria uma Activity somente se a sessão ainda é a mesma e active; sessões terminadas ou canceladas jamais ressuscitam — D-06/D-08/D-11."
    - "findActiveSet e findNextPendingSet excluem qualquer exercício que exercicioForaDeJogo classifica como fora, incluindo skippedByUser."
  artifacts:
    - path: src/native/liveActivitySync.ts
      provides: "Deadline de inatividade com semântica de conclusão e fallback CAS-safe update→start."
    - path: src/engine/sessionModel.ts
      provides: "Seletores de série consistentes com a regra canônica exercicioForaDeJogo."
  key_links:
    - from: previous/current SessionDraft
      to: inactivity timeout
      via: "detector de transição de set para done da mesma sessionLogId"
      pattern: "done"
    - from: findActiveSet/findNextPendingSet
      to: exercicioForaDeJogo
      via: "filtro único antes de procurar active/pending"
      pattern: "exercicioForaDeJogo"
---

<objective>
Corrige o ciclo de vida de retomada sem mudar o significado de inatividade:
três horas contam desde o start ou a última conclusão de série (D-08), e uma
Activity removida pode voltar quando a mesma sessão ativa recebe uma mutação,
mas nunca após finish/cancel (D-06). Também alinha os seletores do card à regra
canônica de exercício fora de jogo, fechando WR-01 e WR-02.

Purpose: fechar CR-04, WR-01 e WR-02 do relatório autoritativo.
Output: timeout semanticamente correto, fallback seguro de Activity e seletores
que não exibem exercício recusado.
</objective>

<execution_context>
@/Users/phmarconato/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/phmarconato/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-CONTEXT.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-VERIFICATION.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deadline de inatividade por série concluída e recuperação segura depois do timeout</name>
  <files>src/native/liveActivitySync.ts, __tests__/liveActivitySync.test.ts</files>
  <read_first>
    src/native/liveActivitySync.ts (linhas 18-83: timer atual que rearma por update; linhas 163-192: subscriber e cleanup);
    __tests__/liveActivitySync.test.ts (linhas 281-322: teste atual que fixa a regra incorreta de rearmar em toda atualização);
    src/store/activeSessionStore.ts (completeSet, finishSession e skipWholeSession: mutações que definem os estados observados);
    src/engine/sessionModel.ts (DraftSet.status e plannedSetId usados para detectar uma conclusão nova);
    .planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-VERIFICATION.md (gaps 3 e 5).
  </read_first>
  <behavior>
    - Teste RED 1: uma sessão active sem conclusão encerra a Activity exatamente no prazo inicial de três horas.
    - Teste RED 2: alteração apenas de reps, carga, RIR ou restEndsAt perto do prazo publica a atualização necessária, mas o timeout original ainda encerra a Activity.
    - Teste RED 3: uma transição de set que não era done para done da mesma sessão substitui o deadline por três horas a partir da nova conclusão.
    - Teste RED 4: timeout → mutação de draft da mesma sessão active com updateLiveActivity=false chama startLiveActivity uma vez; transição finish/cancel antes da resolução impede esse start.
  </behavior>
  <action>
    Reestruturar somente a política de timeout em `liveActivitySync.ts`. Guardar a
    deadline associada ao `sessionLogId` atual e armá-la no start inicial. No
    subscriber, detectar uma nova série concluída comparando os sets do draft
    anterior e atual pela identidade `plannedSetId`: somente uma série que passa
    de não-done para done, na mesma sessão, substitui a deadline. Atualizações de
    reps, carga, RIR e ajustes de descanso ainda podem chamar `updateLiveActivity`,
    mas não podem rearmar o timeout. Remover/substituir o teste que especifica o
    comportamento contrário.

    Quando `updateLiveActivity` resolver `false`, reler o store e confirmar status
    active, draft existente e o mesmo `sessionLogId` que originou a publicação;
    só então chamar `startLiveActivity` com o ContentState recém-derivado. O fallback
    não pode alterar a deadline nem criar nova sessão; se o timeout já ocorreu,
    ele apenas torna observável a retomada exigida sem falsificar uma nova série
    concluída. Aplicar a mesma guarda depois de qualquer await para impedir
    ressuscitar card após finish, `skipWholeSession` ou troca de sessão. Manter
    D-11 como rede de reconciliação e D-12 como banner não bloqueante.

    Escrever os quatro testes RED acima com fake timers e promises controladas,
    implementar o mínimo para GREEN e refatorar os helpers de comparação sem
    duplicar a lógica de criação de ContentState.
  </action>
  <verify>
    <automated>npx jest __tests__/liveActivitySync.test.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    O teste que antes rearmava por simples update é substituído por casos que
    preservam o prazo original em edições sem conclusão. Um novo set done reinicia
    o prazo, e o cenário timeout → update=false → start é comprovado para a mesma
    sessão active. Testes de finish/cancel durante promise pendente comprovam que
    startLiveActivity não é chamado para estado inválido.
  </acceptance_criteria>
  <done>Três horas passam a significar start ou última série concluída, e a Activity retorna com segurança quando a sessão ativa é retomada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Usar exercicioForaDeJogo nos seletores de série do card</name>
  <files>src/engine/sessionModel.ts, __tests__/sessionModel.test.ts</files>
  <read_first>
    src/engine/sessionModel.ts (linhas 289-306: seletores atuais; linhas 576-585: regra canônica exercicioForaDeJogo; linhas 687-703: consumidor existente da mesma regra);
    __tests__/sessionModel.test.ts (fixtures e convenções de teste do modelo puro);
    src/engine/liveActivityContentState.ts (chamadores de findActiveSet/findNextPendingSet que alimentam o card);
    .planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-REVIEW.md (WR-01 confirmado).
  </read_first>
  <behavior>
    - Teste RED 1: findActiveSet ignora uma série active cujo exercício tem skippedByUser e encontra a active elegível seguinte, ou null se ela for a única.
    - Teste RED 2: findNextPendingSet ignora pending de exercício skippedByUser e devolve a próxima pendente elegível.
    - Teste RED 3: o comportamento existente para cutByReplan continua coberto pela mesma regra canônica.
  </behavior>
  <action>
    Alterar `findActiveSet` e `findNextPendingSet` para chamar
    `exercicioForaDeJogo(ex)` antes de procurar séries, substituindo os filtros
    locais de `cutByReplan`. Não criar segundo predicado nem alterar
    `applyExerciseSkipToDraft`: a regra canônica já expressa as duas origens de
    exclusão. Acrescentar fixtures mínimas em `sessionModel.test.ts` que exercitem
    separadamente active e pending sob `skippedByUser`, mais uma regressão que
    confirme a preservação do caso cutByReplan.
  </action>
  <verify>
    <automated>npx jest __tests__/sessionModel.test.ts __tests__/liveActivityContentState.test.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    Os dois seletores deixam de retornar séries de exercício skippedByUser; os
    testes cobrem active e next pending e o ContentState continua derivável quando
    existe alternativa elegível.
  </acceptance_criteria>
  <done>O Lock Screen não pode voltar a apontar para exercício que o dono recusou.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mutação local do draft → ciclo de vida nativo | Um update assíncrono pode observar uma sessão que já terminou ou foi substituída. |
| Estado de recusa do exercício → Lock Screen | A tela bloqueada deve respeitar a decisão do dono registrada no draft. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-08-01 | Denial of Service | timeout de inatividade | medium | mitigate | Deadline por sessionLogId e transição done evita adiar indefinidamente o encerramento por edições cosméticas. |
| T-15-08-02 | Tampering | fallback update→start assíncrono | high | mitigate | Guardas pós-await de status, draft e sessionLogId bloqueiam ressuscitar uma Activity de sessão terminada, cancelada ou substituída. |
| T-15-08-03 | Information Disclosure | exercício recusado no Lock Screen | medium | mitigate | findActiveSet/findNextPendingSet delegam à regra canônica exercicioForaDeJogo e testes impedem regressão. |
</threat_model>

## Artifacts this phase produces

- Política de inatividade derivada de start/conclusão de série em `liveActivitySync.ts`.
- Fallback CAS-safe para recuperar Activity removida durante sessão ainda active.
- Regressões de Jest para deadlines, recuperações e exercícios skippedByUser.

<verification>
Executar `npx jest __tests__/liveActivitySync.test.ts --runInBand`,
`npx jest __tests__/sessionModel.test.ts __tests__/liveActivityContentState.test.ts --runInBand`,
`npx tsc --noEmit` e `npm run verify:native` após o merge da wave.
</verification>

<success_criteria>
O timeout não é adiado por edição arbitrária, a retomada não perde o card da
sessão vigente e nenhum seletor do card usa exercício fora de jogo.
</success_criteria>

<output>
Create `.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-08-SUMMARY.md` when done
</output>
