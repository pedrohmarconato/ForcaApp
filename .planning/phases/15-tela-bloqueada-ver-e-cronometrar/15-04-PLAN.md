---
phase: 15-tela-bloqueada-ver-e-cronometrar
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .env
autonomous: true
requirements: [LOCK-01, LOCK-02, LOCK-03]

user_setup:
  - service: supabase
    why: "O app nativo no iPhone fisico precisa apontar para o Supabase de producao para o login completar fora da rede do Mac — sem isto a UAT com treino real da Plano 15-06 (Sessao 2, D-13) nao roda. Todo dobrado nesta fase (backend-supabase-producao-no-aparelho, criado 2026-08-16, severidade blocking-usage)."
    env_vars:
      - name: EXPO_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> projeto zanqygwsgxkyjiuhrzju -> Settings -> API -> Project URL"
      - name: EXPO_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> projeto zanqygwsgxkyjiuhrzju -> Settings -> API -> anon public key (ou Vercel Dashboard -> forca-app-six -> Settings -> Environment Variables, ja deployado ali desde a Fase 13)"

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "EXPO_PUBLIC_SUPABASE_URL e EXPO_PUBLIC_SUPABASE_ANON_KEY em .env apontam para o projeto de producao zanqygwsgxkyjiuhrzju, nao mais para 127.0.0.1:54321"
    - "O bundle nativo foi reconstruido (npm run resign) apos a mudanca — EXPO_PUBLIC_* sao inlined em build-time via babel-preset-expo (codebase/STACK.md), editar .env sem rebuild nao muda nada no app ja instalado no aparelho"
  artifacts: []
  key_links: []
  prohibitions: []
---

<!-- planner-discipline-allow: 127.0.0.1:54321 -->
<!-- O literal acima e o valor ATUAL que esta plano substitui — necessario
     como documentacao em varios pontos deste arquivo (contexto, action,
     verify) e nao um eco de "o que nao fazer"; ver planner-antipatterns.md
     Comment-Text Discipline, allowlist escape hatch. -->
<objective>
Aponta o app instalado no iPhone fisico para o Supabase de producao
(`zanqygwsgxkyjiuhrzju`), fechando o todo dobrado
`backend-supabase-producao-no-aparelho` (criado na Sessao 1 fisica da Fase 14,
Plano 14-06): hoje `EXPO_PUBLIC_SUPABASE_URL` aponta para `127.0.0.1:54321`,
que dentro do iPhone resolve para o proprio aparelho, nao o Mac — o login
nunca completa fora da rede local, e a Sessao 2 desta fase (D-13, UAT com
treino real) exige login real.

Nao mexe em nenhum arquivo de Live Activity — puramente configuracao de
ambiente + rebuild. Pode rodar em paralelo as Planos 15-01/15-02/15-03 (sem
overlap de arquivos).

Purpose: pre-requisito bloqueante da Plano 15-06 (Sessao 2 fisica) — sem esta
plano, o UAT completo de LOCK-01/02/03 nao tem caminho fisico com dado real.
Output: `.env` apontando para producao; bundle reconstruido via `npm run resign`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/backend-supabase-producao-no-aparelho.md
@.planning/codebase/STACK.md
@scripts/resign.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apontar .env para o Supabase de producao e reconstruir o bundle nativo</name>
  <precondition>Os valores de EXPO_PUBLIC_SUPABASE_URL e EXPO_PUBLIC_SUPABASE_ANON_KEY do projeto zanqygwsgxkyjiuhrzju foram fornecidos (user_setup) antes desta task rodar</precondition>
  <files>.env</files>
  <read_first>
    .planning/todos/pending/backend-supabase-producao-no-aparelho.md (diagnostico completo: .env hoje aponta EXPO_PUBLIC_SUPABASE_URL para 127.0.0.1:54321, causa raiz e por que apontar para o IP do Mac tambem nao resolve);
    .planning/codebase/STACK.md (confirma que EXPO_PUBLIC_* sao inlined no bundle via babel-preset-expo em build-time, nao lidos em runtime)
  </read_first>
  <action>
    Editar `.env` (raiz do repo, gitignored) via `sed` no Bash (o tool `Read`
    e negado para arquivos `.env*` por politica de sandbox — usar substituicao
    direta sem precisar ler o arquivo inteiro primeiro): trocar o valor atual
    de `EXPO_PUBLIC_SUPABASE_URL` (hoje `http://127.0.0.1:54321` ou
    equivalente) pelo Project URL do projeto `zanqygwsgxkyjiuhrzju` fornecido
    via `user_setup`, e trocar `EXPO_PUBLIC_SUPABASE_ANON_KEY` pela anon key do
    mesmo projeto. Nao mexer em nenhuma outra variavel de `.env`. Nao editar
    `.env.example` (documenta o FORMATO, nao precisa dos valores reais). Depois
    da edicao, rodar `npm run resign` (Fase 14, Plano 14-04) para reconstruir o
    bundle nativo com os novos valores embutidos — sem este passo, o app ja
    instalado no aparelho continua rodando o bundle antigo com o Supabase
    local.
  </action>
  <verify>
    <automated>grep -v '^#' .env | grep -c "zanqygwsgxkyjiuhrzju" && ! grep -v '^#' .env | grep -q "127.0.0.1:54321"</automated>
  </verify>
  <acceptance_criteria>
    `.env` contem o project ref `zanqygwsgxkyjiuhrzju` em
    `EXPO_PUBLIC_SUPABASE_URL` (grep positivo) e NAO contem mais
    `127.0.0.1:54321` (grep negativo). `npm run resign` completa os 8/8 passos
    estabelecidos na Fase 14 sem erro, terminando com o app reinstalado no
    aparelho — confirmacao fisica de que o login completa com a conta real
    fica para a Plano 15-06 (Sessao 2, D-13), esta task so prova a
    configuracao e o rebuild.
  </acceptance_criteria>
  <done>.env aponta para o projeto de producao; bundle nativo reconstruido via npm run resign com os novos valores embutidos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env (segredo local, gitignored) -> bundle nativo embutido | EXPO_PUBLIC_ANON_KEY e uma anon key (publica por design, protegida por RLS no lado do servidor, nao um segredo tradicional) — mas o PROJECT REF/URL aponta para dado real de producao a partir desta task |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04-01 | Information Disclosure | .env commitado por engano com credencial de producao | medium | mitigate | `.env` ja esta no `.gitignore` do repo (confirmar antes de commitar qualquer coisa desta task); nenhuma alteracao de `.gitignore` faz parte desta task |
</threat_model>

## Artifacts this phase produces

Nenhum artefato de codigo nesta plano — so configuracao (`.env`) + rebuild.

<verification>
`.env` aponta para o projeto de producao; `npm run resign` completa sem erro.
Login real no aparelho e confirmado fisicamente na Plano 15-06.
</verification>

<success_criteria>
`.env` nao referencia mais `127.0.0.1:54321`; o bundle instalado no aparelho
embute os valores de producao.
</success_criteria>

<output>
Create `.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-04-SUMMARY.md` when done
</output>
