---
phase: 15-tela-bloqueada-ver-e-cronometrar
plan: 07
type: execute
wave: 5
depends_on: ["15-06"]
files_modified:
  - targets/session-widget/OvertimeFormatter.swift
  - targets/session-widget/RestPhaseResolver.swift
  - targets/session-widget/WidgetLiveActivity.swift
  - scripts/verify-live-activity-overtime.sh
  - scripts/verify-native-skeleton.sh
autonomous: true
gap_closure: true
requirements: [LOCK-01, LOCK-02]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Quando restEndsAt vence sem mutação externa, o mesmo card passa de resting para readyOvertime/Pronto a partir de restEndsAt e da data do TimelineView, mesmo com o JavaScript suspenso — D-04."
    - "O texto de overtime reavalia no WidgetKit a cada segundo, cresce de +0:00 até +59:59 e mantém o teto sem qualquer atualização JS por segundo — D-04 e LOCK-02."
    - "Atualizações locais da Activity continuam reservadas às mutações reais do draft; a apresentação temporal não escreve Zustand, não chama activateSet e não depende de timeout JS — D-05/D-06/D-09/D-10."
  artifacts:
    - path: targets/session-widget/OvertimeFormatter.swift
      provides: "Formatador Swift puro, clampado e compilável fora da View para o microtexto de overtime."
    - path: targets/session-widget/RestPhaseResolver.swift
      provides: "Resolução Swift pura da fase efetiva resting/readyOvertime e dos segundos de overtime a partir de restEndsAt e now."
    - path: scripts/verify-live-activity-overtime.sh
      provides: "Teste Swift puro das fronteiras temporal antes/no/depois de restEndsAt e do formatter sem criar um novo target XCTest."
  key_links:
    - from: targets/session-widget/WidgetLiveActivity.swift
      to: targets/session-widget/RestPhaseResolver.swift
      via: "TimelineView periódico fornece timeline.date para derivar a fase efetiva e o overtime de restEndsAt"
      pattern: "TimelineView"
---

<objective>
Fecha a fatia temporal que faltou na implementação: o descanso chega a zero sem
avanço automático (D-09/D-10), e o próprio WidgetKit deriva a transição visual
para `readyOvertime` e seu overtime de `restEndsAt`, inclusive com JavaScript
suspenso (D-04). Esta é a tracer de fechamento: o `ContentState` já entregue por
ActivityKit percorre `TimelineView` → resolvedor Swift puro → card, preservando
as atualizações nativas que o sync já faz para mutações reais do draft e os layouts
aprovados em D-01/D-02/D-03.

Purpose: fechar os blockers CR-01 e CR-02 do `15-VERIFICATION.md` sem
reintroduzir atualização por segundo nem transição de fase por timeout no processo JS.
Output: resolução temporal Swift validada, formatter Swift puro testado e gate nativo
que os executa junto do esqueleto existente.
</objective>

<execution_context>
@/Users/phmarconato/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/phmarconato/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-CONTEXT.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-VERIFICATION.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-REVIEW.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Derivar no WidgetKit a transição temporal de descanso para Pronto e overtime</name>
  <files>targets/session-widget/OvertimeFormatter.swift, targets/session-widget/RestPhaseResolver.swift, targets/session-widget/WidgetLiveActivity.swift, scripts/verify-live-activity-overtime.sh</files>
  <read_first>
    src/native/liveActivitySync.ts (publishUpdate dispara apenas para mutações reais do draft e não deve ganhar timeout de restEndsAt);
    src/engine/liveActivityContentState.ts (a derivação existente e o ContentState que transporta restEndsAt);
    targets/session-widget/WidgetLiveActivity.swift (switch atual de context.state.phase, countdown resting e consumidor de overtime);
    targets/session-widget/SessionActivityAttributes.swift (tipos Swift de phase e restEndsAt consumidos pelo widget);
    .planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-VERIFICATION.md (gaps 1 e 2, que definem a falha a fechar).
  </read_first>
  <behavior>
    - Teste Swift RED 1: para ContentState recebido como resting e now estritamente anterior a restEndsAt, o resolvedor mantém resting e não há segundos de overtime.
    - Teste Swift RED 2: no instante exato de restEndsAt, tanto o estado recebido resting quanto readyOvertime resolvem para readyOvertime e o formatter recebe zero, exibindo +0:00.
    - Teste Swift RED 3: após restEndsAt, ambos os estados recebidos resolvem para readyOvertime; o overtime cresce com now e o formatter exibe +59:59 em 3599 segundos e acima do teto.
    - Teste de fonte: WidgetLiveActivity contém TimelineView ativo para os dois ramos temporais e usa timeline.date no resolvedor, sem setTimeout ou updateLiveActivity no fluxo de expiração.
  </behavior>
  <action>
    Não adicionar nem manter em `src/native/liveActivitySync.ts` um `setTimeout` de
    `restEndsAt`: o sync continua a publicar Activity updates somente para mutações
    reais do draft. Criar `targets/session-widget/RestPhaseResolver.swift` com funções
    Swift puras que recebem a fase de `ContentState`, `restEndsAt` e `now`: quando há
    prazo e a fase recebida é resting ou readyOvertime, resolver para resting apenas
    antes do prazo e para readyOvertime no instante do prazo e depois dele; calcular
    segundos de overtime como diferença não negativa. Para fases não temporais ou
    prazo ausente, preservar a fase recebida sem fabricar overtime. Isto implementa
    D-04 sem mudar Zustand, chamar `activateSet` ou depender de JavaScript acordado,
    preservando D-05/D-06/D-09/D-10.

    Extrair para `targets/session-widget/OvertimeFormatter.swift` uma função Swift
    pura que recebe segundos transcorridos, aplica clamp inclusivo em 3599 e formata
    `+m:ss`. Em `WidgetLiveActivity.swift`, envolver a região que seleciona a
    apresentação temporal em `TimelineView(.periodic(from: .now, by: 1))`. Em toda
    atualização de `timeline.date`, calcular a fase efetiva pelo novo resolvedor;
    renderizar `Text(timerInterval:)` quando ela for resting e renderizar Pronto/Série
    mais o formatter quando ela for readyOvertime. Assim, tanto um ContentState ainda
    recebido como resting quanto um já recebido como readyOvertime permanecem corretos
    antes, exatamente no instante e depois de restEndsAt. Manter `.monospacedDigit()`
    e não alterar as atualizações locais que o sync publica para mutações do draft.

    Criar `scripts/verify-live-activity-overtime.sh` no estilo fail-fast de
    `verify-native-skeleton.sh`: compilar os helpers Swift puros com `xcrun swiftc`
    em diretório temporário, executar primeiro os testes RED e então as asserções
    antes/no/depois descritas em behavior para os dois estados de entrada, além das
    bordas 0, 3599 e acima de 3599 do formatter; limpar o temporário por trap. Incluir
    uma inspeção de fonte delimitada a WidgetLiveActivity que comprove TimelineView e
    timeline.date, sem exigir callback JavaScript.
  </action>
  <verify>
    <automated>bash scripts/verify-live-activity-overtime.sh</automated>
  </verify>
  <acceptance_criteria>
    As provas Swift exercitam antes, exatamente no instante e depois de restEndsAt
    para entrada resting e readyOvertime; no instante, ambas mostram Pronto com
    +0:00, e depois ambas avançam temporalmente até o teto +59:59. O widget usa
    TimelineView e timeline.date na região temporal para ambas as apresentações, e
    o fluxo de expiração não depende de setTimeout, mutação do store ou chamada JS
    recorrente para updateLiveActivity.
  </acceptance_criteria>
  <done>O Lock Screen passa de descanso para Pronto no prazo absoluto e o overtime continua visualmente correto até o teto mesmo com JavaScript suspenso, com provas Swift verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Incluir a prova Swift temporal no gate nativo já existente</name>
  <files>scripts/verify-native-skeleton.sh</files>
  <read_first>
    scripts/verify-native-skeleton.sh (linhas 43-157: padrão set -euo pipefail, duas rodadas idempotentes e mensagens ABORTADO);
    scripts/verify-live-activity-overtime.sh (criado na Task 1: comando de teste Swift puro do resolvedor temporal e formatter);
    package.json (script verify:native aponta para verify-native-skeleton.sh).
  </read_first>
  <action>
    Acrescentar ao gate `scripts/verify-native-skeleton.sh` uma checagem explícita
    que execute `scripts/verify-live-activity-overtime.sh` em cada rodada, depois
    do prebuild que garante a extensão disponível e antes da mensagem de sucesso.
    Seguir o tratamento de falha existente: mensagem identificando a rodada e a
    correção acionável, saída não zero e nenhuma ocultação de erro. Não alterar as
    seis verificações existentes nem as entitlements; esta expansão só conecta a
    prova temporal Swift ao mesmo `npm run verify:native` já usado pela fase.
  </action>
  <verify>
    <automated>npm run verify:native</automated>
  </verify>
  <acceptance_criteria>
    `npm run verify:native` executa duas rodadas do prebuild/esqueleto e, em ambas,
    executa a prova Swift do resolvedor temporal e formatter antes de reportar
    sucesso. Falha nessa prova faz o comando encerrar com erro identificável de rodada.
  </acceptance_criteria>
  <done>O gate nativo passa a cobrir a fase temporal e o overtime clampado além de compilar o target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ActivityKit → WidgetKit | O `ContentState` recebido pelo widget contém restEndsAt e deve permanecer temporalmente coerente enquanto o processo JS está suspenso. |
| TimelineView → apresentação do card | A data do sistema muda a apresentação effective sem mudar o estado persistido nem acionar o app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-07-01 | Denial of Service | expiração de descanso | high | mitigate | Resolvedor puro usa restEndsAt e timeline.date em cada tick do WidgetKit, evitando que suspensão do processo JS congele o card em resting. |
| T-15-07-02 | Tampering | fase e formatter de overtime | medium | mitigate | Helpers Swift puros compilados e testados antes/no/depois do prazo para as entradas resting e readyOvertime, incluindo clamp. |
| T-15-07-03 | Information Disclosure | ContentState | low | accept | Esta correção não acrescenta campos; preserva o contrato mínimo já aceito para LOCK-01. |
</threat_model>

## Artifacts this phase produces

- `targets/session-widget/OvertimeFormatter.swift` — formatter temporal clampado.
- `targets/session-widget/RestPhaseResolver.swift` — resolução temporal da fase efetiva e overtime.
- `scripts/verify-live-activity-overtime.sh` — provas Swift antes/no/depois do prazo e do formatter.
- Extensão de `verify-native-skeleton.sh` — execução da prova Swift no gate existente.

<verification>
Executar `bash scripts/verify-live-activity-overtime.sh`, `npx tsc --noEmit` e
`npm run verify:native`. A checagem física específica fica na 15-09 após todos
os reparos; Dynamic Island não entra novamente no gate por ser item deferido.
</verification>

<success_criteria>
O tempo, sozinho, troca a apresentação nativa de descanso para Pronto e o overtime
continua crescendo até +59:59 sem atividade JavaScript; as regressões estão cobertas
por Swift e o esqueleto nativo.
</success_criteria>

<output>
Create `.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-07-SUMMARY.md` when done
</output>
