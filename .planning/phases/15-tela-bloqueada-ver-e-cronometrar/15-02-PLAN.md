---
phase: 15-tela-bloqueada-ver-e-cronometrar
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/engine/liveActivityContentState.ts
  - src/engine/sessionFlow.ts
  - targets/session-widget/WidgetLiveActivity.swift
  - __tests__/liveActivityContentState.test.ts
  - __tests__/sessionFlow.test.ts
autonomous: true
requirements: [LOCK-01]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Durante blocos de cardio/alongamento (isTimeBased(metricOf(exercicio))), o card mostra so nome do bloco + posicao (ex.: 'Alongamento 2/6'), sem linha de prescricao — D-03, mesmo enquanto o exercicio esta com serie ativa ou em descanso"
    - "Quando o descanso zera e o dono nao age, o card mostra 'Pronto' em destaque + 'Serie X/Y' + o tempo excedido crescente com prefixo '+' — D-04"
    - "O tempo excedido (+m:ss) e clampado em +59:59 — passado 1h o texto para de crescer em largura, nunca reflui o layout do card — E3, decisao do dono 2026-08-16"
    - "As 4 apresentacoes do Dynamic Island (compact/minimal/expanded + Lock Screen) tem conteudo real e diferenciado por fase: compact/minimal priorizam mm:ss durante o descanso e serie X/Y fora dele (D-02); minimal usa SF Symbol (glyph 'timer' em neon durante descanso, icone de estado fora dele), nao digitos literais — E10, decisao do dono"
    - "Todo texto livre (nome do exercicio, nome do bloco) tem .lineLimit(1) e .truncationMode(.tail); a linha de prescricao usa .minimumScaleFactor(0.8) em vez de truncar — um digito cortado nao comunica nada"
    - statement: "Um exercicio medido por tempo/duracao (isTimeBased) nunca alcanca a fase 'measuring' — a derivacao de fase roteia isTimeBased direto para 'blockOnly', tornando o slot Display sem reps x carga estruturalmente inalcancavel, nao so coberto por fallback (E5 empty)"
      verification: backstop
    - statement: "SwiftUI colapsa a regiao .bottom do Dynamic Island expandido quando nao ha tag de tempo excedido nem conteudo de bloco (nenhuma banda vazia reservada) — nao confirmado em simulador/CI, so em aparelho fisico (E8 partial)"
      verification: backstop
  artifacts:
    - path: "src/engine/sessionFlow.ts"
      provides: "posicaoNoBlocoDeMetrica — posicao do exercicio dentro do bloco da MESMA metrica (isTimeBased), para o numerador de 'Alongamento 2/6'"
  key_links:
    - from: "src/engine/liveActivityContentState.ts"
      to: "src/engine/sessionFlow.ts (posicaoNoBlocoDeMetrica)"
      via: "buildLiveActivityContentState chama posicaoNoBlocoDeMetrica ao montar blockLabel/blockIndex/blockTotal"
      pattern: "posicaoNoBlocoDeMetrica"
    - from: "targets/session-widget/WidgetLiveActivity.swift"
      to: "src/engine/liveActivityContentState.ts (LiveActivityContentState)"
      via: "contrato de campos identico — nenhum campo novo introduzido nesta plano, so consumo do que a 15-01 ja tipou"
      pattern: "context.state.phase"
  prohibitions: []
---

<objective>
Completa o card iniciado na Plano 15-01: as fases `blockOnly` (D-03, cardio e
alongamento) e `readyOvertime` (D-04, "Pronto" + tempo excedido), e o
conteudo real das 4 apresentacoes do Dynamic Island (D-01/D-02), seguindo o
contrato de design da `15-UI-SPEC.md` (tipografia, cor, truncamento). Fecha o
Open Question 2 do RESEARCH.md (semantica do numerador "Alongamento 2/6").

Nao toca em ciclo de vida (start/update/end) nem em `activeSessionStore.ts` —
so consome o `ContentState` que a Plano 15-01 ja definiu e ja empurra para o
nativo.

Purpose: fecha LOCK-01 por completo — as 4 apresentacoes do Dynamic Island
com conteudo real, nao so a via minima measuring/resting da 15-01.
Output: `WidgetLiveActivity.swift` com o switch completo sobre as 4 fases e as
4 closures do Dynamic Island preenchidas; `buildLiveActivityContentState`
derivando `blockOnly`/`readyOvertime` corretamente.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-CONTEXT.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-UI-SPEC.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-01-SUMMARY.md
@src/engine/liveActivityContentState.ts
@src/engine/sessionFlow.ts
@src/engine/sessionModel.ts
@targets/session-widget/WidgetLiveActivity.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derivacao completa de fase (blockOnly/readyOvertime) + posicaoNoBlocoDeMetrica</name>
  <files>src/engine/sessionFlow.ts, src/engine/liveActivityContentState.ts, __tests__/liveActivityContentState.test.ts, __tests__/sessionFlow.test.ts</files>
  <read_first>
    src/engine/liveActivityContentState.ts (versao da Plano 15-01: buildLiveActivityContentState so cobre measuring/resting, retorna null sem active/rest);
    src/engine/sessionFlow.ts (posicaoDoExercicio, linhas 24-32 — molde exato a espelhar filtrando por metrica);
    src/engine/sessionModel.ts (isTimeBased/metricOf, linhas 277-283; exercicioForaDeJogo, linha 554-556; DraftExercise.metric);
    __tests__/sessionFlow.test.ts (fixtures `serie`/`exercicio` a reaproveitar para os novos casos)
  </read_first>
  <action>
    `src/engine/sessionFlow.ts`: nova funcao pura `posicaoNoBlocoDeMetrica(draft:
    SessionDraft, exerciseId: string): { indice: number; total: number } | null`
    — mesma forma de `posicaoDoExercicio` (linha 24-32), mas filtra
    `exerciciosEmJogo(draft)` para so os exercicios com
    `isTimeBased(metricOf(ex))` IGUAL ao exercicio de `exerciseId` antes de
    achar indice/total (RESEARCH.md Open Question 2 resolvida: "Alongamento 2/6"
    conta posicao DENTRO do bloco de cardio/alongamento, nao do treino inteiro
    — leitura mais literal do texto de exemplo em CONTEXT.md, marcada como
    premissa do planner, nao contrato travado; confirmavel/corrigivel na
    Sessao 1 fisica se o dono achar estranho). Se `exerciseId` nao for
    isTimeBased, retorna `null` (a funcao so faz sentido dentro do bloco
    reduzido).

    `src/engine/liveActivityContentState.ts`: estender
    `buildLiveActivityContentState` para as 4 fases. Determinar o "exercicio
    corrente" como `active?.exercise ?? findNextPendingSet(draft)?.exercise ??
    null` (o exercicio associado ao `next` de um `RestState` equivalente — ja
    que este arquivo nao tem acesso ao `RestState` local do componente, usar
    `findNextPendingSet(draft)` quando `draft.restEndsAt` estiver setado e
    `active` for null, que e exatamente a situacao "descansando"). Se o
    exercicio corrente existir e `isTimeBased(metricOf(exercicioCorrente))` ->
    fase `blockOnly` SEMPRE (mesmo se `draft.restEndsAt` estiver setado — D-03
    governa o bloco inteiro, nao so o instante de medicao), com `blockLabel:
    exercicioCorrente.name`, `blockIndex`/`blockTotal` de
    `posicaoNoBlocoDeMetrica`, e os demais campos (`targetReps*`,
    `targetLoadKg`) `null`. Senao (exercicio nao isTimeBased): se
    `draft.restEndsAt` setado e `now < restEndsAt` -> fase `resting` (igual
    15-01); se `draft.restEndsAt` setado e `now >= restEndsAt` -> fase
    `readyOvertime` (novo — `restEndsAt` continua propagado, o widget calcula o
    tempo excedido do lado Swift via `Text(timerInterval: context.state.restEndsAt!...Date.distantFuture,
    countsDown: false)`); senao se `active` existe -> fase `measuring` (igual
    15-01); senao -> `null`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx jest __tests__/liveActivityContentState.test.ts __tests__/sessionFlow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `buildLiveActivityContentState` retorna fase `blockOnly` para um exercicio
    `metric: 'tempo'` ou `'tempo_distancia'` tanto com `active` setado quanto
    com `restEndsAt` setado (teste cobrindo os dois casos). Nunca retorna fase
    `measuring` para um exercicio isTimeBased (teste negativo explicito).
    `posicaoNoBlocoDeMetrica` retorna `null` para um exercicio `carga_reps`, e
    indice/total corretos contando SO os exercicios isTimeBased em jogo (teste
    com uma sessao mista musculacao+cardio confirmando que o total nao inclui
    os exercicios de carga x reps). `readyOvertime` retorna quando
    `now >= restEndsAt`, `resting` quando `now < restEndsAt`, ambas com
    `restEndsAt` propagado no ContentState.
  </acceptance_criteria>
  <done>buildLiveActivityContentState deriva corretamente as 4 fases (measuring/resting/readyOvertime/blockOnly); posicaoNoBlocoDeMetrica testada e exportada; suites verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Card Swift completo — as 4 fases + as 4 apresentacoes do Dynamic Island</name>
  <files>targets/session-widget/WidgetLiveActivity.swift</files>
  <read_first>
    targets/session-widget/WidgetLiveActivity.swift (versao da Plano 15-01: switch so cobre .measuring/.resting, DynamicIsland com compact/minimal/expanded ainda genericos);
    .planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-UI-SPEC.md secoes Typography, Color, "Live Activity chrome", "Presentation size budgets and truncation rules", "Countdown rendering contract"
  </read_first>
  <action>
    `targets/session-widget/WidgetLiveActivity.swift`: completar o `switch
    context.state.phase` do corpo `ActivityConfiguration` (Lock Screen) com os
    2 casos que faltam. `.blockOnly`: `Text("\(context.state.blockLabel ?? "")
    \(context.state.blockIndex ?? 0)/\(context.state.blockTotal ?? 0)")` no
    papel Micro (`.caption2`, `.regular`, `.lineLimit(1)`) — sem elemento
    Display grande, per D-03. `.readyOvertime`: elemento Display = `Text("Pronto")`
    em `.title2`/`.bold`/cor accent (`#EBFF00`); Label = `Text("Serie
    \(context.state.setIndex)/\(context.state.setTotal)")`; Micro = a tag de
    tempo excedido `Text(timerInterval: context.state.restEndsAt!...Date.distantFuture,
    countsDown: false)` com `.monospacedDigit()`, CLAMPADA em +59:59 (calcular
    o texto manualmente com `min()` sobre a diferenca em segundos em vez de
    usar `Text(timerInterval:)` sem teto, ja que esse initializer nao aceita um
    limite superior — formatar como string "+mm:ss" a partir de
    `min(3599, Int(Date.now.timeIntervalSince(context.state.restEndsAt!)))`, uma
    unica excecao ao uso de `Text(timerInterval:)` justificada pelo teto
    explicito do dono). `.measuring`: usar `.minimumScaleFactor(0.8)` no
    elemento Display de prescricao (`"\(targetRepsMin)-\(targetRepsMax) reps ×
    \(targetLoadKg) kg"`, ou o texto "Peso corporal" quando `isBodyweight`), sem
    truncar. Todo `Text` de nome (exercicio/bloco) recebe `.lineLimit(1)` +
    `.truncationMode(.tail)`.

    `dynamicIsland`: `compactLeading`/`compactTrailing` mostram
    `Text(timerInterval: Date.now...context.state.restEndsAt!, countsDown: true)`
    com `.monospacedDigit()` quando fase e `.resting`, e
    `Text("\(setIndex)/\(setTotal)")` (sem a palavra "Serie") nas demais fases
    — D-02, ambos em `.caption`/`.bold`. `minimal`: `Image(systemName: "timer")`
    tintado `palette.neon` (`Color(red: 0.922, green: 1.0, blue: 0.0)`) quando
    `.resting`; um icone de estado tintado `text.secondary`
    (`Color(red: 0.545, green: 0.565, blue: 0.596)`) nas demais fases — E10,
    decisao do dono, NUNCA digitos literais no slot minimal.
    `DynamicIslandExpandedRegion(.trailing)`: elemento Display (espelha o Lock
    Screen). `.leading`: elemento Label (secundario). `.bottom`: Micro/tag de
    tempo excedido/blockLabel quando aplicavel, ausente (nao reservando espaco)
    quando nao ha conteudo para essa regiao — Open Decision 3 do UI-SPEC,
    default cosmetico ja aprovado.

    Substituir os 3 placeholders remanescentes do scaffold ainda nao trocados
    pela 15-01 (se algum ficou): `.activityBackgroundTint` para
    `Color(red: 0.039, green: 0.039, blue: 0.039)`, `.activitySystemActionForegroundColor`
    para `Color.white`, `.keylineTint` para `Color(red: 0.922, green: 1.0,
    blue: 0.0)`. Remover a chamada `.widgetURL(...)` que ainda aponta para o
    dominio placeholder do scaffold do `@bacons/apple-targets` — fora de
    escopo desta fase, nao deixar uma URL de exemplo indo para a tela
    bloqueada do dono.
  </action>
  <verify>
    <automated>npm run verify:native</automated>
  </verify>
  <acceptance_criteria>
    `npm run verify:native` sai 0 (o Swift compila e o esqueleto sobrevive a 2
    rodadas de `--clean`). `grep -c "case .blockOnly" targets/session-widget/WidgetLiveActivity.swift`
    e `grep -c "case .readyOvertime" targets/session-widget/WidgetLiveActivity.swift`
    retornam >= 1 cada (as 2 fases novas estao presentes no switch). Leitura
    manual do arquivo confirma que a chamada de URL placeholder do scaffold
    (que apontava para um dominio de exemplo da Expo) nao existe mais.
    Renderizacao
    real em Lock Screen/Dynamic Island e verificada fisicamente na Plano 15-05
    (Sessao 1, D-13) — nao automatizavel aqui.
  </acceptance_criteria>
  <done>WidgetLiveActivity.swift renderiza as 4 fases e as 4 apresentacoes do Dynamic Island com conteudo real, compila e sobrevive a expo prebuild --clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Widget Extension (processo separado) | Mesma fronteira da Plano 15-01 — esta plano adiciona campos (blockLabel/blockIndex/blockTotal) ao mesmo canal, sem novo mecanismo de transporte |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02-01 | Information Disclosure | blockLabel/blockIndex/blockTotal revelando estrutura do plano (ex.: "Alongamento 2/6" expoe quantos exercicios de alongamento existem) | low | accept | Metadado de treino, nao dado sensivel — app pessoal single-user, mesma categoria de informacao que a serie X/Y ja exibida (T-15-01-03 aceito na Plano 15-01) |
</threat_model>

## Artifacts this phase produces

- `src/engine/sessionFlow.ts` — `posicaoNoBlocoDeMetrica` (nova)
- `src/engine/liveActivityContentState.ts` — `buildLiveActivityContentState`
  estendida para as 4 fases (mesma assinatura da 15-01)
- `targets/session-widget/WidgetLiveActivity.swift` — switch de 4 fases +
  4 apresentacoes do Dynamic Island completas

<verification>
`npx tsc --noEmit` limpo; `__tests__/liveActivityContentState.test.ts` e
`__tests__/sessionFlow.test.ts` passam 100%; `npm run verify:native` sai 0.
Fidelidade visual real (cores, tipografia, truncamento em aparelho) e
verificada na Plano 15-05.
</verification>

<success_criteria>
As 4 fases (measuring/resting/readyOvertime/blockOnly) e as 4 apresentacoes
do Dynamic Island tem conteudo real, derivado do `SessionDraft`, sem dado
inventado. LOCK-01 fechado por completo.
</success_criteria>

<output>
Create `.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-02-SUMMARY.md` when done
</output>
