No external API integration: o detector disparou em falso-positivo sobre "Expo SDK 54" (versão de plataforma) e "Expo Modules API" (bridge interno Swift↔JS já usado por `modules/native-info/`) — nenhum dos dois é uma integração de API/SDK/serviço externo de terceiros. Esta fase integra ActivityKit, um framework nativo da Apple embarcado no iOS (não uma rede/API externa com superfície de verbos como `search`/`play`/`skip`), via um módulo Expo local (`modules/live-activity/`) chamando `Activity<T>.request/update/end` diretamente no processo do app. Não há endpoint HTTP, SDK de terceiro, OAuth, webhook ou serviço externo nesta fase.
