---
phase: 15-tela-bloqueada-ver-e-cronometrar
plan: 09
type: execute
wave: 7
depends_on: ["15-08"]
files_modified:
  - App.tsx
  - modules/live-activity/index.ts
  - __tests__/liveActivityPlatformImport.test.ts
autonomous: false
gap_closure: true
requirements: [LOCK-01, LOCK-02, LOCK-03]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Android e web importam App e liveActivitySync sem LiveActivityModule instalado, enquanto iOS preserva as chamadas reais ao módulo nativo e o banner indisponível permanece não bloqueante — D-12."
    - "A rechecagem física cobre somente os cenários iPhone modificados: descanso → Pronto/overtime e timeout de inatividade → retomada; não reabre aceitação de Dynamic Island, item deferido."
    - "LOCK-01, LOCK-02 e LOCK-03 têm evidência de fonte, comportamento Jest/TypeScript, esqueleto nativo e UAT física específica das alterações."
  artifacts:
    - path: modules/live-activity/index.ts
      provides: "Bridge opcional que retorna false quando não há módulo nativo, sem lançar no bootstrap Android/web."
    - path: __tests__/liveActivityPlatformImport.test.ts
      provides: "Imports Android e web sem mock do bridge LiveActivityModule."
  key_links:
    - from: App.tsx
      to: src/native/liveActivitySync.ts
      via: "efeito de sincronização limitado a iOS; o import continua seguro em todas as plataformas"
      pattern: "Platform.OS"
    - from: modules/live-activity/index.ts
      to: LiveActivityModule
      via: "requireOptionalNativeModule somente no ramo iOS, wrappers retornam false se ausente"
      pattern: "requireOptionalNativeModule"
---

<objective>
Fecha a segurança de bootstrap multi-plataforma e repete, no iPhone, somente a
UAT dos caminhos alterados pelos planos 15-07/15-08. A implementação iOS
permanece o escritor real de ActivityKit; Android e web ganham ausência segura,
sem banner novo nem regressão do aviso não bloqueante D-12.

Purpose: fechar CR-03 e produzir a evidência final de fonte, comportamento,
TypeScript/Jest, skeleton nativo e iPhone exigida pelo verificador.
Output: bridge opcional, testes de importação sem mock do módulo e UAT física
curta, sem trabalho de Dynamic Island.
</objective>

<execution_context>
@/Users/phmarconato/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/phmarconato/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-CONTEXT.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-VERIFICATION.md
@.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tornar o bridge ActivityKit opcional fora de iOS e provar imports Android/web</name>
  <files>modules/live-activity/index.ts, App.tsx, __tests__/liveActivityPlatformImport.test.ts</files>
  <read_first>
    modules/live-activity/index.ts (linha 20: requireNativeModule avaliado no carregamento);
    modules/native-info/index.ts (análogo exato da forma pública de um bridge Expo local: tipo NativeModule, constante de módulo e wrapper async fino);
    App.tsx (linhas 3-18 e 29-32: import incondicional e efeito root atual);
    src/native/liveActivitySync.ts (API pública importada por App e comportamento esperado de wrappers booleanos);
    src/components/LiveActivityUnavailableBanner.tsx (guarda Platform.OS e comportamento não bloqueante D-12 a preservar);
    __tests__/liveActivitySync.test.ts (mock explícito usado apenas nos testes de sync, que este novo teste não pode reutilizar);
    .planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-VERIFICATION.md (gap 4 de bootstrap).
  </read_first>
  <behavior>
    - Teste RED Android: com Platform.OS android e sem LiveActivityModule, importar o bridge, liveActivitySync e App não lança; wrappers retornam false.
    - Teste RED web: com Platform.OS web e sem LiveActivityModule, os mesmos imports não lançam e nenhum efeito de sincronização iOS é iniciado.
    - Teste de preservação iOS: quando o optional module existe, os wrappers delegam start/update/end/reconcile ao módulo real; ausência no iOS retorna false e alimenta o fluxo já existente de falha não bloqueante.
  </behavior>
  <action>
    Em `modules/live-activity/index.ts`, usar `modules/native-info/index.ts` como
    análogo estrutural exato: preservar a forma pública de tipo `NativeModule`,
    constante do módulo e wrappers async finos com as assinaturas atuais. A divergência
    intencional é restrita à disponibilidade: Live Activity é um módulo Apple-only,
    portanto usar `requireOptionalNativeModule` apenas no ramo `Platform.OS === 'ios'`
    em vez do `requireNativeModule` obrigatório do NativeInfo, e retornar
    `Promise.resolve(false)` quando a plataforma não é iOS ou o módulo iOS não está
    disponível. Não lançar, não inserir módulo falso no runtime e não remover a
    capacidade de o sync observar `false` para alimentar o banner D-12.

    Em `App.tsx`, manter imports seguros e limitar a chamada de
    `reconcileOrphanActivities`/`initLiveActivitySync` ao ramo iOS do efeito root.
    Android e web não devem assinar um writer sem destino; o retorno de cleanup
    deve continuar correto. Preservar a montagem e a guarda já existente de
    `LiveActivityUnavailableBanner`, que continua sem renderizar fora de iOS.

    Criar `__tests__/liveActivityPlatformImport.test.ts` com módulos isolados por
    plataforma. Para cada caso Android e web, não usar `jest.mock` para
    `modules/live-activity` nem fornecer `LiveActivityModule`; importar bridge,
    sync e App após aplicar a plataforma, usando mocks somente para dependências
    visuais/navegação não relacionadas ao bridge. Confirmar que os imports não
    lançam, que os wrappers retornam false e que o efeito iOS não é iniciado.
    Cobrir também o caminho iOS presente e o iOS ausente para preservar a ponte
    real e o retorno de falha tratado pelo banner.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest __tests__/liveActivityPlatformImport.test.ts __tests__/liveActivitySync.test.ts __tests__/LiveActivityUnavailableBanner.test.tsx --runInBand</automated>
  </verify>
  <acceptance_criteria>
    Os casos Android e web importam App/sync sem qualquer mock de
    `modules/live-activity` e sem `LiveActivityModule` registrado. Eles não lançam
    e seus wrappers resolvem false. O caso iOS comprova delegação quando disponível
    e false quando ausente, preservando a cadeia que torna o banner D-12
    informativo e não bloqueante.
  </acceptance_criteria>
  <done>O bootstrap volta a ser seguro em Android/web sem degradar a Live Activity ou o aviso de falha no iOS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rechecagem física focalizada no iPhone dos cenários alterados</name>
  <files>Nenhum arquivo é alterado; esta task produz somente a evidência de UAT no SUMMARY.</files>
  <read_first>
    scripts/resign.sh (procedimento de rebuild/instalação aprovado na Fase 14);
    .planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-07-SUMMARY.md;
    .planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-08-SUMMARY.md;
    .planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-VERIFICATION.md (somente gaps CR-01, CR-02 e CR-04);
    .planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-CONTEXT.md (D-04, D-08, D-13 e D-14; Dynamic Island está deferida).
  </read_first>
  <what-built>Transição única resting → readyOvertime, overtime temporal clampado, deadline de inatividade por série concluída, fallback seguro update→start e bootstrap sem módulo nativo em Android/web.</what-built>
  <how-to-verify>Rodar primeiro a verificação automatizada abaixo e depois seguir literalmente o roteiro de descanso curto e inatividade de três horas descrito em action, no iPhone físico do dono.</how-to-verify>
  <action>
    No iPhone do dono, reconstruir e instalar com `npm run resign`. Iniciar uma
    sessão, concluir uma série com descanso curto, bloquear o aparelho e observar
    o card sem tocar no app: ao vencer restEndsAt ele deve mostrar Pronto/Série e
    o overtime deve mudar visivelmente de +0:00 para pelo menos +0:02, sem avançar
    série. Para a recuperação por inatividade, iniciar outra sessão active, não
    concluir séries por três horas, confirmar que o card saiu, alterar o draft da
    mesma sessão sem finalizar/cancelar e confirmar que o card reaparece; repetir
    finish/cancel antes de uma mutação pendente para confirmar que não reaparece.
    Não validar nem registrar Dynamic Island, comandos de Lock Screen, notificações,
    sons, next-action ou UX de registro: todos estão fora deste fechamento.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest __tests__/liveActivityPlatformImport.test.ts __tests__/liveActivitySync.test.ts __tests__/sessionModel.test.ts __tests__/liveActivityContentState.test.ts --runInBand && npm run verify:native</automated>
    <human-check>Após os comandos automáticos, o dono responde literalmente: rest_to_ready_overtime=PASS|FAIL, inactivity_timeout_recovery=PASS|FAIL, no_resurrection_after_finish_cancel=PASS|FAIL. Descrever o erro exato para cada FAIL.</human-check>
  </verify>
  <acceptance_criteria>
    A UAT contém resposta explícita para os três cenários alterados e não declara
    qualquer apresentação Dynamic Island como validada. Nenhum resultado é inferido
    por build ou compilação, conforme D-13/D-14.
  </acceptance_criteria>
  <done>Os caminhos iPhone modificados têm PASS/FAIL explícito do dono, somado às provas automatizadas, antes de LOCK-01/LOCK-03 voltarem a ser declarados concluídos.</done>
  <resume-signal>Reporte: rest_to_ready_overtime=PASS|FAIL, inactivity_timeout_recovery=PASS|FAIL, no_resurrection_after_finish_cancel=PASS|FAIL.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bootstrap multiplataforma → módulo nativo | Android/web não possuem a implementação Apple e não podem falhar ao importar o app. |
| Máquina de desenvolvimento → iPhone do dono | A UAT física confirma renderização/ciclo de vida que não é reproduzível por Jest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-09-01 | Denial of Service | bootstrap Android/web | high | mitigate | Bridge opcional e efeito root condicionado a iOS; imports sem mock provam ausência segura do módulo. |
| T-15-09-02 | Repudiation | UAT de Lock Screen | high | mitigate | Checklist físico com três respostas PASS/FAIL literais, precedido por Jest, TypeScript e skeleton nativo. |
| T-15-09-03 | Information Disclosure | banner iOS | low | accept | A mensagem já aprovada informa apenas indisponibilidade de Live Activities e continua invisível fora de iOS. |
</threat_model>

## Artifacts this phase produces

- Bridge `modules/live-activity/index.ts` que tolera a ausência do módulo fora do iOS.
- `__tests__/liveActivityPlatformImport.test.ts` com imports Android/web sem mock do bridge.
- Evidência física focalizada dos três cenários modificados no `15-09-SUMMARY.md`.

## Source coverage audit

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|----|------|------|--------|-------|
| GOAL | — | Card vivo, timer nativo e ciclo de vida correto | 15-07–15-09 | COVERED | Fecha os cinco gaps sem ampliar escopo. |
| REQ | LOCK-01 | Card recupera, não exibe exercício recusado e bootstrap não cai | 15-08, 15-09 | COVERED | |
| REQ | LOCK-02 | Descanso troca para Pronto e overtime é temporal sem tick JS | 15-07, 15-09 | COVERED | |
| REQ | LOCK-03 | Inatividade usa prazo correto e não ressuscita após finish/cancel | 15-08, 15-09 | COVERED | |
| RESEARCH | — | Text(timerInterval:) continua para countdown; formatter clampado é temporal | 15-07 | COVERED | Nenhuma dependência nova. |
| CONTEXT | D-01–D-03 | Layout existente do card e blocos reduzidos | 15-07, 15-08 | COVERED | Preservados; correções não redesenham o card. |
| CONTEXT | D-04 | Pronto + overtime crescente clampado | 15-07, 15-09 | COVERED | |
| CONTEXT | D-05–D-07 | Um ciclo por sessão, término/cancelamento corretos | 15-07, 15-08 | COVERED | |
| CONTEXT | D-08 | Três horas desde start/última série done | 15-08, 15-09 | COVERED | |
| CONTEXT | D-09–D-10 | Nunca auto-avança ao zerar | 15-07, 15-09 | COVERED | |
| CONTEXT | D-11–D-12 | Reconciliação e banner não bloqueante | 15-08, 15-09 | COVERED | |
| CONTEXT | D-13–D-14 | UAT física explícita, não inferida de build | 15-09 | COVERED | |

<verification>
Executar as verificações automatizadas da Task 1 e Task 2, incluindo
`npx tsc --noEmit`, Jest direcionado e `npm run verify:native`; então registrar a
resposta física focalizada. Não realizar deploy, operação de banco ou nova UAT de
Dynamic Island.
</verification>

<success_criteria>
Todos os oito must-haves do relatório tornam-se verificáveis: os cinco gaps têm
prova de fonte/comportamento/nativo e os três caminhos iPhone modificados têm
resposta física explícita, sem reabrir escopo deferido.
</success_criteria>

<output>
Create `.planning/phases/15-tela-bloqueada-ver-e-cronometrar/15-09-SUMMARY.md` when done
</output>
