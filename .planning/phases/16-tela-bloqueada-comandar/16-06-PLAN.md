---
phase: 16-tela-bloqueada-comandar
plan: 06
type: execute
wave: 2
depends_on: ["16-04", "16-05"]
files_modified: []
autonomous: false
requirements: [CMD-01, CMD-02]
gap_closure: true

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - statement: "No aparelho físico do dono, um force-quit seguido de toque em 'Concluir série' na tela bloqueada, seguido de reabertura do app e navegação para o treino ativo, mostra a série já concluída automaticamente (PASS-A) — a reconciliação de 16-04 aplica a intenção sem exigir conclusão manual, fechando a lacuna que 16-03-SUMMARY.md registrou como PASS-B"
      verification: backstop
    - statement: "Uma sequência de toques feitos com o app vivo (bridge JS registrada), seguida de um force-quit e reabertura da MESMA sessão, não duplica nem reaplica nenhuma ação já vista pelo dono enquanto o app estava vivo — nenhuma série avança sozinha, nenhum cronômetro se desloca sozinho, nenhuma série extra é pulada"
      verification: backstop
  artifacts: []
  key_links: []
  prohibitions:
    - statement: "Nunca reportar esta Sessão como aprovada sem a resposta explícita do dono no formato PASS/FAIL para todos os itens do runbook — 'buildou'/'compilou' nunca é critério de conclusão (mesmo D-14 herdado da Fase 15, mesma exigência já aplicada em 16-03-PLAN.md)"
      status: flagged-unverified
    - statement: "MUST NOT reinterpretar um resultado PASS-B (aplicação manual bem-sucedida, sem perda) como falha desta plano — o objetivo desta re-execução é confirmar PASS-A quando possível E, minimamente, confirmar que NENHUM dado foi perdido ou duplicado; um PASS-B sem duplicação ainda fecha os dois gaps do ponto de vista de integridade de dado, mesmo que a UX ideal (PASS-A) não se confirme neste aparelho específico"
      status: flagged-unverified
---

<objective>
Re-executa o cenário de force-quit do runbook físico de `16-03-PLAN.md` DEPOIS dos
fixes das Planos 16-04 (ordem de boot da reconciliação) e 16-05 (dedup/ack do caminho
quente), per exigência explícita do `gap_closure_contract`: "o fluxo de UAT física 16-03
(force-quit -> tap -> expected PASS-A behavior) deve ser re-executável após o fix".

Diferente de `16-03-PLAN.md` (que cobriu os 5 itens do runbook completo pela primeira
vez), esta plano foca em DOIS alvos específicos que só um aparelho físico decide: (1) o
cenário de force-quit agora produz PASS-A (aplicação automática) em vez do PASS-B
observado em `16-03-SUMMARY.md`; (2) uma sequência realista de toques "quentes" seguida
de force-quit NÃO duplica nenhuma ação — o próprio risco que o gap 2 (`16-VERIFICATION.md`)
identificou e que só se manifesta com o app genuinamente vivo, backgrounded, e depois
morto pelo sistema/dono. Os itens de regressão do runbook original (concluir_serie/
ajustar_descanso/pular_descanso/sem_lag) são revalidados en passant dentro do mesmo
roteiro, sem repetir o runbook inteiro linha a linha.

Purpose: fecha CMD-01 (critério de sucesso 3 do ROADMAP) com a garantia mais forte que
16-02-PLAN.md originalmente prometeu (aplicação automática, não só o fallback aceito
"app reaberto para concluir"), e confirma que o mecanismo de dedup da Plano 16-05
realmente impede duplicação em condições reais de aparelho — nenhuma das duas coisas é
verificável sem o iPhone físico do dono.
Output: resposta do dono, no formato PASS/FAIL explícito, para os itens do runbook
abaixo, registrada literalmente no SUMMARY.md desta plano.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-tela-bloqueada-comandar/16-03-SUMMARY.md
@.planning/phases/16-tela-bloqueada-comandar/16-04-PLAN.md
@.planning/phases/16-tela-bloqueada-comandar/16-05-PLAN.md
@scripts/resign.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Sessão física de re-execução: force-quit-então-toque (PASS-A esperado) + sequência quente sem duplicação</name>
  <precondition>As Planos 16-04 e 16-05 já foram executadas e commitadas (mesmo worktree/branch) — sem elas, o build não tem os fixes e o resultado desta sessão reproduziria o PASS-B já conhecido, sem nova informação.</precondition>
  <read_first>
    scripts/resign.sh (comando exato que o dono vai rodar);
    .planning/phases/16-tela-bloqueada-comandar/16-03-SUMMARY.md (o roteiro original e o resultado PASS-B a ser comparado);
    .planning/phases/16-tela-bloqueada-comandar/16-04-SUMMARY.md e 16-05-SUMMARY.md (o que foi de fato implementado, quaisquer desvios do plano)
  </read_first>
  <what-built>
    Dois fixes sobre o mecanismo de tela bloqueada já validado fisicamente em 16-03: (1)
    `reconcileLiveActivityIntents()` agora só roda DEPOIS que o app hidrata a sessão ativa
    (dentro de `startOrResume()`, não mais no boot cru de `App.tsx`) — a intenção de um
    toque no Lock Screen durante o force-quit deixa de ser perdida antes mesmo de o app
    saber que existe uma sessão para aplicá-la; (2) cada toque agora carrega um id
    estável, e uma entrega bem-sucedida com o app vivo remove sua PRÓPRIA entrada da fila
    durável (`ackIntentAction`) — uma ação já vista pelo dono enquanto o app estava aberto
    não fica mais acumulada para ser reaplicada num force-quit posterior.
  </what-built>
  <how-to-verify>
    Roteiro auto-contido — rode na ordem, com o iPhone conectado ao Mac por cabo:

    1. Rodar `npm run resign` neste terminal — reconstrói e instala o app com todo o
       código das Planos 16-04/16-05 (além do já existente das Planos 16-01/16-02).
    2. Abrir o app, logar, iniciar (ou retomar) uma sessão de treino real com pelo menos
       DOIS exercícios pendentes.

    PARTE 1 — sequência quente sem duplicação (fecha o gap 2):
    3. Ativar a primeira série, informar reps/carga reais AINDA COM O APP ABERTO (mesma
       ressalva de `16-03-PLAN.md`: REG-01 só chega na Fase 17, sem valores reais
       informados o botão da tela bloqueada legitimamente não tem o que gravar). Bloquear
       o iPhone SEM fechar o app (só a tela apaga — o app continua vivo/backgrounded).
    4. No Lock Screen, tocar "Concluir série". Desbloquear e confirmar no app que a série
       ficou `done` (comportamento já conhecido, PASS em 16-03). Aguardar uns 5 segundos
       (dá tempo do ack nativo completar).
    5. Bloquear de novo (app ainda vivo, sem force-quit) e tocar "+30s" no descanso da
       série seguinte. Desbloquear e confirmar que o timer aumentou ~30s uma única vez.
    6. AGORA force-quit o app (swipe-up no app switcher) — a fila durável do App Group,
       neste ponto, deveria estar VAZIA das duas ações dos passos 4-5 (já confirmadas +
       ack'd), com no máximo resíduo de ações não confirmadas se algo falhou.
    7. Reabrir o app e navegar para o treino. Confirmar que NADA mudou além do que já
       era esperado dos passos 4-5: a série do passo 4 continua `done` (não avançou
       sozinha de novo), o ajuste do passo 5 não se repetiu (o timer não pulou outros
       ~30s sozinho), e nenhuma série adicional foi pulada — item
       `sem_duplicacao=PASS|FAIL` (descrever exatamente o que divergiu, se houver).

    PARTE 2 — force-quit-então-toque, PASS-A esperado (fecha o gap 1):
    8. Com uma sessão ativa e a Live Activity visível, force-quit o app de verdade
       (swipe-up no app switcher) SEM tocar em nada antes.
    9. Bloquear o iPhone e tocar "Concluir série" no Lock Screen (app já está morto neste
       momento).
    10. Reabrir o app pelo ícone da Home Screen e NAVEGAR PARA O TREINO ATIVO (não basta
        abrir o app — entre na tela do treino, é o gatilho real da reconciliação depois
        do fix da Plano 16-04). Observar o que acontece SEM tocar em nada ainda:
        (a) a série já aparece `done` assim que a tela do treino termina de carregar —
        reportar `force_quit_toque=PASS-A`; OU (b) a série ainda está `active`/`pending`,
        permitindo concluir manualmente sem erro/travamento — reportar
        `force_quit_toque=PASS-B` (mesmo comportamento aceito em 16-03, mas agora
        inesperado se os fixes funcionaram — descrever o que foi observado). Qualquer
        travamento, erro visível, ou série em estado inconsistente é
        `force_quit_toque=FAIL`.
    11. Regressão rápida dos itens já PASS em 16-03 (não repetir o roteiro inteiro,
        só confirmar que nada quebrou): tocar "Pular" numa série de descanso e
        confirmar que SÓ a série seguinte ativou — item `regressao_geral=PASS|FAIL`.

    12. Reportar EXATAMENTE neste formato: `sem_duplicacao=PASS|FAIL`,
        `force_quit_toque=PASS-A|PASS-B|FAIL`, `regressao_geral=PASS|FAIL`, com qualquer
        divergência do esperado descrita por item em FAIL (ou em PASS-B, já que PASS-B
        ainda é aceitável per a prohibition desta plano, mas precisa de descrição do que
        foi observado para registro).
  </how-to-verify>
  <acceptance_criteria>A resposta do dono cobre os três itens no formato explícito acima antes desta plano ser considerada concluída — nenhum item é presumido PASS por ausência de resposta ou por "compilou"/"buildou". Um resultado PASS-B em force_quit_toque (sem duplicação) ainda fecha os gaps do ponto de vista de integridade de dado — não é reinterpretado como falha desta plano, mas é registrado literalmente para follow-up (mesmo padrão de `.planning/todos/pending/force-quit-reconciliacao-pass-b.md` já aberto por 16-03).</acceptance_criteria>
  <resume-signal>Reporte: sem_duplicacao=PASS|FAIL, force_quit_toque=PASS-A|PASS-B|FAIL, regressao_geral=PASS|FAIL, com descrição de qualquer item que não seja um PASS limpo.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iPhone físico do dono | Mesmo aparelho já usado nas sessões físicas das Fases 14/15/16 (16-03) — nenhum trust boundary novo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-06-01 | Repudiation | Resultado da sessão física reportado de forma imprecisa/inferida | high | mitigate | Formato de resposta obrigatório por item (resume-signal), nunca inferido, sempre citado literalmente no SUMMARY.md — mesmo padrão de T-16-03-01 |
| T-16-06-02 | Tampering | Um PASS-B reinterpretado silenciosamente como falha (ou um FAIL reinterpretado como PASS-B aceitável) para "fechar" a fase mais rápido | medium | mitigate | Prohibition explícita nesta plano proíbe as duas reinterpretações — PASS-B sem duplicação fecha os gaps de integridade, mas é sempre registrado literalmente para follow-up, nunca embelezado nem rebaixado |
</threat_model>

## Artifacts this phase produces

Nenhum artefato de código nesta plano — só a resposta do dono, registrada no SUMMARY.md
(mesmo padrão de `16-03-PLAN.md`).

<verification>
Nenhuma verificação automatizada aplicável — este checkpoint depende inteiramente da
confirmação física do dono no aparelho real.
</verification>

<success_criteria>
O dono reportou PASS/FAIL explícito para os três itens do runbook (sem_duplicacao,
force_quit_toque, regressao_geral), incluindo qual dos comportamentos aceitáveis
(PASS-A/PASS-B) o cenário de force-quit produziu desta vez — e, no mínimo, confirmando
que nenhuma duplicação de gravação ocorreu (o requisito não-negociável desta rodada de
fechamento de gap).
</success_criteria>

<output>
Create `.planning/phases/16-tela-bloqueada-comandar/16-06-SUMMARY.md` when done — deve citar literalmente a resposta do dono em todos os itens.
</output>
