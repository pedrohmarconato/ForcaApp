---
phase: 16-tela-bloqueada-comandar
plan: 09
type: execute
wave: 3
depends_on: ["16-07", "16-08"]
files_modified: []
autonomous: false
requirements: [CMD-01, CMD-02]
gap_closure: true

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - { statement: "No aparelho físico do dono, um force-quit com reps/carga JÁ informados antes do force-quit, seguido de toque em 'Concluir série' na tela bloqueada e reabertura do app com navegação para o treino ativo, mostra a série já concluída automaticamente (PASS-A) — sem erro visível 'Informe repetições e carga...', sem série travada", verification: backstop }
    - { statement: "Uma sequência de toques feitos com o app vivo, seguida de force-quit e reabertura da mesma sessão, continua sem duplicar nem reaplicar nenhuma ação (sem_duplicacao permanece PASS — regressão de 16-05/16-06 não pode voltar)", verification: backstop }
    - { statement: "Tocar 'Pular' na ÚLTIMA série de um exercício, inclusive depois de um completeSet() deliberadamente reprovado (reps/carga não informados) ter deixado uma série travada 'active', ativa CORRETAMENTE a próxima série do exercício seguinte — não retorna à série travada (regressao_geral passa a PASS)", verification: backstop }
  artifacts: []
  key_links: []
  prohibitions:
    - statement: "Nunca reportar esta sessão como aprovada sem a resposta explícita do dono no formato PASS/FAIL para todos os itens do runbook — 'buildou'/'compilou' nunca é critério de conclusão (mesmo padrão de 16-03-PLAN.md/16-06-PLAN.md)"
      status: flagged-unverified
    - statement: "MUST NOT marcar CMD-01/CMD-02 como completos em REQUIREMENTS.md ou em qualquer artefato desta plano — mesmo com PASS-A confirmado nos três itens, a marcação de requisito completo é responsabilidade exclusiva da PRÓXIMA rodada de /gsd-verify-work, nunca desta execução (repetir o erro revertido no commit 82c23c8 é uma prohibition explícita desta plano)"
      status: flagged-unverified
---

<objective>
Re-executa o runbook físico de `16-06-PLAN.md` DEPOIS dos fixes de D1 (Plano
16-07 — peek não-destrutivo + ack condicionado ao resultado real de
`completeSet()`), D2 (Plano 16-08 — `setReps`/`setLoad` persistindo via
`saveDraft`, nos dois ramos de retomada) e D3 (Plano 16-08 — invariante de
série ativa única em `activateSet()`). ROADMAP Success Criterion 3 exige UAT
no aparelho físico do dono — nenhum dos três defeitos é verificável só por
teste automatizado no cenário de cold-launch real.

Diferente de `16-06-PLAN.md` (que já confirmou `sem_duplicacao=PASS` mas
reprovou os outros dois itens), esta plano foca em UM diferencial crítico de
protocolo: desta vez o roteiro EXIGE informar reps/carga reais ANTES do
force-quit (o próprio dono já relatou em `16-06-SUMMARY.md` que "funciona mas
só quando eu coloco as reps e cargas antes de dar o force quit" — a causa
raiz era D1+D2, não a premissa do dono) e inclui um passo deliberado para
reproduzir e validar a correção de D3 (série travada por rejeição + "Pular"
no exercício seguinte).

Purpose: fecha CMD-01 (critério de sucesso 3 do ROADMAP) com PASS-A real, e
CMD-02 na parte de "Pular" que a rodada anterior expôs como regressão
(regressao_geral). Nenhuma das três garantias é verificável sem o iPhone
físico do dono.
Output: resposta do dono, no formato PASS/FAIL explícito, para os três itens
do runbook, registrada literalmente no SUMMARY.md desta plano — sem marcar
CMD-01/CMD-02 completos (isso é escopo da próxima verificação).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-tela-bloqueada-comandar/16-06-SUMMARY.md
@.planning/phases/16-tela-bloqueada-comandar/16-07-PLAN.md
@.planning/phases/16-tela-bloqueada-comandar/16-08-PLAN.md
@scripts/resign.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Release reassinado + verificação de proveniência</name>
  <files></files>
  <read_first>
    scripts/resign.sh (comando exato a rodar);
    .planning/phases/16-tela-bloqueada-comandar/16-06-SUMMARY.md (seção "Proveniência do build" — o padrão de verificação a replicar: confirmar que o bytecode Hermes do bundle contém as strings/símbolos novos desta rodada, e que `git status` está limpo no commit que vai para o aparelho)
  </read_first>
  <action>
    Rodar `npm run resign` para reconstruir e instalar o app com o código das
    Planos 16-07 (peekAll/peekIntentQueue/ack condicionado) e 16-08
    (setReps/setLoad->saveDraft, applyServerSetLogs overlay, activateSet com
    invariante de série única) já mergeadas. Confirmar proveniência ANTES de
    convocar a sessão física do dono, mesmo padrão de `16-06-SUMMARY.md`: (a)
    `git status` limpo no commit que gerou o build instalado; (b) o bytecode
    Hermes de `main.jsbundle` contém as strings novas relevantes (ex.:
    `peekQueuedLiveActivityIntents`, `localSetByPlannedSet`,
    `deactivateOtherActiveSets` — ou os identificadores que sobreviverem à
    minificação, se houver; se o bundle não for legível por string search,
    confirmar por outro meio determinístico, ex.: hash do bundle mudou em
    relação ao build de 16-06); (c) os arquivos Swift alterados em 16-07
    (`IntentActionQueue.swift`, `LiveActivityModule.swift`) foram de fato
    recompilados neste build (timestamp do artefato / confirmação do
    `verify:native` já verde na Plano 16-07 Task 2). Registrar os achados de
    proveniência no SUMMARY.md desta plano, no mesmo formato de
    `16-06-SUMMARY.md`.

    IMPORTANTE (constraint do ambiente, per memória do projeto e
    `15-04-PLAN.md`): o build Debug NÃO roda neste aparelho (Fase 14) — o
    build TEM que ser Release. Antes da sessão física, confirmar também qual
    Supabase o build aponta: produção (`zanqygwsgxkyjiuhrzju`, per
    `15-04-PLAN.md`/`STATE.md` desde a Fase 15) OU local via LAN (per memória
    de sessões anteriores do projeto) — os dois registros documentais
    divergem; NÃO presumir qual vale sem checar o `.env` ativo no momento
    deste build (sem imprimir a anon key). Registrar no SUMMARY qual dos dois
    o build usa, para que o runbook da Task 2 seja interpretado corretamente
    (dados gravados no ambiente correto).
  </action>
  <verify>
    <automated>npm run resign</automated>
  </verify>
  <acceptance_criteria>
    `npm run resign` sai 0. O SUMMARY.md desta plano registra: hash/commit do
    build, confirmação de que os arquivos Swift de 16-07 foram recompilados, e
    qual ambiente Supabase o build usa — os três fatos citados literalmente,
    não presumidos.
  </acceptance_criteria>
  <done>Build Release reassinado, instalado no aparelho do dono, com proveniência confirmada e registrada antes de iniciar a sessão física.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Sessão física de re-execução: force_quit_toque (PASS-A esperado), sem_duplicacao (regressão), regressao_geral (Pular após rejeição)</name>
  <precondition>Task 1 desta plano já confirmou o build Release instalado reflete as Planos 16-07 e 16-08 mergeadas — sem essa confirmação, o resultado desta sessão reproduziria os FAILs já conhecidos de 16-06-SUMMARY.md, sem nova informação.</precondition>
  <read_first>
    .planning/phases/16-tela-bloqueada-comandar/16-06-SUMMARY.md (o roteiro anterior e os dois FAILs a comparar, inclusive a transcrição literal do dono: "funciona mas só quando eu coloco as reps e cargas antes de dar o force quit pois com a tela bloqueada nao consigo colocar");
    .planning/phases/16-tela-bloqueada-comandar/16-07-PLAN.md e 16-08-PLAN.md (o que foi de fato implementado)
  </read_first>
  <what-built>
    Três fixes sobre o mecanismo de tela bloqueada testado em 16-06: (1) a fila
    do App Group agora é lida sem destruir as entradas (`peekAll`), e uma
    entrada só é confirmada/removida depois de saber se foi de fato aplicada —
    uma rejeição de validação não perde mais o toque; (2) `setReps`/`setLoad`
    agora persistem em disco a cada toque, e a retomada (nos dois ramos:
    offline e reconciliada com o servidor) preserva esses valores; (3)
    `activateSet()` agora garante que só uma série fica `active` por vez —
    "Pular" não volta mais para uma série travada por uma rejeição anterior.
  </what-built>
  <how-to-verify>
    Roteiro auto-contido — rode na ordem, com o iPhone conectado ao Mac por
    cabo (ou já com o build da Task 1 instalado via `npm run resign`):

    1. Abrir o app, logar, iniciar (ou retomar) uma sessão de treino real com
       PELO MENOS DOIS exercícios pendentes, cada um com pelo menos duas
       séries.

    PARTE 1 — sem_duplicacao, regressão (deve continuar PASS, mesmo roteiro de 16-06):
    2. Ativar a primeira série, informar reps/carga reais AINDA COM O APP
       ABERTO. Bloquear o iPhone SEM fechar o app (só a tela apaga).
    3. No Lock Screen, tocar "Concluir série". Desbloquear e confirmar que a
       série ficou `done`. Aguardar uns 5 segundos.
    4. Bloquear de novo (app ainda vivo) e tocar "+30s" no descanso da série
       seguinte. Desbloquear e confirmar que o timer aumentou ~30s uma única
       vez.
    5. Force-quit o app (swipe-up no app switcher). Reabrir e navegar para o
       treino. Confirmar que NADA mudou além do esperado dos passos 3-4 (a
       série do passo 3 continua `done`, o ajuste do passo 4 não se repetiu) —
       item `sem_duplicacao=PASS|FAIL`.

    PARTE 2 — force_quit_toque, PASS-A esperado (a diferença central desta
    rodada: reps/carga informados ANTES do force-quit, per D2):
    6. Ativar a próxima série pendente. AINDA COM O APP ABERTO, informar reps
       e carga reais no app (não pular esta etapa — sem valores, a rejeição é
       o comportamento CORRETO e esperado, não um bug).
    7. Force-quit o app de verdade (swipe-up no app switcher) SEM tocar em
       "Concluir série" antes.
    8. Bloquear o iPhone e tocar "Concluir série" no Lock Screen (app já está
       morto neste momento).
    9. Reabrir o app pelo ícone da Home Screen e navegar para o treino ativo
       (entrar na tela do treino é o gatilho real da reconciliação). Observar:
       (a) a série já aparece `done` sem nenhum erro visível — reportar
       `force_quit_toque=PASS-A`; (b) aparece a mensagem "Informe repetições e
       carga antes de concluir a série" mesmo com os valores já informados no
       passo 6 — reportar `force_quit_toque=FAIL` (regressão de D2, não
       esperada); (c) qualquer outro travamento/erro/série inconsistente —
       `force_quit_toque=FAIL` (descrever o que foi observado).

    PARTE 3 — regressao_geral, invariante de série única (D3):
    10. Ativar QUALQUER série de um exercício com pelo menos duas séries
        restantes. NÃO informar reps/carga. Tocar "Concluir série" no app
        (não no Lock Screen) — confirmar que aparece o erro esperado "Informe
        repetições e carga..." e a série permanece visível como ativa (esta é
        a rejeição deliberada que deixa a série "travada", reproduzindo o
        gatilho real do bug).
    11. Sem informar reps/carga na série travada, ir até a ÚLTIMA série
        pendente de OUTRO exercício do treino (qualquer um diferente do
        exercício do passo 10), ativá-la (ou aguardar o fluxo natural até
        chegar nela) e tocar "Pular" no descanso dela.
    12. Confirmar que a série ATIVADA depois do "Pular" é a série CORRETA (a
        próxima do treino, não a travada do passo 10) — item
        `regressao_geral=PASS|FAIL` (descrever exatamente o que ativou, se
        divergir do esperado).

    13. Reportar EXATAMENTE neste formato: `sem_duplicacao=PASS|FAIL`,
        `force_quit_toque=PASS-A|FAIL` (PASS-B não é mais um resultado
        aceitável nesta rodada — com D2 corrigido, reps/carga já informados
        DEVEM sobreviver; um PASS-B aqui indica que D1 ou D2 não fecharam de
        fato e conta como FAIL para efeito desta plano), `regressao_geral=PASS|FAIL`,
        com qualquer divergência do esperado descrita por item.
  </how-to-verify>
  <acceptance_criteria>A resposta do dono cobre os três itens no formato explícito acima antes desta plano ser considerada concluída — nenhum item é presumido PASS por ausência de resposta ou por "compilou"/"buildou". Nenhum dos três itens é marcado como fechando CMD-01/CMD-02 em REQUIREMENTS.md ou em qualquer outro artefato desta plano — essa marcação é escopo exclusivo da próxima rodada de /gsd-verify-work, mesmo com PASS/PASS-A/PASS nos três itens.</acceptance_criteria>
  <resume-signal>Reporte: sem_duplicacao=PASS|FAIL, force_quit_toque=PASS-A|FAIL, regressao_geral=PASS|FAIL, com descrição de qualquer item que não seja um PASS limpo.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iPhone físico do dono | Mesmo aparelho já usado nas sessões físicas das Fases 14/15/16 (16-03, 16-06) — nenhum trust boundary novo |
| Build instalado vs. código-fonte mergeado | A sessão física só tem valor se o build instalado de fato contém os fixes de 16-07/16-08 — proveniência verificada explicitamente na Task 1, não presumida |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-09-01 | Tampering | Sessão de UAT rodando contra um build que NÃO contém de fato os fixes de 16-07/16-08 (build stale/errado instalado) | high | mitigate | Task 1 verifica proveniência (git status limpo, símbolos/hash do bundle, recompilação Swift confirmada) ANTES de convocar a sessão física — mesmo padrão de `16-06-SUMMARY.md` |
| T-16-09-02 | Repudiation | Resultado da sessão física reportado de forma imprecisa/inferida, ou PASS-B silenciosamente aceito como equivalente a PASS-A nesta rodada | high | mitigate | Formato de resposta obrigatório por item, citado literalmente no SUMMARY.md; PASS-B é explicitamente redefinido como FAIL para esta rodada específica (D2 deveria eliminar o cenário que produzia PASS-B) |
</threat_model>

## Artifacts this phase produces

Nenhum artefato de código nesta plano — só a resposta do dono e a verificação
de proveniência do build, registradas no SUMMARY.md (mesmo padrão de
`16-03-PLAN.md`/`16-06-PLAN.md`).

<verification>
Nenhuma verificação automatizada substitui a confirmação física do dono para
os três itens do runbook — este é o único caminho de aceitação para CMD-01
critério de sucesso 3 e para a regressão de "Pular" (CMD-02).
</verification>

<success_criteria>
O dono reportou PASS/FAIL explícito para os três itens do runbook
(sem_duplicacao, force_quit_toque, regressao_geral), com PASS-A confirmado
(não PASS-B) para force_quit_toque quando reps/carga foram informados antes
do force-quit — a garantia mais forte que 16-02-PLAN.md originalmente
prometeu. CMD-01/CMD-02 permanecem formalmente `Gaps Found` em
REQUIREMENTS.md até a próxima rodada de `/gsd-verify-work` confirmar
independentemente.
</success_criteria>

<output>
Create `.planning/phases/16-tela-bloqueada-comandar/16-09-SUMMARY.md` when done — deve citar literalmente a resposta do dono em todos os itens.
</output>
