---
phase: 16-tela-bloqueada-comandar
plan: 11
type: execute
wave: 2
depends_on: ["16-10"]
files_modified: []
autonomous: false
requirements: [CMD-01, CMD-02]
gap_closure: true

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - { statement: "No aparelho físico do dono, ajustar a carga de uma série usando SÓ os botões -/+ (stepLoad — nunca o campo de texto, nunca 'usar sugestão'), depois force-quit imediato do app, depois reabrir e tocar 'Concluir série' na tela bloqueada, mostra a série já concluída automaticamente — sem o erro 'Informe repetições e carga...'", verification: backstop }
    - { statement: "No aparelho físico do dono, num exercício de métrica tempo/tempo_distancia (cardio/isometria), informar a duração pelo seletor, depois force-quit imediato, depois reabrir e tocar 'Concluir série' na tela bloqueada, mostra a série já concluída automaticamente", verification: backstop }
    - { statement: "A resposta do dono confirma explicitamente QUAL caminho de UI foi usado em cada um dos dois testes (stepper vs. campo de texto; exercício de métrica tempo vs. carga_reps) — não uma frase agregada cobrindo os dois, a mesma ambiguidade que tornou a evidência de 16-09 inconclusiva para CR-01", verification: backstop }
  artifacts: []
  key_links: []
  prohibitions:
    - statement: "MUST NOT marcar CMD-01/CMD-02 como completos em REQUIREMENTS.md ou em qualquer artefato desta plano — mesmo com PASS confirmado nos dois testes, a marcação de requisito completo é responsabilidade exclusiva da PRÓXIMA rodada de /gsd-verify-work (mesma prohibition explícita de 16-09-PLAN.md, repetir o erro revertido no commit 82c23c8 continua proibido)"
      status: flagged-unverified
    - statement: "MUST NOT aceitar uma resposta agregada do dono (ex.: 'tudo passou') cobrindo os dois testes desta plano como evidência suficiente — cada teste exige confirmação explícita e individual de QUAL caminho de UI foi usado, exatamente a lacuna de cobertura que tornou a evidência de 16-09-SUMMARY.md inconclusiva para CR-01"
      status: flagged-unverified
---

<objective>
Re-executa, no aparelho físico do dono, os dois testes que `16-VERIFICATION.md`
listou em "Human Verification Required" — ambos desenhados especificamente
para fechar a lacuna de cobertura que a rodada anterior (16-09) deixou: a
resposta agregada do dono ("todas foram pass agora") não distinguia qual
caminho de UI havia sido exercitado, então não provava (nem refutava) CR-01.

Diferente de `16-09-PLAN.md` (que testava `force_quit_toque` via qualquer
interação, provavelmente o campo de texto — `setReps`/`setLoad`, que já
persistiam desde antes de 16-09), esta plano EXIGE explicitamente que o
runbook use SÓ o stepper de carga (`stepLoad`, o fix da Plano 16-10 Task 1)
no primeiro teste, e um exercício de métrica `tempo`/`tempo_distancia`
(`setDuration`, o fix da Plano 16-10 Task 2) no segundo — os dois caminhos
que o código antes de 16-10 provavelmente reprovava, per leitura direta do
código nesta rodada de verificação.

Purpose: fecha CMD-01 (critério de sucesso 3 do ROADMAP) com evidência que
distingue qual caminho de UI foi usado — não uma resposta agregada.
Output: resposta do dono, por teste, com o caminho de UI usado nomeado
explicitamente, registrada literalmente no SUMMARY.md — sem marcar
CMD-01/CMD-02 completos (escopo exclusivo da próxima verificação).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-tela-bloqueada-comandar/16-VERIFICATION.md
@.planning/phases/16-tela-bloqueada-comandar/16-09-SUMMARY.md
@.planning/phases/16-tela-bloqueada-comandar/16-10-PLAN.md
@scripts/resign.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Release reassinado + verificação de proveniência do fix desta rodada (16-10)</name>
  <files></files>
  <read_first>
    scripts/resign.sh (comando exato a rodar);
    .planning/phases/16-tela-bloqueada-comandar/16-09-SUMMARY.md (seção "Proveniência do build" — o padrão de verificação a replicar: git status limpo, bytecode do bundle contendo strings novas, ambiente Supabase confirmado sem imprimir a anon key)
  </read_first>
  <action>
    Rodar `npm run resign` para reconstruir e instalar o app com o código da
    Plano 16-10 (stepLoad/setDuration/setDistance/setRir/setEffort
    persistindo via saveDraft) já mergeado. Confirmar proveniência ANTES de
    convocar a sessão física: (a) `git status` limpo no commit que gerou o
    build instalado; (b) o bytecode Hermes de `main.jsbundle` contém as
    strings novas dos warns fire-and-forget introduzidos pela Plano 16-10
    (ex.: a mensagem de warn de `stepLoad` e a de `setDuration` — strings de
    texto literal sobrevivem à compilação Hermes mesmo quando nomes de
    variável local não sobrevivem); (c) confirmar qual Supabase o build usa
    (produção `zanqygwsgxkyjiuhrzju` OU local via LAN, per divergência já
    registrada em `16-09-SUMMARY.md`) checando o `.env` ativo sem imprimir a
    anon key. Registrar os três achados no SUMMARY.md desta plano.

    IMPORTANTE (constraint do ambiente, per memória do projeto): o build
    Debug NÃO roda neste aparelho — o build TEM que ser Release.
  </action>
  <verify>
    <automated>npm run resign</automated>
  </verify>
  <acceptance_criteria>
    `npm run resign` sai 0. O SUMMARY.md desta plano registra: hash/commit do
    build, confirmação de que as strings novas da Plano 16-10 estão no
    bundle, e qual ambiente Supabase o build usa — os três fatos citados
    literalmente, não presumidos.
  </acceptance_criteria>
  <done>Build Release reassinado, instalado no aparelho do dono, com proveniência do fix de 16-10 confirmada e registrada antes de iniciar a sessão física.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Sessão física: force-quit via stepper de carga (Teste 1) e via duração de cardio/isometria (Teste 2), com caminho de UI confirmado por item</name>
  <precondition>Task 1 desta plano já confirmou que o build Release instalado reflete o código da Plano 16-10 (stepLoad/setDuration persistindo via saveDraft) — sem essa confirmação, o resultado desta sessão reproduziria os FAILs já suspeitos por leitura de código em 16-VERIFICATION.md, sem nova informação.</precondition>
  <read_first>
    .planning/phases/16-tela-bloqueada-comandar/16-VERIFICATION.md (seção "Human Verification Required" — os dois testes exatos que esta task reexecuta, e a ressalva de honestidade sobre a resposta agregada de 16-09);
    .planning/phases/16-tela-bloqueada-comandar/16-09-SUMMARY.md (o que já foi testado em 16-09 — para NÃO repetir o mesmo caminho de UI, já que aquela sessão provavelmente usou o campo de texto, não o stepper)
  </read_first>
  <what-built>
    Cinco funções da store (`stepLoad`, `setDuration`, `setDistance`,
    `setRir`, `setEffort`) agora persistem em disco a cada toque/seleção, via
    `saveDraft` fire-and-forget — mesmo mecanismo que `setReps`/`setLoad` já
    usavam desde a Plano 16-08. As duas mais críticas para `canCompleteSet()`
    são `stepLoad` (o stepper de carga, interação primária do milestone) e
    `setDuration` (o único campo que fecha a conclusão de séries de
    cardio/isometria).
  </what-built>
  <how-to-verify>
    Roteiro auto-contido — rode na ordem, com o iPhone conectado ao Mac por
    cabo (ou já com o build da Task 1 instalado via `npm run resign`):

    PARTE 1 — Teste 1: force-quit depois do stepper de carga (nunca o campo de texto):
    1. Abrir o app, logar, iniciar (ou retomar) uma sessão de treino real com
       um exercício de carga × reps NÃO-bodyweight (ex.: qualquer exercício
       de musculação com barra/halter).
    2. Ativar a série. AINDA COM O APP ABERTO, informar reps SÓ pelo campo de
       texto de reps (isso já persistia desde 16-08, não é o alvo deste
       teste). Para a CARGA, usar EXCLUSIVAMENTE os botões `-`/`+` do
       stepper — NÃO tocar no campo de texto de carga, NÃO tocar em "usar
       sugestão". Confirmar visualmente que o valor de carga mudou na tela
       via o stepper.
    3. Force-quit o app de verdade (swipe-up no app switcher) SEM tocar em
       "Concluir série" antes.
    4. Bloquear o iPhone e tocar "Concluir série" no Lock Screen (app já está
       morto neste momento).
    5. Reabrir o app pelo ícone da Home Screen e navegar para o treino ativo
       (entrar na tela do treino é o gatilho real da reconciliação). Observar:
       (a) a série já aparece `done` sem nenhum erro visível — reportar
       `teste_1_stepper=PASS`; (b) aparece "Informe repetições e carga antes
       de concluir a série" mesmo com a carga já ajustada pelo stepper no
       passo 2 — reportar `teste_1_stepper=FAIL`; (c) qualquer outro
       travamento/erro — `teste_1_stepper=FAIL` (descrever o observado).

    PARTE 2 — Teste 2: force-quit depois de informar duração de cardio/isometria:
    6. Em outra série (ou outra sessão), escolher um exercício de métrica
       `tempo` ou `tempo_distancia` (cardio ou isometria — ex.: caminhada,
       prancha). Ativar a série.
    7. AINDA COM O APP ABERTO, informar a duração pelo seletor de tempo do
       app (não informar reps nem carga — esses campos não existem/não são
       exigidos para esse tipo de exercício).
    8. Force-quit o app de verdade (swipe-up no app switcher) SEM tocar em
       "Concluir série" antes.
    9. Bloquear o iPhone e tocar "Concluir série" no Lock Screen.
    10. Reabrir o app pelo ícone da Home Screen e navegar para o treino
        ativo. Observar: (a) a série já aparece `done` sem nenhum erro
        visível — reportar `teste_2_duracao=PASS`; (b) qualquer erro/série
        travada/reprovada — `teste_2_duracao=FAIL` (descrever o observado,
        inclusive se a mensagem de erro menciona reps/carga em vez de
        duração — isso indicaria um bug adicional fora do escopo desta
        rodada, e deve ser registrado como achado novo, não como FAIL do
        item).

    11. Reportar EXATAMENTE neste formato, por item, confirmando o CAMINHO DE
        UI usado em cada um (não uma frase agregada cobrindo os dois):
        `teste_1_stepper=PASS|FAIL (caminho: SÓ stepper de carga, confirmar
        que o campo de texto de carga não foi tocado)`,
        `teste_2_duracao=PASS|FAIL (caminho: exercício de métrica tempo/
        tempo_distancia, nome do exercício usado)`. Qualquer divergência do
        esperado descrita por item.
  </how-to-verify>
  <acceptance_criteria>A resposta do dono cobre os dois itens no formato explícito acima, com o caminho de UI de cada um confirmado individualmente — uma resposta agregada sem essa distinção (ex.: "tudo passou", sem detalhar) NÃO é aceita como conclusão desta task; peça esclarecimento item a item antes de encerrar. Nenhum dos dois itens é marcado como fechando CMD-01/CMD-02 em REQUIREMENTS.md ou em qualquer outro artefato desta plano — essa marcação é escopo exclusivo da próxima rodada de /gsd-verify-work.</acceptance_criteria>
  <resume-signal>Reporte, item a item: teste_1_stepper=PASS|FAIL (com confirmação de que só o stepper de carga foi usado), teste_2_duracao=PASS|FAIL (com o nome do exercício de tempo/tempo_distancia usado), com descrição de qualquer item que não seja um PASS limpo.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iPhone físico do dono | Mesmo aparelho já usado nas sessões físicas das Fases 14/15/16 (16-03, 16-06, 16-09) — nenhum trust boundary novo |
| Build instalado vs. código-fonte mergeado | A sessão física só tem valor se o build instalado de fato contém os fixes da Plano 16-10 — proveniência verificada explicitamente na Task 1, não presumida |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-11-01 | Tampering | Sessão de UAT rodando contra um build que NÃO contém de fato os fixes da Plano 16-10 (build stale/errado instalado) | high | mitigate | Task 1 verifica proveniência (git status limpo, strings novas no bundle, ambiente Supabase confirmado) ANTES de convocar a sessão física, mesmo padrão de 16-09-SUMMARY.md |
| T-16-11-02 | Repudiation | Resultado da sessão física reportado de forma agregada/imprecisa, repetindo a mesma ambiguidade que tornou a evidência de 16-09 inconclusiva para CR-01 | high | mitigate | Formato de resposta obrigatório por item, exigindo o caminho de UI usado nomeado explicitamente; uma resposta agregada é explicitamente rejeitada pelo acceptance_criteria desta task |
</threat_model>

## Artifacts this phase produces

Nenhum artefato de código nesta plano — só a resposta do dono e a verificação
de proveniência do build, registradas no SUMMARY.md (mesmo padrão de
`16-09-PLAN.md`).

<verification>
Nenhuma verificação automatizada substitui a confirmação física do dono para
os dois testes deste runbook — este é o único caminho de aceitação para
CMD-01 critério de sucesso 3, especificamente para os dois caminhos de UI
(stepper de carga; duração de cardio/isometria) que a Plano 16-08/16-09 não
distinguiu.
</verification>

<success_criteria>
O dono reportou PASS/FAIL explícito para os dois testes deste runbook, com o
caminho de UI de cada um confirmado individualmente (não uma resposta
agregada). CMD-01/CMD-02 permanecem formalmente `Gaps Found` em
REQUIREMENTS.md até a próxima rodada de `/gsd-verify-work` confirmar
independentemente.
</success_criteria>

<output>
Create `.planning/phases/16-tela-bloqueada-comandar/16-11-SUMMARY.md` when done — deve citar literalmente a resposta do dono, item a item, com o caminho de UI de cada teste.
</output>
