---
phase: 16-tela-bloqueada-comandar
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - App.tsx
  - src/store/activeSessionStore.ts
  - __tests__/liveActivityIntentQueue.test.ts
  - __tests__/activeSessionStore.test.ts
autonomous: true
requirements: [CMD-01, CMD-02]
gap_closure: true

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "reconcileLiveActivityIntents() chamado com draft ainda não hidratado (null) NÃO drena a fila durável do App Group — a entrada permanece intacta e disponível para uma tentativa posterior, em vez de ser lida e perdida para sempre (fecha 16-VERIFICATION.md gap 1 / 16-REVIEW.md CR-01)"
    - "startOrResume() chama reconcileLiveActivityIntents() DEPOIS de popular draft com status 'active', em TODOS os ramos da função que chegam a esse estado (retomada offline sem rede, retomada reconciliada com o servidor, sessão sem rascunho local) — a chamada não vive mais no useEffect de montagem da raiz de App.tsx, que rodava antes de qualquer hidratação"
    - "Um teste automatizado reproduz a ordem real do boot (draft === null no momento da primeira chamada a reconcileLiveActivityIntents, sem setState prévio) e prova que, depois que o draft é hidratado (segunda chamada), a mesma entrada antes preservada é agora aplicada — documentando a transição PASS-B -> PASS-A observada em 16-03-SUMMARY.md"
    - "startOrResume() dispara reconcileLiveActivityIntents() de fato quando resolve para um draft ativo — provado por um teste de integração em __tests__/activeSessionStore.test.ts que espia drainQueuedLiveActivityIntents (não reimplementa a lógica de aplicação, já coberta em __tests__/liveActivityIntentQueue.test.ts)"
  artifacts:
    - path: "src/store/activeSessionStore.ts"
      provides: "reconcileLiveActivityIntents() com guarda de hidratação (early-return se draft ausente/inativo) + 3 novos call sites dentro de startOrResume(), um por ramo que chega a status 'active'"
    - path: "App.tsx"
      provides: "useEffect de boot sem a chamada prematura a reconcileLiveActivityIntents() — só reconcileOrphanActivities()/initLiveActivitySync()/registerLiveActivityIntentListener() continuam ali"
  key_links:
    - from: "src/screens/ActiveSessionScreen.tsx (iniciar(), await startOrResume(...))"
      to: "src/store/activeSessionStore.ts (reconcileLiveActivityIntents(), chamado de dentro de startOrResume ANTES do await retornar ao chamador)"
      via: "await get().reconcileLiveActivityIntents() logo após cada set({ ..., status: 'active', ... }) que hidrata draft"
      pattern: "reconcileLiveActivityIntents"
  prohibitions:
    - statement: "MUST NOT deixar reconcileLiveActivityIntents() descartar (drenar e perder) uma entrada da fila durável do App Group antes de confirmar que existe um draft ativo com sessionLogId correspondente para aplicá-la — uma entrada só pode ser destruída no momento em que é de fato aplicada ou definitivamente descartada por CAS (sessionLogId nulo/divergente do draft atual), nunca por estar 'de passagem' num momento em que o draft ainda não foi hidratado"
      status: flagged-unverified
    - statement: "MUST NOT introduzir um segundo caminho de gravação paralelo a completeSet()/activateSet()/adjustRest() para aplicar uma intenção reconciliada — reconcileLiveActivityIntents() continua chamando exclusivamente essas três funções já existentes na store, sem lógica de persistência nova, mesmo depois de mudar ONDE e QUANDO é chamada"
      status: flagged-unverified
---

<objective>
Fecha o gap 1 de `16-VERIFICATION.md` (status `gaps_found`, truth #1 FAILED): a
Fase 16 originalmente marcou CMD-01/CMD-02 como `unclassified/unresolved` no
probe determinístico spec-less (nenhum SPEC.md existe para esta fase) — os
`must_haves.truths` abaixo são a resolução explícita e verificável dessa
lacuna, derivados diretamente da causa raiz confirmada tanto por leitura de
código (`16-REVIEW.md` CR-01) quanto pelo próprio UAT físico
(`force_quit_toque=PASS-B`, não o PASS-A que a Plano 16-02 se propôs a
entregar — `16-03-SUMMARY.md`).

Causa raiz (confirmada nesta sessão de planejamento por leitura direta de
`App.tsx` e `src/store/activeSessionStore.ts`, ambos no estado atual pós-16-02):
`App.tsx` chama `reconcileLiveActivityIntents()` no `useEffect` de montagem
da raiz — o PRIMEIRO efeito do boot — antes de qualquer `startOrResume()`
(único ponto que hidrata `draft`, chamado de `ActiveSessionScreen.tsx:278`)
rodar. No cold-launch, `draft` é `null` no momento exato dessa chamada.
`reconcileLiveActivityIntents()` já chama `drainQueuedLiveActivityIntents()`
(que por sua vez chama `IntentActionQueue.drainAll()`, destrutivo — Plano
16-05 trata a raiz Swift desse comportamento para o caminho quente, mas
`drainAll()` em si permanece a operação de leitura+limpeza usada aqui) ANTES
de checar `draft` a cada entrada — a entrada é lida, descartada pela guarda
`!draft`, e perdida (`drainAll()` já removeu do `UserDefaults`), sem segunda
chance porque `reconcileLiveActivityIntents()` só roda uma vez por processo
em `App.tsx`.

Fix (duas camadas, per `16-REVIEW.md` CR-01, as duas alternativas propostas
combinadas para robustez): (1) `reconcileLiveActivityIntents()` ganha uma
guarda de hidratação no topo — se `draft` estiver ausente ou não-`active`,
retorna IMEDIATAMENTE sem chamar `drainQueuedLiveActivityIntents()`, então a
fila nunca é destruída antes de haver um draft candidato; (2) a CHAMADA
migra do `useEffect` de `App.tsx` para dentro de `startOrResume()`, logo
depois de cada `set({ ..., status: 'active', ... })` — o mesmo ponto onde o
dono, ao reabrir o app e navegar para a sessão ativa (o que aconteceu
literalmente no UAT físico: "vai para a home e quando eu entro no treino ele
nao conclui a série"), agora vai encontrar a série já concluída.

Purpose: fecha CMD-01 (critério de sucesso 3 do ROADMAP — force-quit + toque
+ reabertura) e reforça CMD-02 (mesmo mecanismo de reconciliação) sem alterar
o caminho quente (Plano 16-01, intacto) nem inventar um novo caminho de
gravação.
Output: `reconcileLiveActivityIntents()` seguro contra chamada prematura +
wired dentro de `startOrResume()` + testes que reproduzem a ordem real do
boot (o caso nomeado como `missing` em `16-VERIFICATION.md`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-tela-bloqueada-comandar/16-VERIFICATION.md
@.planning/phases/16-tela-bloqueada-comandar/16-REVIEW.md
@.planning/phases/16-tela-bloqueada-comandar/16-02-SUMMARY.md
@App.tsx
@src/store/activeSessionStore.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Guarda de hidratação em reconcileLiveActivityIntents() — nunca drenar antes de haver draft ativo</name>
  <reversibility rating="reversible">Adiciona uma guarda de early-return no topo de uma função já existente; não muda assinatura nem contrato externo (continua `() => Promise&lt;void&gt;`). Reverter é uma remoção de uma linha, sem efeito em cima de outras fases.</reversibility>
  <files>src/store/activeSessionStore.ts, __tests__/liveActivityIntentQueue.test.ts</files>
  <read_first>
    src/store/activeSessionStore.ts (linhas 1556-1601: `reconcileLiveActivityIntents()` completa — a implementação atual chama `drainQueuedLiveActivityIntents()` incondicionalmente dentro do `try`, ANTES de checar `draft` a cada entrada do loop; linha 564 `draft: null` — estado inicial real da store);
    __tests__/liveActivityIntentQueue.test.ts (arquivo inteiro, 270 linhas — os 7 casos existentes usam `withMockedActions(draft())` que já popula `draft` como `'active'` via `useActiveSessionStore.setState(...)` ANTES de chamar `reconcileLiveActivityIntents()`; nenhum caso testa o estado inicial real `draft: null` sem `setState` prévio — exatamente o gap nomeado em `16-VERIFICATION.md`: "todos os 7 casos populam o draft ANTES de chamar reconcile");
    .planning/phases/16-tela-bloqueada-comandar/16-REVIEW.md (seção CR-01, "Fix" — as duas alternativas concretas propostas, a segunda ["tornar a leitura não-destrutiva até confirmar aplicação"] inspira esta guarda, adaptada para não exigir um novo método Swift nesta task)
  </read_first>
  <action>
    RED — em `__tests__/liveActivityIntentQueue.test.ts`, adicionar um novo `it` dentro do
    `describe('reconcileLiveActivityIntents', ...)` existente, ANTES do primeiro caso já
    presente, chamado algo como "chamado com draft ainda null (ordem real do boot, sem
    setState prévio) não drena a fila nem perde a entrada". Corpo do teste: SEM chamar
    `withMockedActions(...)`, explicitamente `useActiveSessionStore.setState({ draft: null
    });` (reproduz o estado inicial real da store no boot — `draft: null` é literalmente o
    valor inicial declarado na linha 564 do store, este `setState` só torna a intenção
    explícita no teste); configurar `mockDrainQueuedLiveActivityIntents.mockResolvedValue([{
    kind: 'completeSet', deltaSeconds: null, sessionLogId: 'log-1', queuedAt: 'T1' }]);`
    (uma entrada REAL na fila simulada); chamar `await
    useActiveSessionStore.getState().reconcileLiveActivityIntents();`; e então `expect(
    mockDrainQueuedLiveActivityIntents).not.toHaveBeenCalled();` — esta é a asserção que
    falha (RED) contra a implementação atual, porque hoje `drainQueuedLiveActivityIntents()`
    É chamado incondicionalmente antes de qualquer checagem de `draft`. Rodar `npx jest
    __tests__/liveActivityIntentQueue.test.ts` e confirmar a falha exatamente nessa
    asserção antes de prosseguir.

    GREEN — em `src/store/activeSessionStore.ts`, dentro da implementação de
    `reconcileLiveActivityIntents: async () => { ... }` (linhas 1563-1601), inserir como
    PRIMEIRA linha do corpo da função, antes do `let entries: QueuedLiveActivityIntent[] =
    [];` já existente: `const draftAtual = get().draft; if (!draftAtual || draftAtual.status
    !== 'active') return;`. Esta guarda faz a função retornar IMEDIATAMENTE sem sequer
    tentar `drainQueuedLiveActivityIntents()` quando não há um draft ativo candidato a
    receber a intenção — a fila do App Group permanece INTACTA (nenhuma leitura, nenhuma
    remoção) para uma chamada futura, em vez de ser destruída e perdida. Não alterar mais
    nada dentro da função — o loop `for (const entry of entries) { const draft =
    get().draft; ... }` que já releva `get().draft` a cada iteração (guarda de CAS, Plano
    16-02) permanece exatamente como está.

    Rodar a suíte novamente e confirmar GREEN.
  </action>
  <behavior>
    - `reconcileLiveActivityIntents()` chamado com `get().draft === null` -> `drainQueuedLiveActivityIntents()` NUNCA é invocado; a fila simulada permanece intocada.
    - `reconcileLiveActivityIntents()` chamado com `get().draft.status !== 'active'` (ex.: `'finished'`) -> mesmo comportamento, nenhuma drenagem.
    - Os 7 casos já existentes (draft populado como `'active'` via `withMockedActions`) continuam passando sem alteração de comportamento.
  </behavior>
  <verify>
    <automated>npx jest __tests__/liveActivityIntentQueue.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/liveActivityIntentQueue.test.ts` passa 100% (8 casos: os 7
    pré-existentes + o novo). `npx tsc --noEmit` sai 0. `grep -c "draftAtual.status !==
    'active'" src/store/activeSessionStore.ts` retorna >= 1 (a guarda está presente, não
    apenas planejada). O novo teste, se a guarda for removida manualmente, volta a falhar
    (confirma que o teste realmente exercita a linha nova, não é vácuo).
  </acceptance_criteria>
  <done>reconcileLiveActivityIntents() nunca drena a fila do App Group antes de confirmar um draft ativo — a entrada de um toque no Lock Screen durante o cold-launch sobrevive a uma primeira chamada prematura, disponível para a próxima.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chamar reconcileLiveActivityIntents() de dentro de startOrResume() — não mais no boot cru de App.tsx</name>
  <precondition>Task 1 desta plano já adicionou a guarda de hidratação em reconcileLiveActivityIntents() — sem ela, mover a chamada para startOrResume() sozinho não seria suficiente para fechar o gap 1 (a chamada em App.tsx ainda existiria e ainda rodaria destrutivamente antes da primeira hidratação).</precondition>
  <files>src/store/activeSessionStore.ts, App.tsx, __tests__/liveActivityIntentQueue.test.ts, __tests__/activeSessionStore.test.ts</files>
  <read_first>
    src/store/activeSessionStore.ts (linhas 579-701: `startOrResume()` completa — três ramos chegam a `status: 'active'`: (a) linha ~627 `set({ draft: local, status: 'active' }); return;` dentro do catch de erro de transporte, "retomada offline"; (b) linhas ~650-655 `set({ draft: draftServidor, status: 'active', sessionMood: aberta.mood, checkInMinutes: aberta.availableMinutes, }); ... try { await saveDraft(draftServidor); } catch {...} return;`, "reconciliado com o servidor"; (c) linhas ~684-697 `set({ draft, status: 'active', sessionMood: aberta.mood, checkInMinutes: aberta.availableMinutes, }); ... try { await saveDraft(draft); } catch {...}` seguido do fim do bloco `try` externo (sem `return` explícito, cai para o fim da função), "sem rascunho local". A função `confirmCheckIn` (linhas 703-732) é uma AÇÃO SEPARADA para sessão NOVA (nenhuma Live Activity pode ter existido para uma sessão que ainda não tinha `sessionLogId`) — não precisa da chamada nova, não tocar);
    App.tsx (arquivo inteiro, 85 linhas — linhas 31-49, o `useEffect` de boot atual encadeia `reconcileLiveActivityIntents().finally(() => { void reconcileOrphanActivities(); })` — esta task remove esse encadeamento);
    __tests__/activeSessionStore.test.ts (linhas 1-90: mocks de módulo já existentes no topo do arquivo — NÃO mocka `modules/live-activity` explicitamente, então usa o `moduleNameMapper` global de `__mocks__/modules-live-activity.ts`, cujo `drainQueuedLiveActivityIntents` resolve `[]` por padrão; linhas 210-234 `beforeEach` com os mocks padrão de `loadDraft`/`getOpenSessionLog`/etc.; linhas 602-648, teste "rascunho local + servidor CONFIRMA a série" — molde EXATO do fixture a replicar: `buildDraftFromDetail(makeDetail(), 'user-1')` + `draft.sessionLogId = 'sl-existente'` + `mock(loadDraft).mockResolvedValue(draft)` + `mock(getOpenSessionLog).mockResolvedValue({ sessionLogId: 'sl-existente', startedAt: 'T0', setLogs: [...] })`, seguido de `await store().startOrResume({...}); await confirmarCheckInSePedido();`)
  </read_first>
  <action>
    RED (parte 1, `liveActivityIntentQueue.test.ts`) — adicionar um segundo novo `it` (após
    o da Task 1), "depois que o draft é hidratado, uma nova chamada aplica a entrada que a
    chamada anterior (draft null) preservou". Corpo: `useActiveSessionStore.setState({
    draft: null });` seguido de `mockDrainQueuedLiveActivityIntents.mockResolvedValue([{
    kind: 'completeSet', deltaSeconds: null, sessionLogId: 'log-1', queuedAt: 'T1' }]);`,
    então `await useActiveSessionStore.getState().reconcileLiveActivityIntents();` +
    `expect(mockDrainQueuedLiveActivityIntents).not.toHaveBeenCalled();` (repete a garantia
    da Task 1); DEPOIS `withMockedActions(draft());` (popula um draft `'active'` com
    `sessionLogId: 'log-1'`, batendo com a entrada da fila) seguido de `await
    useActiveSessionStore.getState().reconcileLiveActivityIntents();` de novo — agora
    `expect(mockDrainQueuedLiveActivityIntents).toHaveBeenCalledTimes(1); expect(
    completeSet).toHaveBeenCalledWith('ex-1', 1);`. Este teste já passa GREEN assim que a
    Task 1 estiver mergeada (não precisa de mudança de produção nova) — serve como
    documentação executável da sequência real "boot cru -> hidratação -> reconciliação"
    prevista para esta task.

    RED (parte 2, `activeSessionStore.test.ts`) — adicionar um novo `it` dentro de
    `describe('retomar sessão (fechar no meio e reabrir)', ...)` (mesmo describe block das
    linhas 602+), replicando o fixture de leitura obrigatória acima: `const draftLocal =
    buildDraftFromDetail(makeDetail(), 'user-1'); draftLocal.sessionLogId =
    'sl-existente'; mock(loadDraft).mockResolvedValue(draftLocal);
    mock(getOpenSessionLog).mockResolvedValue({ sessionLogId: 'sl-existente', startedAt:
    'T0', setLogs: [] });`. Importar `drainQueuedLiveActivityIntents` de
    `'../modules/live-activity'` no topo do arquivo (resolve para o mock global via
    `moduleNameMapper`, já usado por outros testes deste arquivo indiretamente) e chamar
    `mock(drainQueuedLiveActivityIntents).mockResolvedValue([]);` no corpo do teste (fila
    vazia — este teste prova que a CHAMADA acontece, não repete a lógica de aplicação já
    coberta em `liveActivityIntentQueue.test.ts`). Depois `await
    store().startOrResume({ sessionId: 'sess-1', userId: 'user-1', detail: makeDetail()
    }); await confirmarCheckInSePedido();` e então `expect(store().status).toBe('active');
    expect(drainQueuedLiveActivityIntents).toHaveBeenCalled();` — esta última asserção
    FALHA (RED) contra o código atual, porque hoje nada dentro de `startOrResume()` chama
    `reconcileLiveActivityIntents()`.

    GREEN — em `src/store/activeSessionStore.ts`, dentro de `startOrResume`, inserir `if
    (isCurrent()) await get().reconcileLiveActivityIntents();` em CADA UM dos três pontos
    identificados no `read_first`: (a) logo após `set({ draft: local, status: 'active'
    });`, antes do `return;` que a segue; (b) logo após o bloco `try { await
    saveDraft(draftServidor); } catch (e) { ... }`, antes do `return;` que o segue; (c)
    logo após o bloco `try { await saveDraft(draft); } catch (e) { ... }` do ramo "sem
    rascunho local", antes do `} catch (e) {` que fecha o `try` externo da função. Usar a
    mesma função `isCurrent()` já capturada no topo de `startOrResume` (linha 581) — mesmo
    padrão de guarda de concorrência que o resto da função já usa, evitando reconciliar
    contra uma chamada de `startOrResume` já superada por uma mais nova.

    Em `App.tsx`, no `useEffect` de boot (linhas 31-49), REMOVER inteiramente o bloco
    `void useActiveSessionStore.getState().reconcileLiveActivityIntents().finally(() => {
    void reconcileOrphanActivities(); });` e substituir por uma chamada direta `void
    reconcileOrphanActivities();` (sem encadeamento — a ordem "aplicar intents antes de
    reconciliar órfãs" deixa de fazer sentido no boot cru, já que a reconciliação de
    intents agora só roda dentro de `startOrResume`, chamado depois, quando o dono navega
    para a sessão ativa). Atualizar o comentário acima dessa linha para explicar que
    `reconcileLiveActivityIntents()` foi movido para `startOrResume()`
    (`activeSessionStore.ts`) por causa do gap 1 de `16-VERIFICATION.md`/CR-01 de
    `16-REVIEW.md` — não apagar o comentário, reescrevê-lo.
  </action>
  <behavior>
    - `startOrResume()` que resolve pelo ramo "retomada offline" (erro de transporte capturado) chama `reconcileLiveActivityIntents()` antes de retornar.
    - `startOrResume()` que resolve pelo ramo "reconciliado com o servidor" (log aberto encontrado) chama `reconcileLiveActivityIntents()` antes de retornar.
    - `startOrResume()` que resolve pelo ramo "sem rascunho local, log aberto encontrado" chama `reconcileLiveActivityIntents()` antes do fim da função.
    - App.tsx não chama mais `reconcileLiveActivityIntents()` no boot cru.
  </behavior>
  <verify>
    <automated>npx jest __tests__/liveActivityIntentQueue.test.ts __tests__/activeSessionStore.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    Ambas as suítes passam 100% (nenhuma regressão nas ~27 chamadas pré-existentes a
    `startOrResume` em `activeSessionStore.test.ts`). `npx tsc --noEmit` sai 0. `grep -c
    "reconcileLiveActivityIntents" src/store/activeSessionStore.ts` retorna >= 4 (1
    definição + 3 novos call sites dentro de `startOrResume`). `grep -c
    "reconcileLiveActivityIntents" App.tsx` retorna 0 (a chamada saiu completamente do
    boot cru). Leitura manual confirma que os três call sites inseridos usam `if
    (isCurrent())`, consistente com o resto da função.
  </acceptance_criteria>
  <done>Toda vez que startOrResume() resolve para um draft ativo (qualquer um dos três ramos), a fila de intents da tela bloqueada é reconciliada antes do await retornar ao chamador — inclusive quando isso acontece porque o dono, depois de um force-quit, reabriu o app e navegou para a sessão ativa (o cenário exato do UAT PASS-B em 16-03-SUMMARY.md).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fila durável do App Group -> `activeSessionStore` (reconciliação) | Leitura de dado escrito por um processo potencialmente já encerrado — o MOMENTO em que essa leitura é permitida agora é controlado por uma guarda de hidratação, não mais pelo boot cru |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-04-01 | Tampering | `reconcileLiveActivityIntents()` drenando (e destruindo) a fila antes de um draft ativo existir | high | mitigate | Guarda de hidratação (`!draftAtual \|\| draftAtual.status !== 'active'` -> `return` imediato, ANTES de `drainQueuedLiveActivityIntents()`) — fecha 16-VERIFICATION.md gap 1 |
| T-16-04-02 | Tampering | Chamada de `reconcileLiveActivityIntents()` correndo contra uma chamada de `startOrResume()` já superada (dono trocou de sessão duas vezes rápido) | medium | mitigate | Guarda `if (isCurrent())` antes de cada novo call site — mesmo padrão de `epoch`/`isCurrent()` que o resto de `startOrResume()` já usa contra chamadas concorrentes |
| T-16-04-03 | Repudiation | Chamada de `reconcileLiveActivityIntents()` no boot cru de App.tsx (removida) deixando de existir sem substituto equivalente | low | accept | O substituto (chamada dentro de `startOrResume()`) cobre TODOS os ramos que chegam a `status: 'active'` — o boot cru nunca aplicava nada de qualquer forma (o próprio gap 1), então remover a chamada ali não reduz cobertura real, só remove o comportamento destrutivo |
</threat_model>

## Artifacts this phase produces

- `src/store/activeSessionStore.ts::reconcileLiveActivityIntents` — guarda de hidratação nova (early-return antes de `drainQueuedLiveActivityIntents()` quando `draft` ausente/inativo); lógica de aplicação por entrada (loop com CAS por `sessionLogId`) inalterada
- `src/store/activeSessionStore.ts::startOrResume` — 3 novos call sites `if (isCurrent()) await get().reconcileLiveActivityIntents();`, um por ramo que chega a `status: 'active'`
- `App.tsx` — `useEffect` de boot sem a chamada a `reconcileLiveActivityIntents()`; `reconcileOrphanActivities()` chamado diretamente, sem `.finally()` encadeado
- `__tests__/liveActivityIntentQueue.test.ts` — 2 novos casos: guarda de hidratação (draft null não drena) + sequência "boot cru -> hidratação -> reconciliação aplica"
- `__tests__/activeSessionStore.test.ts` — 1 novo caso: `startOrResume()` chama `drainQueuedLiveActivityIntents()` de fato ao resolver para um draft ativo

<verification>
`npx tsc --noEmit` limpo; `npx jest __tests__/liveActivityIntentQueue.test.ts
__tests__/activeSessionStore.test.ts` verde (10 casos novos/existentes cobrindo o gap 1
diretamente, incluindo o caso nomeado como `missing` em `16-VERIFICATION.md`). O
comportamento físico real (force-quit -> toque -> reabrir -> navegar para a sessão ->
série já concluída) só é confirmável na Plano 16-06 (UAT física de re-execução).
</verification>

<success_criteria>
reconcileLiveActivityIntents() nunca destrói a fila do App Group antes de um draft ativo
existir, e é chamada de dentro de startOrResume() (não mais no boot cru de App.tsx) em
todos os ramos que hidratam um draft ativo — provado por teste automatizado reproduzindo
a ordem real do boot, fechando 16-VERIFICATION.md gap 1.
</success_criteria>

<output>
Create `.planning/phases/16-tela-bloqueada-comandar/16-04-SUMMARY.md` when done
</output>
