---
phase: 16-tela-bloqueada-comandar
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/activeSessionStore.ts
  - __tests__/activeSessionStore.test.ts
autonomous: true
requirements: [CMD-01, CMD-02]
gap_closure: true

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "stepLoad(exerciseId, setOrder, direction) chama saveDraft(novo) fire-and-forget imediatamente após atualizar a carga em memória — o ajuste de carga por botões +/- (SessionPlayer.tsx:681,708), a interação PRIMÁRIA de carga do milestone, sobrevive a um force-quit sem depender do campo de texto (setLoad) nem do botão 'usar sugestão'"
    - "setDuration(exerciseId, setOrder, seconds) chama saveDraft(novo) fire-and-forget — o único campo que canCompleteSet() (sessionModel.ts:272-274) exige para exercícios isTimeBased (cardio/isometria) sobrevive a um force-quit mesmo sem reps/carga informados"
    - "setDistance, setRir e setEffort também chamam saveDraft(novo) fire-and-forget, por consistência com as quatro ações já persistidas (setReps, setLoad, stepLoad, setDuration) — nenhuma das sete ações que mutam draft.exercises[].sets[] permanece só em memória ao final desta plano"
    - "Um teste automatizado prova especificamente que um force-quit simulado LOGO DEPOIS de usar só o stepper de carga (stepLoad, nunca setLoad) não impede completeSet() de aprovar"
    - "Um teste automatizado prova especificamente que um force-quit simulado LOGO DEPOIS de informar a duração de um exercício isTimeBased (cardio/isometria) não impede completeSet() de aprovar, mesmo sem reps/carga informados"
    - "applyServerSetLogs() (já generalizado em 16-08-PLAN.md para todos os campos digitados, activeSessionStore.ts:411-432) não precisa de nenhuma mudança nesta plano — o overlay localSetByPlannedSet já preserva actualDurationSeconds/actualDistanceM/actualRir/perceivedEffort assim que esses campos chegam a existir no rascunho local persistido"
  artifacts:
    - path: "src/store/activeSessionStore.ts"
      provides: "stepLoad/setDuration/setDistance/setRir/setEffort persistindo via saveDraft(novo) fire-and-forget, mesmo padrão de setReps/setLoad (16-08-PLAN.md)"
  key_links:
    - from: "src/components/session/SessionPlayer.tsx (botões +/- de carga, :681,:708)"
      to: "src/store/activeSessionStore.ts::stepLoad"
      via: "onPress -> stepLoad(...) -> saveDraft(novo)"
      pattern: "saveDraft(novo)"
    - from: "src/engine/sessionModel.ts::canCompleteSet (ramo isTimeBased, :272-274)"
      to: "src/store/activeSessionStore.ts::setDuration"
      via: "actualDurationSeconds só é gravado por setDuration, agora persistido"
      pattern: "actualDurationSeconds"
  prohibitions:
    - statement: "MUST NOT introduzir um segundo caminho de gravação paralelo a saveDraft para stepLoad/setDuration/setDistance/setRir/setEffort — o único mecanismo de persistência continua sendo saveDraft(novo).catch(...), mesmo padrão já usado por setReps/setLoad desde 16-08-PLAN.md (prohibition herdada de 16-04-PLAN.md, reafirmada porque esta plano toca as cinco funções restantes)"
      status: flagged-unverified
    - statement: "MUST NOT alterar a lógica de cálculo já existente e já testada em stepLoad (incremento de carga por loadIncrementKg), setDuration/setDistance (clamp de valor positivo), setRir (clamp 0-10) ou setEffort — esta plano só acrescenta a chamada de persistência após o set() já existente, sem tocar o comportamento de cálculo"
      status: flagged-unverified
    - statement: "MUST NOT deixar WR-01 (ack sem .catch(), activeSessionStore.ts:1682,1696,1702,1707), WR-02 (invariante de série única não reforçada na retomada, activeSessionStore.ts:374-520,690), WR-03 (IntentActionQueue.enqueue/writeAll não atômico, IntentActionQueue.swift:59-77) ou WR-04 (falha de escrita no App Group sem os_log, IntentActionQueue.swift:59-67) sem uma justificativa de deferimento nomeada individualmente — os quatro são adiados por decisão do dono para esta rodada (ver seção 'Deferido nesta rodada' abaixo), nunca descartados em silêncio nem tocados por esta plano"
      status: flagged-unverified
---

<objective>
Fecha CR-01 (`16-REVIEW.md`) / o gap `D2` remanescente (`16-VERIFICATION.md`): a
persistência fire-and-forget via `saveDraft` que `16-08-PLAN.md` conectou a
`setReps`/`setLoad` NÃO cobre `stepLoad` (os botões `-`/`+` de carga em
`SessionPlayer.tsx:681,708` — a interação PRIMÁRIA de ajuste de carga do
milestone, per `PROJECT.md`) nem `setDuration` (o único campo que
`canCompleteSet()` exige para exercícios `isTimeBased`/cardio/isometria,
`sessionModel.ts:272-274`). Um force-quit logo depois de qualquer um desses
dois caminhos reproduz o MESMO sintoma documentado como
`force_quit_toque=FAIL` em `16-06-SUMMARY.md` — só que por um caminho de UI
que nem o teste automatizado de `16-08` (`__tests__/activeSessionStore.test.ts:607-642`,
só `setReps`/`setLoad`) nem o UAT físico de `16-09` (resposta agregada, sem
confirmação de qual caminho foi usado) exercitaram.

Esta plano estende o MESMO padrão fire-and-forget de `setReps`/`setLoad` às
cinco ações restantes que mutam `draft.exercises[].sets[]`: `stepLoad`,
`setDuration` (as duas críticas para `canCompleteSet()`), e `setDistance`/
`setRir`/`setEffort` (por consistência, per escopo explícito desta rodada).
`applyServerSetLogs()` (o overlay `localSetByPlannedSet` que 16-08 já
generalizou para TODOS os campos digitados — `actualDurationSeconds`,
`actualDistanceM`, `actualRir`, `perceivedEffort`, `activeSessionStore.ts:
411-432`) já cobre esses campos assim que eles existirem no rascunho local
persistido; nenhuma mudança adicional a essa função é necessária nesta
rodada — confirmado por leitura direta do código antes deste planejamento.

**Nota sobre o edge-probe de borda (fallback determinístico, sem SPEC.md
desta fase):** as duas linhas `CMD-01`/`CMD-02` retornadas pelo probe
permanecem `unclassified`/`unresolved` (herdado de `16-08-PLAN.md`, que já
sinalizou a mesma ambiguidade para `CMD-02` sem SPEC formal). Nenhum defeito
de borda adicional, distinto de CR-01 (a causa raiz tratada por esta plano),
foi identificado para nenhum dos dois requisitos. As duas linhas seguem
sinalizadas como suposição do planner — não resolvidas por padrão
determinístico — para avaliação explícita do verificador na próxima rodada,
nunca descartadas em silêncio.

Purpose: fechar a causa raiz de CR-01 para o caminho de UI real que o dono
usa (stepper de carga) e para o único caminho que existe para cardio/isometria
(duração), não só para o caminho de teclado já coberto por 16-08.
Output: `stepLoad`/`setDuration`/`setDistance`/`setRir`/`setEffort`
persistindo via `saveDraft`, com testes que reproduzem especificamente os
dois cenários de force-quit que `16-VERIFICATION.md` e `16-REVIEW.md`
apontaram como reproduzíveis hoje.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-tela-bloqueada-comandar/16-VERIFICATION.md
@.planning/phases/16-tela-bloqueada-comandar/16-REVIEW.md
@.planning/phases/16-tela-bloqueada-comandar/16-08-SUMMARY.md
@src/store/activeSessionStore.ts
@src/engine/sessionModel.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: stepLoad persiste via saveDraft — o ajuste de carga por +/- (interação primária)</name>
  <reversibility rating="reversible">Acrescenta uma chamada fire-and-forget a uma função já existente (saveDraft), mesmo mecanismo já em produção desde 16-08 para setReps/setLoad. Reverter é remover uma linha, sem efeito em nenhuma outra fase nem em nenhum contrato externo.</reversibility>
  <files>src/store/activeSessionStore.ts, __tests__/activeSessionStore.test.ts</files>
  <read_first>
    src/store/activeSessionStore.ts:1203-1233 (setReps/setLoad completas — o padrão fire-and-forget EXATO a replicar: `const novo = withSet(...)`, `set({ draft: novo })`, depois `saveDraft(novo).catch((e) => { console.warn(...) })`, sem await, sem debounce);
    src/store/activeSessionStore.ts:1235-1252 (stepLoad completa hoje — só `set({ draft: novo })`, nenhuma chamada a saveDraft; a variável `novo` já existe nomeada, só falta a chamada de persistência depois do set);
    src/components/session/SessionPlayer.tsx:668-717 (os dois TouchableOpacity de `-`/`+` de carga chamando stepLoad em :681 e :708 — confirma que é o único caminho wired aos botões; setLoad só é acionado pelo campo de texto em :698 e pelo botão "usar sugestão" em :726, caminhos secundários);
    src/engine/sessionModel.ts:262-278 (canCompleteSet — para exercício não-bodyweight, exige actualLoadKg != null && actualLoadKg > 0, o único campo que stepLoad grava);
    __tests__/activeSessionStore.test.ts:607-642 (describe "D2: setReps/setLoad persistem via saveDraft" — molde exato para o teste de "chama saveDraft com o campo atualizado");
    __tests__/activeSessionStore.test.ts:644-689 (describe "D2/D2b" — helper capturaDraftPersistido em :654-659, e o teste do ramo OFFLINE em :661-689 — molde exato para o round-trip de force-quit simulado a replicar para stepLoad)
  </read_first>
  <action>
    Em `stepLoad` (activeSessionStore.ts:1235-1252), logo após `set({ draft: novo })`,
    acrescentar `saveDraft(novo).catch((e) => { console.warn('[activeSession] carga (stepper) não persistida (não-fatal):', e); });`
    — mesmo padrão fire-and-forget de setReps/setLoad, sem await, sem debounce
    (sessionDraftStorage.ts já serializa escritas da mesma chave via
    withKeyQueue). Não tocar a lógica de cálculo do incremento (base + direction
    * ex.loadIncrementKg) nem o guard de bodyweight, ambos inalterados.

    No arquivo de teste, acrescentar dois casos: (1) dentro do describe "D2"
    (:607-642), um novo teste "stepLoad chama saveDraft com o draft cujo
    actualLoadKg foi incrementado" — ativa ex-1/1, limpa o mock de saveDraft,
    chama stepLoad('ex-1', 1, 1), confirma que saveDraft foi chamado e que o
    último draft passado tem actualLoadKg correspondente ao incremento
    calculado a partir de loadIncrementKg do fixture ex-1 (2.5); (2) dentro do
    describe "D2/D2b" (ou um describe irmão dedicado), um teste round-trip que:
    ativa ex-1/1, chama setReps('ex-1', 1, 8) (já persiste, herdado de 16-08) e
    stepLoad('ex-1', 1, 1) (o fix desta task — NUNCA chamar setLoad neste
    teste, para provar especificamente o caminho do stepper), captura o draft
    persistido via capturaDraftPersistido (ou padrão equivalente lendo o
    último mock(saveDraft).mock.calls), simula a reabertura via startOrResume
    com loadDraft mockado para esse draft e getOpenSessionLog rejeitando com
    erro de transporte (ramo offline, mesmo molde de :661-689), e confirma que
    completeSet('ex-1', 1) retorna true — provando que um force-quit logo
    depois de usar SÓ o stepper de carga (nunca o campo de texto) não reprova
    mais.
  </action>
  <behavior>
    - stepLoad('ex-1', 1, 1) -&gt; saveDraft chamado com um draft cujo exercises[0].sets[0].actualLoadKg reflete o incremento.
    - Force-quit simulado depois de setReps + stepLoad (nunca setLoad) -&gt; completeSet('ex-1', 1) retorna true.
  </behavior>
  <verify>
    <automated>npx tsc --noEmit && npx jest __tests__/activeSessionStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc --noEmit` sai 0. `npx jest __tests__/activeSessionStore.test.ts`
    passa 100%. `grep -c "saveDraft(novo)" src/store/activeSessionStore.ts`
    retorna &gt;= 3 (setReps + setLoad, herdados de 16-08, mais stepLoad desta
    task). O teste round-trip que usa stepLoad (não setLoad) existe e passa —
    confirmável por `npx jest __tests__/activeSessionStore.test.ts -t stepLoad`.
  </acceptance_criteria>
  <done>stepLoad persiste via saveDraft; um force-quit simulado logo depois de ajustar carga só pelo stepper (sem tocar setLoad) não derruba mais canCompleteSet().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: setDuration persiste via saveDraft — o único campo que fecha canCompleteSet() para cardio/isometria</name>
  <precondition>Task 1 desta plano já estabeleceu o padrão de teste round-trip (capturaDraftPersistido + startOrResume simulando force-quit) a ser replicado aqui para um exercício isTimeBased.</precondition>
  <files>src/store/activeSessionStore.ts, __tests__/activeSessionStore.test.ts</files>
  <read_first>
    src/store/activeSessionStore.ts:1269-1283 (setDuration completa hoje — só `set({ draft: withSet(...) })`, sem nomear `novo`, sem saveDraft);
    src/engine/sessionModel.ts:262-278 (canCompleteSet — ramo isTimeBased em :272-274: `return set.actualDurationSeconds != null && set.actualDurationSeconds > 0;`, IGNORA reps/carga por completo para esse metric — actualDurationSeconds é o ÚNICO campo relevante);
    __tests__/cardioTempoDistancia.test.ts:85-162 (detalheComCardio() — especificamente o exercício ex-prancha em :130-160, metric: 'tempo', 1 set, target_duration_seconds: 45 — o fixture-molde a reproduzir localmente em activeSessionStore.test.ts, já que o makeDetail() deste arquivo não tem hoje nenhum exercício isTimeBased);
    __tests__/activeSessionStore.test.ts:1-133 (estrutura de makeDetail() e dos mocks já existentes — o novo fixture local desta task deve seguir a MESMA forma de SessionDetail, com um exerciseId/exercise_key distintos de ex-1/ex-2 para não colidir)
  </read_first>
  <action>
    Em `setDuration` (activeSessionStore.ts:1269-1283), nomear o resultado de
    `withSet(...)` numa variável `novo` (mesma estrutura de setReps/setLoad/
    stepLoad) e, após `set({ draft: novo })`, acrescentar `saveDraft(novo).catch((e)
    =&gt; { console.warn('[activeSession] duração não persistida (não-fatal):',
    e); });`. Não tocar o clamp já existente (seconds &lt;= 0 ou não-finito vira
    null, Math.round no valor válido).

    No arquivo de teste, criar um novo helper local (ex.:
    `makeDetailComExercicioDeTempo`), modelado byte-a-byte na estrutura do
    exercício ex-prancha de `cardioTempoDistancia.test.ts:130-160` (metric:
    'tempo', 1 set, target_duration_seconds), com um exerciseId/exercise_key
    próprios deste arquivo (ex.: 'ex-tempo'). Adicionar: (1) dentro do describe
    "D2" (:607-642), um teste "setDuration chama saveDraft com
    actualDurationSeconds atualizado"; (2) um teste round-trip (mesmo molde da
    Task 1) que usa esse novo fixture, ativa a série de tempo, chama
    setDuration('ex-tempo', 1, 45), captura o draft persistido, simula
    force-quit + reabertura (ramo offline, mesmo molde de :661-689), e
    confirma que completeSet('ex-tempo', 1) retorna true SEM que reps/carga
    tenham sido informados em nenhum momento — provando que o único campo que
    canCompleteSet() exige para esse metric sobrevive ao force-quit.
  </action>
  <behavior>
    - setDuration('ex-tempo', 1, 45) -&gt; saveDraft chamado com actualDurationSeconds === 45.
    - Force-quit simulado depois de setDuration num exercício isTimeBased (sem setReps/setLoad) -&gt; completeSet() retorna true.
  </behavior>
  <verify>
    <automated>npx tsc --noEmit && npx jest __tests__/activeSessionStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc --noEmit` sai 0. `npx jest __tests__/activeSessionStore.test.ts`
    passa 100%. `grep -c "saveDraft(novo)" src/store/activeSessionStore.ts`
    retorna &gt;= 4 (setReps + setLoad + stepLoad + setDuration). O teste
    round-trip do exercício isTimeBased existe e passa — confirmável por
    `npx jest __tests__/activeSessionStore.test.ts -t setDuration`.
  </acceptance_criteria>
  <done>setDuration persiste via saveDraft; um exercício isTimeBased (cardio/isometria) com só a duração informada sobrevive a um force-quit simulado e completeSet() aprova, sem depender de reps/carga.</done>
</task>

<task type="auto">
  <name>Task 3: setDistance/setRir/setEffort persistem via saveDraft — decisão de escopo explícita, sem exceção remanescente</name>
  <files>src/store/activeSessionStore.ts, __tests__/activeSessionStore.test.ts</files>
  <read_first>
    src/store/activeSessionStore.ts:1254-1267 (setRir), :1285-1298 (setDistance), :1300-1309 (setEffort) — as três funções remanescentes sem saveDraft, todas com a mesma forma `set({ draft: withSet(...) })` inline, sem nomear `novo`;
    Task 1 e Task 2 desta mesma plano (o padrão fire-and-forget já estabelecido nas cinco funções anteriores — replicar sem variação, sem alterar clamps/cálculos já existentes: setRir clampa 0-10, setDistance/setEffort não têm clamp adicional além do já existente)
  </read_first>
  <action>
    Aplicar o MESMO padrão fire-and-forget das Tasks 1/2 às três funções
    restantes: em cada uma, nomear o resultado de `withSet(...)` numa variável
    `novo`, manter `set({ draft: novo })`, depois acrescentar
    `saveDraft(novo).catch((e) =&gt; { console.warn(...) })` com uma mensagem de
    warn específica por campo ("RIR não persistido (não-fatal)", "distância
    não persistida (não-fatal)", "esforço percebido não persistido
    (não-fatal)"). Não alterar o clamp 0-10 de setRir nem os clamps de
    valor-positivo já existentes em setDistance.

    No arquivo de teste, adicionar três testes simples (mesmo molde de
    :607-642): um por função, confirmando que saveDraft é chamado com o campo
    correspondente atualizado (actualRir, actualDistanceM, perceivedEffort).
    Nenhum teste round-trip de canCompleteSet() é necessário para estes três —
    não são campos que canCompleteSet() exige (reps/carga/duração já cobertos
    pelas Tasks 1/2).

    Registrar nesta task, como decisão de escopo desta rodada (ver também a
    seção "Decisão de escopo" abaixo, fora de `&lt;tasks&gt;`): ao final desta
    plano, as SETE ações que mutam `draft.exercises[].sets[]` (setReps,
    setLoad, stepLoad, setRir, setDuration, setDistance, setEffort) persistem
    via saveDraft — nenhuma exceção deliberada remanescente.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest __tests__/activeSessionStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc --noEmit` sai 0. `npx jest __tests__/activeSessionStore.test.ts`
    passa 100%. `grep -c "saveDraft(novo)" src/store/activeSessionStore.ts`
    retorna exatamente 7 (setReps, setLoad, stepLoad, setRir, setDuration,
    setDistance, setEffort — a contagem final e completa desta rodada, sem
    duplicação nem função remanescente).
  </acceptance_criteria>
  <done>Todas as sete ações de escrita que mutam draft.exercises[].sets[] persistem via saveDraft — nenhuma exceção deliberada remanescente ao final desta plano.</done>
</task>

</tasks>

## Decisão de escopo: quais ações de escrita persistem (CR-01/16-VERIFICATION.md gap 1)

Ao final desta plano, as SETE ações que mutam `draft.exercises[].sets[]` —
`setReps`, `setLoad` (já persistidas desde 16-08-PLAN.md), `stepLoad`,
`setDuration`, `setDistance`, `setRir`, `setEffort` (as cinco fechadas por
esta plano) — persistem via `saveDraft(novo).catch(...)` fire-and-forget,
sem exceção. Esta é a decisão explícita que `16-VERIFICATION.md` (gap 1,
item "missing") e `16-REVIEW.md` (CR-01, "Fix") pediram para não ficar
implícita como em `16-08-SUMMARY.md` (que não mencionou a exclusão de
`stepLoad`/`setDuration` como escopo deliberado — era lacuna não-intencional,
não decisão). Nenhuma ação de escrita de série do draft permanece só em
memória depois desta plano.

## Deferido nesta rodada

Os quatro achados WARNING do `16-REVIEW.md` (rodada 16-07/16-08) permanecem
em aberto, por decisão explícita do dono de escopo restrito a CR-01/D2 nesta
rodada — nenhum é tratado por esta plano:

- **WR-01** — `void ackQueuedLiveActivityIntent(...)` sem `.catch()`
  (`src/store/activeSessionStore.ts:1682,1696,1702,1707`). Deferido: fora do
  escopo do dono para esta rodada (persistência de rascunho, não fila de
  intents).
- **WR-02** — invariante de série `active` única não reforçada na retomada
  (`applyServerSetLogs` :374-520, ramo offline :690). Deferido: mesma razão —
  escopo desta rodada é exclusivamente a persistência de draft (CR-01), não a
  normalização de um rascunho já corrompido em disco.
- **WR-03** — `IntentActionQueue.enqueue`/`writeAll` não atômico entre
  processos concorrentes (`modules/live-activity/ios/IntentActionQueue.swift:
  59-77`). Deferido: código Swift da fila de intents, fora do escopo desta
  rodada (store JS/TS).
- **WR-04** — falha de escrita no App Group engolida sem `os_log`
  (`modules/live-activity/ios/IntentActionQueue.swift:59-67`). Deferido:
  mesma razão de WR-03.

Nenhum dos quatro é descartado — ficam registrados aqui para uma rodada
futura, caso o dono decida priorizá-los.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rascunho local persistido (AsyncStorage) -> completeSet()/canCompleteSet() na retomada | Dado local não-autoritativo decidindo se uma série pode ser concluída; nenhum trust boundary novo além do já aceito em 16-08-PLAN.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-10-01 | Tampering | stepLoad/setDuration/setDistance/setRir/setEffort persistindo em disco com maior frequência de escrita (a cada toque do stepper/seletor) | low | accept | Mesmo mecanismo/escopo de armazenamento já aceito em T-16-08-03 (reps/carga não são segredo, `sessionDraftStorage.ts:5`) — nenhuma classe de dado nova, nenhum destino novo |
| T-16-10-02 | Repudiation | A resposta agregada do UAT físico de 16-09 sendo reaproveitada para fechar esta rodada, sem confirmar qual caminho de UI foi de fato exercitado desta vez | high | mitigate | Plano 16-11 (checkpoint físico) exige confirmação explícita por teste/caminho de UI — nunca uma frase agregada cobrindo os dois testes |
| T-16-10-03 | Denial of Service (local, ao próprio dono) | Escrita concorrente rápida de múltiplos toques no stepper de carga corrompendo o rascunho em disco | low | accept | `sessionDraftStorage.ts` já serializa escritas da mesma chave via `withKeyQueue` — mesma mitigação já aceita para setReps/setLoad em 16-08-PLAN.md, sem mudança nesta plano |
</threat_model>

## Artifacts this phase produces

- `src/store/activeSessionStore.ts::stepLoad` — persiste via `saveDraft(novo)` fire-and-forget
- `src/store/activeSessionStore.ts::setDuration` — persiste via `saveDraft(novo)` fire-and-forget
- `src/store/activeSessionStore.ts::setDistance` — persiste via `saveDraft(novo)` fire-and-forget
- `src/store/activeSessionStore.ts::setRir` — persiste via `saveDraft(novo)` fire-and-forget
- `src/store/activeSessionStore.ts::setEffort` — persiste via `saveDraft(novo)` fire-and-forget
- `__tests__/activeSessionStore.test.ts::makeDetailComExercicioDeTempo` (novo helper local) — fixture com exercício `metric: 'tempo'`, molde de `cardioTempoDistancia.test.ts:130-160`
- `__tests__/activeSessionStore.test.ts` — novos casos: persistência de stepLoad/setDuration/setDistance/setRir/setEffort, round-trip de force-quit via stepLoad (sem setLoad) e via setDuration (exercício isTimeBased, sem reps/carga)

<verification>
`npx tsc --noEmit` limpo; `npx jest __tests__/activeSessionStore.test.ts` verde
com os novos casos. O comportamento físico real (force-quit depois do stepper
de carga; force-quit depois de informar duração de cardio -> reabrir -> série
concluível) só é confirmável na Plano 16-11 (UAT física), depois desta plano
estar mergeada.
</verification>

<success_criteria>
As sete ações de escrita que mutam draft.exercises[].sets[] persistem via
saveDraft, sem exceção — provado por testes que reproduzem especificamente os
dois cenários que `16-VERIFICATION.md`/`16-REVIEW.md` apontaram como
reproduzíveis hoje (stepper de carga, exercício isTimeBased), fechando CR-01/
D2 no nível de mecanismo e teste unitário.
</success_criteria>

<output>
Create `.planning/phases/16-tela-bloqueada-comandar/16-10-SUMMARY.md` when done
</output>
