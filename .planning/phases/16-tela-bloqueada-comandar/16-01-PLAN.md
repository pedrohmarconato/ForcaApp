---
phase: 16-tela-bloqueada-comandar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/live-activity/ios/CompleteSetIntent.swift
  - modules/live-activity/ios/IntentActionQueue.swift
  - modules/live-activity/ios/LiveActivityModule.swift
  - modules/live-activity/ios/SkipRestIntent.swift
  - modules/live-activity/ios/AdjustRestIntent.swift
  - modules/live-activity/index.ts
  - targets/session-widget/CompleteSetIntent.swift
  - targets/session-widget/SkipRestIntent.swift
  - targets/session-widget/AdjustRestIntent.swift
  - targets/session-widget/WidgetLiveActivity.swift
  - src/native/liveActivityIntentBridge.ts
  - App.tsx
  - scripts/verify-native-skeleton.sh
  - __tests__/liveActivityIntentBridge.test.ts
autonomous: true
requirements: [CMD-01, CMD-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Tocar 'Concluir série' no Lock Screen registra a série pelo MESMO caminho completeSet() -> outbox -> servidor já existente (CMD-01) — nenhum caminho de gravação paralelo é criado; a Live Activity permanece espelho, nunca fonte de verdade"
    - "Tocar '-30s'/'+30s' no Lock Screen chama adjustRest(deltaSeconds) com o delta exato do botão tocado, e o próprio perform() atualiza a Activity via Activity.update() nativo — o card reflete o ajuste sem depender do round-trip até o JS terminar (CMD-02, 'sem lag perceptível')"
    - "Tocar 'Pular' no Lock Screen ativa a PRÓXIMA série pendente via activateSet(), a mesma função que o botão 'Pular descanso' do app já usa — nenhuma lógica de domínio nova é escrita em Swift (CMD-02)"
    - "Nenhum dos três botões abre o app a partir de dentro de perform() — a única forma de reabertura é widgetURL() no corpo do card, fora dos botões de intent (Pitfall 2 do RESEARCH.md)"
    - statement: "Cada toque escreve a intenção na fila durável do App Group (IntentActionQueue) ANTES de tentar o evento in-process — condição necessária para o cold-launch (Plano 16-02) funcionar, mas só comprovável de ponta a ponta em aparelho físico (Plano 16-03)"
      verification: backstop
  artifacts:
    - path: "src/native/liveActivityIntentBridge.ts"
      provides: "Único listener onIntentAction -> despacha para completeSet/activateSet/adjustRest já existentes na store, resolvendo a série via findActiveSet/findNextPendingSet"
    - path: "modules/live-activity/ios/CompleteSetIntent.swift"
      provides: "LiveActivityIntent real (roda no processo do app) — grava na fila durável e emite onIntentAction"
    - path: "modules/live-activity/ios/IntentActionQueue.swift"
      provides: "Fila durável UserDefaults(suiteName: group.com.pmarconato.forcaapp.shared), write+read, cap de 20 entradas"
  key_links:
    - from: "targets/session-widget/WidgetLiveActivity.swift (Button(intent:))"
      to: "modules/live-activity/ios/CompleteSetIntent.swift / SkipRestIntent.swift / AdjustRestIntent.swift"
      via: "AppIntents perform() roteado pelo iOS para o processo do app (RESEARCH.md Pattern 1)"
      pattern: "Button(intent:"
    - from: "modules/live-activity/ios/LiveActivityModule.swift (sendEvent onIntentAction)"
      to: "src/native/liveActivityIntentBridge.ts"
      via: "LiveActivityModule.addListener('onIntentAction', ...) via Events()/OnCreate/shared"
      pattern: "onIntentAction"
    - from: "src/native/liveActivityIntentBridge.ts"
      to: "src/store/activeSessionStore.ts (completeSet/activateSet/adjustRest)"
      via: "useActiveSessionStore.getState().<ação>(...) — nenhuma lógica de gravação nova"
      pattern: "getState()."
  prohibitions:
    - statement: "MUST NOT fabricar ou usar valor default para actualReps/actualLoadKg/actualDurationSeconds só para forçar o toque de 1-tap do Lock Screen a 'suceder' — reusar exatamente o mesmo gate canCompleteSet() que o fluxo do app já aplica; uma série sem valor real informado continua não-registrável também pelo Lock Screen (nenhum número inventado, ecoando a regra da própria store)"
      status: flagged-unverified
    - statement: "MUST NOT deixar os intents da tela bloqueada agirem sobre um exercício que o dono já pulou explicitamente ou que foi cortado por replanejamento (skippedByUser/cutByReplan) — mesmo os helpers findActiveSet/findNextPendingSet hoje só filtrarem cutByReplan (gap WR-01 herdado da verificação da Fase 15), esta fase não deve ampliar em silêncio o raio de alcance desse gap ao lhe dar um novo ponto de entrada sem supervisão"
      status: flagged-unverified
    - statement: "MUST NOT deixar 'Pular descanso' avançar mais de uma série pendente por toque — cada toque na tela bloqueada é exatamente uma ação deliberada, espelhando a semântica de 'Pular descanso' do próprio app; nenhum comportamento de avanço em lote/cascata a partir de toque duplo ou de uma fila de intents acumulada"
      status: flagged-unverified
---

<objective>
Fatia tracer + expansão da Fase 16 (CMD): três `LiveActivityIntent`s
(`CompleteSetIntent`, `SkipRestIntent`, `AdjustRestIntent`) tornam o card de
Live Activity somente-leitura da Fase 15 interativo, sem trocar de
arquitetura de dados — o toque no botão termina no MESMO caminho
`completeSet()`/`adjustRest()`/`activateSet()` que já existe em
`src/store/activeSessionStore.ts`. Esta plano cobre o caminho "quente" (app
com a ponte JS já viva); a reconciliação para cold-launch (force-quit) é a
Plano 16-02, dedicada por recomendação explícita do RESEARCH.md ("bloquear a
Fase 16 em um plano dedicado de reconciliação... sem ele, o critério de
sucesso 3 não tem como passar de forma confiável").

Nota (probe spec-less, fallback): a Fase 16 não tem SPEC.md. O edge-probe
determinístico devolveu CMD-01 e CMD-02 como `unclassified/unresolved`
(nenhuma keyword de shape bateu) — não foram auto-backstopadas. Os
`must_haves.truths` acima foram derivados diretamente dos 3 success criteria
do ROADMAP para a Fase 16 (goal-backward), e essa lacuna do probe fica
registrada aqui como premissa explícita, nunca descartada em silêncio. A
varredura adversarial de proibições (Stage 1 recall -> Stage 2 precisão)
surfaceou as prohibitions acima, todas `flagged-unverified` (nenhuma foi
descartada em silêncio).

Nota de arquitetura (decisão do planner, não do RESEARCH.md): as cópias dos
três Intents em `targets/session-widget/` NÃO são bytes idênticos às cópias
em `modules/live-activity/ios/` (diferente do precedente de
`SessionActivityAttributes.swift`, que só usa `ActivityKit`, disponível nos
dois targets). `LiveActivityModule`/`IntentActionQueue` fazem parte do pod
`LiveActivityModule` (`ExpoModulesCore` como dependency, ver
`modules/live-activity/LiveActivityModule.podspec`), que NÃO é linkado no
target `session-widget` (seu `expo-target.config.js` só declara
`entitlements`, nenhuma dependency de pod). Como `perform()` de um
`LiveActivityIntent` SEMPRE roda no processo do app quando o intent também
está no target do app (RESEARCH.md Pattern 1, confirmado por fonte Apple
DTS), a cópia em `targets/session-widget/` nunca executa de verdade — existe
só para o `Button(intent:)` do SwiftUI compilar no target da extensão. Por
isso ela é um `perform()` trivial (`return .result()`), sem referenciar
`IntentActionQueue`/`LiveActivityModule`, documentado com um comentário
explicando o motivo.

Purpose: CMD-01 exige que o dono conclua a série atual com 1 toque no Lock
Screen pelo MESMO caminho já existente; CMD-02 exige pular/ajustar o
descanso com o timer nativo refletindo o ajuste imediatamente.
Output: os três `LiveActivityIntent`s funcionais no caminho "app já aberto
antes" (ponte JS viva), botões renderizados no card, e a suíte
`__tests__/liveActivityIntentBridge.test.ts` provando o despacho
evento->ação da store para os 3 tipos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-tela-bloqueada-comandar/16-RESEARCH.md
@.planning/phases/16-tela-bloqueada-comandar/16-PATTERNS.md
@modules/live-activity/ios/LiveActivityModule.swift
@modules/live-activity/index.ts
@targets/session-widget/WidgetLiveActivity.swift
@src/native/liveActivitySync.ts
@src/store/activeSessionStore.ts
@src/engine/sessionModel.ts
@App.tsx
@scripts/verify-native-skeleton.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ponta a ponta — "Concluir série" pela tela bloqueada (CMD-01, caminho quente)</name>
  <precondition>scripts/verify-native-skeleton.sh existe e sai com exit 0 no HEAD atual (herdado das Fases 14/15) antes desta task modificá-lo</precondition>
  <reversibility rating="costly">O formato dos registros da fila durável (IntentActionQueue: kind/deltaSeconds/sessionLogId/queuedAt) e o nome do evento onIntentAction viram o contrato que a Plano 16-02 (leitura da fila) e a Fase 17 (REG-02, mesmo canal de stepper) também vão consumir; mudar depois exige reabrir os arquivos Swift duplicados + o bridge TS + a Plano 16-02 em coordenação. Nenhum consumidor externo lê este contrato (app pessoal, sem publicação) — chato de mudar, não sem volta.</reversibility>
  <files>modules/live-activity/ios/CompleteSetIntent.swift, modules/live-activity/ios/IntentActionQueue.swift, modules/live-activity/ios/LiveActivityModule.swift, targets/session-widget/CompleteSetIntent.swift, targets/session-widget/WidgetLiveActivity.swift, modules/live-activity/index.ts, src/native/liveActivityIntentBridge.ts, App.tsx, __tests__/liveActivityIntentBridge.test.ts</files>
  <read_first>
    modules/live-activity/ios/LiveActivityModule.swift (arquivo inteiro, 102 linhas — hoje não declara Events/OnCreate/shared; `Name("LiveActivityModule")` é a primeira linha de `definition()`, as 5 AsyncFunctions vêm depois);
    modules/live-activity/index.ts (arquivo inteiro, 41 linhas — `LiveActivityModuleEvents` hoje é `{}`; `requireNativeModule<LiveActivityModuleType>('LiveActivityModule')` na linha 20);
    targets/session-widget/WidgetLiveActivity.swift (linhas 37-56 `secondaryLine`, 58-98 `primaryValue`, 110-132 `lockScreenBody` — o case `.measuring` está nas linhas 118-122; linhas 164-170 `ActivityConfiguration` com a cadeia `.padding().activityBackgroundTint(...).activitySystemActionForegroundColor(...)`);
    src/native/liveActivitySync.ts (padrão de módulo-singleton com listener/subscribe exportado: `startFailureListeners`, `subscribeLiveActivityStartFailure`, linhas 18-51);
    src/store/activeSessionStore.ts (linhas 1085-1109: `activateSet`/`adjustRest` — assinaturas exatas a reusar; linha 236 `inFlight`, linhas 1209-1265 `completeSet` — já idempotente por `status==='done'` e já trava reentrância por `(sessionLogId, plannedSetId)`, nenhuma mudança necessária nesta task);
    src/engine/sessionModel.ts (linhas 290-307: `findActiveSet`/`findNextPendingSet` — só filtram `cutByReplan`, não `skippedByUser`; comportamento herdado, ver prohibition desta plano);
    App.tsx (arquivo inteiro, 67 linhas — linhas 15-18 import de `initLiveActivitySync`/`reconcileOrphanActivities`, linhas 29-32 o `useEffect` que os monta e devolve `initLiveActivitySync()` como cleanup direto);
    __tests__/liveActivitySync.test.ts (linhas 1-60: padrão `jest.mock('../modules/live-activity', () => ({...}), { virtual: true })` + mock de `useActiveSessionStore` via `zustand.create` + fixture `draft()` completa — molde a seguir para o novo arquivo de teste)
  </read_first>
  <action>
    PARTE A — `modules/live-activity/ios/IntentActionQueue.swift` (novo, só `Foundation`,
    sem `ExpoModulesCore`): declarar `enum QueuedIntentActionKind: String, Codable {
    case completeSet, skipRest, adjustRest }`; `struct QueuedIntentAction: Codable { let
    kind: QueuedIntentActionKind; let deltaSeconds: Int?; let sessionLogId: String?; let
    queuedAt: String }`; `enum IntentActionQueue` com `private static let suiteName =
    "group.com.pmarconato.forcaapp.shared"`, `private static let key =
    "pendingLiveActivityIntentActions"`, `private static let maxEntries = 20` (cap
    explícito, mitigação de DoS local do RESEARCH.md Security Domain). Função `static
    func enqueue(_ action: QueuedIntentAction)`: lê o array atual de
    `UserDefaults(suiteName:)` via `JSONDecoder`, acrescenta a nova entrada, se
    `count > maxEntries` remove os mais antigos (`removeFirst`), regrava via
    `JSONEncoder`. Função `static func drainAll() -> [QueuedIntentAction]`: lê o array,
    remove a chave (`removeObject(forKey:)`) e devolve os itens lidos — código real, não
    chamado pelo lado JS ainda nesta task (só a Plano 16-02 adiciona o `AsyncFunction`
    que o expõe, mesmo padrão que `reconcileOrphans` ficou pronto e não-invocado na
    Plano 15-01).

    PARTE B — `modules/live-activity/ios/CompleteSetIntent.swift` (novo, `import
    AppIntents`, `import ActivityKit`): `struct CompleteSetIntent: LiveActivityIntent`
    (NUNCA `AppIntent` genérico — RESEARCH.md Pitfall 5) com `static var title:
    LocalizedStringResource { "Concluir série" }`. `func perform() async throws -> some
    IntentResult`: 1) resolve `sessionLogId` via
    `Activity<SessionActivityAttributes>.activities.first?.attributes.sessionLogId`
    (pode ser `nil` se nenhuma Activity estiver viva — grava mesmo assim, a Plano 16-02
    descarta entradas com `sessionLogId == nil` na reconciliação); 2) chama
    `IntentActionQueue.enqueue(QueuedIntentAction(kind: .completeSet, deltaSeconds: nil,
    sessionLogId: sessionLogId, queuedAt: ISO8601DateFormatter().string(from:
    Date())))` — durável PRIMEIRO; 3) chama `LiveActivityModule.shared?.sendEvent(
    "onIntentAction", ["kind": "completeSet"])` — round-trip in-process, só chega ao JS
    se a bridge já estiver viva; 4) `return .result()`. Nenhuma chamada de rede, nenhuma
    lógica de "qual é a série atual" em Swift (RESEARCH.md Anti-Patterns).

    `targets/session-widget/CompleteSetIntent.swift` (novo): `struct CompleteSetIntent:
    LiveActivityIntent` com o MESMO `title`, mas `perform()` trivial —
    `func perform() async throws -> some IntentResult { return .result() }` — com um
    comentário no topo do arquivo explicando que este `perform()` nunca executa de fato
    (o iOS sempre roteia para o processo do app quando o intent também está lá,
    RESEARCH.md Pattern 1) e existe só para `Button(intent: CompleteSetIntent())`
    compilar no target `session-widget`, que não linka `ExpoModulesCore`.

    PARTE C — `modules/live-activity/ios/LiveActivityModule.swift`: dentro de
    `definition()`, ANTES das `AsyncFunction`s já existentes, acrescentar
    `static weak var shared: LiveActivityModule?` (propriedade estática da classe, fora
    de `definition()`), `OnCreate { LiveActivityModule.shared = self }`, e
    `Events("onIntentAction")` — ordem "Name -> lifecycle hooks -> events -> functions"
    (Expo Modules API). As 5 `AsyncFunction`s existentes (`startActivity`,
    `updateActivity`, `endActivity`, `isActivityRunning`, `reconcileOrphans`)
    permanecem sem nenhuma mudança de assinatura ou corpo.

    PARTE D — `targets/session-widget/WidgetLiveActivity.swift`: no case `.measuring`
    de `lockScreenBody` (hoje só `primaryValue(state)` + `secondaryLine(state)` dentro
    do `VStack`), acrescentar `Button(intent: CompleteSetIntent()) { Text("Concluir
    série") }` como terceiro filho do `VStack`, com `.tint(activityNeon)` (cor já
    fixada no topo do arquivo, mesma usada no timer/valor primário — nenhuma cor nova
    sem passar por `/gsd-ui-phase 16`, RESEARCH.md Open Question 1). Na
    `ActivityConfiguration` (linha ~166-170), acrescentar
    `.widgetURL(URL(string: "forcaapp://session/active"))` à cadeia existente após
    `.activitySystemActionForegroundColor(Color.white)` — reabertura do app pelo CORPO
    do card, nunca por dentro de um `LiveActivityIntent` (RESEARCH.md Pitfall 2); o
    scheme `forcaapp` já está registrado em `app.json` (`"scheme": "forcaapp"`), nenhuma
    mudança de `RootNavigator.js`/`linking.js` é necessária — um deep link não-mapeado
    ainda assim relança o app na tela padrão, que é o suficiente para o critério de
    sucesso 3 do ROADMAP ("app reaberto para concluir").

    PARTE E — `modules/live-activity/index.ts`: trocar `type LiveActivityModuleEvents =
    {}` por `type LiveActivityModuleEvents = { onIntentAction: (event:
    LiveActivityIntentActionEvent) => void }`. Exportar o novo tipo `export type
    LiveActivityIntentActionEvent = { kind: 'completeSet' } | { kind: 'skipRest' } | {
    kind: 'adjustRest'; deltaSeconds: number }` (união discriminada, per convenção
    TypeScript do projeto — nunca `any`). Exportar `export const
    subscribeLiveActivityIntentAction = (listener: (event:
    LiveActivityIntentActionEvent) => void): (() => void) => { const subscription =
    LiveActivityModule.addListener('onIntentAction', listener); return () =>
    subscription.remove(); }` — usa o `.addListener` já tipado que `NativeModule<Events>`
    do Expo Modules API expõe automaticamente sobre a constante `LiveActivityModule` já
    existente no arquivo (linha 20), sem precisar exportar a instância crua do módulo.

    PARTE F — `src/native/liveActivityIntentBridge.ts` (novo, molde:
    `src/native/liveActivitySync.ts`): importar
    `subscribeLiveActivityIntentAction`/`LiveActivityIntentActionEvent` de
    `'../../modules/live-activity'`, `findActiveSet`/`findNextPendingSet` de
    `'../engine/sessionModel'`, `useActiveSessionStore` de
    `'../store/activeSessionStore'`. Função privada `handleIntentAction(event:
    LiveActivityIntentActionEvent): void` com um `switch (event.kind)`: case
    `'completeSet'` — `const draft = useActiveSessionStore.getState().draft; if
    (!draft) return; const alvo = findActiveSet(draft) ?? findNextPendingSet(draft); if
    (alvo) void useActiveSessionStore.getState().completeSet(alvo.exercise.exerciseId,
    alvo.set.setOrder);`; case `'skipRest'` — resolve `draft` da mesma forma, `const
    proxima = findNextPendingSet(draft); if (proxima)
    useActiveSessionStore.getState().activateSet(proxima.exercise.exerciseId,
    proxima.set.setOrder);`; case `'adjustRest'` —
    `useActiveSessionStore.getState().adjustRest(event.deltaSeconds);`. Exportar `export
    const registerLiveActivityIntentListener = (): (() => void) =>
    subscribeLiveActivityIntentAction(handleIntentAction);` — nenhuma lógica de domínio
    nova, só despacho.

    PARTE G — `App.tsx`: importar `registerLiveActivityIntentListener` de
    `'./src/native/liveActivityIntentBridge'`. No `useEffect` das linhas 29-32, trocar o
    corpo — em vez de `return initLiveActivitySync();` — para capturar os dois
    unsubscribes (`const unsubscribeSync = initLiveActivitySync(); const
    unsubscribeIntents = registerLiveActivityIntentListener();`) e devolver uma função
    de cleanup que chama os dois. A chamada a `reconcileOrphanActivities()` continua a
    primeira linha do efeito, sem mudança de ordem relativa a esta task (a Plano 16-02
    reordena para inserir `reconcileLiveActivityIntents()` antes dela).

    Testes: `__tests__/liveActivityIntentBridge.test.ts` (novo, molde
    `__tests__/liveActivitySync.test.ts`) — `jest.mock('../modules/live-activity', () =>
    ({ subscribeLiveActivityIntentAction: jest.fn() }), { virtual: true })` NÃO é o
    padrão aqui porque o arquivo sob teste importa a função direto; em vez disso mockar
    `'../src/store/activeSessionStore'` com um objeto `{ useActiveSessionStore: {
    getState: jest.fn() } }` cujo `getState()` devolve `{ draft, completeSet: jest.fn(),
    activateSet: jest.fn(), adjustRest: jest.fn() }` reconfigurável por teste (fixture
    de `SessionDraft` no mesmo formato de `draft()` em `liveActivitySync.test.ts`, com
    pelo menos um exercício com uma série `active` e outro com uma série `pending`).
    Casos: (1) `kind: 'completeSet'` com série ativa presente chama `completeSet` com
    `(exerciseId, setOrder)` corretos da série ativa; (2) `kind: 'completeSet'` sem
    série ativa mas com série pendente chama `completeSet` sobre a PRÓXIMA pendente;
    (3) `kind: 'skipRest'` chama `activateSet` sobre a série pendente, NUNCA
    `completeSet`; (4) `kind: 'adjustRest', deltaSeconds: -30` chama `adjustRest(-30)`
    com o valor exato; (5) `draft === null` não chama nenhuma ação (guarda de
    segurança); (6) invocar `registerLiveActivityIntentListener()` chama
    `subscribeLiveActivityIntentAction` exatamente uma vez com a função de handler.
  </action>
  <behavior>
    - `handleIntentAction({kind:'completeSet'})` com série `active` presente -> chama `completeSet(exerciseId, setOrder)` da série ativa, nunca da pendente.
    - `handleIntentAction({kind:'completeSet'})` sem série ativa, com série `pending` -> chama `completeSet` sobre a série pendente.
    - `handleIntentAction({kind:'skipRest'})` -> chama `activateSet` sobre a próxima série pendente; nunca chama `completeSet`.
    - `handleIntentAction({kind:'adjustRest', deltaSeconds:-30})` -> chama `adjustRest(-30)` com o valor exato recebido.
    - `handleIntentAction(...)` com `draft === null` -> nenhuma ação da store é chamada.
  </behavior>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx jest __tests__/liveActivityIntentBridge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc --noEmit` sai 0. `__tests__/liveActivityIntentBridge.test.ts` passa 100%
    cobrindo os 6 casos do parágrafo de testes acima.
    `grep -c "LiveActivityIntent" modules/live-activity/ios/CompleteSetIntent.swift`
    retorna >= 1 (conforma ao protocolo certo, não `AppIntent` genérico).
    `grep -c "IntentActionQueue.enqueue" modules/live-activity/ios/CompleteSetIntent.swift`
    retorna >= 1 e a mesma busca em `targets/session-widget/CompleteSetIntent.swift`
    retorna 0 (confirma que a cópia da extensão é o stub trivial, não referencia a
    fila). `grep -c "registerLiveActivityIntentListener" App.tsx` retorna >= 1.
  </acceptance_criteria>
  <done>CompleteSetIntent real grava na fila durável e emite onIntentAction; o bridge JS despacha para completeSet() com a série correta; o botão "Concluir série" aparece no card em .measuring; suíte de teste e tsc verdes.</done>
</task>

<task type="auto">
  <name>Task 2: SkipRestIntent + AdjustRestIntent — completa CMD-02 sobre o bridge já provado</name>
  <files>modules/live-activity/ios/SkipRestIntent.swift, modules/live-activity/ios/AdjustRestIntent.swift, targets/session-widget/SkipRestIntent.swift, targets/session-widget/AdjustRestIntent.swift, targets/session-widget/WidgetLiveActivity.swift, scripts/verify-native-skeleton.sh</files>
  <read_first>
    modules/live-activity/ios/CompleteSetIntent.swift (criado na Task 1 — molde exato de estrutura a replicar para os dois novos intents, incluindo a ordem enqueue-antes-de-sendEvent);
    targets/session-widget/CompleteSetIntent.swift (criado na Task 1 — molde do stub trivial da extensão);
    targets/session-widget/WidgetLiveActivity.swift (case `.resting` de `lockScreenBody`, linhas 113-117 no arquivo original — hoje só `primaryValue`+`secondaryLine`);
    scripts/verify-native-skeleton.sh (linhas 89-122 — checagens (d)/(e) de autolinking/Podfile.lock; ponto de inserção da nova checagem (g) logo após a checagem (f), antes do `amarelo "  Rodada..."` final)
  </read_first>
  <action>
    PARTE A — `modules/live-activity/ios/SkipRestIntent.swift` (novo): `struct
    SkipRestIntent: LiveActivityIntent` com `static var title: LocalizedStringResource {
    "Pular descanso" }`, sem `@Parameter`. `perform()` idêntico em estrutura ao de
    `CompleteSetIntent` (Task 1): resolve `sessionLogId` da mesma forma, chama
    `IntentActionQueue.enqueue(QueuedIntentAction(kind: .skipRest, deltaSeconds: nil,
    sessionLogId: sessionLogId, queuedAt: ...))`, depois
    `LiveActivityModule.shared?.sendEvent("onIntentAction", ["kind": "skipRest"])`.

    `modules/live-activity/ios/AdjustRestIntent.swift` (novo): `struct
    AdjustRestIntent: LiveActivityIntent` com `static var title: LocalizedStringResource
    { "Ajustar descanso" }`, `@Parameter(title: "Delta em segundos") var deltaSeconds:
    Int`, `init() {}` e `init(deltaSeconds: Int) { self.deltaSeconds = deltaSeconds }`
    (os dois inits — RESEARCH.md Code Examples). `perform()`: enfileira
    `QueuedIntentAction(kind: .adjustRest, deltaSeconds: deltaSeconds, sessionLogId:
    ..., queuedAt: ...)`, depois `LiveActivityModule.shared?.sendEvent("onIntentAction",
    ["kind": "adjustRest", "deltaSeconds": deltaSeconds])`.

    PARTE B — `targets/session-widget/SkipRestIntent.swift` e
    `targets/session-widget/AdjustRestIntent.swift` (novos, stubs triviais igual ao
    padrão de `targets/session-widget/CompleteSetIntent.swift` da Task 1): mesmo
    `title`/`@Parameter` (a extensão TAMBÉM precisa do `@Parameter deltaSeconds` em
    `AdjustRestIntent` para o `Button(intent: AdjustRestIntent(deltaSeconds: -30))`
    compilar), mas `perform()` sempre `return .result()`, com o mesmo comentário de
    "nunca executa de fato" da Task 1.

    PARTE C — `targets/session-widget/WidgetLiveActivity.swift`, case `.resting` de
    `lockScreenBody`: acrescentar, como terceiro filho do `VStack` (depois de
    `primaryValue(state)` e `secondaryLine(state)`), um `HStack` com três botões:
    `Button(intent: AdjustRestIntent(deltaSeconds: -30)) { Text("-30s") }`,
    `Button(intent: SkipRestIntent()) { Text("Pular") }`,
    `Button(intent: AdjustRestIntent(deltaSeconds: 30)) { Text("+30s") }` — todos com
    `.tint(activityNeon)` (mesma cor da Task 1, consistência visual). Rótulos curtos
    (`-30s`/`Pular`/`+30s`) por causa do espaço apertado do Lock Screen (RESEARCH.md
    Open Question 1) — se a Plano 16-03 (UAT física) reportar aperto visual real, é o
    dono quem decide o ajuste, não presumir agora.

    PARTE D — `scripts/verify-native-skeleton.sh`: acrescentar uma checagem (g) logo
    após a checagem (f) (antes de `amarelo "  Rodada ${rodada}: (a)-(f) OK."` — trocar
    essa mensagem para `(a)-(g)`): para cada um dos três nomes de struct
    (`CompleteSetIntent`, `SkipRestIntent`, `AdjustRestIntent`), confirmar com `grep -q`
    que o struct está declarado TANTO em
    `modules/live-activity/ios/${nome}.swift` QUANTO em
    `targets/session-widget/${nome}.swift` (dois arquivos, presença apenas — não diff
    de conteúdo, já que os pares NÃO são bytes-idênticos por design nesta fase, ver
    Nota de arquitetura do objective). Se qualquer um dos seis arquivos faltar ou não
    declarar o struct esperado, abortar com `vermelho "ABORTADO: [rodada ${rodada}]
    ${nome} não existe nos dois targets (app + extensão)."` e `exit 1`, mesmo padrão de
    mensagem das checagens (b)-(f) já existentes.
  </action>
  <verify>
    <automated>npx jest __tests__/liveActivityIntentBridge.test.ts &amp;&amp; npm run verify:native</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/liveActivityIntentBridge.test.ts` continua verde (regressão —
    nenhuma mudança nesta task altera o bridge JS). `npm run verify:native` sai 0,
    incluindo a nova checagem (g) confirmando os 6 arquivos de Intent presentes nos
    dois targets. `grep -c "Button(intent: SkipRestIntent" targets/session-widget/WidgetLiveActivity.swift`
    e `grep -c "Button(intent: AdjustRestIntent" targets/session-widget/WidgetLiveActivity.swift`
    retornam ambos >= 1.
  </acceptance_criteria>
  <done>SkipRestIntent e AdjustRestIntent existem nos dois targets, os botões -30s/Pular/+30s aparecem no case .resting do card, e scripts/verify-native-skeleton.sh prova (g) que nenhum dos seis arquivos de Intent foi esquecido em um dos dois targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tela bloqueada (extensão de widget) -> processo do app principal | `Button(intent:)` cruza para `perform()` rodando no processo do app — ponto onde uma ação do dono vira gravação real via `completeSet()`/outbox/servidor |
| Fila durável do App Group | `UserDefaults(suiteName:)` compartilhado entre extensão e app — durabilidade para quando o processo do app não estava vivo no momento do toque |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01-01 | Tampering | IntentActionQueue (fila do App Group) crescendo sem limite | high | mitigate | `maxEntries = 20` no `enqueue()` — nunca acumula indefinidamente mesmo se o app nunca reabrir |
| T-16-01-02 | Repudiation / Tampering | Toque duplo/rápido contornando o App Intent (RESEARCH.md Pitfall 4, thread 739243) | low | accept | `completeSet` já é idempotente por `status==='done'` e já trava reentrância por `(sessionLogId, plannedSetId)` (`activeSessionStore.ts:1220-1228`) — nenhuma mitigação nova necessária, o novo call site passa pelo MESMO `completeSet` |
| T-16-01-03 | Information Disclosure | Botões de ação visíveis a qualquer pessoa que veja o aparelho fisicamente bloqueado | low | accept | Mesma aceitação já registrada na Fase 15 (T-15-01-03) — app pessoal single-user, sem dado sensível além do próprio treino |
| T-16-01-04 | Elevation of Privilege | `perform()` tentando abrir o app ou executar trabalho pesado (rede, replanejamento) | high | mitigate | Os três `perform()` desta plano só enfileiram e emitem evento — nenhuma chamada de rede, nenhum `Link`/navegação dentro do intent (RESEARCH.md Pitfall 2, Out of Scope do REQUIREMENTS.md) |
</threat_model>

## Artifacts this phase produces

- `modules/live-activity/ios/CompleteSetIntent.swift`, `SkipRestIntent.swift`,
  `AdjustRestIntent.swift` — `LiveActivityIntent`s reais (perform() no processo do app)
- `modules/live-activity/ios/IntentActionQueue.swift` — `QueuedIntentActionKind`,
  `QueuedIntentAction`, `IntentActionQueue.enqueue`/`drainAll` (fila durável App Group)
- `targets/session-widget/CompleteSetIntent.swift`, `SkipRestIntent.swift`,
  `AdjustRestIntent.swift` — stubs de compilação (perform() nunca executa) para o
  `Button(intent:)` da extensão
- `modules/live-activity/ios/LiveActivityModule.swift` — `static weak var shared`,
  `OnCreate`, `Events("onIntentAction")` (novos; AsyncFunctions existentes inalteradas)
- `modules/live-activity/index.ts` — `LiveActivityIntentActionEvent` (tipo),
  `subscribeLiveActivityIntentAction`
- `src/native/liveActivityIntentBridge.ts` — `registerLiveActivityIntentListener`
- `targets/session-widget/WidgetLiveActivity.swift` — botões `Button(intent:)` em
  `.measuring` (Concluir série) e `.resting` (-30s/Pular/+30s), `.widgetURL(...)`
- `scripts/verify-native-skeleton.sh` — checagem (g), presença dos 6 arquivos de Intent
- `__tests__/liveActivityIntentBridge.test.ts` — cobertura do despacho evento->ação

<verification>
`npx tsc --noEmit` limpo; `__tests__/liveActivityIntentBridge.test.ts` verde cobrindo
completeSet/skipRest/adjustRest; `npm run verify:native` sai 0 incluindo a checagem (g).
Comportamento físico real (toque no Lock Screen, timer refletindo o ajuste) NÃO é
verificável aqui — depende da Plano 16-03 (UAT física). O caminho de cold-launch
(force-quit) depende da Plano 16-02.
</verification>

<success_criteria>
Os três `LiveActivityIntent`s existem nos dois targets, compilam (via
`verify:native`), e o bridge JS despacha corretamente para `completeSet`/`activateSet`/
`adjustRest` já existentes — provado por teste automatizado, sem depender de aparelho
físico para o caminho "app já vivo".
</success_criteria>

<output>
Create `.planning/phases/16-tela-bloqueada-comandar/16-01-SUMMARY.md` when done
</output>
