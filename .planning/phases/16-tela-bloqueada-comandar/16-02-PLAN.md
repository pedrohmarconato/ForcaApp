---
phase: 16-tela-bloqueada-comandar
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - modules/live-activity/ios/LiveActivityModule.swift
  - modules/live-activity/index.ts
  - src/store/activeSessionStore.ts
  - App.tsx
  - __tests__/liveActivityIntentQueue.test.ts
autonomous: true
requirements: [CMD-01, CMD-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Na próxima abertura do app em foreground (não no cold-launch em si), reconcileLiveActivityIntents() drena a fila do App Group e aplica cada intenção pendente contra o MESMO completeSet()/activateSet()/adjustRest() já existentes — nenhum caminho de gravação paralelo (CMD-01, CMD-02, critério de sucesso 3 do ROADMAP)"
    - "Uma intenção drenada cujo sessionLogId não bate com o draft.sessionLogId atual (ou cujo sessionLogId é null) é descartada silenciosamente do ponto de vista de gravação — nunca aplicada contra a sessão errada"
    - "A fila do App Group é sempre completamente drenada (nunca deixa resíduo) ao reconciliar, mesmo quando nenhuma entrada é aplicável"
  artifacts:
    - path: "modules/live-activity/ios/LiveActivityModule.swift"
      provides: "AsyncFunction drainIntentQueue() -> [QueuedIntentActionRecord], lê+limpa a fila via IntentActionQueue.drainAll()"
    - path: "src/store/activeSessionStore.ts"
      provides: "reconcileLiveActivityIntents(): Promise<void> — CAS por sessionLogId, mesmo padrão de reconcileRemoteSessionClosed"
  key_links:
    - from: "App.tsx (useEffect de boot)"
      to: "src/store/activeSessionStore.ts (reconcileLiveActivityIntents)"
      via: "chamado ANTES de reconcileOrphanActivities() no mesmo efeito de montagem"
      pattern: "reconcileLiveActivityIntents"
    - from: "src/store/activeSessionStore.ts (reconcileLiveActivityIntents)"
      to: "modules/live-activity/index.ts (drainQueuedLiveActivityIntents)"
      via: "await drainQueuedLiveActivityIntents() — única leitura da fila do App Group"
      pattern: "drainQueuedLiveActivityIntents"
  prohibitions:
    - statement: "MUST NOT aplicar uma intenção drenada da fila do App Group contra uma sessão que não é mais a CURRENTLY ACTIVE (dono iniciou outra sessão, ou a sessão que gerou a intenção já terminou/foi cancelada) entre o toque (ou a gravação na fila) e o momento em que ela é processada — toda intenção reconciliada reconfirma sessionLogId contra o draft ATUAL antes de escrever, exatamente como o guard de CAS que completeSet()/finishSession() já usam"
      status: flagged-unverified
---

<objective>
Plano dedicada à reconciliação de cold-launch — recomendação explícita do
RESEARCH.md ("sem ele, o critério de sucesso 3 não tem como passar de forma
confiável"). A Plano 16-01 entregou o caminho "quente" (ponte JS já viva
quando o toque acontece); esta plano fecha o caminho "frio": quando o dono
força o fechamento do app e depois toca um botão da tela bloqueada, o
`perform()` já grava a intenção na fila durável do App Group (Plano 16-01,
`IntentActionQueue.enqueue`) mesmo que o evento in-process se perca (a
bridge Hermes/RN pode não estar de pé — RESEARCH.md Pitfall 3, Assumption
A1). Esta plano expõe a LEITURA dessa fila para o JS e a aplica, com guarda
de CAS por `sessionLogId`, na PRÓXIMA vez que o app abrir em foreground —
mesmo momento em que `reconcileOrphanActivities()` (Fase 15) já roda hoje.

Depende da Plano 16-01 por tocar os mesmos arquivos (`LiveActivityModule.swift`,
`modules/live-activity/index.ts`, `App.tsx`) que ela criou/modificou — não
pode rodar em paralelo (Wave 2).

Purpose: fecha o gap de arquitetura que nenhuma fonte documental garante
(RESEARCH.md Assumption A1: "nenhuma fonte documental encontrada garante um
tempo mínimo" de execução do `perform()` até a bridge JS subir) — a fila
durável é o único mecanismo confiável quando essa janela falha.
Output: `reconcileLiveActivityIntents()` na store, wired em `App.tsx`, e o
teste que prova a drenagem + guarda de sessão.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-tela-bloqueada-comandar/16-RESEARCH.md
@.planning/phases/16-tela-bloqueada-comandar/16-01-SUMMARY.md
@src/store/activeSessionStore.ts
@src/native/liveActivitySync.ts
@App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expor a fila durável para o JS — drainIntentQueue()</name>
  <precondition>modules/live-activity/ios/LiveActivityModule.swift já declara Events("onIntentAction")/OnCreate/shared e modules/live-activity/ios/IntentActionQueue.swift já expõe IntentActionQueue.drainAll() (Plano 16-01, Task 1) antes desta task adicionar a nova AsyncFunction</precondition>
  <files>modules/live-activity/ios/LiveActivityModule.swift, modules/live-activity/index.ts</files>
  <read_first>
    modules/live-activity/ios/LiveActivityModule.swift (linhas 4-19: `LiveActivityContentStateRecord: Record` — molde EXATO de `@Field` a seguir para o novo record; linhas 49-101: `definition()` completo, incluindo as 5 `AsyncFunction`s já existentes e os blocos `OnCreate`/`Events` que a Plano 16-01 adicionou);
    modules/live-activity/ios/IntentActionQueue.swift (criado na Plano 16-01 — `QueuedIntentAction`/`drainAll()` exatos a mapear para o novo Record);
    modules/live-activity/index.ts (arquivo inteiro pós-Plano-16-01 — `LiveActivityModuleType`/`LiveActivityIntentActionEvent`/`subscribeLiveActivityIntentAction` já existem; padrão de wrapper async fino, ex. `reconcileLiveActivityOrphans`, linhas 39-41)
  </read_first>
  <action>
    PARTE A — `modules/live-activity/ios/LiveActivityModule.swift`: declarar (fora de
    `definition()`, ao lado de `LiveActivityContentStateRecord`) `public struct
    QueuedIntentActionRecord: Record { @Field var kind: String = ""; @Field var
    deltaSeconds: Int? = nil; @Field var sessionLogId: String? = nil; @Field var
    queuedAt: String = ""; public init() {} }`. Dentro de `definition()`, depois das 5
    `AsyncFunction`s já existentes, acrescentar `AsyncFunction("drainIntentQueue") { ()
    -> [QueuedIntentActionRecord] in IntentActionQueue.drainAll().map { action in var
    record = QueuedIntentActionRecord(); record.kind = action.kind.rawValue;
    record.deltaSeconds = action.deltaSeconds; record.sessionLogId =
    action.sessionLogId; record.queuedAt = action.queuedAt; return record } }` —
    conversão pura de `QueuedIntentAction` (Codable, definido na Plano 16-01) para o
    `Record` que a ponte Expo consegue serializar para JS, sem lógica nova de negócio.

    PARTE B — `modules/live-activity/index.ts`: acrescentar ao tipo
    `LiveActivityModuleType` (interface que estende `NativeModule<LiveActivityModuleEvents>`)
    a assinatura `drainIntentQueue(): Promise<QueuedLiveActivityIntent[]>`. Exportar o
    novo tipo `export type QueuedLiveActivityIntent = { kind: 'completeSet' |
    'skipRest' | 'adjustRest'; deltaSeconds: number | null; sessionLogId: string |
    null; queuedAt: string }` (união de string literal para `kind`, nunca `any`).
    Exportar `export const drainQueuedLiveActivityIntents = ():
    Promise<QueuedLiveActivityIntent[]> => LiveActivityModule.drainIntentQueue()` —
    mesmo padrão fino dos outros wrappers do arquivo (`reconcileLiveActivityOrphans`
    etc.).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc --noEmit` sai 0.
    `grep -c "drainQueuedLiveActivityIntents" modules/live-activity/index.ts` retorna
    >= 1. `grep -c "AsyncFunction(\"drainIntentQueue\")" modules/live-activity/ios/LiveActivityModule.swift`
    retorna 1 (exatamente uma declaração, não duplicada).
  </acceptance_criteria>
  <done>drainQueuedLiveActivityIntents() existe e é tipado; a fila do App Group é lida e limpa através da ponte Expo, sem lógica de negócio nova no Swift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reconcileLiveActivityIntents() com guarda de CAS por sessão, wired no boot</name>
  <files>src/store/activeSessionStore.ts, App.tsx, __tests__/liveActivityIntentQueue.test.ts</files>
  <read_first>
    src/store/activeSessionStore.ts (linhas 124-235: shape de `ActiveSessionState`/interface das ações — ponto de inserção da nova assinatura `reconcileLiveActivityIntents: () => Promise<void>`; linhas 1528-1545: `reconcileRemoteSessionClosed` — molde do padrão "reconfere sessionLogId antes de aplicar"; linhas 1085-1109: `activateSet`/`adjustRest` — assinaturas exatas a chamar; linhas 1209-1265: `completeSet` — assinatura exata);
    src/engine/sessionModel.ts (linhas 290-307: `findActiveSet`/`findNextPendingSet`, já importadas em `src/native/liveActivityIntentBridge.ts` da Plano 16-01 — mesmo padrão de resolução de série);
    App.tsx (pós-Plano-16-01: `useEffect` com `reconcileOrphanActivities()`,
    `initLiveActivitySync()`, `registerLiveActivityIntentListener()`);
    __tests__/liveActivitySync.test.ts (linhas 1-60: fixture `draft()` completa e padrão de `jest.mock('../modules/live-activity', ..., { virtual: true })` — molde do novo arquivo de teste)
  </read_first>
  <action>
    PARTE A — `src/store/activeSessionStore.ts`: importar
    `drainQueuedLiveActivityIntents`/`type QueuedLiveActivityIntent` de
    `'../../modules/live-activity'`. Na interface `ActiveSessionState`, acrescentar
    `reconcileLiveActivityIntents: () => Promise<void>` ao lado de
    `reconcileRemoteSessionClosed`. Implementação (dentro de `create<ActiveSessionState>`):
    `reconcileLiveActivityIntents: async () => { let entries:
    QueuedLiveActivityIntent[] = []; try { entries = await
    drainQueuedLiveActivityIntents(); } catch (error) {
    console.warn('[liveActivity] não foi possível drenar a fila de intents:', error);
    return; } for (const entry of entries) { const draft = get().draft; if (!draft ||
    draft.status !== 'active' || !entry.sessionLogId || draft.sessionLogId !==
    entry.sessionLogId) continue; switch (entry.kind) { case 'completeSet': { const
    alvo = findActiveSet(draft) ?? findNextPendingSet(draft); if (alvo) await
    get().completeSet(alvo.exercise.exerciseId, alvo.set.setOrder); break; } case
    'skipRest': { const proxima = findNextPendingSet(draft); if (proxima)
    get().activateSet(proxima.exercise.exerciseId, proxima.set.setOrder); break; } case
    'adjustRest': { if (entry.deltaSeconds != null)
    get().adjustRest(entry.deltaSeconds); break; } } } }` — guarda de CAS: cada
    iteração relê `get().draft` (não uma cópia capturada antes do loop), reconferindo
    `sessionLogId` a CADA entrada, porque `completeSet` dentro do loop pode ela mesma
    mudar o draft antes da próxima iteração processar. `entry.sessionLogId == null` é
    SEMPRE descartado (nunca aplicado contra sessão ambígua — prohibition desta plano).

    PARTE B — `App.tsx`: reordenar o `useEffect` de boot para chamar
    `useActiveSessionStore.getState().reconcileLiveActivityIntents()` ANTES de
    `reconcileOrphanActivities()` (aplica ações pendentes na store primeiro, depois
    sincroniza o card nativo com o estado já atualizado) — usar `.then()`/`.finally()`
    encadeado, não `Promise.all` (ordem importa): `void
    useActiveSessionStore.getState().reconcileLiveActivityIntents().finally(() => {
    void reconcileOrphanActivities(); });`. `initLiveActivitySync()` e
    `registerLiveActivityIntentListener()` continuam chamados de forma síncrona no
    mesmo efeito, sem esperar as duas promises acima (mesma ordem relativa que a Plano
    16-01 já estabeleceu).

    Testes: `__tests__/liveActivityIntentQueue.test.ts` (novo, molde
    `__tests__/liveActivitySync.test.ts`) — `jest.mock('../modules/live-activity', ()
    => ({ drainQueuedLiveActivityIntents: jest.fn() }), { virtual: true })`, store real
    via `zustand` (não mockada, para exercitar `completeSet`/`activateSet`/`adjustRest`
    de verdade contra um draft fixture, OU mockar as três ações individualmente se o
    fixture completo for custoso — decisão do executor, mantendo os asserts sobre QUAL
    ação foi chamada e com QUE argumentos). Casos: (1) fila com uma entrada
    `completeSet` cujo `sessionLogId` bate com `draft.sessionLogId` -> aplica
    `completeSet` sobre a série ativa/pendente resolvida; (2) entrada com
    `sessionLogId` DIFERENTE do draft atual -> NÃO aplica nenhuma ação (CAS); (3)
    entrada com `sessionLogId: null` -> NÃO aplica (descartada por ambiguidade); (4)
    fila vazia -> nenhuma ação chamada, sem erro; (5) `drainQueuedLiveActivityIntents`
    rejeitando (erro de bridge) -> `reconcileLiveActivityIntents()` não lança, resolve
    normalmente; (6) entrada `adjustRest` com `deltaSeconds: 45` -> chama
    `adjustRest(45)` com o valor exato.
  </action>
  <behavior>
    - Entrada `completeSet` com `sessionLogId` igual ao `draft.sessionLogId` atual -> `completeSet` é chamado com a série resolvida.
    - Entrada com `sessionLogId` diferente do draft atual -> nenhuma ação de gravação é chamada (CAS).
    - Entrada com `sessionLogId: null` -> nenhuma ação de gravação é chamada.
    - `drainQueuedLiveActivityIntents()` rejeitando -> `reconcileLiveActivityIntents()` resolve sem lançar.
  </behavior>
  <verify>
    <automated>npx jest __tests__/liveActivityIntentQueue.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/liveActivityIntentQueue.test.ts` passa 100% cobrindo os 6 casos
    acima. `npx tsc --noEmit` sai 0. `grep -c "reconcileLiveActivityIntents"
    App.tsx` retorna >= 1. Leitura manual de `src/store/activeSessionStore.ts` confirma
    que `reconcileLiveActivityIntents` relê `get().draft` A CADA iteração do loop, não
    uma única vez antes dele (evita aplicar contra um draft já obsoleto por uma
    iteração anterior).
  </acceptance_criteria>
  <done>reconcileLiveActivityIntents() drena e aplica a fila do App Group com guarda de CAS por sessionLogId, chamado no boot do app antes de reconcileOrphanActivities(); suíte de teste dedicada e tsc verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fila durável do App Group -> `activeSessionStore` | Leitura de dado escrito por um processo potencialmente já encerrado (cold-launch) — a store precisa decidir se o que está na fila ainda é aplicável |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02-01 | Tampering | `reconcileLiveActivityIntents()` aplicando uma intenção contra a sessão ERRADA | high | mitigate | Guarda de CAS por `sessionLogId` reconferida a cada iteração do loop contra `get().draft` atual (não uma cópia capturada antes); entradas com `sessionLogId` nulo ou divergente são sempre descartadas |
| T-16-02-02 | Denial of Service (local) | Fila do App Group nunca sendo drenada se o app nunca reabrir | medium | mitigate | O cap de 20 entradas já aplicado no `enqueue()` (Plano 16-01) limita o pior caso; esta plano garante que toda reabertura em foreground drena e limpa a fila por inteiro |
</threat_model>

## Artifacts this phase produces

- `modules/live-activity/ios/LiveActivityModule.swift` — `QueuedIntentActionRecord`,
  `AsyncFunction("drainIntentQueue")`
- `modules/live-activity/index.ts` — `QueuedLiveActivityIntent` (tipo),
  `drainQueuedLiveActivityIntents`
- `src/store/activeSessionStore.ts` — `reconcileLiveActivityIntents()`
- `__tests__/liveActivityIntentQueue.test.ts` — cobertura da drenagem + guarda de CAS

<verification>
`npx tsc --noEmit` limpo; `__tests__/liveActivityIntentQueue.test.ts` verde. O
comportamento real de cold-launch (force-quit físico seguido de toque) só é
comprovável na Plano 16-03 (UAT física) — esta plano prova a LÓGICA de reconciliação
de forma determinística, não o timing real do `perform()` em aparelho.
</verification>

<success_criteria>
A fila do App Group é sempre drenada e aplicada com guarda de sessão na próxima
abertura do app, com cobertura automatizada da lógica de CAS — pré-condição necessária
(não suficiente sozinha) para o critério de sucesso 3 do ROADMAP.
</success_criteria>

<output>
Create `.planning/phases/16-tela-bloqueada-comandar/16-02-SUMMARY.md` when done
</output>
