---
phase: 17-tela-bloqueada-registrar-e-antecipar
plan: 03
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - modules/live-activity/ios/IntentActionQueue.swift
  - targets/session-widget/AdjustRepsIntent.swift
  - modules/live-activity/ios/AdjustRepsIntent.swift
  - targets/session-widget/SessionActivityAttributes.swift
  - modules/live-activity/ios/SessionActivityAttributes.swift
  - targets/session-widget/WidgetLiveActivity.swift
  - src/engine/liveActivityContentState.ts
  - src/native/liveActivitySync.ts
  - src/native/liveActivityIntentBridge.ts
  - modules/live-activity/index.ts
  - src/store/activeSessionStore.ts
  - scripts/verify-native-skeleton.sh
  - __tests__/liveActivityContentState.test.ts
  - __tests__/liveActivityIntentBridge.test.ts
  - __tests__/liveActivityIntentQueue.test.ts
autonomous: true
requirements: [REG-02]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tocar +/- de reps na tela bloqueada aplica o MESMO stepReps() da store (Plano 17-02), com clamp em 0 e passo fixo de 1"
    - "O card do Lock Screen mostra reps E carga em edição lado a lado, com a mesma marca de herdado para os dois campos"
    - "Um toque na tela bloqueada durante um cold-launch (app force-quit) fica na fila até o app reabrir, e a reconciliação aplica adjustReps/adjustLoad do mesmo jeito que já aplica completeSet/skipRest/adjustRest desde a Fase 16"
  artifacts:
    - targets/session-widget/AdjustRepsIntent.swift
    - modules/live-activity/ios/AdjustRepsIntent.swift
  key_links:
    - "AdjustRepsIntent.perform() -> IntentActionQueue.enqueue(kind: .adjustReps, deltaValue:) -> sendEvent -> liveActivityIntentBridge.ts case 'adjustReps' -> stepReps() -> ack"
    - "reconcileLiveActivityIntents() (cold-launch) -> case 'adjustReps'/'adjustLoad' -> mesmo alvo (findActiveSet ?? findNextPendingSet) -> stepReps()/stepLoad() -> ack"
  prohibitions:
    - "Nenhum LiveActivityIntent novo escreve em ActivityKit direto — mesmo prohibition do Plano 17-01, reforçado para AdjustRepsIntent"
    - "Nenhuma entrada de adjustReps/adjustLoad na fila do App Group fica permanentemente sem tratamento no cold-launch — reconcileLiveActivityIntents() precisa cobrir os dois kinds novos, senão toques dados com o app fechado ficam presos para sempre"
---

<objective>
Espelha o Plano 17-01 para o campo REPS (AdjustRepsIntent, currentReps/isRepsInherited no
ContentState, case adjustReps na ponte) e fecha a lacuna de reconciliação a frio: até esta
task, um toque em +/- de reps ou carga com o app force-quit ficaria enfileirado sem NENHUM
case no switch de `reconcileLiveActivityIntents()` — a entrada nunca seria confirmada nem
descartada, sobrevivendo indefinidamente na fila (até o cap de 20 cortá-la silenciosamente).
Este plano fecha os dois lados do mesmo contrato (hot path + cold path) na mesma passada.

Depende de 17-01 (o padrão Swift Queue+Intent+ContentState+widget já estabelecido, mais o
campo genérico `deltaValue` e o caso `.adjustReps` já declarados no enum) e de 17-02
(`stepReps()` da store, que a ponte e a reconciliação chamam).

Purpose: completar REG-02 para os dois campos que D-09 exige ("dois pares de −/+") e
fechar o gap de cold-launch antes de PRED-01 (Plano 17-05) tocar os mesmos arquivos de
novo.
Output: `AdjustRepsIntent` funcional nos dois targets, `currentReps`/`isRepsInherited` no
ContentState, e `reconcileLiveActivityIntents()` tratando os quatro `kind` possíveis
(completeSet/skipRest/adjustRest/adjustReps/adjustLoad — cinco, precisamente).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-tela-bloqueada-registrar-e-antecipar/17-CONTEXT.md
@.planning/phases/17-tela-bloqueada-registrar-e-antecipar/17-RESEARCH.md
@.planning/phases/17-tela-bloqueada-registrar-e-antecipar/17-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdjustRepsIntent (stub + impl) + fila</name>
  <files>modules/live-activity/ios/IntentActionQueue.swift, targets/session-widget/AdjustRepsIntent.swift, modules/live-activity/ios/AdjustRepsIntent.swift, scripts/verify-native-skeleton.sh</files>
  <read_first>
    modules/live-activity/ios/IntentActionQueue.swift (já tem .adjustReps no enum e deltaValue na struct, adicionados pelo Plano 17-01 — confirme o estado atual antes de editar)
    modules/live-activity/ios/AdjustLoadIntent.swift (molde direto — criado pelo Plano 17-01, mesma estrutura a replicar trocando kg por reps)
    targets/session-widget/AdjustLoadIntent.swift (molde do stub)
  </read_first>
  <action>
    `IntentActionQueue.swift` NÃO precisa de mudança nesta task — `.adjustReps` e
    `deltaValue` já existem desde o Plano 17-01. Confirme lendo o arquivo antes de seguir
    (se algo estiver ausente, o Plano 17-01 não foi aplicado corretamente — pare e reporte).

    Crie `targets/session-widget/AdjustRepsIntent.swift`: stub `struct AdjustRepsIntent:
    LiveActivityIntent` com `@Parameter(title: "Delta em repetições") var deltaReps: Int`,
    `init()`, `init(deltaReps:)`, `perform()` retornando `.result()` — mesmo molde de
    `AdjustLoadIntent.swift` (Plano 17-01), trocando `Double` por `Int` e o nome do
    parâmetro.

    Crie `modules/live-activity/ios/AdjustRepsIntent.swift`: implementação real,
    `@available(iOS 16.2, *)`, mesmo `@Parameter deltaReps: Int`, `perform()` resolve
    `sessionLogId`, gera `actionId`, chama `IntentActionQueue.enqueue(QueuedIntentAction(kind:
    .adjustReps, deltaSeconds: nil, deltaValue: Double(deltaReps), sessionLogId:, queuedAt:,
    id: actionId))` e `sendEvent("onIntentAction", ["kind": "adjustReps", "deltaReps":
    deltaReps, "id": actionId])`.

    Em `scripts/verify-native-skeleton.sh` linha ~151: adicione `AdjustRepsIntent` à lista
    do loop `for nome_intent in ...` (agora com AdjustRestIntent, AdjustLoadIntent E
    AdjustRepsIntent).
  </action>
  <acceptance_criteria>
    - `grep -q "struct AdjustRepsIntent" targets/session-widget/AdjustRepsIntent.swift` e o mesmo em `modules/live-activity/ios/AdjustRepsIntent.swift`
    - `grep -c "Double(deltaReps)" modules/live-activity/ios/AdjustRepsIntent.swift` retorna 1 (conversão explícita Int -> Double para o campo genérico)
    - `grep -c "AdjustRepsIntent" scripts/verify-native-skeleton.sh` inclui a nova entrada na lista de nomes verificados
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify-native-skeleton.sh</automated>
  </verify>
  <done>AdjustRepsIntent existe nos dois targets, verify-native-skeleton.sh passa cobrindo os 4 intents (CompleteSet/SkipRest/AdjustRest/AdjustLoad/AdjustReps).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContentState de reps em edição + ponte JS (adjustReps)</name>
  <files>targets/session-widget/SessionActivityAttributes.swift, modules/live-activity/ios/SessionActivityAttributes.swift, src/engine/liveActivityContentState.ts, src/native/liveActivitySync.ts, src/native/liveActivityIntentBridge.ts, modules/live-activity/index.ts, __tests__/liveActivityContentState.test.ts, __tests__/liveActivityIntentBridge.test.ts</files>
  <read_first>
    src/engine/liveActivityContentState.ts (estado ATUAL pós Plano 17-01 — já tem currentLoadKg/isLoadInherited/loadIncrementKg; leia antes de estender)
    src/native/liveActivityIntentBridge.ts (estado ATUAL pós Plano 17-01 — já tem case 'adjustLoad')
    modules/live-activity/index.ts (estado ATUAL — já tem 'adjustLoad' na união)
  </read_first>
  <behavior>
    - Draft com `active` set, `actualReps: null`, `targetRepsMin: 8` -> `currentReps: 8, isRepsInherited: true`.
    - Mesmo set com `actualReps: 12` (digitado/ajustado) -> `currentReps: 12, isRepsInherited: false`.
    - Fase não-measuring -> `currentReps: null, isRepsInherited: false`.
    - `liveActivityIntentBridge`: evento `{kind:'adjustReps', deltaReps: 1, id:'y'}` chama
      `stepReps(exercise.exerciseId, set.setOrder, 1)` no alvo resolvido, depois ack.
  </behavior>
  <action>
    Em ambos `SessionActivityAttributes.swift` (as DUAS cópias): adicione `var currentReps:
    Int?` e `var isRepsInherited: Bool` ao `ContentState`, na mesma posição relativa nos
    dois arquivos (logo após `currentLoadKg`/`isLoadInherited`/`loadIncrementKg` do Plano
    17-01, antes de `isBodyweight`).

    Em `src/engine/liveActivityContentState.ts`: adicione `currentReps: number | null` e
    `isRepsInherited: boolean` ao tipo `LiveActivityContentState`. No `contentStateFor(...)`
    (já recebe `draft` desde o Plano 17-01), calcule-os só em `phase === 'measuring'`:
    `currentReps = set.actualReps ?? suggestReps({actualReps: null, targetRepsMin:
    set.targetRepsMin > 0 ? set.targetRepsMin : null, lastReps:
    draft.lastRepsByExercise[exerciseIdentity(exercise)]})`; `isRepsInherited =
    set.actualReps == null`. Import `suggestReps` de `./sessionModel` (adicionado no Plano
    17-02).

    Em `src/native/liveActivitySync.ts`, `buildFinishedContentState`: adicione
    `currentReps: null, isRepsInherited: false` ao literal (mesmo padrão do Plano 17-01
    para os campos de carga).

    Em `modules/live-activity/index.ts`: estenda `LiveActivityIntentActionEvent` com `| {
    id: string; kind: 'adjustReps'; deltaReps: number }` e `QueuedLiveActivityIntent.kind`
    com `'adjustReps'`.

    Em `src/native/liveActivityIntentBridge.ts`: adicione `case 'adjustReps':` ao switch,
    mesmo formato de `adjustLoad` — resolve alvo via `findActiveSet(draft) ??
    findNextPendingSet(draft)`, chama `useActiveSessionStore.getState().stepReps(
    alvo.exercise.exerciseId, alvo.set.setOrder, event.deltaReps > 0 ? 1 : -1)`, depois
    `ackQueuedLiveActivityIntent(event.id)`.
  </action>
  <acceptance_criteria>
    - `diff -q targets/session-widget/SessionActivityAttributes.swift modules/live-activity/ios/SessionActivityAttributes.swift` retorna vazio
    - Testes novos cobrindo os 4 cenários do `<behavior>` passam em `liveActivityContentState.test.ts` e `liveActivityIntentBridge.test.ts`
    - `npx tsc --noEmit` sem erros
  </acceptance_criteria>
  <verify>
    <automated>npx jest __tests__/liveActivityContentState.test.ts __tests__/liveActivityIntentBridge.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>ContentState carrega currentReps/isRepsInherited computados em measuring, as duas cópias Swift continuam idênticas, bridge aplica adjustReps via stepReps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stepper de reps no widget + reconciliação cold-launch (adjustReps/adjustLoad)</name>
  <files>targets/session-widget/WidgetLiveActivity.swift, src/store/activeSessionStore.ts, __tests__/liveActivityIntentQueue.test.ts</files>
  <read_first>
    targets/session-widget/WidgetLiveActivity.swift (estado ATUAL pós Plano 17-01 — case .measuring já tem o stepper de carga; leia antes de adicionar o de reps)
    src/store/activeSessionStore.ts linhas 1694-1774 (reconcileLiveActivityIntents — switch atual com completeSet/skipRest/adjustRest)
    __tests__/liveActivityIntentQueue.test.ts linhas 220-410 (describe('reconcileLiveActivityIntents') — padrão de mock e asserção, especialmente o teste de adjustRest em torno da linha 396)
  </read_first>
  <behavior>
    - Entrada `{kind: 'adjustReps', deltaValue: 1, sessionLogId: 'log-1', ...}` na fila
      (draft ativo, sessão correspondente) -> `stepReps` chamado com direção +1 no alvo
      correto (findActiveSet ?? findNextPendingSet), depois ack.
    - Entrada `{kind: 'adjustLoad', deltaValue: -2.5, ...}` -> `stepLoad` chamado com
      direção -1, depois ack.
    - `sessionLogId` divergente do draft atual -> descarte definitivo (ack sem aplicar),
      mesmo comportamento já testado para os outros kinds.
    - Sem draft ativo (`status !== 'active'`) -> nenhuma chamada de stepReps/stepLoad, sem
      ack (a fila inteira é ignorada até haver draft, mesma guarda de hidratação).
  </behavior>
  <action>
    No case `.measuring` de `lockScreenBody` (`WidgetLiveActivity.swift`, já modificado
    pelo Plano 17-01 com o stepper de carga): adicione, ACIMA ou ao lado do stepper de
    carga (dentro do mesmo VStack/HStack, decisão de layout do executor — critério: os
    DOIS pares de −/+ cabem sem cortar conteúdo, tamanho de alvo de toque é o que o UAT
    físico do Plano 17-07 vai avaliar), um segundo par: `Button(intent:
    AdjustRepsIntent(deltaReps: -1))` com "−", o texto de `state.currentReps.map(String.init)
    ?? "—"` com `.opacity(state.isRepsInherited ? 0.6 : 1.0)`, e `Button(intent:
    AdjustRepsIntent(deltaReps: 1))` com "+". Reps aparecem para TODO exercício
    `carga_reps` (inclusive bodyweight — só a carga é omitida para bodyweight, reps
    sempre existem).

    Em `src/store/activeSessionStore.ts`, dentro de `reconcileLiveActivityIntents` (switch
    em `entry.kind`, linhas ~1747-1772): adicione dois casos novos seguindo o MESMO padrão
    do case `adjustRest` (linha 1767) — `case 'adjustReps': { const alvo =
    findActiveSet(draft) ?? findNextPendingSet(draft); if (alvo && entry.deltaValue != null)
    get().stepReps(alvo.exercise.exerciseId, alvo.set.setOrder, entry.deltaValue > 0 ? 1 :
    -1); void ackQueuedLiveActivityIntent(entry.id); break; }` e o equivalente `case
    'adjustLoad':` chamando `get().stepLoad(...)` com a mesma resolução de alvo e sinal.
    Ambos sempre confirmam (ack incondicional, mesmo padrão de `adjustRest` — diferente de
    `completeSet`, que só confirma quando a validação realmente aplicou; ajustar reps/carga
    não tem uma "reprovação" de canCompleteSet a respeitar, é sempre aplicável).
  </action>
  <acceptance_criteria>
    - Todos os casos do `<behavior>` existem como `it()` em `liveActivityIntentQueue.test.ts` (describe `reconcileLiveActivityIntents`) e passam
    - `grep -c "case 'adjustReps'" src/store/activeSessionStore.ts` retorna 1
    - `grep -c "case 'adjustLoad'" src/store/activeSessionStore.ts` retorna 1
    - `grep -c "AdjustRepsIntent(deltaReps:" targets/session-widget/WidgetLiveActivity.swift` retorna 2
  </acceptance_criteria>
  <verify>
    <automated>npx jest __tests__/liveActivityIntentQueue.test.ts && bash scripts/verify-native-skeleton.sh</automated>
  </verify>
  <done>O widget mostra os dois pares de −/+ (reps e carga) no case measuring, e reconcileLiveActivityIntents() trata os cinco kinds possíveis (completeSet/skipRest/adjustRest/adjustReps/adjustLoad) sem deixar nenhuma entrada presa na fila indefinidamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Mesmas do Plano 17-01 (Lock Screen -> App Group queue -> processo do app).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-07 | Tampering | `AdjustRepsIntent.perform()` / `stepReps()` | medium | mitigate | Delta sempre ±1 fixo por toque; `stepReps()` clampa em `Math.max(0, ...)` |
| T-17-08 | Denial of Service | `reconcileLiveActivityIntents` processando fila acumulada | low | accept | Cap de 20 entradas já herdado da Fase 16 continua valendo para os 5 kinds; ack incondicional nos dois casos novos evita reprocessamento infinito da mesma entrada |
| T-17-09 | Repudiation | Entrada de fila sem `sessionLogId` correspondente ao draft atual | low | mitigate | Regra de CAS herdada (16-12): `sessionLogId` divergente descarta por definição; `sessionLogId` nulo usa `nasceuNestaSessao` como prova de origem — nenhum toque é aplicado contra a sessão errada |

</threat_model>

<verification>
- `npx jest __tests__/liveActivityContentState.test.ts __tests__/liveActivityIntentBridge.test.ts __tests__/liveActivityIntentQueue.test.ts` verde
- `bash scripts/verify-native-skeleton.sh` verde
- `npx tsc --noEmit` sem erros
</verification>

<success_criteria>
REG-02 está completo no código para os dois campos (reps e carga): hot path (app em
foreground) e cold path (reconciliação pós force-quit) tratam os cinco kinds de intent sem
lacuna. A prova física fica para o Plano 17-07.
</success_criteria>

<output>
Create `.planning/phases/17-tela-bloqueada-registrar-e-antecipar/17-03-SUMMARY.md` when done
</output>
