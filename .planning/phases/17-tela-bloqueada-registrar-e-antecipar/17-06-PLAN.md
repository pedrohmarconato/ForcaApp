---
phase: 17-tela-bloqueada-registrar-e-antecipar
plan: 06
type: execute
wave: 4
depends_on: ["17-04", "17-05"]
files_modified: []
autonomous: true
requirements: [REG-01, REG-02, PRED-01]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A suite Jest completa passa apos a integracao de todos os planos 17-01..17-05"
    - "tsc --noEmit nao acusa erro em nenhum arquivo tocado pela fase"
    - "scripts/verify-native-skeleton.sh passa, incluindo a checagem de diff-parity entre as duas copias de SessionActivityAttributes.swift"
    - "expo prebuild --clean nao apaga nenhum target/modulo nativo desta fase (mesma garantia que o script ja prova para a fundacao herdada da Fase 14)"
  artifacts: []
  key_links:
    - "Este plano nao cria artefato novo — e o gate de integracao antes do UAT fisico do Plano 17-07"
  prohibitions:
    - "Nenhuma correcao de bug aqui vira feature nova ou refatoracao nao pedida — so o necessario para os gates ficarem verdes"
---

<objective>
Gate de integracao pos-codigo, antes de pedir o aparelho fisico do dono. Roda a suite
completa (nao so os arquivos tocados por plano isolado) para pegar qualquer interacao
entre os cinco planos anteriores que nenhum teste de plano individual cobriria — por
exemplo, um campo novo do ContentState que quebra um teste de outro dominio que constroi
um LiveActivityContentState literal manualmente.

Purpose: nenhuma sessao fisica com o dono comeca sobre uma base vermelha. "Compilou" nao e
o alvo aqui — o alvo e TODOS os automatizados disponiveis, verdes, ao mesmo tempo.
Output: confirmacao (ou correcao) de que o codigo das cinco waves anteriores integra sem
regressao.
</objective>

## Artifacts this phase produces (deste plano)

Nenhum artefato de código novo — plano de verificação/integração puro. Único produto é o
binário Release compilado e assinado (`npm run resign`) e o `17-06-SUMMARY.md` com o
resultado de cada gate automatizado.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-tela-bloqueada-registrar-e-antecipar/17-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite completa + tsc + skeleton nativo</name>
  <files></files>
  <read_first>
    .planning/phases/17-tela-bloqueada-registrar-e-antecipar/17-VALIDATION.md (Sampling Rate e Wave 0 Requirements — confirme que nenhum item da lista ficou sem caso novo antes de rodar a suite)
  </read_first>
  <action>
    Rode `npm test` (suite Jest completa) e `npx tsc --noEmit`. Se qualquer teste falhar
    ou o compilador acusar erro, investigue a CAUSA RAIZ antes de corrigir — priorize
    entender se a falha e uma interacao entre planos (ex.: um teste de outro dominio que
    constroi um LiveActivityContentState sem os campos novos, e agora falha por
    incompletude de tipo) antes de mexer no teste. Corrija no arquivo de PRODUCAO quando o
    teste estiver certo e a implementacao errada; corrija o teste SO quando ele estava
    testando um contrato que esta fase mudou deliberadamente (documente qual decisao — D-XX
    — justifica a mudanca do teste). Rode `bash scripts/verify-native-skeleton.sh` e
    confirme que as 8 checagens (a-h, incluindo o diff-parity adicionado no Plano 17-01)
    passam nas duas rodadas.
  </action>
  <acceptance_criteria>
    - `npm test` termina com 0 falhas
    - `npx tsc --noEmit` termina sem erros
    - `bash scripts/verify-native-skeleton.sh` termina com exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm test && npx tsc --noEmit && bash scripts/verify-native-skeleton.sh</automated>
  </verify>
  <done>Suite completa, tsc e skeleton nativo verdes simultaneamente, sem nenhuma falha pendente de correcao.</done>
</task>

<task type="auto">
  <name>Task 2: Build de producao (resign) — confirma que o binario embarca as mudancas</name>
  <files></files>
  <read_first>
    scripts/resign.sh (confirme que continua em -configuration Release, sem regressao para Debug)
  </read_first>
  <precondition>Xcode 26.6 licenciado nesta maquina permite build e assinatura sem o aparelho conectado; a INSTALACAO no iPhone fisico so acontece no Plano 17-07, com o dono presente.</precondition>
  <action>
    Rode `npm run resign` (build Release completo, sem instalar — o script ja documenta
    que a instalacao/pareamento fica para a sessao com o dono). Se o build falhar, use o
    log de erro do Xcode para localizar a causa (tipicamente um dos arquivos Swift novos
    desta fase com erro de sintaxe/tipo nao detectavel por `verify-native-skeleton.sh`,
    que so confere presenca e estrutura, nao compilacao completa). Corrija e rode de novo
    ate o build terminar com sucesso. NAO prossiga para o Plano 17-07 com um build
    quebrado — "compilou" e o MINIMO aqui, nao o criterio de conclusao da fase inteira
    (esse continua sendo o UAT fisico do proximo plano).
  </action>
  <acceptance_criteria>
    - `npm run resign` termina com sucesso (build Release assinado, sem erro de compilacao Swift)
  </acceptance_criteria>
  <verify>
    <automated>npm run resign</automated>
  </verify>
  <done>O binario Release compila com todas as mudancas desta fase (Swift + TS), pronto para instalacao na sessao fisica do Plano 17-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhuma nova — este plano so executa verificacao sobre codigo ja revisado nos planos
anteriores.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-14 | Tampering | Build Release divergindo de Debug (nao embute main.jsbundle) | medium | mitigate | scripts/verify-native-skeleton.sh checagem (f) ja prova que resign.sh continua em -configuration Release antes deste plano rodar |

</threat_model>

<verification>
- npm test, npx tsc --noEmit, bash scripts/verify-native-skeleton.sh e npm run resign todos verdes
</verification>

<success_criteria>
Toda verificacao automatizada disponivel neste repositorio passa simultaneamente, e o
binario Release compila com as mudancas das cinco waves anteriores — o codigo esta pronto
para a sessao fisica do Plano 17-07.
</success_criteria>

<output>
Create `.planning/phases/17-tela-bloqueada-registrar-e-antecipar/17-06-SUMMARY.md` when done
</output>
