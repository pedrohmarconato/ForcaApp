---
phase: 12-p-gina-de-instala-o-guiada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/installDetection.ts
  - src/screens/InstallScreen.tsx
  - src/navigation/linking.ts
  - src/navigation/linkingConfig.ts
  - src/navigation/AuthNavigator.tsx
  - src/navigation/OnboardingNavigator.tsx
  - src/navigation/MainNavigator.tsx
  - __tests__/installDetection.test.ts
  - __tests__/InstallScreen.test.tsx
  - __tests__/navigationLinking.test.ts
autonomous: true
requirements: [INST-02]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Um aluno deslogado que acessa /instalar no Safari do iPhone (UA com iPhone/iPad, sem CriOS/FxiOS/EdgiOS/OPiOS) vê os 3 passos numerados (Compartilhar -> Adicionar à Tela de Início -> Adicionar) com a copy literal de 12-UI-SPEC.md — verificado renderizando InstallScreen com installDetection mockado para o Estado 1 (D-locked: Conteúdo do passo a passo, 12-CONTEXT.md)."
    - "Um aluno em iOS usando Chrome/Firefox (UA com CriOS ou FxiOS) vê a instrução 'Abra este link no Safari' via Notice tone=info, nunca o passo a passo de instalação (Estado 2)."
    - "Um visitante em desktop/Android, OU qualquer UA não reconhecido, vê 'Abra este link no seu iPhone' com a URL atual exibida — nunca tela em branco (Estado 3, degradação segura por construção, Pitfall 4 de 12-RESEARCH.md)."
    - "Um usuário já instalado (matchMedia display-mode:standalone OU navigator.standalone) vê 'Você já instalou o ForçaApp' com o CTA 'Abrir o ForçaApp', sem repetir o passo a passo (Estado 4, critério de sucesso 2 do ROADMAP Fase 12)."
    - "linkingInterceptor.config.screens.Instalar === 'instalar' e AuthNavigator registra <Stack.Screen name=\"Instalar\"> — um aluno deslogado que abre a URL /instalar chega na tela, nunca cai silenciosamente em Login (Pattern 1 de 12-RESEARCH.md, o mecanismo central desta fase; hoje linkingInterceptor.config.screens é {} vazio)."
    - "installDetection.ts nunca lança exceção nem retorna undefined para um UA/ambiente não reconhecido — todo branch degrada para false por construção (Pitfall 4), coberto com UAs sintéticos/vazios/desconhecidos no teste unitário."
    - "A página não tem nenhum efeito no app nativo: Platform.OS !== 'web' faz InstallScreen retornar null SEM chamar isIOS/isSafari/isStandalone (mesmo padrão de UpdateBanner.tsx:80)."
    - { statement: "Um aluno deslogado num iPhone real não vê um flash perceptível da tela 'Carregando...' antes do passo a passo de /instalar aparecer — o branch loadingSession de RootNavigator.js não tem NavigationContainer (Pitfall 1 de 12-RESEARCH.md); supabase-js getSession() lê de storage local primeiro e normalmente resolve rápido, mas isso só é confirmável em rede real, não em jsdom.", verification: backstop }
  artifacts:
    - src/utils/installDetection.ts
    - src/screens/InstallScreen.tsx
    - __tests__/installDetection.test.ts
    - __tests__/InstallScreen.test.tsx
  key_links:
    - "linkingInterceptor.config.screens.Instalar='instalar' (linking.ts) <-> AuthNavigator's <Stack.Screen name=\"Instalar\"> e OnboardingNavigator's <Stack.Screen name=\"Instalar\"> — os DOIS navigators recebem o MESMO objeto linkingInterceptor; um nome de rota desalinhado entre eles quebra a resolução do path para quem não tem sessão Main."
    - "LINKING_CONFIG.screens.Instalar='instalar' (linkingConfig.ts, top-level sibling de Home/Training/Progress/Profile) <-> MainNavigator's <BottomTab.Screen name=\"Instalar\" options={{ tabBarButton: () => null }}>."
    - "InstallScreen <-> installDetection.ts (isIOS/isSafari/isStandalone) — a tela só orquestra o render a partir do retorno síncrono do utilitário, sem lógica de detecção duplicada na tela."
    - "InstallScreen's CTA (Estado 4) <-> prop homeRoute recebida via children={() => <InstallScreen homeRoute=\"Login|Questionnaire|Home\" />} em cada um dos 3 pontos de registro — nenhuma navegação hardcoded dentro do componente."
  prohibitions:
    - statement: "A rota pública /instalar nunca renderiza dado de usuário autenticado (perfil, sessão, nome, e-mail) — InstallScreen não recebe nem lê nenhuma prop/hook de autenticação, mesmo estando registrada na árvore Main."
      status: resolved
      verification: test
    - statement: "Nenhuma coleta ou telemetria do userAgent/estado de detecção é enviada a qualquer endpoint — installDetection.ts e InstallScreen.tsx não fazem nenhuma chamada de rede (fetch/axios/analytics)."
      status: resolved
      verification: test
    - statement: "Nenhum asset de screenshot ou imagem pesada nova é adicionado à página — só os ícones Feather já usados no design system (decisão do dono, 12-CONTEXT.md: 'sem screenshots pesados')."
      status: resolved
      verification: judgment
    - statement: "Nenhum botão ou link novo de entrada para /instalar é adicionado em qualquer tela existente do app (Home, Perfil etc.) — o único acesso é a URL direta compartilhada pelo dono (decisão do dono, 12-CONTEXT.md: 'Porta de entrada: só a rota direta... Sem botão novo no app')."
      status: resolved
      verification: test
---

<objective>
Entregar INST-02 por completo: uma rota pública `/instalar`, alcançável em
QUALQUER estado de sessão (deslogado, onboarding incompleto, logado), que
detecta iOS/Safari/standalone de forma síncrona e pura e mostra os 4 estados
de copy literal de `12-UI-SPEC.md` — sem nunca vazar dado de usuário, sem
nenhum efeito no app nativo, e sem nunca cair numa tela em branco mesmo com
UA não reconhecido.

Nota sobre a sonda de edge coverage: INST-02 veio "unclassified"/"unresolved"
da sonda determinística de edges (não há SPEC.md nesta fase — modo spec-less
fallback). Os 4 edges reais fornecidos pelo orquestrador (branch de loading
sem NavigationContainer; detecção inconclusiva; CTA multi-árvore; regressão
futura de tabBarButton) foram autorados manualmente acima em
`must_haves.truths`: os que são verificáveis localmente (detecção
inconclusiva -> Estado 3, CTA via prop homeRoute, guarda estrutural do
tabBarButton no Plano/Task 2) viraram truths de string; o único
verificável apenas em dispositivo real (flash de loading antes do
NavigationContainer montar) virou o marcador `{ statement, verification:
backstop }`. As proibições do domínio (12-CONTEXT.md/12-RESEARCH.md: zero
dado de auth, zero telemetria, zero asset pesado, zero botão novo de
entrada) foram autoradas como `must_haves.prohibitions` descriptor-less,
flagged-unverified — cada uma tem uma checagem real nas tasks abaixo, mas o
descritor de checagem automática não é fiado aqui (fail-closed por design).

Purpose: qualquer aluno leigo, com ou sem conta, consegue instalar o
ForçaApp sozinho a partir do link que o dono compartilha, sem instrução
verbal.
Output: `src/utils/installDetection.ts`, `src/screens/InstallScreen.tsx`,
registro da rota `Instalar` em `linking.ts` + `AuthNavigator.tsx` (Task 1) e
em `linkingConfig.ts` + `OnboardingNavigator.tsx` + `MainNavigator.tsx`
(Task 2), com testes automatizados cobrindo os 4 estados e a resolução de
rota nas 3 árvores.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-p-gina-de-instala-o-guiada/12-CONTEXT.md
@.planning/phases/12-p-gina-de-instala-o-guiada/12-RESEARCH.md
@.planning/phases/12-p-gina-de-instala-o-guiada/12-UI-SPEC.md
@src/navigation/RootNavigator.js
@src/navigation/linking.ts
@src/navigation/linkingConfig.ts
@src/navigation/AuthNavigator.tsx
@src/navigation/OnboardingNavigator.tsx
@src/navigation/MainNavigator.tsx
@src/components/ui/AuthLayout.tsx
@src/components/ui/Button.tsx
@src/components/ui/Feedback.tsx
@src/components/ui/Logo.tsx
@src/components/UpdateBanner.tsx
@src/theme/theme.ts
@__tests__/UpdateBanner.test.tsx
@__tests__/navigationLinking.test.ts
@__tests__/profileScreen.test.tsx
@__tests__/serviceWorkerConfig.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: /instalar reachable para o aluno deslogado — installDetection.ts + linkingInterceptor + AuthNavigator + os 4 estados de InstallScreen</name>
  <reversibility rating="reversible">Utilitário puro novo + tela nova + duas linhas de registro em linking.ts/AuthNavigator.tsx, tudo aditivo — um `git revert` restaura o estado anterior sem migração de dado nem efeito em nenhuma rota existente.</reversibility>
  <files>src/utils/installDetection.ts, src/screens/InstallScreen.tsx, src/navigation/linking.ts, src/navigation/AuthNavigator.tsx, __tests__/installDetection.test.ts, __tests__/InstallScreen.test.tsx</files>
  <read_first>
    12-RESEARCH.md (Pattern 1 — "the core mechanism"; Pattern 3 — web-only guard; Pitfall 1, 3, 4; tabela Don't Hand-Roll; seção Code Examples com installDetection.ts e o mock de useNavigation);
    12-UI-SPEC.md (Design System, StepRow anatomy, Copywriting Contract Estados 1-4, tabelas Color/Typography/Spacing);
    12-CONTEXT.md (decisões travadas);
    src/navigation/linking.ts (linkingInterceptor.config.screens, hoje `{}` vazio);
    src/navigation/AuthNavigator.tsx (Stack SEM generic — a única exceção do Pitfall 3, não precisa de tipo de ParamList);
    src/components/ui/AuthLayout.tsx (padrão de card/spacing a REPLICAR inline — NÃO importar, hardcoda chrome de login);
    src/components/ui/Button.tsx, src/components/ui/Feedback.tsx (Notice, Chip), src/components/ui/Logo.tsx (ForcaLockup);
    src/components/UpdateBanner.tsx (padrão `Platform.OS !== 'web' -> return null`, linha 80);
    src/theme/theme.ts (tokens exatos: spacing, borderRadius, typography, colors);
    __tests__/UpdateBanner.test.tsx (padrão `@jest-environment jsdom` + `Object.defineProperty(Platform, 'OS', ...)`, usar se o teste precisar de `window.location`/`window.matchMedia` reais);
    __tests__/profileScreen.test.tsx (padrão de mock `jest.mock('@react-navigation/native', () => ({ useNavigation: () => ({ navigate: mockNavigate }) }))`).
  </read_first>
  <behavior>
    - isIOSDevice(ua, maxTouchPoints): UA "iPhone" -> true; UA "iPad" -> true; UA "Macintosh"+maxTouchPoints=5 -> true (unmask iPadOS 13+); UA "Macintosh"+maxTouchPoints=0 -> false (Mac real); UA "Mozilla/5.0 (Windows NT 10.0)" -> false; UA "" -> false.
    - isSafariBrowser(ua): UA com "Safari" e sem outro token -> true; UA com "CriOS" -> false; UA com "FxiOS" -> false; UA com "EdgiOS" -> false; UA com "OPiOS" ou "OPT/" -> false; UA sem "Safari" e sem token nenhum -> false (fallback seguro).
    - isIOS()/isSafari(): com navigator.userAgent mockado (jsdom + Object.defineProperty, mesmo padrão de UpdateBanner.test.tsx), delegam corretamente para isIOSDevice/isSafariBrowser.
    - isStandalone(): navigator.standalone=true -> true; window.matchMedia('(display-mode: standalone)').matches=true -> true; ambos false -> false; window.matchMedia ausente (não é function) -> não lança, cai só para navigator.standalone.
    - InstallScreen (Platform.OS mockado para 'web', installDetection mockado via jest.mock): ios=true+safari=true -> renderiza os 3 StepRow com a copy exata do Estado 1; ios=true+safari=false -> renderiza a copy do Estado 2 (Notice); ios=false -> renderiza a copy do Estado 3 (headline + URL); standalone=true -> renderiza a copy do Estado 4 (chip INSTALADO + CTA "Abrir o ForçaApp"); Platform.OS !== 'web' -> retorna null e installDetection NÃO é chamado (spy nas 3 funções).
  </behavior>
  <action>
    Criar `src/utils/installDetection.ts` — puro, síncrono, sem rede. Exportar
    dois helpers puros testáveis sem nenhum DOM: `isIOSDevice(ua: string,
    maxTouchPoints: number): boolean` (true quando `ua` casa
    `/iPad|iPhone|iPod/`, OU quando `ua` casa `/Macintosh/` E
    `maxTouchPoints > 1` — unmask do iPadOS 13+ que finge ser Mac);
    `isSafariBrowser(ua: string): boolean` (true quando `ua` casa `/Safari/`
    E NÃO casa `/CriOS|FxiOS|EdgiOS|OPiOS|OPT\//`). Exportar 3 wrappers
    públicos: `isIOS(): boolean` (guarda `typeof navigator === 'undefined'`
    -> false; senão chama `isIOSDevice(navigator.userAgent,
    navigator.maxTouchPoints ?? 0)`); `isSafari(): boolean` (mesma guarda,
    chama `isSafariBrowser(navigator.userAgent)`); `isStandalone(): boolean`
    (guarda `typeof window === 'undefined'` -> false; senão true quando
    `(navigator as unknown as {standalone?:boolean}).standalone === true` OU
    `typeof window.matchMedia === 'function' &&
    window.matchMedia('(display-mode: standalone)').matches` — nunca lança,
    cada branch degrada para `false` por construção, nunca `undefined`).

    Em `src/navigation/linking.ts`: alterar `linkingInterceptor.config` de
    `{ screens: {} }` para `{ screens: { Instalar: 'instalar' } }` — este
    ÚNICO objeto é compartilhado por Auth E Onboarding (Pattern 1 de
    12-RESEARCH.md); não criar um segundo objeto de linking.

    Em `src/navigation/AuthNavigator.tsx`: importar `InstallScreen` de
    `../screens/InstallScreen`; adicionar
    `<Stack.Screen name="Instalar">{() => <InstallScreen homeRoute="Login" />}</Stack.Screen>`
    alongside Login/SignUp/ForgotPassword (o `Stack` deste arquivo não tem
    generic — nenhum tipo de ParamList a atualizar aqui, ao contrário de
    Onboarding/Main no Task 2).

    Criar `src/screens/InstallScreen.tsx`. Guarda de topo: `if (Platform.OS
    !== 'web') return null;` ANTES de chamar qualquer função de
    installDetection (zero efeito no nativo, mesmo formato de
    `UpdateBanner.tsx:80`). Props: `{ homeRoute: string }` (nome da tela que
    o CTA do Estado 4 navega — cada ponto de registro passa sua própria
    rota-lar). Computar de forma síncrona (sem useEffect):
    `const standalone = isStandalone(); const ios = isIOS(); const safari =
    isSafari();`. Derivar o estado, `standalone` checado PRIMEIRO: Estado 4
    quando `standalone`; senão Estado 1 quando `ios && safari`; senão Estado
    2 quando `ios && !safari`; senão Estado 3 (desktop/Android E qualquer UA
    não reconhecido caem aqui por construção, Pitfall 4 — nunca um
    try/catch).

    Casca de página (comum aos 4 estados, réplica inline do padrão de
    `AuthLayout.tsx` — NÃO importar `AuthLayout`): `SafeAreaView`
    (`theme.colors.surface.canvas`) > `ScrollView`
    (`contentContainerStyle` padding `theme.spacing.xl`, centralizado) >
    card `View` (`maxWidth: 420`, `alignSelf: 'center'`, `borderRadius:
    theme.borderRadius.xxl`, `borderWidth: 1`, `borderColor:
    theme.colors.border.subtle`, `backgroundColor: theme.colors.surface.card`,
    `padding: theme.spacing.xxl`) contendo `ForcaLockup` de
    `../components/ui/Logo` no topo (`marginBottom: theme.spacing.xxxl`).

    Estado 1 (`ios && safari`): eyebrow `Text` "INSTALAÇÃO" (`fontSizes.micro`,
    `fontWeights.semiBold`, `letterSpacing.wider`, `textTransform:
    'uppercase'`, `color: theme.colors.text.quiet` — mesmo papel/forma do
    `tagline` de AuthLayout); Display headline "Instale o ForçaApp no seu
    iPhone" (`fontSizes.display`, `fontWeights.semiBold`,
    `lineHeights.tight`); apoio "Leva menos de um minuto. Siga os 3 passos
    abaixo." (`fontSizes.base`, `color: theme.colors.text.secondary`); 3
    instâncias do `StepRow` local (badge circular `width/height:
    theme.spacing.huge`, `borderRadius: theme.borderRadius.pill`,
    `backgroundColor: theme.colors.veil.faint`, `borderWidth: 1`,
    `borderColor: theme.colors.border.focus`, número `fontSizes.xl`/
    `fontWeights.bold`/`color: theme.colors.text.accent`; `Feather` 18px
    `color: theme.colors.text.primary` — Passo 1 `icon="share"` título
    "Toque em Compartilhar" descrição "Na barra de baixo do Safari, toque no
    ícone do quadrado com a seta para cima."; Passo 2 `icon="plus-square"`
    título "Toque em \"Adicionar à Tela de Início\"" descrição "Deslize a
    lista de opções até encontrar \"Adicionar à Tela de Início\" e toque
    nela."; Passo 3 `icon="check-circle"` título "Toque em \"Adicionar\""
    descrição "Confirme tocando em \"Adicionar\", no canto superior direito
    da tela."; `marginBottom: theme.spacing.xxl` entre linhas, `0` na
    última); nota final "Pronto! O ícone do ForçaApp vai aparecer na sua
    Tela de Início, igual a um aplicativo de verdade." (`fontSizes.lg`/`400`/
    `color: theme.colors.text.secondary`).

    Estado 2 (`ios && !safari`): Display headline "Abra este link no
    Safari"; `Notice` de `../components/ui/Feedback` com `tone="info"`,
    `title="Você está usando outro navegador. Para instalar o ForçaApp,
    este link precisa ser aberto no Safari — o navegador que já vem no
    iPhone."`, `description="Toque e segure este link, escolha \"Copiar\" e
    cole na barra de endereço do Safari. Ou toque no menu do seu navegador
    e escolha \"Abrir no Safari\", se essa opção aparecer."` — sem passos
    numerados; reusar `Notice` como está (não editar Feedback.tsx, a
    tipografia interna dele governa este bloco).

    Estado 3 (`!ios`, cobre desktop/Android E qualquer UA não reconhecido):
    Display headline "Abra este link no seu iPhone"; corpo "O ForçaApp só
    pode ser instalado por um iPhone, usando o Safari. Envie este endereço
    para o seu iPhone (por mensagem ou e-mail) e abra por lá."
    (`fontSizes.lg`/`400`/`color: theme.colors.text.secondary`); chip de URL:
    `View` com borda (`borderColor: theme.colors.border.subtle`,
    `backgroundColor: theme.colors.surface.card`) contendo `Text`
    selecionável (`fontFamily: theme.fonts.ui`, `color:
    theme.colors.text.primary`) lendo `typeof window !== 'undefined' ?
    window.location.href : ''` (mesma guarda defensiva já usada em
    `linkingConfig.ts`'s `webOrigin` — nunca lança quando `window` está
    ausente); sem botão de copiar, sem `expo-clipboard` (não está em
    package.json, UI-SPEC proíbe adicionar); sem QR code (deferido).

    Estado 4 (`standalone`): badge circular (mesma forma do badge do
    StepRow) com `Feather` `check-circle` colorido
    `theme.colors.accent.main` (único uso de neon em ícone fora do número do
    StepRow, aprovado pelo UI-SPEC); `Chip` de `../components/ui/Feedback`
    com `label="INSTALADO"` `tone="accent"` (o `fontSizes.micro` interno do
    Chip já bate com o papel Label de 9px); Display headline "Você já
    instalou o ForçaApp"; corpo "O aplicativo está pronto na sua Tela de
    Início. Pode fechar esta página e abrir o app por lá."; `Button` de
    `../components/ui/Button` `variant="primary"` `label="Abrir o
    ForçaApp"` `onPress={() => navigation.navigate(homeRoute)}` (via
    `useNavigation()` do `@react-navigation/native`) — sem ícone, é o único
    controle funcional da página inteira; sem passo a passo repetido.

    `StepRow` local (escopo do módulo, não exportado): props `{ number:
    number; icon: React.ComponentProps<typeof Feather>['name']; title:
    string; description: string }`.
  </action>
  <verify>
    <automated>npx jest __tests__/installDetection.test.ts __tests__/InstallScreen.test.tsx -x && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/installDetection.test.ts -x` sai com código 0.
    - `npx jest __tests__/InstallScreen.test.tsx -x` sai com código 0.
    - `npx tsc --noEmit` sai com código 0.
    - `linkingInterceptor.config.screens.Instalar === 'instalar'` em `src/navigation/linking.ts`.
    - `src/navigation/AuthNavigator.tsx` contém `<Stack.Screen name="Instalar">`.
    - `grep -rn "fetch(\|axios\|analytics" src/utils/installDetection.ts src/screens/InstallScreen.tsx` não retorna nenhuma linha (prohibition: zero telemetria).
    - InstallScreen renderizada sem nenhum mock/provider de `AuthContext` continua funcionando nos 4 estados (prohibition: zero dado de auth).
  </acceptance_criteria>
  <done>Um aluno deslogado que abre /instalar no Safari do iPhone chega na tela e vê os 4 estados corretos (mesmo com a árvore Main ainda não registrada); a página não tem efeito nenhum fora do web.</done>
</task>

<task type="auto">
  <name>Task 2: Estender o alcance de /instalar para Onboarding e Main — linkingConfig.ts + as 2 árvores restantes + guarda de regressão</name>
  <reversibility rating="reversible">Registro aditivo em mais 2 navigators tipados + 1 extensão de teste existente — reverter é remover as 3 linhas novas e o describe adicionado, sem efeito em nenhuma rota já existente.</reversibility>
  <files>src/navigation/linkingConfig.ts, src/navigation/OnboardingNavigator.tsx, src/navigation/MainNavigator.tsx, __tests__/navigationLinking.test.ts</files>
  <read_first>
    12-RESEARCH.md (Pattern 1 — receita completa dos 5 edits; Pattern 2 —
    tabBarButton:null é garantia só do v6 instalado; Pitfall 2; Pitfall 3;
    seção Code Examples com o teste de `/instalar` via getStateFromPath);
    src/navigation/linkingConfig.ts (LINKING_CONFIG.screens atual — Home/
    Training/Progress/Profile);
    src/navigation/OnboardingNavigator.tsx (OnboardingStackParamList
    tipado, `createStackNavigator<OnboardingStackParamList>()`);
    src/navigation/MainNavigator.tsx (MainTabParamList tipado, os 4
    `<BottomTab.Screen>` existentes);
    __tests__/navigationLinking.test.ts (suíte existente a estender, não
    tocar a suíte de ActiveSession já presente);
    __tests__/serviceWorkerConfig.test.ts (suíte 4 — padrão de ler um
    arquivo como texto via `fs.readFileSync` e afirmar via regex, reusar
    esta técnica para o guard estrutural do tabBarButton);
    src/screens/InstallScreen.tsx (criado no Task 1 — só importar, não
    editar).
  </read_first>
  <action>
    Em `src/navigation/linkingConfig.ts`: adicionar `Instalar: 'instalar',`
    como chave top-level dentro de `LINKING_CONFIG.screens`, sibling de
    `Home`/`Training`/`Progress`/`Profile` — NUNCA aninhada sob nenhuma
    delas (aninhar produz `/home/instalar`, quebrando a URL literal `/instalar`
    que o Estado 3 exibe de volta ao usuário — Anti-Pattern de
    12-RESEARCH.md).

    Em `src/navigation/OnboardingNavigator.tsx`: importar `InstallScreen`
    de `../screens/InstallScreen`; adicionar `Instalar: undefined;` ao tipo
    exportado `OnboardingStackParamList` (Pitfall 3 — este navigator É
    tipado, ao contrário de AuthNavigator); adicionar
    `<Stack.Screen name="Instalar" options={{ headerShown: false }}>{() =>
    <InstallScreen homeRoute="Questionnaire" />}</Stack.Screen>` junto às
    telas existentes.

    Em `src/navigation/MainNavigator.tsx`: importar `InstallScreen` de
    `../screens/InstallScreen`; adicionar `Instalar: undefined;` ao tipo
    exportado `MainTabParamList` (também tipado,
    `createBottomTabNavigator<MainTabParamList>()`); adicionar
    `<BottomTab.Screen name="Instalar" options={{ tabBarButton: () => null
    }}>{() => <InstallScreen homeRoute="Home" />}</BottomTab.Screen>` junto
    aos 4 `<BottomTab.Screen>` existentes — `tabBarButton: () => null`
    esconde a 5ª aba E seu espaço reservado no `@react-navigation/bottom-tabs@6.6.1`
    instalado (Pattern 2, verificado contra o source instalado); é garantia
    só do v6 — não levar essa suposição para um upgrade futuro sem
    reverificar `BottomTabBar.tsx`/`BottomTabItem.tsx`.

    Estender `__tests__/navigationLinking.test.ts` (arquivo existente,
    adicionar um novo `describe`, não tocar a suíte de ActiveSession já
    presente) com: (1) a asserção exata da seção Code Examples de
    12-RESEARCH.md — `getStateFromPath('/instalar', LINKING_CONFIG)`
    resolve e `state.routes[0].name === 'Instalar'`; (2) um guard estrutural
    de regressão para o Pitfall 2 — ler `src/navigation/MainNavigator.tsx`
    como texto via `fs.readFileSync` (mesma técnica da suíte 4 de
    `__tests__/serviceWorkerConfig.test.ts`, que lê `register-sw.js` como
    texto) e afirmar que a string `tabBarButton: () => null` aparece a até
    200 caracteres de distância da ocorrência de `name="Instalar"` no
    arquivo (escopo pela proximidade, para não dar falso-positivo se outra
    tela também usar `tabBarButton: () => null` no futuro).
  </action>
  <verify>
    <automated>npx jest __tests__/navigationLinking.test.ts -x && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/navigationLinking.test.ts -x` sai com código 0.
    - `getStateFromPath('/instalar', LINKING_CONFIG).routes[0].name === 'Instalar'`.
    - `MainTabParamList` e `OnboardingStackParamList` incluem `Instalar: undefined`.
    - `npx tsc --noEmit` sai com código 0 (os dois navigators tipados compilam limpo com a tela nova).
    - `grep -n "tabBarButton: () => null" src/navigation/MainNavigator.tsx` retorna pelo menos uma linha.
    - `grep -rln "'Instalar'" src/screens/` não retorna nenhum arquivo além de `InstallScreen.tsx` (prohibition: zero botão novo de entrada em outra tela).
  </acceptance_criteria>
  <done>/instalar resolve corretamente nas 3 árvores de navegação (Auth, Onboarding, Main) — um aluno em qualquer estado de sessão que abre a URL chega na mesma InstallScreen, com o CTA do Estado 4 navegando para a rota-lar certa de cada árvore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aluno (navegador não autenticado) -> rota `/instalar` | Rota pública nova, alcançável mesmo sem sessão — não pode vazar dado de usuário nem abrir um bypass do gate de auth existente. |
| `navigator.userAgent`/`matchMedia` (controlado pelo cliente) -> `installDetection.ts` | String controlada pelo cliente, parseada de forma síncrona por regex — não pode admitir ReDoS nem lançar exceção. |
| `InstallScreen` reachable a partir da árvore Main (usuário logado) | Primeira tela deste projeto que é ao mesmo tempo pública E alcançável dentro do app autenticado — superfície nova para vazamento acidental de contexto de sessão. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Spoofing | installDetection.ts (UA string) | low | accept | Pior caso é mostrar a copy errada (ex.: fallback desktop em vez dos passos iOS) — nunca exposição de dado/auth; Pitfall 4 já exige fallback seguro por construção. |
| T-12-02 | Tampering | homeRoute prop nos 3 pontos de registro | medium | mitigate | Cada registro passa um valor estático hardcoded ("Login"/"Questionnaire"/"Home"), nunca dinâmico/vindo de input; `tsc --noEmit` + `OnboardingStackParamList`/`MainTabParamList` pegam um nome de rota inválido em tempo de compilação. |
| T-12-03 | Information Disclosure | InstallScreen registrada também na árvore Main (usuário logado) | medium | mitigate | InstallScreen não recebe nem lê nenhuma prop/hook de `AuthContext`/sessão/perfil — é conteúdo estático puro, testado (Task 1) sem nenhum provider de auth montado. |
| T-12-04 | Denial of Service | Regex de installDetection.ts contra UA controlada pelo atacante | low | mitigate | Todos os regex são casamentos de token literal, sem quantificador aninhado (`/iPad\|iPhone\|iPod/`, `/CriOS\|FxiOS\|EdgiOS\|OPiOS\|OPT\//`, `/Safari/`, `/Macintosh/`) — O(n) no tamanho da UA, sem risco de backtracking catastrófico. |
| T-12-05 | Elevation of Privilege | Registro de `Instalar` como sibling top-level em `MainTabParamList`/`LINKING_CONFIG` | medium | mitigate | Aditivo apenas nos 3 navigators — nenhuma rota, guard ou ordem existente muda; `Instalar` não abre nenhum caminho de navegação novo além do próprio CTA de volta à rota-lar da árvore em que já está montado. |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | accept | Nenhuma instalação de pacote nesta fase — 12-RESEARCH.md confirma zero dependência nova (Package Legitimacy Audit: not applicable). |

</threat_model>

<verification>
`npx jest __tests__/installDetection.test.ts __tests__/InstallScreen.test.tsx
__tests__/navigationLinking.test.ts -x` sai com código 0; `npx tsc --noEmit`
sai com código 0; `linkingInterceptor.config.screens.Instalar` e
`LINKING_CONFIG.screens.Instalar` valem `'instalar'`; os 3 navigators
registram `Instalar`; `MainNavigator.tsx` contém `tabBarButton: () => null`
perto da ocorrência de `Instalar`.
</verification>

<success_criteria>
Critério de sucesso 1 (rota `/instalar` existe com passo a passo) e critério
2 (detecção de standalone, sem repetir o passo a passo) do ROADMAP Fase 12
fechados nos 3 estados de sessão (deslogado, onboarding, logado). Critério 3
(UAT do dono/aluno num iPhone real) fica para o Plano 12-02.
</success_criteria>

## Artifacts this phase produces

- `src/utils/installDetection.ts` — novo (Task 1): `isIOS`/`isSafari`/
  `isStandalone` + helpers puros `isIOSDevice`/`isSafariBrowser`.
- `src/screens/InstallScreen.tsx` — novo (Task 1): orquestra os 4 estados +
  `StepRow` local.
- `src/navigation/linking.ts` — modificado (Task 1):
  `linkingInterceptor.config.screens.Instalar`.
- `src/navigation/AuthNavigator.tsx` — modificado (Task 1):
  `<Stack.Screen name="Instalar">` (homeRoute="Login").
- `src/navigation/linkingConfig.ts` — modificado (Task 2):
  `LINKING_CONFIG.screens.Instalar`.
- `src/navigation/OnboardingNavigator.tsx` — modificado (Task 2):
  `OnboardingStackParamList` + `<Stack.Screen name="Instalar">`
  (homeRoute="Questionnaire").
- `src/navigation/MainNavigator.tsx` — modificado (Task 2): `MainTabParamList`
  + `<BottomTab.Screen name="Instalar" options={{tabBarButton: () => null}}>`
  (homeRoute="Home").
- `__tests__/installDetection.test.ts` — novo (Task 1).
- `__tests__/InstallScreen.test.tsx` — novo (Task 1): 4 estados + guarda
  web-only.
- `__tests__/navigationLinking.test.ts` — modificado (Task 2): resolução de
  `/instalar` + guarda estrutural do `tabBarButton`.

<output>
Create `.planning/phases/12-p-gina-de-instala-o-guiada/12-01-SUMMARY.md` when done
</output>
