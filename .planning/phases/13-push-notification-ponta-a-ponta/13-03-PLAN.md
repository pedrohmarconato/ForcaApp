---
phase: 13-push-notification-ponta-a-ponta
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/utils/pushBadge.ts
  - src/screens/HomeScreen.tsx
  - __tests__/pushBadge.test.ts
autonomous: true
requirements: [PUSH-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Com Notification.permission === 'granted' e um treino pendente hoje (todaySession.status === 'pending' e ehHoje), a Home chama navigator.setAppBadge(1); sem treino pendente hoje (ou nenhuma sessão de hoje), chama navigator.clearAppBadge()/setAppBadge(0) — verificado por jest com navigator mockado."
    - "Sem 'setAppBadge' in navigator (iOS < 16.4) OU com Notification.permission !== 'granted' (default ou denied), pushBadge.ts nunca chama navigator.setAppBadge — no-op silencioso, sem lançar exceção (edge de PUSH-04, per specless-probe-fallback)."
    - "Uma falha de navigator.setAppBadge (Promise rejeitada — comportamento real documentado em navegadores sem suporte pleno) é engolida via .catch silencioso — nunca aparece como erro não tratado nem crasha a tela Home."
  artifacts:
    - src/utils/pushBadge.ts
    - __tests__/pushBadge.test.ts
  key_links:
    - "HomeScreen.tsx (todaySession, ehHoje já calculados na tela) -> pushBadge.updateTrainingBadge(pendente: boolean) -> navigator.setAppBadge/clearAppBadge, gated por Notification.permission — nenhuma lógica de pendência duplicada em pushBadge.ts (a tela decide O QUE contar, o util decide COMO/SE chamar a API)."
  prohibitions:
    - statement: "pushBadge.ts nunca recalcula 'treino pendente' por conta própria (nenhuma query/import de trainingRepository) — recebe o booleano já resolvido pela tela chamadora, evitando uma segunda fonte de verdade sobre o que é 'pendência de treino'."
      status: resolved
      verification: judgment
---

<objective>
Fechar PUSH-04: o ícone do app exibe um badge quando há treino pendente hoje,
mas SOMENTE quando o aluno já concedeu permissão de notificação (gate
explícito do ROADMAP: "gated por permissão de push concedida") — sem
depender de nenhuma peça de servidor nova, reusando o `todaySession`/`ehHoje`
que `HomeScreen.tsx` já calcula para o card de destaque.

Nota sobre a sonda de edge coverage: PUSH-04 veio "unclassified"/"unresolved"
da sonda determinística. O único edge real (iOS < 16.4 ou permissão negada ->
no-op silencioso) foi autorado manualmente em `must_haves.truths` como truth
verificável localmente via jest (mock de `navigator`), sem precisar de
device real — a chamada real de `navigator.setAppBadge` em produção
continua dependendo de iOS 16.4+ com PWA instalado, coberta pelo UAT do
Plano 13-04.

Purpose: dar ao aluno um sinal visual passivo (badge) de que há um treino
esperando, sem abrir o app, coerente com o padrão "silent push proibido" (o
badge nunca é atualizado por um push silencioso — só pela própria página, no
foco).
Output: `src/utils/pushBadge.ts`, hook em `HomeScreen.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-push-notification-ponta-a-ponta/13-CONTEXT.md
@.planning/phases/13-push-notification-ponta-a-ponta/13-RESEARCH.md
@src/screens/HomeScreen.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pushBadge.ts — util puro gated por suporte e permissão (PUSH-04)</name>
  <reversibility rating="reversible">Util novo, sem efeito colateral fora do próprio badge do ícone — remover é apagar o arquivo e o hook na Home.</reversibility>
  <files>src/utils/pushBadge.ts, __tests__/pushBadge.test.ts</files>
  <read_first>
    13-RESEARCH.md ("Code Examples" — `navigator.setAppBadge` gated por
    permissão, citação webkit.org/blog/14112; Pitfall 4 — badge nunca sozinho
    via push silencioso, só junto de uma notificação visível OU, como aqui,
    recalculado no foco da página);
    src/utils/alertShim.ts (molde de util `Platform.OS`/feature-detection com
    guarda silenciosa, mesmo estilo a seguir aqui embora sem Platform.OS —
    setAppBadge só existe no alvo web).
  </read_first>
  <behavior>
    - Teste 1: `'setAppBadge' in navigator` true, `Notification.permission ===
      'granted'`, `updateTrainingBadge(true)` chama `navigator.setAppBadge`
      com o argumento `1`.
    - Teste 2: mesmas condições, `updateTrainingBadge(false)` chama
      `navigator.clearAppBadge` (ou `setAppBadge(0)` se `clearAppBadge`
      indisponível) — nunca deixa um badge "1" preso quando não há mais
      pendência.
    - Teste 3: `'setAppBadge' in navigator` false (API ausente) —
      `updateTrainingBadge(true)` não lança, não chama nada.
    - Teste 4: `Notification.permission === 'default'` (nunca pedida) ou
      `'denied'` — `updateTrainingBadge(true)` não chama `setAppBadge` mesmo
      com a API presente.
    - Teste 5: `navigator.setAppBadge` mockado para retornar uma Promise
      rejeitada — `updateTrainingBadge(true)` não lança, a rejeição é
      engolida (`.catch` silencioso).
  </behavior>
  <action>
    Criar `src/utils/pushBadge.ts`: `export const updateTrainingBadge =
    (pendente: boolean): void => { ... }` — corpo síncrono que primeiro checa
    `typeof navigator === 'undefined' || !('setAppBadge' in navigator) ||
    typeof Notification === 'undefined' || Notification.permission !==
    'granted'` e retorna cedo (no-op) se qualquer uma for verdadeira; senão,
    se `pendente`, chama `(navigator as any).setAppBadge(1).catch(() => {})`;
    senão, chama `('clearAppBadge' in navigator ? (navigator as
    any).clearAppBadge() : (navigator as any).setAppBadge(0)).catch(() =>
    {})`. Tipar com um `interface NavigatorComBadge` mínimo (ou `as any`
    pontual documentado) já que `lib.dom` do TS pode não ter os tipos da
    Badging API — comentário explicando por quê, mesmo padrão de exceção
    pontual de tipos já usado em outros utils do repo quando a API é nova
    demais para os typings padrão.
  </action>
  <verify>
    <automated>npx jest __tests__/pushBadge.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/pushBadge.test.ts` sai com código 0 e cobre os 5 comportamentos do bloco `<behavior>`.
    - `grep -n "Notification.permission" src/utils/pushBadge.ts` confirma o gate de permissão antes de qualquer chamada a `setAppBadge`.
  </acceptance_criteria>
  <done>pushBadge.ts entregue, testado, no-op silencioso garantido para os 3 casos de ausência de suporte/permissão/falha de Promise.</done>
</task>

<task type="auto">
  <name>Task 2: Hook em HomeScreen.tsx — badge reflete o treino de hoje (PUSH-04)</name>
  <reversibility rating="reversible">Só um `useEffect` a mais na tela — remover não afeta nenhum dado nem navegação existente.</reversibility>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    src/screens/HomeScreen.tsx linhas 1-45 (imports) e 160-204 (`todaySession`,
    `ehHoje`, `ehAtrasado` já calculados — a pendência de hoje é
    `ehHoje && todaySession?.status === 'pending'`, mesma condição que já
    decide o título "Seu treino de hoje" vs "Seu próximo treino"; NÃO
    recalcular isso de outra forma).
  </read_first>
  <action>
    Em `src/screens/HomeScreen.tsx`, importar `updateTrainingBadge` de
    `'../utils/pushBadge'`. Adicionar um `useEffect` (após o cálculo de
    `ehHoje`, antes do `return`) com dependências `[ehHoje,
    todaySession?.status]` que chama `updateTrainingBadge(ehHoje &&
    todaySession?.status === 'pending')` — reusa EXATAMENTE a mesma condição
    já usada para `tituloDestaque`/`ehAtrasado`, sem introduzir uma segunda
    definição de "pendência". Nenhuma outra tela precisa deste hook: a Home é
    a única fonte de verdade de "treino de hoje" no app (mesmo comentário já
    presente no cabeçalho do arquivo).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . && npx jest __tests__/pushBadge.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p .` sai com código 0 (nenhum erro de tipo introduzido pelo hook novo).
    - `grep -n "updateTrainingBadge(ehHoje" src/screens/HomeScreen.tsx` confirma que o hook reusa a variável `ehHoje` já existente, sem recalcular a condição.
  </acceptance_criteria>
  <done>Badge do ícone reflete o treino pendente de hoje, gated por permissão, sem duplicar a lógica de "treino de hoje" já existente na Home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `navigator.setAppBadge` (API do navegador) | Só chamada no contexto da própria página, nunca a partir de um push recebido em segundo plano — sem superfície de rede nova. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-09 | Information Disclosure | Badge revelando "há algo pendente" a quem olha o ícone num device compartilhado | low | accept | O badge é só um número (contagem), sem nenhum texto/dado de treino — mesmo risco mínimo já aceito para qualquer ícone de app com contador; nenhuma mitigação adicional necessária para o volume/uso familiar deste projeto. |

</threat_model>

<verification>
`npx jest __tests__/pushBadge.test.ts` verde; `npx tsc --noEmit -p .` sai com
código 0.
</verification>

<success_criteria>
Com permissão de push concedida, o ícone do app mostra um badge quando há
treino pendente hoje e some quando não há — sem depender de nenhuma peça de
servidor nova, sem duplicar a lógica de "treino de hoje" da Home.
</success_criteria>

## Artifacts this phase produces

- `src/utils/pushBadge.ts` — novo (Task 1): `updateTrainingBadge()`, gated por
  suporte + permissão, no-op silencioso.
- `__tests__/pushBadge.test.ts` — novo (Task 1): guarda permanente dos 5 casos.
- `src/screens/HomeScreen.tsx` — modificado (Task 2): hook reusando `ehHoje`/`todaySession.status`.

<output>
Create `.planning/phases/13-push-notification-ponta-a-ponta/13-03-SUMMARY.md` when done
</output>
