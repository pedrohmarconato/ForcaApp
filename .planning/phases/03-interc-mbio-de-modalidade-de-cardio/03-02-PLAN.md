---
phase: 03-interc-mbio-de-modalidade-de-cardio
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/sessionModel.ts
  - src/constants/cardioModalidades.ts
  - src/services/sessionExecutionRepository.ts
  - src/store/activeSessionStore.ts
  - src/services/cardioModalidadesAceitasRepository.ts
  - __tests__/cardioSwap.test.ts
  - __tests__/cardioSwapFluxo.test.ts
  - __tests__/cardioModalidadesAceitas.test.ts
autonomous: true
requirements: [REQ-06]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "applyCardioSwapToDraft (D-01) preserva targetDurationSeconds de toda série do exercício trocado e zera targetDistanceM em todas elas — a meta de distância da modalidade original nunca sobrevive à troca."
    - "applyCardioSwapToDraft (D-04) atualiza DraftExercise.metric para 'tempo_distancia' quando a nova modalidade está em CARDIO_MODALIDADES_COM_DISTANCIA e para 'tempo' caso contrário — isso é o que faz SessionPlayer mostrar (ou esconder) o campo de distância REALIZADA sem que este plano precise tocar SessionPlayer.tsx."
    - "swapExercise no store grava no servidor (RPC swap_session_exercise) ANTES de tocar o draft local — falha do servidor não deixa a troca aparecer aplicada na tela (mesmo padrão de skipExercise, servidor primeiro)."
    - "Retomar uma sessão (startOrResume) reaplica trocas registradas no servidor (OpenSessionLog.exerciseSwaps) ao draft reconstruído — uma troca feita e o app fechado antes do rascunho local gravar não se perde na retomada."
    - "getModalidadesAceitas nunca inventa aceitação: usuário sem cardio_modalidades preenchido (null) ou com lista vazia recebe array vazio, nunca as 9 modalidades do catálogo inteiro — decisão documentada nesta plan resolvendo a Assumption A1 do RESEARCH.md (leitura estrita de D-02, 'só as aceitas', distinta do fallback do GERADOR de plano)."
    - statement: "O probe determinístico de edge coverage devolveu REQ-06 como 'unclassified'/'unresolved' (nenhum SPEC.md nesta fase) — a cobertura de borda de REQ-06 não fica assumida em silêncio: ela é satisfeita pela soma explícita dos truths D-01..D-08 espalhados pelos 6 plans desta fase (03-01..03-06), cada um verificável por teste automatizado; nenhuma borda adicional além dessas é assumida coberta."
      verification: backstop
  artifacts:
    - src/services/cardioModalidadesAceitasRepository.ts
  key_links:
    - "activeSessionStore.swapExercise -> sessionExecutionRepository.swapSessionExercise -> RPC swap_session_exercise (migration 0034, Plano 03-01) -> applyCardioSwapToDraft (sessionModel.ts) — mesma ordem servidor-primeiro de skipExercise."
    - "activeSessionStore.applyServerSetLogs -> OpenSessionLog.exerciseSwaps -> applyCardioSwapToDraft (reduce) — mesmo ponto único de reconciliação que já reaplica exerciseSkips na retomada."
  prohibitions:
    - "swapExercise nunca aplica applyCardioSwapToDraft ao draft local antes de a RPC swap_session_exercise confirmar — mutação otimista do estado local antes da confirmação do servidor está fora de escopo e contraria o padrão estabelecido (skipExercise, saveSetLog)."
    - "getModalidadesAceitas nunca oferece uma modalidade fora do que o usuário aceitou quando a lista não está vazia — nenhum fallback 'mostrar todas' é aceitável para lista não-vazia, mesmo que incompleta ou desatualizada."
    - "Nenhuma lógica de troca é adicionada a cardioPrescrito.ts ou cardioPrescritoRepository.ts nesta ou em nenhuma task futura da fase — o lado PRESCRITO permanece inteiramente alheio à existência de trocas (D-06)."
---

<objective>
TRACER da Fase 3: o caminho completo de uma troca de modalidade de cardio, do motor puro até o
servidor e de volta, para UMA modalidade — sem UI ainda (a UI de entrada vem no Plano 03-03).
Entrega: `applyCardioSwapToDraft` (D-01, D-04) em `sessionModel.ts`; `swapSessionExercise` +
extensão de `getOpenSessionLog`/`OpenSessionLog` em `sessionExecutionRepository.ts`;
`swapExercise` no store com o mesmo padrão servidor-primeiro-depois-draft de `skipExercise`, e
reconciliação na retomada; e `cardioModalidadesAceitasRepository.ts` (capability nova — hoje
não existe NENHUM caminho de leitura cliente↔banco para `cardio_modalidades` fora do
onboarding).

Purpose: RESEARCH.md confirma que os dois pontos de entrada da UI (Planos 03-03/03-04) e o
histórico (Plano 03-05) dependem inteiramente desta base: sem `applyCardioSwapToDraft` e
`swapExercise` testados e corretos, qualquer botão de UI estaria chamando uma engrenagem que
não existe. Este plano prova a engrenagem ponta a ponta (chamada → servidor → draft →
retomada) ANTES de qualquer expansão de UI, seguindo TRACER_MODE: entry point/sheet/visibilidade
ficam para os planos seguintes; aqui é o motor + persistência + store, verificado por teste
automatizado real (store↔repositório mockado), não manual.
Output: motor puro testado, repositório de escrita/leitura testado, store com ação nova
testada, repositório de leitura de "modalidades aceitas" testado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-CONTEXT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-RESEARCH.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-PATTERNS.md
@src/engine/sessionModel.ts
@src/constants/cardioModalidades.ts
@src/services/sessionExecutionRepository.ts
@src/store/activeSessionStore.ts
@__tests__/recusaDeclaradaFluxo.test.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `src/engine/sessionModel.ts`: `applyCardioSwapToDraft` (função nova), `DraftExercise.swappedFrom` (campo novo, opcional)
- `src/constants/cardioModalidades.ts`: `isCardioModalidade` (type guard novo, molde de `isSkipReason`)
- `src/services/sessionExecutionRepository.ts`: `swapSessionExercise` (função nova), `ServerCardioSwap` (tipo novo), `OpenSessionLog.exerciseSwaps` (campo novo)
- `src/store/activeSessionStore.ts`: `swapExercise` (ação nova no store)
- `src/services/cardioModalidadesAceitasRepository.ts` (arquivo novo) — `getModalidadesAceitas`
- `__tests__/cardioSwap.test.ts` (arquivo novo)
- `__tests__/cardioSwapFluxo.test.ts` (arquivo novo)
- `__tests__/cardioModalidadesAceitas.test.ts` (arquivo novo)
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Motor de troca (D-01/D-04) + repositório de escrita + store, servidor primeiro</name>
  <files>src/engine/sessionModel.ts, src/constants/cardioModalidades.ts, src/services/sessionExecutionRepository.ts, src/store/activeSessionStore.ts, __tests__/cardioSwap.test.ts, __tests__/cardioSwapFluxo.test.ts</files>
  <read_first>
    src/engine/sessionModel.ts (INTEIRO — já em contexto; releia
    `applyExerciseSkipToDraft`/`removeExerciseSkipFromDraft` linhas 505-541 como molde exato de
    imutabilidade/spread, `DraftExercise` linhas 92-120 para o ponto de inserção do campo
    `swappedFrom`, `isSkipReason` linhas 52-54 como molde do type guard,
    `coerceDraftNumerics` linhas 408-459 — `swappedFrom` NÃO precisa de coerção numérica,
    sobrevive pelo spread `...ex`, mas confirme isso lendo o corpo);
    src/constants/cardioModalidades.ts (INTEIRO — `CARDIO_MODALIDADES` e
    `CARDIO_MODALIDADES_COM_DISTANCIA`, os 9/6 nomes exatos); src/services/sessionExecutionRepository.ts
    linhas 1-53 (tipos `ServerExerciseSkip`/`OpenSessionLog`, para o molde de `ServerCardioSwap`),
    linhas 159-236 (`getOpenSessionLog` INTEIRA — select com sibling relation
    `exercise_skips(planned_exercise_id, reason, note)` linha 170, processamento `flatMap` das
    linhas 212-222, retorno linha 224-235), linhas 238-273 (`skipSessionExercise` INTEIRA — molde
    exato de `swapSessionExercise`); src/store/activeSessionStore.ts linhas 1-60 (imports, para
    saber onde adicionar `applyCardioSwapToDraft`/`swapSessionExercise`/`CardioModalidade`),
    linhas 111-180 (interface `ActiveSessionState`, ponto de inserção de `swapExercise` logo
    após `unskipExercise` linha 174), linhas 330-447 (`applyServerSetLogs` INTEIRA — o reduce de
    `comRecusas` linhas 435-439 é o molde exato para o reduce de trocas; o retorno final linhas
    442-446 precisa passar a usar o resultado do NOVO reduce), linhas 1395-1483 (`skipExercise`/
    `unskipExercise` INTEIRAS — molde exato de `swapExercise`, incluindo `operationEpoch`,
    `errMsg`, `saveDraft`, `pendingAdaptation`); __tests__/recusaDeclaradaFluxo.test.ts (INTEIRO
    — molde de mock de `sessionExecutionRepository` e de `sessionDraftStorage`, e dos 2 modos de
    falha "servidor primeiro" e "recusa some na retomada" que este teste replica para troca).
  </read_first>
  <behavior>
    `applyCardioSwapToDraft(draft, exerciseId, toModality)`:
    - Teste 1 (D-01): exercício com 2 séries (`targetDurationSeconds: 1200`,
      `targetDistanceM: 5000` cada) trocado para 'Remo Ergômetro' -> cada série mantém
      `targetDurationSeconds: 1200` e `targetDistanceM` vira `null` nas duas.
    - Teste 2 (D-04, com distância): trocado para uma modalidade de
      `CARDIO_MODALIDADES_COM_DISTANCIA` (ex.: 'Remo Ergômetro') -> `ex.metric` vira
      `'tempo_distancia'`.
    - Teste 3 (D-04, só tempo): trocado para uma modalidade FORA de
      `CARDIO_MODALIDADES_COM_DISTANCIA` (ex.: 'Pular Corda') -> `ex.metric` vira `'tempo'`.
    - Teste 4 (rótulo D-08): `ex.name` vira o `toModality`; `ex.swappedFrom` vira o `ex.name`
      ORIGINAL (o nome antes da troca).
    - Teste 5 (troca dupla não perde a origem): aplicar `applyCardioSwapToDraft` duas vezes em
      sequência (ex.: "Corrida" -> "Remo Ergômetro" -> "Bicicleta Ergométrica") -> `swappedFrom`
      final continua "Corrida" (a origem VERDADEIRA), não "Remo Ergômetro" (a origem
      intermediária) — usar `ex.swappedFrom ?? ex.name` na implementação.
    - Teste 6 (exercício errado intocado): exercício com `exerciseId` diferente do alvo não é
      alterado (mesmo padrão de `applyExerciseSkipToDraft`).
    - Teste 7 (imutabilidade): o `draft` original passado não é mutado (comparar referência do
      array `exercises` e de cada `DraftExercise`/`DraftSet` antes/depois).

    `swapExercise` (store), via `useActiveSessionStore.getState()` com `sessionExecutionRepository`
    mockado (`jest.mock`, mesmo padrão de `recusaDeclaradaFluxo.test.ts`):
    - Teste 8 (servidor primeiro): chamar `swapExercise('ex-2', 'Remo Ergômetro')` -> `swapSessionExercise`
      é chamado com `{ sessionLogId, plannedExerciseId: 'ex-2', toModality: 'Remo Ergômetro', note: null }`
      ANTES de `draft.exercises[1].name` mudar (mock resolve, então assert pós-await é
      suficiente: `toHaveBeenCalledWith` + o draft final reflete a troca).
    - Teste 9 (falha do servidor não aplica localmente): `swapSessionExercise` mockado para
      rejeitar -> `swapExercise` retorna `false`, `store().saveError` não é nulo, e
      `store().draft!.exercises[1].name` continua o nome ORIGINAL (não mudou).
    - Teste 10 (retomada reaplica troca do servidor): `getOpenSessionLog` mockado retornando
      `exerciseSwaps: [{ plannedExerciseId: 'ex-2', toModality: 'Remo Ergômetro', note: null }]`
      -> após `startOrResume`, `store().draft!.exercises` contém o exercício com `name: 'Remo
      Ergômetro'` e `swappedFrom` igual ao nome original do plano.
    - Teste 11 (rascunho local sem a troca não apaga a troca do servidor): mesmo modo de falha 2
      de `recusaDeclaradaFluxo.test.ts` — `loadDraft` mockado devolvendo um draft local SEM o
      swap, `getOpenSessionLog` devolvendo o swap -> o resultado final tem a troca aplicada
      (servidor é autoritativo).
  </behavior>
  <action>
    Em `src/constants/cardioModalidades.ts`, adicione `isCardioModalidade` — type guard
    `(v: unknown): v is CardioModalidade => typeof v === 'string' &&
    (CARDIO_MODALIDADES as readonly string[]).includes(v)`, molde exato de `isSkipReason` em
    `sessionModel.ts:52-54`.

    Em `src/engine/sessionModel.ts`: importe `CardioModalidade` e
    `CARDIO_MODALIDADES_COM_DISTANCIA` de `../constants/cardioModalidades`. Adicione ao tipo
    `DraftExercise` (logo após `skipNote?: string | null;`) o campo
    `swappedFrom?: string | null;` com comentário citando D-08 ("nome ORIGINAL do exercício
    antes da troca — null quando nunca foi trocado; ausente em rascunho anterior à Fase 3 lido
    como não-trocado"). Adicione, ao lado de `applyExerciseSkipToDraft`/`removeExerciseSkipFromDraft`,
    a função `applyCardioSwapToDraft(draft: SessionDraft, exerciseId: string, toModality:
    CardioModalidade): SessionDraft` — mesmo shape de spread imutável: mapeia `draft.exercises`,
    para o exercício de `exerciseId` retorna um novo objeto com `name: toModality`,
    `swappedFrom: ex.swappedFrom ?? ex.name` (preserva a origem verdadeira em trocas
    encadeadas), `metric: CARDIO_MODALIDADES_COM_DISTANCIA.includes(toModality) ?
    'tempo_distancia' : 'tempo'` (D-04 — isto é o que liga/desliga o campo de distância
    REALIZADA em `SessionPlayer.tsx` sem tocar aquele arquivo, porque ele decide
    `temDistancia` por `metricOf(exercise) === 'tempo_distancia'`), e `sets:
    ex.sets.map((s) => ({ ...s, targetDistanceM: null }))` (D-01 — `targetDurationSeconds`
    nunca é tocado, sobrevive pelo spread). Comentário de bloco no mesmo estilo dos demais
    (por que zera distância, por que troca o metric, cite D-01/D-04/D-08).

    Em `src/services/sessionExecutionRepository.ts`: importe `CardioModalidade`,
    `isCardioModalidade` de `../constants/cardioModalidades`. Adicione o tipo
    `ServerCardioSwap = { plannedExerciseId: string; toModality: CardioModalidade; note: string
    | null; }` ao lado de `ServerExerciseSkip`. Adicione ao tipo `OpenSessionLog` o campo
    `exerciseSwaps: ServerCardioSwap[];` com comentário no mesmo espírito do de
    `exerciseSkips` (vem do servidor porque o rascunho local não é autoritativo). Em
    `getOpenSessionLog`, estenda a string do `.select(...)` (linha 170) adicionando
    `, cardio_exercise_swaps(planned_exercise_id, to_modality, note)` como sibling relation
    (mesmo padrão de `exercise_skips(...)`). Após o bloco `exerciseSkips` (linhas 212-222),
    adicione processamento análogo para `exerciseSwaps`: `flatMap` sobre
    `row.cardio_exercise_swaps ?? []`, validando `typeof sw?.planned_exercise_id === 'string'`
    e `isCardioModalidade(sw?.to_modality)` — descartar (não coagir) linha malformada, mesmo
    raciocínio de `isSkipReason` para `exerciseSkips`. Adicione `exerciseSwaps` ao objeto de
    retorno de `getOpenSessionLog` (linha ~234). Depois da função `skipSessionExercise`
    (linha 273), adicione `swapSessionExercise` — mesmo shape exato de `skipSessionExercise`:
    `supabase.rpc('swap_session_exercise', { p_session_log_id, p_planned_exercise_id,
    p_to_modality: params.toModality, p_note: params.note ?? null })`, mesmo tratamento de
    erro (`thrownRequestError`/`throwResponseError`), valida `row?.planned_exercise_id`,
    retorna `{ plannedExerciseId, toModality: isCardioModalidade(row.to_modality) ?
    row.to_modality : params.toModality, note }`.

    Em `src/store/activeSessionStore.ts`: importe `applyCardioSwapToDraft` de
    `../engine/sessionModel`, `swapSessionExercise` de
    `../services/sessionExecutionRepository`, `CardioModalidade` de
    `../constants/cardioModalidades`. Adicione à interface `ActiveSessionState`, logo após
    `unskipExercise: (exerciseId: string) => Promise<boolean>;` (linha 174), a assinatura
    `swapExercise: (exerciseId: string, toModality: CardioModalidade) => Promise<boolean>;`
    com comentário citando D-01 (servidor primeiro, mesma razão de `skipExercise`). Implemente
    `swapExercise` logo após `unskipExercise` (após linha 1483) — shape IDÊNTICO a
    `skipExercise` (linhas 1403-1453): valida `draft`/`sessionLogId`, encontra o exercício alvo
    (`if (!alvo) return false`), guarda de toque duplo (`if (alvo.name === toModality) return
    true`), captura `epoch`/`sid`, chama `await swapSessionExercise({ sessionLogId: sid,
    plannedExerciseId: exerciseId, toModality, note: null })` dentro de try/catch (catch seta
    `saveError` via `errMsg(e)` e retorna `false` — sem tocar o draft), depois do sucesso
    reconfirma `operationEpoch`/`sessionLogId` ainda válidos, aplica
    `applyCardioSwapToDraft(atual, exerciseId, toModality)`, `set({ draft: novo, saveError:
    null, pendingAdaptation: get().pendingAdaptation?.exerciseId === exerciseId ? null :
    get().pendingAdaptation })`, tenta `saveDraft(novo)` em try/catch não-fatal (mesmo
    `console.warn`/`storageWarning` de `skipExercise`), retorna `true`.

    Em `applyServerSetLogs` (linhas 344-447): após o bloco `comRecusas` (linha 435-439),
    adicione um reduce análogo para trocas: `const comTrocas = (aberta.exerciseSwaps ??
    []).reduce((acc, swap) => applyCardioSwapToDraft(acc, swap.plannedExerciseId,
    swap.toModality), comRecusas);` — comentário citando que este é o único ponto de
    reconciliação por onde as duas retomadas (local e reconstruída) passam, mesmo raciocínio já
    documentado para `comRecusas` linhas 428-434. Troque o `return { ...comRecusas,
    declinedReplanFingerprints: ... }` final (linha 442-446) para usar `comTrocas` no lugar de
    `comRecusas`.
  </action>
  <verify>
    <automated>npx jest __tests__/cardioSwap.test.ts __tests__/cardioSwapFluxo.test.ts -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/cardioSwap.test.ts __tests__/cardioSwapFluxo.test.ts -q` verde (11
    testes descritos em `<behavior>`, ler "Tests: X passed"); `npx tsc --noEmit` sem erros
    novos nos 4 arquivos tocados; `npx jest __tests__/recusaDeclaradaFluxo.test.ts -q` continua
    verde (a extensão de `applyServerSetLogs` não quebra a reconciliação de recusas
    existente); `grep -n "swapExercise" src/store/activeSessionStore.ts` mostra a assinatura na
    interface E a implementação.
  </acceptance_criteria>
  <done>applyCardioSwapToDraft, swapSessionExercise e swapExercise existem, testados, seguindo
  server-first; retomada reaplica trocas do servidor pelo mesmo ponto único de reconciliação
  que já reaplica recusas.</done>
  <reversibility rating="reversible">Código de aplicação (engine/service/store), revertível via
  git; não persiste nada por si só sem a RPC do Plano 03-01, que já tem seu próprio checkpoint.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repositório de "modalidades aceitas" (D-02, capability nova)</name>
  <files>src/services/cardioModalidadesAceitasRepository.ts, __tests__/cardioModalidadesAceitas.test.ts</files>
  <read_first>
    src/services/cardioGoalRepository.ts (INTEIRO — molde de estilo: cliente único
    `config/supabaseClient`, `toNum`/defesa de shape, erro sempre propaga); supabase/migrations/0008_questionario_usuario.sql
    linhas 27-41 (RLS "questionario select own" já cobre este SELECT — `auth.uid() = usuario_id`,
    `grant select ... to authenticated` — nenhuma migration nova é necessária para este
    repositório); src/constants/cardioModalidades.ts (após a Task 1 desta plan, já contém
    `isCardioModalidade`); 03-RESEARCH.md seção "Pitfall 4" e "Open Questions #1" (o dilema do
    fallback vazio — leia para entender POR QUE a decisão abaixo é a leitura estrita, não a do
    gerador de plano).
  </read_first>
  <behavior>
    `getModalidadesAceitas(userId)`:
    - Teste 1: `cardio_modalidades` com 3 nomes válidos -> devolve os 3, na mesma ordem.
    - Teste 2 (decisão do fallback vazio — D-02 estrito): `cardio_modalidades: null` OU
      `questionario_usuario` inexistente para o usuário (`maybeSingle` devolve `data: null`)
      -> devolve `[]` (array vazio), NUNCA as 9 modalidades do catálogo — comentário no código
      explicando que este é o comportamento da TELA DE TROCA, distinto do comentário da
      migration 0021 sobre o GERADOR de plano ("vazio = qualquer modalidade" é sobre o prompt
      da IA, não sobre o que esta tela oferece).
    - Teste 3: `cardio_modalidades: []` (lista vazia explícita) -> devolve `[]`.
    - Teste 4 (defesa contra drift): `cardio_modalidades` contém um nome que NÃO está em
      `CARDIO_MODALIDADES` (dado deformado/desatualizado) -> esse nome é filtrado, não
      propagado (usar `isCardioModalidade` como filtro).
    - Teste 5: erro do Supabase (`error` não nulo na resposta) -> a função relança o erro
      (nunca engole como lista vazia).
  </behavior>
  <action>
    Crie `src/services/cardioModalidadesAceitasRepository.ts`. Header comment explicando que
    esta é uma capability NOVA (RESEARCH.md confirma que hoje só existe caminho de ESCRITA para
    `cardio_modalidades`, nunca leitura fora do onboarding) e documentando a DECISÃO desta plan
    sobre o fallback vazio: leitura estrita de D-02 ("só as modalidades aceitas") — ausência de
    declaração NUNCA vira "aceita tudo" nesta tela, ao contrário do comentário da migration
    0021 sobre o GERADOR de plano (contextos diferentes: gerar plano vs. oferecer troca em
    sessão). Exporte `getModalidadesAceitas(userId: string): Promise<readonly
    CardioModalidade[]>` — `supabase.from('questionario_usuario').select('cardio_modalidades').eq('usuario_id',
    userId).maybeSingle()`; `if (error) throw error;`; `const lista = data?.cardio_modalidades;
    if (!Array.isArray(lista)) return [];`; `return lista.filter(isCardioModalidade);`. Sem
    `null` no tipo de retorno — o CHAMADOR (Plano 03-03, `ActiveSessionScreen.tsx`) é quem
    distingue "carregando" (`useState<CardioModalidade[] | null>(null)`) de "resolvido vazio"
    (`[]`), não este repositório.
  </action>
  <verify>
    <automated>npx jest __tests__/cardioModalidadesAceitas.test.ts -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/cardioModalidadesAceitas.test.ts -q` verde (5 testes); `npx tsc --noEmit`
    sem erros novos; `grep -n "cardio_modalidades" src/services/cardioModalidadesAceitasRepository.ts`
    confirma a leitura da coluna correta.
  </acceptance_criteria>
  <done>getModalidadesAceitas existe, testado, nunca inventa aceitação quando a lista está
  vazia/ausente, e filtra nomes fora do catálogo.</done>
  <reversibility rating="reversible">Arquivo novo, sem efeito em nenhum outro módulo até o
  Plano 03-03 importá-lo.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Cliente → `swapSessionExercise`/RPC | O repositório envia `toModality` como veio do chamador; a validação de vocabulário fechado É da RPC (migration 0034, Plano 03-01) — este código nunca deve ser a ÚNICA barreira. |
| Servidor → cliente (retomada) | `getOpenSessionLog`/`exerciseSwaps` processa dado do servidor — `isCardioModalidade` descarta (não coage) uma linha com `to_modality` fora do catálogo conhecido, para nunca propagar um valor não tipado para a UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-05 | Tampering | `swapExercise` chamado com `toModality` fora do union `CardioModalidade` (bypass de tipo em runtime, ex. JS puro sem TS) | medium | mitigate | A validação REAL é a RPC (T-03-03 no Plano 03-01, CHECK constraint); aqui o TypeScript une o union type e `isCardioModalidade` na leitura de volta do servidor evita propagar um valor não reconhecido para o draft. |
| T-03-06 | Information Disclosure | `getModalidadesAceitas` lida por outro usuário via `userId` arbitrário | low | accept | RLS "questionario select own" (`auth.uid() = usuario_id`, migration 0008) já impede — o parâmetro `userId` só serve para o filtro `.eq()`, a policy do banco é quem decide o que retorna independente do valor passado. |
</threat_model>

<verification>
- `npx jest __tests__/cardioSwap.test.ts __tests__/cardioSwapFluxo.test.ts __tests__/cardioModalidadesAceitas.test.ts -q` verde.
- `npx jest __tests__/recusaDeclaradaFluxo.test.ts -q` continua verde (regressão).
- `npx tsc --noEmit` sem erros novos.
- D-07 (avaliação por TEMPO, independente de modalidade) não exige NENHUMA mudança de código
  nesta plan — `computeCardioOutcome` (`sessionModel.ts:324-334`) já é cego à identidade do
  exercício. A prova de regressão é `npx jest __tests__/cardioTempoDistancia.test.ts -t
  "outcome de cardio"` continuar verde sem edição: se este plano tocasse
  `computeCardioOutcome` por engano, esse describe já existente pegaria a regressão.
</verification>

<success_criteria>
Prova, com teste automatizado real (não manual), o caminho completo motor→servidor→draft→retomada
de uma troca de modalidade para um exercício único, satisfazendo D-01, D-04 e a leitura estrita
de D-02 — base sobre a qual os Planos 03-03/03-04/03-05 constroem a UI e a visibilidade.
</success_criteria>

<output>
Criar `.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-02-SUMMARY.md` ao final.
</output>
