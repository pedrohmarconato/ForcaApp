---
phase: 03-interc-mbio-de-modalidade-de-cardio
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - src/components/session/SwapModalitySheet.tsx
  - src/components/session/SessionQueue.tsx
  - src/screens/ActiveSessionScreen.tsx
  - __tests__/swapModalitySheet.test.tsx
  - __tests__/activeSessionScreen.test.tsx
  - __tests__/cardioTempoDistancia.test.ts
autonomous: true
requirements: [REQ-06]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Um exercício de cardio da fila da sessão (isTimeBased(metricOf(ex)) === true) oferece um botão 'Trocar modalidade' ao lado de 'Não vou fazer' — satisfaz o Success Criterion 1 do ROADMAP da Fase 3 para o entry point 1."
    - "SwapModalitySheet lista SÓ as modalidades de getModalidadesAceitas — lista vazia mostra estado vazio explícito, nunca as 9 modalidades do catálogo (D-02)."
    - "Confirmar a troca chama activeSessionStore.swapExercise; falha do servidor mostra alerta e NÃO fecha o sheet nem aplica a troca na tela (mesmo padrão de onConfirmarRecusa)."
    - "Após a troca, a fila da sessão ativa mostra 'Trocado de X' sob o nome do novo exercício — metade 'sessão ativa' de D-08."
  artifacts:
    - src/components/session/SwapModalitySheet.tsx
  key_links:
    - "SessionQueue (botão Trocar modalidade) -> ActiveSessionScreen (estado troca + modalContent) -> SwapModalitySheet -> activeSessionStore.swapExercise (Plano 03-02) -> RPC swap_session_exercise (Plano 03-01)."
    - "ActiveSessionScreen -> cardioModalidadesAceitasRepository.getModalidadesAceitas (Plano 03-02) -> SwapModalitySheet (prop modalidades)."
---

<objective>
Entry point 1 do REQ-06 (ROADMAP Success Criterion 1): o exercício de cardio da sessão oferece
"trocar modalidade" na fila, listando só as modalidades aceitas do usuário, e a troca fica
visível na sessão ativa ("Trocado de X" — metade de D-08; a metade histórico é o Plano 03-05).

Purpose: RESEARCH.md (Pattern 1) mapeia este entry point para a FILA (`SessionQueue.tsx`), ao
lado do botão "Não vou fazer" já existente — mesmo padrão de UI, mesma tela dona do sheet
(`ActiveSessionScreen.tsx`). Este plano consome o motor/store provados no Plano 03-02
(tracer) e adiciona só a camada de apresentação: um sheet novo (molde `SkipReasonSheet`) e a
fiação de estado que a tela já usa para o fluxo de recusa.
Output: `SwapModalitySheet.tsx` novo, `SessionQueue.tsx` com o botão e o rótulo pós-troca,
`ActiveSessionScreen.tsx` fiando os dois.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-CONTEXT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-RESEARCH.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-PATTERNS.md
@src/components/session/SkipReasonSheet.tsx
@src/components/session/SessionQueue.tsx
@src/screens/ActiveSessionScreen.tsx
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-02-PLAN.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `src/components/session/SwapModalitySheet.tsx` (novo) — sheet de escolha de modalidade
- `SessionQueue.tsx`: prop `onSolicitarTroca` (novo), botão "Trocar modalidade" (novo)
- `ActiveSessionScreen.tsx`: estado `troca`/`trocaBusy`/`modalidadesAceitas`/`modalidadesAceitasErro` (novo), `onConfirmarTroca` (novo)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SwapModalitySheet — sheet de escolha de modalidade</name>
  <files>src/components/session/SwapModalitySheet.tsx</files>
  <read_first>
    src/components/session/SkipReasonSheet.tsx (INTEIRO, já em contexto — molde exato: Modal
    nativo + `inline` prop, `ScrollView` com lista fechada radio-select, `TextField` de nota
    opcional, `Button` de confirmação com guarda `busy || <nada selecionado>`,
    `TouchableOpacity` de "Voltar"); src/components/progress/CardioPrescritoSection.tsx linhas
    56-81 (padrão de 3 estados: `erro` -> `Notice` com ação "Tentar novamente"; `=== null` ->
    `Skeleton`; resolvido vazio -> `EmptyState` — replicar para `modalidades === null`
    (carregando) vs `modalidades.length === 0` (vazio) vs lista com itens); src/components/ui/Feedback.tsx
    linhas 166-192 (`EmptyStateProps`/`EmptyState` — `icon`, `title`, `description`, `action`
    opcionais) e a seção de `Skeleton`/`Notice` (para replicar os mesmos 3 estados);
    src/constants/cardioModalidades.ts (tipo `CardioModalidade`).
  </read_first>
  <behavior>
    - Teste 1 (carregando): `modalidades={null}` -> renderiza um `Skeleton` (ou indicador
      equivalente), nenhum item de lista, botão de confirmar ausente/desabilitado.
    - Teste 2 (vazio, D-02): `modalidades={[]}` -> renderiza `EmptyState` com título citando
      "Nenhuma modalidade cadastrada" (ou texto equivalente), NENHUM item de
      `CARDIO_MODALIDADES` aparece na árvore renderizada.
    - Teste 3 (erro): `erro={true}` -> renderiza `Notice` tone="danger" com ação de
      `onRecarregar`.
    - Teste 4 (lista fechada): `modalidades={['Corrida', 'Remo Ergômetro', 'Pular Corda']}`,
      `exercicioAtualNome="Caminhada"` -> exatamente 3 itens selecionáveis, cada um com
      `testID="swap-modality-<nome>"` e `accessibilityRole="radio"`.
    - Teste 5 (exclui a modalidade atual): `modalidades={['Corrida', 'Caminhada']}`,
      `exercicioAtualNome="Caminhada"` -> só "Corrida" aparece como opção selecionável
      (evita troca-para-si-mesma).
    - Teste 6 (confirmar): selecionar "Remo Ergômetro", pressionar o botão de confirmação ->
      `onConfirm('Remo Ergômetro')` é chamado com a modalidade escolhida.
    - Teste 7 (busy trava): `busy={true}` -> botão de confirmação desabilitado mesmo com
      seleção feita; reabrir com `visible` alternando reseta a seleção (mesmo `useEffect` de
      `SkipReasonSheet`).
  </behavior>
  <action>
    Crie `src/components/session/SwapModalitySheet.tsx`, mesma estrutura de
    `SkipReasonSheet.tsx` (Modal nativo/prop `inline`, `Pressable` de backdrop, `View` de card
    com handle, kicker, title, description) — SEM o campo de nota opcional: a assinatura de
    `activeSessionStore.swapExercise` (Plano 03-02) é `(exerciseId, toModality)`, sem nota, e
    `swapSessionExercise`/a RPC sempre recebem `note: null` a partir do store — não há onde uma
    nota digitada aqui chegaria, então este sheet não a coleta (evita um campo que pareceria
    funcionar mas seria descartado em silêncio). Props: `{ visible: boolean;
    exercicioAtualNome: string; modalidades: readonly CardioModalidade[] | null; erro?:
    boolean; onRecarregar?: () => void; onConfirm: (toModality: CardioModalidade) => void;
    onDismiss: () => void; busy?: boolean; inline?: boolean; }`. Título: `Trocar
    ${exercicioAtualNome}`. Descrição: explica que a duração-alvo é preservada e que o registro
    de km some/reaparece conforme a nova modalidade (cite D-01/D-04 em linguagem de usuário,
    sem jargão técnico).

    Corpo condicional (mesma ordem de prioridade de `CardioPrescritoSection.tsx:60-81`): (1)
    `erro` -> `Notice` tone="danger" title="Não foi possível carregar as modalidades" action=
    `<Button label="Tentar novamente" variant="outline" compact onPress={onRecarregar} />`; (2)
    `modalidades === null` -> `Skeleton` (mesma altura aproximada da lista); (3)
    `modalidades.length === 0` -> `EmptyState icon="activity" title="Nenhuma modalidade
    cadastrada" description="Complete a anamnese de cardio no questionário para habilitar a
    troca."` (sem ação de navegação — fora de escopo desta fase abrir o questionário a partir
    daqui); (4) lista fechada: `ScrollView` com `modalidades.filter((m) => m !==
    exercicioAtualNome).map((m) => <TouchableOpacity testID={`swap-modality-${m}`}
    accessibilityRole="radio" ...>)`, mesmo padrão radio-select de `SkipReasonSheet` (estado
    `toModality: CardioModalidade | null`), `Button` de confirmação com `disabled={busy ||
    toModality == null}`, `onPress={() => { if (busy || toModality == null) return;
    onConfirm(toModality); }}`. `useEffect` em `visible` reseta `toModality` (mesmo padrão de
    `SkipReasonSheet`, mesma razão: reabrir nunca herda seleção anterior).
  </action>
  <verify>
    <automated>npx jest __tests__/swapModalitySheet.test.tsx -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/swapModalitySheet.test.tsx -q` verde (7 testes de `<behavior>`, criar
    o arquivo de teste — molde `SkipReasonSheet`/testes de componente com
    `@testing-library/react-native`, `render`/`fireEvent`); `npx tsc --noEmit` sem erros novos.
  </acceptance_criteria>
  <done>SwapModalitySheet existe, cobre os 3 estados de carregamento/vazio/erro e a lista
  fechada excluindo a modalidade atual.</done>
  <reversibility rating="reversible">Componente novo, sem consumidor até a Task 2.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Wire entry point 1 — SessionQueue + ActiveSessionScreen</name>
  <files>src/components/session/SessionQueue.tsx, src/screens/ActiveSessionScreen.tsx, __tests__/activeSessionScreen.test.tsx, __tests__/cardioTempoDistancia.test.ts</files>
  <read_first>
    src/components/session/SessionQueue.tsx (INTEIRO, já em contexto — linhas 27-40 `Props`,
    linhas 64-68 desestruturação, linhas 100-122 botão "Não vou fazer" E a estrutura
    condicional `recusado ? ... : onSolicitarRecusa && !foraDeJogo ? ... : null`, linhas 84-98
    onde `cutNote`/`meta` são exibidos sob o nome do exercício); src/screens/ActiveSessionScreen.tsx
    linhas 1-65 (imports), linhas 95-146 (estado `recusa`/`recusaBusy`/`modalVisible`/
    `modalContent`, `onConfirmarRecusa` INTEIRO), linhas 480-560 (as DUAS renderizações de
    `SkipReasonSheet` — uma fora do Modal de andamento linha 492-499, outra inline dentro dele
    linha 511-520 — e o `SessionQueue` com `onSolicitarRecusa`/`onActivateSet`/`onUnskipExercise`
    linhas 537-557); __tests__/cardioTempoDistancia.test.ts INTEIRO — ESTE é o arquivo certo
    para a maior parte dos testes novos desta task, não `activeSessionScreen.test.tsx`: ele já
    define `detalheComCardio()` (linhas 72-149, exercício "Caminhada" `metric:
    'tempo_distancia'` + "Prancha" `metric: 'tempo'`) e já tem os describes
    `'fila de séries (SessionQueue)'` (linha 433, renderiza lógica de `SessionQueue`/`doneLine`
    contra esse fixture) e `'store: registrar um cardio de verdade'` (linha 276, chama
    `store().startOrResume({ ..., detail: detalheComCardio() })` diretamente contra o store
    real com `sessionExecutionRepository` mockado — SEM renderizar nenhuma tela); note que o
    mock de `sessionExecutionRepository` deste arquivo (topo) HOJE só tem
    `startSessionLog/saveSetLog/finishSessionLog/getOpenSessionLog/getLastLoadByExercise` — vai
    precisar de `swapSessionExercise` adicionado; __tests__/activeSessionScreen.test.tsx linhas
    1-210 (mocks + fixture `detail`, linhas 90-161 — ESTE fixture tem só 2 exercícios de FORÇA,
    "Supino Reto" e "Flexão", NENHUM cardio; NÃO adicione um 3º exercício a ele — dezenas de
    asserções já fixas no arquivo, como `/A SEGUIR · EXERCÍCIO 2 DE 2/` na linha 255, quebrariam);
    linhas 163-166 (o mock de `trainingRepository`/`getSessionDetail` — para testar o entry
    point 1 dentro de `ActiveSessionScreen` com um exercício de cardio, importe `getSessionDetail`
    no teste novo e use `mock(getSessionDetail).mockResolvedValueOnce(detalheComCardioLocal)`
    só NAQUELE teste, sem tocar o `detail` module-level); linhas 339-400 (describe "modal 'Ver
    andamento'" — molde exato de como abrir a sessão, abrir o modal de andamento e disparar
    `onSolicitarRecusa`/`SkipReasonSheet`, a replicar para `onSolicitarTroca`/
    `SwapModalitySheet`). .planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-02-PLAN.md
    (assinatura de `swapExercise` e de `getModalidadesAceitas`, já implementadas).
  </read_first>
  <action>
    Em `src/components/session/SessionQueue.tsx`: adicione à `Props` o campo opcional
    `onSolicitarTroca?: (exercise: DraftExercise) => void;` (mesmo comentário de
    `onSolicitarRecusa` adaptado). Desestruture em `SessionQueue`. No bloco condicional de
    ações (linhas 100-122), adicione, DEPOIS do `TouchableOpacity` de "Não vou fazer" (mesmo
    nível, mesmo `styles.acao`), um novo `TouchableOpacity` condicionado a `onSolicitarTroca &&
    !foraDeJogo && isTimeBased(metricOf(ex))` (já há `isTimeBased`/`metricOf` importados no
    arquivo), `onPress={() => onSolicitarTroca(ex)}`, `testID={`swap-${ex.exerciseId}`}`,
    `accessibilityRole="button"`, `accessibilityLabel={`Trocar modalidade de ${ex.name}`}`,
    `<Text style={styles.acaoLabel}>Trocar modalidade</Text>`. No bloco onde `cutNote`/`meta`
    são decididos (linhas 84-98), adicione um novo ramo ANTES de `meta ? ... : null` (depois de
    `recusado`/`cutByReplan`, que têm prioridade por serem estados mais terminais):
    `ex.swappedFrom ? <Text style={styles.cutNote}>{`Trocado de ${ex.swappedFrom}.`}</Text> :
    meta ? ...`.

    Em `src/screens/ActiveSessionScreen.tsx`: importe `SwapModalitySheet` de
    `../components/session/SwapModalitySheet`, `getModalidadesAceitas` de
    `../services/cardioModalidadesAceitasRepository`, `type CardioModalidade` de
    `../constants/cardioModalidades`, `swapExercise` do store (`useActiveSessionStore((s) =>
    s.swapExercise)`). Adicione estado `troca` (`{ exerciseId: string; nome: string } | null`,
    mesma forma de `recusa` mas sem variante "sessao" — troca é sempre por exercício),
    `trocaBusy` (boolean), `modalidadesAceitas` (`CardioModalidade[] | null`, inicial `null`),
    `modalidadesAceitasErro` (boolean, inicial `false`). Adicione
    `carregarModalidadesAceitas` (`useCallback` assíncrono: `if (!user) return;
    setModalidadesAceitasErro(false); try { setModalidadesAceitas(await
    getModalidadesAceitas(user.id)); } catch (e) { setModalidadesAceitasErro(true); }`, deps
    `[user]`). Estenda o union de `modalContent` para incluir `'swap_modality'`. Adicione
    `onConfirmarTroca` (`useCallback` recebendo `(toModality: CardioModalidade)`, mesma forma
    de `onConfirmarRecusa`: guarda `!troca || trocaBusy`, `setTrocaBusy(true)`, `const ok =
    await swapExercise(troca.exerciseId, toModality)` — chamada de 2 argumentos, casando
    exatamente com a assinatura `(exerciseId, toModality)` do store (Plano 03-02) e com
    `onConfirm: (toModality: CardioModalidade) => void` do `SwapModalitySheet` (Task 1) —, se
    `!ok` -> `Alert.alert('Não foi possível trocar', saveError ?? 'Tente novamente.')` e
    `return` (sem fechar o sheet), senão `setTroca(null); setModalContent('queue');
    setModalVisible(false);`, `finally { setTrocaBusy(false); }`).

    Renderize `SwapModalitySheet` nos DOIS pontos onde `SkipReasonSheet` já aparece (linhas
    492-499 e 511-520): (a) fora do Modal de andamento, mesma forma de `visible={troca != null
    && modalContent !== 'swap_modality'}`, `exercicioAtualNome={troca?.nome ?? ''}`,
    `modalidades={modalidadesAceitas}`, `erro={modalidadesAceitasErro}`,
    `onRecarregar={carregarModalidadesAceitas}`, `busy={trocaBusy}`,
    `onConfirm={onConfirmarTroca}`, `onDismiss={() => setTroca(null)}`; (b) inline dentro do
    Modal de andamento, condicionado a `modalContent === 'swap_modality' && troca != null`,
    mesmas props + `inline visible`. No `SessionQueue` renderizado dentro do Modal de andamento
    (linhas 537-557), adicione `onSolicitarTroca={(ex: DraftExercise) => { setTroca({
    exerciseId: ex.exerciseId, nome: ex.name }); setModalContent('swap_modality'); if
    (modalidadesAceitas == null && !modalidadesAceitasErro) void
    carregarModalidadesAceitas(); }}` (busca lazy: só na primeira vez que o aluno pede a
    troca, não em toda sessão com cardio).

    Testes — distribua em TRÊS lugares, cada um no arquivo mais natural para o que testa (NÃO
    crie um 4º arquivo novo):

    (1) Em `__tests__/cardioTempoDistancia.test.ts`: adicione ao factory de
    `jest.mock('../src/services/sessionExecutionRepository', ...)` (topo do arquivo) a entrada
    `swapSessionExercise: jest.fn(async (params: any) => ({ plannedExerciseId:
    params.plannedExerciseId, toModality: params.toModality, note: null })),`. No describe
    `'store: registrar um cardio de verdade'` (linha 276), adicione um NOVO describe
    `'store: trocar modalidade de cardio'` com o MESMO `beforeEach` (`startOrResume` com
    `detalheComCardio()`), testando `store().swapExercise('ex-cardio', 'Remo Ergômetro')`
    diretamente: (a) `swapSessionExercise` chamado com `plannedExerciseId: 'ex-cardio'`,
    `toModality: 'Remo Ergômetro'`; (b) `store().draft!.exercises[0].name === 'Remo Ergômetro'`
    e `.metric === 'tempo_distancia'` depois. No describe `'fila de séries (SessionQueue)'`
    (linha 433), adicione: (c) renderizar `<SessionQueue draft={draft} metaFor={() => null}
    onSolicitarTroca={jest.fn()} />` (`@testing-library/react-native`, importar `render` se
    ainda não importado no arquivo) com o draft de `detalheComCardio()` -> o botão
    `testID="swap-ex-cardio"` existe (Caminhada é `tempo_distancia`); com a variante
    `musculacao` (linha 501, `metric: 'carga_reps'`) o botão NÃO existe; (d) aplicar
    `applyCardioSwapToDraft` ao draft antes de renderizar -> a linha mostra
    `/Trocado de Caminhada/`.

    (2) Em `src/components/session/SwapModalitySheet.tsx`'s próprio teste (Task 1 desta plan)
    já cobre o sheet isoladamente — não duplique aqui.

    (3) Em `__tests__/activeSessionScreen.test.tsx`: adicione `swapSessionExercise: jest.fn(async
    () => undefined),` ao factory de `sessionExecutionRepository` (mesmo padrão de
    `skipSessionExercise`, linha ~44), e um NOVO `jest.mock('../src/services/cardioModalidadesAceitasRepository',
    () => ({ getModalidadesAceitas: jest.fn(async () => ['Remo Ergômetro']) }))` no topo do
    arquivo — sem isso, `ActiveSessionScreen` importaria o módulo real, que importa o cliente
    Supabase real, e o teste quebraria no import. Importe `getSessionDetail` de
    `'../src/services/trainingRepository'` (mock já existe no arquivo). Adicione UM novo
    `it('entry point 1: abre o sheet de troca a partir da fila, confirma e fecha')` dentro (ou
    ao lado) do describe `'modal "Ver andamento"'` (linha 339) que: usa
    `mock(getSessionDetail).mockResolvedValueOnce(<um objeto SessionDetail local com 1
    exercício de cardio, mesmo shape mínimo de detalheComCardio() em cardioTempoDistancia.test.ts
    — NÃO importe o `detail` module-level nem o modifique>)`, renderiza a tela, abre "Ver
    andamento", pressiona `swap-<exerciseId>`, seleciona uma modalidade no
    `SwapModalitySheet` inline, pressiona confirmar, e afirma `swapSessionExercise` foi chamado
    E que o sheet fecha (volta para `'Andamento do treino'` ou fecha o modal, mesmo padrão de
    asserção do teste de recusa na linha 396-398). Adicione um segundo `it` para o caminho de
    falha: `mock(swapSessionExercise).mockRejectedValueOnce(new Error('falhou'))` -> após
    confirmar, o `Alert.alert` é chamado (mockar `Alert.alert` se ainda não estiver, mesmo
    padrão de `LoginScreen.test.tsx`) e o sheet PERMANECE aberto (não some da árvore).
  </action>
  <verify>
    <automated>npx jest __tests__/activeSessionScreen.test.tsx __tests__/cardioTempoDistancia.test.ts -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/activeSessionScreen.test.tsx __tests__/cardioTempoDistancia.test.ts -q`
    verde; `npx tsc --noEmit` sem erros novos; `grep -n "swap-" src/components/session/SessionQueue.tsx`
    mostra o `testID` do botão novo; `grep -n "SwapModalitySheet" src/screens/ActiveSessionScreen.tsx`
    mostra as DUAS renderizações (fora e dentro do Modal de andamento).
  </acceptance_criteria>
  <done>O exercício de cardio da fila oferece "Trocar modalidade"; confirmar chama swapExercise
  do store; falha não fecha o sheet; a fila mostra "Trocado de X" após a troca.</done>
  <reversibility rating="reversible">UI + fiação de estado local, revertível via git; nenhuma
  mudança de schema.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Seleção do usuário (lista fechada) → `swapExercise` | `toModality` só pode vir de um item renderizado a partir de `modalidades` (já filtrada por `getModalidadesAceitas`/catálogo) — não há campo de texto livre para modalidade nesta UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-07 | Tampering | UI oferece modalidade fora da lista aceita por bug de filtro | low | accept | A validação REAL (defesa em profundidade) é a RPC (T-03-03/T-03-04, migration 0034) — mesmo se este componente tivesse um bug de filtro, o servidor rejeita `to_modality` fora do catálogo com 22023, e a UI já trata esse erro via `Alert.alert` sem aplicar a troca. |
</threat_model>

<verification>
- `npx jest __tests__/swapModalitySheet.test.tsx __tests__/activeSessionScreen.test.tsx __tests__/cardioTempoDistancia.test.ts -q` verde.
- `npx tsc --noEmit` sem erros novos.
</verification>

<success_criteria>
Satisfaz o Success Criterion 1 do ROADMAP da Fase 3 para o entry point da fila, e a metade
"sessão ativa" de D-08 (rótulo "Trocado de X" visível na fila enquanto a sessão está em
andamento).
</success_criteria>

<output>
Criar `.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-03-SUMMARY.md` ao final.
</output>
