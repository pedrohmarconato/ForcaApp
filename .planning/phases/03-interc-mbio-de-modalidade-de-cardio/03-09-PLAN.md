---
phase: 03-interc-mbio-de-modalidade-de-cardio
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/session/SessionQueue.tsx
  - src/screens/ActiveSessionScreen.tsx
  - __tests__/activeSessionScreen.test.tsx
autonomous: true
requirements: [REQ-06]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "O botão 'Trocar modalidade' da fila (SessionQueue.tsx) some quando qualquer série do exercício já está status: 'done' — mesmo predicado do guard de CR-01 em activeSessionStore.swapExercise (`alvo.sets.some(s => s.status === 'done')`), provado por teste de integração de componente contra a tela real."
    - "A CTA 'Trocar modalidade em vez de recusar' dentro de SkipReasonSheet (ramo sem_equipamento) também some nas mesmas condições, e o botão de confirmação volta ao rótulo padrão 'Não vou fazer' em vez de 'Recusar mesmo assim' — o aluno não caminha mais até uma recusa do servidor que ele não esperava (03-UAT.md teste 5, caveat)."
    - "O botão 'Não vou fazer' continua visível e funcional nas mesmas condições de antes — esta plan esconde só o CAMINHO até a troca, nunca o caminho até a recusa declarada."
  artifacts:
    - src/components/session/SessionQueue.tsx
    - src/screens/ActiveSessionScreen.tsx
  key_links:
    - "DraftExercise.sets[].status === 'done' -> condição de exibição de 'Trocar modalidade' (SessionQueue) e de recusaEhCardio (ActiveSessionScreen) -> SwapModalitySheet nunca mais abre a partir de um exercício com série concluída, fechando o beco sem saída que o 03-UAT.md teste 5 (caveat) registrou."
  prohibitions:
    - "O guard client-side de CR-01 em activeSessionStore.swapExercise (src/store/activeSessionStore.ts:1518-1521) não é alterado nem duplicado nesta plan — esta plan só esconde o CAMINHO até a recusa antes de o aluno chegar lá, não reimplementa a recusa em si."
    - "Nenhuma mudança nesta plan toca src/components/session/SwapModalitySheet.tsx nem a distinção de estado vazio CR-02 (`opcoes.length === 0` vs `modalidades.length === 0`) — fora de escopo desta plan, comportamento preservado bit a bit."
---

<objective>
Fecha o dead-end de UX registrado no 03-UAT.md teste 5 (`caveat`, não bloqueador): o botão
"Trocar modalidade" CONTINUA visível depois de uma série concluída e o seletor ABRE
normalmente, sem aviso — a recusa só aparece ao confirmar ("Não é possível trocar a
modalidade depois de uma série concluída."). Interação sem saída: o aluno entra num fluxo que
já sabe, de antemão (o mesmo predicado que o guard de `activeSessionStore.swapExercise` já
calcula), que vai terminar em recusa.

Purpose: decisão do dono (owner_decisions, "IN — UX") — fechar isso coerentemente com o guard
de CR-01, escondendo o caminho ANTES de o aluno entrar nele, em vez de deixá-lo descobrir a
recusa só ao confirmar. `exercicioForaDeJogo` (usado hoje para esconder "Trocar
modalidade"/"Não vou fazer" quando o exercício foi recusado ou cortado por replanejamento)
NÃO considera séries concluídas — confirmado por leitura direta de
`src/engine/sessionModel.ts:554-556`, que é exatamente por que o botão sobrevive à conclusão
de uma série hoje. Esta plan reaproveita o MESMO predicado do guard client-side de CR-01
(`sets.some(s => s.status === 'done')`) nos DOIS pontos de entrada da troca, sem tocar
`exercicioForaDeJogo` (que continua correto para o que já protege) nem o guard em si.
Output: os dois entry points da troca (fila e `SkipReasonSheet`) escondem a opção assim que
há uma série concluída no exercício.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-UAT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-CONTEXT.md
@src/components/session/SessionQueue.tsx
@src/screens/ActiveSessionScreen.tsx
@src/store/activeSessionStore.ts
@__tests__/activeSessionScreen.test.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `src/components/session/SessionQueue.tsx`: condição de exibição do botão "Trocar
  modalidade" ganha `&& !ex.sets.some((s) => s.status === 'done')`
- `src/screens/ActiveSessionScreen.tsx`: `recusaEhCardio` ganha `&&
  !recusaExercicio.sets.some((s) => s.status === 'done')`
- `__tests__/activeSessionScreen.test.tsx`: 2 testes novos provando os dois entry points
  escondidos após série concluída
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: Esconde os dois entry points da troca assim que há série concluída</name>
  <files>src/components/session/SessionQueue.tsx, src/screens/ActiveSessionScreen.tsx, __tests__/activeSessionScreen.test.tsx</files>
  <read_first>
    src/components/session/SessionQueue.tsx linhas 62-127, já em contexto — a condição atual
    do botão "Trocar modalidade" (linha 117): `onSolicitarTroca && !foraDeJogo &&
    isTimeBased(metricOf(ex))`, SEM checar `ex.sets`; contraste com o botão "Não vou fazer"
    (linha 106), que também não checa séries concluídas — CORRETO manter assim, recusar uma
    série já feita continua fazendo sentido (o exercício segue nas próximas semanas), só a
    TROCA precisa da guarda nova; src/screens/ActiveSessionScreen.tsx linhas 357-377, já em
    contexto — `recusaEhCardio` (linha 364): `recusaExercicio != null &&
    isTimeBased(metricOf(recusaExercicio))`, mesma lacuna; `isTimeBased`/`metricOf` já
    importados no topo do arquivo (linhas 40-41), nenhum import novo necessário em nenhum dos
    dois arquivos; src/store/activeSessionStore.ts linhas 1503-1521, já em contexto — o guard
    de CR-01 em `swapExercise`: `alvo.sets.some((s) => s.status === 'done')` — ESTE é o
    predicado exato a replicar nos dois pontos de entrada (mesma condição, não uma
    aproximação); src/components/session/SkipReasonSheet.tsx linhas 78-160, já em contexto —
    `ofereceTroca` (linha 82-86) deriva do prop `ehCardio` recebido de fora; quando
    `ehCardio` for `false`, `ofereceTroca` também é `false`, o botão `skip-reason-oferecer-troca`
    não renderiza (linha 149) E o rótulo do botão principal volta a `'Não vou fazer'` (linha
    159, ramo `else`) — ou seja, a correção em `ActiveSessionScreen.tsx` sozinha já resolve o
    comportamento completo dentro de `SkipReasonSheet.tsx`, SEM editar este arquivo;
    __tests__/activeSessionScreen.test.tsx linhas 187-273 (fixture `detailComCardio`, um único
    exercício `ex-cardio`/`Caminhada` com UMA série `st-c1`) e linhas 402-411 (`abrirSessao`,
    `modaisVisiveis`) e linhas 464-497 (`it('Android desfaz recusa sem onDismiss manual')` — o
    MOLDE exato de precondicionar o draft via `useActiveSessionStore.setState((s: any) => ({
    draft: { ...s.draft!, exercises: s.draft!.exercises.map(...) } }))` DEPOIS de abrir a
    sessão, técnica já estabelecida neste arquivo para simular estado sem precisar dirigir a
    UI de medição do SessionPlayer) e linhas 500-528 (entry point 1) e 530-564 (entry point 2)
    — os testes existentes que os novos precisam conviver com, sem editar nenhuma asserção.
  </read_first>
  <behavior>
    Teste novo 1 (entry point 1, fila): com `detailComCardio` (exercício `ex-cardio`, série
    `st-c1`), após `abrirSessao`, `setState` marca `exercises[0].sets[0].status = 'done'` (e
    `actualDurationSeconds`/`actualDistanceM` preenchidos, para o fixture ser realista) — abrir
    "Ver andamento" e `screen.queryByLabelText('Trocar modalidade de Caminhada')` é `null`;
    `screen.getByLabelText('Não vou fazer Caminhada')` continua presente (a recusa não foi
    escondida, só a troca).
    Teste novo 2 (entry point 2, SkipReasonSheet): mesma precondição de série `done`; pressiona
    "Não vou fazer Caminhada" (abre o sheet de recusa), seleciona "Equipamento indisponível"
    (`skip-reason-sem_equipamento`) — `screen.queryByTestId('skip-reason-oferecer-troca')` é
    `null`, e o botão de confirmação mostra o texto `'Não vou fazer'` (não `'Recusar mesmo
    assim'`).
  </behavior>
  <action>
    Em `src/components/session/SessionQueue.tsx`, linha 117: troque a condição
    `onSolicitarTroca && !foraDeJogo && isTimeBased(metricOf(ex))` por
    `onSolicitarTroca && !foraDeJogo && isTimeBased(metricOf(ex)) &&
    !ex.sets.some((s) => s.status === 'done')` — mesmo predicado do guard de CR-01 em
    `activeSessionStore.ts:1518`, sem introduzir uma segunda definição do que "já tem série
    feita" significa. NÃO toque a condição do botão "Não vou fazer" (linha 106) nem
    `exercicioForaDeJogo` (`sessionModel.ts:554-556`) — permanecem exatamente como estão.

    Em `src/screens/ActiveSessionScreen.tsx`, linha 364: troque `const recusaEhCardio =
    recusaExercicio != null && isTimeBased(metricOf(recusaExercicio));` por `const
    recusaEhCardio = recusaExercicio != null && isTimeBased(metricOf(recusaExercicio))
    && !recusaExercicio.sets.some((s) => s.status === 'done');` — mesmo predicado,
    mesma fonte de verdade (`DraftExercise.sets`). Como `SkipReasonSheet.ofereceTroca` (linha
    82-86 daquele arquivo) já deriva inteiramente do prop `ehCardio` recebido, nenhuma edição
    é necessária em `SkipReasonSheet.tsx`: com `recusaEhCardio=false`, o componente já cai
    sozinho no ramo "sem oferta de troca" (CTA ausente, rótulo do botão principal volta a
    `'Não vou fazer'`/`'Recusar o treino'`) — comportamento herdado, não reimplementado.

    Em `__tests__/activeSessionScreen.test.tsx`, adicione os 2 testes descritos em
    `<behavior>` dentro (ou logo após) do bloco que já cobre os entry points de troca
    (linhas 500-564) — reuse `detailComCardio`, `abrirSessao`-equivalente inline (mesmo padrão
    de setup das linhas 500-528) e o padrão de `useActiveSessionStore.setState((s: any) =>
    ({...}))` das linhas 472-486 para marcar a série como `done` sem precisar dirigir a UI de
    medição do `SessionPlayer`.
  </action>
  <verify>
    <automated>npx jest __tests__/activeSessionScreen.test.tsx -q && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/activeSessionScreen.test.tsx -q` verde INTEIRO (os testes 500-528 e
    530-564 pré-existentes continuam passando SEM edição de asserção, mais os 2 novos); `npx
    tsc --noEmit` sem erros novos; `grep -n "isTimeBased(metricOf(ex))" src/components/session/SessionQueue.tsx`
    mostra a condição estendida com `!ex.sets.some`; `grep -n "recusaEhCardio ="
    src/screens/ActiveSessionScreen.tsx` mostra a condição estendida com
    `!recusaExercicio.sets.some`.
  </acceptance_criteria>
  <done>Os dois entry points da troca (fila e SkipReasonSheet) somem assim que qualquer série
  do exercício já está concluída — mesmo predicado do guard de CR-01, sem duplicar a lógica de
  recusa em si; "Não vou fazer" continua disponível nas mesmas condições de antes.</done>
  <reversibility rating="reversible">Mudança de condição de renderização client-side,
  revertível via git; nenhuma mudança de contrato de API, nenhuma migration.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Nenhuma nova | Esta plan só altera QUANDO um botão client-side é renderizado; não abre nem estreita nenhuma fronteira de confiança — a autorização real continua vivendo no servidor (guard client-side de CR-01 hoje, migration 0036/Plano 03-08 quando aplicada). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-13 | Tampering | Um bypass do gate client-side desta plan (ex.: JS desabilitado, build alterado) ainda alcançaria o guard de `activeSessionStore.swapExercise` (CR-01) e, uma vez aplicada, a migration 0036 (Plano 03-08) no servidor | low | accept | Esta plan é puramente cosmética/UX — a autorização de verdade nunca dependeu dela; um bypass desta camada cai exatamente no mesmo comportamento (recusa) que já existe hoje, só sem a melhoria de UX. Nenhum risco novo é introduzido. |
</threat_model>

<verification>
- `npx jest __tests__/activeSessionScreen.test.tsx -q` verde.
- `npx tsc --noEmit` sem erros novos.
- Os testes pré-existentes de entry point 1/2 (linhas 500-564) passam sem edição de asserção.
</verification>

<success_criteria>
Fecha o dead-end de UX do 03-UAT.md teste 5 (caveat): o aluno não entra mais no seletor de
troca quando o resultado já é conhecido de antemão como recusa — coerente com o guard de
CR-01, sem duplicar sua lógica.
</success_criteria>

## Deferred / Known Risks

A UAT frontmatter registra que as migrations concedem `execute` em 58 funções, mas nenhum
`grant select/insert/update/delete` em tabela nenhuma para o papel `authenticated`; staging e
produção funcionam só por terem sido criadas sob o comportamento legado do Supabase, e um
projeto NOVO criado a partir destas migrations sobe quebrado com "permission denied for table
planned_sessions". Isso afeta as 36 migrations inteiras, não só a Fase 3. **O dono excluiu
isso explicitamente desta rodada de fechamento de gaps** — não há task para isso em nenhuma
das plans 03-07/03-08/03-09; fica registrado aqui para não se perder, candidato a uma fase
futura dedicada a hardening de grants.

<output>
Criar `.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-09-SUMMARY.md` ao final.
</output>
