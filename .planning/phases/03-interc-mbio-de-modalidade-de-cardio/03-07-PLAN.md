---
phase: 03-interc-mbio-de-modalidade-de-cardio
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/sessionExecutionRepository.ts
  - __tests__/sessionExecutionRepository.test.ts
  - __tests__/integration/getSessionLogDetail.postgrest.test.ts
  - jest.integration.config.js
  - package.json
autonomous: true
requirements: [REQ-06]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "getSessionLogDetail lê planned_sets.exercise_id (não planned_exercise_id, coluna inexistente na tabela) no select e no consumo das linhas de set_logs — as 9 suítes pré-existentes do describe('getSessionLogDetail') em __tests__/sessionExecutionRepository.test.ts continuam verdes após a correção, sem edição de asserção."
    - "Existe em disco um harness de integração novo (__tests__/integration/getSessionLogDetail.postgrest.test.ts) que importa e chama a função REAL getSessionLogDetail de src/services/sessionExecutionRepository (não uma réplica local), sem mockar o cliente supabase; é excluído da suíte padrão por testPathIgnorePatterns e roda só por `npm run test:integration:pg` — satisfaz a decisão OD-02 do dono. Afirmação ESTÁTICA, verificável por leitura do arquivo; o resultado de EXECUTAR o harness contra Postgres real é a truth de backstop seguinte, deliberadamente não misturada aqui."
    - statement: "Rodado localmente contra o stack Supabase real (OrbStack/Docker, via `npm run test:integration:pg`), getSessionLogDetail devolve o detalhe da sessão sem lançar o erro 42703 de coluna ausente que o 03-UAT.md teste 3 reproduziu contra PostgREST."
      verification: backstop
  artifacts:
    - src/services/sessionExecutionRepository.ts
    - __tests__/sessionExecutionRepository.test.ts
    - __tests__/integration/getSessionLogDetail.postgrest.test.ts
    - jest.integration.config.js
  key_links:
    - "sessionExecutionRepository.getSessionLogDetail -> planned_sets.exercise_id (coluna real, migration 0001) -> PostgREST -> SessionHistoryDetailScreen — o link que G-03-3 quebrou (commit 55aab43, plano 03-05) volta a existir de ponta a ponta, provado por um teste que fala com Postgres de verdade, não só com um mock que nunca validou o nome da coluna."
    - "package.json testPathIgnorePatterns -> __tests__/integration/ excluído da suíte padrão (`npm test`/`jest`) -> jest.integration.config.js roda SÓ esses arquivos via `npm run test:integration:pg` — o projeto continua sem CI (PROJECT.md) e o harness novo nunca entra na verificação local padrão sem ser chamado explicitamente."
  prohibitions:
    - "Nenhuma referência a `planned_sets.planned_exercise_id` (coluna que não existe nessa tabela) volta a aparecer em src/ depois desta plan — grep de regressão confirma 0 ocorrências do padrão `planned_sets(set_order, planned_exercise_id` em todo o diretório src/. As colunas planned_exercise_id de exercise_skips e de cardio_exercise_swaps são LEGÍTIMAS (colunas reais dessas duas tabelas, migrations 0020/0034) e não são tocadas por esta prohibition."
---

<objective>
Fecha G-03-3 (BLOCKER, 03-UAT.md teste 3): `getSessionLogDetail` seleciona
`planned_sets(set_order, planned_exercise_id, planned_exercises(...))` em
`src/services/sessionExecutionRepository.ts:891`, mas a tabela `planned_sets`
(migration `0001_modelo_treino.sql:89-98`) não tem coluna `planned_exercise_id`
— a coluna real é `exercise_id`. PostgREST devolve `42703` reproduzido ao vivo
no 03-UAT.md; o detalhe do histórico de QUALQUER sessão com séries registradas
quebra com "Não foi possível carregar este treino."

Purpose: os 27 testes de swap desta fase passam com a consulta quebrada porque
`__tests__/sessionExecutionRepository.test.ts` mocka o cliente Supabase — o
nome errado de coluna nunca chega ao PostgREST no mock. Por decisão do dono
(OD-02), este plano fecha as DUAS pontas: corrige o nome da coluna (GREEN) E
cria, ANTES da correção, um teste de integração que fala com um Postgres REAL
(RED primeiro — TDD) para que esta classe inteira de erro (nome de coluna que
não existe) nunca mais fique invisível atrás de um mock.
Output: `getSessionLogDetail` corrigido; harness de integração novo, fora da
suíte padrão, documentado e rodável via `npm run test:integration:pg`; os 3
mocks pré-existentes que reproduziam o nome de coluna errado corrigidos junto.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-UAT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-VERIFICATION.md
@AGENTS.md
@src/services/sessionExecutionRepository.ts
@__tests__/sessionExecutionRepository.test.ts
@supabase/migrations/0001_modelo_treino.sql
@package.json
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `src/services/sessionExecutionRepository.ts`: `getSessionLogDetail` select e leitura de `set_logs.planned_sets` corrigidos para `exercise_id` (era `planned_exercise_id`)
- `__tests__/sessionExecutionRepository.test.ts`: 3 mocks (`planned_sets`) corrigidos para `exercise_id`
- `__tests__/integration/getSessionLogDetail.postgrest.test.ts` (NOVO): harness de integração real contra Postgres local, fora da suíte padrão
- `jest.integration.config.js` (NOVO): config Jest dedicada (ts-jest, `testEnvironment: 'node'`) para o harness de integração
- `package.json`: script `test:integration:pg` novo; `testPathIgnorePatterns` estendido para excluir `__tests__/integration/` da suíte padrão (`jest`/`npm test`)
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED (harness de integração real) -> GREEN (corrige exercise_id em getSessionLogDetail)</name>
  <precondition>Stack Supabase local (OrbStack/Docker) está de pé e alcançável, e SUPABASE_INTEGRATION_SERVICE_ROLE_KEY está exportada no shell (copiada de `supabase status`) — checar com `curl -sf http://127.0.0.1:54321/rest/v1/ -o /dev/null` (ou o host de SUPABASE_INTEGRATION_URL, se sobrescrito) retornando exit 0 antes de semear qualquer fixture.</precondition>
  <files>src/services/sessionExecutionRepository.ts, __tests__/sessionExecutionRepository.test.ts, __tests__/integration/getSessionLogDetail.postgrest.test.ts, jest.integration.config.js, package.json</files>
  <read_first>
    src/services/sessionExecutionRepository.ts linhas 856-945 (`getSessionLogDetail` INTEIRA,
    já em contexto — note especialmente a linha 891: o select de `set_logs` pede
    `planned_sets(set_order, planned_exercise_id, planned_exercises(name, exercise_order,
    metric))`, e a linha 916 (`const plannedExerciseId: string | null =
    l?.planned_sets?.planned_exercise_id ?? null;`) — as DUAS únicas ocorrências do bug,
    confirmadas por grep desta sessão de planejamento: nenhuma outra chamada a
    `planned_sets(...)` em src/ referencia essa coluna); supabase/migrations/0001_modelo_treino.sql
    linhas 89-100 (DDL real de `planned_sets`: colunas `id, exercise_id, set_order,
    target_reps_min, target_reps_max, target_load_kg, target_rir, created_at` — a FK para
    `planned_exercises` chama-se `exercise_id`, NUNCA `planned_exercise_id`; contraste com
    `exercise_skips`/`cardio_exercise_swaps`, migrations 0020/0034, que têm coluna
    `planned_exercise_id` DE VERDADE — essas duas permanecem intocadas, o bug é só dentro do
    embed de `planned_sets`); __tests__/sessionExecutionRepository.test.ts linhas 847-1081
    (describe('getSessionLogDetail') INTEIRO, já em contexto — os mocks de `planned_sets` nos
    testes "Pitfall 2 fechado" linha 988, "D-08 histórico" linha 1029 e "sem troca" linha 1068
    usam `planned_exercise_id: '...'` DENTRO do objeto `planned_sets` — o MESMO nome errado do
    bug de produção, o que é exatamente por que o mock nunca capturou o 42703: o mock aceita
    qualquer nome de propriedade, PostgREST não); src/config/supabaseClient.js INTEIRO (o
    cliente real do app — usa `react-native-url-polyfill/auto` e SecureStore, por isso o
    harness de integração NÃO pode importá-lo direto; precisa mockar o módulo com um cliente
    Node puro antes de importar o repositório); package.json linhas 1-30 (`scripts`) e 107-121
    (bloco `"jest"` — preset `jest-expo`, sem `testPathIgnorePatterns` hoje; é aqui que a nova
    exclusão entra) e a lista de `devDependencies` (linha ~86, `"ts-jest": "^29.1.1"` — já
    instalado, não precisa `npm install`); supabase/config.toml linha 10 (`[api] port =
    54321` — a porta real do stack local, usada como default de SUPABASE_INTEGRATION_URL).
  </read_first>
  <behavior>
    Harness de integração (`__tests__/integration/getSessionLogDetail.postgrest.test.ts`),
    escrito e observado FALHANDO (RED) ANTES de tocar `sessionExecutionRepository.ts`:
    - Teste único: semeia (via cliente `service_role`, bypassando RLS) um usuário real
      (`admin.auth.admin.createUser`), um `training_plans`, um `planned_sessions` e um
      `session_logs` para esse usuário — SEM `planned_exercises`/`planned_sets`/`set_logs`
      (não são necessários: o erro 42703 do select de `set_logs` acontece no PLANEJAMENTO da
      consulta pelo PostgREST, independente de quantas linhas casam com o filtro
      `session_log_id`, exatamente como reproduzido no 03-UAT.md teste 3 — cabeçalho responde
      normal, a segunda consulta estoura). Chama a função REAL `getSessionLogDetail`
      (importada do módulo de produção, nunca uma réplica) com o `session_log_id` semeado.
      RODANDO CONTRA O CÓDIGO AINDA NÃO CORRIGIDO: a promise rejeita com um erro cujo `.code`
      é `'42703'` — comando `npm run test:integration:pg` sai com falha, mensagem do
      PostgREST menciona `planned_sets_1.planned_exercise_id does not exist` (mesmo texto do
      03-UAT.md). Este é o RED — rode e CONFIRME a falha antes de prosseguir.
    - APÓS a correção (GREEN, mesmo teste, sem editar a asserção): a promise resolve;
      `detalhe.sessionLogId` é o id semeado; `detalhe.exercises` é `[]` (nenhum set_log
      semeado — comportamento correto, não erro).
    - `afterAll` deleta o usuário criado (`admin.auth.admin.deleteUser`) — cascade
      (`on delete cascade` em training_plans/planned_sessions/session_logs, migration 0001)
      apaga o resto numa chamada só.
  </behavior>
  <action>
    PRIMEIRO (RED), sem tocar `sessionExecutionRepository.ts`:

    Crie `jest.integration.config.js` na raiz do projeto: `module.exports = { preset:
    'ts-jest', testEnvironment: 'node', rootDir: '.', testMatch:
    ['<rootDir>/__tests__/integration/**/*.test.ts'], testTimeout: 20000 }`. Config
    SEPARADA da suíte padrão (não usa `jest-expo` — este harness é Node puro contra
    `@supabase/supabase-js`, sem RN) e roda SÓ arquivos dentro de `__tests__/integration/`.

    Em `package.json`: adicione ao objeto `"scripts"` a entrada `"test:integration:pg": "jest
    --config jest.integration.config.js"`. No bloco `"jest"` existente (linhas 107-121),
    adicione a chave `"testPathIgnorePatterns": ["/node_modules/",
    "<rootDir>/__tests__/integration/"]` — sem isso, `npm test`/`jest` (suíte padrão,
    sem stack local, sem CI conforme PROJECT.md) tentaria rodar o harness novo e falharia sem
    Postgres disponível; o projeto continua 100% mockado por padrão.

    Crie `__tests__/integration/getSessionLogDetail.postgrest.test.ts`: no topo, um comentário
    de cabeçalho explicando o propósito (G-03-3, OD-02, fora da suíte padrão por decisão do
    dono, como rodar via `npm run test:integration:pg` e como subir o stack: `supabase start`
    seguido de `supabase status` para copiar o `service_role key`). Leia
    `SUPABASE_INTEGRATION_URL` de `process.env` com default `'http://127.0.0.1:54321'` e
    `SUPABASE_INTEGRATION_SERVICE_ROLE_KEY` de `process.env` SEM default. TRAVA DE SEGURANÇA
    (topo do arquivo, roda no import, antes de qualquer `describe`): valide
    `SUPABASE_INTEGRATION_URL` com uma regex que só aceita `http://127.0.0.1` ou
    `http://localhost` (com porta opcional) — se não bater, `throw new Error(...)` imediato
    (nunca rodar este harness, mesmo por engano de env var, contra staging/produção — as duas
    URLs reais estão documentadas em AGENTS.md e NUNCA devem aparecer aqui). Se
    `SUPABASE_INTEGRATION_SERVICE_ROLE_KEY` estiver ausente, `throw new Error(...)` com
    instrução explícita ("rode `supabase start`, copie o service_role key de `supabase
    status`, exporte SUPABASE_INTEGRATION_SERVICE_ROLE_KEY e rode de novo") — NUNCA
    `test.skip`/`describe.skip` silencioso (decisão OD-02: "nunca silenciosamente verde").
    Crie o client admin com `createClient(url, serviceRoleKey, { auth: { autoRefreshToken:
    false, persistSession: false } })`. Chame `jest.mock('../../src/config/supabaseClient',
    () => ({ supabase: admin }))` ANTES do import de
    `getSessionLogDetail` (jest hoisting garante isso automaticamente mesmo com o import no
    topo do arquivo) — importe `getSessionLogDetail` de
    `'../../src/services/sessionExecutionRepository'`. Em `beforeAll`: crie o usuário
    (`admin.auth.admin.createUser({ email: \`pgtest-g033-${Date.now()}@forca.test\`,
    password: 'senha-teste-integracao-123', email_confirm: true })`), depois insira em
    sequência `training_plans` (`user_id`, `name`), `planned_sessions` (`plan_id`, `user_id`,
    `week_number: 1`, `title`) e `session_logs` (`planned_session_id`, `user_id`) via o client
    admin, verificando `error`/dado retornado em cada insert e lançando com mensagem clara se
    algum falhar. Guarde `sessionLogId`. Em `afterAll`, `admin.auth.admin.deleteUser(userId)`.
    UM `it(...)` chamando `getSessionLogDetail(sessionLogId)` e afirmando `not.toBeNull()`,
    `sessionLogId` igual ao semeado, `exercises` igual a `[]`.

    Rode `npm run test:integration:pg` — CONFIRME que falha com um erro cujo `code` é
    `'42703'` (RED). Só então prossiga para a correção.

    AGORA (GREEN) em `src/services/sessionExecutionRepository.ts`: na linha 891, troque
    `planned_sets(set_order, planned_exercise_id, planned_exercises(name, exercise_order,
    metric))` por `planned_sets(set_order, exercise_id, planned_exercises(name,
    exercise_order, metric))`. Na linha 916, troque `l?.planned_sets?.planned_exercise_id ??
    null` por `l?.planned_sets?.exercise_id ?? null`. Logo acima da query de `linhas` (antes
    da linha 888), adicione um comentário curto documentando por que a degradação
    `erroDeColunaAusente` (usada no cabeçalho, linha 882, para `active_seconds` — gap real
    entre prod na 0022 e a 0028) NÃO é estendida a esta segunda query: as colunas novas aqui
    (`actual_duration_seconds`/`actual_distance_m`/`perceived_effort`/`metric`) nasceram na
    migration 0014, muito antes de `exercise_skips`(0020)/`cardio_exercise_swaps`(0034) — que
    este MESMO select já embute nas outras duas queries desta função — logo nenhum ambiente
    real pode ter 0034 sem já ter 0014; a topologia de migrations torna esse gap impossível,
    diferente do caso genuíno de `active_seconds`.

    Em `__tests__/sessionExecutionRepository.test.ts`, troque `planned_exercise_id:` por
    `exercise_id:` DENTRO dos três objetos mock de `planned_sets` (linhas 988, 1029, 1068 —
    NÃO toque `cardio_exercise_swaps: [{ planned_exercise_id: ... }]` na linha 1012, que é
    coluna real e correta daquela tabela).

    Rode `npm run test:integration:pg` de novo — CONFIRME GREEN. Rode a suíte padrão inteira
    e `tsc` (ver `<verify>`).
  </action>
  <verify>
    <automated>npm run test:integration:pg && npx jest __tests__/sessionExecutionRepository.test.ts -q && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `npm run test:integration:pg` verde (o teste novo passou contra Postgres real, não mock);
    `npx jest __tests__/sessionExecutionRepository.test.ts -q` verde INTEIRO — as 9
    pré-existentes do describe `getSessionLogDetail` (incluindo as 3 cujo mock foi editado)
    continuam passando, e nenhuma outra suíte do arquivo regrediu; `npx jest -q` (suíte
    padrão completa) reporta "Tests: X passed" sem nenhuma falha nova (ler o resumo, não o
    exit code isolado, per AGENTS.md linha 70); `npx tsc --noEmit` sem erros novos;
    `grep -rn "planned_sets(set_order, planned_exercise_id" src/` não retorna nenhuma linha;
    `git log -1 --format=%s` do primeiro commit deste task começa com `test(03-07)` (RED) e o
    da correção com `fix(03-07)` (GREEN), duas commits atômicas mínimas.
  </acceptance_criteria>
  <done>getSessionLogDetail lê exercise_id (coluna real de planned_sets); um teste de
  integração real contra Postgres local prova isso fora de mock, escrito e observado
  falhando antes da correção; os 3 mocks pré-existentes que reproduziam o nome errado foram
  corrigidos junto; o harness fica fora da suíte padrão via testPathIgnorePatterns.</done>
  <reversibility rating="reversible">Correção de código + infraestrutura de teste nova,
  revertível via git; nenhuma migration, nenhuma mutação em banco vivo além dos dados
  efêmeros criados/apagados pelo próprio harness dentro do stack LOCAL.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Harness de integração (chave `service_role`) → stack Supabase LOCAL | O harness usa a chave `service_role` (bypassa RLS) para semear/limpar dados de teste — poder elevado, por isso a trava de loopback é obrigatória antes de qualquer chamada de rede. |
| `getSessionLogDetail` (JWT do usuário) → `set_logs`/`planned_sets` | Sem mudança de fronteira: esta plan só corrige o NOME de uma coluna já lida sob RLS "own" existente; nenhuma nova permissão é concedida. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-09 | Information Disclosure / Elevation of Privilege | Harness de integração usando `service_role` sobre a rede | high | mitigate | Trava de loopback hard-fail (regex sobre `SUPABASE_INTEGRATION_URL`, só `127.0.0.1`/`localhost`) roda ANTES de qualquer chamada de rede e lança erro se falhar; a chave `service_role` só é lida de env var, nunca hardcoded/commitada; o harness fica fora de `npm test`/qualquer CI via `testPathIgnorePatterns`, então nunca roda sem invocação explícita e deliberada do desenvolvedor. |
| T-03-10 | Tampering | Reintrodução futura de `planned_sets.planned_exercise_id` (regressão exata deste bug) | medium | mitigate | Grep de regressão nos acceptance criteria (`grep -rn "planned_sets(set_order, planned_exercise_id" src/` deve retornar vazio) MAIS o harness de integração novo, que falha alto (42703) contra Postgres real se o bug voltar — diferente da suíte mockada, que não pega esse erro por construção. |
</threat_model>

<verification>
- `npm run test:integration:pg` verde (Postgres local real).
- `npx jest -q` (suíte padrão completa) verde — ler "Tests: X passed".
- `npx tsc --noEmit` sem erros novos.
- `grep -rn "planned_sets(set_order, planned_exercise_id" src/` vazio.
</verification>

<success_criteria>
Fecha G-03-3: o detalhe do histórico volta a abrir para qualquer sessão com séries
registradas (trocada ou não), e a classe de erro que o causou (nome de coluna inválido
invisível a um mock) ganha cobertura permanente fora da suíte padrão, por decisão explícita
do dono (OD-02).
</success_criteria>

<output>
Criar `.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-07-SUMMARY.md` ao final.
</output>
