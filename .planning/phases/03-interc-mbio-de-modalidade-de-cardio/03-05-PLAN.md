---
phase: 03-interc-mbio-de-modalidade-de-cardio
plan: 05
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - src/services/sessionExecutionRepository.ts
  - src/engine/sessionModel.ts
  - src/screens/SessionHistoryDetailScreen.tsx
  - __tests__/sessionExecutionRepository.test.ts
  - __tests__/sessionHistory.test.tsx
autonomous: true
requirements: [REQ-06]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "getSessionLogDetail agora seleciona actual_duration_seconds, actual_distance_m e perceived_effort — o gap pré-existente (Pitfall 2: cardio no histórico mostrava 'null reps × peso corporal') está fechado, PRÉ-REQUISITO de D-08 conforme 03-CONTEXT.md, não escopo extra."
    - "Quando o exercício tem uma troca registrada em cardio_exercise_swaps, HistoryExercise.name vira a modalidade NOVA e HistoryExercise.swappedFrom carrega o nome ORIGINAL — SessionHistoryDetailScreen exibe 'Trocado de X' — metade 'histórico' de D-08."
    - "Exercício sem troca (swappedFrom null/ausente) e exercício de força continuam se comportando exatamente como antes desta plan — as 4 suítes de teste pré-existentes de getSessionLogDetail e a suíte de SessionHistoryDetailScreen passam sem modificação de asserção."
  artifacts: []
  key_links:
    - "session_logs -> cardio_exercise_swaps (embed PostgREST na MESMA query do cabeçalho de getSessionLogDetail, mesmo padrão de exercise_skips em getOpenSessionLog) -> HistoryExercise.swappedFrom -> SessionHistoryDetailScreen."
    - "sessionModel.formatCardioSetResult (novo, réplica intencional do algoritmo de SessionQueue.doneLine) -> SessionHistoryDetailScreen.descreveSerie — mesma saída para a mesma entrada, provado por teste de paridade (mitiga o Pitfall 5 do RESEARCH.md sem forçar dependência de arquivo com o Plano 03-03)."
  prohibitions:
    - "Nenhuma mudança nesta plan altera o comportamento de exercícios de FORÇA no histórico — descreveSerie só entra no ramo cardio quando isTimeBased(metric) é verdadeiro; o ramo de reps×carga permanece bit-a-bit o mesmo."
---

<objective>
Fecha a metade "histórico" de D-08 e corrige o gap pré-existente confirmado pelo RESEARCH.md
(Pitfall 2): `getSessionLogDetail`/`descreveSerie` nunca liam `actual_duration_seconds`/
`actual_distance_m`, então cardio no histórico já era ilegível ANTES desta fase existir.
03-CONTEXT.md marca esse conserto como pré-requisito de D-08, não como escopo extra — este
plano o entrega junto com o rótulo "trocado de X".

Purpose: sem este plano, D-08 fica pela metade (só "sessão ativa", nunca "histórico") e o bug
pré-existente continua invisível. RESEARCH.md (Pattern 4, Don't Hand-Roll) recomenda expor o
par original↔trocado via LEFT JOIN/embed na MESMA query já usada, não uma consulta paralela —
este plano segue essa recomendação embutindo `cardio_exercise_swaps` na query do CABEÇALHO
(`session_logs`, que já tem a relação de chave estrangeira), preservando a contagem de
consultas e a compatibilidade com os 4 testes de `getSessionLogDetail` já existentes.
Output: `getSessionLogDetail` estendido, `formatCardioSetResult` novo em `sessionModel.ts`
(réplica testada do algoritmo já usado em `SessionQueue.doneLine`), `SessionHistoryDetailScreen`
mostrando cardio corretamente e o rótulo de troca.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-CONTEXT.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-RESEARCH.md
@.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-PATTERNS.md
@src/services/sessionExecutionRepository.ts
@src/screens/SessionHistoryDetailScreen.tsx
@__tests__/sessionExecutionRepository.test.ts
@__tests__/sessionHistory.test.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `sessionExecutionRepository.ts`: `HistorySetLog`/`HistoryExercise`/`SessionLogDetail` estendidos (campos novos opcionais, sem quebrar consumidores existentes), `getSessionLogDetail` estendida
- `sessionModel.ts`: `formatCardioSetResult` (função pura nova)
- `SessionHistoryDetailScreen.tsx`: `descreveSerie` com ramo cardio, seções mostrando "Trocado de X"
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender getSessionLogDetail (Pitfall 2 + D-08 histórico) e formatCardioSetResult</name>
  <files>src/services/sessionExecutionRepository.ts, src/engine/sessionModel.ts, __tests__/sessionExecutionRepository.test.ts</files>
  <read_first>
    src/services/sessionExecutionRepository.ts linhas 747-845 (`HistorySetLog`/`HistoryExercise`/
    `SessionLogDetail`/`getSessionLogDetail` INTEIRA, já em contexto — note especialmente:
    `actualReps: number` NÃO nullable hoje, o `select` de `linhas` na linha 803 não traz
    `actual_duration_seconds`/`actual_distance_m`/`perceived_effort`, e `consultaCabecalho`
    (linhas 779-788) já embute `planned_sessions(title, week_number)` na MESMA query de
    `session_logs` — é o MOLDE exato de onde embutir `cardio_exercise_swaps`); linhas 159-236
    (`getOpenSessionLog` — o embed `exercise_skips(planned_exercise_id, reason, note)` na
    linha 170 é o precedente direto de embutir uma relação to-many via FK numa query de
    `session_logs`, mesma técnica a replicar aqui com `cardio_exercise_swaps`); já em contexto
    também de plans anteriores desta fase: `sessionExecutionRepository.ts` importa
    `CardioModalidade`/`isCardioModalidade` de `../constants/cardioModalidades` desde o Plano
    03-02 — REUSE esse import, não duplique; src/engine/sessionModel.ts linhas 268-334 (`isTimeBased`,
    `metricOf`, `formatDuration`, `formatDistance`, `formatPace`, `paceSecondsPerKm` — os
    blocos que `formatCardioSetResult` combina); src/components/session/SessionQueue.tsx linhas
    42-62 (`doneLine` — o ALGORITMO EXATO a replicar em `formatCardioSetResult`: monta
    `[formatDuration(duration)]`, se `distanceM != null` acrescenta `formatDistance` +
    `formatPace(paceSecondsPerKm(...))`, se `perceivedEffort` acrescenta o texto, junta com
    ' · '); __tests__/sessionExecutionRepository.test.ts linhas 1-52 (mocks/`makeBuilder`/
    `fromMock`, `beforeEach` que reseta os mocks) e linhas 787-899 (`describe('getSessionLogDetail'
    ...)` INTEIRO, já em contexto — os 4 testes existentes usam objetos de mock SEM
    `cardio_exercise_swaps` no cabeçalho e SEM `planned_exercise_id`/`metric` em `planned_sets`;
    a extensão precisa ser aditiva o bastante para eles continuarem passando sem edição).
  </read_first>
  <behavior>
    `formatCardioSetResult` (`sessionModel.ts`):
    - Teste 1: `{ durationSeconds: 1200, distanceM: 5000, perceivedEffort: 'moderado' }` ->
      `"20:00 · 5 km · 4:00 /km · moderado"` (paridade byte a byte com o que `doneLine`
      produziria para o mesmo `DraftSet`).
    - Teste 2: `{ durationSeconds: 300, distanceM: null, perceivedEffort: null }` -> `"5:00"`
      (sem distância, sem pace, sem esforço — nenhum campo inventado).
    - Teste 3 (paridade anti-drift, Pitfall 5): para uma bateria de 4 combinações
      duration/distance/effort, `formatCardioSetResult(params)` produz EXATAMENTE a mesma
      string que uma réplica local do corpo de `doneLine` (copiado no teste como fixture de
      comparação) — se um dos dois divergir no futuro, este teste pega o drift.

    `getSessionLogDetail` (repositório, `sessionExecutionRepository.test.ts`, mocks
    `supabase.from`):
    - Teste 4 (Pitfall 2 fechado): linha de `set_logs` com `actual_reps: null,
      actual_duration_seconds: 1200, actual_distance_m: 5000, perceived_effort: 'moderado'` e
      `planned_sets.planned_exercises.metric: 'tempo_distancia'` -> `res.exercises[0].sets[0]`
      tem `actualDurationSeconds: 1200`, `actualDistanceM: 5000`, `perceivedEffort:
      'moderado'`, `actualReps: null` (sem lançar, sem coagir para 0).
    - Teste 5 (D-08 histórico): cabecalho com `cardio_exercise_swaps: [{ planned_exercise_id:
      'pe-9', to_modality: 'Remo Ergômetro' }]`, linha com `planned_sets.planned_exercise_id:
      'pe-9'`, `planned_sets.planned_exercises.name: 'Corrida'` -> `res.exercises[0].name ===
      'Remo Ergômetro'` e `res.exercises[0].swappedFrom === 'Corrida'`.
    - Teste 6 (sem troca): cabecalho SEM `cardio_exercise_swaps` (campo ausente, como os
      mocks pré-existentes) -> `res.exercises[0].swappedFrom` é `null`/`undefined`, `name`
      continua o nome do plano — os 4 testes PRÉ-EXISTENTES deste describe continuam passando
      SEM edição (confirmar rodando a suíte inteira).
  </behavior>
  <action>
    Em `src/engine/sessionModel.ts`, adicione, ao lado de `formatDuration`/`formatDistance`
    (linhas 302-314), a função `formatCardioSetResult(params: { durationSeconds: number |
    null; distanceM: number | null; perceivedEffort: PerceivedEffort | null; }): string` —
    corpo IDÊNTICO ao algoritmo de `SessionQueue.doneLine` (linhas 44-53): monta
    `[formatDuration(params.durationSeconds)]`, se `params.distanceM != null` acrescenta
    `formatDistance(params.distanceM)` e `formatPace(paceSecondsPerKm(params.durationSeconds,
    params.distanceM))`, se `params.perceivedEffort` acrescenta o texto do esforço, `join('
    · ')`. Comentário citando que esta é a MESMA fórmula usada em `SessionQueue.doneLine`
    (sessão ativa) — replicada aqui para o histórico em vez de importar componente de UI
    dentro do motor puro (`sessionModel.ts` não pode depender de `components/`), com paridade
    garantida por teste, não por importação cruzada (decisão documentada: puxar
    `SessionQueue.tsx` para esta wave criaria dependência de arquivo com o Plano 03-03 sem
    ganho proporcional — ambas as implementações são funções puras já cobertas por teste).

    Em `src/services/sessionExecutionRepository.ts`: nos tipos (linhas 747-769), widen
    `HistorySetLog.actualReps` de `number` para `number | null` (compatível com uso existente
    — um `number` sempre satisfaz `number | null`), adicione OPCIONAIS (`?:`, para não quebrar
    literais de objeto em testes existentes que não os têm) `actualDurationSeconds?: number |
    null; actualDistanceM?: number | null; perceivedEffort?: PerceivedEffort | null;`.
    Importe `PerceivedEffort`/`ExerciseMetric` de `../engine/sessionModel` (adicione ao import
    já existente `import type { Outcome, SkipReason } from '../engine/sessionModel';`). Em
    `HistoryExercise`, adicione OPCIONAIS `metric?: ExerciseMetric | null; swappedFrom?: string
    | null;`.

    Em `consultaCabecalho` (linhas 779-788), acrescente `, cardio_exercise_swaps(planned_exercise_id,
    to_modality)` ao FINAL das duas variantes da string de `.select(...)` (com e sem
    `active_seconds`) — mesma técnica de embed de `exercise_skips` em `getOpenSessionLog:170`,
    aproveitando que `cardio_exercise_swaps.session_log_id` referencia `session_logs.id`. Isso
    mantém a CONTAGEM de queries igual à de hoje (não adiciona um terceiro `.from(...)`), o que
    preserva os 4 testes existentes sem qualquer edição — eles simplesmente não têm esse campo
    no mock e o código trata a ausência como lista vazia.

    Na query de `linhas` (linha 800-805), estenda o `.select(...)` para
    `'actual_reps, actual_load_kg, actual_rir, actual_duration_seconds, actual_distance_m,
    perceived_effort, outcome, completed_at, planned_sets(set_order, planned_exercise_id,
    planned_exercises(name, exercise_order, metric))'` — adiciona `planned_exercise_id` (nível
    `planned_sets`) e `metric` (nível `planned_exercises`) aos já existentes `set_order`/
    `name`/`exercise_order`.

    Na montagem do resultado (linhas 808-844): logo após obter `c = cabecalho.data as any`,
    construa `const mapaTrocas = new Map<string, string>(); for (const sw of
    (c.cardio_exercise_swaps ?? []) as any[]) { if (typeof sw?.planned_exercise_id === 'string'
    && isCardioModalidade(sw?.to_modality)) mapaTrocas.set(sw.planned_exercise_id,
    sw.to_modality); }` (linha malformada é DESCARTADA, não coagida — mesmo raciocínio de
    `isSkipReason` em `exerciseSkips`). No loop de `porExercicio` (linhas 810-826), leia
    `const plannedExerciseId: string | null = l?.planned_sets?.planned_exercise_id ?? null;`,
    `const metric = l?.planned_sets?.planned_exercises?.metric ?? null;`, `const toModality =
    plannedExerciseId ? mapaTrocas.get(plannedExerciseId) : undefined;`. Troque a CHAVE de
    agrupamento de `${ordemEx}::${nome}` para `plannedExerciseId ?? \`${ordemEx}::${nome}\``
    (preserva o agrupamento antigo quando o mock/dado não tem o id — retrocompatível com os
    testes existentes). Ao criar a entrada nova do Map, use `name: toModality ?? nome` (nome
    exibido é a modalidade NOVA quando há troca) e adicione `metric` e `swappedFrom: toModality
    ? nome : null` (nome ORIGINAL, só quando houve troca). No push de cada série, adicione
    `actualDurationSeconds: toNum(l.actual_duration_seconds), actualDistanceM:
    toNum(l.actual_distance_m), perceivedEffort: l.perceived_effort ?? null` aos campos já
    existentes (mantenha `actualReps: l.actual_reps` sem `?? 0` — `null` é o valor correto e
    honesto para uma série de cardio, nunca inventar 0 reps).
  </action>
  <verify>
    <automated>npx jest __tests__/sessionExecutionRepository.test.ts -t "getSessionLogDetail" -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/sessionExecutionRepository.test.ts -q` verde INTEIRO (não só o describe
    de `getSessionLogDetail` — confirma que as demais suítes do arquivo não regrediram);
    `npx tsc --noEmit` sem erros novos; os 4 testes PRÉ-EXISTENTES de `getSessionLogDetail`
    (linhas 788, 834, 861, 889 do arquivo original) passam sem NENHUMA linha alterada — só
    testes NOVOS foram adicionados ao describe.
  </acceptance_criteria>
  <done>getSessionLogDetail lê duração/distância/esforço de cardio e expõe o par
  original↔trocado; formatCardioSetResult existe e tem paridade provada com doneLine.</done>
  <reversibility rating="reversible">Extensão aditiva de tipos e query; nenhuma migration,
  nenhuma mudança de contrato para os consumidores existentes.</reversibility>
</task>

<task type="auto">
  <name>Task 2: SessionHistoryDetailScreen — cardio legível + rótulo "trocado de X"</name>
  <files>src/screens/SessionHistoryDetailScreen.tsx, __tests__/sessionHistory.test.tsx</files>
  <read_first>
    src/screens/SessionHistoryDetailScreen.tsx (INTEIRO, já em contexto — `descreveSerie`
    linhas 26-30, montagem de `sections` linha 82, `SectionList` linhas 96-119, estilos
    `exerciseName`/`setText` linhas 154-175); __tests__/sessionHistory.test.tsx (INTEIRO, já em
    contexto — o `describe('SessionHistoryDetailScreen', ...)` linhas 65-90 é o único teste
    deste componente; o mock de mesma linha 67-79 NÃO tem `metric`/`swappedFrom` no exercício
    nem os campos novos nos sets — precisa continuar passando sem edição, só ganhar um `it`
    novo).
  </read_first>
  <action>
    Em `src/screens/SessionHistoryDetailScreen.tsx`: importe `isTimeBased`,
    `formatCardioSetResult`, `type ExerciseMetric` de `../engine/sessionModel` (ao lado do
    `import type { Outcome } ...` já existente). Reescreva `descreveSerie` para aceitar um
    segundo parâmetro `metric: ExerciseMetric | null | undefined`: se
    `isTimeBased(metric)`, retorna `formatCardioSetResult({ durationSeconds:
    s.actualDurationSeconds ?? null, distanceM: s.actualDistanceM ?? null, perceivedEffort:
    s.perceivedEffort ?? null })`; senão, mantém EXATAMENTE o corpo atual, trocando só
    `${s.actualReps} reps` por `${s.actualReps ?? '—'} reps` (nunca inventar reps quando
    ausente — `actualReps` agora é nullable no tipo).

    Na montagem de `sections` (linha 82), adicione `metric: ex.metric ?? null, swappedFrom:
    ex.swappedFrom ?? null` ao objeto (`{ title: ex.name, metric: ..., swappedFrom: ...,
    data: ex.sets }`). Em `renderSectionHeader` (linha 99-101), acrescente, logo abaixo do
    `<Text style={styles.exerciseName}>`, `{section.swappedFrom ? <Text
    style={styles.swapNote}>{`Trocado de ${section.swappedFrom}`}</Text> : null}`. Em
    `renderItem` (linha 102-113), troque a assinatura para `({ item, section }) => (...)` e
    troque a chamada `descreveSerie(item)` por `descreveSerie(item, section.metric)`.
    Adicione o estilo `swapNote` ao `StyleSheet.create` (mesma família visual de `subtitle`:
    `color: theme.colors.text.quiet, fontFamily: theme.fonts.ui, fontSize:
    theme.typography.fontSizes.xs, fontStyle: 'italic'`).

    Em `__tests__/sessionHistory.test.tsx`, adicione ao `describe('SessionHistoryDetailScreen',
    ...)` um novo `it('cardio: mostra duração/distância/pace e o rótulo de troca, sem reps')`
    — mock `getSessionLogDetail` resolvendo com um exercício `{ name: 'Remo Ergômetro', order:
    1, metric: 'tempo_distancia', swappedFrom: 'Corrida', sets: [{ setOrder: 1, actualReps:
    null, actualLoadKg: null, actualRir: null, actualDurationSeconds: 1200, actualDistanceM:
    5000, perceivedEffort: 'moderado', outcome: 'on_target' }] }`; renderiza
    `SessionHistoryDetailScreen`; assere `getByText('Remo Ergômetro')`,
    `getByText(/Trocado de Corrida/)`, `getByText(/20:00/)`, `getByText(/5 km/)`,
    `queryByText(/reps/)` é `null` (nenhum texto "reps" aparece na linha de cardio).
  </action>
  <verify>
    <automated>npx jest __tests__/sessionHistory.test.tsx -q</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/sessionHistory.test.tsx -q` verde (teste pré-existente linha 66
    INALTERADO + teste novo passando); `npx tsc --noEmit` sem erros novos; abrir o detalhe de
    uma sessão com cardio (harness manual, se aplicável) não mostra mais "null reps × peso
    corporal" para séries de cardio.
  </acceptance_criteria>
  <done>O histórico mostra cardio corretamente formatado (tempo/distância/pace/esforço) e o
  rótulo "Trocado de X" quando aplicável; força continua idêntico.</done>
  <reversibility rating="reversible">UI de leitura, sem escrita, revertível via git.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Leitura RLS "own" (`session_logs`/`cardio_exercise_swaps`) → tela | Puro consumo de dado já autorizado por RLS "own" (herdada, migration 0034); esta plan não abre nenhuma fronteira de confiança nova, só lê mais colunas da mesma linha já autorizada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-03-08 | Information Disclosure | `to_modality` de outro usuário vazando via embed malformado | low | accept | O embed é sobre `session_logs`, já filtrado por `id = sessionLogId` + RLS "own" (0001) — a mesma RLS "own" de `cardio_exercise_swaps` (migration 0034) filtra o array embutido; não há caminho novo para ver dado alheio. |
</threat_model>

<verification>
- `npx jest __tests__/sessionExecutionRepository.test.ts __tests__/sessionHistory.test.tsx -q` verde.
- `npx tsc --noEmit` sem erros novos.
- Os 5 testes pré-existentes tocados (4 de `getSessionLogDetail` + 1 de `SessionHistoryDetailScreen`) passam sem edição de asserção.
</verification>

<success_criteria>
Fecha D-08 por completo (sessão ativa via Plano 03-03 + histórico via este plano) e corrige o
gap pré-existente de cardio no histórico (Pitfall 2), tratado como pré-requisito de D-08 per
03-CONTEXT.md, não como escopo adicional.
</success_criteria>

<output>
Criar `.planning/phases/03-interc-mbio-de-modalidade-de-cardio/03-05-SUMMARY.md` ao final.
</output>
