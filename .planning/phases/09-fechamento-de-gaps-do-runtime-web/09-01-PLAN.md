---
phase: 09-fechamento-de-gaps-do-runtime-web
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - App.tsx
  - src/utils/alertShim.ts
  - src/store/alertStore.ts
  - src/components/AlertHost.tsx
  - src/screens/ActiveSessionScreen.tsx
  - __tests__/alertHostWeb.test.tsx
  - __tests__/alertShim.test.ts
  - __tests__/activeSessionScreen.test.tsx
autonomous: true
requirements: [WEB-01, SESS-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No PWA web, o diálogo 'Concluir treino?' (com séries pendentes, ActiveSessionScreen.tsx) mostra um Modal custom visível com os botões 'Continuar treino'/'Concluir', ambos funcionais — não é mais um clique morto (critério de sucesso 2 do ROADMAP Fase 9)."
    - "Durante a sessão ativa (status 'active'/'awaiting_checkin'), o Wake Lock é solicitado via activateKeepAwakeAsync('active-session'); ao concluir (status 'finished'), o Wake Lock é liberado via deactivateKeepAwake('active-session') — D-05."
    - "Ao o app voltar de background/tela bloqueada durante a sessão ativa, o Wake Lock é readquirido via listener de document.visibilitychange — D-07 (expo-keep-awake web não faz isso sozinho, confirmado lendo ExpoKeepAwake.web.ts na íntegra)."
    - "No nativo (Platform.OS !== 'web'), showAlert repassa para Alert.alert real com os mesmos argumentos — nenhuma mudança de comportamento (D-03)."
    - "Sem suporte a Wake Lock (iOS < 16.4, browser incompatível), a ativação falha silenciosamente — nenhum aviso novo na UI (D-06)."
  artifacts:
    - src/utils/alertShim.ts
    - src/store/alertStore.ts
    - src/components/AlertHost.tsx
    - __tests__/alertHostWeb.test.tsx
    - __tests__/alertShim.test.ts
  key_links:
    - "App.tsx monta AlertHost dentro de AuthProvider (D-04)"
    - "alertShim.ts -> alertStore.ts (branch web) / Alert.alert real (branch nativo, D-03)"
    - "ActiveSessionScreen status -> activateKeepAwakeAsync/deactivateKeepAwake por tag"
    - "document.visibilitychange -> re-aquisição do Wake Lock"
  prohibitions:
    - statement: "alertShim/AlertHost nunca usa window.alert ou window.confirm no branch web (D-02)"
      status: flagged-unverified
      verification: "grep -rn 'window\\.\\(alert\\|confirm\\)' src/ deve retornar vazio"
    - statement: "O shim não implementa Alert.prompt (nenhum call site usa; fora de escopo do CONTEXT.md)"
      status: flagged-unverified
      verification: "src/utils/alertShim.ts não exporta nenhuma função de prompt"
    - statement: "Nenhuma dependência de pacote nova é instalada nesta fase"
      status: flagged-unverified
      verification: "git diff package.json não contém entrada nova em dependencies/devDependencies"
    - statement: "No nativo, o comportamento do Alert não muda — repasse puro para Alert.alert real (D-03)"
      status: flagged-unverified
      verification: "__tests__/questionnaireDiasESessao.test.tsx (spy em Alert.alert) continua passando sem modificação (validado na Plan 09-02)"
    - statement: "O Wake Lock nunca permanece ativo depois que status vira 'finished'"
      status: flagged-unverified
      verification: "teste em __tests__/activeSessionScreen.test.tsx assevera deactivateKeepAwake chamado na transição para 'finished'"
---

<objective>
Construir o núcleo da correção de WEB-01 (shim central de `Alert.alert` + `AlertHost`
custom no web) e de SESS-01 (ciclo de vida correto do Wake Lock) num único corte
vertical ancorado em `ActiveSessionScreen.tsx` — o arquivo onde as duas dívidas do
ROADMAP Fase 9 se concentram (critério de sucesso 2 do WEB-01 e o comportamento
completo do SESS-01).

Nota sobre a sonda de edge coverage: WEB-01/SESS-01 vieram "unclassified"
(unresolved) da sonda automática desta fase — não há SPEC.md com `## Edge
Coverage`. Os `must_haves.truths` acima foram derivados diretamente dos critérios
de sucesso da Fase 9 no ROADMAP.md e das decisões D-01 a D-08 travadas em
`09-CONTEXT.md`, não de uma seção SPEC formal. Isso é registrado aqui como
suposição explícita, não descartado em silêncio.

Purpose: provar a arquitetura inteira (utility shim -> store -> component ->
ponto de montagem global -> tela real) numa única tela antes de replicar a
migração nas outras 3 telas (Plans 09-02/09-03).
Output: `src/utils/alertShim.ts`, `src/store/alertStore.ts`,
`src/components/AlertHost.tsx` novos; `App.tsx` e `ActiveSessionScreen.tsx`
modificados; `__tests__/alertHostWeb.test.tsx` e `__tests__/alertShim.test.ts`
novos; `__tests__/activeSessionScreen.test.tsx` estendido.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-CONTEXT.md
@.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-RESEARCH.md
@.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: alertShim + alertStore + AlertHost + montagem em App.tsx + migrar o diálogo "Concluir treino?" + reescrever o ciclo de vida do Wake Lock em ActiveSessionScreen.tsx</name>
  <files>App.tsx, src/utils/alertShim.ts, src/store/alertStore.ts, src/components/AlertHost.tsx, src/screens/ActiveSessionScreen.tsx, __tests__/alertHostWeb.test.tsx, __tests__/activeSessionScreen.test.tsx</files>
  <read_first>
    src/utils/haptics.ts (arquivo inteiro — molde de shim por Platform.OS, `isWeb()` função + try/catch silencioso, D-06);
    src/services/auth/secureStorage.ts (linhas 14, 30, 180-184 — segundo molde de shim, variante `const isWeb = Platform.OS === 'web'` como valor);
    src/components/session/SwapModalitySheet.tsx (linhas 25-40 imports, 198-215 styles — molde visual Modal + StyleSheet + theme);
    src/theme/theme.ts (linhas 70-97 — tokens confirmados: `overlay`, `status.danger` = '#DC827B', `surface.card`, `borderRadius.xxl`, `elevation.floating`);
    src/store/manualPlanStore.ts (linha 149 — convenção `create&lt;State&gt;((set, get) =&gt; ...)`);
    App.tsx (arquivo inteiro, 46 linhas — único ponto de composição global, `AuthProvider` linhas 31-34);
    src/screens/ActiveSessionScreen.tsx (arquivo inteiro — foco: linhas 7-24 imports/useKeepAwake, 82-84 leitura de `status`/`draft`, 248-275 `onConcluirTreino`, 322-340 branch `status === 'finished'`);
    __tests__/swapModalitySheet.test.tsx (molde de teste `render`/`fireEvent` sem DOM literal);
    __tests__/activeSessionScreen.test.tsx (linhas 1-90 mocks de módulo, 247-265 `beforeEach`/`renderScreen`/`useActiveSessionStore.setState`).
  </read_first>
  <behavior>
    - `AlertHost` sem nenhum alerta ativo no `useAlertStore` não renderiza Modal (retorna null).
    - Chamar `showAlert('Concluir treino?', 'Ainda há séries não registradas. Deseja concluir mesmo assim?', [{ text: 'Continuar treino', style: 'cancel' }, { text: 'Concluir', onPress: fn }])` com `AlertHost` montado faz o texto "Concluir treino?" aparecer na árvore renderizada.
    - Pressionar o botão "Concluir" chama `fn` exatamente 1 vez e fecha o alerta (`current` volta a `null`).
    - Com `status` do `activeSessionStore` em `'active'`/`'awaiting_checkin'`, `activateKeepAwakeAsync('active-session')` é chamado; ao mudar para `'finished'`, `deactivateKeepAwake('active-session')` é chamado.
    - Disparar `visibilitychange` com `document.visibilityState = 'visible'` enquanto `status` ainda é `'active'` chama `activateKeepAwakeAsync` de novo (readquisição, D-07).
  </behavior>
  <action>
    Criar `src/store/alertStore.ts`: Zustand `create&lt;AlertState&gt;((set) =&gt; ({...}))` (molde `manualPlanStore.ts:149`), um único slot `current: { title: string; message: string | null; buttons: AlertButton[] | null } | null`, ações `show(a)`/`dismiss()`. Definir e exportar NESTE arquivo o tipo `AlertButton = { text: string; onPress?: () =&gt; void; style?: 'default' | 'cancel' | 'destructive' }` — `alertShim.ts` importa esse tipo daqui (não o contrário), para não criar dependência circular entre os dois módulos novos.

    Criar `src/utils/alertShim.ts`: exporta `showAlert(title: string, message?: string, buttons?: AlertButton[]): void`, MESMA assinatura de `Alert.alert` (D-01). `if (Platform.OS !== 'web') { Alert.alert(title, message, buttons); return; }` (repasse puro nativo, D-03, molde `isWeb()`/`isWeb` de `haptics.ts:10`/`secureStorage.ts:30`); no branch web chama `useAlertStore.getState().show({ title, message: message ?? null, buttons: buttons ?? null })`.

    Criar `src/components/AlertHost.tsx`: default export, lê `useAlertStore((s) =&gt; s.current)` e `dismiss`; retorna `null` se `current` for `null`. Senão renderiza `Modal` (`visible transparent animationType="fade" onRequestClose={dismiss}`) com `Pressable` backdrop (`testID="alert-host-backdrop"`, `onPress={dismiss}`) contendo um `Pressable` card interno (`onPress={() =&gt; undefined}` para não fechar ao tocar no conteúdo) com título, mensagem opcional e os botões. Default de botões quando `buttons` for `null`: `[{ text: 'OK' }]`. Cada botão é `TouchableOpacity` com `testID={\`alert-host-button-${i}\`}`; ao pressionar chama `dismiss()` e depois `b.onPress?.()` (sem `await`, igual ao `Alert.alert` nativo). Estilo `destructive` usa `theme.colors.status.danger` (confirmado em `theme.ts:90`, `'#DC827B'` — usar o token direto, sem fallback `?? theme.colors.text.accent`). `StyleSheet.create` usando `theme.colors.overlay` (backdrop), `theme.elevation.floating` + `theme.borderRadius.xxl` + `theme.colors.surface.card` (card) — mesmo molde de `SwapModalitySheet.tsx:199-211`.

    Montar `&lt;AlertHost /&gt;` em `App.tsx` dentro de `&lt;AuthProvider&gt;` (D-04), como irmão de `&lt;StatusBar /&gt;`/`&lt;RootNavigator /&gt;` (linhas 31-34 atuais); adicionar `import AlertHost from './src/components/AlertHost';`.

    Em `src/screens/ActiveSessionScreen.tsx`: adicionar `import { showAlert } from '../utils/alertShim';`. NÃO remover `Alert` do import de `react-native` ainda (ainda há 3 call sites nesta tela, migrados na Task 2 deste mesmo plano). Migrar SOMENTE o call site de `onConcluirTreino` (o `Alert.alert('Concluir treino?', 'Ainda há séries não registradas. Deseja concluir mesmo assim?', [{ text: 'Continuar treino', style: 'cancel' }, { text: 'Concluir', onPress: finalizar }])` dentro do `if (!isSessionComplete(draft))`) de `Alert.alert(` para `showAlert(`, argumentos idênticos — é o critério de sucesso 2 do ROADMAP.

    Reescrever o ciclo de vida do Wake Lock (D-05/D-06/D-07): trocar `import { useKeepAwake } from 'expo-keep-awake';` por `import { activateKeepAwakeAsync, deactivateKeepAwake } from 'expo-keep-awake';`. Remover a chamada solta `useKeepAwake();`. Definir `const WAKE_LOCK_TAG = 'active-session';` no escopo do módulo (fora do componente). Adicionar DOIS `useEffect`s DEPOIS que `status` é lido do store (`const status = useActiveSessionStore((s) =&gt; s.status);`) — nunca antes dessa linha, para não referenciar `status` em temporal dead zone: (1) efeito com dependência `[status]`: se `status === 'active' || status === 'awaiting_checkin'`, chama `activateKeepAwakeAsync(WAKE_LOCK_TAG).catch(() =&gt; {})` e retorna cleanup que chama `deactivateKeepAwake(WAKE_LOCK_TAG).catch(() =&gt; {})`; caso contrário, chama `deactivateKeepAwake(WAKE_LOCK_TAG).catch(() =&gt; {})` diretamente e retorna sem registrar cleanup; (2) efeito com dependência `[status]`: guard `if (typeof document === 'undefined') return;` (nativo não tem `document` global); se `status` não é `'active'`/`'awaiting_checkin'`, retorna sem registrar listener; senão registra `document.addEventListener('visibilitychange', onVisibilityChange)` onde `onVisibilityChange` chama `activateKeepAwakeAsync(WAKE_LOCK_TAG).catch(() =&gt; {})` quando `document.visibilityState === 'visible'`; cleanup remove o listener.

    Estender `__tests__/activeSessionScreen.test.tsx`: adicionar `jest.mock('expo-keep-awake', () =&gt; ({ activateKeepAwakeAsync: jest.fn().mockResolvedValue(undefined), deactivateKeepAwake: jest.fn().mockResolvedValue(undefined) }));` junto aos demais `jest.mock(...)` do topo do arquivo (mesma seção dos mocks de `agendaRepository`/`planEditRepository`). Adicionar um novo `describe('Wake Lock lifecycle (SESS-01)', ...)` com casos: (a) `useActiveSessionStore.setState({ status: 'active' })` (mais o `draft` mínimo necessário para renderizar) leva a `activateKeepAwakeAsync` chamado com `'active-session'`; (b) transição para `status: 'finished'` leva a `deactivateKeepAwake` chamado com `'active-session'`; (c) com `status: 'active'`, disparar `document.dispatchEvent(new Event('visibilitychange'))` após mockar `Object.defineProperty(document, 'visibilityState', { value: 'visible', configurable: true })` leva a uma NOVA chamada de `activateKeepAwakeAsync`.

    Criar `__tests__/alertHostWeb.test.tsx` (molde `swapModalitySheet.test.tsx`): mockar `Platform.OS = 'web'` preservando os componentes reais do RN (`jest.mock('react-native', () =&gt; ({ ...jest.requireActual('react-native'), Platform: { ...jest.requireActual('react-native').Platform, OS: 'web' } }));`); renderizar `&lt;AlertHost /&gt;`, chamar `showAlert(...)` com o mesmo payload do "Concluir treino?", afirmar `getByText('Concluir treino?')` truthy, `fireEvent.press` no botão "Concluir" e afirmar que o `onPress` injetado foi chamado 1 vez.
  </action>
  <reversibility rating="reversible">
    D-02/D-04/D-05 são código de UI/lifecycle local, sem migração de banco nem dado de usuário envolvido — reversível via `git revert` sem perda de dado.
  </reversibility>
  <verify>
    <automated>npx jest __tests__/alertHostWeb.test.tsx __tests__/activeSessionScreen.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    src/store/alertStore.ts exporta `useAlertStore` e o tipo `AlertButton`;
    src/utils/alertShim.ts exporta `showAlert(title, message?, buttons?)` com o corpo `if (Platform.OS !== 'web')`;
    App.tsx contém `&lt;AlertHost /&gt;` dentro de `&lt;AuthProvider&gt;`;
    ActiveSessionScreen.tsx chama `showAlert(` (não `Alert.alert(`) para o diálogo "Concluir treino?";
    ActiveSessionScreen.tsx não importa mais `useKeepAwake` de `expo-keep-awake`; importa `activateKeepAwakeAsync`/`deactivateKeepAwake`;
    `npx jest __tests__/alertHostWeb.test.tsx __tests__/activeSessionScreen.test.tsx` passa sem falhas;
    `npx tsc --noEmit` sem erros novos.
  </acceptance_criteria>
  <done>AlertHost renderiza no web e o diálogo "Concluir treino?" funciona via showAlert; Wake Lock ativa/desativa corretamente por status e readquire em visibilitychange; alertHostWeb.test.tsx e activeSessionScreen.test.tsx passam.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar os 3 call sites restantes de ActiveSessionScreen.tsx + teste de repasse nativo do shim</name>
  <files>src/screens/ActiveSessionScreen.tsx, __tests__/alertShim.test.ts</files>
  <read_first>
    src/screens/ActiveSessionScreen.tsx (após a Task 1 — handlers `onConfirmarRecusa` linhas ~162-185, `onConfirmarTroca` linhas ~187-207, `onConcluirTreino`/`finalizar` linhas ~248-262);
    src/services/auth/secureStorage.ts (linhas 14, 30 — molde de mock de `Platform.OS` para um módulo utilitário puro, sem precisar preservar componentes RN);
    __tests__/secureStorageWeb.test.ts (precedente de teste de shim com `Platform.OS` mockado).
  </read_first>
  <action>
    Migrar os 3 call sites informativos (1 botão) de `Alert.alert` restantes em `ActiveSessionScreen.tsx` para `showAlert`, trocando só o nome da função — argumentos idênticos: `Alert.alert('Não foi possível registrar', saveError ?? 'Tente novamente.')` em `onConfirmarRecusa`; `Alert.alert('Não foi possível trocar', saveError ?? 'Tente novamente.')` em `onConfirmarTroca`; `Alert.alert('Não foi possível concluir', saveError ?? 'Tente novamente.')` em `onConcluirTreino`/`finalizar`. Depois de migrar os 4 (este + o da Task 1), remover `Alert` do bloco de import `react-native` no topo do arquivo — o único uso restante do símbolo era esses 4 call sites.

    Criar `__tests__/alertShim.test.ts`: mockar `(Platform as { OS: string }).OS = 'ios'` no `beforeEach` e restaurar no `afterEach` (molde `direcao03-fase1-fundacoes.test.tsx`, ver TESTING.md); `jest.spyOn(Alert, 'alert').mockImplementation(() =&gt; undefined)`; chamar `showAlert('t', 'm', [{ text: 'OK' }])` e afirmar `Alert.alert` foi chamado com `('t', 'm', [{ text: 'OK' }])`; afirmar também que `useAlertStore.getState().current` permanece `null` (o branch web nunca é tocado no nativo).
  </action>
  <verify>
    <automated>npx jest __tests__/alertShim.test.ts __tests__/activeSessionScreen.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "Alert\." src/screens/ActiveSessionScreen.tsx` não retorna nenhuma linha;
    `grep -n "import.*Alert" src/screens/ActiveSessionScreen.tsx` não retorna nenhuma linha (import removido);
    `npx jest __tests__/alertShim.test.ts` passa;
    `npx tsc --noEmit` sem erros novos.
  </acceptance_criteria>
  <done>ActiveSessionScreen.tsx não contém mais nenhum Alert.alert nem import de Alert; teste de repasse nativo (alertShim.test.ts) prova D-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-only (browser/PWA) | `Alert`/`Wake Lock` são APIs 100% client-side; nenhuma chamada de rede nova, nenhum dado cruza para o backend nesta fase. |
| Título/mensagem hardcoded -> UI | Os textos exibidos no `AlertHost` vêm de literais de código nos 12 call sites — nunca de input do usuário ou de resposta de servidor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Denial of Service | ActiveSessionScreen.tsx (ciclo de vida do Wake Lock) | medium | mitigate | `useEffect` com cleanup explícito chamando `deactivateKeepAwake` na transição para `status === 'finished'` e no unmount (Pitfall 2 do RESEARCH.md: `useKeepAwake()` sem tag não pode ser desligado condicionalmente) — coberto por teste dedicado assertando `deactivateKeepAwake` chamado em `'finished'` (Task 1). |
| T-09-02 | Tampering | alertShim.ts / AlertHost.tsx | low | accept | Todo conteúdo do diálogo (título/mensagem/botões) vem de strings hardcoded nos call sites, nunca de input externo do usuário ou de rede — sem superfície de injeção; `Text` do RN não interpreta HTML/markup. |
| T-09-03 | Elevation of Privilege | `navigator.wakeLock.request('screen')` | low | accept | Screen Wake Lock é uma API sem permissão elevada do browser (sem prompt de permissão ao usuário); nenhuma credencial ou escopo novo é solicitado. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | Nenhuma dependência nova é instalada nesta fase (RESEARCH.md "Package Legitimacy Audit: Não aplicável" — `alertShim`/`AlertHost`/wake lock usam só `react-native`/`zustand`/`expo-keep-awake` já em produção). Gate mantido como registro; reabrir se uma lib nova for introduzida numa fase futura. |

</threat_model>

<verification>
`npx jest __tests__/alertHostWeb.test.tsx __tests__/alertShim.test.ts __tests__/activeSessionScreen.test.tsx` passa; `npx tsc --noEmit` sem erros novos; `App.tsx` monta `AlertHost`; `ActiveSessionScreen.tsx` não importa mais `useKeepAwake`.
</verification>

<success_criteria>
`AlertHost` renderiza no web e o diálogo "Concluir treino?" funciona ponta a ponta (critério de sucesso 2 do ROADMAP); Wake Lock ativa/desativa corretamente por `status` e readquire em `visibilitychange`; nativo continua chamando `Alert.alert` real sem mudança de comportamento.
</success_criteria>

## Artifacts this phase produces

- `src/utils/alertShim.ts` — novo; exporta `showAlert(title, message?, buttons?)`, tipo `AlertButton` importado de `alertStore.ts`.
- `src/store/alertStore.ts` — novo; exporta `useAlertStore` (Zustand) e o tipo `AlertButton`.
- `src/components/AlertHost.tsx` — novo; default export `AlertHost`.
- `__tests__/alertHostWeb.test.tsx` — novo.
- `__tests__/alertShim.test.ts` — novo.
- `__tests__/alertNoAlertRemanescente.test.ts` — novo (criado na Plan 09-04).
- Modificado: `App.tsx` (monta `&lt;AlertHost /&gt;`).
- Modificado: `src/screens/ActiveSessionScreen.tsx` (4 call sites migrados para `showAlert`; `useKeepAwake()` substituído por `activateKeepAwakeAsync`/`deactivateKeepAwake` + `WAKE_LOCK_TAG` + listener de `visibilitychange`).
- Modificado: `src/screens/QuestionnaireScreen.tsx`, `src/screens/SignUpScreen.tsx` (Plan 09-02).
- Modificado: `src/screens/JointLobbyScreen.tsx`, `src/screens/PostQuestionnaireChat.tsx` (Plan 09-03).
- Modificado: `__tests__/activeSessionScreen.test.tsx` (novo describe de Wake Lock lifecycle), `__tests__/jointLobbyScreen.test.tsx` (Plan 09-03).

<output>
Create `.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-01-SUMMARY.md` when done
</output>
