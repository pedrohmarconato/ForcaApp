---
phase: 09-fechamento-de-gaps-do-runtime-web
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - __tests__/alertNoAlertRemanescente.test.ts
autonomous: false
requirements: [WEB-01, SESS-01]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "grep -rn 'Alert\\.' src/ retorna somente ocorrências em src/utils/alertShim.ts e src/components/AlertHost.tsx — auditoria completa da classe fechada (critério de sucesso 1 do ROADMAP, 12 call sites em 4 arquivos)."
    - "__tests__/alertNoAlertRemanescente.test.ts existe e passa, protegendo as Fases 10-13 (ex.: opt-in de push) de reintroduzir Alert.alert cru fora do shim (D-08)."
    - "UAT do dono no iPhone real confirma WEB-01 (diálogo visível e funcional) e SESS-01 (tela não escurece durante a sessão ativa; bloqueio volta ao normal ao concluir) — critério de sucesso 3 do ROADMAP — condicionado à versão do iOS >=18.4 verificada ANTES do teste (bug WebKit 254545)."
  artifacts:
    - __tests__/alertNoAlertRemanescente.test.ts
  key_links:
    - "__tests__/alertNoAlertRemanescente.test.ts varre src/screens, src/components, src/store recursivamente"
    - "UAT do dono -> critério de sucesso 3 do ROADMAP Fase 9 (condicionado à versão do iOS)"
  prohibitions:
    - statement: "A guarda de regressão não pode passar sem varrer nada (sweep silenciosamente vazio)"
      status: flagged-unverified
      verification: "expect(arquivosVarridos).toBeGreaterThan(20) dentro do próprio teste, molde __tests__/loadInputLayoutWeb.test.ts:172"
    - statement: "O UAT do dono nunca é dado como concluído sem a checagem prévia da versão do iOS (bug WebKit 254545, corrigido só no iOS 18.4)"
      status: flagged-unverified
      verification: "checkpoint de UAT desta plan lista a checagem como passo 1, antes de qualquer teste de Wake Lock"
    - statement: "'Passou no Lighthouse' nunca é aceito como critério de conclusão desta fase (restrição do milestone, STATE.md)"
      status: flagged-unverified
      verification: "o checkpoint de UAT exige teste manual no iPhone real, não métrica automatizada de PWA score"
---

<objective>
Fechar a Fase 9 com a guarda permanente de regressão (D-08 — teste jest que impede
qualquer `Alert.alert` cru de voltar a existir fora do shim, protegendo as Fases
10-13) e o UAT do dono no iPhone real que confirma WEB-01 e SESS-01 ponta a ponta.
Este plan só pode começar depois que TODOS os 12 call sites migraram (Plans
09-01/09-02/09-03) — a varredura falharia se rodasse antes.

Nota sobre a sonda de edge coverage: WEB-01/SESS-01 vieram "unclassified"
(unresolved) da sonda automática — não há SPEC.md com `## Edge Coverage` nesta
fase. Os `must_haves.truths` acima vêm diretamente dos 3 critérios de sucesso da
Fase 9 no ROADMAP.md e da decisão D-08 do CONTEXT.md.

Purpose: transformar a auditoria pontual desta fase numa proteção permanente
(D-08) e fechar o loop de verificação humana que o milestone exige (STATE.md:
"cada fase relevante termina com item de UAT explícito do dono no iPhone real").
Output: `__tests__/alertNoAlertRemanescente.test.ts` novo; UAT aprovado ou
achados registrados para retrabalho.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-CONTEXT.md
@.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-RESEARCH.md
@.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar a guarda permanente de regressão contra Alert.alert cru (D-08)</name>
  <files>__tests__/alertNoAlertRemanescente.test.ts</files>
  <read_first>
    __tests__/loadInputLayoutWeb.test.ts (linhas 120-176 — molde de varredura recursiva `readdirSync`/`readFileSync`, coleta de `infratores`, guarda `expect(arquivosVarridos).toBeGreaterThanOrEqual(N)` contra a varredura silenciosamente parar de varrer);
    src/utils/alertShim.ts, src/components/AlertHost.tsx (únicos arquivos onde `Alert`/`Alert.alert` é esperado — exceções da varredura).
  </read_first>
  <action>
    Criar `__tests__/alertNoAlertRemanescente.test.ts`: função recursiva `listarArquivosRecursivo(dir)` (`readdirSync` com `withFileTypes: true`, recursão em diretórios, filtro `.tsx?`/`.jsx?`) sobre `src/screens/`, `src/components/`, `src/store/`; `PERMITIDOS = new Set(['alertShim.ts', 'AlertHost.tsx'])` (pulados na varredura); para cada arquivo restante, `readFileSync` e testar o regex `/\bAlert\s*[.,]/` no conteúdo — se casar, empilhar em `infratores`. `describe('guarda: nenhum Alert.alert fora do shim (D-08)', ...)`, `it('grep Alert\\. zerado fora de alertShim.ts/AlertHost.tsx', ...)`: `expect(arquivosVarridos).toBeGreaterThan(20)` (guarda contra a varredura parar de varrer silenciosamente, molde `loadInputLayoutWeb.test.ts:172`) e `expect(infratores).toEqual([])`.
  </action>
  <verify>
    <automated>npx jest __tests__/alertNoAlertRemanescente.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `npx jest __tests__/alertNoAlertRemanescente.test.ts` passa;
    `grep -rn "Alert\." src/` retorna somente ocorrências em `src/utils/alertShim.ts` e `src/components/AlertHost.tsx`;
    `npx jest` (suíte completa) — ler a saída textual "Tests: N passed" (não o exit code, ver `AGENTS.md`: o `--runInBand` pode sair 1 com handle aberto mesmo com tudo verde) — nenhum teste falhando;
    `npx tsc --noEmit` sem erros.
  </acceptance_criteria>
  <done>__tests__/alertNoAlertRemanescente.test.ts existe, passa e varre mais de 20 arquivos; grep -rn "Alert\." src/ restrito a alertShim.ts/AlertHost.tsx.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT do dono no iPhone real — WEB-01 (diálogos) + SESS-01 (Wake Lock)</name>
  <what-built>
    WEB-01 completo: 12 call sites migrados de `Alert.alert` cru para o shim
    `showAlert`/`AlertHost` (diálogo modal custom no web, repasse puro no
    nativo), com guarda de regressão permanente (D-08). SESS-01 completo: ciclo
    de vida do Wake Lock em `ActiveSessionScreen.tsx` — ativa durante a sessão
    (`status` `active`/`awaiting_checkin`), libera ao concluir (`status`
    `finished`), readquire ao voltar de background/tela bloqueada
    (`visibilitychange`).
  </what-built>
  <how-to-verify>
    1. ANTES de testar SESS-01: confira a versão do iOS do iPhone de teste
       (Ajustes -&gt; Geral -&gt; Informações -&gt; Versão do software). Existe um bug
       documentado da Apple/WebKit (`bugs.webkit.org#254545`) que faz a Screen
       Wake Lock API NÃO funcionar em apps instalados pela Tela de Início (Home
       Screen Web App) em qualquer iOS entre 16.4 e 18.3.x — corrigido só no
       iOS 18.4 (lançado março/2025). Se a versão for menor que 18.4, o passo 4
       abaixo VAI falhar mesmo com a implementação correta — isso é limitação
       de plataforma documentada, não bug desta fase. Registre a versão
       encontrada na resposta.
    2. Abra o ForçaApp no PWA instalado (ícone na Tela de Início) no iPhone —
       não no Safari em aba comum.
    3. Inicie um treino e, com pelo menos uma série ainda pendente, toque em
       "Concluir treino". Confirme que aparece um diálogo modal visível (card
       temático, não um clique morto) com os botões "Continuar treino" e
       "Concluir" — ambos precisam funcionar (cancelar mantém o treino aberto;
       concluir fecha e vai para o resumo).
    4. Durante a sessão ativa, deixe o iPhone parado sem tocar por tempo
       suficiente para o bloqueio automático normalmente disparar — confirme
       que a tela NÃO escurece. Bloqueie a tela manualmente (botão lateral) e
       desbloqueie de novo no meio do treino — confirme que a tela continua
       sem escurecer sozinha depois disso (readquisição via visibilitychange).
    5. Conclua o treino (chegue na tela de resumo pós-treino) — confirme que, a
       partir daí, o iPhone volta a bloquear a tela normalmente depois do
       tempo configurado (o lock foi liberado).
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" se os 5 passos passaram, ou descreva o que falhou —
    inclua a versão do iOS encontrada no passo 1, especialmente se o passo 4
    falhar.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build/Test tooling | O teste de varredura roda no clone do repo (CI/local), não em runtime do app — não é uma superfície de ataque do produto, é infraestrutura de qualidade. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Denial of Service | `__tests__/alertNoAlertRemanescente.test.ts` (guarda) | low | mitigate | Guarda `expect(arquivosVarridos).toBeGreaterThan(20)` impede que a própria varredura pare de varrer silenciosamente (ex.: regex que para de casar, pasta renomeada) e o teste vire um "sempre verde" falso-positivo. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | Nenhuma dependência nova nesta plan. |

</threat_model>

<verification>
`npx jest __tests__/alertNoAlertRemanescente.test.ts` passa; `npx jest` (suíte completa) sem falhas na saída textual; `grep -rn "Alert\." src/` restrito a `alertShim.ts`/`AlertHost.tsx`; UAT do dono aprovado (ou achados registrados) com a versão do iOS documentada.
</verification>

<success_criteria>
Auditoria completa da classe `Alert.alert` fechada e protegida permanentemente (D-08); SESS-01 confirmado em uso real no iPhone do dono, com a limitação de plataforma (bug WebKit 254545) documentada e verificada antes do teste, não descoberta durante ele.
</success_criteria>

## Artifacts this phase produces

- `__tests__/alertNoAlertRemanescente.test.ts` — novo (esta plan).
- `src/utils/alertShim.ts`, `src/store/alertStore.ts`, `src/components/AlertHost.tsx` — criados na Plan 09-01.
- `__tests__/alertHostWeb.test.tsx`, `__tests__/alertShim.test.ts` — criados na Plan 09-01.
- Modificado (Plan 09-01): `App.tsx`, `src/screens/ActiveSessionScreen.tsx`, `__tests__/activeSessionScreen.test.tsx`.
- Modificado (Plan 09-02): `src/screens/QuestionnaireScreen.tsx`, `src/screens/SignUpScreen.tsx`.
- Modificado (Plan 09-03): `src/screens/JointLobbyScreen.tsx`, `src/screens/PostQuestionnaireChat.tsx`, `__tests__/jointLobbyScreen.test.tsx`.

<output>
Create `.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-04-SUMMARY.md` when done
</output>
