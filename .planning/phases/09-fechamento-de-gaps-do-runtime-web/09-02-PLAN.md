---
phase: 09-fechamento-de-gaps-do-runtime-web
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/screens/QuestionnaireScreen.tsx
  - src/screens/SignUpScreen.tsx
autonomous: true
requirements: [WEB-01]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "grep -c 'Alert\\.' src/screens/QuestionnaireScreen.tsx e src/screens/SignUpScreen.tsx retornam 0 — os 7 call sites (6 + 1) migraram para showAlert."
    - "__tests__/questionnaireDiasESessao.test.tsx continua passando SEM modificação — o teste espiona Alert.alert diretamente e confirma que showAlert preservou o repasse nativo (D-03) para os call sites 'Sessão Expirada' e 'Erro ao Salvar'."
    - "O onPress async de signOut (call site 'Sessão Expirada') e o onPress de navigate (call site 'Cadastro realizado!') continuam funcionando sem await na chamada de showAlert, igual ao Alert.alert nativo."
  artifacts:
    - src/screens/QuestionnaireScreen.tsx (modificado)
    - src/screens/SignUpScreen.tsx (modificado)
  key_links:
    - "QuestionnaireScreen.tsx -> src/utils/alertShim.ts (criado na Plan 09-01)"
    - "SignUpScreen.tsx -> src/utils/alertShim.ts"
  prohibitions:
    - statement: "Nenhuma mudança de texto (título/mensagem) nos 7 diálogos migrados — migração é de mecanismo, não de conteúdo (discretion do CONTEXT.md)"
      status: flagged-unverified
      verification: "diff de cada Alert.alert(...) vs showAlert(...) mostra só a troca do nome da função, argumentos idênticos"
    - statement: "__tests__/questionnaireDiasESessao.test.tsx não é modificado nesta migração"
      status: flagged-unverified
      verification: "git diff __tests__/questionnaireDiasESessao.test.tsx vazio depois desta plan"
    - statement: "No nativo, o comportamento do Alert não muda — repasse puro para Alert.alert real (D-03)"
      status: flagged-unverified
      verification: "__tests__/questionnaireDiasESessao.test.tsx (spy em Alert.alert, linhas 171/186/203/212) passa sem alteração"
---

<objective>
Migrar os 6 call sites de `Alert.alert` em `QuestionnaireScreen.tsx` e o único call
site em `SignUpScreen.tsx` para `showAlert` (`src/utils/alertShim.ts`, criado na
Plan 09-01) — mesmo mecanismo já provado na Plan 09-01, só troca de import e nome
da função, sem mudar texto nem comportamento (WEB-01, discretion: "copy dos
diálogos: manter os textos atuais").

Nota sobre a sonda de edge coverage: WEB-01 veio "unclassified" (unresolved) da
sonda automática — não há SPEC.md com `## Edge Coverage` nesta fase. Os
`must_haves.truths` acima foram derivados dos 6+1 call sites auditados em
`09-RESEARCH.md` (tabela "Auditoria completa dos 12 call sites") e da decisão
D-01/discretion do CONTEXT.md, não de uma seção SPEC formal.

Purpose: fechar 7 dos 12 call sites de WEB-01 sem tocar em `ActiveSessionScreen.tsx`
(já fechado na Plan 09-01) nem em `JointLobbyScreen.tsx`/`PostQuestionnaireChat.tsx`
(Plan 09-03, em paralelo).
Output: `src/screens/QuestionnaireScreen.tsx` e `src/screens/SignUpScreen.tsx`
migrados; nenhum teste novo (nenhuma suíte dedicada a `SignUpScreen` existe hoje;
`questionnaireDiasESessao.test.tsx` fica intocado como prova de regressão).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-CONTEXT.md
@.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-RESEARCH.md
@.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar os 6 call sites de QuestionnaireScreen.tsx</name>
  <files>src/screens/QuestionnaireScreen.tsx</files>
  <read_first>
    src/screens/QuestionnaireScreen.tsx (linhas 1-30 imports, 160-180 `handleSessionExpiration`, 419-517 `handleSubmit`);
    src/utils/alertShim.ts (criado na Plan 09-01 — assinatura de `showAlert`);
    __tests__/questionnaireDiasESessao.test.tsx (linhas 1-50 mocks, 168-214 — espiona `Alert.alert` diretamente; este arquivo de teste NÃO deve ser modificado).
  </read_first>
  <action>
    Trocar `import { ..., Alert, ... } from 'react-native';` removendo `Alert`, adicionar `import { showAlert } from '../utils/alertShim';`. Migrar os 6 call sites, texto idêntico preservado (trocar só `Alert.alert(` por `showAlert(`, argumentos inalterados): `handleSessionExpiration` — `('Sessão Expirada', 'Sua sessão expirou. Por favor, faça login novamente.', [{ text: 'OK', onPress: async () =&gt; {...} }])`; `handleSubmit` — `('Erro Interno', 'Funcionalidade indisponível. Tente novamente mais tarde.')`; `('Erro', 'Usuário não autenticado. Faça login novamente.')`; `('Erro Interno', 'Não foi possível determinar o armazenamento local.')`; `('Campos Incompletos', 'Verifique se todos os campos obrigatórios foram preenchidos corretamente, incluindo data de nascimento, peso e altura válidos.')`; e no catch de submissão — `` ('Erro ao Salvar', `Não foi possível salvar seus dados. Detalhes: ${errorMessage}`) ``.

    NÃO alterar `__tests__/questionnaireDiasESessao.test.tsx` — o teste espia `Alert.alert` diretamente e continua passando porque `showAlert` repassa para `Alert.alert` real no ambiente nativo padrão do jest deste repo (D-03; `Platform.OS` não é `'web'` no `testEnvironment` `react-native/jest/react-native-env.js` usado pelo preset `jest-expo` flat).
  </action>
  <verify>
    <automated>npx jest __tests__/questionnaireDiasESessao.test.tsx __tests__/questionnaireScreen.test.tsx __tests__/doseCardioQuestionario.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "Alert\." src/screens/QuestionnaireScreen.tsx` não retorna nenhuma linha;
    `npx jest __tests__/questionnaireDiasESessao.test.tsx` passa SEM nenhuma modificação no arquivo de teste (confirma D-03 preservado — as asserções em `Alert.alert` nas linhas 186/203/212 continuam válidas);
    `npx jest __tests__/questionnaireScreen.test.tsx __tests__/doseCardioQuestionario.test.tsx` passam sem regressão;
    `npx tsc --noEmit` sem erros novos.
  </acceptance_criteria>
  <done>QuestionnaireScreen.tsx sem nenhum Alert.alert/import de Alert; questionnaireDiasESessao.test.tsx passa sem nenhuma modificação, provando D-03.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar o call site de SignUpScreen.tsx</name>
  <files>src/screens/SignUpScreen.tsx</files>
  <read_first>
    src/screens/SignUpScreen.tsx (arquivo inteiro — call site nas linhas 47-51, dentro de `handleSignUp`);
    src/utils/alertShim.ts.
  </read_first>
  <action>
    Trocar `import { Alert, Pressable, StyleSheet, Text, View } from 'react-native';` removendo `Alert`, adicionar `import { showAlert } from '../utils/alertShim';`. Migrar o call site: `Alert.alert('Cadastro realizado!', 'Um email de confirmação foi enviado. Por favor, verifique sua caixa de entrada.', [{ text: 'OK', onPress: () =&gt; navigation.navigate('Login') }])` -&gt; `showAlert(...)` com os mesmos 3 argumentos, incluindo o `onPress` de navegação.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "Alert\." src/screens/SignUpScreen.tsx` não retorna nenhuma linha;
    `npx tsc --noEmit` sem erros novos (não existe suíte de teste dedicada para `SignUpScreen` neste repo — confirmado por busca em `__tests__/`, nenhuma ação de teste necessária além do type-check).
  </acceptance_criteria>
  <done>SignUpScreen.tsx sem nenhum Alert.alert/import de Alert; tsc limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-only (browser/PWA) | Migração de mecanismo de diálogo, sem I/O novo — mesma superfície de `Alert.alert` já existente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Tampering | QuestionnaireScreen.tsx / SignUpScreen.tsx | low | accept | Conteúdo dos diálogos vem de strings hardcoded (título/mensagem já existentes no código, alguns com `errorMessage` interpolado a partir de erro já classificado pela camada de serviço, nunca HTML/markup renderizável). |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | Nenhuma dependência nova nesta plan — só reaproveita `src/utils/alertShim.ts` já criado na Plan 09-01. |

</threat_model>

<verification>
`npx jest __tests__/questionnaireDiasESessao.test.tsx __tests__/questionnaireScreen.test.tsx __tests__/doseCardioQuestionario.test.tsx` passam sem modificação nos arquivos de teste; `npx tsc --noEmit` sem erros novos; `grep -n "Alert\." src/screens/QuestionnaireScreen.tsx src/screens/SignUpScreen.tsx` vazio.
</verification>

<success_criteria>
7 dos 12 call sites de WEB-01 migrados sem quebrar nenhum teste existente; comportamento nativo (D-03) provado automaticamente pelo teste que já espiona `Alert.alert`.
</success_criteria>

## Artifacts this phase produces

- `src/utils/alertShim.ts`, `src/store/alertStore.ts`, `src/components/AlertHost.tsx` — criados na Plan 09-01, consumidos aqui.
- Modificado: `src/screens/QuestionnaireScreen.tsx` (6 call sites migrados, import `Alert` removido).
- Modificado: `src/screens/SignUpScreen.tsx` (1 call site migrado, import `Alert` removido).
- Modificado (Plan 09-01): `App.tsx`, `src/screens/ActiveSessionScreen.tsx`.
- Modificado (Plan 09-03): `src/screens/JointLobbyScreen.tsx`, `src/screens/PostQuestionnaireChat.tsx`, `__tests__/jointLobbyScreen.test.tsx`.
- Novo (Plan 09-04): `__tests__/alertNoAlertRemanescente.test.ts`.

<output>
Create `.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-02-SUMMARY.md` when done
</output>
