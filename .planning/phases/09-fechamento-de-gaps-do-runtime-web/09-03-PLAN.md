---
phase: 09-fechamento-de-gaps-do-runtime-web
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/screens/JointLobbyScreen.tsx
  - src/screens/PostQuestionnaireChat.tsx
  - __tests__/jointLobbyScreen.test.tsx
autonomous: true
requirements: [WEB-01]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "grep -c 'Alert\\.' src/screens/JointLobbyScreen.tsx retorna 0 — o corpo de confirmarPadrao migrou para showAlert preservando a assinatura do prop confirmar."
    - "src/screens/PostQuestionnaireChat.tsx não importa mais Alert (import morto removido, nenhuma chamada existia no arquivo)."
    - "Um teste novo em __tests__/jointLobbyScreen.test.tsx exercita confirmarPadrao SEM mock de confirmar injetado e confirma que showAlert é chamado com o botão destructive 'Encerrar' — fecha a lacuna de teste identificada em 09-RESEARCH.md (Wave 0 Gaps)."
  artifacts:
    - src/screens/JointLobbyScreen.tsx (modificado)
    - src/screens/PostQuestionnaireChat.tsx (modificado)
    - __tests__/jointLobbyScreen.test.tsx (modificado)
  key_links:
    - "JointLobbyScreen.tsx (confirmarPadrao) -> src/utils/alertShim.ts (criado na Plan 09-01)"
    - "JointLobbyView prop `confirmar` (default confirmarPadrao) -> encerrar() -> sair()/beforeRemove listener"
  prohibitions:
    - statement: "A assinatura do prop confirmar (titulo, mensagem, onSim) não muda — só o corpo default de confirmarPadrao"
      status: flagged-unverified
      verification: "__tests__/jointLobbyScreen.test.tsx linha ~67/75 (injeção de mock de confirmar) continua passando sem alteração"
    - statement: "PostQuestionnaireChat.tsx não ganha nenhuma outra alteração além da remoção do import morto de Alert"
      status: flagged-unverified
      verification: "git diff src/screens/PostQuestionnaireChat.tsx mostra só a remoção da linha 'Alert,' no bloco de import"
---

<objective>
Migrar o último call site real de `Alert.alert` fora de `ActiveSessionScreen.tsx`
(o corpo de `confirmarPadrao` em `JointLobbyScreen.tsx`, um caso especial: é a
implementação default de um prop injetável, não uma chamada solta) e remover o
import morto de `Alert` em `PostQuestionnaireChat.tsx` (WEB-01, D-01 — mesma
assinatura de `Alert.alert`, só troca de mecanismo, texto preservado). Fecha uma
lacuna de teste explícita: hoje `__tests__/jointLobbyScreen.test.tsx` só injeta um
mock de `confirmar`, nenhum teste exercita o `confirmarPadrao` real.

Nota sobre a sonda de edge coverage: WEB-01 veio "unclassified" (unresolved) da
sonda automática — não há SPEC.md com `## Edge Coverage` nesta fase. Os
`must_haves.truths` acima vêm do call site #12 e do import morto auditados em
`09-RESEARCH.md` ("Auditoria completa dos 12 call sites") e da lacuna de teste
documentada em "Wave 0 Gaps"/"No Analog Found" de `09-PATTERNS.md`.

Purpose: fechar os 2 arquivos restantes de WEB-01 sem tocar em
`ActiveSessionScreen.tsx` (Plan 09-01) nem em `QuestionnaireScreen.tsx`/
`SignUpScreen.tsx` (Plan 09-02, em paralelo).
Output: `src/screens/JointLobbyScreen.tsx` e `src/screens/PostQuestionnaireChat.tsx`
migrados; `__tests__/jointLobbyScreen.test.tsx` com um caso novo cobrindo o
caminho real de `confirmarPadrao`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-CONTEXT.md
@.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-RESEARCH.md
@.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrar confirmarPadrao (JointLobbyScreen.tsx) + testar o caminho real sem mock injetado</name>
  <files>src/screens/JointLobbyScreen.tsx, __tests__/jointLobbyScreen.test.tsx</files>
  <read_first>
    src/screens/JointLobbyScreen.tsx (linhas 1-97 topo/tipos, 76-84 `confirmarPadrao`, 120-190 `encerrar`/`sair`/`hardwareBackPress`/`beforeRemove`, 220-265 render do `StackHeader` com `onBack={sair}` na linha ~261, 480-503 `export default JointLobbyScreen`);
    src/components/ui/Controls.tsx (componente `StackHeader` — localizar o testID/accessibilityLabel do botão de voltar renderizado por `onBack`);
    __tests__/jointLobbyScreen.test.tsx (arquivo inteiro — hoje só injeta `confirmar` mock nas linhas 67/75 de `renderizar()`; nenhum teste exercita `confirmarPadrao` real; `montarHook()` já usa `status: 'lobby'`, um dos estados "dupleViva" que dispara a confirmação);
    src/utils/alertShim.ts (criado na Plan 09-01 — assinatura de `showAlert`).
  </read_first>
  <behavior>
    - Renderizar `JointLobbyView` SEM passar a prop `confirmar` (usa o default `confirmarPadrao` real) e disparar a saída explícita (pressionar o botão de voltar do `StackHeader` renderizado com `onBack={sair}`) chama `showAlert` (mockado) com título `'Encerrar o treino conjunto?'`, mensagem `'Sair encerra o treino para você e para o seu parceiro.'` e um array de botões contendo `{ text: 'Ficar no treino', style: 'cancel' }` e `{ text: 'Encerrar', style: 'destructive', onPress: expect.any(Function) }`.
    - O teste existente que injeta um mock de `confirmar` (linhas 67/75, `renderizar()`) continua passando sem nenhuma alteração — a assinatura do prop não muda.
  </behavior>
  <action>
    Trocar `import { AccessibilityInfo, Alert, BackHandler, StyleSheet, View } from 'react-native';` removendo `Alert`, adicionar `import { showAlert } from '../utils/alertShim';`. Migrar o corpo de `confirmarPadrao`: `Alert.alert(titulo, mensagem, [{ text: 'Ficar no treino', style: 'cancel' }, { text: 'Encerrar', style: 'destructive', onPress: onSim }])` -&gt; `showAlert(titulo, mensagem, [...])` mesmos argumentos — a assinatura do prop `confirmar` (`(titulo: string, mensagem: string, onSim: () =&gt; void) =&gt; void`) usada por `JointLobbyView` e injetada pelo teste existente NÃO muda, só o corpo da função default.

    Em `__tests__/jointLobbyScreen.test.tsx`, adicionar `jest.mock('../src/utils/alertShim', () =&gt; ({ showAlert: jest.fn() }));` no topo do arquivo (mesma seção dos demais `jest.mock(...)` de módulo, linhas 11-25) e `import { showAlert } from '../src/utils/alertShim';`. Adicionar um novo `describe` que chama `renderizar(montarHook())` SEM passar `confirmar` na chamada (usa o default real `confirmarPadrao` de `JointLobbyView`) e dispara a saída explícita através do botão de voltar renderizado pelo `StackHeader` com `onBack={sair}` (localizar o seletor exato — `testID`/`accessibilityLabel` — inspecionando `src/components/ui/Controls.tsx`); afirmar que `showAlert` foi chamado com `('Encerrar o treino conjunto?', 'Sair encerra o treino para você e para o seu parceiro.', expect.arrayContaining([expect.objectContaining({ text: 'Encerrar', style: 'destructive' })]))`.
  </action>
  <verify>
    <automated>npx jest __tests__/jointLobbyScreen.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "Alert\." src/screens/JointLobbyScreen.tsx` não retorna nenhuma linha;
    `npx jest __tests__/jointLobbyScreen.test.tsx` passa, incluindo o novo caso sem `confirmar` injetado e o caso existente com `confirmar` injetado (linha ~67/75, sem alteração);
    `npx tsc --noEmit` sem erros novos.
  </acceptance_criteria>
  <done>JointLobbyScreen.tsx sem nenhum Alert.alert/import de Alert; teste novo prova que confirmarPadrao real chama showAlert com o botão destructive "Encerrar".</done>
</task>

<task type="auto">
  <name>Task 2: Remover o import morto de Alert em PostQuestionnaireChat.tsx</name>
  <files>src/screens/PostQuestionnaireChat.tsx</files>
  <read_first>
    src/screens/PostQuestionnaireChat.tsx (linhas 1-20 — bloco de import de `react-native`, `Alert,` na linha 11).
  </read_first>
  <action>
    Remover a linha `Alert,` do bloco de import `react-native` — nenhuma chamada `Alert.alert`/`Alert.` existe neste arquivo (confirmado: `grep -n "Alert" src/screens/PostQuestionnaireChat.tsx` retorna só a linha do import antes desta mudança). Nenhuma outra alteração no arquivo.
  </action>
  <verify>
    <automated>npx jest __tests__/postQuestionnaireChatInit.test.tsx __tests__/postQuestionnaireChatSkip.test.tsx __tests__/postQuestionnaireChatUnavailable.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "Alert" src/screens/PostQuestionnaireChat.tsx` não retorna nenhuma linha;
    `npx jest __tests__/postQuestionnaireChatInit.test.tsx __tests__/postQuestionnaireChatSkip.test.tsx __tests__/postQuestionnaireChatUnavailable.test.tsx` passam sem regressão;
    `npx tsc --noEmit` sem erros novos.
  </acceptance_criteria>
  <done>PostQuestionnaireChat.tsx sem import de Alert; testes existentes do arquivo passam sem regressão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-only (browser/PWA) | Migração de mecanismo de diálogo destrutivo (encerrar dupla), sem I/O novo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Tampering | JointLobbyScreen.tsx (confirmarPadrao, ação destrutiva) | low | accept | Título/mensagem hardcoded; o botão `destructive` ('Encerrar') só dispara `onSim` (que chama `j.sair()`, já protegido por confirmação explícita do usuário) — a migração preserva exatamente a mesma barreira de confirmação que já existia com `Alert.alert`, não remove nem enfraquece. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | Nenhuma dependência nova nesta plan — só reaproveita `src/utils/alertShim.ts` já criado na Plan 09-01. |

</threat_model>

<verification>
`npx jest __tests__/jointLobbyScreen.test.tsx __tests__/postQuestionnaireChatInit.test.tsx __tests__/postQuestionnaireChatSkip.test.tsx __tests__/postQuestionnaireChatUnavailable.test.tsx` passam; `npx tsc --noEmit` sem erros novos; `grep -n "Alert\." src/screens/JointLobbyScreen.tsx` e `grep -n "Alert" src/screens/PostQuestionnaireChat.tsx` vazios.
</verification>

<success_criteria>
Os 2 arquivos restantes de WEB-01 (fora de ActiveSessionScreen/QuestionnaireScreen/SignUpScreen) fechados; lacuna de teste do `confirmarPadrao` real preenchida.
</success_criteria>

## Artifacts this phase produces

- `src/utils/alertShim.ts`, `src/store/alertStore.ts`, `src/components/AlertHost.tsx` — criados na Plan 09-01, consumidos aqui.
- Modificado: `src/screens/JointLobbyScreen.tsx` (`confirmarPadrao` migrado, import `Alert` removido).
- Modificado: `src/screens/PostQuestionnaireChat.tsx` (import morto `Alert` removido).
- Modificado: `__tests__/jointLobbyScreen.test.tsx` (novo describe cobrindo `confirmarPadrao` real).
- Modificado (Plan 09-01): `App.tsx`, `src/screens/ActiveSessionScreen.tsx`.
- Modificado (Plan 09-02): `src/screens/QuestionnaireScreen.tsx`, `src/screens/SignUpScreen.tsx`.
- Novo (Plan 09-04): `__tests__/alertNoAlertRemanescente.test.ts`.

<output>
Create `.planning/phases/09-fechamento-de-gaps-do-runtime-web/09-03-SUMMARY.md` when done
</output>
